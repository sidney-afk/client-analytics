name: Client entry visible boot

on:
  pull_request:
    paths:
      - 'index.html'
      - 'package.json'
      - 'qa/boot/**'
      - 'qa/client-continuity*.js'
      - 'scripts/client-continuity*.js'
      - 'test/helpers/client-continuity-source.js'
      - 'test/samples-authoritative-read.js'
      - 'supabase/functions/client-token-verify/index.ts'
      - 'test/client-entry-preflight.js'
      - '.github/workflows/client-entry-visible-boot.yml'
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'package.json'
      - 'qa/boot/**'
      - 'qa/client-continuity*.js'
      - 'scripts/client-continuity*.js'
      - 'test/helpers/client-continuity-source.js'
      - 'test/samples-authoritative-read.js'
      - 'supabase/functions/client-token-verify/index.ts'
      - 'test/client-entry-preflight.js'
      - '.github/workflows/client-entry-visible-boot.yml'
  workflow_dispatch:

concurrency:
  group: client-entry-visible-boot-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  visible-boot:
    name: Streamed visible boot (one attempt)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Resolve Playwright version
        id: playwright-version
        run: node -e "require('fs').appendFileSync(process.env.GITHUB_OUTPUT, 'version=' + require('./package.json').devDependencies.playwright + '\n')"
      - name: Install browser test dependencies
        run: npm install --no-package-lock
      - name: Cache Playwright Chromium
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-${{ runner.arch }}-playwright-${{ steps.playwright-version.outputs.version }}
      - name: Install Chromium and system dependencies
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium
      - name: Verify cached Chromium
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install chromium
      - name: Drive streamed first paint, reload, and BFCache once
        run: npm run test:boot
      - name: Prove repaired source and original false-empty detection offline
        run: node test/client-continuity-monitor.js --strict-source
      - name: Observe anonymous continuity with entirely intercepted fixtures
        run: node qa/client-continuity.js
      - name: Exercise inactive launcher with fictional read-only transport
        run: node scripts/client-continuity-run.js --fixture
