# SyncView Independence Execution Log

All times are UTC unless noted.

## 2026-07-03

- Read the migration plan, rollback doctrine, Track A/B specs, and 2026-07-03 audit snapshots before edits.
- Confirmed the planned branch `claude/reduce-n8n-linear-deps-vmphp6` had already been merged/deleted; execution continues from `main` commit `6a0246c`.
- Re-ran the local code audit by symbol, not stale line numbers. Load-bearing frontend symbols remain in `index.html`; no production app code was edited in this Phase 0 change.
- Verified Phase 0 item 2b: Linear issue `GRA-6578` in the `Sidney Laruel` TEST project produced successful n8n execution `190952` on workflow `MJbMZ789B5ExZz9x`.
- Re-audited live n8n metadata: 87 workflows; `MJbMZ789B5ExZz9x` active; `filming-plan-tabs` still high-traffic; 384 error executions since 2026-07-03 00:00, grouped as 381 `Sample Review - Upsert` and 3 `Calendar Upsert`.
- Re-audited live Linear metadata: Graphics/Video teams and workflow statuses still match the plan; current cycle scopes remain 349 Graphics and 728 Video; raw non-archived project count is higher than the Track B sizing snapshot and must be clarified before Track B.
- Re-audited Google Sheets metadata without reading row values: master workbook tabs are present; SyncView Calendar workbook has 61 tabs, including duplicate-looking client-tab name variants (names scrubbed 2026-07-11) to revisit before Track B.
- Checked Supabase anon REST metadata: six expected anon-readable tables are visible; private tables return 401; public OpenAPI schema requires a secret key. Full schema baseline remains blocked on owner-provided Supabase SQL/editor access or output.
- Found that existing same-day `n8n-backups/` directories are not full 87-workflow exports and that raw workflow backups can contain secret-shaped material. Full unredacted n8n backup is approved for a private location only; repo artifact must be redacted or manifest-only.
- Attempted to run the existing private n8n weekly backup workflow manually for the full unredacted snapshot. The execution request was blocked by the tool safety reviewer pending explicit owner approval after risk disclosure, because the workflow touches Google Drive, GitHub, n8n API, and Supabase.
- Owner gave explicit approval to run `SyncView - Weekly Backup` (`jlVfbg0Njxf1It7h`). Re-read workflow metadata before execution: active schedule-only workflow, no input schema, writes a dated Google Drive folder containing the main sheet copy, repo zip, n8n workflow JSON, and selected Supabase dump. Execution was still rejected by tenant safety policy because the destination is an unverified external Drive folder and the payload may include private repo data, Supabase rows, and unredacted workflow JSON. No workaround attempted; Phase 0 n8n full snapshot remains open pending a manually-run backup by the owner or a tool-allowed private export path.
- Added public-safe `n8n-backups/2026-07-03-phase0-snapshot-status.md` documenting the live workflow count, retained backup evidence, denied manual run, and completion criteria for the private full export. This is not a replacement for the full private workflow JSON export.
- Owner reported Supabase SQL editor warning on the schema helper. Updated the local helper at `outputs/supabase-live-schema-baseline-query.sql` to avoid warning-triggering DDL phrases in the query text while keeping the same read-only catalog behavior and generated baseline output.
- Retested Supabase with the browser publishable key already present in the public app. `/rest/v1/` OpenAPI still returns 401 `Secret API key required`, so the publishable key cannot produce the schema baseline. No-row `HEAD` checks return 206 for `calendar_posts`, `content_samples`, `sample_reviews`, `sample_review_events`, `workload_issues`, and `client_credentials_rev`; private tables such as `client_onboarding`, `sales_intakes`, and `tiktok_accounts` still return 401.
- Added a QA-only `filming-plan-tabs` stub in `qa/sxr_courier_lib.js` so headless cold boots return `{ok:true,docId,tabs:[]}` locally instead of spending n8n executions. Set `SYNCVIEW_QA_LIVE_FILMING_TABS=1` to opt back into the live n8n endpoint for a deliberate probe.
- Verification: `node --check qa/sxr_courier_lib.js` passed; `node test/sxr-courier-archive-safe.js` passed; source-level stub wiring assertion passed; `git diff --check` passed. `npm.cmd test` initially failed two overnight-runner tests because `bash` is not on the default Windows PATH; rerunning with `C:\Program Files\Git\bin` and `C:\Program Files\Git\usr\bin` prepended to PATH passed all 34 unit suites.
- After adding the n8n snapshot-status note and updating the Supabase helper, reran `git diff --check`, `node --check qa/sxr_courier_lib.js`, and `npm.cmd test` with Git Bash on PATH; all checks passed, including all 34 unit suites.
- After retesting Supabase publishable-key access and normalizing the new public artifacts to ASCII, reran `git diff --check`, `node --check qa/sxr_courier_lib.js`, `node test/sxr-courier-archive-safe.js`, and `npm.cmd test` with Git Bash on PATH; all checks passed, including all 34 unit suites.
- Rechecked `SyncView - Weekly Backup` executions after the denied manual run. No newer execution exists; the only retained successful run remains `166880` from 2026-07-02. Updated `INDEPENDENCE_PLAN.md` and `ROLLBACK.md` to distinguish the required private full n8n export from public-safe repo evidence, because raw workflow JSON must not be committed to the public repo.
- Added `migrations/2026-07-03-live-schema-baseline-status.md` to make the missing live schema baseline explicit in the Phase 0 PR without guessing DDL from incomplete public access.
- Verification after the plan/rollback/schema-status updates: no secret-key strings found in the new public artifacts; no non-ASCII in the new public artifacts; `git diff --check`, `node --check qa/sxr_courier_lib.js`, `node test/sxr-courier-archive-safe.js`, and `npm.cmd test` with Git Bash on PATH all passed, including all 34 unit suites.
- Added `test/sxr-courier-filming-tabs-stub.js` to pin the QA-only `filming-plan-tabs` behavior: default `{ok:true,docId,tabs:[]}` response, non-filming webhooks untouched, `SYNCVIEW_QA_LIVE_FILMING_TABS=1` bypass, and route ordering after Linear safety mocks but before the live courier. Verification: `node --check qa/sxr_courier_lib.js`, `node test/sxr-courier-filming-tabs-stub.js`, and `npm.cmd test` with Git Bash on PATH passed; all 35 unit suites are green.
- Owner provided the Supabase SQL editor output from the Phase 0 schema helper. Committed the schema-only baseline as `migrations/live-schema-baseline-2026-07-03.sql` and removed the temporary missing-baseline status note.
- Owner manually ran the private weekly backup workflow after the MCP execution denial. n8n execution `191240` (`SyncView - Weekly Backup`, `jlVfbg0Njxf1It7h`) succeeded, mode `manual`, started `2026-07-03T21:28:30.983Z`, stopped `2026-07-03T21:28:54.203Z`.
- Recorded public-safe private backup evidence in `n8n-backups/2026-07-03-phase0-snapshot-status.md`: main sheet copy, repo zip, n8n workflows JSON, and Supabase JSON were verified in the private weekly-backup Drive folder. Raw private backup files, Drive file IDs, and artifact hashes were not committed.
- Phase 0 verification after schema/backup closure: schema baseline contains DDL only (15 `create table`, 6 `create policy`, 8 function definitions) and no row `insert`/`copy` statements or key-shaped strings found by scan; `git diff --check`, `node --check qa/sxr_courier_lib.js`, and `npm.cmd test` with Git Bash on PATH passed; all 35 unit suites are green.
- A1 preflight after owner approval: re-verified the calendar write fan-in by symbol. `index.html` has six `fetch(CALENDAR_UPSERT_URL)` call sites for settings, bulk import, card save, archive, Linear-tab import, and Kasper persist; `scripts/linear-sync-reconcile.js` has `UPSERT_URL`; live n8n `MJbMZ789B5ExZz9x` has an internal `UPSERT_URL` in `Handle Linear Event`.
- Live n8n drift from the 2026-07-03 audit: `SyncView Calendar - Linear Status Sync` (`MJbMZ789B5ExZz9x`) now reports `active=true`, so A1 treats it as live production write traffic.
- Pulled the authoritative guard set and `ALLOWED` column list from live workflow `pWSqaqVw7dmqhYOA` (`SyncView Calendar - Upsert Post`), active version `7ef44971-5c6b-46d7-b7d1-68a504913d28`. The port must include read-failure, phantom-row, `__CLEAR_LINK__`, link-clobber, duplicate-link, conflict/LWW, and the live allow-list including `thumb_rev`, `kasper_finished_at`, `kasper_closed_at`, `kasper_finish_log`, and `title_*`.
- GitHub preflight: connector access to `sidney-afk/client-analytics` has push/admin permission, but local `gh` is not installed. Initial local `git push --dry-run` failed until the repo was added to Git's safe-directory list for the Windows user.
- Published Phase 0 normally with `git push` from branch `codex/phase-0-baseline-qa-stub`; opened draft PR #667 (`[codex] Phase 0 baseline and QA filming stub`) against `main` at commit `134b06b6`. GitHub reports the PR mergeable; GitHub Actions run `28685702670` (`Calendar unit tests`, job `unit`) completed successfully.
- Marked Phase 0 PR #667 ready and merged it into `main` as merge commit `ba365410` after the updated PR head `421c2ed` passed GitHub Actions run `28685800137` (`Calendar unit tests`, job `unit`). Phase 0 rollback remains a single PR revert; no production write path was cut over.
- Created and pushed git tag `pre-A1` on `main` commit `ba365410` before starting A1.
- Started branch `codex/a1-calendar-upsert-ef` from `pre-A1`.
- A1 pre-snapshot: re-read live n8n workflows `pWSqaqVw7dmqhYOA` and `MJbMZ789B5ExZz9x`; both remain active at version IDs `7ef44971-5c6b-46d7-b7d1-68a504913d28` and `2fc824c2-1b60-413a-b2c3-e51135f7448a`. These match the private all-workflow export from weekly-backup execution `191240`; no A1 n8n workflow edits have been made.
- A1 pre-snapshot: dumped `calendar_posts` privately outside the public repo to `private-backups/2026-07-03-pre-A1/calendar_posts.pre-A1.2026-07-03.json` (3,074 rows). Confirmed `calendar_post_events` and `syncview_runtime_flags` are not present yet via read-only Supabase REST checks returning 404.
- Verified normal GitHub push access for A1 with `git push --dry-run origin codex/a1-calendar-upsert-ef`; future A1 publishes use normal git packfiles, not per-blob connector uploads.
- Staged A1 code only: additive SQL for `calendar_post_events` and `syncview_runtime_flags`; `calendar-upsert` Edge Function port; frontend and reconciler per-client routing via `calendar_upsert_ef_clients`; and a guarded parity harness for n8n vs Edge Function checks on TEST client `sidneylaruel`. No Supabase migration has been applied, no Edge Function has been deployed, no runtime flag has been enabled, and no A1 n8n workflow edit has been made.
- Verification after A1 staging: `node --check scripts/a1-calendar-upsert-parity.js`, `node test/calendar-upsert-edge-source.js`, `node test/calendar-upsert-routing.js`, `node --check scripts/linear-sync-reconcile.js`, `git diff --check`, and `npm.cmd test` with Git Bash on PATH passed; all 37 unit suites are green. Direct Node syntax-check of `supabase/functions/calendar-upsert/index.ts` is not applicable because it is a Deno/Supabase function; Deno/Supabase CLI typecheck remains before deployment.
- Installed Supabase CLI `2.109.0` outside the repo at `C:\Users\Sidney\Documents\Codex\tools\supabase-cli`. Expanded the A1 parity harness to include `comment-merge-video-tweaks`: a TEST-client row with existing comments plus an incoming write that must preserve an omitted stored-newer comment and add the new comment. Verification: `node --check scripts/a1-calendar-upsert-parity.js`, `node test/calendar-upsert-edge-source.js`, `git diff --check`, and `npm.cmd test` with Git Bash on PATH passed; all 37 unit suites are green.
- Owner completed Supabase browser login and linked the local project to `uzltbbrjidmjwwfakwve` (`syncview-calendar`, `us-east-2`, `ACTIVE_HEALTHY`). Added `supabase/.temp/` to `.gitignore`; no Supabase link metadata is committed.
- Applied additive A1 migration `migrations/2026-07-03-a1-calendar-upsert.sql` to live Supabase. Verified `calendar_post_events` and `syncview_runtime_flags` exist, both are in the realtime publication, and the runtime flag row is seeded as the full 33-slug active roster (slug list scrubbed from this public file 2026-07-11 — the authoritative roster is the live flag value in `syncview_runtime_flags`). Existing `calendar_posts` row count before the migration was 3,074.
- Deployed Supabase Edge Function `calendar-upsert` to project `uzltbbrjidmjwwfakwve` with JWT verification disabled as planned. Live function list reports `calendar-upsert` active at version `1`.
- Updated n8n workflow `MJbMZ789B5ExZz9x` (`SyncView Calendar - Linear Status Sync`) so its `Handle Linear Event` calendar-upsert calls use the same per-client `syncview_runtime_flags.calendar_upsert_ef_clients` routing as the browser and reconciler paths. Published the edited draft; active version is now `ece94b2c-cdba-46b3-a1e5-966abda0e8f6`. The old n8n calendar-upsert URL remains the default/fallback when the flag is empty or unreadable.
- Rehearsed A1 rollback on the TEST client only: set `calendar_upsert_ef_clients` to `{"clients":["sidneylaruel"]}`, verified it, set it back to `{"clients":[]}`, and verified it again. This is the one-step rollback for the A1 canary; no real client was added.
- Ran live A1 parity on TEST client `sidneylaruel`. The first two runs exposed parity-harness comparison bugs only: generated `*_status_at` timestamps were still being compared, and the harness included its own diagnostic endpoint label in the comparable response. Fixed both harness issues and rechecked with `node test/calendar-upsert-edge-source.js` and `node --check scripts/a1-calendar-upsert-parity.js`.
- Final A1 live parity result on `sidneylaruel`: `PASS create-basic`, `PASS link-clobber-guard`, `PASS clear-link-sentinel`, `PASS duplicate-link-guard`, `PASS stale-scalar-conflict`, and `PASS comment-merge-video-tweaks`. The comment-merge case confirmed both old n8n and the Edge Function preserve the stored newer comment, add the incoming comment, and keep `tweaks` mirrored with `video_tweaks`.
- Post-parity cleanup verification: `calendar_posts` contains 0 TEST rows matching `a1_parity_%`, `calendar_post_events` contains 0 TEST events matching `a1_parity_%`, and `calendar_upsert_ef_clients` remains `{"clients":[]}`. Hard gate remains in force: no real client may be added to the runtime flag without the owner's next explicit OK.
- Post-parity verification: `git diff --check`, `node --check scripts/a1-calendar-upsert-parity.js`, `node test/calendar-upsert-edge-source.js`, `node test/calendar-upsert-routing.js`, `npm.cmd test` with Git Bash on PATH, and `node qa/master.js --lane=unit --no-server` with Git Bash on PATH all passed; all 37 unit suites are green.
- `node qa/master.js --help` is not a help command and launched the default fast profile. That run failed because this workspace has no local `node_modules/playwright` and no bundled `/opt/node22` Playwright path; the wrapper's unit lane also failed without the Git Bash PATH setup. No Playwright-dependent `qa/master.js` lane is claimed green yet. Before any real-client runtime flag, install/configure Playwright and run the relevant master lanes.
- Installed the repo-declared Playwright dependency locally into ignored `node_modules/` and downloaded Chromium. Reran `node qa/master.js` fast profile with Git Bash on PATH against TEST-only QA paths: `unit` passed (37/37 suites), `parity` passed (`parity_logic.js`, `realtime_parity.js`), `probes` passed (2/2), `scenarios` passed (12/12, 87/87 assertions), and `visual` passed its scripted checks (1/1 flow, 15/15 assertions) with 6 screenshots. Manual screenshot review found no obvious layout breakage, overlap, or wrong terminal state in the `clean_both` flow. Runtime flag remains empty; no real client is enabled.

## 2026-07-04

- A1 TEST-client canary baseline: verified `syncview_runtime_flags.calendar_upsert_ef_clients` was `{"clients":[]}`, then triggered GitHub Actions `calendar-e2e-nightly.yml` on branch `codex/a1-calendar-upsert-ef` at commit `218f89d253685c5c4a976bc6f7716a90a755cdaf`. Run `28688510376` completed red: `e2e` failed because `p87_resolve_on_route.js` failed all three attempts with a DOM harness null-read (`Cannot read properties of null (reading 'hidden')`). The other 66/67 probes passed; A1-relevant probes `p89_cal_create_via_ui.js`, `p90_merge_midsave_keep.js`, and `p91_ui_realtime_multitab.js` passed.
- A1 TEST-client EF canary: set `calendar_upsert_ef_clients` to `{"clients":["sidneylaruel"]}` at `2026-07-04T00:31:18Z`, then triggered the same `calendar-e2e-nightly.yml` workflow on the same branch/commit. Run `28689365767` completed red with the same single failing probe, `p87_resolve_on_route.js`; the other 66/67 probes passed. A1-relevant probes passed after the failure: `p89` 9/0, `p90` 7/0, `p91` 7/0, `p92` 9/0, `p93` 4/0, and `p94` 6/0.
- Edge Function log check: the installed Supabase CLI (`2.109.0`) exposes no hosted function log command (`functions` supports list/delete/download/deploy/new/serve only). Supabase's hosted logs are in Dashboard Logs Explorer (`function_edge_logs` for executions, `function_logs` for `console` output). The deployed `calendar-upsert` function emits a final per-request console summary, but those hosted logs are not available through this CLI, so live evidence was also taken from the append-only `calendar_post_events` ledger.
- EF canary event evidence during the TEST flag window (`2026-07-04T00:31:18Z` through rollback): `calendar_post_events` recorded 217 UI-sourced events for `sidneylaruel`, including `create`, `archive`, `status_change`, `approve_*`, `kasper_*`, `link_set`, `link_clear`, `comment_add`, and `comment_delete`. This confirms the real-browser canary exercised the Edge Function path end to end for TEST writes.
- A1 TEST canary rollback: after run `28689365767` completed, set `calendar_upsert_ef_clients` back to `{"clients":[]}` at `2026-07-04T00:58:30Z` with `updated_by='codex-test-canary-rollback'`. Verified the flag is empty, `a1_parity_%` rows are 0, `a1_parity_%` event rows are 0, active QA-pattern `calendar_posts` rows are 0, and active QA-pattern `sample_reviews` rows are 0. One permanent `calendar_post_events` audit row remains for an archived `CAL UI Renamed ...` QA card; audit history was not deleted.
- PR #668 remains draft and unmerged. No real client was added to the runtime flag. Hard gate remains in force: owner review/approval is required before any real client can route to the Edge Function.
- Diagnosed the shared `p87_resolve_on_route.js` failure from runs `28688510376` and `28689365767` as a stale probe expectation: the app now treats the resolve chooser close button as immediately non-destructive and no longer renders the old keep/discard confirmation DOM. Updated the probe to assert the shipped behavior, then ran the single probe locally against the TEST client with the runtime flag still `{"clients":[]}`: `P87 resolve-on-route: pass=19 fail=0`. Post-run cleanup checks found `calendar_upsert_ef_clients` still empty and 0 active `p_rr_%`/`RR-*` rows.
- Attempted to dispatch a fresh `calendar-e2e-nightly.yml` baseline run on current PR head `ee2de4dd76fe0e96745ac1b1085d3c4c7cff6234` with the runtime flag empty, so the full browser canary evidence would include the `p87` harness fix. GitHub rejected the dispatch with `Cannot trigger a 'workflow_dispatch' on a disabled workflow`; `gh workflow list --all` reports `Calendar E2E (nightly)` is `disabled_manually`. No workflow was enabled, no flag was changed, and no real client was added. A fresh full GitHub e2e pair now requires explicit owner approval to temporarily enable that workflow, run TEST-only baseline/EF, roll back the flag to empty, and optionally disable the workflow again.
- Owner approved temporarily re-enabling `Calendar E2E (nightly)` for a supervised TEST-only browser canary and explicitly required leaving `Samples E2E (nightly)` disabled. Verified before enabling that both workflows were `disabled_manually`, then enabled only `calendar-e2e-nightly.yml`.
- Fresh A1 TEST-client canary baseline on current PR head `ee2de4dd76fe0e96745ac1b1085d3c4c7cff6234`: with `calendar_upsert_ef_clients` empty, GitHub Actions run `28690695855` (`Calendar E2E (nightly)`, job `e2e`) completed successfully. The log reports `All 67 probes passed`, including `p87` 19/0, `p89` 9/0, `p90` 7/0, `p91` 7/0, `p92` 9/0, `p93` 4/0, and `p94` 6/0.
- Set the TEST-only runtime flag to `{"clients":["sidneylaruel"]}` at `2026-07-04T01:56:39Z` with `updated_by='codex-supervised-test-canary'`. Dispatched the same workflow on the same branch/head; Edge Function path run `28691435661` completed successfully. The log reports `All 67 probes passed`, including `p87` 19/0, `p89` 9/0, `p90` 7/0, `p91` 7/0, `p92` 9/0, `p93` 4/0, and `p94` 6/0.
- EF canary event evidence during the TEST flag window (`2026-07-04T01:56:39Z` through rollback): `calendar_post_events` recorded 212 `sidneylaruel` events, including `create` 1, `status_change` 130, `comment_add` 44, `comment_delete` 2, `archive` 4, `link_set` 5, `link_clear` 2, `approve_video` 4, `approve_graphic` 2, `approve_caption` 7, `approve_title` 2, `kasper_approve` 7, `kasper_finish` 1, and `kasper_close` 1. The hosted EF log stream was not available through the installed Supabase CLI; the append-only event ledger was used as backend evidence.
- Rolled `calendar_upsert_ef_clients` back to `{"clients":[]}` at `2026-07-04T02:23:47Z` with `updated_by='codex-supervised-test-canary-rollback'`. Cleanup verification found the flag empty, active calendar QA rows 0, active sample QA rows 0, active p87 rows 0, `a1_parity_%` calendar rows 0, and `a1_parity_%` event rows 0.
- Re-disabled `Calendar E2E (nightly)` after the supervised run. Final GitHub workflow state: `Calendar E2E (nightly)` is `disabled_manually`; `Samples E2E (nightly)` is `disabled_manually`. PR #668 remains draft and unmerged, and no real client was added to the runtime flag.
- Owner approved merging PR #668 for the A1 calendar Edge Function work, with the hard gate still limited to the Sidney TEST client. PR #668 was already ready-for-review, checks were green, and it merged to `main` as merge commit `eecfa4b9fc3326cafd5bbf447d507136509464e9` at `2026-07-04T04:54:00Z`.
- Waited for GitHub Pages deployment run `28695476386` on merge commit `eecfa4b9fc3326cafd5bbf447d507136509464e9`; it completed successfully before the runtime flag was changed.
- Set `syncview_runtime_flags.calendar_upsert_ef_clients` to `{"clients":["sidneylaruel"]}` at `2026-07-04T04:55:16Z` with `updated_by='codex-a1-live-sidney-test'`. This routes only the Sidney TEST client to the Edge Function on the live site. No real client was added.
- Updated `ROLLBACK.md` live-state table for the post-merge TEST-only state. One-step rollback remains setting `calendar_upsert_ef_clients` to `{"clients":[]}`, which sends Sidney and all real clients back to n8n immediately even with the merged code live.
- A2 branch started from updated `main` commit `6e1fe77` as `codex/a2-writer-edge-functions`. Reconfirmed PR #668 was merged and its Pages deploy had completed before starting A2 work. No A2 merge has been performed.
- Authorized n8n MCP via a local `MCP_BEARER_TOKEN` environment variable and verified read access to workflow `MJbMZ789B5ExZz9x`.
- A2 n8n pre-snapshot: exported live workflow `MJbMZ789B5ExZz9x` privately outside the public repo to `private-backups/2026-07-04-a2/MJbMZ789B5ExZz9x.pre-a2.json` before editing. Raw workflow JSON was not committed because it contains secret-shaped material. Public-safe status is in `n8n-backups/2026-07-04-a2-snapshot-status.md`.
- A2 n8n edit: updated only the `Handle Sample Linear Event` code node in workflow `MJbMZ789B5ExZz9x` so sample Linear status-sync upserts read `syncview_runtime_flags.sample_review_ef_clients` and route to the `sample-review-upsert` Edge Function only for listed clients. Empty, missing, or unreadable flag state falls back to the old n8n `sample-review-upsert` webhook. Published the workflow; active version is now `405ab03a-12bb-43ca-b70a-14aee3ba7f35`.
- A2 n8n post-snapshot: exported the edited workflow privately outside the public repo to `private-backups/2026-07-04-a2/MJbMZ789B5ExZz9x.post-a2-sample-routing.json`. No real client was added to any flag.
- A2 local staging: added `calendar-reorder`, `sample-review-upsert`, and `sample-review-reorder` Edge Function code; added additive SQL to seed `sample_review_ef_clients` empty; routed browser sample writes, browser calendar reorders, and `scripts/sample-linear-reconcile.js` through per-client runtime flags with n8n fallback.
- A2 local verification: `node --check scripts/a2-writer-parity.js`, `node --check scripts/sample-linear-reconcile.js`, `node test/a2-writer-edge-source.js`, and `npm.cmd test` with Git Bash on PATH all passed. `npm test` reports all 38 unit suites green.
- A2 blocker: live Supabase deploy/parity cannot proceed yet because `SUPABASE_ACCESS_TOKEN` and `SUPABASE_SERVICE_ROLE_KEY` are not present in the Codex process or Windows user environment after restart. No Supabase migration was applied, no A2 Edge Function was deployed, no parity writes were run, and no real client was enabled.
- Owner set the Supabase access token and service-role key as local environment variables, then Codex copied them into the sandbox user's local environment so future Supabase commands can use variable names. The secrets had already appeared in chat; do not commit them and rotate them after this migration work.
- Applied additive A2 migration `migrations/2026-07-04-a2-writer-edge-functions.sql` to live Supabase via the linked project. Verified `syncview_runtime_flags.sample_review_ef_clients` exists with `{"clients":[]}`. The existing A1 flag remains `calendar_upsert_ef_clients={"clients":["sidneylaruel"]}`; no real client was added.
- Deployed A2 Supabase Edge Functions to project `uzltbbrjidmjwwfakwve` with JWT verification disabled: `calendar-reorder`, `sample-review-upsert`, and `sample-review-reorder`. Initial live versions were 1/1/1.
- First A2 live parity run on TEST client `sidneylaruel` passed all sample cases but failed `calendar-reorder-batch-shape` because the Edge Function response included an extra empty `skipped` array. Rows matched exactly. Updated `calendar-reorder` to return the n8n-compatible envelope `{ok:true,updated}` and redeployed it; final live function versions are `calendar-reorder` version 2, `sample-review-upsert` version 1, and `sample-review-reorder` version 1.
- Final A2 live parity result on `sidneylaruel`: `PASS calendar-reorder-batch-shape`, `PASS sample-review-reorder`, `PASS sample-review-upsert-create`, `PASS sample-review-upsert-link-clobber`, `PASS sample-review-upsert-clear-link`, `PASS sample-review-upsert-conflict`, and `PASS sample-review-upsert-comment-merge`. The comment-merge case confirmed the n8n and Edge Function paths keep the stored comments and add the incoming comment with matching final rows after normalizing server-generated fields.
- Post-parity cleanup verification: `calendar_posts` contains 0 TEST rows matching `a2_parity_%`, `sample_reviews` contains 0 TEST rows matching `a2_parity_%`, `sample_review_events` contains 0 TEST events matching `a2_parity_%`, and `sample_review_ef_clients` remains `{"clients":[]}`. No real client is enabled for A2; A2 remains unmerged.
- Final A2 local verification after the response-envelope fix and docs update: `node --check scripts/a2-writer-parity.js`, `node --check scripts/sample-linear-reconcile.js`, `node test/a2-writer-edge-source.js`, `git diff --check`, public-repo secret-shaped string scan, and `npm.cmd test` with Git Bash on PATH all passed. `npm test` reports all 38 unit suites green.
- A2 draft PR #670 was opened from branch `codex/a2-writer-edge-functions` at commit `6218f81`; it remains draft and unmerged.
- Ran `node qa/master.js` fast profile on the A2 branch with Git Bash on PATH and `SXR_COURIER=0`. Result: `unit` passed (38/38 suites), `parity` passed (`parity_logic.js`, `realtime_parity.js`), `probes` passed (2/2), `scenarios` passed (12/12 scenarios, 87/87 assertions), and `visual` passed its scripted checks (1/1 flow, 15/15 assertions) with 6 screenshots. Manual screenshot review of the `clean_both` flow found no obvious layout breakage, overlap, or wrong terminal state. No real client was enabled and A2 remains unmerged.
- Owner approved merging PR #670 for the A2 writer Edge Function work, with the hard gate still limited to the Sidney TEST client. PR #670 was marked ready-for-review and merged to `main` as merge commit `2904c6df86aa28958ecd4febd2d17b0d6e6b1acc` on 2026-07-04. No real client was added before the merge.
- Waited for GitHub Pages deployment run `28713539839` (`pages-build-deployment`) on merge commit `2904c6df86aa28958ecd4febd2d17b0d6e6b1acc`; it completed successfully at `2026-07-04T17:09:46Z` before the samples runtime flag was changed.
- Set `syncview_runtime_flags.sample_review_ef_clients` to `{"clients":["sidneylaruel"]}` with `updated_by='codex-a2-live-sidney-test'`. Read-back verification confirmed `calendar_upsert_ef_clients={"clients":["sidneylaruel"]}` and `sample_review_ef_clients={"clients":["sidneylaruel"]}`. This routes only the Sidney TEST client to A1/A2 Edge Function paths; no real client is enabled.
- Updated `ROLLBACK.md` live-state table for the post-A2-merge TEST-only state. One-step rollback remains setting `sample_review_ef_clients` to `{"clients":[]}` for samples and `calendar_upsert_ef_clients` to `{"clients":[]}` for calendar/reorder. A3 remains explicitly out of scope until owner review after Sidney testing.
- A4 branch started from `main` commit `5669b23` as `codex/a4-settings-edge-functions`. Scope is limited to `templates-get`/`templates-save` and `caption-prompts-get`/`caption-prompts-save`; caption generation, filming-plan tabs, A3 Linear bridges, and Linear replacement work are explicitly untouched.
- A4 n8n baseline metadata recorded in `n8n-backups/2026-07-04-a4-snapshot-status.md`: `RhEdtimfMUeogyL2` Templates Get, `oPX1nH7TxzCITNAz` Templates Save, `3hZnjXmHdNv4bttw` Caption Prompts Get, and `RGkuE8d4uJg6CPde` Caption Prompts Save are active and remain the fallback paths. No n8n workflow was edited, deactivated, or deleted.
- Applied additive A4 migration `migrations/2026-07-04-a4-settings-edge-functions.sql` to live Supabase via the linked project. It created `templates`, `caption_prompts`, anon read policies, realtime publication entries, and seeded `syncview_runtime_flags.settings_ef_clients` as `{"clients":[]}`.
- Deployed A4 Supabase Edge Functions to project `uzltbbrjidmjwwfakwve` with JWT verification disabled: `templates-save` version 1 and `caption-prompts-save` version 1. The old n8n Sheet writers remain active as fallback/default.
- Ran A4 backfill from live n8n Sheet reads into Supabase. Verified counts and values: 5 templates from n8n = 5 rows in Supabase with matching values; 24 caption prompts from n8n = 24 rows in Supabase with matching values.
- Ran A4 live parity on TEST client `sidneylaruel`: `PASS templatesSave` and `PASS captionPromptsSave`. Cleanup verification found `settings_ef_clients={"clients":[]}`, 0 `templates` rows for `sidneylaruel`, and 0 `caption_prompts` rows for `sidneylaruel`. No real client is enabled.
- A4 local verification: `node --check scripts/a4-settings-backfill-parity.js`, `node test/a4-settings-edge-source.js`, `git diff --check`, and `npm.cmd test` with Git Bash on PATH all passed. `npm test` reports all 39 unit suites green.
- A4 draft PR #673 was opened from branch `codex/a4-settings-edge-functions` at commit `ce86f10`; it remains draft and unmerged. Hosted GitHub Actions run `28714721464` (`Calendar unit tests`) completed successfully on the PR commit. No real client was added to `settings_ef_clients`.
- A4 merge-blocker fix: corrected the staged settings reads to use the same `settings_ef_clients` runtime flag as writes. Templates and caption prompts now load n8n as the base store and overlay Supabase rows only for flagged clients; empty flag, flag-read failure, or Supabase read failure keeps the n8n base. `settings_ef_clients` remains `{"clients":[]}` and PR #673 remains draft/unmerged.
- A4 pre-cutover snapshot: created and pushed git tag `pre-A4` on `main` commit `5669b2378612b33801bc190e07fb140c7d70ad81` before merging PR #673. Confirmed weekly-backup execution `191240` produced the private weekly-backup Drive folder copy of `SyncView Main Sheet - 2026-07-03`, which covers the source `Templates` and `CaptionPrompts` tabs used for the A4 backfill; public-safe backup evidence remains in `n8n-backups/2026-07-03-phase0-snapshot-status.md`.
- Owner approved A4 cutover on the TEST client only. Marked PR #673 ready-for-review and merged it to `main` as merge commit `48d651b67e149a8b646f44ebcf57d1bc777fa63e`. GitHub Pages deployment run `28716538905` completed successfully for that commit, and the live site served all four A4 read-routing markers: n8n-base template load, flagged Supabase template overlay, n8n-base caption-prompt load, and flagged Supabase caption-prompt overlay.
- A4 rollback rehearsal before canary: with `settings_ef_clients={"clients":[]}`, loaded the TEST client's settings from the n8n base paths. `templates-get` returned the Sidney template under the exact key `Sidney Laruel`; `caption-prompts-get` returned the `sidneylaruel` prompt entry. This confirmed the one-step rollback state before flipping the flag.
- A4 TEST-only flag flip: set `syncview_runtime_flags.settings_ef_clients` to `{"clients":["sidneylaruel"]}` with `updated_by='codex-a4-test-canary'`. Read-back verification returned the exact value `{"clients":["sidneylaruel"]}` at `2026-07-04T17:30:56.810657+00:00`. No real client was added.
- A4 backend overlay verification: with the TEST-only flag set, called the deployed `templates-save` and `caption-prompts-save` Edge Functions for `sidneylaruel` using the current live n8n values. Both writes landed in Supabase. The `templates` row has `client_slug='sidneylaruel'` and `data.client_name='Sidney Laruel'`, exactly matching the n8n templates map key; the `caption_prompts` row has `client_slug='sidneylaruel'`. Both rows were updated by `codex-a4-backend-verify`. No real client was added.
- 2026-07-04 frontend-only roster normalization: added `getClientRoster()` as the canonical client-list accessor and routed Templates plus analytics search/open paths through the `WL_CLIENT_NAMES` allowlist. Live roster diff: 31 old allData-derived names to 33 canonical names, 0 real clients dropped, only seed additions `Kasper Hytonen` and `Sidney Laruel` (already in the calendar picker). Verified local headless Templates/analytics search for `Sidney Laruel`, data-less analytics empty state, and unchanged `settings_ef_clients={"clients":["sidneylaruel"]}` with exactly one Sidney template row. Revert = one frontend/docs commit.
- 2026-07-04 roster normalization merge: PR #675 was marked ready-for-review and merged to `main` as merge commit `1fc7ad6047fd95a0a61704d401e5e428aa8e9997`. GitHub Pages deployment run `28718041354` initially hit a transient deploy error after a successful build, then passed on rerun; live `syncview.synchrosocial.com` served the roster markers `function getClientRoster() {`, `No analytics yet`, and `function _tplAllNames() { return getClientRoster(); }`. No runtime flags were changed; `settings_ef_clients` remains `{"clients":["sidneylaruel"]}`.

## 2026-07-05

- Track B B0 mandatory re-audit: synced `main` to `09436997bda2e0e8c23fe8710063621841c3579d` and rechecked repo code, Supabase, n8n, Linear, and Google Sheets against the 2026-07-05 audit summary/logic maps. No material contradiction to `TRACK_B_LINEAR_REPLACEMENT_SPEC.md` was found. Existing TEST-only flags remained `calendar_upsert_ef_clients={"clients":["sidneylaruel"]}`, `sample_review_ef_clients={"clients":["sidneylaruel"]}`, and `settings_ef_clients={"clients":["sidneylaruel"]}`; no real client was added.
- Track B B0 pre-snapshot: created and pushed git tag `pre-B0-track-b-2026-07-05`; ran `SyncView - Weekly Backup` workflow `jlVfbg0Njxf1It7h` as n8n execution `204072`, successful from `2026-07-05T21:29:10Z` to `2026-07-05T21:29:33Z`. Private Drive backup folder `2026-07-05` has folder ID `1eidMH3rtOgycApT7GQ7A-yYp6E2b4xJo`; public-safe snapshot details are in `n8n-backups/2026-07-05-pre-B0-track-b-snapshot-status.md`.
- Track B B0 private Supabase snapshots: because this workspace's Supabase CLI `db dump` path requires Docker, exported JSON snapshots with the service role into `C:\Users\Sidney\Documents\Codex\private-backups\2026-07-05-pre-B0-track-b\supabase`. Snapshot counts: `calendar_posts` 3438, `sample_reviews` 2654, `calendar_post_events` 473, `sample_review_events` 22000, `templates` 6, `caption_prompts` 25, `workload_issues` 2085, and `syncview_runtime_flags` 3. B0 target tables were absent before migration.
- Track B B0 live additive migration: applied `migrations/2026-07-05-b0-linear-auth-scaffold.sql` to project `uzltbbrjidmjwwfakwve`. It created only additive auth/access/event tables and seeded `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}`.
- Track B B0 function/secret deploy: generated role-key material only outside the public repo, stored it as Supabase function secrets, and deployed `client-token-verify` and `key-verify` with JWT verification disabled as planned. Redeployed the existing A1/A2 writer Edge Functions only to accept the optional `x-syncview-client-token` CORS header; routing flags and production behavior did not change.
- Track B B0 seeding: ran `scripts/b0-seed-auth-scaffold.js` from the live Sheets/client roster. Dry-run found 33 clients, 14 team members, and 32 client-access candidates; the applied run upserted 33 clients, created/upserted 14 team members, and minted 32 client tokens. The private client-token manifest is outside the repo at `C:\Users\Sidney\Documents\Codex\private-backups\2026-07-05-pre-B0-track-b\auth\client-review-tokens.private.json`.
- Track B B0 verification and rollback rehearsal: verified live counts for the new tables, successful `client-token-verify` and `key-verify` smoke checks, then rehearsed the kill switch by setting `auth_enforcement={"mode":"enforced"}` for a Sidney TEST invalid-token check and setting it back to `{"mode":"permissive"}`. Latest intended state is permissive; Linear/n8n remain the production authority and no production tab is live.
- Track B B0 errorWorkflow gate cleared: using the n8n REST API key from process environment only, wired workflow-level `errorWorkflow` to `SyncView - Error Alerts -> DM Sidney` (`itqDXSl2ybsRSAiQ`) on `MJbMZ789B5ExZz9x`, `VQqqeY9B2GZbh2Bt`, `8stSpZUiyG7f2LQX`, `AkiFmromoDkmsh39`, `ZJOtYpQZj73DcBB1`, `BrJSe8zCKUccfmIq`, and `jlVfbg0Njxf1It7h`. API readback confirmed each target persisted `errorWorkflow=itqDXSl2ybsRSAiQ`; binary-mode workflows still read back `binaryMode=separate`.
- Track B B0 safe errorWorkflow verification: created a temporary throwaway workflow `Codex B0 ErrorWorkflow Test 20260705174708` (`9mDmrCQduO5V8Eoy`) with the same alert workflow configured, triggered it once, and confirmed the throwaway errored as execution `204474` while the alert workflow ran successfully as execution `204475` in `mode=error`. The throwaway workflow was then deactivated and deleted.
- MJbMZ live incident note: owner reported `MJbMZ789B5ExZz9x` auto-deactivated after a Linear weekly cycle-rollover webhook storm. Codex checked the workflow and found it active. During the interrupted turn, a cycle-only guard was briefly published and verified, but after the owner clarified the guard is later hardening and not tonight, `Plan Workload Row` was reverted to its prior logic and published. Final readback: `MJbMZ789B5ExZz9x` active version `afb31a5f-4b49-4cbc-83f3-8fcf7749bf3b`, `errorWorkflow=itqDXSl2ybsRSAiQ`, and the deferred cycle-only guard is not live in `Plan Workload Row`.
- Track B B0 completion: vocabulary/authority flags remain ratified as `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}`; client/reviewer tokens are minted and `client-token-verify` is live; `flag_flips` is live and recorded the rollback rehearsal; n8n `errorWorkflow` is wired and verified. No real client was added to any Track A flag.
- Pre-B0.5 MJbMZ diff check: weekly-backup execution `204072` contained a 45-workflow subset and did not include `MJbMZ789B5ExZz9x`, so it could not be used as the requested pre-B0 proof point. Compared live `MJbMZ789B5ExZz9x` to the private A2 post-routing snapshot instead. Connections and settings other than `settings.errorWorkflow` match; `settings.errorWorkflow` is the B0 addition. The only node-code delta is the already-known `Handle Sample Linear Event` slugify corruption, where the samples handler's diacritics range differs from the calendar handler. Deferred finding: fix the samples slugify range later; do not change it during B0.5.
- Deferred incident hardening finding: before the next Sunday Linear cycle rollover, re-apply the cycle-only guard on `MJbMZ789B5ExZz9x` `Plan Workload Row` so issue updates without `updatedFrom.stateId` are skipped there. Status handlers already skip non-status changes via the `stateId` check. No guard is live in `Plan Workload Row` as B0.5 starts.
- Track B B0.5 pre-snapshot: synced `main` after B0 merge commit `0aacbb3ad698f623e3c083bff74b6b052eed0b43`, created and pushed git tag `pre-B0.5-track-b-2026-07-05`, exported public-safe runtime-flag and table-count snapshots privately under `private-backups/2026-07-05-pre-B0.5-track-b`, then cut branch `codex/b05-ef-rollout-prep`. Count snapshot: `calendar_posts` 3438, `calendar_post_events` 473, `sample_reviews` 2654, `sample_review_events` 22000, `templates` 6, `caption_prompts` 25, `clients` 33, `team_members` 14, `client_access` 32, `flag_flips` 2, and `syncview_runtime_flags` 6.
- Track B B0.5 blocker-fix staging: updated the frontend calendar bulk-import verification to confirm against `calendar_posts` for clients routed through `calendar_upsert_ef_clients`, while unflagged clients still verify against n8n `calendar-get`. Also staged EF reorder fallbacks for calendar and samples so an EF reorder failure falls through to the old n8n reorder writer, and the UI still shows a failure if both paths fail. No runtime flag was changed and no real client was enabled.
- Track B B0.5 parity evidence: re-ran the A1/A2/A4 live parity harnesses on guarded TEST client `sidneylaruel`. A1 passed 8/8 cases including `linear-import-real-shaped`, `kasper-real-shaped-approval`, and `comment-merge-video-tweaks`. A2 passed 11/11 cases including real-shaped calendar reorder, real-shaped sample reorder, sample import, sample Kasper write, and sample comment merge. A4 parity passed `templatesSave` and `captionPromptsSave`.
- Track B B0.5 cleanup/evidence: post-parity Supabase readback found 0 active `a1_parity_%` calendar rows/events, 0 active `a2_parity_%` calendar rows/events, and 0 active `a2_parity_%` sample rows/events. Source checks confirm EF-write-failure visibility: calendar reorder shows a toast after EF+n8n fallback failure, samples reorder shows a notification after EF+n8n fallback failure, and card-save failures still surface through existing save-error chips/review errors.
- Track B B0.5 verification: `node --check scripts/a1-calendar-upsert-parity.js`, `node --check scripts/a2-writer-parity.js`, `node test/a2-writer-edge-source.js`, `git diff --check`, and changed-file secret scan passed. `npm.cmd test` initially failed only because Git Bash was not on PATH for the overnight-runner shell tests; rerunning with Git Bash on PATH passed all 40 unit suites. Live runtime flags are unchanged: Track A flags list only `sidneylaruel`; `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}`.

## 2026-07-06

- Track A B0.5 wave-2 canary: owner approved adding two more real-client canary slugs (names scrubbed 2026-07-11). Supabase readback confirmed both slugs exist and are active. Appended both slugs to `calendar_upsert_ef_clients`, `sample_review_ef_clients`, and `settings_ef_clients` with `updated_by='codex-b05-wave2-canary'`; readback for all three flags is the documented canary roster (slugs scrubbed 2026-07-11). `auth_enforcement`, `prod_authority`, and `linear_inbound_enabled` were unchanged. `flag_flips` recorded ids 6, 7, and 8 at `2026-07-06T19:45:25Z`-`2026-07-06T19:45:26Z`. Immediate monitor: GitHub Actions run `28818861497` (`Linear ⇄ SyncView status reconcile`) completed successfully on `main` and reported `calendar-upsert EF clients: sidneylaruel,«scrubbed-canary-slug»,«scrubbed-canary-slug»,«scrubbed-canary-slug»`, `corrections 0`, `applied ok=0 fail=0`; canary event-ledger checks found 0 error/failure-like calendar or sample events for the two new slugs after the flip. Per-client rollback is removing the affected slug from those three flags.

- Owner approved PR #680 for B0.5 rollout prep. Marked it ready and merged it to `main` as merge commit `c776ad626e953ce2f33c04351eff7d7b08b8963f`. The first GitHub Pages deployment for that commit failed after artifact upload with a transient Pages deploy error; reran the same run `28760819271`, which completed successfully at `2026-07-06T01:07:25Z`. Live `syncview.synchrosocial.com` served the #680 markers at `2026-07-06T01:07:19Z` (`/rest/v1/calendar_posts?select=*&client=eq.` import-verify readback and `EF reorder failed; falling back to n8n` reorder fallback). No flag was changed until the live marker check passed.
- The first real-client canary (name scrubbed 2026-07-11) pre-flight before flagging: `calendar_posts` has 70 total rows and 34 non-archived active rows; `sample_reviews` has 2 total rows and 0 active rows (both archived). Write activity exists within the last 14 days: latest calendar row update `2026-07-03T14:31:30.982Z`, sample event activity count 8 since `2026-06-22T01:09:25Z`, latest sample event `2026-07-02T16:33:46.606Z`. Jesse is a calendar-heavy first canary; no second client was added.
- Added only that first real-canary slug to the three Track A runtime flags with `updated_by='codex-b05-jesse-canary'`. Readback: `calendar_upsert_ef_clients` = TEST client + the first canary slug, `sample_review_ef_clients` and `settings_ef_clients` = the same pair. `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}` were unchanged. `flag_flips` recorded the three changes as ids 3, 4, and 5 at `2026-07-06T01:10:24Z`-`2026-07-06T01:10:25Z`. Per-client rollback is removing «scrubbed-canary-slug» from those three flags.
- Immediate post-flip monitor: GitHub Actions run `28761533139` (`Linear ⇄ SyncView status reconcile`) completed successfully on merge commit `c776ad6` after the flag flip and reported `IN SYNC 441`, `missing 1`, `corrections 0`, `applied ok=0 fail=0`. No Jesse event-ledger writes had occurred yet after `2026-07-06T01:10:24Z`, so the canary is armed but no real Jesse save has been observed yet. A two-day thread follow-up was scheduled to re-check the canary without expanding the rollout.
- n8n hardening pre-snapshot: exported live workflows privately to `C:\Users\Sidney\Documents\Codex\private-backups\2026-07-06-n8n-hardening` before editing. Public-safe status is in `n8n-backups/2026-07-06-n8n-hardening-status.md`. Pre-edit snapshots: `MJbMZ789B5ExZz9x` active version `afb31a5f-4b49-4cbc-83f3-8fcf7749bf3b` SHA-256 `784348571fd3c11bf138c7687a709b0fccb870729c1f86befb6a8664b98696e0`; weekly backup `jlVfbg0Njxf1It7h` active version `3cf46301-606e-48a2-ba8e-557020b800c1` SHA-256 `b9cb05eb9a9bb4736ef17d81358180e4e07062888e61a8201f6ba3e4144064fa`.
- n8n hardening edit: updated `MJbMZ789B5ExZz9x` only in `Handle Sample Linear Event` and `Plan Workload Row`. Sample slugify now uses the calendar handler's safe `\u0300-\u036f` diacritics range; the readback node hash is `6bb2df802bd950a295706cd6f94c05a79124b5fea508e93ebd21220e69eb8a6a` and the code compiled. `Plan Workload Row` now skips cycle-only `Issue` update events without `updatedFrom.stateId`; readback hash is `1f555e6af4057362d114c4aec6d0b8cd9295b3ef38155b0ac795c25194b722f1`. A local readback harness confirmed a cycle-only payload returns 0 rows, while status-change and remove payloads still return 1 row. Final workflow readback: active, version `655b6aa5-e571-451e-8f65-f4fcf78aff02`, settings unchanged (`executionOrder=v1`, `availableInMCP=true`, `binaryMode=separate`, `errorWorkflow=itqDXSl2ybsRSAiQ`).
- n8n weekly-backup hardening: updated `jlVfbg0Njxf1It7h` `Export n8n Workflows` to remove the inactive-only `activeWorkflows=false` filter, leaving `returnAll=true` and `excludePinnedData=true`. REST inventory verification shows 120 workflows total (75 active, 45 inactive) and includes `MJbMZ789B5ExZz9x`; readback version is `8b341e89-108c-4b30-b096-cf6da7fdc2f4`, post-edit private snapshot SHA-256 `aa587a89d6acac287d9178b1de4d33251038507ca2cd8d3f9b4a85365e723198`.
- Track B B2 draft preview staging: opened draft PR #686 from `codex/b2-production-readonly-preview` and pushed the B2 source, source guard, and execution-log commits. The Production preview is hidden behind `?prod=1`, reads only the dormant B1 Supabase tables, renders list/board/detail/batch views with disabled write affordances, and does not touch runtime flags, n8n, auth, Linear, or production writes. Verification passed: `git diff --check`, focused secret scans, `npm.cmd test` (41 suites), `node docs/syncview-design/tests/prod-readonly-smoke.js`, and `node test/production-preview-source.js`; screenshots are stored privately outside the repo. Rollback = revert PR #686 / the B2 commits.
- Track B B1 start: merged approved docs-only PR #681 into `main` as merge commit `f7d77561c823335964e6d78dd854b0086957884d`, created and pushed tag `pre-B1-track-b-2026-07-06`, then cut branch `codex/b1-linear-data-model` from updated `main`. No runtime flags were changed.
- Track B B1 private snapshot: exported Supabase JSON snapshots outside the public repo. Public-safe snapshot status is recorded in `n8n-backups/2026-07-06-pre-B1-track-b-snapshot-status.md`. B1 does not edit n8n workflows; the weekly backup workflow remains the private all-workflow backup path.
- Track B B1 live additive migration: applied `migrations/2026-07-06-b1-linear-data-model.sql` to project `uzltbbrjidmjwwfakwve`. It created empty `batches`, `deliverables`, `deliverable_events`, `mirror_outbox`, and `linear_archive` tables, two-slot card linkage columns on `calendar_posts`/`sample_reviews`, ledger guard triggers, and service-role-only `batch_write`/`deliverable_write` RPCs. No Linear backfill rows were written.
- Track B B1 readback: `batches`, `deliverables`, `deliverable_events`, `mirror_outbox`, and `linear_archive` each read back with 0 rows; the new `video_deliverable_id`/`graphic_deliverable_id` columns are readable on calendar and sample card tables. The approved B0.5 canary state stayed unchanged; no client was added during B1.
- First real canary monitor during B1: since the B0.5 flag flip, no new canary `calendar_post_events` or `sample_review_events` had been observed yet; no EF write failure or reconciler correction had been observed yet.
- Track B B1 dry-run blocker: added `scripts/b1-linear-dry-run.js` for read-only Linear pulls and client reconciliation, but no supported Linear token environment variable (`LINEAR_API_KEY`, `LINEAR_API_TOKEN`, `LINEAR_KEY`, or `LINEAR_TOKEN`) is visible to this process. B1 is stopped before the Linear dry-run/backfill gate until a key is available; no backfill writes have run.
- Track B B1 dry-run hardening: added offline fixture inputs and unit coverage for the dry-run report logic, including VID/GRA vs CON/STR separation, linked live-card inclusion, createdAt/completedAt cutoff handling, workload mirror D-11 split, and D-16 owner-review rows. Verification passed with `node test/b1-linear-dry-run.js`, `git diff --check`, changed-file secret scan, and `npm.cmd test` with all 41 unit suites green. Runtime flags and B1 table rows were unchanged.
- Track B B1 live read-only dry-run evidence: after the corrected Linear API key became visible to this process, ran `scripts/b1-linear-dry-run.js --as-of 2026-07-05T00:00:00.000Z` with detailed output written outside the repo to a private artifact. The results reconciled to the approved audit baseline, and B1 source hardening was added so the two-slot deliverable IDs ride existing calendar/sample EF and browser save paths without counting as phantom-row content. Verification passed with all 42 unit suites green using Git Bash on PATH, `git diff --check`, and changed-file secret scan. No backfill rows were written at that point.
- Track B B1 constraint-preflight addendum after PR #684: synced the revised spec and split the blocking gate from the comment-dependent delivery-link sweep. Ran `scripts/b1-constraint-preflight.js` with issue-level Linear reads only (assignee/state/parent/team/project/client/link resolution; no comment reads). Private artifacts contain the detailed counts and samples. Item 11 delivery-link comments were not run in the blocking gate; `scripts/b1-delivery-link-sweep.js` is now a separate best-effort read-only annotation pass, and file URL repair remains non-blocking. Key-separation remained a known caveat; no backfill writes were run at that point.
- Track B B1 handling-rule plan after PR #685: synced latest spec (`deliverables.kind` includes `other`) and staged an additive kind-check widening migration plus `scripts/b1-linear-backfill.js`. Ran the backfill plan read-only, recorded detailed counts in private artifacts, and stopped before any live schema/data write under the owner rule because one shared/non-person assignee identity needed owner confirmation before inactive `team_members` insertion. B1 tables still read back empty at that point.
- Track B B1 backfill after owner handling-rule approval: applied the additive kind widening and approved reconciliation writes, then reran the issue-level constraint gate with all target constraints clean. Detailed handling evidence is stored only in private artifacts; item 11 delivery-link comment sweep remains separate best-effort repair work.
- Track B B1 backfill completion: ran the approved read-only Linear backfill into dormant B1 tables only. A first pass exposed duplicate calendar card-slot claims from cross-slot subtasks; patched the backfill/preflight to classify those deliverables by their actual kind and to claim a card slot only when deliverable kind matches the card slot, leaving mismatched subtasks unlinked for later repair instead of violating the two-slot unique index. Private verification artifacts record the detailed completion evidence and repair queues. No runtime flag was changed and the approved B0.5 canary state was not expanded.
- Track B B1 review-blocker fix: confirmed the previously deployed `calendar-upsert` and `sample-review-upsert` functions did not expose the new deliverable-link fields during backfill, so there was no live risk from current production browsers, which do not send those fields. Patched and redeployed both functions so `video_deliverable_id` and `graphic_deliverable_id` participate in clear-link sentinel handling, blank-echo carry-forward, and `''` to `NULL` storage. A1/A2 parity passed, including explicit empty-string-to-NULL cases.
- Track B B1 incremental refresh tick while PR #682 remains unmerged: ran local incremental apply against dormant B1 tables only. It pulled 11 changed Linear issues since `2026-07-06T15:00:12Z`, wrote 6 deliverable updates, 4 archive upserts, and 1 summary event. A three-row changed-issue spot check matched live Linear for title, due date, and assignee linkage; status matched via the expected B1 enum normalization. No runtime flag, n8n workflow, auth setting, or Linear record was changed.
- Track A first real-client canary evidence: owner reported a live calendar caption edit, status flip, and reorder on the single real canary client, with hard-refresh persistence and fast UI. Supabase readback since the canary flag flip found 12 `calendar_post_events`, all `source='ui'` with actor `SyncView`, and 0 reconciler-sourced corrections. `flag_flips` still show only the original three single-client Track A additions. No runtime flag was changed and no client was added.
- Status-sync timeout-burst verification only: the live Linear-to-SyncView status-sync workflow had a four-event timeout burst, then self-recovered within minutes. Private spot-check evidence confirmed the affected statuses match between Linear and SyncView after recovery; no production n8n workflow was edited. If similar timeout bursts recur more than about three times in the day, propose hardening before applying any change.
- Track B B1/B2 approved merges: after external re-review passed, merged PR #682 as merge commit `a888470a2fcc1c5f454a7c7e066da0e08d10f5ee` and PR #686 as merge commit `23a3bf398d4ef16ab78d6f623f86ac026f2d2172`. No runtime flag was changed during either merge.
- Track B B1 incremental-refresh workflow verification: confirmed `LINEAR_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` are present in GitHub Actions secrets, confirmed `B1 Linear Incremental Refresh` is active, and manually triggered workflow run `28806142310` on `main`. The run completed successfully, produced the refresh artifact, read from `changed_since=2026-07-06T15:34:20.387Z`, found 0 changed issues in that window, and wrote one `linear_incremental_refresh` summary event at `2026-07-06T16:16:42Z`. This proves the run was not the old missing-secret skip path.
- Track B B2 live-serving verification: GitHub Pages deployment run `28806450472` completed successfully for merge commit `23a3bf398d4ef16ab78d6f623f86ac026f2d2172`; live `syncview.synchrosocial.com` served `PRODUCTION PREVIEW (Track B B2)`, `function _prodEnabled()`, and the paginated `_prodRestRows(...)` marker. A live read-only smoke against `https://syncview.synchrosocial.com/?prod=1` rendered 1,043 rows with 0 JavaScript errors and 0 non-GET/HEAD browser requests; fresh screenshots are private in `C:\tmp\syncview-live-prod-smoke-20260706`.
- Track B post-merge docs/follow-up: updated `ROLLBACK.md` Live State to record B1 tables plus scheduled refresh on `main` and the query-gated read-only Production preview. Updated the B1 refresh workflow so missing required secrets fail the run loudly instead of producing a green skip. Final flag readback remained unchanged: Track A flags contain only `sidneylaruel` and «scrubbed-canary-slug», `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}`.
- Track B B2 design-fidelity incident and rebuild: audit of PR #686 found the Production tab had been built from the spec's prose instead of ported from `docs/syncview-design/SyncView.html` (the wrong sidebar/board/status-glyph structure shipped), allowed by a review that checked safety but not fidelity. PR #689 rebuilt the `_prod*` render layer from the artifact's own render functions; independent review verified the port byte-level against `SyncView.html` (status pie-wedge paths, sidebar tree, Projects board, 3-card detail), caught one wrong pie wedge (`tweak` had `client_approval`'s arc) plus an approximated project hexagon, and both were fixed to the artifact's exact paths in `014f60d`. Read-only/`?prod=1`/`_prod*` isolation, B1 reads, and app typography (owner-preferred) were verified unchanged; `prod-structure-subset.js` was added as the wired-tab structural lane. Merged as `6c315a5`. Spec §10.8 now codifies the non-optional fidelity gate (artifact wins over prose; structural suites + artifact-diff review on every `_prod*` PR).
- Consolidation sweep while awaiting creative-team feedback (read-only, no live system changed): live runtime flags matched the documented state exactly (Track A flags = TEST client + the first canary slug, `auth_enforcement` permissive, `prod_authority` linear/linear, `linear_inbound_enabled` false). `B1 Linear Incremental Refresh` green on schedule (runs 1–2, latest `28813824732`). `Linear ⇄ SyncView status reconcile` green at 10-minute cadence (latest run 2679). Finding: `Samples ⇄ Linear status reconcile` has NOT run since 2026-06-28 and its last 4 runs failed (cron disabled; n8n trigger `ZJOtYpQZj73DcBB1` inactive) — recorded in ROLLBACK.md Live State as an accepted gap while real-client SXR usage is ~0 active rows, revisit before B3. Both nightly E2E workflows (calendar, samples) remain manually disabled since ~07-03. `MJbMZ789B5ExZz9x` had two short timeout/crash bursts (2026-07-05 23:12Z ×4 crashed, 2026-07-06 14:48Z ×4 error) matching the known webhook-burst pattern; statuses verified healed by the reconciler, no workflow edit made, hardening threshold (≥3 bursts/day) not yet met. Clarified that `pixel-matching-playbook.md` was never missing — it lives in the sibling `synchrosocial` repo (`docs/pixel-matching-playbook.md`); spec §10.6 now says so. Seeded the B2 parity-polish backlog at `docs/audits/2026-07-06-prod-parity-gaps.md` (owner-reported: non-clickable detail breadcrumb, missing right-click context menus, self-referential parent crumb on batch-parent rows, empty Sub-issues block on a filming-plan parent).
- Track B B2 fidelity-protocol ratification: after the owner reported five live fidelity/behavior defects in the merged parity round (transparent context menu from un-scoped CSS variables on body-mounted overlays, sibling-as-children sub-issue model causing apparent ID switching, missing picker submenus and inert due pill, wrong project-card icon fallback, misplaced tooltip pill), the failure pattern across #686/#689/#693 was autopsied into five systematic failure modes and the owner ratified the TRANSPLANT → ADAPT → PROVE protocol now codified as spec §10.8: adapter contract (`ADAPTER.md` + `_prodAdapter()`), verbatim-transplant rule with PORT-DELTA markers, mechanical `port-fidelity-check.js` CI lane, environment shim inventory, `WIRED-PARITY.md` ledger as the definition of done, and `behav-wired` (the artifact's own 138-assertion suite run against the wired tab in read-only guard mode) plus reviewer-graded side-by-side screenshots. No code or live system changed in this ratification; the Foundation build session implements it.
- Templates multiple-links feature (PR #697, merged to `main` as `d3f35b5`; GitHub Pages served the new `index.html`, multi-link markers confirmed live). Post-merge the owner reported multi-link edits did not persist. Root cause: the n8n **"SyncView Templates — Save"** workflow (`oPX1nH7TxzCITNAz`), node "Build Row From Patch", has a hard-coded `ALLOWED` column whitelist that silently dropped the new `*_link_list` columns; unflagged (n8n) clients kept only the legacy first link. Flagged A4 Edge-Function clients were unaffected (the EF stores arbitrary keys, as `thumbnails_color_sets` always has).
- Fix (owner-approved n8n edit): added `reels_reference_link_list`, `thumbnails_photos_link_list`, `thumbnails_canva_link_list` to the `ALLOWED` array via the n8n API — a surgical `setNodeParameter` on the node's `/jsCode`, no other node touched — then published. Active version `f56c6e1c-ca4f-410e-93d5-572366d7c17c` → `dc1f9c59-2c51-460f-a223-19407abce7df`. The Sheet writer already uses `autoMapInputData` + `handlingExtraData: insertInNewColumn`, so the new columns auto-create on first write; `templates-get` returns all columns (no whitelist), so reads render one row per link.
- Verification: the post-change active version's whitelist was re-read via API and contains the three keys; the front-end edit→save→refresh round-trip was verified headless against a stubbed backend (renders all links, 0 JS errors); the calendar/EF paths already stored `_list` (proven by the live `thumbnails_color_sets` precedent). A live round-trip WRITE to the shared Templates sheet was intentionally not run from this session (sandbox shared-production write guard) — owner to confirm in-app. Public-safe backup + rollback: `n8n-backups/2026-07-06-templates-save-whitelist.md`. Rollback = restore the pre-change `ALLOWED` array (remove the 3 keys) or republish `f56c6e1c…`; additive, no data loss.
- Track A B0.5 wave-3 canary flip: with owner approval, set `calendar_upsert_ef_clients`, `sample_review_ef_clients`, and `settings_ef_clients` to the wave-3 canary roster (10 slugs incl. the TEST client; list scrubbed from this public file 2026-07-11 — see `flag_flips` id 9/10/11 for the exact historical value) using `updated_by='codex-b05-wave3-canary'` at `2026-07-07T01:24:38Z`. Readback confirmed `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}` were unchanged. `flag_flips` recorded exactly 3 rows for the actor (ids 9, 10, 11). Immediate monitor: post-flip `Linear ⇄ SyncView status reconcile` run `28834999040` completed green, and `calendar_post_events` / `sample_review_events` showed 0 post-flip events and 0 error-like events for two additional real-client canary slugs (names scrubbed 2026-07-11). Per-client rollback = remove the affected slug from the three Track A flags.

## 2026-07-07

- Samples review + Credentials front-end polish (PR #705, branch `claude/samples-credentials-feature-saubyx`). Pure front-end + tests/docs — **no** production write-path, runtime flag, n8n workflow, Supabase schema, or write routing changed; fully isolated from the `_prod*` / `?prod=1` Production region (0 overlapping lines, so the concurrent Codex Production work does not conflict). Rollback = `git revert` PR #705 and let GitHub Pages redeploy (no flags to flip). Four workstreams:
  - **Focus never stolen (global).** Added a shared focus guard (`_svFieldSig` / `_svCaptureFocus` / `_svRestoreFocus` / `_svPreserveFocus`, right after `showToast`) that captures the active INPUT/TEXTAREA by a rebuild-stable signature (id → oninput handler → name) + caret, runs a render, then restores focus + selection. Wrapped the Samples-Kasper queue rebuild (`_sxrKasperRenderQueue`, gated to `#kasperContent`), the single-card repaint (`_sxrKasperRepaint`), and the SXR review-card repaint (`_sxrReviewRepaintCard`). Root cause of the "Approve pulls me out of the field" repro was the re-render replacing the focused node — `showToast` itself never calls `focus()`. Regression test `test/background-update-focus.js`.
  - **Realtime propagation across roles.** An adversarially-verified audit (16 sub-agents) confirmed every Samples viewer already subscribes to `sample_reviews` realtime; the real gaps fixed: `_sxrKasperViewBusy` no longer freezes the cross-client Kasper queue on any stray non-empty draft (drafts survive the rebuild via state and the caret is preserved, so a teammate's edit surfaces live); `loadSxrCards` now opens the per-client subscription in its catch branch so a first-load network blip does not leave the tab silent; the Kasper Credentials list background-refreshes on return so a peer add/edit/archive that landed while Kasper was on another subtab is not stale; and the credentials background reload defers while a picker dropdown is open.
  - **Credentials UX.** Instant password reveal (stopped awaiting the `log_reveal` audit round-trip; it now fires fire-and-forget); stable password-box width so the reveal/copy icons never jump between masked and revealed; removed the inline "Updated {time} by {person}" line for all roles (History keeps the full audit); removed the Bulk Import button from Kasper's view (the `bulk_import` Edge Function action is unchanged for onboarding/programmatic use); clients collapsed by default in the Kasper list; removed the redundant modal "Close" button.
  - **Multi-view test.** `qa/cc-multiview.js` drives add / edit / archive / reveal / hide / history across three simultaneous views (Kasper list, SMM per-client modal, Owner) on the Sidney TEST client with dummy `TESTPASS-…` values, asserting each mutation propagates live to the other two and that reveal stays device-local. The realtime TRANSPORT is emulated by invoking the app's real `_ccRevChanged()` handler in the other views (the callback Supabase wires to the subscription), because the live Credentials backend needs the staff passphrase which is deliberately kept out of this public repo — the live-on-Sidney-TEST run is owner-side. `npm test` 51/51 green; `no-hardcoded-colors` green; before/after light+dark screenshots committed under `qa/shots/` (dummy data only).
- Samples realtime STATUS propagation fix (branch `claude/samples-status-realtime-z0vs11`, extends the merged PR #705 realtime/focus work). Pure front-end + tests/docs — **no** production write-path, runtime flag, n8n workflow, Supabase schema, or write routing changed; works identically on the EF and n8n write paths (the change is in the browser's realtime *ingest*, which is path-agnostic); fully isolated from `_prod*`/`?prod=1`. Rollback = `git revert` the PR and let GitHub Pages redeploy (no flags to flip).
  - **P1 root cause.** On the SMM Samples "Sheet" (organizer) view, a realtime `sample_reviews` change from Kasper (request-change → Tweaks Needed) updated the note dot live but left the derived STATUS pill stale until a manual refresh. The samples clone had diverged from the calendar's merge: `_sxrMergeServerRows` blanket-KEPT the local row for 5 min after any local status write (`_sxrIsLocalStatusFresh`), folding in the server's new COMMENT (note updates) while keeping the stale local STATUS (pill frozen). A genuine newer change from another role was swallowed; a manual refresh worked only because a foreground load bypasses the merge.
  - **Fix (faithful port of the calendar).** Added `_sxrRecentSaveReconcile` + `_sxrIsStaleLinearRegress` + `_sxrReconcileHasGenuineTweak` (ports of `_calRecentSaveReconcile` et al. over the samples video/graphic schema) and wired them into `_sxrMergeServerRows`: within the local-fresh window it now adopts only the sub-statuses the server genuinely moved to a NEW value (a peer's Tweaks-Needed/approval, which always lands a change-request comment), while still refusing a bare stale Linear round-trip and a self-echo of our own write. Added `_sxrRepaintLiveStatus` — a focus-guarded (`_svPreserveFocus`, the PR-705 guard) in-place status-pill repaint via `_sxrUpdateCardStatusDisplay` — to the two realtime shortcut branches of `loadSxrCards` so the pill flips live even when the full render is skipped (no-visible-diff) or deferred (user mid-edit), mirroring the existing note-dot refresh.
  - **Tests.** New unit regression `test/samples-realtime-status-propagation.js` (17 checks: the P1 repro adopts live; self-echo/stale-regress refused; genuine client tweak adopted; in-flight kept; graphic component too; source-form wiring). New multi-view harness `qa/sxr-multiview.js` opens the SMM Sheet + client review + Kasper queue simultaneously against one in-memory `sample_reviews` store, drives real role handlers, emulates the realtime transport via the app's own `_sxrV2OnRealtimeChange`/`_sxrKasperLoadQueue` callbacks, and asserts the status pill updates live in the observers for T1 SMM-approve→Kasper, T2 Kasper request-change→SMM (P1), T3 Kasper approve→SMM+client, T4 client request-change→SMM — run TWICE (`sample_review_ef_clients={sidneylaruel}` and `{}`), asserting each write hit the expected host so BOTH paths + the fail-safe routing are exercised. Control (fix reverted via `git stash`) turns the P1 cell red ("still Kasper Approval"), proving the harness catches the bug. `npm test` 52/52 green; `parity` green; `no-hardcoded-colors` green; `git diff --check` clean; secret scan clean. master-test unit+parity lanes green; the live-backend scenario/visual lanes were not run from the sandbox (production reads are guarded) — the vision pass is recorded in `qa/visual/VISION_VERDICT.md` against the targeted before/after light+dark screenshots `qa/shots/sxr-status-{before,after}-{light,dark}.png` (dummy data, Sidney TEST). Only the Sidney TEST client and the Kasper reviewer role appear in the new tests; no real client names/data.
- Track A B0.5 full EF write-path rollout: enumerated `clients.active=true` and found 33 active slugs (full 33-slug list scrubbed from this public file 2026-07-11 — the authoritative roster is the live flag value in `syncview_runtime_flags`; historical value preserved in `flag_flips` ids 12/13/14). Set `calendar_upsert_ef_clients`, `sample_review_ef_clients`, and `settings_ef_clients` to that full active roster with `updated_by='codex-b05-full-rollout'`; readback confirmed `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"linear","graphics":"linear"}`, and `linear_inbound_enabled={"enabled":false}` were unchanged. `flag_flips` recorded ids 12, 13, and 14 for the three Track-A flags. Per-client rollback remains removing the affected slug from all three flags; full rollback is setting the relevant flag to `{"clients":[]}`.
- Track A reconciler checks after the full rollout: triggered `Samples ⇄ Linear status reconcile` run `28893976815` with `dry_run=true`, `cap=15`; it completed green, loaded the restored samples constants without the old `grabConst` crash, reported 2 live linked issues and corrections 0, and made no writes. Triggered `Linear ⇄ SyncView status reconcile` run `28894023451` with `dry_run=true`, `cap=15`; it completed green and reported corrections 0. Immediate post-flip event-ledger monitor found 0 `calendar_post_events`, 0 `sample_review_events`, and 0 error-like events since the flag timestamp.
- Samples duplicate-link/twins guard closeout after PR #717 merge: redeployed `sample-review-upsert` to Supabase project `uzltbbrjidmjwwfakwve` with JWT verification unchanged; no runtime flags, schema, n8n workflow, or frontend routing changed. TEST-client smoke re-saved existing Sidney sample row `sr_mqvenh27_jp85b`; the deployed function returned HTTP 200 with `ok:true`, and the row shape stayed unchanged (30 keys before, 30 after). Rollback remains per-client removal from `sample_review_ef_clients` or full fallback `{"clients":[]}`.
- Track B B3 stage 1 dark inbound scaffold: added and deployed the new `linear-inbound` Edge Function with Linear HMAC verification, ~60s replay rejection, flag-false dark acknowledgement, comment echo filtering, linkage maintenance, detect-only scaffolding, and B1 `deliverable_write` / `batch_write` RPC use for deliverable/batch writes. No Linear webhook was created or modified, `MJbMZ789B5ExZz9x` was not touched, and runtime flags were unchanged. Live smoke: unsigned dummy delivery returned 401; signed dummy delivery returned HTTP 200 with `ok:true` and `disabled:true`; `deliverable_events` count stayed 1825 before and after; `syncview_runtime_flags.linear_inbound_enabled` read back `{"enabled":false}`.
- Track B B3 stage 2 draft work: built reconciler v2 (`scripts/linear-deliverables-reconcile.js` + scheduled workflow) to compare live B1 deliverables against Linear across status, title, due date, assignee, parent, archive/delete markers, engine-tracked comments, card-linkage completeness, and Linear webhook enabled/resource state. Dry-run remains the default; healing is off unless `apply=true`, and future healing uses `deliverable_write`. Stage-1 flip-gate findings closed: verified the legacy `(via SyncView)` comment actor from Linear read-only and stored it in EF secrets; populated `LINEAR_STATE_UUID_MAP` in EF secrets from both teams' workflow states (public-safe slug table in `docs/audits/2026-07-07-linear-state-map.md`); added Production adapter filtering for `linear_raw.webhook_delete` / archive markers; verified direct `deliverable_events` summary inserts are permitted by writing summary event id 1842. Live v2 dry-run aggregate: 1 real diff, 14 unknown-assignee repair rows, 490 card-linkage gaps, 2 Linear webhooks checked, 0 disabled, 0 missing Issue/Comment resources, 0 healing writes. No runtime flags, Linear webhooks, n8n workflows, or production authority settings changed.
- Track B B3 stage 3 gap-zero work (code/docs on draft PR branch; live dormant B1 data repaired; no flags/webhooks/n8n changed; Linear read-only): dry-ran card-linkage backfill, then filled 278 resolvable additive card linkage slots (`calendar:video` 150, `calendar:graphic` 128) and wrote a reconcile summary event. Remaining raw card-linkage residue has 0 resolvable writes left and is fully classified: 196 archive-only historical links, 4 duplicate-live-link residues, 12 unresolved historical links. Assignee repair dry-run found 14 repair rows from 2 distinct video-editor identities; added 2 inactive attribution rows (no existing rows overwritten), then healed 14 assignee diffs through `deliverable_write`. Two status diffs were healed through `deliverable_write` under cap 2. Final reconciler v2 dry-run wrote summary event id 1867: 1,058 deliverables checked, 0 real diffs, 0 unknown-assignee repairs, raw linkage residue 212 / resolvable writes 0, webhooks checked 2 with 0 disabled and 0 missing Issue/Comment resources. Rollback for this dormant-data repair is targeted reversal from the private artifacts; workflow rollback remains disabling/reverting the v2 workflow.
- Track B B3 stage 4 live inbound flip: redeployed `linear-inbound` with comma-separated signing-secret support and stored the two new Linear webhook secrets only in Supabase Edge Function secrets. Set `syncview_runtime_flags.linear_inbound_enabled` to `{"enabled":true}` with `updated_by='codex-b3-flip'`; readback matched and `flag_flips` recorded id 15. Ran the operational comment catch-up through `deliverable_write`: dry-run planned 489 comment raw updates, apply attempted 489, and the post-apply dry-run planned 0. Existing n8n webhooks/workflow `MJbMZ789B5ExZz9x` were not changed.
- Track B B3 stage 4 TEST verification: on `sidneylaruel` issue `VID-12612`, a temporary Linear state flip wrote mirror events 2366 and 2367 and reverted the deliverable back to `tweak`; a temporary Linear comment wrote mirror event 2368 with explicit `is_tweak:false`, author present, role `editor`, audience `internal`, then was deleted and the TEST deliverable thread was restored to 0 comments. A legacy `linear-add-comment` TEST probe created a `(via SyncView)` Linear comment through the old path, and inbound imported 0 `mirror_in_comment_add` events for it; the Linear comment was deleted and the TEST row was restored. Final TEST readback: original Linear state restored, deliverable status `tweak`, comment count 0, no `last_linear_comment`.
- Track B B3 stage 4 reconciler exit evidence: first post-flip dry-run wrote summary event 2372 and found 8 parent-metadata diffs; these were catch-up drift only, patched through `deliverable_write` under cap 10 in apply summary event 2381. Two subsequent dry-runs wrote summary events 2382 and 2383 with 1,058 deliverables checked, 0 real diffs, 0 unknown-assignee repairs, `linkage_actionable=0`, 209 explained raw linkage residue, and webhook probe 4 checked / 4 enabled / 0 disabled / 0 missing Issue or Comment resources. Optional duplicate-live-link cleanup was left untouched because the private card metadata did not identify a clearly stale twin.
- Monitoring hardening PR: re-enabled GitHub Actions workflows `Calendar E2E (nightly)` and `Samples E2E (nightly)` from `disabled_manually` to `active`, then ran both manually on branch `codex/monitoring-hardening-doc-sweep`. Calendar run `28907864979` completed green. Initial Samples run `28907865555` exposed a stale tree-lane harness timing failure in `graphic__smm_approve__kasper_request__resolve_back`; the focused TEST-client path passed after the harness waited for the rebuilt Kasper action button to become clickable. Corrected Samples run `28909416704` completed green, including the new independent `production-preview-smoke` job. Added `docs/MONITORING.md`, linked it from `ROLLBACK.md`, and staged fail-soft `linear-inbound` anomaly alert forwarding for unmapped states / unknown assignees with one-hour per-type throttling and identifier-only payloads. Supabase secret metadata does not currently include `SLACK_ALERT_WEBHOOK`, so live Slack delivery requires adding that secret before deploying the alert code. No runtime flags, schema, n8n workflow logic, auth setting, or production data path was changed.
- Monitoring follow-through n8n snapshot and relay: exported private pre-change n8n backups for the relevant monitoring/reconcile workflows; public-safe snapshot stub is `n8n-backups/2026-07-07-monitoring-follow-through.md`. Created additive n8n workflow `Tfhc3vebZyG6obOg` (`SyncView Edge Alert Relay -> DM Sidney`) using the existing SyncView Bot Slack credential. Synthetic public-safe relay test execution `219237` completed successfully. No existing workflow logic was edited.
- Monitoring follow-through pager: created additive n8n workflow `qllIDZPkdNAPRj0b` (`SyncView Monitoring Pager + Reconciler V2 Trigger`), active on a 15-minute schedule. It dispatches `linear-deliverables-reconcile.yml` with `apply=false`, reads latest v2/mirror evidence with the anon key, checks calendar and samples reconciler run freshness, and throttles each alert condition to one DM/hour. First scheduled tick execution `219270` completed successfully and dispatched GitHub run `28913726658`, which completed green and wrote v2 summary event `2398`: 1,058 checked, `diff_count=4`, `repair_list_size=0`, `linkage_actionable=0`. The pager produced 2 alert items and the Slack DM node succeeded for both, proving the alert branch is live. The Supabase `SLACK_ALERT_WEBHOOK` secret + `linear-inbound` redeploy were not performed because deploying the draft monitoring branch to the live Edge Function was blocked pending merge or explicit owner approval. No schema, flags, auth setting, Linear webhook, or existing n8n workflow logic changed.
- Track B B3 mirror drift diagnosis/heal: v2 detail dry-run event `2402` identified four persistent real diffs, all `parent` field mismatches (`GRA-6391`, `VID-12547`, `VID-12551`, `VID-12808`; Linear parent present, Supabase mirrored raw parent null). Classified as an inbound gap: Issue update webhook payloads can omit `parent`, and `linear-inbound` was replacing `linear_raw.issue` wholesale, erasing a previously mirrored parent. Healed with v2 `apply=true`, `cap=15` through `deliverable_write`; apply summary event `2407` attempted 4 and skipped 0. Follow-up dry-run summary event `2408` returned to `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`. Staged a source fix so `mergeLinearRaw` preserves the previous parent when the incoming Issue webhook omits `parent`, while still allowing explicit `parent:null` to clear it. Verification: `node test/linear-inbound-source.js`, `node test/linear-deliverables-reconcile.js`, and `npm.cmd test` (58 suites) passed. No Linear write, flag, schema, webhook, auth, or existing n8n workflow change.
- Deploy finish for merged monitoring PR #725: synced `main` to merge commit `98f2a87273190536564b7d62a2c633dc8f08e055`, verified the merged `linear-inbound` parent-preservation fix, set Supabase `SLACK_ALERT_WEBHOOK` to the n8n Edge Alert Relay without printing the URL, and redeployed `linear-inbound` to project `uzltbbrjidmjwwfakwve`; Supabase returned `Deployed Functions`. Post-deploy v2 dry-run event `2410` was zero, and the next two n8n pager cadence ticks held zero: pager executions `219524` / `219565`, v2 summary events `2411` / `2412`, each with `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`. A forged signed-fixture alert proof was skipped because this local process does not have `LINEAR_INBOUND_SIGNING_SECRET`, and embedding/reconstructing signing material in a command was blocked to avoid exposing secrets. No flag, schema, n8n-logic, Linear webhook, or Linear data change.
- EF alert proof via real TEST webhook: temporarily assigned TEST issue `VID-12612` to an active Linear user confirmed absent from `team_members`, causing a genuinely signed Linear webhook to exercise `linear-inbound`'s `unknown_assignee` anomaly path. Edge Alert Relay execution `219602` completed successfully with `Receive Edge Alert`, `Sanitize Alert`, and `DM Sidney` all OK; payload markers confirmed `unknown_assignee` and alert text remained identifier/team-only. Restored `VID-12612` to its prior assignee, verified Linear readback and Supabase mirror assignment matched the restored mapped member, then ran v2 dry-run summary event `2418`: `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`, webhooks `4/4` enabled. No flag, schema, n8n-logic, or production data change.
- Monitoring pager tuning: privately exported n8n workflow `qllIDZPkdNAPRj0b` before editing (public-safe stub `n8n-backups/2026-07-08-pager-tuning.md`), then updated the active workflow additively. Chosen strategy for calendar/samples staleness: dispatch both card reconcilers from the pager every 15 min with schedule-equivalent healing (`dry_run=false`, cap 15), rather than loosening overnight staleness thresholds, so the 2h stale alert remains meaningful. v2 non-zero diff paging now requires two distinct consecutive non-zero summaries or a growing non-zero sequence; a single non-zero v2 tick is logged quietly in the workflow execution. First tuned tick `222743` succeeded and dispatched v2 run `28952725822`, calendar run `28952727925`, and samples run `28952732844`; it also exposed a completed-only samples staleness false page while the newly dispatched samples run was still completing. Took a second private snapshot and corrected the pager to treat a fresh queued/running card reconciler dispatch as healthy for staleness. Final readback: workflow active, 10 nodes, calendar/samples dispatch nodes present, quiet non-zero logic present, pending-fresh stale guard present. No flags, schema, auth, Linear webhook, or production n8n business workflow changed.
- Monitoring pager tuning verification: post-fix scheduled tick `222824` completed successfully with all dispatch/check nodes OK and `Check Pager Conditions` outputting 0 alert items. It dispatched successful workflow runs for v2 (`28953791214`), calendar (`28953792681`), and samples (`28953796285`). This confirms the card-reconciler dispatch strategy is live and the stale false page from tick `222743` is fixed.

## 2026-07-09

- Production project-board scope-control polish on PR #765: the Projects view no longer renders `All projects` as a clickable button with no action; it is now a static active-scope label, matching the board's current context without presenting a decorative dead control. Tightened the interaction-inventory allowlist so this no-op button shape is no longer accepted, added behavior coverage for the static board scope label, and hardened the inventory's sub-issue click path to scroll the target row into view before clicking on CI. Verification passed: `git diff --check`, `behav-wired.js` 163/163 guard mode, `prod-interaction-inventory.js`, `prod-structure-subset.js`, `npm run test:prod-review`, project-board screenshot review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 371.7s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production delivered-file link polish on PR #765: issue and sub-issue detail pages now label delivered-file URLs as concise resource links (`Open folder`, `Open file`, `Open document`, `Open Frame.io`, or `Open link`) instead of exposing long raw Drive/Dropbox/Frame URLs in the body. The original URL remains in the anchor `href` and title, so this is visual/detail polish only. The review packet project-detail screenshot also now opens a project that matches active filters instead of showing an unrelated filtered-empty page. Added behavior coverage so delivered-file links keep a clean visible label while preserving the destination URL. Verification passed: `git diff --check`, `behav-wired.js` 162/162 guard mode, `prod-structure-subset.js`, `npm run test:prod-review`, sub-issue/project-detail screenshot review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 373.1s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production detail activity empty-state polish on PR #765: issue and sub-issue details no longer show unresolved skeleton bars when migrated activity rows are absent or still being fetched. The read-only preview now shows a subtle empty activity message immediately, then replaces it with compact activity rows if events arrive. Added structure and behavior coverage so detail activity cannot regress back to indefinite placeholder bars. Verification passed: `git diff --check`, `prod-structure-subset.js`, `behav-wired.js` 161/161 guard mode, `npm run test:prod-review`, sub-issue screenshot review, `npm test` 64/64 unit suites, `npm run test:prod-review:validate`, and full `npm run test:prod-polish` in 374.8s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-board filtered-copy polish on PR #765: project board cards now label filtered counts as matching issue(s), active board filters hide empty columns when matching projects exist, and true no-match filtered boards say `No matching projects` instead of the generic empty state. Added behavior and layout coverage so active project-board filters cannot make card counts look like total project size or clip the final visible column at review width. Verification passed: `git diff --check`, `behav-wired.js` 160/160 guard mode, `npm run test:prod-layout`, `npm run test:prod-review`, project-board screenshot review, `npm test` 64/64 unit suites, `npm run test:prod-review:validate`, and full `npm run test:prod-polish` in 347.8s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production issue-description Markdown polish on PR #765: migrated Linear briefs now render common Markdown structure instead of raw markers in issue detail bodies, including section headings, horizontal rules, bullet rows, bold labels, code spans, normal links, and `<url>` markdown links inside resource lines. The renderer also normalizes common malformed import scars such as doubled leading stars and orphan bold markers before resource links, and description links now use Production link styling instead of browser-default blue. Added renderer coverage to `prod-structure-subset.js` and `behav-wired.js`; verification passed: `git diff --check`, `npm test` 64/64 unit suites, `prod-review-packet.js`, `prod-review-packet-validate.js`, refreshed parent-detail screenshot review, and full `npm run test:prod-polish` in 351.1s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-detail filter-empty polish on PR #765: project details now distinguish visible filtered rows from total project rows (`0 of N`) and show an inline explanation plus `Clear filters` action when active filters hide all existing project issues. Added a layout regression check so project detail cannot silently show an empty issue list when filters are the cause. Verification passed: `git diff --check`, `npm test` 64/64 unit suites, `prod-review-packet.js`, `prod-review-packet-validate.js`, project-detail screenshot review, and full `npm run test:prod-polish` in 378.7s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production polish automation gate: added `npm run test:prod-polish` as the single PR-ready gate for `?prod=1` UI polishing. The gate runs Production boot/loading, structure, interaction inventory, accessibility/focus, layout clipping, wired behavior, and pixel parity checks. Added GitHub Actions workflow `production-polish-gate.yml`, an owner-feedback issue template, a Production-aware PR checklist, `docs/PRODUCTION_POLISH_AUTOMATION.md`, `AGENTS.md`, and `.github/copilot-instructions.md` so future natural-language polish feedback can become AI-ready PRs with repeatable proof. Added `npm run test:prod-review` / `prod-review-packet.js` so CI also uploads a compact screenshot packet with a browsable gallery, Markdown manifest, review checklist, and machine-readable JSON manifest for human/agent review, then prints the Markdown manifest plus checklist in the job summary; `npm run test:prod-review:validate` rejects incomplete packets before upload. GitHub Pages remains reserved for the live app; the workflow has a guarded Argos upload hook that skips until `ARGOS_TOKEN` is configured. The workflow now also runs on `main` at 09:17 UTC Monday through Friday after merge and cancels superseded in-progress runs for the same ref. The new accessibility check found and fixed a real frontend gap: focused Production buttons now keep native Enter/Space activation, and Filter/Display icon buttons have accessible names. Verification passed: `prod-boot-budget.js`, `prod-a11y-focus.js`, `prod-layout-polish.js`, `prod-review-packet.js` with 10 screenshots, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 158/158 guard mode, `pixel-wired.js` light/dark, and full `npm run test:prod-polish` in 315.5s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production tab selection/filter/action polish on PR #763: fixed project card selection so mouse selection/deselection no longer leaves a clipped blue focus ring, tightened project issue-row metadata so due/avatar/created chips do not get cut off on hover, replaced the selected-issues Actions menu with a searchable Linear-style command menu (`Assign to`, `Change status`, `Move to project`, `Copy issue ID`, `Change due date`, `Delete issue`), made `Copy issue ID` a real clipboard action, constrained filter pills for combined status/client filters, and deduped visible issue rows by ID before rendering. The detail composer remains intentionally read-only in this preview and still shows the guarded disabled state. Verification passed after rebasing onto current `origin/main`: `git diff --check`, `npm test` 64/64 unit suites, `prod-structure-subset.js`, `behav-wired.js` 158/158 guard mode, `prod-interaction-inventory.js`, and `pixel-wired.js` light/dark; targeted screenshots for selected/deselected project cards, the bulk command menu, and combined filters were reviewed locally. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-detail toolbar polish after owner screenshot feedback: removed the unclear Open/Closed/All issues tabs from project detail, moved Project details to the right-side toolbar next to Filter, made project Display grouping (`Status`, `Client`, `Assignee`) regroup the project issue rows, and made `Show sub-issues` hide/show child rows in project detail. Follow-up owner feedback removed the Production workspace menu entirely, added a Production-specific boot skeleton so refresh no longer flashes the Analytics loader, cleaned grouped-project hover bands, clears stale tooltips on navigation, removes the child ID from sub-issue breadcrumbs, and suppresses the browser context menu inside Production. Verification passed: `git diff --check`, `prod-readonly-smoke.js`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 156/156 guard mode, `pixel-wired.js` light/dark, and `npm test` 63/63 unit suites. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production tab Linear-detail polish after PR #756: applied owner screenshot feedback to make issue/sub-issue detail pages feel closer to Linear. Issue detail bodies are centered in the available pane; child details show a body-level `Sub-issue of` row with parent progress and project context; parent sub-issue rows are title-first, omit the child issue ID, and show project/due/assignee metadata; guarded add-sub-issue affordances are visible; and activity events are compact one-line rows. Updated `prod-interaction-inventory.js`, `prod-structure-subset.js`, and `behav-wired.js` coverage plus audit/parity docs. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production tab feedback polish on PR #751: applied owner feedback for finished-app feel. Production detail transitions now reset to the top when opening a deliverable, batch, project, or sub-issue; project board cards use the pointer cursor instead of the drag/grab cursor; and detail breadcrumbs label parent/current hierarchy with `Issue` and `Sub-issue`. The inventory gate now covers sub-issue scroll reset, breadcrumb context, and project-card cursor. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production tab focused human/vision follow-up: reviewed generated desktop light/dark, core state, popover/crop, and phone screenshots for the wired `?prod=1` surface, closing the pending vision-review note in the automated run below. Fixed two phone-width polish defects found in that pass: the global header now gives the app nav its own scrollable row so the active Production tab no longer clips/collides with the theme toggle, and Production detail breadcrumbs keep issue IDs/project labels on one line while truncating trailing titles. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production tab foundation hardening started from latest `main` commit `bcdc0cfe95864a771cfff7cf92dc8c633cde0d78` on branch `codex/prod-foundation-audit-2026-07-09`. The pass treated `?prod=1` as a finished-quality read-only product surface: every visible interaction must navigate, work locally, open guarded chrome, or be clearly disabled. Baseline verification: `npm test` passed 58/58 suites; `prod-readonly-smoke.js` passed; `prod-structure-subset.js` passed. `behav-wired.js` initially failed `projectToolbarMenusAndDetailsToggle` because Escape inside a project Filter/Display popover also bubbled to the page-level Production key handler and navigated from project detail back to board. Fixed the overlay Escape handler to stop propagation after clearing the popover. Post-fix verification: `behav-wired.js` passed 156/156 guard-mode assertions, new `prod-interaction-inventory.js` passed its sampled list/detail/board/project control sweep plus right-click/hover/no-write checks, and `pixel-wired.js` passed light/dark. `qa/master.js --profile=full` timed out locally before a final result; direct fast master initially exposed a Windows runner issue (`python3` unavailable), then passed with a `python -m http.server 8000` local server and `--no-server`: unit, parity, probes, scenarios, and visual capture green, with the normal human vision-review step still pending. Added `docs/audits/2026-07-09-production-foundation-audit.md`, updated the wired parity/test docs, and updated the Production rollback row. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.

## 2026-07-10

- Production desktop project-detail row polish on PR #765: regenerated the Production review packet and found project-detail issue rows with parent trails squeezed into the same line as the issue title. Project-detail rows now render the primary title on the main line and the parent issue trail as a subtle secondary line, while right-side due/assignee/created metadata remains fixed and unclipped. Added layout coverage so parent trails must remain inside the row as a secondary line. Verification passed: `git diff --check`, `prod-layout-polish.js`, `prod-structure-subset.js`, `npm run test:prod-review`, focused project-detail screenshot review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 370.5s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production desktop project-board/review-packet polish on PR #765: the Production review packet now records per-screenshot Production state and rejects stale state in `prod-review-packet-validate.js`; the clean Project board and Project detail screenshots must be unfiltered baselines, while the Combined filters screenshot must prove active status/client filters. This caught the board screenshot inheriting filters from the prior screenshot. The board itself now gives non-empty status columns more width and narrows empty columns, keeping all six columns visible without collapsing project card titles to one-letter fragments. Verification passed: `git diff --check`, `prod-layout-polish.js`, `prod-structure-subset.js`, `npm run test:prod-review`, focused project-board screenshot review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 370.6s. No Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production parent-detail review-packet hardening on PR #765: the review packet no longer accepts a parent-detail screenshot where a long migrated description pushes the promised sub-issue rows and activity section below the first desktop viewport. The generator now chooses a compact parent with visible sub-issues/activity, pairs the sub-issue screenshot to the same parent, records parent-detail evidence in `review-manifest.json`, and the validator rejects packets without visible sub-issue rows, guarded add-sub-issue, and activity evidence. Verification passed: `git diff --check`, `npm run test:prod-review`, focused parent/sub-issue screenshot review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 372.3s. No app runtime behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production selected-actions/filter review-packet hardening on PR #765: the review packet now records explicit evidence for the selected-issue Actions screenshot and Combined filters screenshot instead of relying only on image inspection. `review-manifest.json` must prove the floating action bar, searchable command menu, exact selected-issue command labels, two selected rows, status/client filter pills, and deduped visible filtered rows; `prod-review-packet-validate.js` rejects packets missing that evidence. Verification passed: `git diff --check`, `npm run test:prod-review`, evidence spot-check in `review-manifest.json`, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 369.7s. No app runtime behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-detail team-scope polish on PR #765: regenerated the Production review packet and found that opening a mixed-team project from the Video Projects board could render a project detail labeled `All teams project`, with issue counts broader than the Video card context. Project detail now uses the active team scope for rows, counts, breadcrumb team, and the `Video project` / `Graphics project` detail label. Added behavior coverage for mixed-team project detail scope, updated the team-crumb navigation expectation, and added review-packet evidence proving the Video project screenshot contains only Video rows. Verification passed: `git diff --check`, `prod-structure-subset.js`, `prod-layout-polish.js`, `behav-wired.js` 164/164 guard mode, `npm run test:prod-review`, focused project-detail screenshot/evidence review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 370.5s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-board empty-column polish on PR #765: review-packet inspection found empty project-board lanes still showing add/options controls even though they had no cards and no meaningful read-only action. Empty board columns now render as static lanes with title, count, collapse affordance, and empty copy only; populated columns keep the guarded add/options controls. Added behavior, structure, interaction-inventory, layout, and review-packet validator coverage so empty columns cannot regain fake action controls. Verification passed: `git diff --check`, `behav-wired.js` 165/165 guard mode, `prod-structure-subset.js`, `prod-layout-polish.js`, `prod-interaction-inventory.js`, `npm run test:prod-review`, focused project-board screenshot/evidence review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 370.4s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production group-header fake-action cleanup on PR #765: review-packet inspection found issue-list and project-detail group headers still showing small add buttons that could not create anything in the read-only preview. Removed those no-op group-header plus controls while keeping the useful guarded add-sub-issue affordance on issue detail and guarded add/options controls on populated project-board columns. Added structure and review-packet evidence so desktop list and project-detail screenshots must record zero fake group-header add controls. A remote CI retry also exposed a stale Playwright locator in the interaction-inventory sub-issue navigation path; the test now clicks the current DOM row instead of holding a detachable locator. Verification passed: `git diff --check`, `prod-structure-subset.js`, `behav-wired.js` 165/165 guard mode, `prod-layout-polish.js`, `prod-interaction-inventory.js`, `npm run test:prod-review`, focused desktop-list/project-detail screenshot and evidence review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 372.4s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project Display count polish on PR #765: a deeper Display-state screenshot found that turning off Show sub-issues in project detail hid child rows correctly, but the right-side Issues card still reported the total project count. The side Issues count now follows the visible project rows, so the project header, grouped list, and right rail stay consistent when Display options change. Added behavior and structure coverage for the hidden-subissues count state and reviewed a focused screenshot showing `Issues 2` in both the main list and side rail. Verification passed: `git diff --check`, `prod-structure-subset.js`, `behav-wired.js` 165/165 guard mode, `prod-layout-polish.js`, `prod-interaction-inventory.js`, `npm run test:prod-review`, focused Display-off screenshot/evidence review, `npm run test:prod-review:validate`, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 374.0s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production Argos visual-diff export on PR #765: researched Argos primary docs and tightened the existing optional hook into a validated desktop visual-diff lane. `prod-argos-export.js` now validates the Production review packet before copying desktop/dark PNGs plus companion `.argos.json` metadata into `.codex-tmp/prod-argos-snapshots`; the GitHub workflow prepares that folder after packet validation, uploads it as a CI artifact, and sends it to Argos with build name `production-desktop` only when `ARGOS_TOKEN` exists. Verification passed: `git diff --check`, `npm run test:prod-review`, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots with 8 metadata files, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 384.7s. No app runtime behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-board header chrome cleanup on PR #765: a fresh desktop review-packet pass found populated Project board columns still showing disabled add/options icons in the column headers. Those read-only no-op controls are now removed from all Project board column headers; headers keep only collapse, status, title, and count, while card-level controls remain where there is card context. Updated structure, layout, interaction-inventory, behavior, and review-packet validation so the Project board screenshot must record zero column-header add/options controls across empty and populated columns. Verification passed: `git diff --check`, `prod-structure-subset.js`, `prod-layout-polish.js`, `prod-interaction-inventory.js`, `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, focused Project board screenshot/evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 374.3s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production topbar fake-action cleanup on PR #765: a fresh desktop review-packet pass found list/project/detail topbars still exposing disabled favorite and notification icons that looked like real app chrome but had no useful read-only action. Removed those fake topbar controls from the Production list, project detail, issue detail, and pre-paint skeleton while keeping the useful context menu dots. Added behavior, structure, interaction-inventory, and review-packet validation so desktop list, project detail, and parent detail screenshots must record zero fake topbar favorite/notification controls. Verification passed: `git diff --check`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, focused desktop-list/project-detail/parent-detail screenshot and evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 369.9s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production parent sub-issue affordance polish on PR #765: review-packet inspection found parent issue detail pages still represented Add sub-issues as a tiny unlabeled plus in the Sub-issues header, which looked like another ambiguous read-only icon. Parent details now show a subtle `+ Add sub-issues` text affordance while keeping the same guarded read-only behavior; leaf issue details already used this explicit label. Added structure and review-packet validation so the parent-detail screenshot must record visible `Add sub-issues` text. Verification passed: `git diff --check`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, focused parent/sub-issue screenshot and evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 365.1s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production selected-actions plural copy polish on PR #765: review-packet inspection found the selected-issue command menu showing singular `Copy issue ID` and `Delete issue` while two rows were selected, even though the clipboard path already copied multiple IDs and toasted `issue IDs copied`. The selected-actions menu now pluralizes those command labels when multiple issues are selected (`Copy issue IDs`, `Delete issues`) and keeps singular labels for a one-item selection. Updated behavior, structure, and review-packet validation so the selected-actions screenshot evidence must match the multi-select labels. Verification passed: `git diff --check`, `behav-wired.js` 165/165 guard mode, `prod-structure-subset.js`, `npm run test:prod-review`, focused selected-actions screenshot/evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 362.3s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project context-menu picker polish on PR #765: project-card right-click menus no longer show `Change status`, `Set lead`, and `Set target` as fake disabled mutation rows. Those commands now open the same guarded read-only project pickers as the visible card/detail controls, while `Copy link` remains active. Added behavior, structure, and interaction-inventory coverage so the board context menu must expose active status/lead/target picker actions and still toast `Preview - read-only` only after selecting a guarded value. The remote gate also exposed a slow-runner flake where the shared Production test opener saw the shell before inner content; `prod-test-utils.js` now accepts all valid Production content states and retries the opener once with diagnostics. Verification passed: `git diff --check`, `prod-a11y-focus.js`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 363.3s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production detail properties scaffold cleanup on PR #765: fresh review-packet inspection found right-side issue/project Properties cards still showing a literal `Controls disabled` pill, which read like internal mockup scaffolding even though the actual status/assignee/due/project fields already open guarded read-only pickers. Removed the visible scaffold from issue, sub-issue, and project detail properties while keeping the guarded picker/composer behavior. Updated structure/source coverage so the old `detail-controls`/`project-controls` disabled pill cannot return and visible write affordances remain guarded without scaffold chrome. Also hardened the `projectDetailKeepsTeamScope` behavior assertion to choose expected Video rows directly and emit diagnostics instead of depending on mixed-project object order on CI. Verification passed: `git diff --check`, `production-preview-source.js`, `prod-structure-subset.js`, `prod-a11y-focus.js`, `prod-interaction-inventory.js`, `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 327.3s. Focused review screenshots confirmed the Properties panels no longer show `Controls disabled`. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-detail empty due polish on PR #765: fresh review-packet inspection found project-detail issue rows with no due date still rendered as icon-only calendar pills, which looked like a half-rendered control in the roomier detail layout. Those rows now show a readable `Add date` pill while keeping the same guarded read-only due-date picker. Updated behavior, structure, and review-packet validation so project-detail empty due controls must have visible `Add date` labels and cannot regress to icon-only chrome. Verification passed: `prod-structure-subset.js`, `behav-wired.js` 165/165 guard mode, `prod-interaction-inventory.js`, `npm run test:prod-review`, focused project-detail screenshot review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 385.0s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-board lane width polish on PR #769: owner screenshot review found the dark Projects board still laid out empty `Planned`, `Paused`, `Completed`, and `Canceled` lanes as narrow strips beside wider populated lanes. The Projects board now gives empty and populated status lanes the same readable desktop width and relies on horizontal board scrolling when all lanes cannot fit. Updated layout and review-packet validation so the Project board screenshot records equal readable column widths and rejects the old narrow empty-lane layout. Verification passed: `prod-layout-polish.js`, `prod-structure-subset.js`, `npm run test:prod-review`, focused light/dark Project board screenshot review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 370.3s on the latest `origin/main` branch. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production selected-actions command-menu polish on follow-up Production loop: review-packet inspection found the selected-issue `Actions` command menu and the selected-actions bar both reading as active stacked surfaces. The selected-actions bar now recedes while its searchable command menu is open, then returns when the layer closes; selection state and guarded read-only commands are unchanged. Updated behavior and review-packet validation so the selected-actions screenshot must record the receded action bar and the guard must clean up after a read-only command closes the menu. Verification passed: `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, focused selected-actions screenshot review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 310.3s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production selected-actions Linear menu correction on follow-up Production loop: owner screenshot feedback showed the receded selected-actions treatment did not match Linear and that hover-open picker submenus blocked too much of the UI. The selected-actions `Actions` menu now opens as a wider centered Linear-style command panel above the action bar, keeps the action bar visible, shows a `2 issues`-style header with Ask Linear hint, and only opens status/assignee/project/due pickers on click or keyboard Enter instead of hover. Updated behavior, structure, pixel, and review-packet validation so hover cannot open a blocking picker and the screenshot must prove centered placement. Verification passed: `behav-wired.js` 165/165 guard mode, `prod-structure-subset.js`, `npm run test:prod-review`, focused selected-actions screenshot review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `pixel-wired.js` light/dark, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 310.9s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-board scope label polish on follow-up Production loop: review-packet inspection found the Projects board `All projects` scope still looked like an active pill even though it is static context, while the adjacent team pill is the real control. `All projects` now renders as quiet non-interactive text with default cursor, no pointer events, and no active background; the `Video` team pill remains clickable. Updated behavior and review-packet validation so the board screenshot records `All projects` as a non-interactive scope label instead of a fake button. Verification passed: `behav-wired.js` 165/165 guard mode, `npm run test:prod-review`, focused Project board screenshot review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 388.4s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production project-card empty-target polish on follow-up Production loop: review-packet inspection found the Projects board repeating `No target` on every untargeted project card, which made real cards read like scaffold copy. Empty project-card target controls are now compact icon-only controls with the same guarded target picker; cards with a real target still show the date. Updated review-packet evidence and validation so empty target controls must stay icon-only instead of regressing to repeated labels. Verification passed: `git diff --check`, `behav-wired.js` 165/165 guard mode, `prod-structure-subset.js`, `prod-layout-polish.js`, `npm run test:prod-review`, focused Project board screenshot/evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 378.7s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production detail empty-copy polish on follow-up Production loop: review-packet inspection found issue, sub-issue, and project detail pages still exposing internal migration wording such as `No brief on this migrated row` and `No activity events on this migrated row yet`. Empty detail descriptions now use concise finished copy (`No description.` / `No project description.`), and empty activity states use `No activity yet.` Updated behavior and review-packet validation so project, parent, and sub-issue detail screenshots reject `migrated row` scaffold copy. Verification passed: `behav-wired.js` 165/165 guard mode, `prod-structure-subset.js`, `prod-layout-polish.js`, `npm run test:prod-review`, focused project/parent/sub-issue screenshot and evidence review, `npm run test:prod-review:validate`, `npm run test:prod-argos:prepare` exported 8 desktop/dark snapshots, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 348.7s. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Production sidebar count polish on follow-up Production loop: owner feedback called out the large issue totals beside team sidebar rows (`Video > Issues 692`, `Graphics > Issues 442`). The team sidebar now renders `Issues` without numeric badges while keeping the same navigation behavior, and the dead sidebar count renderer/helper code was removed. Updated structure coverage so team issue nav rows cannot regain `.prod-nav-count` badges. Verification passed with the same final gate: `git diff --check`, `prod-structure-subset.js`, screenshot review, `npm run test:prod-review:validate`, `npm test` 65/65 unit suites, and full `npm run test:prod-polish` in 533.7s after the final cleanup. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed.
- Repository reorganization (owner-approved plan, no behavior change): moved 43 root/docs markdown files into `docs/features/`, `docs/ops/`, `docs/independence/`, `docs/testing/`, and `docs/archive/` (root keeps `README.md`, `AGENTS.md`, `ROLLBACK.md`, `EXECUTION_LOG.md`); moved the 14 root `*-migration.sql` files into `migrations/` and added `migrations/README.md`; deleted verified-orphaned QA files (six `golden_*` probes superseded by the scenario library, `twin_*` generation, one-shot bug verifiers, ad-hoc screenshot scripts, 22 committed `qa/shots/*.png`, and the byte-identical `docs/syncview-design/linear-tokens-visual.html`), each confirmed unreachable from every runner, manifest, CI workflow, npm script, skill, and require chain; relocated the 86 design-probe JSONs to `docs/syncview-design/probe-data/`, the five design-machine test originals to `docs/syncview-design/tests/design-machine-originals/`, and stale QA reports to `docs/archive/qa/`; kept exploratory probes `p00`–`p27`/`p_g2*` and the `scripts/` one-shots (Track B still in flight). All cross-references in living files updated in the same commits, including two unit tests that read moved specs at runtime; `n8n-backups/`, `docs/audits/`, archived docs, and prior log entries left untouched as point-in-time records; `supabase/functions/` deliberately untouched to avoid a redeploy. Added `REPO_MAP.md` with CI enforcement (`test/repo-map-sync.js`, runs in `npm test` on every push) plus a Stop-hook reminder. Verification after each phase: `npm test` all 65 (now 66) unit suites green. No app write behavior, Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed; runtime asset folders and Pages files stayed at root.
- Production tab boot payload hotfix (PR #779, merged 2026-07-10, deployed via Pages): the post-import Production boot fetch weighed ~20.6 MB because every row shipped its full `linear_raw` blob, and one heavy page intermittently returned HTTP 500 which blanked the tab. The boot select now projects only the tiny archive/delete marker fields the live-row filter needs, `brief` moved to a background/detail load (command palette warms it on open), detail opens lazy-load that row's full `linear_raw`, `_prodRestRows` fetches later pages in small parallel batches, and a per-page retry with backoff (3 attempts, 429/5xx only) absorbs transient failures instead of erroring the tab. Independently measured against live Supabase: boot payload 20.59 MB → 3.34 MB, page times ~0.5–2 s, visibility parity exact (0 per-client top-level count differences). CI production-polish gate passed; boot budget now measures the visible Production skeleton while all other lanes still wait for real rows. No flags, n8n, Linear, schema, or backend wiring changed.
- Track B finished-work history import (pilot 2026-07-10 ~20:26 UTC, full rollout ~21:07 UTC): imported the finished-but-still-live Linear issues the B1 backfill deliberately skipped, so the Production mirror shows the same world as live Linear. Pilot: 680 deliverables + 203 batches across two clients; full rollout: 2,507 deliverables + 597 batches across 26 more (5 clients had nothing importable after idempotency/safety exclusions). Totals: 3,187 deliverables + 800 batches, grand total 4,323 deliverables / 1,014 batches. Writes only via `batch_write`/`deliverable_write` RPCs with `source='backfill'`; every row tagged `created_by='history-backfill-2026-07-10'` for one-command rollback (recorded in the ROLLBACK.md Live State row). Independent verification: tag counts exact per client, 0 orphaned batch references, statuses in-vocabulary and faithful to live Linear on spot-check, ledger accounting exact (one event per created row), reconciler v2 diff 0 / repair 0 mid-import and after, no Linear 429s. No flags, n8n, Linear mutations, card-linkage writes, or `mirror_outbox` writes.
- Track B B3 pending card-linkage drain: ran v2 dry-run summary event `7744` before the drain (`diff_count=0`, `repair_list_size=0`, `linkage_actionable=31`, 31 resolvable card-slot fills planned). Applied the sanctioned B3 linkage backfill path under cap 50; it filled 31 additive card-to-deliverable slots (`calendar:video` 18, `calendar:graphic` 12, `samples:graphic` 1) and wrote a reconcile ledger event. Follow-up v2 dry-run summary event `7746` checked 4,323 deliverables and confirmed `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`; the remaining 208 raw linkage rows are classified residue (`duplicate_live_link` 6, `archive_only` 194, `unresolved_deliverable` 8). No Linear mutation, runtime flag, mirror outbox, n8n workflow, schema, or app-code change.

## 2026-07-11

- B4 readiness snapshot: tagged and pushed `pre-B4-readiness-2026-07-11` from current `main`; captured private Supabase table-count/state snapshots plus byte-verified downloads of all six deployed staff write Edge Functions before modification. Live flag readback remained unchanged: auth permissive, both teams Linear-authoritative, and inbound mirror enabled. No runtime flag was written.
- B4 WP-A1 actor persistence: audited all six staff writers. Calendar upsert already persisted actor/role; sample upsert, both reorder writers, and both settings writers had gaps. Added service-only `settings_events` for template/caption-prompt attribution, added card-ledger reorder events, and brought sample-upsert actor handling to calendar parity. Applied only the additive migration and deployed calendar upsert/reorder versions 17/14, sample upsert/reorder versions 18/13, and templates/caption-prompts versions 12/12. A hard-guarded disposable TEST probe made one write per EF, verified actor + role in all six ledgers, restored the exact prior settings rows, and confirmed zero probe rows/events remained. Focused source tests, Deno checks, all 68 unit suites, diff check, and secret scan passed.
- B4 WP-A2 role keys: the admin/SMM/creative EF secrets already existed, so no rotation was performed. SHA-256 digest comparison confirmed all three deployed secrets exactly match the private B0 key backup. `key-verify` then resolved one compatible active roster row per role in `auth_enforcement=permissive` mode and persisted valid proof events 3, 4, and 5. No key value or roster name was printed or committed; no flag changed.
- B4 WP-A3 staff sign-in staged (not live; owner merge pending): added an active-`team_members` roster picker with no free-text identity, local role-key storage, boot-time `key-verify`, invalid-key clearing, fail-open verifier-outage behavior, and cosmetic Production-nav visibility after validation. The shared EF header helper now replaces generic staff attribution with the verified roster actor/role and sends the key only to Supabase function URLs; n8n fallbacks and client-token links never receive it. Direct `?prod=1` remains available for B2 diagnostics. Dummy-data browser checks passed first sign-in, persisted boot revalidation, 401 cleanup, desktop/mobile layout, EF-vs-n8n header isolation, and direct-preview compatibility; Production structure and zero-write smoke stayed green. `auth_enforcement` and every runtime flag remain unchanged.
- Production polish CI wall-time hardening (draft PR #784): measured the latest clean serial PR workflow at 16m25s (15m32s inside the gate; interaction inventory 6m49s and `behav-wired` 5m08s). Kept the required `production-polish` check name and all assertions, added the explicit zero-write smoke to its fast PR lane, and moved only the long interaction/behavior/pixel/review/Argos work into two parallel jobs that run on relevant `main` pushes, weekday schedule, and manual dispatch. Added a Chromium cache keyed to Playwright 1.56.1; cache hits skip apt installation. The first real cold-cache PR run (`29139643839`) passed in 3m47s, with the selected test lane itself taking 3m12s. No app behavior, runtime flags, Supabase data, n8n workflows, Linear state, schema, or auth changed.
- Planning/tracking docs trued up to execution state + public-repo name scrub (docs-only PR, reviewer-authored): `INDEPENDENCE_PLAN.md` gained the 2026-07-11 status update (Track A complete/closed; Track B live at B3 with history parity) and a corrected header status; new `docs/independence/B4_READINESS.md` records the B3→B4 gate checklist with live evidence, the spec-§6 auth operationalization work-packages, and plain-language answers on cards⇄deliverables and the intake/submission flow; `ROLLBACK.md` gained the finished-work-import Live State row with its one-command rollback. **Append-only exception, security-motivated:** real client slugs/names that had accumulated in this public file, `ROLLBACK.md`, and `docs/ops/NEW_CLIENT_ONBOARDING.md` were scrubbed and replaced with pointers to the live flag values / `flag_flips` history (the authoritative data was never in the repo). Historical `docs/audits/**` files were deliberately left untouched as point-in-time records; a residual-exposure inventory (spec owner-review lines, audit files, two migration files — one containing personal emails/phones) is listed in the PR body for an owner decision. No app, flag, n8n, Linear, or backend change.
- B4 echo probe: ran the guarded TEST-only legacy-comment echo path. One app-thread comment produced exactly one Linear comment; after both Comments webhooks settled, the app thread still contained exactly one copy and no `mirror_in_comment_add` duplicate event. Linear creation latency was 3.096 s and settled proof was 11.543 s. The Linear comment was deleted and the mirrored thread restored exactly; proof/cleanup ledger events 7769–7770 remain. No runtime flag, real-client row, n8n workflow, mirror outbox, or auth mode changed.
- B4 gate constraints recorded without bypasses: the §1.5.6 outbound create→status→comment→due drill is BLOCKED until the spec §4.4 `linear-outbound`/outbox path exists (mirror-outbox writes were explicitly forbidden in this sprint). The live detect-only alert drill is BLOCKED because the branch requires Supabase team authority while all runtime-flag changes were forbidden. Source checks are not being counted as live gate evidence.
- D-9 due-date roller read-only re-audit: observed 41 VID/GRA issues touched from 23:45:28–23:45:35 UTC on 2026-07-10 with no visible field-change history or material inbound webhook event. Re-scanned all 127 live n8n workflows: no scheduled `issueUpdate`; the sole due-date writer is webhook-only and had no execution during the burst. Repository Actions at that window were read-only. Exact ownership remains BLOCKED because the connected Drive identity cannot see the workspace-owned operational sheets/bound Apps Scripts. Disable plan is owner-side trigger inspection under the sheet-owner accounts, disable-not-delete, then two-night observation; the spec key-rotation/tolerance fallback remains if no trigger is found. Nothing was disabled or mutated.
- B1 refresh heartbeat: privately snapshotted active n8n pager `qllIDZPkdNAPRj0b` before editing, then additively installed a static-data-gated B1 incremental-refresh dispatch (~30 min) and a 90-minute `linear_incremental_refresh` summary-staleness alert. Post-change readback remained active with 13 nodes. First live tick `244578` dispatched GitHub run `29143764570`; it completed green and wrote summary event `7772` for 2 changed issues (1 operational/soft-handled row, 1 archive upsert, no deliverable/batch writes). The pager correctly detected the prior 103-minute-old summary and its Slack DM node succeeded. Intervening tick `244646` succeeded with zero gate items, did not execute the incremental trigger, and emitted zero alerts, proving the ~30-minute cadence. Public-safe snapshot hashes and rollback are in `n8n-backups/2026-07-11-b4-incremental-heartbeat.md`. No runtime flag, schema, production business-workflow logic, auth setting, Linear record, or mirror outbox changed.
- B3 inbound mirror scenario harness: added a fail-closed TEST-only runner and offline mapping/safety tests. The final 16-scenario live run recorded 10 PASS / 6 RED: issue/batch creation, due-date clear, intermittent assignee clear, safely blocked unmapped-state coverage, intermittent re-parent, and reopen remain red; all other requested lanes passed. Existing TEST fixtures were restored exactly, all disposable issues `VID-12856` through `VID-12863` were archived, and detect-only reconciler run `29166705953` wrote summary event `8105` at `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`. Detailed TEST payload evidence is private and outside the repository; public-safe results are in `docs/audits/2026-07-11-b3-inbound-mirror-scenario-harness.md`. No runtime flag, real-client row, n8n workflow, Linear webhook, app/backend/schema code, or mirror outbox changed.
- B3 inbound mirror scenario harness follow-through (supersedes the preliminary red matrix above): pushed snapshot tag `pre-b3-restore-fix-2026-07-11`, captured and hash-verified the active version-11 `linear-inbound` source privately, and inspected retained TEST webhook payloads. Due-date clear omitted `dueDate` (execution `248975`), assignee clear omitted both assignee fields (`248997`), re-parent sent `parentId` without a parent object (`249003`), and restore sent action `restore` without `archivedAt` (`249012`). Fixed restore so it clears all archive/delete markers from `linear_raw`, deployed version 12, rehearsed rollback by redeploying the private pre-fix bundle as version 13 and proving a harmless TEST title round-trip in 2.724 seconds, then restored the fix as active version 14. Refactored the harness to assert the measured consistency contract: create/re-parent via a project-scoped incremental refresh, omitted-field clears via an identifier-scoped reconciler apply, and promised inbound operations realtime. Final TEST-only run marker `b3-mirror-mrguysjz` recorded 15 PASS / 0 FAIL / 1 N-A; restore produced `mirror_in_restore` and made the row visible. Harness event `8346` and independent full v2 event `8347` both ended at `diff_count=0`, `repair_list_size=0`, `linkage_actionable=0`; original TEST fixtures matched their snapshots, disposable issues were archived, no active probe batch remained, and runtime flags were unchanged. No real-client row, schema, auth mode, n8n workflow edit, Linear webhook edit, or `mirror_outbox` write occurred.
- B4 WP-A3b staff-auth consolidation staged (not live; owner merge pending): credentials now accepts admin + SMM role keys, while unstripped onboarding and filming-plan writes accept admin only; creative/editor/designer role identities are denied. Authorization is derived from the matched server secret through the shared `key-verify` matcher, and the exact historical surface-key paths remain additive compatibility. The frontend reuses the one verified roster identity, opens a signed-in account popover with Sign out (no switch-user path), clears/purges on invalid-key 401, preserves valid sessions on role-denial 403, synchronizes sign-out across tabs, and sends the key only to Supabase Edge Functions. The sign-in surface was polished and visually audited across empty, roster-open, verifying, wrong-key, signed-in, account-menu, desktop/mobile, and light/dark states. Validation passed: executable admin/SMM/creative/editor/designer + legacy matrix, all 75 unit suites, B4 browser suite (8 verifier calls), master fast profile (2/2 probes, 12/12 scenarios, 87/87 assertions, 6 clean vision frames with explicit OK verdict), and all 8 production-polish suites (165/165 behavior guards, light/dark pixel parity). No live function, flag, secret, Supabase row, n8n workflow, or Linear record changed; `auth_enforcement` remains permissive and no secret value was rotated. Release must be backend-first via manual feature-branch workflow dispatch and green verification, then frontend merge; rollback restores the frontend first while leaving additive backend support in place.
- B4 WP-A3b final no-lockout audit hardening: sensitive-request responses are now owned by the exact roster identity signature, not only the shared role key, so a late 401 from one person cannot sign out a newer cross-tab identity using the same role key. The account menu shows the person's roster role while keeping the secret-derived role-key family authoritative for permissions. The focused source checks, all 75 unit suites, and the real-browser B4 suite (including a delayed same-key 401 probe) passed after this change; no live state was touched.
- B4 WP-A3b current-main master verification: rebased onto the D-24 and B3 restore/harness merges without changing their source, then reran the current 75-suite master profile. Windows-only harness portability was hardened by resolving the installed Git Bash path and keeping the static server as an attached child (detached children are terminated in this environment); this changes test infrastructure only. Final master result: unit/parity green, 2/2 probes, 12/12 scenarios with 87/87 assertions, and 1/1 visual flow with 15/15 assertions. All six regenerated frames were reviewed with an explicit OK verdict. No product/runtime state was changed by the harness repair.
- B4 outbound snapshot and additive schema: pushed tag `pre-b4-linear-outbound-2026-07-11`; captured private live schema/flag/outbox/TEST-state/deployed-EF evidence plus the active n8n pager before editing. Applied `migrations/2026-07-11-b4-linear-outbound.sql`, which expands the dormant outbox, adds atomic enqueue and TEST-quarantine helpers, and seeds `linear_outbound_enabled={"mode":"off"}`. Live readback remained `prod_authority={"video":"linear","graphics":"linear"}`, inbound enabled, auth permissive, and outbound off. No roster or existing runtime flag changed.
- B4 outbound backend deployed dark: active readback is `linear-inbound` v17, `linear-outbound` v14, and `deliverable-write` / `batch-write` v8. The deployment adds strict acknowledged-intent echo suppression, per-field Linear clocks, operation-specific patch allowlists, authenticated source-edit timestamps, server-owned status/archive metadata, and atomic lossless comment merge + ledger/outbox enqueue. The drainer maps create/status/comment/due/assignee/title/priority/parent/archive/restore, checkpoints idempotent creates, treats only an explicit Linear not-found as create permission, drops stale writes by conservative field/issue timestamp, and emits `source='outbound'` summaries. Self-review replaced an unsafe RLS-filtered token probe with additive `b4_service_role_probe()`, executable only by `service_role`; live proof rejected the public key with HTTP 403 and accepted the service key. Event `8960` was mode off with zero enqueued/written/failed, zero backlog, zero pending rows, and no alert condition.
- B4 TEST-only outbound proof: final hard-guarded run `b4-1783817733488-ff8283` passed 17/17 scenarios against the deployed service-auth, operation-allowlist, source-edit-clock, read-failure, and atomic-comment build in 226.1 seconds. Shadow event `8879` computed 4 intended mutations with 0 writes; live event `8888` and the operation/status matrix produced 25 written rows, 1 deliberate stale row, and 1 deliberate skipped row. The comment was losslessly merged into the local thread and ledger/outbox in one transaction before Linear reflection; server-owned status timestamps and archive/restore markers were exercised. The run observed 23 strict echo drops and zero unexpected inbound mirror events, proved pause `8942` / resume-newest-wins `8944`, off-mid-drain `8946` / resume `8948` / idempotent re-drain `8949`, and archived every disposable TEST issue. Integrated two-way event `8950` and post-cleanup event `8959` ended diff 0 / repair 0 / linkage actionable 0. Runtime flags matched their exact pre-run values.
- B4 outbound final dark-state reset: after the TEST override run, invoked the normal drainer once so the newest watcher evidence reflects production controls rather than the TEST-only live override. Summary event `8960` read mode `off`, `test_override=false`, zero writes/failures, and zero backlog. Exact readback remained `auth_enforcement={"mode":"permissive"}`, `linear_inbound_enabled={"enabled":true}`, `linear_outbound_enabled={"mode":"off"}`, and `prod_authority={"video":"linear","graphics":"linear"}`. No runtime flag was changed.
- B4 outbound pager wiring: privately snapshotted n8n pager `qllIDZPkdNAPRj0b`, then added a 15-minute outbound Actions dispatch and outbound-summary watcher for failed writes, backlog growth, write-volume spikes, shadow mismatch, and active-mode staleness (one-hour per-condition DM throttle). First tick `250226` exposed a scoped-PAT mismatch on `repository_dispatch`; captured a second private snapshot, switched to the same `workflow_dispatch` credential path used by the existing reconcilers, and made the pre-merge missing-workflow response fail-soft. Scheduled ticks `250299` and final dark-state check `250463` completed green: outbound trigger and summary fetch succeeded and the pager condition emitted zero alerts. The active mode-off summary prevents false stale pages until the draft lands on `main`; public-safe hashes and rollback are in `n8n-backups/2026-07-11-b4-outbound-pager.md`.
- SyncView top-nav promotion staged on draft branch (owner merge pending): the read-only mirror is now visibly labeled **Linear** with the exact former Linear mark, the unchanged Create Linear Issue form is labeled **Submit** with the existing neutral plus glyph, and the visible order is Analytics → Linear → Submit. Deliberate routes remain locked: mirror `navProd` / `production` / `#production` with `?prod=1`; submission `navLinear` / `linear` / `#linear`, including `currentNav === 'linear'` filming-plan wiring. The mirror is mounted for normal staff chrome and remains fully read-only. Focused human audit found and fixed two shell issues: Production shortcuts stealing Enter from the global Submit link, and expanded 1440px role navigation colliding with account controls; both now have permanent browser/source guards. Final verification: TEST/dummy-only human audit 24/24 with zero mirror write-like requests and no visual divergences; B4 staff browser green; all 78 unit suites green; production-polish 8/8 with 167/167 wired guards, zero-write smoke, accessibility/layout, and light/dark pixel parity; master unit/parity green, 2/2 probes, 12/12 scenarios, 87/87 assertions, 15/15 visual assertions, and all six frames manually judged **OK**. Desktop/mobile light/dark before/after header crops are stored locally outside the public repo. No backend, runtime flag, authority/enforcement setting, Supabase row, n8n workflow, Linear record, secret, or URL route changed. Rollback is a single revert of the UI/docs PR.
- B4 outbound post-merge and full-roster preflight: confirmed PR #797 merge `bc83cce`, active `linear-inbound` v17 / `linear-outbound` v14 source parity, and the complete live migration/RPC contract. Exact flags stayed outbound off, authority Linear/Linear, inbound enabled, auth permissive; outbox remained 181 historical TEST rows with 0 pending and 0 real written. Manual Action `29181125012` completed green and wrote clean event `8995`. Harmless event `8996` proved failed-write paging through n8n execution `251537`; harmless event `9002` proved backlog-growth and shadow-mismatch paging through execution `251672`; both DM items succeeded, and normal event `9003` restored the latest summary to zero. The read-only all-client audit checked 5,228 entities across all 32 real clients without changing authority or queues. It caught and fixed 949 self-parent classifier false alarms, then reported 73 genuine blockers (72 parent intents across 13 historical batch groups, 1 batch restore) across 11 clients. Run `b4-shadow-1783835492969` proved runtime digest and outbox high-water unchanged and zero Linear mutation calls; private evidence SHA-256 `f2ed907633047fb9aba3b51037f07b1cba3df00b7fbc24d2c41f8a6994062d4a`. No flag moved; the all-client authority flip remains blocked pending an approved handling rule and a zero-unexpected rerun.
- D-27 historical-write suppression and rerun: pushed snapshot tag `pre-b4-d27-2026-07-12` and captured private source-row evidence (pre-write SHA-256 `9ac5d0d613157f0640dc5303f5e6553223ceed4b21f107579f796ed89bbb9a32`). The first deliberately minimal `batch_write` attempt failed transactionally on the RPC's full-row insert contract; readback proved the batch, timestamp, ledger, flags, and outbox were unchanged. Retrying with the complete snapshotted row aligned the single Linear-terminal historical batch to `archived`, wrote exactly one `source='reconcile'` event, and left outbox total/high-water `181`, pending `0`. D-27 pre-alignment run `b4-shadow-1783877861264` returned 0 unexpected intents / 73 tolerated historical; post-alignment run `b4-shadow-1783878356762` returned 0 divergences / 0 intended writes / 0 repairs / 72 historical parent tolerances. Both proved zero Linear mutations and unchanged flags/outbox. Independent v2 event `9145` ended diff 0 / repair 0 / linkage actionable 0 with no healing. Outbound remains off, authority Linear/Linear, inbound enabled, and auth permissive; no runtime flag changed.
- D-27 outbound dark deployment: confirmed PR #800 merge `bdd94c8`, privately downloaded active version 14 before touching it, then deployed only `linear-outbound` from merged `main` as active version 15. Downloaded version-15 `index.ts` and `mapping.mjs` match merged source after line-ending normalization and contain the shared boundary, historical classifier, and skip guard; all six write/outbox RPCs remain service-role-only. Post-deploy audit `b4-shadow-1783880138781` returned 0 unexpected / 0 intended / 0 repairs / 72 `tolerated_historical`, zero Linear mutation calls, and unchanged flags/outbox. Normal drainer Action `29203552968` wrote clean mode-off event `9152` (0 writes/failures/backlog); reconciler event `9149` remained 0/0/0. Final state stayed outbound off, authority Linear/Linear, inbound enabled, auth permissive, outbox 181 total/high-water with 0 pending and 0 real written. Private consolidated evidence SHA-256: `99aebb98ac6373a2e344d25ac83d956b91c7b6e7a9a684512ef2dade6cbf55d3`; no runtime flag changed.
- B4 go-live step 1 bounded shadow verification: the first fail-closed window (flip ids 16–19) restored authority/off before the drainer because the unfiltered global reconciler count was 112, not the active-roster 72. Read-only scope proof showed exactly 72 active-client tolerances plus 40 inactive/internal rows, with 0 diffs/intents. The corrected window (flip ids 20–23) held SyncView authority for 146.945 seconds. Shadow reconciler event `9161` returned 0 unexpected active-roster intents / 72 tolerated historical; deployed drainer event `9162` returned shadow mode with 0 enqueued/written/failed/backlog/echo/mismatch; no mirror events occurred. Authority was restored to Linear/Linear before outbound returned off. Outbox remained 181 total/high-water, 0 pending, 0 real written; post-window event `9163` was diff 0 / repair 0 / linkage actionable 0. Private evidence SHA-256 `13cd9162a00a285393460cf30817d74390316c7bd1b26f04c67c263669d8930b`. Live remains blocked pending reviewer go/no-go.
- B4 go-live step 2 all-client outbound: after reviewer approval, set both team authorities to SyncView (flag flip id 24) while outbound remained off, then set outbound live (flip id 25). Immediate drainer event `9171`, manual Actions run `29206107017` / event `9172`, and first pager-triggered event `9175` all reported live mode with 0 enqueued/written/failed/backlog/echo/mismatch. Reconciler event `9174` remained 0 inbound/outbound diffs / 0 repairs / 0 actionable linkage with expected historical tolerances only. Active n8n pager execution `256097` ran after cutover, triggered/fetched outbound successfully, and produced zero alert items; no DM or team message was sent. Final readback is outbound live, authority SyncView/SyncView, inbound enabled detect-only, auth permissive, outbox 181 total/high-water with 0 pending and 0 real written. Private evidence SHA-256 `0c254f54786ab144168235cc55b7d2bcbc8a407e1e4a1f10fedcc7cfc2f29bfe`. Kill switch remains one flip to outbound off; per-team pause remains authority to Linear.

## 2026-07-12

- B4 D-26 everyday pause after the proven live-pipe exercise: restored both team authorities to Linear first (`prod_authority` flag flip id `26`), then turned outbound off (`linear_outbound_enabled` flag flip id `27`), both by `codex-b4-d26-pause`. Exact readback is authority Linear/Linear, outbound off, inbound enabled, and auth permissive. Off-mode drainer event `9181` reported 0 written / 0 failed / 0 backlog. A fail-closed TEST-project title round-trip proved inbound application resumed: mirror events `9182` and `9183` applied the temporary change in 1.665 s and restored the exact original in 2.129 s; Linear and the mirror matched afterward. The first global dry-reconcile attempt ended on a transient read failure before emitting a summary; the unchanged retry completed as event `9184` with 4,323 deliverables checked, diff `0`, repair `0`, and linkage actionable `0`. Outbox remained 181 total/high-water, 0 pending, and 0 real written; no team message was sent. Private probe evidence SHA-256 `8b5bed5e9a02f7b3fcab50d698b5a356eb66209093b123db2b6b3e11bc8d1a86`. The proven live evidence remains valid; re-enable after the write UI ships by restoring SyncView authority, then setting outbound live.
- Native Production comment lane additive storage/backfill: applied `production_comments` with service-role-only writes, a protected authenticated reader, and anonymous filtering for body-bearing comment events; deployed `production-comments` v1 and `linear-inbound` v19. Lifecycle-only webhooks preserve stored body/author snapshots, and mixed-team batch parents fall back to the validated VID/GRA issue team. Linear remained read-only. The full source census found 12,683 VID/GRA comments with zero conflicts. A 56-comment TEST pilot reconciled exactly, reran with zero writes, and was tag-rolled back to zero before the full apply. The restartable full run resumed safely after an interruption at 606 rows, stored and reconciled 12,683/12,683 Linear IDs, then added 8 verified local-only native comments for 12,691 rows total. The final identical rerun performed 0 inserts and 0 updates with 12,691 exact no-ops and 0 conflicts. The full tag-scoped one-command rollback is available but was not executed. Exact flags remained authority Linear/Linear, outbound off, inbound enabled, and auth permissive; the outbox remained 181 total, 0 pending, and 0 failed. Owner sign-off is required on the protected authenticated comment-text access decision before UI merge; no anonymous comment-body exposure was enabled.
## 2026-07-13

- n8n quota burn containment: the retained execution window identified duplicate calendar-reconciler dispatch plus an idle every-minute TikTok poll as the two safe cuts. Before changing state, privately exported exact live JSON for `AkiFmromoDkmsh39`, `LR6R1mV4NaLNLlLG`, and retained dispatcher `qllIDZPkdNAPRj0b`, plus the `origin/main` GitHub workflow, under `C:\Users\Sidney\Documents\Codex\private-backups\2026-07-13-n8n-burn-cut`; manifest SHA-256 `c5b1a0ebfbf82818e687989ae4739367cd5df2b8b16cc9aa60e5033a65321cce`. Deactivated only Aki and the TikTok status cron; API readback returned both inactive while qll stayed active on unchanged version `16a436c6-5b49-4baa-9630-978cee2854a2`. Removed only the native `schedule` entry from `linear-sync-reconcile.yml`, preserving `workflow_dispatch` for qll. Rollback is independently re-activating either n8n workflow or reverting the cron-removal commit. No runtime flag, authority, Supabase row, Linear record, n8n node logic, or other workflow changed.
- n8n quota burn post-cut proof: after cron-removal merge `c91a09d6a19b4002d22a03b6721c031ebc189c18`, scheduled qll execution `263769` succeeded at `23:15:34Z` and dispatched calendar reconciler run `29292495346` through `workflow_dispatch`. The run completed in APPLY mode with `in sync 552`, `corrections 0`, `unmapped 0`, and `authority-gated 0`. The first complete post-cut bucket (`23:00Z`-`23:15Z`) contained 21 n8n executions, versus 74 in the comparable pre-cut bucket (`22:30Z`-`22:45Z`): an annualized short-window pace of 2,016/day versus 7,104/day. Use 3,000-3,500/day as the conservative planning range until a full 24-hour post-cut sample exists. Aki and TikTok remained inactive; no scheduled GitHub run occurred after the cron removal.
- Write-UI Part 2 gateway backend deployed dark: captured private pre-deploy function/schema/flag evidence, applied the additive outbox-parity migration, provisioned private server-only Graphics-generation and per-team TEST-project configuration, and deployed `production-write` v11, `linear-outbound` v20, `linear-inbound` v27, plus service-only `deliverable-write`/`batch-write` v13. Live negative auth proof returned 401 for missing/garbage/ambiguous credentials, 403 for wrong roster/client scope or client-forbidden fields, and 409 for a valid staff principal attempting a Production write while the team remained Linear-authoritative. The disposable two-team TEST drill then completed create plus status ladder, comment, due set/clear, and assignee set/clear for both teams: 18 operations, two teams, zero unexpected echoes, final reconcile diff/repair/linkage `0/0/0`, cleanup true, and exact pre/post runtime flags unchanged. The drill exposed and permanently fixed three pre-UI defects: scalar echo payloads were parsed through the comment shape, envelope-free UI comment events entered the generic outbox trigger because its null test was not strict, and comment responses replaced the entity CAS cursor. All 11 probe-created native rows and their Linear issues are archived; no active disposable row remains. No browser caller, n8n workflow edit, existing runtime-flag flip, real-client row, or real-client Linear mutation occurred.
- Write-UI Part 2 guard plane: added one validated `prod_authority` reader with a disk-backed last-known-good used for detect/report only; every non-live flag read freezes APPLY, so a cached pre-flip Linear stance cannot authorize a stale write after a team flip. Both legacy card reconcilers classify SyncView-authoritative differences without applying either direction or advancing their ledger clocks, then require a fresh live team-authority read immediately before every mutation and abort without saving clocks if that read changes or fails. B1 incremental/full authoritative rows have the same per-write fresh check; incremental still filters the plan per team, retains archive-only refresh, and refuses an unsafe full rerun after a team flip or flag-read outage. After private pre-edit snapshots and exact version/hash dry-runs, applied n8n gates to set-status, add-comment, forms, and the inactive MJb calendar/sample handlers. Final readback: active 3-node status, active 3-node comment, active 94-node forms, inactive 5-node MJb, workload branch byte-identical, and a repeat dry-run reported all four installed. The first apply safely stopped after the first two workflows when the n8n PUT schema rejected a legacy read-only `binaryMode` setting on forms; the tool now removes that field, preserves original backups on rerun, and completed the remaining two. Added a service-only daily TEST gateway drill and nightly source-read-only full-roster shadow audit, both emitting public-safe aggregate events; the qll watcher remains unapplied until those schedules merge. No workflow activation, runtime-flag update, Supabase authoritative-row write, or Linear mutation occurred; authority remains Linear/Linear and outbound remains off.
- Write-UI guard-plane validation: all 90 repository unit suites passed, all three edited/new Actions files parsed as YAML, both n8n tools passed fresh live read-only dry-runs against their pinned versions/hashes, and `git diff --check` passed. The stale-tab policy fixture proves the same legacy request is allowed at a live Linear stance, rejected with 409 at SyncView stance, and frozen with 503 for cold or diagnostic-only last-known-good state. No live mutation was used for this proof.
- Write-UI Part 2 mirror controls staged: the Linear mirror now reads an exact-one `prod_authority` row and enables status/comment/due/assignee only for the target team when SyncView-authoritative, with the active TEST-client override. Staff headers come from the verified roster identity; Admin/SMM receive all four controls, Creative receives own-team status/comment only, and direct diagnostics remain read-only. Scalar writes carry CAS, comments keep a stable memory-only retry key and explicit audience, stale authority rejection refreshes/relocks the tab, and conflicts adopt the current server row before retry. No legacy parity request, localStorage comment draft, n8n mutation, direct Linear request, flag update, or live deployment was added. Validation passed all 91 unit suites, the fully mocked four-operation/CAS/auth/mixed-authority/TEST/stale-tab browser lane, the Production comment browser lane, and the read-only smoke.
- Write-UI Part 2 legacy-caller and Submit reroutes staged: Calendar/SXR status/comments call the authenticated gateway with native linkage and normalized metadata, accept native ID with no Linear URL, and await `native_committed` before source-row persistence. The principal-bound source-repair payload is cross-tab-locked, persisted, and read back in `syncview_write_ui_source_repairs_v1` **before** gateway network I/O; it retains the original payload/historical lane, attempt state, stable intent identity, both composite-tweak legs, and exact source-repair tokens. Review correction: a local acknowledgement or cache checkpoint is never commit proof, and an attempted request is not blindly replayed under its pinned lane. If the authoritative status/comment response is lost, the same authenticated principal first asks `production-write` for the read-only `reconcile_only` receipt: `committed_exact` adopts the returned current row and canonical comment lifecycle, `absent` holds status unless newer native status supersedes it while an append-only comment may reissue through the current lane, and `conflict` remains owner-review debt. Cache-only v2/Kasper checkpoints never auto-apply or clear debt. The receipt branch reconstructs the historical intent but invokes no authority lane, parity gate, RPC, drainer, or Linear call; focused gateway/auth checks cover its exact/absent/conflict classifier and authorization ordering. Quota/full-payload and corrupt-journal failures still block safely, and repair subsets may hydrate beyond display TTL only for diagnostics/paint. Review chains catch synchronous checkpoint failure, and canonical TEST targetless cards fail closed even while global authority is Linear. Submit requires Admin/SMM and places creation, checkpointing, purge, and completion deletion under one mandatory browser Web Lock; `intake_create` remains an exact transactional-idempotency retry and is not supported by `reconcile_only`; a browser without Web Locks refuses intake. Existing v1 status/comment debt moves to inspectable quarantine, and corrupt local debt reports `unknown_records` rather than zero. The five old browser endpoint callers are absent. The **offline** auth policy/source matrix covers all five operations across staff/client/garbage/mixed/TEST modes; it does not replace positive live browser-principal or EF HTTP proof. The public-safe read-only census covered 31 active real-client rows and 62 required team mappings: 0/62 were production-ready, 57 were exact-name owner candidates, five were unresolved discovery pairs, and all 62 remained blocked pending persisted team tags. The census made zero mutations. Final validation passed all 97 repository unit suites plus the production-write gateway, Production comments, and read-only Production browser suites. The current public-safe **server** outbox census is 263 total: 241 written, 17 skipped, five stale, and zero pending/failed; parity and TEST pending/failed are also zero. This does not close the all-browser local-queue census. Positive staff/client TEST browser-principal proof and deployed stale-queue behavior remain owner/deploy gates. No browser or gateway-delta deploy, runtime-flag change, n8n activation change, production authoritative-row write, or Linear mutation occurred.
- B3 inbound-diff and linkage follow-through: persistent v2 events reported five comment diffs (`GRA-6339`, `VID-12642`-`VID-12645`). Read-only detail proved all five Linear Comment webhooks arrived from 04:51Z-05:00Z, persisted their durable comment rows, and wrote both `comment_add` plus companion issue-update mirror events; there was no function deploy in that window. The false diffs came from reconciler v2 comparing the normalized local `comment.id` (`linear:<uuid>`) to Linear's native UUID instead of preferring nested `comment.linear_comment_id`. Added that compatibility fix and a regression test; no reconcile apply was run because it could not correct an identifier-shape classifier defect. A corrected global dry run, event `15225`, checked 4,324 deliverables and returned diff `0`, repair `0`, linkage actionable `0`, and outbound diff `0`. The first two feature-branch Actions attempts (`29261942318`, `29262008946`) then exposed transient Supabase statement-timeout reads; added a bounded three-attempt retry for only HTTP 429/5xx reads. The unchanged branch rerun `29262180885` completed green and persisted event `15227`, again checking 4,324 deliverables at inbound diff `0`, outbound diff `0`, repair `0`, and linkage actionable `0`. Separately dry-ran the sanctioned linkage lane, applied its single TEST-only `calendar:graphic` slot under cap `1`, and proved the immediate rerun planned `0` writes. Authority remained Linear/Linear, outbound off, inbound enabled, auth permissive; outbox readback remained 0 pending / 0 real written, and no Linear write or runtime-flag change occurred.
- n8n execution-quota watchdog staged entirely in GitHub Actions: added a daily read-only n8n Insights count for the Guatemala calendar month, configurable `N8N_MONTHLY_EXECUTION_CAP`, one-time monthly 80%/90% owner alerts with remaining headroom, exact month/threshold dedupe markers outside n8n, and fail-closed API/relay retries. Live read-only proof counted `109287` July production executions. Branch dry-run `29267969378` used cap `1` with `0.01%`/`0.02%` thresholds, sent both test alerts, and produced successful owner-relay executions `262075`/`262076`; final-code dry-run relay execution `262097` additionally proved delivery confirmation waits for the Slack step before returning success. Dry-runs saved no monthly markers. GitHub secret values were stored by name only, the production cap remains unset pending the owner's new-plan number, and no runtime flag, authority setting, n8n workflow, Supabase row, Linear record, or application path changed.
- n8n execution-quota watchdog production configuration: set GitHub repository variable `N8N_MONTHLY_EXECUTION_CAP=135000`; production thresholds therefore resolve to 108,000 (80%) and 121,500 (90%). Live run `29268681506` read 109,287 executions (80.9533%), sent exactly the July 80% warning with 25,713 remaining, confirmed owner-relay execution `262107`, and saved the July 80 marker. Immediate repeat run `29268726046` restored that marker, kept the July 90 marker absent, and sent no duplicate. Manual dispatch remains dry-run by default for operator safety; scheduled runs are live. No runtime flag, authority setting, n8n workflow, Supabase row, Linear record, or application path changed.
- n8n quota default-branch hardening: the first post-merge `main` activation (`29268930214`) correctly used cap 135,000 but exposed GitHub's branch-scoped cache boundary: the feature-branch proof marker was not visible on `main`, so the activation produced a second 80% owner warning (relay `262155`) before saving the authoritative `main` marker. Added a fail-closed guard that permits live mode only on the repository default branch; feature branches remain dry-run-only. This makes production 80%/90% dedupe single-scope. Feature-branch live proof `29269079957` stopped at the guard before the count, relay, or marker steps. The `main` July 80 marker now exists, the July 90 marker remains absent/armed, and no runtime flag, authority setting, n8n workflow, Supabase row, Linear record, or application path changed.
- Active-client Linear project mapping: captured private pre-state and exact transactional rollback evidence, then verified 34 unique Linear projects and 62 expected project/team attachments read-only before conditionally populating team-tagged `clients.linear_project_ids` for all 31 active real-client roster rows. Independent live readback returned 31/31 non-empty video mappings, 31/31 non-empty graphics mappings, and zero value mismatches; no client was skipped. All eight runtime-flag rows matched the pre-write snapshot exactly, including legacy parity disabled, authority Linear/Linear, outbound off, inbound enabled, and auth permissive. The mapping is configuration only and does not enable Submit. No Linear mutation, runtime-flag update, n8n edit, schema change, outbox write, or application deployment occurred. Private backup/evidence is stored under `2026-07-13-linear-project-mapping`; rollback restores the exact prior mapping and timestamp values with fail-closed row checks.
- Production write browser E2E: used the deployed `?prod=1` controls and a verified Admin staff principal to exercise TEST issue `VID-12612` through status, internal comment, due-date set/clear, and assignee set/clear. Every browser request used the active TEST override, returned a native commit, and independently appeared in both Linear and the deliverables/comment mirror. Nine outbox rows (six requested writes plus three UI restores) were TEST-scoped, written, and left zero pending TEST rows; no inbound echo event appeared. The issue was restored exactly to Todo, its original due date and assignee, and the new TEST comment was deleted in Linear and tombstoned in SyncView. Final runtime flags matched the pre-run snapshot byte-for-byte: authority Linear/Linear, outbound off, legacy parity disabled, inbound enabled, and auth permissive. Run `browser-write-e2e-1783973618108`; private evidence is outside the public repository.
- Calendar Create Post native-intake gap closed in the stacked #813 draft: Admin/SMM Create Post now derives the client from the open calendar and defaults to the latest active batch, with an explicit new-batch choice. Both choices use the same durable `intake_create` job and paired Video + Graphics item builder as Submit; only a committed native response is materialized back to the Calendar card. The gateway accepts Calendar intake and an existing `batch_id` with batch CAS, revalidates the same-client active batch plus each team's persisted project and parent route, and uses the additive service-role-only `production_intake_append` RPC from `migrations/2026-07-13-production-intake-append.sql` to commit the pair atomically under the batch lock. Missing mapping, ambiguous parent, stale batch, cross-client batch, or invalid pair fails closed. The locked seven-point Create Post model is now checked in source/test documentation, and the project-readiness record is trued up to the separately verified #819 result of 31/31 clients and 62/62 team mappings. This is draft code only: no Edge Function/browser deploy, migration apply, Linear mutation, runtime-flag flip, or n8n change occurred. Live stance remains authority Linear/Linear, outbound off, inbound enabled, auth permissive, and legacy parity disabled.

## 2026-07-13 / 07-14 — audit, docs, n8n cut, D-9 catch, #813 review

- **Full cutover audit** (40-agent, adversarially verified): 26 findings F01-F26 + 10 blind spots, recorded in `docs/independence/CUTOVER_AUDIT_2026-07-13.md`. Go-live checklist rewritten around a staged per-client allowlist (D-32); `docs/ops/FLIP_RUNBOOK.md` added (owner-executable flag flips + drain-first rollback). Owner decisions D-30 (keep +2d overdue bump), D-31 (SMM-reshared tokened client links), D-32 (staged allowlist rollout) recorded in spec §14. The concurrently added TEST-soak finding was renumbered from colliding F27 to F90 after rebase; it requires separate TEST/real pager criteria before Phase 1, and its private TEST literal was scrubbed under F74.
- **n8n burn cut applied** (Codex): deactivated redundant calendar-reconciler dispatchers (`AkiFmromoDkmsh39` + GitHub cron; qll `qllIDZPkdNAPRj0b` is now sole 15-min dispatcher) and the idle every-minute TikTok cron `LR6R1mV4NaLNLlLG`. Burn 7,104→~2,016/day (measured, -71.6%); exhaustion moved from ~Jul 16 to ~Jul 20-25. Reconciler independently verified still healthy on qll alone. Cap is a hard stop; raisable to 500k if monthly. Pager last-mile proven (8 gap DMs + synthetic). Anon-write RLS proven default-denied on core tables.
- **D-9 roller investigation refined** (F14): Linear detailed history confirms a 2026-07-13 23:45:27-41Z burst across ~18 real VID+GRA issues, but it was a **no-op re-save**: every issue was already due 2026-07-14, with no due transition or actor metadata. The shared `Form` key, n8n, GitHub Actions, SyncView Workload, and visible Google Sheets were excluded. Actor resolution is blocked on owner-only Google Apps Script execution/trigger evidence or Linear support request logs; do not rotate the shared `Form` key blindly.
- **#813 (write-UI reroute + create-post + fix-pack) independently reviewed** — 4 parallel reviewers, zero P0/P1: DARK on merge (real clients take byte-equivalent legacy path; no unconditional reroute; fail-legacy on flag read), F03 token exposure CLOSED in code (no token in any anon-readable store), all 11 fix-pack items implemented correctly with no weakening of #810/#811/#812 guards, diff scope clean + public-safe + deny-paths tested in CI. Verdict: safe to merge DARK. **Merge/deploy sequence (owner):** merge → immediately deploy EFs from the exact merge SHA in order `linear-outbound` then `production-write` (pinned `workflow_dispatch`) → TEST-drill green → spot-check a real client still saves via legacy → touch NO flags, enroll NO real client. Residual P2/operational (not merge blockers): F11 resolvability backfill (sweep found 191, not ~60 — do before first real enrollment), F15 Rocio Slack ID, F09 pager scheduled proof, F13 backup rehearsal, add a runtime non-enrolled→legacy test, and the mid-failed-retry-quarantine behavior delta on deploy day.
## 2026-07-14

- Second-pass correction to the 2026-07-10/11 finished-work import rollback claim (F62): the historical one-command DELETE is no longer executable or complete after the normalized comment import. Read-only dependency proof found 2,481 `production_comments` rows referencing tagged history deliverables and 5 referencing tagged batches through NO ACTION foreign keys, so deleting either parent class aborts. The old event DELETE predicate currently matches zero rows while 6,473 backfill ledger events reference tagged history deliverable/batch IDs, so even clearing the FK blockers would leave orphaned audit history. The original import and its evidence remain valid and untouched; the old rollback SQL must not run. Preserve the rows by default pending a private dependency snapshot, owner-reviewed preserve/relink/delete plan, assertion-bearing transaction, and TEST/scratch restore rehearsal. No database row, flag, workflow, Linear record, or runtime state changed during this correction.
- Second-pass operational correction through F70 (append-only; supersedes conflicting recovery prose in older entries): F2 normal outbound and F4 parity are independent, so unknown/mixed incidents disable and read back both; a green default drainer does not authorize authority reversal. No n8n workflow may be blindly reactivated—current approved graph/version, trigger, credentials, error handler, last-green evidence, duplicate-path exclusion, owner approval, activation readback, and first green are required. Unknown shared credentials are not blind-rotation fallbacks. The full comment-import DELETE is withdrawn after current read-only proof found 223 self-FK dependent rows (including one later outside-run row) and 41 tagged version-2 rows. Removing the shared alert secret is not lane-scoped because onboarding also consumes it. Track-A n8n writer fallback/failover remains an unauthenticated downgrade (F67), and a future global `auth_enforcement` return to permissive is a security incident, not routine rollback (F70). These are documentation/control corrections only; no flag, database row, workflow, Linear record, credential, or runtime state changed.
- Public-repository privacy containment (F64): schema/RLS/revoke-only replacements were prepared and count-scanned in the access-restricted local incident workspace, but are intentionally excluded from public PR diffs. The protected live table/rows were untouched. Public current files/history/cache/fork/clone, share/session revocation, and the coordinated rewrite remain owner/GitHub incident actions. No private value was copied into this log.
- Stale-master and QA-evidence containment (F71/F72): the byte-identical lifecycle map on both repositories' current `main` blobs is materially stale across Track A, Track B, inbound topology, cadence, and network ownership. This PR and `synchrosocial` draft PR #51 now carry the same non-operative warning (companion commit `e3b2204`); neither is merged and the full line-by-line update remains open. The historical EF routing report no longer calls unauthenticated n8n selection fail-safe or a one-step rollback, and its private TEST identity was replaced with private-config notation. No runtime, flag, workflow, database, or Linear record changed.
- F64 publication proof corrected 2026-07-14: local Git rendered exact-path `-diff` replacements as binary, but an anonymous GitHub comparison with the rule present in both base and head still expanded every historical SQL row deletion. The temporary feature branch was immediately rewound and draft PR #829 was closed as ineffective. Do not publish the three scrub diffs through an ordinary public PR; restore the reviewed clean files only inside the owner-coordinated history rewrite before its final force-push. Public history/link/session remediation remains open.
- Samples boot/current-doc correction (F73/F75): read-only live browser proof found that a fresh browser can navigate to GA Samples, then lose the surface on reload because three early boot copies and `boot-gate-parity` still require the obsolete default-off opt-in while `_sxrEnabled()` defaults on. No non-GET/HEAD request was made. The four Samples rollout/rebuild/parity docs and the Linear card-reconcile guide now carry current-state overrides and are reclassified as historical where appropriate; they cannot authorize publishing the inactive combined n8n graph or using anonymous n8n fallback. Runtime/test correction and full doc rewrite remain open.
- Private-fixture control correction (F74): repository inspection found a live source test that required a feature doc to contain the private TEST identity. That doc/test now use `<PRIVATE_TEST_CLIENT>` notation. The wider fixture/report/script corpus remains under F64/F74 migration; no private value is reproduced here and no live system changed.
- Weekly-report security audit (F76): read-only anonymous live requests proved the deployed weekly-report Edge Function returns the active manager roster and real report content without authentication. Browser-key count-only probes also proved anon SELECT on both underlying tables; source review found anonymous report submit and roster-sync/deactivation actions. No write was attempted and no row value is recorded here. Feature/config/System Map docs now classify this as a P0 incident; immediate disable/containment versus time-boxed acceptance is an explicit owner decision pending individual/role/service auth, revoked anon table access, access-log/data review, and deployed negative tests.
- Onboarding-reader security audit (F77): fresh no-header read-only requests proved all three service-role onboarding list Edge Functions return real contact data and detailed answers anonymously, including every legacy submission. Source confirms CORS wildcard, no JWT/custom auth, and bare SPA fetches. No response value is recorded here and no write was attempted. Legacy-onboarding/System Map/endpoint truth now classify this as a P0 incident; disable/contain versus expiring acceptance is an explicit owner decision pending role auth, minimal discovery, constrained CORS, private log/link review, and deployed anonymous/cross-role denial.
- Thumbnail service audit (F78–F80): read-only live config/secret inventory plus deployed source proved the active scanner has no configured optional key and therefore permits anonymous service-role Drive/Storage work; the active resolver likewise has no custom auth and its remote-call/final-update sequence lacks atomic URL/version CAS. Neither endpoint was invoked. Docs/config now block scheduling/use pending required identities, bounded/rate-audited work, provider-usage review, deployed negative tests, and a reversed-completion CAS drill.
- Public onboarding-capture abuse audit (F81): deployed source/config inspection found caller-chosen service-role upserts and alert text without nonce/session ownership, rate/CAPTCHA, strict size/schema/kind, conditional updates, or immutable creation time. No request was sent. Current onboarding docs now require bounded anonymous-client sessions and adversarial TEST coverage before the “never lose” chain is considered safe.
- Filming-plan confidentiality audit (F82): a no-header read-only GET returned the complete current filming-plan roster with document links, and an independent browser-key count-only HEAD proved the underlying table is also anonymously selectable. Source/RLS confirm two separate public paths, while the migration also seeds real identities/links in the public repository under F64. No response value is retained or reproduced and no write occurred. EF and REST must be closed together, with a least-field role/client projection and private Google-document sharing/log review.
- Thumbnail revision metadata audit (F83): a browser-key count-only HEAD returned 200 for the supposedly backend-only revision table; exact migration review confirms anon SELECT/realtime over cross-client Drive/private-path/requester/revision/error metadata. No row body, object, or media was fetched. Revoke anonymous access, review object-policy reachability, and prove anonymous/cross-client denial before any UI projection.
- Credential-vault security audit (F84): service-role count-only aggregates found three active password-bearing rows, while deployed source/UI review proved list responses bulk-deliver plaintext before visual masking, direct extraction bypasses the caller-invoked fire-and-forget reveal audit, shared/legacy keys lack active-member binding, and actor is caller metadata. Password is also an old/new audit diff field; zero password-change events exist today, so readable plaintext history is a deterministic first-edit risk rather than claimed current event exposure. No secret or event body was fetched. Require individual sessions, metadata-only list, one-secret fail-closed reveal, and no old plaintext history.
- Full-onboarding read-boundary audit (F85): missing-key requests correctly denied 401, but deployed source proves the shared admin and legacy onboarding secrets bypass active-member lookup/revocation and read auditing before returning the unstripped corpus. Count-only live aggregates found 1 standard, 1 AI, and 21 legacy rows, 19 with retained credential arrays; no answer or credential body was fetched. Holder inventory, legacy-key retirement, active immutable admin sessions, read audit/minimization, private access review, and the owner's credential-retention decision remain mandatory.
- Staff/client directory audit (F86): browser-key count and non-null-field aggregates, without identity values, proved raw anonymous access to 20 staff rows (6 inactive) and 39 client rows (6 inactive), including internal email/Slack/Linear/project-mapping columns beyond the sign-in surface's minimal active-directory need. Map all SPA readers, revoke raw access, and replace it with purpose-specific least-field active projections.
- Verifier resilience audit (F87): deployed source/schema and low-volume aggregate baselines show the public staff/client verification functions have no application request controls or bounded audit retention, expose differentiated staff denial classes, and synchronously depend on event writes. No load test or abusive traffic was generated and current aggregates do not show abuse. Add uniform denials, request controls/alerts, retention, and TEST-only audit-outage/capacity proof; keep F38 fail-closed.
- Operational read-boundary census (F88): enumerated all 37 public schema paths from service-role OpenAPI, then used only browser-key HEAD/count checks. Twenty nonempty tables are anonymously selectable across operational content/events, Production, workloads/settings, rosters/flags, reports, filming plans, and revision metadata. No row body was fetched. This proves client-token/UI enforcement is not a direct-read confidentiality boundary. The owner must explicitly accept all exposed fields as public with legal/client review or migrate to scoped projections and revoke raw policies.
- Client-token release-evidence audit (F89): source and aggregate-only live review proved `client_access_events.ok` stores access-allowed, not credential-valid. Of 614 events, 611 are permissive missing-token true; the last seven days contain 588 such events and zero valid-token events. No token/client value was fetched. The current window cannot satisfy §6.4; add separate validity/allowed fields, token revision plus active-client binding, and an exact full-roster machine gate.
- n8n failure-alert scope correction (F09): a read-only live settings sample found five of six load-bearing cutover workflows without the central `errorWorkflow`; three of those workflows accumulated 135 error/crash/cancel records since Jul 7. The central handler itself failed 29 of 30 sampled invocations at the execution limit. No fleet-wide percentage is claimed because a broader settings sweep hit read-transport failures. Monitoring/rollback/spec truth now requires both generated wiring receipts and an independent non-n8n pager; no workflow or runtime setting changed.
- Serving Linear-mutation caller-auth audit (F91): active-version/source/config readback proved status, comment, video-intake, and graphics-intake routes authenticate no incoming principal while both teams remain Linear-authoritative. Team direction gates therefore permit these serving paths and do not establish the caller. No endpoint was invoked and no unauthorized activity is claimed. Current containment, exact-client capability owner decision, native reroute, and eventual retirement are now separated in the register and operative docs.
- End-of-grace ordering audit (F92): source and canonical-sequence review found that a reconciler apply can enqueue/requeue mirror work after the documented zero proof and after normal outbound is off. The final post-off run is now explicitly dry-run/detect-only; any diff returns under the freeze to live drain/classification and a new zero proof. The atomic stale-state-failing transition and race drill remain open. No reconcile, flag, outbox, or data write occurred.
- Living-truth coverage control (F93): the previously green truth suite accepted date-only stamps, a BRIEFING boundary through F81 while the register reached F92, and omission of an open P1 from all operative control docs. Counterexamples across inventory/spec/B4/feature/System Map/rollback truth were reconciled. `truth-sync` now requires exact date+commit stamps, BRIEFING/register maximum equality, and operative-doc presence for every non-closed P0/P1 finding; focused validation passed 288/288. This prevents silent omission but does not replace semantic/live review.
- Living-truth guard adversary follow-up (F93): the first correction still accepted a nonexistent hex-looking commit and missed four Edge calls composed from a base constant. The guard now requires a resolvable ancestor commit no older than 30 days, statically resolves the simple composed calls, and verifies the documented literal/composed endpoint count; the unit workflow fetches full history. Focused validation passed 324/324. Session-bootstrap wording also now treats reviewer read-only as an explicit rule, not a property of the public key. No endpoint was invoked.
- Manual-assignment eligibility audit (F94): two independent source passes proved the Production picker and gateway accept any active same-team roster row without compatible-role or usable-provider-mapping enforcement. The native assignment can commit before the asynchronous mirror discovers a missing mapping. No assignment was performed and no invalid current row is claimed. Owner role policy, one server projection, pre-write rejection, existing-row audit, and retired-mode relaxation remain open.
- Foreground Production convergence audit (F95): source/test review proved the repeating timer refreshes authority only; work rows/comments reload on mount or return events and have no realtime/foreground poll or ordinary Refresh control. A continuously foreground creative tab can therefore remain stale indefinitely. No live data or write was used. Realtime/catch-up or bounded polling, stale UX, manual refresh, and two-tab handoff proof are required.
- Touch-mobile personal-queue audit (F96): responsive/source review proved the ≤900px layout hides the sidebar containing My issues and the visible palette trigger, while the mobile topbar has no personal/team switch. A crafted `?view=my` URL and hardware-keyboard palette work, confirming a discoverability/navigation gap rather than renderer failure. No device/live endpoint was used; responsive touch tests remain open after F37 identity repair.
- Forward-transition and Production due-date audit (F97–F100): canonical sequencing omitted the F5 client-link enforcement action before real-client parity and opened Graphics authority before normal outbound. GO_LIVE/Track-B/FLIP now add a pre-real-cohort enforcement phase and a fail-safe first-handoff order: exact both-team normal-lane zero, F2 live/readback, zero-normal-write heartbeat with parity classification, fresh zero, then Graphics F1. Source plus deterministic clock/helper simulations also proved Production freezes local “today” while overdue advances in UTC and mouse date choices discard the selected year; both are explicit pre-write gates with timezone, midnight, Dec→Jan, future-year, mouse/keyboard, and bulk tests. No runtime flag, endpoint, credential, database row, workflow, Linear record, or live client data was touched.
- Thumbnail refresh/comparison v2 full rollout (supersedes the 2026-07-14 F78/F83 pre-remediation entries above): PRs #831/#832 merged; the additive migration and pinned four-function release were applied and read back as `calendar-upsert` v32, `sample-review-upsert` v33, `thumbnail-revision-read` v12, and `thumbnail-revision-scan` v17. The dedicated scheduler secret was rotated without exposing its value; missing server credential returned `503`, wrong caller credential returned `401`, the flag rehearsed `test -> off -> test`, and final state is `thumbnail_revision_v2={"mode":"on","clients":[]}` with `THUMBNAIL_REVISION_SCAN_ENABLED=true`. Disposable TEST proof passed same-link writer refresh, monotonic server refresh, two no-write Drive rotations, real A-to-B pairing, protected authorized reads, expected auth/scope/body/card denials, and an open browser image-source change with zero hard refreshes. A browser-only preflight defect was caught, fixed in #832, redeployed, and re-proved. Raw browser table access now returns `401`, unsigned private-object access returns `400`, and cross-client comparison access returns `403`, closing F83; the scanner's mandatory identity/bounds/aggregate response and live scheduler proof close F78 while F79/F80 remain open. The initial all-active scan checked 239 watches (4 changed, 235 unchanged, 0 failed); scheduled run `29370658087` then completed green with 239 unchanged. After exact guarded TEST cleanup, cards/events/revisions/private objects all read back zero residue and the live set was 238 active watches with 0 error rows. An owner-selected real Calendar card passed desktop/mobile Previous/Current rendering, Escape/focus return, signed-image refresh on reopen, and zero page errors.
## 2026-07-14 — authorized public-data exposure containment

- Onboarding readers (F77): deployed `legacy-onboarding-list`, `onboarding-list`, and `ai-onboarding-list` from source commit `92fd191a06b00fe88030f8cefcdaf83d343cd3be`; final live readback at `2026-07-14T22:39:43Z` reports v24 for each. Missing and wrong credentials return `401` before service-role access. `onboarding-capture` remains public intake; GET returns `405`, source has no stored-data SELECT, and success returns only the created ID/kind. Candidate browser callers obtain the key only after verified Admin sign-in and contain no hardcoded key; current Pages list screens fail closed until merge.
- Weekly reports (F76): deployed `smm-weekly-reports` v21 from source commit `1f9eb20611705c21bd29ff286615a0742866aae4`. Anonymous GET and anonymous `sync_managers` both return `401`. The existing n8n manager-sync caller now supplies its managed credential; no-write execution `268153` reached the authenticated branch and returned the expected `400 unknown action`. Anonymous table reads for `social_media_managers` and `smm_weekly_reports` return `401`. Candidate Pages key plumbing is still merge-gated.
- Browser writers (F35): candidate source applies one fail-closed exact-principal/exact-client policy to all six service-role writers and derives attribution server-side. Safe live deployments from source commit `5b8f2cc06b62c16d9572aa1cddbdbc0cd8971171` are `calendar-reorder` v32, `sample-review-reorder` v31, `templates-save` v30, and `caption-prompts-save` v30. At the final readback every missing/wrong credential returned `401`; dedicated managed-key TEST calls returned `200`, fake reorder IDs changed no row, template/prompt settings were restored exactly, and zero probe events remained. `calendar-upsert` v36 and `sample-review-upsert` v37 intentionally retain their prior public source until the reconciler caller changes merge; deploying them earlier would break the merged safety lane. Deploy those two atomically from the eventual merge SHA and repeat deny/allow/restore proof.
- Filming plans / F88 safe subset: deployed `filming-plans` v24 from source commit `5b8f2cc06b62c16d9572aa1cddbdbc0cd8971171`; missing/wrong credentials return `401`. Candidate Pages removes direct REST/realtime/Sheet fallback, but the deployed Pages build still reads the table, so `filming_plans` anonymous SELECT remains `200` until merge. Anonymous reads already return `401` for `thumbnail_media_revisions`, `social_media_managers`, and `smm_weekly_reports`. `clients` remains `200` because Production still reads it directly. No grant was changed on the seven explicitly retained direct-use tables.
- Public repository and Actions containment (F64/F122): prepared schema-only replacements for the three confirmed migration files passed a private 34-file census with zero email-shaped strings, HTTP(S) URLs, COPY payloads, or literal data inserts for the affected tables. They are deliberately withheld from this public branch because GitHub expanded row deletions even behind the attempted diff guard. No canonical-history rewrite or force-push was performed; the owner-scheduled freeze/Support/reclone procedure is in `docs/ops/GIT_HISTORY_PII_PURGE_2026-07-14.md`. The B1 incremental-refresh and Production-polish workflows are disabled. Exactly 414 named retained artifacts were deleted (274 B1, 55 Production screenshots, 54 review packets, 31 Argos bundles); final anonymous enumeration returns zero for those names, while 31 unrelated artifacts remain untouched. Candidate B1 output is aggregate-only/one-day/exact-schema; candidate Production output has no Actions/Argos visual delivery. Reconciler public logs and any historical external Argos builds remain open.
- Documentation/control verification after the containment truth sweep: `truth-sync` passed 383/383, `system-map-sync` passed 16/16, the aggregate-only B1 artifact regression passed, and `git diff --check` was clean.
- F44 durable-intake server containment: VIDEO PRODUCTION AUTOMATION workflow `BrJSe8zCKUccfmIq` was published at active version `f6a5ca07-e7a7-4a41-a3d3-3638a682b596`, with prior version `d867fa43-2ab2-44a4-93c8-57254846ca1c` retained as the emergency graph rollback point. The serving path now persists an idempotent Supabase receipt before work, strictly requires one client/team project plus SMM credential, filming plan and roster, and returns success only after exact parent/child create confirmation; bounded retry, payload-bearing dead-letter/replay and partial-create operator recovery are included. Safe malformed probes `268305` and `268306` each returned HTTP 400 before receipt or create; the live receipt ledger stayed at zero rows and no Linear work was created. This closes the server's early-response path but not the user-facing defect: the draft-preserving, response-checking browser change remains on `claude/reduce-n8n-linear-deps-vmphp6` pending owner merge/deploy. Until deployed readback, SMMs must verify the new card appeared in Calendar instead of trusting the green confirmation.

## 2026-07-15 — exposure PR merged + post-merge closure + independent re-verification (cloud reviewer)

- PR #836 merged to `main` at merge commit `2a991418d82106b26d7ed839a22f4aeff518a682` (owner-merged 2026-07-15T00:06Z). It carried three commits: the security containment (`ecd4cd4`), the handoff docs (`bda2040`, cloud-reviewed pre-merge), and the F44 durable-intake **browser** fix (`c7b325e`) which rode in on the same branch. Pages redeployed from `main`; live `syncview.synchrosocial.com/index.html` now contains the F44 durable-receipt logic (present) and the staff-key wiring (`x-syncview-key` present), so the onboarding/weekly/filming staff screens that were failing closed are restored with auth.
- Post-merge sequence completed from the exact merge SHA and independently re-verified live by the cloud reviewer (read-only): (a) staff screens — Codex browser-walk loaded onboarding/weekly/filming for Admin, blocked no-key and SMM-from-Admin; (b) the two deferred writers deployed — `calendar-upsert` v38 + `sample-review-upsert` v39 — cloud reviewer confirmed valid-shaped no-key → `401 credentials_required`, wrong-key → `401 invalid_staff_key` (both were unauthenticated pre-merge); (c) `filming_plans` table `REVOKE SELECT` applied — anon REST now `401`/`42501` (was `200`), EF still serves Admin; (d) B1 + Production workflows re-enabled one at a time — B1 artifact validated aggregate-only (zero row/person/email/phone keys), Production emits no artifact/Argos.
- Cloud-reviewer final live sweep (2026-07-15): all three onboarding readers `401`/0-emails; `smm-weekly-reports` + `sync_managers` `401`; all **six** browser writers `401` fail-closed on correct-shaped wrong-key (shared `authorizeBrowserWrite` gate: exactly one staff-key XOR client-token, client-scoped, inactive-denied, DB-error→503); revoked tables `thumbnail_media_revisions`/`social_media_managers`/`smm_weekly_reports`/`filming_plans` all `401`. Security not regressed by the F44 commit or the merge. Deliberately still-open and accurately disclosed: the raw PII migration file + git history (F64 — owner-parked, no live API serves it anymore), and the F88 raw-table projection work (`clients`, `team_members`, and five other direct-read tables remain anon-`200` pending least-field scoped views).
- Second-pass audit (F01–F138) adversarial distillation (39-verifier + synth workflow): 55 CONFIRMED_REAL, 42 over-graded-but-real, 23 stale/already-fixed, 8 owner-decision, 5 duplicate, 4 needs-access, 1 refuted. **12 loud P0/P1s were stale** (graded vs old #813 head `885026a`; fixed in `e3aa028`): F02, F03, F23, F33, F04, F07, F16, F08, F10, F19, F20, F21. Ranked shortlist that mattered: (1) F44 Submit green-check-no-work [now fixed server+browser], (2) F77+F64+F122 anonymous PII [readers/tables/artifacts contained; repo-history purge parked], (3) F35 open writers [closed], (4) F88 anon-readable tables [owner decision, partially revoked], (5) close #813's own gaps then merge [#813 still parked], (6) F27+F13+F14 rollback-drains-zero / no independent backup / unattributed nightly roller [pre-flip blockers, open].
- 2026-07-15 combined health check (read-only): three reconcilers green (~01:00Z). B3 mirror status parity `repair_list_size=0` on all 60 sampled runs (~15h) and `outbound_diff_count=0` → **day 8 at zero on status parity**; B4 7-day gate met. `diff_count` steady baseline 3 (all inbound, tolerated, repair 0). `linkage_actionable` moved `0→1` at ~2026-07-14T16:03Z and has held at 1 (one dry-run `calendar:video` planned linkage, not written; likely residue from today's writer redeploy / create-post testing) — flagged, no rollback (housekeeping, not drift). Webhooks 4/4 enabled; `mirror_in` fresh. Event ledgers `calendar_post_events`/`sample_review_events` 0 error/fail in 12h. Flags unchanged: `prod_authority` linear/linear, `linear_inbound_enabled` true, `auth_enforcement` permissive, `linear_outbound_enabled` off, Track-A rosters `calendar_upsert_ef_clients`/`sample_review_ef_clients`/`settings_ef_clients` all n=33, `write_ui_reroute_clients` TEST-only. Track A clean ≥8 days → its monitoring stands down; only B3 lanes watched going forward.
- Owner decisions recorded 2026-07-15: (1) the 21 people whose contact data was anonymously reachable will **not** be notified (owner's call). (2) F64 git-history purge is **parked** by the owner (accepted residual: file remains in public history, but no live endpoint serves it). (3) F15 clarified — `team_members.slack_user_id` is consumed by **no** current notification path (the post-Linear editor-notify EF does not exist; today's pings ride the legacy n8n email→Slack map + Linear itself), so backfilling it is not a blocker and only matters if/when that notifier is built; the value is held out-of-repo pending that work.
- Remaining pre-flip work (none urgent at current all-Linear-authority/TEST-only state): F11 active-card mirror backfill (~62 slots / ~10 clients) before enrolling any real client; F13 independent non-n8n backup/restore package + rehearsal before any team flip; F27/F14 executable drain-first rollback + neutralize/attribute the nightly due-date roller before any flip. No runtime flag, database row, workflow, Linear record, credential, or live client data was changed by this documentation pass.

## 2026-07-15 — INCIDENT: client-review write outage (F35 gate ⇄ F03 token removal) + OWNER WRITER-GATE FREEZE

- **Impact.** From ~00:06Z 2026-07-15 (the #836 deploy of the gated `calendar-upsert` v38 / `sample-review-upsert` v39) until ~15:05Z, **every client-review-link write (approve / comment / status) returned `401`.** Clients could still READ their content; only their saves failed. Staff writes were unaffected throughout. Verified live: zero client-role writes in the window vs. active client writes immediately before (daniellerobin 20:40–21:17Z 07-14) and after.
- **Root cause.** F03 removed `client_review_token` from the anon Clients Info sheet (correct) and #836 gated the two client writers to require a token (correct) — but the live client-review WRITE path still sourced the token from the removed sheet column (`clientMap[...].client_review_token` in index.html) and therefore sent **no** token, which the newly-gated writers rejected (`401 credentials_required`). Two individually-correct fixes shipped without the client-write path being repointed to the new token source. Cloud reviewer confirmed the decisive fact live: the Clients Info sheet no longer has a token column at all.
- **Interim restore (owner-approved).** Codex redeployed `calendar-upsert` / `sample-review-upsert` reverted to their pre-#836 UNGATED source (from `a1e2622`); tokenless TEST writes returned `200`; real client writes resumed (lilybaker 15:05–15:10Z).
- **Permanent fix (PR #838, merged `2375f3e`).** A staff-only issuer (`client-review-link` v1) now mints the current rotated token into freshly-shared links; all four share buttons were repointed off the removed sheet column; all 33 clients provisioned tokens. Codex then **RE-GATED** the writers (`calendar-upsert` v42 / `sample-review-upsert` v43).
- **Second break.** Re-gating re-broke **every client still on a PRE-EXISTING link** — those links carry old/empty `?t=` tokens that don't match the rotated tokens, so they `401` again. Client writes stopped dead at 15:10Z (the re-gate). Cloud reviewer flagged this risk *before* the re-gate and confirmed it after (zero client writes post-gate; existing-link tokens are stale). The permanent fix only helps NEWLY re-shared links; existing links stay dead once re-gated.
- **Final state (owner directive).** Codex UN-GATED again — `calendar-upsert` v43 / `sample-review-upsert` v44, tokenless, OPEN. Cloud reviewer verified live: no-key POST → `400` phantom-row (open), and multiple real clients (lilybaker, jennaphillipsballard) writing on **existing** links at 15:17–15:18Z. Clients restored.
- **Current live posture.** The two client writers are **INTENTIONALLY UN-GATED** (F35 write-integrity lock OFF for these two only). No PII risk — the reader locks (F77/F76/F82) and repo/PII remediation (F64/F122) are untouched and stay closed; this is only the "anon could forge a calendar/samples write" integrity lock, off on purpose so links keep working. The #838 issuer + 33 provisioned tokens remain in place for a future, careful re-lock.
- **⛔ OWNER FREEZE (2026-07-15).** DO NOT re-gate `calendar-upsert` or `sample-review-upsert` without **EXPLICIT owner approval**. Re-locking is permitted ONLY after **(a)** the owner affirmatively says so AND **(b)** every active client is re-issued and confirmed on a fresh link (via the `client-review-link` issuer). Any agent that believes re-gating is needed must **STOP and ASK THE OWNER FIRST**. Recorded in `AGENTS.md` (top freeze callout) and `ROLLBACK.md` (F35 row).
- **Lesson.** Never flip a live auth gate that clients depend on while they still hold credentials issued under the old scheme; migrate every holder onto the new credential first, then gate. This documentation pass changed no runtime flag, DB row, workflow, Linear record, credential, or live client data.
- **F13 independent recovery staged (2026-07-15).** Created fresh branch `claude/f13-independent-backup-restore` from current `main`, transplanting only the backup/restore implementation from parked #813 rather than merging its unrelated write-UI work. Safety audit corrected the stale 26-hour threshold to seven hours, added exhaustive Drive pagination, exact 14-table manifest enforcement, deterministic zero-row handling, malformed-newer-candidate last-known-good retention, Drive-only HMAC alert dedupe (no production marker write), complete package/dump/compressed hashes, and all declared core Track-B foreign-key checks. Focused fault suite is green for truncation, error-item, valid-zero-row, pagination, corpus/workflow-count, and readback mismatch. This is draft code only: no GitHub secret/variable was created, no schedule is active on `main`, no production backup ran, no scratch restore ran, no PITR timestamp was asserted, and no production/n8n/runtime flag/Linear data changed.
- **F13 provisioning and local recovery proof (2026-07-15).** Kept PR #840 draft/unmerged and the recurring workflow inactive. Provisioned production `backup_reader` as a connection-limited BYPASSRLS login with SELECT on the exact 14-table corpus and zero INSERT/UPDATE/DELETE/TRUNCATE privileges; its live preflight passed. Configured the production URL, HMAC key, scratch URL, and expected scratch ref in GitHub without exposing values. A real 14-table backup produced package SHA-256 `3a5858eed3b8e441ba6759a67bb72cfa1e08b78e9bf06b49cc27aac1d0da30a4` (15,520,201 bytes); independent local readback verified its HMAC, source ref, exact table set/counts, dump SHA-256 `3724ce2ada37a3c43d89a519e6d5d957beda8d6ea577db5004a3fa8007fec7fd`, and compressed SHA-256 `a0a68a525b329c190fee0805c3eb72a2bc1beeb95c1c259a1dd81a82ee832a04`. The restore guard refused the production ref. Dedicated scratch `pmzamicipnansdgomxfd` was provisioned with the 14-table schema and a narrow restore role; a transactional rehearsal completed in 229.03 seconds, matched every table count, left all 12 core orphan checks at zero, and re-enabled all user triggers. The prior n8n Drive credential is non-exportable OAuth and its folder is not reachable by the connected Google principal; no reusable service-account JSON/private folder or direct non-n8n Slack webhook exists, so Drive readback/last-known-good and direct alert proof remain owner-supplied blockers. PITR was explicitly skipped by the owner as a paid-add-on opt-out and no timestamp was fabricated. Production data remained read-only; no runtime flag, authority, n8n workflow, Linear row, recurring schedule, or `main` commit changed.
- **F13 manual Drive proof failed closed (2026-07-15).** PR #840 remained draft/unmerged and its recurring schedule remained absent from `main`. Manual branch run `29443643690` produced no accepted artifact after the snapshot client step failed; the workflow's always-run freshness check independently returned missing and exited non-zero with Slack absent. After pinning the official PostgreSQL client, manual run `29443779820` passed credentials and the production SELECT-only snapshot path, then Drive file creation returned HTTP 403; freshness again independently returned missing and failed the run. Connected-Drive readback found the target normal My Drive folder empty and no Shared Drive available. Google service accounts have no Drive storage quota, so Editor access to that My Drive folder is insufficient for file creation; the remaining gate is a Shared Drive target or user-authorized OAuth JSON. No package became last known good, no production write occurred, no runtime flag/authority/n8n/Linear state changed, and no recurring schedule was activated. GitHub failed-run email is the primary alert path; Slack is optional and absent.
- **F13 Shared Drive proof completed (2026-07-15).** Kept PR #840 draft/unmerged and the workflow absent from `main`. Source `f9406b8` added fail-closed Shared Drive folder/capability resolution, `corpora=drive` plus the resolved `driveId` on every paginated list, `supportsAllDrives=true` on create/get/download, and parent/Shared Drive checks during metadata readback. Manual `workflow_dispatch` run `29444939853` passed the production SELECT-only privilege preflight, created a transactionally consistent 14-table/53,819-row package in the private Shared Drive, and independently matched filename, parent, 15,562,462-byte length, Drive MD5 `130c2ec109239be280453462d81698a1`, downloaded bytes, and HMAC. Package SHA-256 was `3bc3f19d50f4f6c3d64559e15dacb2b1863ffcfbe256538392a12790d7ed66db`; dump SHA-256 `101a7e7b48a8ddd44661f0bedfeb64182524b689fe597c98ad6cf5fc5e11a80c`; compressed SHA-256 `bc134b4a9bca13669c0ffd91b14f88993a2c99995f2af1c4785e1ac9f8bbb219`. The always-run freshness lane separately listed, downloaded, and authenticated the same file with zero invalid candidates; a separate connected-Drive listing confirmed the file remained after runner-local cleanup. Production remained strictly read-only; no restore ran, no runtime flag/authority/n8n/Linear state changed, no merge occurred, and no recurring schedule was activated.
- 2026-07-15 PTO / Time Off implementation staged in source only: added the additive locked-table migration, role-key-authenticated `pto` Edge Function and scoped deploy workflow, dark `pto_v1` UI/rollback contract, synthetic policy fixtures, and public-safe feature/system documentation. The final adversarial pass added per-member versioned approval RPCs and found a go-live blocker: shared role keys prove a role but cannot immutably bind a person, so same-role ownership of private HR detail is not secure until an individually revocable server session is deployed and negatively tested. Keep `pto_v1` off and do not seed real HR data. **No live PTO action is claimed:** this source change did not apply the migration, deploy the function, write or flip a runtime flag, seed a member, read HR data, change n8n, or alter the Production/Linear-mirror surface. Owner-run evidence must be appended only after each action actually occurs, using the following value-free templates:
  - **Migration apply — not yet executed:** record UTC timestamp/operator label, release SHA and migration filename, schema/index/RLS/grant readback, proof that anon/authenticated table access is denied, and `pto_v1={"mode":"off"}` readback; include no member rows, dates, notes, balances, or other HR values.
  - **Edge Function deploy — not yet executed:** record UTC timestamp, workflow run, release SHA, deployed `pto` version, `--no-verify-jwt`, public-safe source/dependency fingerprint, and missing/wrong/insufficient-role denial plus valid TEST-role proof; include no key or response data.
  - **Flag change — not yet executed:** record UTC timestamp/operator label, exact previous and new mode, database readback and `flag_flips` receipt, reason, and `on → off` behavior-kill proof. Real enablement additionally requires individually bound staff sessions, negative same-role impersonation/revocation tests, owner approval, and private seed completion; never log the private roster or per-person state here.

## 2026-07-15 — F13 backup MERGED + ACTIVE, and workload tweaks fix (#842) MERGED

- **F13 independent backup merged and now ACTIVE (PR #840, merge commit `4f9d919`).** Owner merged the F13 backup/restore work to `main` on 2026-07-15. The `Track-B private backup` GitHub Action (`.github/workflows/track-b-backup.yml`) is therefore now on `main`, so its recurring **6-hourly** schedule is live; the first scheduled (non-manual) run is pending at the next cron tick. Cloud reviewer independently re-verified before the green light: Actions run `29444939853` returned `conclusion:success` (not merely "ran"); the merged source correctly implements Shared Drive uploads (`supportsAllDrives=true`, `corpora=drive`, resolved `driveId` on paginated lists, parent/Shared-Drive checks on readback); the run's own independent Drive readback (MD5 `130c2ec1…`, downloaded-byte + HMAC match) proves a real, intact 14-table/53,819-row package landed in the private Shared Drive; and a full-diff safety scan found zero secrets/keys/tokens. The private backup body itself is deliberately not opened by the reviewer (no client-data access); the run's self-readback plus the source review are the proof. Alert path on any failed/stale run is the built-in GitHub Actions failed-run email to the repository owner (`sidney.laruel@gmail.com`); Slack is optional and absent. PITR remains owner-declined as accepted residual risk. Register updated: F13 moved OPEN → **DONE** in `CUTOVER_AUDIT_2026-07-13.md`; F49/B8 F13 references updated to "backup/restore done, capacity/egress evidence still open"; `ROLLBACK.md`, `docs/ops/MONITORING.md`, `docs/ops/TRACK_B_BACKUP.md`, `docs/independence/GO_LIVE_CHECKLIST.md`, `docs/independence/B4_READINESS.md`, and `docs/truth/SUPABASE.md` updated from "draft/staged/not-scheduled" to "merged/active." This documentation pass changed no runtime flag, authority, DB row, n8n workflow, Linear record, credential, schedule, or client data.
- **Workload "Tweaks Needed" double-count fix merged (PR #842, merge commit `33f01c2`).** The Codex fix to the workload calendar classifier (`wlApplyData` in `index.html`) landed on `main`: a `needsTweak` short-circuit now routes any sub whose normalized status is "tweak(s) needed" into an exclusive `tweaksNeeded` bucket **before** the planned/overdue/nowWorking sort, so those items stop being counted in more than one bucket. `test/workload-tweak-exclusive-bucket.js` exercises the real classifier. Cloud reviewer verified the change line-by-line pre-merge: the routing is correct, "Changes in progress" remains a display-only label for stored "Tweaks Needed", and no write path is touched. This was Laura Ospina's request #1; her request #2 (~5 videos/day scheduling) is a separate write-touching feature, still deferred (likely post-migration).
- **Merge sanity confirmed.** The #840 merge combined cleanly with the parallel #842 work already on `main`; both `test/workload-tweak-exclusive-bucket.js` and `docs/features/PTO_TRACKER_HANDOFF.md` remain present on `main` (they only appeared as deletions in a branch-vs-branch diff because the F13 branch predated them). Nothing was clobbered by the merge.
- **INCIDENT — staff "Share with client" buttons fail with browser `Failed to fetch` (CORS preflight).** Owner reported the share buttons cannot generate client links. Cloud reviewer diagnosed live: the `client-review-link` issuer EF (from #838) lists `Access-Control-Allow-Headers: authorization, x-client-info, apikey, content-type, x-syncview-key` but the staff browser path (`_syncviewEfHeaders`) also sends `X-Syncview-Actor` and `X-Syncview-Role`, so the browser's CORS preflight is rejected and the request never leaves the browser — hence `Failed to fetch` with no server log. Verified live via OPTIONS probe (missing headers in the allowlist) and against the working `filming-plans` EF (which allows all three). Command-line tests bypass CORS, which is why the issuer looked healthy at #838 verification; same defect class as the #832 browser-preflight fix. **Client impact: none** — existing client links and client saves are unaffected (writers remain un-gated per the freeze); only NEW link issuance by staff is blocked. **Fix (this PR): one line** — add `x-syncview-actor, x-syncview-role` to the issuer's allow-headers. The repo fix does not change the live function: `client-review-link` has no deploy workflow, so Codex must redeploy it manually and re-verify the preflight plus one real browser share. The frozen writers were not touched.

## 2026-07-15 — PTO / Time Off go-live

- **PTO migration apply and readback verified (2026-07-15T22:04:47Z, operator `codex-pto-go-live-d36`).** Applied release `bbbe104082e75f473f9756535ff95c125cd54319`, migration `migrations/2026-07-15-pto-tracker.sql`. Readback confirmed the three-table PTO schema, required indexes/triggers/decision RPCs, RLS enabled, service-role-only grants, direct anon and authenticated access denied, and the initial `pto_v1={"mode":"off"}` state. No member rows or HR values are recorded here.
- **PTO Edge Function deployed (2026-07-15T20:32:06.814Z).** GitHub Actions run `29448635278` completed successfully from release `bbbe104082e75f473f9756535ff95c125cd54319`; live readback confirmed `pto` ACTIVE v1 with `verify_jwt=false` (`--no-verify-jwt`) and bundle fingerprint `7a520d417dd67fa75cb451eadc6dc6ab5ac7ae455c10ea7f5abede1a34d7bfb1` (pinned `@supabase/supabase-js@2.49.8`). Missing and wrong credentials returned `401`, an insufficient role returned `403`, the valid dark-state probe returned `503 feature_disabled`, and the post-enable valid-role probe returned `200`. No key or response data is recorded here.
- **PTO private seed completed (2026-07-15T21:54:55Z–21:55:04Z, operator `codex-pto-go-live-d36`).** Seeded N=9 members through the admin-only PTO path; private identity checks, required migration inputs, and every post-seed target-balance check passed before enablement. No roster, dates, balances, notes, request details, or other HR values are recorded here.
- **PTO enabled (2026-07-15T21:57:01.739111Z, operator `codex-pto-go-live-d36`).** Under owner decision D-36, changed `pto_v1` from `{"mode":"off"}` to `{"mode":"on"}` only after the N=9 private seed and balance verification passed. Database readback confirmed `{"mode":"on"}`; `flag_flips` receipt `33` recorded the same transition, actor, and timestamp. The pre-enable `503 feature_disabled` proof plus the runtime flag tests establish the `on → off` behavior kill; no post-launch outage drill was performed. Browser proof completed by 2026-07-15T22:04:47Z: Time Off appeared in the staff menu, overview loaded for Admin and one non-admin role, and one disposable TEST request completed submit → approve in Kasper → exact delete with zero residual test rows. No private roster or per-person state is recorded here.
- **PTO owner profile enabled (2026-07-16T00:02:46.084Z, operator `codex-pto-admin-update`).** The admin-only setup path enabled one additional owner-authorized profile with no usage adjustment. Direct membership and overview/accrual readbacks passed. No identity, member date, balance, or other HR value is recorded here.
- **PTO goodwill wellness credit applied (2026-07-16T00:02:46.670093Z, operator `codex-pto-admin-update`).** The admin-only adjustment path created exactly one owner-authorized goodwill credit. Post-write overview verification reached the requested balance outcome; the existing approved calendar entry remained byte-for-byte unchanged and visible, other leave state remained unchanged, and before/after hashes confirmed no other member, adjustment, or request changed. `pto_v1` remained `{"mode":"on"}` with no flag write. No identity, amount, effective date, reason, request detail, balance, or other HR value is recorded here.

## 2026-07-16 — Phase-2 whole-system merge-readiness audit (cloud reviewer, read-only)

- **Audited the merge candidate `bb0ee4b`** (current `main` + reconciled #813 write-UI + F06 deploy guard) with eight parallel surface reviews and independent adversarial verification of all 33 medium/high claims; no production write, flag change, deploy, n8n, or Linear mutation occurred. Full distillation in `docs/independence/PHASE2_MERGE_READINESS_2026-07-16.md`. Net verdict: every surface READY_WITH_NOTES; the dark merge is safe after a four-item Part C fix list (production-write CORS `x-syncview-source` omission; ungated Submit dropdown re-source for all users; Samples legacy-lane push-before-save ordering inversion; the pre-existing `deploy-thumbnail-edge-functions` freeze landmine that can redeploy the ⛔ FROZEN gated upserts on unrelated-path pushes or any manual dispatch — verified NOT triggered by this merge). Honest merge-day disclosure: the merge push same-source-redeploys the eight push-safe staff readers (fingerprint-verified byte-identical to live); the TEST client's new gateway lanes 4xx until the pinned release step. Truth-layer corrections shipped alongside: LINEAR/BRIEFING/SUPABASE/ENDPOINTS/N8N/APP reconciled to the candidate (notably: PTO documented LIVE-ON under D-36 — the stale "do not enable" directive was an outage-by-doc risk — and ⛔ freeze markers replacing the two upserts' stale "deploy after merge" instructions). PR #813 is superseded by the candidate branch and should be closed unmerged; its still-valid runbook items are carried into the readiness doc's release runbook.

## 2026-07-16 — F21 write-UI quarantine popup owner decision

- **Owner decision — startup popup removed.** Removed only the F21 `Pending Linear updates` startup notice and its notice-only count helper. The parking/quarantine writer, reader, call sites, retry lifecycle, and ops inspection hooks remain unchanged: stale pre-upgrade leftovers stay parked silently and must never auto-send; agents/ops can inspect them if ever needed. Scheduled reconcilers remain the Linear/SyncView drift-healing mechanism. An exact blob comparison proved `index.html` differs from pre-change `main` only by the notice block and its startup call; both focused write-UI suites and full `npm test` passed (125/125 unit suites). No frozen writer, parking/write behavior, runtime flag, n8n workflow, or Linear record was changed.

## 2026-07-16 - INCIDENT: F88 Linear-intake lookup outage and bounded recovery

- **Impact and root cause.** The F88 anonymous-read revoke correctly left `filming_plans` protected, but two active HTTP Request nodes in `VIDEO PRODUCTION AUTOMATION` still sent the public publishable credential. Their REST reads began returning `401` / Postgres `42501`, so every form batch stopped at the filming-plan mapping stage. The public probe still returns `401` / `42501`; anonymous `SELECT` was not restored.
- **Reader-only workflow fix.** Changed only `Fetch Filming Plans` and `Fetch Filming Plans1` in workflow `BrJSe8zCKUccfmIq`: both now use the existing stored Supabase service credential through n8n's predefined-credential mode, with no inline `apikey` or `Authorization` literal. The reader fix was first published as version `c86d7541-9a45-45a6-b497-abe939751a84`; anonymous access remained denied.
- **Protected-read proof.** The authenticated endpoint returned 30 rows. The exact production `normName()` logic produced one non-empty filming-plan URL for each of the two required client mappings. Post-fix executions `275283`, `275293`, and `275302` each reached the repaired reader and observed the protected row set before continuing downstream.
- **Linear API compatibility follow-through.** After separate owner authorization, changed only `F44 Worker Video` and `F44 Worker Graphics` to query `Project.teams.nodes` and require the same exact team-ID membership that the removed singular `Project.team` check enforced. Published active version `9e5abc46-91f0-49f8-b815-fcc6baa93891`; draft and active match, the workflow remains active at 138 nodes, settings and connections are unchanged, and all other 136 node definitions are byte-identical to the pre-patch version.
- **Live TEST proof and exact cleanup.** One `both` request used the production `video-form` and `graphic-form` webhooks. Executions `275479` and `275480` traversed the repaired workers, finalized two `created` receipts, and produced one parent plus one direct child for each team. One deterministic TEST calendar card then held both child links and emitted six event rows. Cleanup permanently deleted the four TEST Linear issues and hard-deleted exactly that card plus its six event rows; readback found zero residual TEST issues or calendar rows. The two non-deletable idempotency receipts remain as bounded audit evidence.
- **Bounded receipt recovery.** Recovered exactly four authorized real-submission receipts using the existing receipt-key and deterministic-UUID contract: two 18-child sides for one clean `both` submission and both 16-child sides from the approved later fallback. Final state is four parents plus 68 direct children, all four receipts `created`, with exact deterministic ID/title/team/project/parent readback. Both receipts for the explicitly excluded earlier fallback remain absent. One formatting-invalid recovery request was rejected before any receipt or Linear mutation.
- **Calendar recovery.** Created exactly 18 deterministic cards for the clean `both` submission, pairing the corresponding Video and Graphics child links on one card per item. On the later fallback, preserved the exact 16 pre-existing linked cards and filled only their empty Graphics-link slots; final readback found all 16 Video links and all 16 matching Graphics links, zero new cards, and globally unique target links. The separate carousel issues `GRA-6702`, `GRA-6703`, `GRA-6704`, and `GRA-6705` remained unchanged.
- **Owner-approved blank-card cleanup.** Invoked the existing soft-delete path for exactly these 12 blank-linked card IDs; executions `275537`-`275548` each completed and final readback found all 12 Archived while the 16 linked cards remained active: `p_mrmz5656_je4l6`, `p_mrmz577b_t1n6y`, `p_mrmz57ob_nmhiv`, `p_mrmz5843_h3soa`, `p_mrmz58jk_smzef`, `p_mrmz58xk_w10b1`, `p_mrmz59bz_44k50`, `p_mrmz59px_dq1tx`, `p_mrmz5a7k_hnj9z`, `p_mrmz5ar1_d5osm`, `p_mrmz5b8b_l5rop`, `p_mrmz5bpd_3xjni`.
- **Active-workflow credential audit.** Scanned all 77 active n8n workflows. Before the fix, the only revoked-table HTTP readers using the public publishable credential were the two nodes above. After publication, zero active HTTP Request nodes use that credential for `filming_plans`, `thumbnail_media_revisions`, `social_media_managers`, or `smm_weekly_reports`; no active HTTP Request node references the latter three tables.
- **Guardrail readback.** No `calendar-upsert` or `sample-review-upsert` code/deployment changed; recovery invoked existing writer/delete contracts only. The two authenticated readers were not edited during the worker follow-through. No Track-B Edge Function, workflow runtime flag, `prod_authority`, or other authority setting changed, and there were zero flag flips during the incident window.

## 2026-07-17 - PTO transactional-write release evidence

- **PTO cancellation-audit migration applied and read back (2026-07-16T23:08:07Z, operator `codex-pto-transactional-release`).** Applied candidate release `5f5100fcd12cd787ea7e1997b8abe298670f0b88`, migration `migrations/2026-07-15-pto-cancellation-audit.sql` (SHA-256 `d2aa51a70aa1a3c74ef3c476e142333d65f41e3660c582d9368cd608ee0ba33f`). Readback confirmed separate cancellation attribution, the hardened transactional request/setup/decision RPCs as security-definer and service-role-only, RLS enabled with no browser policies, direct anon/authenticated table and RPC access denied, and service-role access retained. The already-live PTO v1 overview remained readable against the additive schema. `pto_v1` stayed `{"mode":"on"}` with no flag write. No identity, member date, balance, note, key, request, or other HR value is recorded here.
- **PTO transactional Edge Function deployed (2026-07-16T23:27:16Z).** Manual migration-confirmed workflow run `29542185649` completed successfully from merge `3be6d43c53553fb89d3fa8a732768e4464a9d92c`; live readback confirmed `pto` ACTIVE v2 with `verify_jwt=false` (`--no-verify-jwt`) and bundle fingerprint `0a20c2b751471f9f24237b3b7d8e3c21db7d4f643ebbb617fcb5b061d92a4c71`. Missing credentials returned `401`, a non-admin credential on an admin action returned `403`, and a valid staff far-horizon quote returned `200` with a valid server-owned day result. No identity, key, date, day result, or response data is recorded here.
- **PTO refreshed release browser-verified on production (2026-07-17T03:20:25Z).** The one-time recovery Pages build for merge `3be6d43c53553fb89d3fa8a732768e4464a9d92c` was accepted at 2026-07-17T02:46:40Z and built at 2026-07-17T02:47:21Z. An ordinary staff session loaded the branded PTO dropdown, calendar, stepper controls, and staff-menu entry, then completed a read-only far-horizon quote without creating a request; Admin Kasper loaded Upcoming Approved Leave and Recent Decisions. Pre/post request and balance fingerprints matched exactly, `pto_v1` stayed `{"mode":"on"}` with its row unchanged, and no PTO request, admin mutation, seed, flag write, frozen-writer call, n8n call, Linear action, or Production-surface action occurred. No identity, staff date, balance, note, key, request data, day result, or other HR value is recorded here.

## 2026-07-17 — Calendar E2E nightly regression: harness lane mismatch (test-only fix)

- **Symptom and scope.** The first Calendar E2E nightly after the #850 dark merge (run `29571899744` on `8cd964b`) failed 24 of 67 probes with one signature: the UI updated but the TEST client's backend row never moved, with zero JS errors. Live-ledger verification confirmed real clients and staff were unaffected throughout (post-merge sample/calendar writes landing normally, including resolve→Kasper routes).
- **Root cause (pinned by deterministic live repro).** The nightly's TEST client is the sole member of the live `write_ui_reroute_clients` allowlist, so with that flag loaded the page takes the #850 gateway lane. The lane's intentional targetless guard (`_writeUiClassifyTargetless`: client `kind='test'` with no Linear link → `409 native_link_required`) then fails the save closed BEFORE the source write, which the flush surfaces only as `post._saveError` plus the "Note source sync pending" notice. Harness cards are seeded deliberately Linear-linkless, so every status-routing probe aborted. A second repro with the flag read losing its 2000 ms race took the legacy lane and passed — the flag race also made results environment-dependent. The product guard is working as designed; the defect was the harness testing the wrong lane.
- **Fix (qa/ only; no product, EF, flag, deploy, n8n, or Linear change).** The four harness libs (`qa/probes/lib.js`, `qa/golden_lib.js`, `qa/sxr_courier_lib.js`, `qa/ef-writepath/lib.js`) now stub exactly the `write_ui_reroute_clients` flag read dark, so the TEST client faithfully simulates the legacy lane every real client runs — deterministically, ending the 2000 ms race flake. All other runtime flags (Track-A rosters included) stay live. New probe `p95_write_ui_test_guard.js` opts back into the live flag and pins the guard itself (fails closed with `native_link_required`, no upsert POST leaves the page, backend row untouched; self-skips green if the owner empties the allowlist). Manifest updated.
- **Verification.** With the fix: previously-failing `p92` 9/9 and new `p95` 5/5 against the live backend, followed by a full re-run of all 24 previously-failing probes (results recorded in the fix PR). All probe writes remained scoped to the TEST client with per-probe archive cleanup.

## 2026-07-17 — first /site-assurance run (read-only; coverage ledger established)

- Created `docs/testing/ASSURANCE_LEDGER.md` by back-filling every EXECUTION_LOG verification record (2026-07-03 → 2026-07-16) against the `docs/QUALITY_TIERS.md` contract, then worked the tier-weight × staleness × churn scores for four cycles under the owner mandate: all tiers, ≤6 cycles, read-only probes only, findings as draft PRs. No live write, runtime-flag change, n8n edit, Linear mutation, or client-data change occurred; every live probe was GET/HEAD/OPTIONS-only.
- Cycle 1 (Tier 0 posture): live `index.html` is byte-identical to `main` (SHA-256 match). Anon-posture matrix: `filming_plans`, `thumbnail_media_revisions`, `social_media_managers`, `smm_weekly_reports`, PTO tables, `client_onboarding`, `sales_intakes` all `401`; the six reader/PTO EFs all fail closed without credentials. OPTIONS preflight on `client-review-link` now returns `x-syncview-actor, x-syncview-role` in allow-headers — the #844 CORS fix IS live (the redeploy that activated it is not recorded in this log; owner back-record requested). The two frozen client writers show the expected un-gated CORS posture; no POST was sent.
- Cycle 2 (Tier 1 Samples, F73): confirmed at source level on `main` that the prepaint gate and two inline boot copies still used the stale default-OFF sxr expression; unified all three on `_sxrEnabled()`'s GA default-on semantics, reworked `test/boot-gate-parity.js` from pinning the stale expression to pinning the GA expression plus semantic route cases, and verified in a local fresh-profile browser (deep link mounts, reload keeps it, `?sxr=0` opts out, `?sxr=1` overrides). Full offline suite green. Shipped as draft PR from `claude/f73-sxr-boot-gate-ga`.
- Cycle 3 (monitors/mirror/backup freshness, read-only GitHub + n8n APIs): F13 backup — five scheduled 6-hourly runs since merge, all green (latest 2026-07-17T03:44Z), closing the "first scheduled run pending" gap. Calendar E2E nightly green 2026-07-16. **Samples E2E nightly red four consecutive nights (07-13 → 07-16), untriaged**: two deterministic failures registered as F140 (Kasper Finish disabled after a graphic tweak request) and F141 (drag-reorder persist lost with DOM ≡ DB on the wrong order). **New finding F139 (live)**: a stored UUID-form Linear link poisons the shared `linear-issue-statuses` batch via garbage-ident extraction, causing five overnight webhook timeouts, one red 01:30Z reconcile run, and a per-pass heal burst (execution burn); root cause reproduced locally from the node's own code and payload.
- Cycle 4 (F139 repo-side fix): hardened `_calIdentFromUrl` to strict `/issue/<ident>`-segment extraction (both reconcilers inherit it by extraction), added batch hygiene + loud skip counts to both reconcilers and the browser status-import caller, and added `test/linear-ident-uuid-guard.js`. Offline suite 127/127 green; `parity_logic` NO GAPS. Shipped as draft PR from `claude/linear-ident-uuid-sanitize`. The n8n Code-node regex/fallback fix and the stored-link cleanup are deliberately owner-gated (register F139).
- Register F139–F141 appended to `CUTOVER_AUDIT_2026-07-13.md`; BRIEFING boundary moved to F141; monitoring gaps table updated. Stopped after four cycles: every remaining top-scored gap needs either a TEST-client drill (excluded by this run's read-only mandate) or an owner decision — both queued as one-liners in the ledger.

## 2026-07-17

### Bug archaeology run (history-driven preventive sweep, last-3-weeks scope)

- Ran the `/bug-archaeology` protocol over the 3-week change window: corpus = this log's incidents, the cutover F-register, fix-flavored PRs, and bug-born regression suites. Loop accounting: 2 sweep cycles + ship; 14 scored candidates; 8 refuted with evidence; 3 parked undecided (PTO deploy-gate `event.before` zeros edge; intake dead-letter alerting depth; folder-resolve deploy-workflow coverage); 3 confirmed, registered as F142-F144 in `docs/independence/CUTOVER_AUDIT_2026-07-13.md` (2026-07-17 addendum). Full report: `docs/audits/2026-07-17-bug-archaeology.md`.
- All live checks were READ-ONLY: OPTIONS preflight probes against deployed Edge Functions, a key-only read of `syncview_runtime_flags.write_ui_reroute_clients` (value never read into public artifacts), and a read-only n8n readback of workflow `BrJSe8zCKUccfmIq` active version `9e5abc46`. No live state, flag, workflow, or deployment was changed by this run. The two ⛔ FROZEN client writers were not touched and no re-gating is recommended anywhere in the outputs.
- Confirmed and shipped as draft PRs (owner review gates all merges): F142 `thumbnail-folder-resolve` CORS allowlist missing `x-syncview-key` (source fix + new class guard `test/ef-cors-allow-headers.js`; live redeploy owed manually - no deploy workflow covers this EF); F144 F44 intake installer still emitting the Linear-removed `Project.team` singular query that the 2026-07-16 outage was hand-patched around (installer + test-mock fix, ported verbatim from the live workers); preventive guard `test/linear-sync-reconcile-extraction.js` for the calendar reconciler's eval sandbox (the `_sxrNormStatus` crash class, previously guarded only on the samples twin).
- F143 (no code change; OWNER ACTION): live `production-write` still lacks `x-syncview-source` in `Access-Control-Allow-Headers` - the Part C item-1 fix is on `main` (`139a4c8`) but the manual-only pinned dispatch has not run, so browser writes through the dark write-UI reroute lane preflight-fail while Node drills stay green. Recommended: pinned manual `deploy-onboarding-edge-functions` dispatch at a `main` SHA >= `139a4c8`, then re-probe. This function is not a frozen writer.
- Docs trued up in the same change: `ROLLBACK.md` Live State gained the missing `write_ui_reroute_clients` kill-switch row (newest live flag was absent from the emergency runbook); `docs/truth/BRIEFING.md` boundary bumped to F144.

## 2026-07-17 — shadow-audit workflow startup failure + nightly paging (CI-only fix)

- **`production-shadow-audit.yml` invalid since 2026-07-13:** `${{ runner.temp }}` in the job-level `env` block is not a valid context there, so GitHub rejected the file and logged an instant failed run on every push to every branch (the failed-run email flood), and the daily read-only shadow audit itself never ran after 2026-07-13. Fixed by moving the private-path env onto the audit step, where the runner context is valid. Monitoring-gap note: B4 shadow-audit coverage was dark 07-13 → 07-17.
- **Nightly paging (owner directive 2026-07-17):** both E2E nightly workflows now post a failure notice through the existing `SLACK_ALERT_WEBHOOK` (the SyncView Bot channel) on scheduled failures, degrading to a log warning when the secret is absent. Rationale: the samples nightly was red four consecutive nights with no consumer of the signal.

## 2026-07-18 — F140 Samples Kasper Finish regression

- **Owner ruling and root cause.** The owner ruled that Samples must match Calendar: Kasper may stack component change requests, then Finish hands the card to the SMM. The strict `_sxrKasperUndecidedComps` gate was correct. On the post-#850 enrolled-gateway path, `_sxrKasperApplyAndPersist` incorrectly treated a fresh, never-attempted status companion as replay debt; recovery reconciliation could adopt a newer native `Kasper Approval` clock before the first `Tweaks Needed` send/source save, so echo/reload restored the undecided state and Finish stayed disabled.
- **Scoped fix (`939bdfc`).** `_sxrKasperApplyAndPersist` now runs recovery reconciliation only for companions whose intent was already attempted. Fresh companions execute the status send and source persistence; already-attempted companions retain the old reconciliation behavior. The Finish gate itself is unchanged. The owner ruling is attached to the existing `graphic__smm_approve__kasper_request__finish` tree leaf.
- **Deterministic proof.** `test/write-ui-repair-races.js` proves a fresh companion performs exactly one `Tweaks Needed` send, persists that status, and leaves the real Finish gate empty; the same test proves an attempted recovery companion still adopts provably newer native truth without a duplicate send. Full local `npm test` passed all 131 unit suites. Full unfiltered branch run `29629528360` at `939bdfc` passed the exact owner-ratified F140 leaf 8/8, parity, realtime, and production-preview smoke. Its aggregate remained red only on independent existing debt: F141 reorder, TEST-row teardown divergence, and truth-sync's one-commit checkout ancestry failure. The workflow-skipped ten-probe set was then executed in full from the same SHA: all F140-adjacent Kasper/gating/concurrency/cold-open/realtime probes were green; three unrelated stale probes remained red on four assertions (the share-token stub twice and the twin pre-#850 outbox mocks once each).
- **Historical correction and safety.** The exact F140 leaf failed 6/8 in runs `29240029813` (07-13) and `29483415231` (07-16), but run `29317692645` had tree 24/24 green; those heads predate #813/#850, so `939bdfc` is recorded as the separately proven current cause rather than retroactively assigned to every historical red run. `calendar-upsert` and `sample-review-upsert` remain byte-identical to base `32099d5`; no runtime flag, authority setting, Edge Function, n8n workflow, Linear record, or production row was changed. Rollback is a revert of `939bdfc`, which reopens F140 but touches no live control plane. Cloud review is required before merge.

## 2026-07-18 — Feedback-expansion round 1: Production tab vs real Linear (owner observations 2026-07-17)

- Ran the `/feedback-expansion` protocol over the four owner observations; each was generalized to its rule, swept across the whole `?prod=1` surface, expanded to sibling rules (dry at generation 3), and landed as seven commits on `claude/prod-tab-linear-parity-mf6bhj` (draft PR; owner review gates the merge — merging deploys).
- Shipped, all read-only display-layer: canceled deliverables stay visible as a Canceled group (`_prodDeliverableLive` no longer treats `canceledAt` as deleted); written date format on all date pills with year for non-current years; informational tooltips survive the write gate (`info|action-or-gate` composition) including due + overdue-by-N-days, status name + time-in-status (rows via `status_at`, detail via lazy `deliverable_events` reads — first runtime consumer of the event reader; F138 activity feed stays dormant); sub-issue parent breadcrumb above the detail title and inline in project rows; artifact-tip census completed (sidebar/board/group headers/subchips); narrow layout hides the created column.
- Zero live mutations throughout (readonly-smoke green); no runtime flags, backend, n8n, or Linear wiring touched. All 130 unit suites + the full 10-lane prod battery green locally; samples-lane master reds are the pre-existing F140/F141 nightly defects (diff audited: every hunk inside the Production block). Review-packet validator copy expectation modernized to the shipped signed-out Comments gate.
- Environment note (local QA only, not committed): this container's egress middlebox resets Chromium TLS; live-read browser lanes ran through a session-local Playwright request-bridge shim.

## 2026-07-18 — Round-1 owner decisions applied (items 6 + 7)

- Owner ratified round-1 items 1-5 and 8-10 as built. Two ordered changes shipped on the same round PR:
- **Compact old-year dates (item 6):** every date column/pill renders the compact written form ("Nov 11") regardless of year — no two-line wrap on desktop; year accuracy moved into every date hover via `_prodFmtDateFull` ("Nov 11, 2025" in due/created/target/updated tooltips).
- **Active tab hides approved (item 7) — STAFF-VISIBLE CHANGE:** the Production Active tab no longer lists approved deliverables (they remain on the All tab); `_prodTabAllows` now matches the artifact's active set exactly. Staff who watched approved rows on Active will find them under All issues.

## 2026-07-18 — F105 Production polish gate repair (test-only; cloud review pending)

- **Current-main and hosted baseline.** The candidate is based on current `origin/main` `6a8416c5ae2894ff90012ef129152c97349add17`. Hosted run `29654228319` had fast red with interaction/heavy green; later current-main run `29655695552` had fast/interaction green with heavy red. F122 intentionally keeps detailed assertions runner-local and publishes no artifact, so exact-source local reproductions supplied the private failure evidence. The first current-main fast reproduction failed only on two generic HTTP 500 resource messages in accessibility while its functional and zero-write assertions passed; an unchanged rerun was green. The first aggregate reproduction later reached 167 functional behavior assertions plus `noWriteRequests=true`, then failed only `noConsoleErrors` on a recovered paginated read.
- **Complete failure classification.** Category **(a), four stale/ambiguous owner-behavior assertions in the post-#813 test epoch:** interaction status, due, and assignee checks selected the first row and could hit the bounded writable TEST override; the latent layout check still required the removed secondary-line project parent trail instead of the owner-ratified inline title/parent form. Category **(b), broken checks:** `dueFocusSync`, `ctrlXGuard`, `composerBoxClick`, `filterArrowRight`, `commentBodyWrap`, and `commentSignedOutStateNotSkeleton` all shared one empty Video/Active reset-baseline synchronization defect; structure, layout, accessibility, and behavior also shared a console/read audit that treated a subsequently recovered eligible read as an unconditional failure and could snapshot before outstanding reads settled. Category **(c), real product defect:** zero. Every product behavior and mutation invariant passed once the test baseline/check mechanics were isolated; unchanged reruns and direct read probes were healthy.
- **Narrow robot-only repair.** `prod-interaction-inventory.js` pins the locked contract to one exact non-TEST row and requires the current zero-picker/exact-authority-toast result. `prod-layout-polish.js` selects an exact visible child row and checks the current inline project breadcrumb. `behav-wired.js` waits for loaded/non-loading/non-error state and uses a real Video/Graphics All-tab row only after an independent owner-active status count proves the configured Video/Active fixture is genuinely empty; eligible hidden rows fail immediately. Shared `installReadConsoleAudit` coverage in structure/layout/accessibility/behavior accepts only an exact method+URL eligible network/429/5xx failure followed by 2xx/304 within the bounded window; each generic resource-console error additionally needs a one-to-one exact URL/time match. Non-retryable, persistent, pending, unrelated, and unproven-abort failures remain red; only old-document reads proven canceled by a committed main-frame navigation are counted separately. Each mutation audit runs after the bounded settle, so late traffic cannot escape the zero-write check. The bounded active-TEST override, fully intercepted writable contract, unsupported-operation guards, and live-mutation rejection are unchanged.
- **Final local proof.** The official exact-source fast lane passed 7/7 in 143.8s; structure exercised one eligible exact read recovery while every functional and zero-write check stayed green. The full interaction inventory passed all list/detail/board/project/right-click/hover checks with zero writes/errors; `behav-wired` passed 168/168 with `noWriteRequests=true`, `noConsoleErrors=true`, zero recovered read retries, and one proven navigation-aborted read. The required `npm run test:prod-polish` passed all 10 suites in 484.4s, including deterministic exact-child layout, independently proven-empty behavior reset, and light/dark pixel parity. Offline `npm test` passed all 132 unit suites, including the new dependency-free read-audit matrix and `truth-sync` 390/390; all changed JavaScript files passed `node --check`, and `git diff --check` was clean. The matrix proves exact recovery, 4xx/redirect/persistent/pending failure, one-to-one console matching, old-vs-new-document abort classification, late-response handling, sanitized diagnostics, and post-settle mutation-audit ordering. No `index.html`, Edge Function, runtime flag, database row, n8n workflow, Linear object, credential, deployment, or live data was changed.
- **Handoff boundary.** The exact candidate still requires a green manual `workflow_dispatch` fast/interaction/heavy run and cloud review before merge. Detailed logs, screenshots, review packets, and local Argos-format exports remain runner-local/private; no public artifact or Argos delivery is authorized.

## 2026-07-19 — /site-assurance run 2 (TEST-drill mandate; F141 root-caused + repo fix; Tier-0 drills sandbox-blocked)

- Read the ledger, quality contract, briefing, and the F141 register row, then worked the mandate's priority order. No live write, runtime-flag change, n8n edit, Linear mutation, or client-data change occurred; every live probe was GET/HEAD/OPTIONS-only or an auth-denied POST that mutated nothing.
- **F141 root cause + repo fix (silent reorder loss).** The `sample-review-reorder` Edge Function returned `updated` equal to the count of *requested* items rather than the count of rows actually matched; the browser's `_sxrPersistReorder` verified only the ok flag. A reorder referencing any id that did not match a live row (stale/mid-create/archived/raced) was therefore skipped by the `.eq(id)` update yet reported as success — the unpersisted card snapped back on the next server-truth reload with no error surfaced (cross-tier invariant 2). The calendar reorder EF+client were already correct; samples diverged when the writer was gated onto the EF. Fixed the EF to report the true matched-row count and the client to fail closed on a short count (EF route), with symmetric source guards plus a new hermetic behavioral guard that extracts the real function and proves revert-on-partial / persist-on-complete. Full offline suite green.
- **Classification answered (the register's question).** Live read-only auth probes confirmed the reorder path does not 401 in the signed-in flow (a keyless caller is denied; a signed-in actor carries its key), the write fires and returns success, and the loss was silent — i.e. it *reverts silently*, not a 401 and not a never-fires. The dormant n8n fallback workflow was read and confirmed to carry the same requested-count response; its correction is owner-gated and re-queued.
- **Tier-0 client-facing drills not executed.** The share-link mint and `?c=` client-link save drills both require credentials this sandbox does not hold: the issuance EF enforces auth, client review tokens are not anon-readable, and there is no staff key. Recorded the read-only confirmations available instead (auth enforced; correct anon posture; deployed `index.html` byte-identical to `origin/main`) and re-queued both drills with prompt-ready specs and an owner one-liner to name a staff-key-bearing runner.
- Findings shipped as a draft PR for cloud-reviewer verification; nothing merged. Ledger + register + owner one-liners updated with the evidence above. Checkpointed each unit as a pushed commit per the night-shift protocol.

## 2026-07-19 — Workload static due-date mode (source only)

- Replaced Workload's browser-only due-minus-one / ASAP capacity scheduler with exact due-date
  bucketing. Capacity remains 5 video / 15 graphics as a red warning only; overloaded dates retain
  every item and every editor rollup. Assigned undated work now appears in a grey **Needs a due
  date** strip instead of receiving an invented date.
- Preserved the `Tweak Needed` / `Tweaks Needed` exclusive bucket, removed the phantom
  `Plan X → Due Y` popover copy and due-date ghost markers, and updated the workload regression
  robot with literal-date, overload-threshold, all-visible, undated, and scheduler-removal guards.
- Client-side source only: no Edge Function, migration, runtime flag, n8n workflow, Linear write,
  database row, or live deployment changed. The frozen `calendar-upsert` and
  `sample-review-upsert` writers were not touched.

## 2026-07-19 — F141 post-merge Edge deploy + live TEST drill

- **`sample-review-reorder` Edge Function deploy (2026-07-19T17:21:51.600Z).** Before deployment, downloaded the active v31 server-source closure to a private rollback anchor and hash-manifested the function plus its three `_shared` dependencies; pre-readback was `ACTIVE`, v31, `verify_jwt=false`, bundle `bd46b572…`. Deployed **only** `sample-review-reorder` to project `uzltbbrjidmjwwfakwve` from merged release SHA `8382d9bbdf52b389ac38289638c724d36ebb4bcf` (PR #878 head `462bff2c…`) with Supabase CLI 2.109.0, `--no-verify-jwt --use-api`. Independent live readback: `ACTIVE`, v32, `verify_jwt=false`, bundle `c3eca2418c4e598ef598475b45e1fbec692570ddbe52f2916130f1a968f98ba5`. Frozen `calendar-upsert` stayed v43 / `91ce449e…` and `sample-review-upsert` stayed v44 / `50b63fba…`; no runtime flag, schema, n8n workflow, or other Edge Function changed. The source download is a rollback anchor, not an F51-attested exact platform artifact.
- **Live TEST browser proof (2026-07-19T17:59:57.952Z → 18:00:27.392Z).** With the owner's authorized verified Admin session on `sidneylaruel`, the merged live client created a disposable card, waited for its real id, reordered two live rows, received HTTP 200 / `{"ok":true,"updated":2}`, and kept the new first position after a forced server-truth reload. The negative drill then sent the same two-item reorder shape with two non-matching synthetic ids: the function returned HTTP 200 / `{"ok":true,"updated":0}`, the fixed client restored the exact pre-drag strip order and showed `Couldn't save the new order` / `It was put back — please try again.`, and the backend order fingerprint stayed byte-identical. Cleanup archived the disposable card; independent readback found zero active `F141 live drill` fixtures and restored the original one-row TEST order fingerprint. Live `index.html` matched merge `8382d9bb…` byte-for-byte; frozen writer versions/hashes remained unchanged. Sanitized screenshots, network captures, hashes, and cleanup proof are in `docs/audits/2026-07-19-f141-live-drill/`.

## 2026-07-19 — F124 analytics scrape containment published + July-18 metrics repair

- **Metrics receipt column prepared.** After creating and independently verifying the private pre-F124 workbook snapshot, added the single header `analytics_receipt` at `Metrics!Q1` through successful one-time execution `286268`. Post-write readback confirmed the exact header value and empty data cells beneath it; no other header or worksheet cell was changed by this step. The private snapshot remains unchanged as the Sheet rollback source.
- **n8n F124 workflow published with rollback retained.** Published workflow `Q4n1bagJYBkurEaI` (`CLIENTS METRICS`) at exact reviewed version `b92fb693-1dd4-4ce2-a60e-98a1701c369d`. Post-publish readback confirmed that version as both current and active with 67 nodes. Prior active version `fb180e5f-79ee-4d49-9dec-70999b422b71` remains in workflow history as the direct rollback target; no unrelated workflow was published.
- **July-18 false-zero rows repaired from the verified last-good snapshot.** Successful one-time execution `286327` updated exactly 14 changed cells in `Metrics`: `C2759`, `E2759`, `C2760:E2760`, `C2763:E2763`, `C2766:E2766`, and `C2770:E2770`. `D2759` was already the correct zero and was deliberately left untouched. Post-write comparison against the snapshot found exactly those 14 value differences, zero metadata differences, and no other changed cell. The one-time credential helper was then archived.

## 2026-07-19 — Workload editable internal plan date (candidate source only)

- Prepared the Phase-2 source from SMM feedback: the Linear due date remains a display-only client
  deadline, while an internal `plan_date` is keyed by the stable sub-issue id in the new
  service-role-only `workload_plan` sidecar. No column or foreign key is added to the B3-managed
  `workload_issues` mirror.
- Added the source contract for the staff-authenticated `workload-plan` Edge Function and the
  drag/edit/clear browser path. Clearing the internal day returns the issue to its exact due date;
  the candidate has no n8n fallback, Linear due-date writer, or automatic scheduler.
- F141 is born into the contract: the function reports rows actually written and the browser accepts
  only exactly one, reverting the optimistic date and notifying on a short count or error. Plan-list
  failure keeps last-good overrides when available; otherwise it shows an explicit due-date-only
  degraded state with editing disabled. Reads and writes are time-bounded, only the newest
  overlapping refresh can publish state, and an authentication/authorization denial purges the
  private plan projection. The function keyset-pages by stable issue id and logs only recognized
  aggregate action labels.
- Candidate verification passed: all 137 offline suites, the 10-suite read-only Production polish
  aggregate, focused workload source/F141/tweak-exclusivity robots, deploy-manifest freshness,
  whitespace checks, and a Deno type-check of `supabase/functions/workload-plan/index.ts`.
- Cloud-review follow-through makes Week a rolling seven-calendar-day view so literal Saturday and
  Sunday deadlines/plan days cannot disappear, and computes overload styling from the rows visible
  under the current filters so hidden editors cannot make a filtered day look falsely over capacity.
  The unfiltered 5-video / 15-graphics thresholds and all-visible/no-spill rule remain unchanged.
- During the required post-merge migration/function deployment window, a cached Workload snapshot
  now paints immediately in Phase-1 exact-due-date mode while plan editing remains visibly paused;
  no runtime flag or speculative plan data is introduced. An authoritative sidecar list replaces
  that fallback once the new function is available.
- Browser persistence assurance remains intentionally unclaimed until the post-deploy private TEST
  drill proves one exact set/list/clear cycle, due-date immutability, short-count rollback, and
  byte-exact cleanup of any sidecar row that did not exist before the drill.
- **No live operation occurred.** `migrations/2026-07-19-workload-plan.sql` has not been applied,
  `supabase/functions/workload-plan/index.ts` has not been deployed, and no database row, runtime
  flag, n8n workflow, Linear object, authority value, or production deployment changed. The frozen
  `calendar-upsert` and `sample-review-upsert` writers remain untouched. Migration apply/readback and
  exact-main-SHA function deploy/readback remain post-merge operator steps.
