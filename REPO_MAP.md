# Repo map

The one-page guide to where everything lives. **This file is enforced**: the
offline unit suite (`test/repo-map-sync.js`, run by `npm test` and by CI on
every push) fails if a top-level path or `docs/` subdirectory is added,
renamed, or removed without updating this map — so keep it current in the same
commit as any structural change.

## Top level

Linear-exit combined runtime preparation and release-order holds:
`docs/audits/2026-09-06-linear-exit-runtime-integration.md`. Retains the canonical
G0–G10 plan in PR1268, exact component inputs and separate source/serving proof.

Native Create Post editor projection (stacked on PR1309):
`scripts/native-intake-manifest/editor-projection-lane.mjs`,
`scripts/native-intake-manifest/editor-projection-journeys.mjs`,
`scripts/native-intake-manifest/editor-projection-shim.mjs`,
`test/native-intake-editor-projection.js`, `test/native-intake-editor-browser.js`,
and `docs/audits/2026-09-05-native-intake-editor-projection.md`.
Staff-only read; no new migration, flag, public roster or existing-work picker change.

Native assignee eligibility draft (on PR1302): `scripts/native-intake-manifest/assignee-lane.mjs`
(real-handler assignment journeys, denied provider transport, PR1302-head negative control),
`scripts/native-intake-manifest/fault-shim.mjs` (one-read fault wrapper over the pinned shim),
`scripts/native-assignee-catalog-dryrun.js` (roster readiness aggregate, counts only),
`test/native-assignee-eligibility.js`, `test/native-assignee-policy.js`,
`test/native-assignee-catalog-dryrun.js` (complete-or-refuse REST paging), and
`docs/audits/2026-09-05-native-assignee-eligibility.md`. Adds no migration, flag or RPC.

Server-owned completion of accepted native intake (draft, unapplied, disabled):
`migrations/2026-09-05-native-intake-reconcile.sql`, `scripts/native-intake-reconcile/`
(runner library, dry-run REST entry, proof lane and gateway loader),
`test/native-intake-reconcile.js`, `test/native-intake-reconcile-cli.js` (public
output carries no identifier), `.github/workflows/native-intake-reconcile.yml`
(manual-only, no schedule) and `docs/audits/2026-09-05-native-intake-reconcile.md`.
Includes the card provenance table and one fact-recording trigger per card table;
automatic card creation is structurally held (`card_creation_held`). Stacked on
PR1302; integrated ledger item 160 (formerly 156) and its corrections.

Disabled native-only intake draft: `migrations/2026-09-05-native-only-intake.sql`,
`scripts/native-intake-manifest/native-only-lane.mjs`, `test/native-only-intake.js`,
and `docs/audits/2026-09-05-native-only-intake.md`. Retains PR1293's harness and manifest contracts.

Root expected-request manifest draft: `migrations/2026-09-05-native-intake-root-manifest.sql`,
`scripts/native-intake-manifest/` (pinned bootstrap/shim, actual gateway proof),
`test/native-intake-manifest.js`, and `docs/audits/2026-09-05-native-intake-manifest.md`.
Server-only prospective storage/detectability; no deployed recovery/materializer claim.

Mapped feedback: `docs/features/COMPONENT_FEEDBACK.md` owns the draft read contract;
`supabase/functions/production-comments/feedback.mjs` provides the staff-only exact
component source projection; `test/component-feedback-read.js` executes its real
handler; `docs/syncview-design/tests/prod-feedback-browser.js` tests the actual
component renderer, loader and focus behavior with fictional transport.
`test/prod-context-menu-pixel-contract.js` preserves the pixel comparator's
icon, shortcut and explicit submenu/refusal contracts.

Comment add receipt reconstruction: `qa/comment-receipt-fingerprint/README.md`
documents the source-only repair and limits; `run.mjs` in that folder loads the
complete gateway handler with synthetic RPC-shaped persistence.
`test/comment-receipt-fingerprint.js` runs the baseline/candidate exact-receipt,
unchanged accepted-hash and changed-request refusal matrix offline.

Review composer preservation: `docs/ops/REVIEW_FEEDBACK_DRAFTS.md` owns the
schema/authority/recovery limits. `test/review-draft-ownership.js` executes the
actual helpers through `test/helpers/review-draft-source.js`.
`qa/feedback-drafts/` contains complete-document failure/retry/identity and real
BFCache tests with fictional intercepted transports; its README states the
server assumptions and local-only proof limits.
The blocked Calendar visibility experiment and its red fresh-page prerequisite
are recorded in `docs/ops/CALENDAR_FEEDBACK_RECOVERY_ACCESS_BLOCKER.md`;
`qa/feedback-drafts/calendar-recovery-access.js` is its intercepted regression.
The bounded frontend extension and legacy-debt limits live in
`docs/ops/CALENDAR_FEEDBACK_RECONCILIATION.md`.
`qa/feedback-drafts/calendar-recovery-races.js` preserves nullable-cell, alias and
native/source race acceptance controls, including unresolved failures.
`qa/feedback-drafts/calendar-recovery-contract.js` verifies visible safe holds
(missing metadata or context, unreserved/unproven companion status, changed row).

Calendar feedback recovery slice 1: `docs/ops/CALENDAR_FEEDBACK_RECOVERY_CONTRACT.md`
is the executable contract; `migrations/2026-09-05-calendar-feedback-recovery.sql`
adds the service-only `calendar_feedback_recovery_apply_v1` RPC and its
insert-only evidence table; `production-write` gains the additive
`recover_source` modifier. `qa/calendar-feedback-recovery/` (README, `pg.js`
disposable PostgreSQL loader, `seam.js` psql-backed Supabase seam, `edge.mjs`
in-process handler loader, `handler.mjs` matrix, `browser.js` actual-control
matrix) is the decisive proof; `test/calendar-feedback-recovery-handler.js` and
`test/calendar-feedback-recovery-browser.js` are its unit entry points (they
skip loudly without PostgreSQL 16 / Chromium).
The bounded independent corrections and preserved negative controls are in
`docs/audits/2026-09-06-calendar-feedback-recovery-independent-corrections.md`:
accepted-comment-bound status receipts, constrained fields, missing-body holds,
and refused root-note draft retention; no frozen writer changes.

First Samples release composition and held live gates:
`docs/ops/FIRST_SAMPLES_RELEASE_PACKET.md`. The recovery browser runner accepts
`SAMPLES_RECOVERY_TARGET_SHA` and records the exact integrated document tested.

Samples recovery prerequisite: `scripts/samples-recovery-build.js` creates a
guarded, offline reader inverse without changing the checkout. The operating
constraints and pinned receipts live in `docs/ops/SAMPLES_RECOVERY_REHEARSAL.md`.
`test/samples-recovery-build.js` checks drift refusals and unverified read behavior;
`qa/boot/samples-recovery-rehearsal.js` exercises actual forward/recovery/forward
documents with intercepted writes, owned receipt conservation and negative controls.

Samples local-work correction: `test/samples-local-work.js` exercises held saves,
reload recovery, client/actor ownership, storage failures and legacy mixed caches.
`test/helpers/samples-work-context.js` loads the shipped recovery helpers for the
existing mocked writer suites. `node qa/boot/samples-local-work.js` runs the full
local app with intercepted writes, real typing/debounce, client switches, fresh
browser recovery and accessible storage retry. Recovery records are separate
from verified read caches; unknown legacy draft bytes remain private and unassigned.
Field-level recovery debt survives later batches until matching acknowledgement;
existing staff identity cleanup suspends the Samples view without deleting its
owned recovery bytes. Storage Retry reattempts incomplete per-record hydration.

Samples G1 regression: `test/samples-authoritative-read.js` executes the shipped
per-client reader, loader and cache with fictional fixtures and no external I/O.
The existing `qa/boot/client-entry-sequence.js` supplies exact-count Samples receipts
in its fully mocked client-navigation lane.

| Path | What it is |
|---|---|
| `index.html` | **The entire application** — a single-file SPA, served to production by GitHub Pages from `main`. Merging to `main` ships immediately. Deliberate nav mapping: visible **SyncLinear** (labelled `Linear` until 2026-08-21) = `navProd` / key `production` / `#production` (`?prod=1` alias, authority-gated native mirror); visible **Submit** = `navLinear` / key `linear` / `#linear` (submission form). The mirror's status/comment/due/assignee controls write only through `production-write` when role/team/authority gates pass (or for the bounded active-TEST override); current authority must be read back, not inferred from this map. |
| `404.html`, `CNAME` | GitHub Pages plumbing (SPA fallback redirect; the `syncview.synchrosocial.com` domain). Must stay at root. |
| `README.md` | Project overview: what SyncView does, architecture, development, deployment. |
| `REPO_MAP.md` | This file. |
| `CLAUDE.md` | **The file a Claude Code session actually loads on turn one.** Carries only what a session needs in its first minute and would otherwise ask the owner for again: that the F27 capture is already automated on his machine (`f27capture` / `$env:USERPROFILE\.syncview\f27-capture.ps1`) and must not be re-derived by hand, which deploy lane needs a capture and which does not, that the lanes fail CLOSED on a digest mismatch and digests are per-function, and not to merge anything between handing over a deploy SHA and the dispatch. `AGENTS.md` outranks it and holds the standing rules; this file exists because `AGENTS.md` is NOT auto-loaded, so operator facts recorded only there were repeatedly not read. |
| `AGENTS.md` | Instructions for coding agents (Production-tab rules, gates, doc upkeep). |
| `ROLLBACK.md` | **The one-step-rollback runbook ("law")** + the Live State table. Stays at root so it is findable in an emergency. |
| `EXECUTION_LOG.md` | Running dated log of every deploy, flag flip, migration, backup, and incident (rollback rule 5). |
| `package.json` | npm scripts for every test lane (see "Test & automation entry points" below); dev deps are Playwright + Argos only. |
| `synchro-social-favicon.png`, `synchro-social-logo.png`, `syncview-favicon.png` | Runtime images referenced by `index.html` via relative URL. Must stay at root. |
| `.gitattributes`, `.gitignore` | LF normalization (unit tests string-extract from `index.html`); ignore rules for generated artifacts. |

## Runtime asset folders (served by GitHub Pages — do NOT move)

All referenced from `index.html` by **relative URL**; moving them breaks the live site.

| Path | What it is |
|---|---|
| `onboarding-ai/` | Images for the AI onboarding funnel. |
| `onboarding-audio/` | Music-genre preview MP3s for the onboarding form. |
| `onboarding-video/` | Subtitle-style/sample preview MP4s for the onboarding form. |
| `nav-icons/` | The owner's header tab icons (2026-08-21). One `<slug>.png` per tab (rendered at 16px in `#headerNav`) plus one `<slug>-favicon.png` per tab (swapped into `link[rel=icon]` by `navTo`, so the browser tab icon follows the section). Slugs are the icon set's own names, mapped to routes in `SYNCVIEW_TAB_FAVICONS`; note `synclinear.*` belongs to the **`production`** route. |
| `thumbnail-styles/` | Thumbnail font/style preview JPGs for the onboarding form. |
| `thumbnails/` | **SyncThumbnails** — a self-contained sister app (own README/CLAUDE.md, no build step). Nothing in SyncView references it; it is served at `/thumbnails/`. |

## Backend & data

The draft private card-history slice is mapped in
`docs/ops/CARD_CHANGE_HISTORY.md`: additive
`migrations/2026-09-05-card-change-journal.sql`, real local-only
`scripts/card-change-journal-rehearsal.js`, and its target-boundary test
`test/card-change-journal.js`. `scripts/card-change-journal-rollback.sql` is the
future owner-operated capture-disable transaction that retains history. The SQL
rehearsal also runs against the existing
disposable PostgreSQL service in `calendar-unit-tests.yml`; it reaches no live
backend. Existing `scripts/track-b-backup.js` and
`scripts/track-b-restore-rehearsal.js` own versioned private package coverage and
remain separate from any production writer deployment.
`test/track-b-backup-corpus.js` gates legacy-versus-history package coverage;
`scripts/track-b-history-backup-prerequisites.sql` prepares the separately
reviewed private backup grants and scratch-only restore helper after all
expanded-corpus schema prerequisites exist.
`scripts/card-history-backup-rehearsal.js` proves the actual 21-table private
dump/package/restore against disposable PostgreSQL. Its PR #1293 manifest
prerequisite is fetched at an immutable commit and hash-checked for this local
proof only; it does not install production schema or deploy the gateway.
The corrective `history-v5` format adds actual FK closure and replay evidence;
`scripts/card-history-closed-corpus-rehearsal.js` proves its 33-table data restore
against source-derived dependency DDL, including the gated F27 relation shapes.
`test/track-b-backup-closed-corpus.js` preserves v3/v4 compatibility and gates v5.
`scripts/track-b-history-v5-backup-prerequisites.sql` is its distinct manual
private-grant/scratch helper artifact. Full schema reconstruction remains blocked;
`docs/audits/2026-09-05-card-history-restore-correction.md` records proof limits.

| Path | What it is |
|---|---|
| `supabase/` | Standard Supabase CLI layout: `supabase/config.toml` + `supabase/functions/` (Edge Functions). B4 outbound lives in `supabase/functions/linear-outbound/`, `supabase/functions/deliverable-write/`, `supabase/functions/batch-write/`, and shared write/auth code under `supabase/functions/_shared/`; `supabase/functions/_shared/linear-create-id.mjs` owns F203's deterministic Linear create UUIDs. `supabase/functions/linear-inbound/` owns strict echo suppression and carries F27's sole new per-function frozen Deno lock/config; `.github/workflows/deploy-f27-linear-inbound.yml` is its dispatch-only exact-SHA P.3 deploy/captured-v39 restore lane and has no push trigger. `.github/workflows/deploy-f27-section4-closures.yml` is the separate dispatch-only, exact-SHA, strictly serial F27 §4 deploy/source-exact restore lane for only `linear-outbound`, `production-write`, `deliverable-write`, and `batch-write`; it does not replace the P.3 lane or the onboarding lane. `supabase/functions/workload-plan/index.ts` is Workload's deliberate-manual sidecar gateway. Candidate source separates the staff-authenticated global plan projection (Admin/SMM/Creative) from the unchanged Admin/SMM-only writer; the read widening is not live until this exact function source is manually deployed. Other scoped deploys are path-triggered by `.github/workflows/deploy-onboarding-edge-functions.yml`, `.github/workflows/deploy-thumbnail-edge-functions.yml`, `.github/workflows/deploy-pto-edge-functions.yml`, and `.github/workflows/deploy-description-image-upload.yml` (the staff-only writer behind pasting an image into a SyncLinear description, `supabase/functions/description-image-upload/`) — do not move. |
| `supabase/functions/workload-linear/` | Source-only deliberate-manual gateway for staff Workload deadline/weight metadata and Admin/SMM-only Linear due-date writes. `policy.mjs` owns bounded IDs, exact `2× Workload` / `3× Workload` label interpretation, real-date validation, and post-commit receipt shaping. No deployment has occurred. |
| `migrations/` | Manually-applied Supabase SQL, kept for provenance (no auto-runner). `2026-07-11-b4-linear-outbound.sql` is the additive durable-outbox/switch delta; `2026-07-23-f203-production-issue-create.sql` is F203's source-only additive root/sub-issue create, identity-linkage, and identity-conflict quarantine RPC delta (not live-applied); `2026-07-23-production-comment-thread-lifecycle.sql` is the source-only additive F39/F42/F43 protected-reader, lifecycle, receipt, and two-surface import delta (not live-applied; existing `comment` operation reused); `2026-07-20-f27-team-rollback.sql` is the installed corrective per-team rollback delta: generation-fenced final CAS, exact replay echo binder, reserved no-provider drill, and pre-COMMIT TEST enqueue self-probe (attempt 2 applied exactly once on 2026-08-02; the 2026-08-01 failed attempt and Section 7 recovery remain historical evidence); `2026-07-15-pto-tracker.sql` is the locked PTO schema plus default-off flag source; `2026-07-15-pto-cancellation-audit.sql` is the source-only additive cancellation-attribution hardening; `2026-07-19-workload-plan.sql` represents the live, service-role-only sidecar for internal Workload plan dates (effective grants read back; exact revoke-correction artifact provenance remains F147). PTO migrations contain no HR seed data. See `migrations/README.md` for the baseline-plus-deltas layout and source-only-vs-applied distinction. |
| `n8n-backups/` | Point-in-time n8n workflow snapshots — the rollback anchors required by `ROLLBACK.md` rule 2. Purely archival; read by no code. |

## Documentation (`docs/`)

| Path | What it is |
|---|---|
| `docs/CLIENT_LIFECYCLE_MAP.md` | **THE CANONICAL client lifecycle map** (traffic → booking → sales → onboarding → provisioning → samples → production). The former byte-identical mirror in the `synchrosocial` repo is retired (owner decision 2026-07-19, after proven silent drift — see docs/audits/2026-07-19-vault-audit.md): that repo now holds only a stub pointing here. Edit this copy only. |
| `docs/FIND_ANYTHING.md` | The one-hop retrieval router: "I want to know X → open exactly Y" for both repos, plus the register index (F-/D-/OQ-/KQ-/VA-numbers) and the owner's no-session path. Draft pending owner ratification (vault audit 2026-07-19, proposal P4). |
| `docs/features/` | **Status-bearing feature contracts** — one doc per shipped feature. Each must state whether it is current deployed truth, a blocked future design, or non-operative history; completed SQL/workflow rollout recipes must not remain executable-looking. `HIRING_PROCESS.md` records the isolated live Hiring Process boundary: private application capture, staff review, and strict interview-booking mirroring are operating, while candidate invitation delivery remains default-off. Repo-wide visible-control rules live in `UI_DESIGN_STANDARDS.md`; `PTO_VISUAL_DIRECTIONS.html` is the synthetic, non-operative comparison board for the three staff and three Kasper layout directions. |
| `docs/ops/` | Runbooks: new-client onboarding, Linear reconcile safety net, monitoring/rollback coverage map (`docs/ops/MONITORING.md`), the installed F27 per-team incident procedure (`docs/ops/FLIP_RUNBOOK.md` §R2), the executed F27 preparatory-deploy/snapshot/install/source-exact defective-release recovery contract (`docs/ops/F27_INSTALL_RUNBOOK.md`) plus its generated drift-checked operator checklist, the post-flip defect record and video-flip pre-flight (`docs/ops/FLIP_BUG_LEDGER.md`), the open-repair register (`docs/ops/OPEN_REPAIRS.md`), the owner-side capture/rename/upload procedure for the sealed F27 rollback bundle the Section 4 deploy lane refuses to run without (`docs/ops/F27_SECTION4_CAPTURE_PLAYBOOK.md`), the scope for moving the Workload board off its Linear-derived table (`docs/ops/WORKLOAD_NATIVE_SOURCE.md`), the storage decision behind pasting an image into a description and what shipped on it (`docs/ops/DESCRIPTION_IMAGE_UPLOAD.md` — render half #1204, paste half 2026-09-05), and pending cleanup checklists. |
| `docs/independence/` | The active independence program: `docs/independence/INDEPENDENCE_PLAN.md` (strategy), Track A (Edge Functions) and Track B (Linear replacement) specs, dependency audits, and the source-only owner-gated `F42_CARD_COMMENT_IMPORT_RUNBOOK.md`. |
| `docs/testing/` | How to test — start at `docs/testing/README.md` (the map: suites, gates, the two safety contracts, and the four testing skills). Then: `docs/testing/CALENDAR-TEST-CATALOG.md` (what to check), `docs/testing/HEADLESS-TESTING-GUIDE.md` (how to run live probes), `docs/testing/PRODUCTION_POLISH_AUTOMATION.md` (the prod-polish gate), and the interaction-path generator. |
| `docs/truth/` | **Living current-truth docs** — start at `docs/truth/BRIEFING.md` (session briefing) and `docs/truth/README.md` (the contract). Updated in place, freshness-stamped, drift-checked by `test/truth-sync.js`. Conclusions live here; evidence stays in `docs/audits/`. |
| `docs/audits/` | Dated audit evidence (2026-07-03 →). Historical record — do not edit old audits; append later handoffs. `2026-07-19-boot-refresh-history-audit.md` preserves the immutable early-frame/refresh/history evidence behind F149–F162, the separately labelled publication source-only read-truth recon behind F163–F169, remediation-phase controlled synthetic-browser Calendar evidence behind F170–F171, the source-only staff Analytics follow-up behind F172, and successive exact-head remediation-review evidence behind F173–F185. Its pre-split `c9a79ef` evidence remains historical. The later owner handoff parks F176/F179 containment intact in unmerged #908, non-blocking for #891 and out of scope for expansion here; F185 is a separately deferred hosted-runner portability finding. `2026-07-22-staff-boot-refresh-history-audit.md` is the docs-only Round 2 staff punch list behind F186–F199, with one controlled fictional password-handoff observation added to existing F155 and every new row explicitly source-only. `2026-07-23-production-tab-graphics-gap-audit.md` is the public-safe, findings-only Graphics full-day parity/bridge/bug audit behind F200–F205; it includes the 72/4,600 attribution classification and authorizes no fix or live change. Current conclusions belong in `docs/truth/`. |
| `docs/vision/` | **Owner vision statements** — recorded owner intent behind major directions (dated, distilled from the owner's own words, with the owner's pinned follow-ups). Start at `STEP_BACK_2026-07-18.md` (the enterprise-atlas mandate and the sessions-feed-the-vault doctrine). |
| `docs/archive/` | Completed/superseded docs: finished migration plans, spent prompts, incident notes, old QA reports (`docs/archive/qa/`). Nothing here is current truth. |
| `docs/syncview-design/` | The **locked design kit** for the visible **Linear** mirror (internally the `production` surface): `docs/syncview-design/SyncView.html` (behavior source of truth), `docs/syncview-design/linear-design-tokens.md` (visual build spec), `docs/syncview-design/WIRED-PARITY.md` (parity contract), wired test gates in `docs/syncview-design/tests/`, raw probe measurements in `docs/syncview-design/probe-data/`, prototype-era suites in `docs/syncview-design/tests/design-machine-originals/`. |

## Test & automation entry points

Inactive first-Samples-repair monitoring preparation:
`scripts/client-continuity-monitor.js` (read truth, relay/fallback and liveness),
`scripts/client-continuity-view.js` (request/DOM observer and private scoped census),
`scripts/client-continuity-transport.js` (context transport denial and bounded safety finalization),
`scripts/client-continuity-run.js` (inactive manual launcher and private receipts),
`docs/ops/client-continuity.config.example.json` (inactive secret-reference template),
`scripts/client-continuity-actions.js` (gated TEST persistence runner),
`test/client-continuity-monitor.js` (offline fixtures and actual reader checks),
`qa/client-continuity.js` and `qa/client-continuity-fixtures.js` (intercepted boot harness reuse),
`qa/client-continuity-transport.js` (independent loopback transport escape regressions),
`test/client-continuity-view.js` and `test/helpers/client-continuity-source.js` (census/launcher/source coverage), and
`docs/ops/CLIENT_CONTINUITY_PREPARATION.md` (activation/recovery requirements).

Separate disabled initial Samples read: `scripts/samples-read-subscriptions.js`
(exact real-SDK read-subscription attribution), `scripts/samples-initial-read.js`
(post-teardown initial-only contract), `scripts/samples-initial-read-run.js`
(private manual launcher, separate receipts/observer/recovery),
`qa/samples-initial-sdk/package.json` (isolated fixture-only SDK dependency),
`qa/samples-initial-read.js` and `test/samples-initial-read.js` (real-SDK intercepted
faults and contract isolation), `docs/ops/SAMPLES_INITIAL_READ.md` and
`docs/ops/samples-initial-read.example.json` (held activation and private references).

Inactive hosted continuation: `scripts/client-continuity-hosted.js` (view,
receipt observer, bounded drill and independent-host sentinel commands),
`scripts/client-continuity-observer.js` (receipt validation and durable alert intent),
`scripts/client-continuity-delivery.js` (existing relay terminal confirmation and optional direct bot readback),
`scripts/client-continuity-test-ui.js` and `qa/client-continuity-test-ui.js`
(actual anonymous controls against isolated persisted fixtures; live automation unavailable),
`test/client-continuity-operations.js` (synthetic operation/delivery/state boundaries),
`docs/ops/client-continuity-operations.example.json` (inactive private reference template),
`docs/ops/CLIENT_CONTINUITY_OPERATIONS.md` (finite activation/disable handoff), and
`.github/workflows/client-continuity-hosted-view.yml`,
`.github/workflows/client-continuity-hosted-observer.yml`,
`.github/workflows/client-continuity-hosted-delivery-drill.yml` (dormant hosted package).

| Command / trigger | What runs | Notes |
|---|---|---|
| `npm test` | `test/run-all.js` → every `test/*.js` (dependency-free; normally offline, with F63 using only an explicitly required disposable PostgreSQL 16 service) | Runs on every push (`calendar-unit-tests.yml`). It never targets a live backend. Run before every commit. |
| `npm run test:e2e` | `qa/run-probes.js` → probes in `qa/probes/nightly-manifest.txt` | **Live backend**, test client only. Nightly (`calendar-e2e-nightly.yml`). |
| `npm run test:master` | `qa/master.js` — all master-registered lanes (unit, boot, parity, probes, scenarios, temporal, visual); feature-scoped PTO runs separately | Samples nightly runs a lane subset (`samples-e2e-nightly.yml`). |
| `npm run test:boot` | `qa/boot/client-entry-sequence.js` — fully intercepted streamed-document guard for real visible first paint, reload, history, persisted BFCache revocation, F102 client entry, active Calendar primary/post-load/realtime ownership, staff Calendar BFCache recovery, and F117 legacy Samples exact-client binding/late-read denial | Runs in the dedicated `client-entry-visible-boot.yml` PR check and as the master `boot` lane; no live data and no CI rerun. |
| `node qa/calendar-organize/run.js` | Calendar Organize-menu journey — Calendar Organize-menu journey: an interaction pass over the real menu plus a seeded round-trip fuzz proving auto-organize-by-date restores the exact manual order and writes nothing | Fully mocked and CI-safe; every non-localhost request is aborted, which is also how the "no reorder webhook" assertion is made. There is deliberately no live lane. Invoked directly rather than via an `npm` script on purpose: `package.json` is content-pinned by the F27 reviewed closure (`REVIEWED_BLOB_SHA256`), so adding a script there fails `test/f27-reconciler-closure.js` until the pin is re-attested. |
| `npm run test:pto-lifecycle` | `qa/pto-lifecycle/run.js` — stateful synthetic PTO human journeys with action/result screenshots and policy time travel | Fully mocked and CI-safe (`pto-ui-tests.yml`); curated synthetic evidence lives in the dated PTO lifecycle audit. |
| `npm run test:pto-live-drill` | `qa/pto-lifecycle/live-drill.js` — one privately gated disposable unpaid TEST request, approval, exact deletion, zero request-row residue, and flag readback | Local release drill only; never CI, never a real staff profile, screenshots remain untracked. |
| `npm run test:prod-polish` | `docs/syncview-design/tests/prod-polish-gate.js` — 10 Production-surface suites: locked live-read/zero-mutation, fully mocked authority/write gateway, interaction, behavior, and visual lanes | PR fast gate plus post-merge/scheduled/manual long lanes (`production-polish-gate.yml`); no suite may mutate a live backend. F105 repaired the post-#813 fixture/layout/read-audit epoch; only exact eligible read recovery is accepted, pending/unmatched errors stay red, and the exact candidate must pass the full aggregate before merge. |
| `node scripts/query-shape-sweep.js` | Estate-wide query-shape validation: every statically-written (relation, column) pair in `scripts/`, `supabase/`, `qa/` and `index.html` checked against the live schema, plus text-typed ORDER columns listed for judgement | READ-ONLY; live schema via `SUPABASE_ACCESS_TOKEN` (one information_schema SELECT through the Management API) or offline via `--schema=<file>`. Born 2026-08-27 from the 42703 double failure and the lexicographic order_index defect; run before any flip or edge-function deploy (`PRE_FLIP_HEALTH_CHECK.md` item 12). Its archive-thread catch is fixed and pinned by `test/archive-comment-thread-order.js`, which executes the shuffle, the mixed-cursor page-skip, and composite-cursor continuity. |
| `qa/overnight_runner.sh` | Continuous unattended QA loop | Local only; see the `/overnight-test` skill. Historical F176 source evidence covers issuer propagation beyond the 39-probe registry. The complete containment work is isolated in parked, unmerged draft #908 by owner decision; it is non-blocking for #891 and must not be expanded or reopened in this integration. |
| Reconcile crons | `scripts/linear-sync-reconcile.js`, `scripts/sample-linear-reconcile.js`, `scripts/linear-deliverables-reconcile.js`, `scripts/b1-linear-backfill.js`, `scripts/f200-attribution-live-snapshot.js` | Scheduled GitHub Actions plus a local-only private F200 read-only snapshot; `scripts/` also holds tested one-shot ops tools. |
| SyncLinear panel truth | `index.html`, `test/prod-batch-parent-panels.js`, `test/cal-link-clear-honesty.js`, `docs/ops/OPEN_REPAIRS.md` | Seven places the Production surface stated something false, found in one live owner test (2026-08-30). A batch parent is a SYNTHETIC node minted from the `batches` row, and its Assets grid read three columns the f34/f53 migration deliberately revoked from the browser grant — so they arrived `undefined`, printed **Missing**, and contradicted the plan link the same parent rendered from its granted `description` column three inches above; the child was right because it reads the same batch row through a service-role edge function. Measured: 199 synthetic parents, 189 carrying a URL in the description. Widening `PROD_BATCH_SELECT` is NOT the repair — that read returns 42501 and takes the whole tab down — so the panel now says **Unavailable** with the explanation rendered as the visible VALUE (a state pill alone left the loud `Not provided` in place, and a tooltip on a non-focusable span reaches neither keyboard nor touch). The parent also drops `deliverable_file`, which is empty there by construction — synthetic parents only, since a real hierarchy parent is a real deliverable and six hold an artifact today. Both phantom refreshes are gone (Description Refresh, whose Linear-staleness premise the flip retired, and Refresh access on a parent the prober cannot authorize), while `_prodRefreshDescription` stays because the error-banner Retry buttons are its real callers. The `invalid_artifact_url` copy stopped refusing folders and now names Frame.io, the shape the team actually ships, matching `assetTypeAllowed` since the 2026-08-16 ruling. A video deliverable explains its read-only Assets panel instead of rendering four rows and no reason. And the remove-link cross — which STAYS, because clearing is the seal's escape hatch while re-pointing is what the seal forbids — stopped promising a clear that `_calAdoptDeliverableLinks` silently restores on the next load for any card carrying a native deliverable. |
| Video canonical artifact | `migrations/2026-08-30-artifact-video-projection.sql`, `supabase/functions/production-write/index.ts`, `supabase/functions/production-write/policy.mjs`, `index.html`, `.github/workflows/deploy-f27-section4-closures.yml`, `ROLLBACK.md`, `test/video-artifact-rollback-record.js`, `test/prod-asset-attach-gate.js`, `test/production-attachments.js` | An editor attaches the finished video in SyncLinear the way a designer attaches a thumbnail, and it lands on the calendar card as the Video URL. Attach was graphics-only at six layers and the deepest was the database: `production_artifact_write` only knew how to PROJECT a graphics artifact (`file_url` to `calendar_posts.thumbnail_url` keyed on `graphic_deliverable_id`), so widening the guards alone would fall through to `artifact_card_projection_scope_invalid` and roll every attach back. The video branch writes `asset_url` keyed on `video_deliverable_id` and deliberately does NOT set `thumb_rev`: `syncview_thumbnail_thumb_rev_before_write` fires on `update of thumbnail_url, ASSET_URL` and mints its own token, so verifying a token it has already discarded is the exact 2026-08-06 defect. Deploy is bottom-up (migration, gateway, browser) because each layer fails closed alone; **rollback is top-down and stops before the database**, which is additive and unreachable once the gateway is restored — reverting it alone is the one sequence that turns a clean `operation_forbidden` into a 500 `native_write_failed` under a control the panel still offers. The rollback suite pins that order in both records rather than merely checking the files exist, because the failure mode is a present file that confidently says the wrong thing. One-step containment is `prod_authority.video` back to `linear` (asserted inside the function, so no deploy and no stale-tab window), at the cost of stopping every native video write, not just attach. The Section 4 closure pin moved to `cddf9a01`; PR #1182 moved the source without it and turned `unit` red on main. |
| Editable batch assets | `migrations/2026-08-31-batch-asset-write.sql`, `supabase/functions/production-write/index.ts`, `supabase/functions/production-write/policy.mjs`, `index.html`, `test/batch-asset-write.js`, `test/prod-asset-attach-gate.js`, `test/dropbox-share-query-keys.js` | Raw footage and the Frame folder become editable by any staff member; the Filming plan does not, at three layers. Both live on `batches` and were written once at intake -- the gateway refused every batch-entity mutation except `comment` -- so a wrong folder link was permanent from every seat in the product (owner, 2026-08-30: "anyone should be able to change the link of the raw footage, or the frame folder... just not the filming plan"). `production_batch_asset_write` carries the slot whitelist in the DATABASE, so `filming_doc_url` is unreachable even for a caller that names it; `PROD_ASSET_SPECS` gives each slot a `write` operation and the filming plan has none, so no control renders for it rather than a disabled one. `batch_asset` is the one operation where a creative is NOT confined to their own team: a batch is one shoot with one set of files worked by both teams and carries a single `team` value, so the match would hand the shared folder to whichever team happened to be recorded -- the suite fails if that widening leaks to any other operation. Shape is enforced, reachability is only reported: a frame folder created a minute ago is not shared yet, and refusing it would rebuild the dead end. CAS is against the BATCH clock, and a write drops the cached reads of every sibling of the batch except the one mid-edit. The editor moved from one header button to a per-row control, because three writable slots and one permanently unwritable one cannot be expressed by a single button. |
| Pasted description images | `migrations/2026-09-05-description-images.sql`, `supabase/functions/description-image-upload/index.ts`, `supabase/functions/description-image-upload/policy.mjs`, `.github/workflows/deploy-description-image-upload.yml`, `supabase/config.toml`, `index.html`, `test/description-image-upload.js`, `test/prod-description-image-paste.js`, `qa/probes/p96_description_image_upload.js`, `docs/ops/DESCRIPTION_IMAGE_UPLOAD.md` | Ctrl+V a screenshot into a parent or sub-issue description in SyncLinear and it lands as a markdown image with an https URL, which #1204 already renders and the outbound mirror carries to Linear as ordinary markdown. Storage is a PUBLIC bucket with UUID paths (the option the doc recommended: a private bucket's signed URLs would reach Linear as dead text). The browser never holds a storage key: bytes go to the function, which binds the object to one verified admin/SMM roster actor, checks the declared type AGAINST the magic bytes (SVG under a PNG label is the case), applies a 4 MiB / 8000px ceiling, requires each format's closing structure (IEND, GIF trailer, EOI, RIFF length) so a header-only body is refused, reserves a `description_images` ledger row BEFORE the object with an hourly per-actor AND per-role-key ceiling counted on that row (the actor header is caller-chosen; the role secret is not), and withdraws the row if the storage write fails. A fail-closed `description_image_upload_enabled` runtime flag is read before anyone is authenticated. The nightly probe exercises the deployed function's CORS, flag, key, actor binding and byte check against the live backend. Sizing: the browser downscales to 1600px on the long edge before upload (the only lever that reaches Linear, which renders at natural size) and PNG stays PNG; in the panel a 360px height cap with click-to-open keeps a tall capture from swallowing the layout. A placeholder line holds the caret during the upload, is swapped in place on success and removed on failure, and Save refuses a draft that still carries one. Plain text pastes are never intercepted. |
| Post-scoped shared assets | `supabase/functions/production-write/index.ts`, `index.html`, `test/post-asset-resolution.js` (which retired the 2026-09-01 parent-batch-asset-borrow suite, whose assertions pinned the defect) | Raw footage and the Frame folder belong to the POST, not to the `batches` row that stores them. Measured 2026-09-05 across all 6,332 browser-visible deliverables: of 1,138 posts, 44 span more than one batch row, so a link saved on the parent reached no sub-issue and one saved on a sub-issue reached no parent. The 2026-09-01 borrow this replaces walked only downward and was gated on the reader's own batch carrying none of the three, so the owner's own save switched it off. `assetSnapshot` resolves the post from either direction (a child's `linear_raw -> issue -> parent -> id`, a parent's own `linear_issue_uuid`), orders the post's batch rows native-`bat_`-first then id ascending -- the tie-break the projection already uses for competing parent claims -- and answers PER SLOT. Correctness needs only that every seat picks the SAME row, not the right one. The read also names a write target per slot: the row that slot's value already lives on, else the first bucket belonging to this post alone, else nothing (43 of 1,567 buckets hold more than one post, so a shared bucket is never offered for an empty slot). Cache invalidation follows the post rather than the batch id. |
| Asset grid holds across a refresh; verdicts reused by URL | `index.html`, `supabase/functions/production-write/index.ts`, `migrations/2026-09-05-asset-evidence-by-url.sql`, `test/prod-asset-refresh-holds.js`, `test/asset-evidence-reuse.js` | Owner, 2026-09-05, with the post-level fix live: the Open link pill "disappears and reappears two or three times" on a tab return, and "99% of the time the asset links are not gonna change" so why wait for the probe. Browser: a cached asset read is preserved on its scope STAMP alone (`complete` only says whether the latest read landed, and a tab return invalidates twice with a re-read in between), the delta tick and a batch write mark stale instead of deleting, pill entries survive with a batch/scope stamp that `_prodBatchFileFor` refuses at use time, and `_prodEnsureAssets` does not start a read that `requestStillCurrent()` would refuse on landing (stamped row, projection load in flight). Gateway: `heldAssetEvidence` reuses a verdict the ledger already holds for the same `(slot, url_sha256)` within `ASSET_EVIDENCE_MAX_AGE_MS`, across every deliverable, in place of the network step only; clock- and slot-dependent checks run first; a status-less `unavailable` is never reused; the reading row's ledger entry is still written with the original `checked_at`; the approval gate still probes live; `recheck: true` from Refresh access forces a probe. A real hierarchy parent draws no empty Deliverable file row. |
| Sub-issue file pills | `index.html`, `supabase/functions/production-write/index.ts`, `test/deliverable-file-two-way.js` | The pill beside each sub-issue is asked for once per distinct batch row of the POST, each under the scope of the row that names that batch and skipping attribution sentinels, because `batch_files_read` answers one batch id and the list is a Linear-parent set: on a split post the response carried the parent row alone and every pill was omitted. |
| Card surface from the binding | `migrations/2026-09-05-artifact-card-binding-first.sql`, `scripts/b1-linear-backfill.js`, `test/artifact-card-binding-first.js` | `production_artifact_write` routed the card projection on the `origin` filing column, so a row whose `origin` says `manual` while it carries a real, correctly two-way-bound card was refused EVERY attach and the whole write rolled back (7 live rows measured 2026-09-05). The surface is now resolved from the two-way binding when `origin` names none; rows whose origin already names a surface are untouched, and a card no surface binds back to is still refused. The producer is fixed with it: the backfill adopted `preferred.card_id` while `origin` fell back to the literal `manual`. |
| Deliverable file, both windows | `supabase/functions/production-write/index.ts`, `index.html`, `test/deliverable-file-two-way.js` | The Deliverable file on a sub-issue and the Video URL on the SMM calendar are one field with two windows, and the sub-issue list can open it. Editor-to-calendar is the database projection in `production_artifact_write`; the reverse is a READ, not a writer, because giving the calendar save path a deliverable write would have it lock deliverable-then-card while the artifact path locks card-then-deliverable -- a deadlock on the first afternoon both happen at once. So when a deliverable carries no canonical file, the panel shows the link on the card BOUND to it and names the surface in visible text; the first edit promotes it to canonical through the ordinary attach path, which projects the same value back onto the card it came from. Only the bound card may speak: `graphicsApprovalArtifactCandidate` tolerates a blank binding because it answers "does an artifact exist", where old data is not a contradiction; this answers "what file does this row have", where an unbound card is not evidence, so the binding is required in both directions. The sub-issue pill needs the URL client-side, which the browser view deliberately does not carry (exposing `file_url` there publishes every deliverable link under the public anon key), so `batch_files_read` answers the whole batch in ONE authenticated request -- no probing, because a pill opens a link rather than certifying one, and probing each child would cost four outbound checks per sub-issue. Per-team permission is applied per row and a refused row is simply absent. The glyph lives outside `PROD_ICON`, which `port-fidelity-check` holds byte-identical to the artifact. |
| Assets seed unreadable, not absent | `index.html`, `test/prod-batch-parent-panels.js`, `test/production-attachments.js` | The Assets panel printed "Not provided / Missing" on all four rows for every deliverable, on first paint and permanently on any refused read. `issue.assets` is hardcoded to four empty strings for every row the projection builds, because NO asset column is browser-readable -- the browser view carries 46 columns and none is asset-bearing, and `deliverables.file_url` and `batches.filming_doc_url` both answer 42501 to the browser key -- so an empty slot means the reader has not been told, never that the post lacks the asset. The 2026-08-30 batch-parent fix was right and too narrow: it gated the honest label on `syntheticBatchParent === true`, and the failure path only rescued rows in state `checking`, which a real deliverable can never reach because `checking` requires a URL the projection cannot supply. Permanent, not transient, for a creative opening the other team (both teams work here as of 2026-08-31), for the 686 live rows whose `client_slug` is not an active client, and for any 401 or network failure. Measured 2026-08-30: 5,888 live deliverables, 1,340 in a batch whose own description carries the filming-plan URL the row was calling Missing. Found by the unknowable-assertion sweep. |
| Controls that could never act | `index.html`, `test/project-move-not-offered.js`, `test/prod-batch-parent-panels.js`, `docs/ops/OPEN_REPAIRS.md` (item 87) | Output of the unknowable-assertion sweep, 2026-08-31: twelve agents over six falsehood classes, every candidate handed to an independent verifier told to REFUTE it. 36 raised, **18 survived, 18 refuted** -- and the refutations are load-bearing, one of them killing the claim that the pre-hydration `Preview - read-only` chip is a lie (authority is deliberately never cached, so the tab genuinely is read-only for that whole interval). Two fixed here. The **Project row** built a searchable list of every client, ticked the current one, and answered the pick with `Preview - read-only`, while being the only row in that side card with no write-gate attributes -- so it read as the one control that still worked. Nothing writes `client_slug` on any surface for any role, so it is refused at the picker DOOR (four callers: side card, context menu, bulk palette, Shift+P -- gating three leaves the fourth) with its own sentence rather than borrowing the ratified preview string, which describes a temporary condition. **Labels on a batch parent** answered 404 `entity_not_found` every time, because the parent id IS the batch id, and `_prodLabelErrorText` has no branch for it -- so it claimed a transient failure and offered a Retry that re-fired forever. It now short-circuits like Description and Assets already did, SETTLES (an unset state strands the button on `Loading labels`), and drops the Retry while a genuine read failure keeps its own. The remaining sixteen survivors are recorded, unfixed, with the traps their obvious fixes carry. |
| Workload says what it is not showing | `index.html`, `test/workload-excluded-reported.js`, `docs/ops/OPEN_REPAIRS.md` (item 87) | Two sweep findings that turned out to be one shape. `wlApplyData` removed a row from EVERY panel and said nothing in two branches: no assignee AND no work day or deadline (the "Needs assignment" strip is keyed on having somewhere to put it, so the row falls past it and every later bucket), and an assignee not on the roster of THAT ROW'S team -- whose comment called them "sub-issues stuck on former editors", one case it catches and not what the predicate asks, since a current designer on a video-team row was dropped identically. Measured 2026-08-30 by replaying the bucketing over `workload_issues`: 210 active sub-issues on the seed roster, 167 visible, **42 discarded (20%)**, with two clients losing every row so a filter to either produced a blank board with no message. **The repair is to REPORT, never to re-bucket**, and the suite pins that as a load-bearing negative: routing them into the strip turns five chips into forty-seven built with `applyEditorFilter=false`, so none carries a "Set work day" button; routing them into a visible bucket collapses unassigned rows into a phantom editor and distorts the capacity model auto-placement runs on; widening the team predicate opens a second capacity lane for someone who already has one. So nothing moves -- `wlExcludedSummaryText` is a pure function (the defect is a CLAIM, and a claim is what a unit test can hold) and the banner speaks last, after any real plan or refresh failure. With nothing visible it contradicts the empty-board reading FIRST, then gives the count. |
| Flip test rounds | `docs/ops/FLIP_TEST_ROUND2.md`, `docs/ops/FLIP_TEST_ROUND3.md`, `docs/ops/FLIP_DAY_TEST_LOG_2026-08-30.md`, `docs/ops/FLIP_BUG_LEDGER.md` | The tester briefs, one per round, each written for the four seats that actually use the surface (video editor, graphics designer, SMM, and Kasper/client) because the same screen lies differently to different people. Round 2 verified panel TRUTH; round 3 verifies the asset spec and is the first round where a tester can break something by writing to it. Each round carries a `what NOT to file` section, so correct-but-surprising behaviour (a folder saved and reported unreachable, a sub-issue with no file showing no pill, the honest pre-hydration read-only state) does not consume a reporting cycle. |
| Sync that reports what it did | `index.html`, `test/prod-silent-refresh-honesty.js`, `test/cal-link-clear-honesty.js` | Two more sweep findings, both a surface stating something it could not know. **A failed background refresh was recorded as a successful sync**: `_prodLoadData` catches in silent mode and RETURNS, and an async function that catches and returns resolves -- so `_prodDeltaRefresh` walked past the failure into its success tail, stamping `lastFullSyncAt`, clearing `lastSyncError` and resetting `refreshFailures` to zero. Three harms, worst last: nothing was read; the backoff never engaged; and clearing the error ERASED a notice the tab had already earned, with the `finally` arm repainting the freshness control as clean over data that had stopped refreshing. **The fix is a return value, not a throw** -- rethrowing is the obvious repair and breaks the tab-return refresh and the two post-create reloads, whose unhandled rejection fails the boot probes on their zero-console-error contract; those callers ignore the return value, so a boolean costs them nothing. **And the samples remove-link twin**: `_sxrAdoptDeliverableLinks` refills a cleared link on the next load exactly as the calendar adopter did, so the dialog promised a clear that does not stick. Behaviour deliberately unchanged -- the adopter exists because a native sample is materialized before the Linear mirror drains. The suite pins the copy-paste trap: taking `_calLinearClear` verbatim brings `calState`, which is empty on the samples surface and would silently give every sample the wrong promise with the fix apparently applied. |
| A queue that said nothing was waiting | `index.html`, `test/smm-review-stranded-media.js`, `docs/ops/OPEN_REPAIRS.md` (item 87.3) | `_calReviewItems` runs `if (!_calHasMedia(p)) return false;` BEFORE the awaiting-approval test and `_calApprovalBadgeCount` repeats the same skip, so the badge agreed with the wrong list and the queue printed "Nothing waiting on SMM approval right now" over a card that was waiting. Measured over 695 non-archived posts: 11 sit at For SMM Approval and **exactly 1** was hidden -- one client's ONLY awaiting card, scheduled for the next working day, so for that client the queue was 100% wrong and the badge said zero. Three other surfaces already flagged the same card; the queue the SMM actually works from did not. **The media gate is correct and stays** -- `_calReviewCardHtml` is built around media, so admitting the card renders Approve and Request changes over nothing to look at. What was wrong is the SILENCE, so a counted notice names the stranded posts in BOTH queue states (a non-empty queue quietly missing a card is the same lie as an empty one) and the empty sentence becomes "Nothing can be reviewed here right now." **The badge deliberately does not count them**: it means "items you can review" and a reader checks it against the list length, so counting unrenderable cards would make the two disagree and quietly redefine it as "items needing attention". The suite pins that decision so the symptom is not later "fixed" into the contradiction. |
| Refusals that name their own reason | `index.html`, `test/bulk-refusals-honest.js`, `test/project-move-not-offered.js`, `docs/syncview-design/tests/prod-structure-subset.js` | `Delete issues` in the bulk palette was built by the same helper as the live commands -- no disabled class, no title, no tip -- and wired to `_prodReadonlyGuard(); _prodClearLayer();`, so pressing it dismissed the menu and toasted "Preview - read-only": the two things a command that HAD run would also do. The identical Delete one menu away was greyed and explained. That asymmetry is what made it dangerous rather than untidy, because post-flip the palette really does write on its other four rows. `PROD_DELETE_UNSUPPORTED` and `PROD_MOVE_UNSUPPORTED` replace the preview sentence, which read as "not yet" -- a permission someone could be granted. Saying **"for anyone"** closes that reading; Delete names archiving because `archiveCalPost` genuinely writes through for every staff seat, worded as taking the POST out of the schedule rather than deleting the issue, because the sub-issues survive; Move names no remedy because none exists. `_prodReadonlyGuard(reason)` now takes an optional reason and still defaults to the preview sentence, which is correct for a control genuinely still checking authority. **A refused palette row KEEPS `data-prod-ctx`**, which looks wrong and is the repair: the search box and the arrow keys both index that attribute, and the first version of the disabled `Move to project...` row dropped it -- so typing a query hid every row around it, left it on screen unexplained, and silently broke two heavy-lane assertions that only run on jobs skipped by most PRs. Rows are refused in `activate()` and `hi()` instead, `sel` gains a real -1 state, and the `|| visible()[0]` Enter fallback is gone: with nothing highlighted, Enter must do nothing rather than fire whatever sorted first. |
| A red lane that named nothing | `docs/syncview-design/tests/prod-readonly-smoke.js`, `docs/syncview-design/tests/prod-polish-gate.js`, `test/prod-polish-names-the-check.js`, `docs/ops/OPEN_REPAIRS.md` (item 88) | `Production read-only smoke [timeout_unspecified]` is true and useless: the suite walks fourteen sections and any of them can time out, while its own output stays runner-private under F122. The suite now announces each section (`SMOKE_STAGE <name>`) and the gate qualifies the code with the LAST one announced, so a red run reads `timeout_unspecified@board_open` -- the code says WHAT, the stage says WHERE. Legal names are harvested from the suite's own source, exactly as `BEHAV_WIRED_CHECKS` is, so nothing a run prints can ride into a public summary by looking like a stage. The three classifiers fold into one `failureReason(text)` so the `reason:` line stays a single call: the first draft inlined an IIFE, which preserved the never-quote-output invariant and destroyed the ability to CHECK it in one regex -- and a guard you cannot read is most of the way to a guard nobody keeps. |
| Refusals that name no dead control | `index.html`, `test/locked-pill-names-no-dead-control.js`, `test/write-ui-failure-messages.js`, `docs/ops/OPEN_REPAIRS.md` (87.8 / 87.14 / 87.16, decision in 89) | Three sweep findings, one shape: a refusal naming a remedy the flip deleted. `native_link_required` said "Reload before trying again" over a refusal `makePayload` raises from the row's own state -- nothing on that path reads a cache and no client-side code backfills a deliverable id from a url, so a reload re-reads the same server row and refuses identically; the code's own comment had said "forever after" for months. The locked status pill said "Link a Linear sub-issue first" on two surfaces, naming a control that renders as the EMPTY STRING post-flip (`_calLinearSlotHtml` on an empty sealed slot, whose own note says the warning it replaced "was actively asking people to create the defect"). Measured live, cards not archived: 111 carry a Linear video link with no deliverable id (36 still in flight) and 149 the graphics equivalent; 66 have a fully unlinked video component and 36 a fully unlinked graphic. **Both now state the problem and name NO remedy**, which is the load-bearing choice -- there is no in-app path (creation closed, paste sealed, Import from Linear only makes more), so any remedy named would be invented, reproducing the exact defect. They do say the work item cannot be created from this screen, because that is a fact and it is the half that stops the reader hunting. The escalation to name is one owner sentence, tracked as item 89, and lands in exactly two constants. The locks are unchanged and the suites pin that as a load-bearing negative: `_calCompLinked` drives the N/A display rule and the client view, so unlocking the pill would put a live status menu on a component with nothing behind it. Both surfaces read ONE constant, because the string lived inline twice and the pill label is recomputed by two separate in-place updaters -- which is how these shipped as two findings rather than one. |
| Realtime reload floor | `index.html`, `test/calendar-realtime-burst-coalesces.js`, `docs/truth/APP.md` (Calendar), `docs/ops/OPEN_REPAIRS.md` (129) | The Calendar reloads the whole client on every realtime event behind a 350 ms debounce. The 4-second coalescing window beside it keys off `_calLastLocalWriteAt`, so it only ever applied to writes the TAB made; backend writers (the reconcilers of item 76, measured at 200 row writes an hour across 9 clients) got none, and every 350 ms window containing one row write became its own full reload — ten to fifteen refreshes in a row, pill and repaint, reported by the owner. `CAL_V2_RT_MIN_RELOAD_MS` (8 s) is a floor a foreign burst re-arms against, which is the identical move the self-echo branch always made. The suite drives the REAL handler with a virtual clock and counts reloads — 15 writes 700 ms apart go 15 → 2, a 20-row trickle 20 → 8 — each with a MUTANT run that removes the floor and asserts the storm returns, because a count is the only thing that separates this fix from its absence. The first event after a quiet period still reloads on the debounce, and the self-echo path is asserted unchanged. Browser half only: the cure is item 76. |
| A component with no work item | `index.html`, `test/caption-has-no-work-item.js`, `docs/ops/OPEN_REPAIRS.md` (127) | `caption` and `title` own no row in `deliverables` — their notes live in `caption_tweaks`/`title_tweaks` on the card and `_calLinearUrlFor` returns `''` for both. But all four writers collapse with `comp === 'graphic' ? 'graphic' : 'video'`, and `_writeUiNativeId` used the same ternary, so a caption note was aimed at the VIDEO deliverable. Silent while video was Linear-authoritative; the 2026-08-28 flip turned it into a 409 `native_link_required` on the 188 non-archived cards with no video component — killing the whole row save, not just the note — and on the 566 that have one it filed the caption note into the video deliverable's canonical thread tagged `component:'video'`. Clients were never affected (their comments route legacy here); it was staff **Request change** / **Approve after tweaks** only, which is why the neighbouring plain **Comment** button kept working and made the split diagnosable. `_writeUiComponentHasWorkItem` is the one rule, carried by all four collapse sites and by `_writeUiNativeId`; an UNKNOWN component keeps the historic `video` default, because silently making a real work item source-only is the worse direction. The suite runs the real extracted writers on both card shapes and re-runs them against a MUTANT predicate that always says "has a work item", asserting the refusal and the mis-filing both return — a source-regex check cannot tell those two behaviours apart. Nine writer harnesses load the predicate rather than stubbing it, so the rule under test is the shipped one. |
| Set all skips what it cannot set | `index.html`, `test/setall-skips-what-it-cannot-set.js`, `docs/ops/OPEN_REPAIRS.md` (87.15) | Filed as a false promise; it was data loss. `_sxrSetAllSettable` returned `true` unconditionally AND was dead -- neither the menu nor the apply loop called it -- so Set all iterated every component regardless of whether one had anything behind it, on a card whose own pill is greyed for exactly that reason 180 lines up. An unlinked component then reaches `_sxrPushStatusToLinear` with no url and no native id, so `_writeUiClassifyTargetless` raises `native_link_required` **in the video leg**, before the graphic leg and before the source upsert: with video unlinked, both pills flip on screen, a "Write not saved" dialog appears, and NOTHING persists -- the graphics status the user meant to set is destroyed with the impossible one; with graphic unlinked, the video leg has already committed natively, so the deliverable moves, the sample row does not, and the two systems disagree. Measured: 1,135 samples with a fully unlinked video component, 1,045 graphics, and **205 with video unlinked while graphic IS linked** -- the exact mixed card where Set all looks sensible. **The predicate is `_calCompLinked` on BOTH surfaces**, which is the load-bearing detail: porting the calendar's `_calSetAllSettable` verbatim was the named trap, because it tested the Linear url alone while the pill lock beside it tests url OR native deliverable id -- so a native-only card would have an unlocked, working pill and be silently skipped. Latent today (0 samples and 0 live cards carry an id with no url) and precisely why it was worth fixing first: post-flip, native-only is the shape new work arrives in. The menu header and the confirm both disclose the skip before the reader picks a status, matching the calendar. Found while in there: the calendar's own empty-case notify still said "link the Linear sub-issues first" -- a third site of the 87.8 dead-control instruction the sweep had not listed. |
| Batch parent renders without freezing | `index.html`, `test/batch-parent-labels-terminate.js`, `test/kasper-cal-cache-bounded.js`, `migrations/2026-08-31-batch-asset-team-fallback.sql`, `docs/ops/OPEN_REPAIRS.md` (90/91/92) | Three round-3 tester findings, all introduced by the asset spec. **The freeze**: `_prodRender` calls `_prodEnsureLabels` every render, and the synthetic batch-parent branch sat ABOVE the shared memo guard while calling `_prodRefreshLabelSurfaces` unconditionally -- which calls `_prodRender`, which re-enters. Infinite synchronous recursion; the tab died on any parent, taking the asset panel and the file pills with it. The memo check goes INSIDE the branch because below the shared guard sit the writes check and the staff-identity read, which a synthetic parent must reach neither of. **It was also the CI failure** item 88 spent a night narrowing through the smoke lane: the page was mid-blown-render when Playwright clicked, so `parent_link_gone_no_parent_field` was an accurate reading of a render loop. `production-polish` went green on the fix. **The unsaveable folders**: `batches.team` is null on 303 of 1,644 batches, and BOTH the gateway permission check and `production_assert_authority` refused on it -- so a gateway-only fix would have looked right and changed nothing. Both derive the team from the batch's deliverables now, and deliberately do not write it back: repairing the column belongs to intake, and guessing one in on a read path is how a wrong value becomes permanent. **The storage ceiling**: one Kasper calendar cache key per client, never evicted, growing with the roster until Chrome's ~10MB per-origin limit broke Create Post. Bounded by age AND count -- age alone bounds nothing when every entry is fresh, which is what a full sweep produces -- and the suite pins that nothing outside this cache is swept, the archive ledger next door being local state rather than a cache. |
| Create Post batch picker | `index.html`, `test/native-batch-picker-search.js`, `test/create-post-picker.js` | The "Add to a previous batch" dropdown used to `.slice(0, 5)` its COMPATIBLE list. That cap was invisible when written — the median client had five eligible batches — and by 2026-08-26 it hid batches for 15 of 37 clients, worst case 28, with nothing on screen saying so. An SMM reported the same batch missing twice; the second time it ranked sixth. The cap is gone (the ranking already surfaces the useful ones, and `incompatible` stays capped because it is advisory). A name filter appears above the select once the list passes `CAL_NATIVE_BATCH_FILTER_MIN`, matching every whitespace-separated term anywhere in the name — a `<select>`'s own type-ahead is prefix-only and every option here starts with the same client name. It REBUILDS the options rather than hiding `<option>` elements, which Safari ignores. |
| First paint vs third-party CDNs | `index.html` (head), `test/first-paint-not-cdn-hostage.js`, `qa/boot/client-entry-sequence.js` | The head carried a render-blocking Google Fonts stylesheet and a `defer`red Chart.js CDN script; a stylesheet blocks every script after it and a deferred script runs BEFORE `DOMContentLoaded`, so between them first paint was hostage to two third-party hosts. Measured 2026-08-26 with both unreachable: **12,906ms → 349ms**, and neither change helps alone. `async` is safe because `_chartReady` polls for up to 6s and every call site guards on the global being absent — the test pins that guard too, since removing it would quietly un-earn the change. The BFCache boot harness strips these tags by URL rather than by exact attribute spelling, and asserts each strip matched something: the old attribute-pinned patterns stopped matching the moment the tags changed and let an offline scenario reach the real network. |
| Card deep links open the card | `index.html`, `test/calendar-card-deep-link.js`, `docs/ops/OPEN_REPAIRS.md` (134) | Owner report 2026-09-03: "I click on the link and it just goes to the first thing." Two independent SILENT failures produced that one sentence, which is why it could not say which had happened. A card hidden by the Organize filters -- saved PER CLIENT and re-applied on open, so a link to an unscheduled or months-old card is filtered out before it can be found -- left the reader on the calendar's ordinary first card, advised to go check the filters themselves. It now clears them and restarts the search, once, AFTER the frame budget rather than up front so a saved view is never discarded on a link that was about to paint anyway, reusing the Organize menu's own Clear so ordering mode survives. And an unresolved client slug opened the first PINNED client saying nothing, which reads as a broken link rather than an unknown client; it now says so before falling back. |
| Linear linkage triage operators | `scripts/card-linkage-leak-check.js`, `scripts/calendar-native-link-gap-check.js`, `scripts/batch-parent-recovery-dry-run.js`, `scripts/batch-parent-row-triage.js`, `test/card-linkage-leak-check.js`, `test/calendar-native-link-gap-check.js`, `test/batch-parent-recovery-classify.js`, `test/batch-parent-row-triage.js`, `docs/ops/OPEN_REPAIRS.md` | Four read-only on-demand measurements of how cards, deliverables, and batches are bound to Linear — none has an apply path and none writes anywhere. The leak check re-measures the creation-path gap by week instead of quoting a number that stopped being true, and separates unlinked-and-archived (a discarded draft) from unlinked-and-live (actionable). The gap check finds the cards a `native_link_required` refusal can still reach, scored by whether the slot is blocked at all — a defect count with no reachability dimension is not a measurement. The batch-parent dry run reads each parentless active batch's children out of Linear and prints the parent it WOULD write; its classifier is pure and unit-pinned because the dangerous outcome is not "no answer" but a confident wrong one, so an unread probe blocks the verdict and a parentless issue is only called the batch parent on a measured shape-B signal. The parent-row triage answers the separate question of what to do about parent issues that were imported into `deliverables` as work: its ordering is the safety argument, because a card-bound row is never collateral and a row that is the only live thing in its batch cannot be removed at all — an empty batch reads as finished work that was simply never imported. Reports are public-safe (F64): client slugs and issue bodies stay out of the text report. |
| Client review-token provisioning | `supabase/functions/_shared/client-review-token-policy.mjs`, `supabase/functions/client-review-link/index.ts`, `migrations/2026-08-04-client-access-auto-provision.sql`, `scripts/provision-client-access.js`, `index.html`, `test/client-access-provisioning.js`, `docs/ops/NEW_CLIENT_ONBOARDING.md` | Closes the post-B0-seed onboarding gap that left a new client's share button dead on `review_token_missing`: `public.client_access` rows had only ever come from the one-time 2026-07-05/06 seed. Three layers provision the token — a roster-insert trigger plus one-time backfill (source-only migration), on-demand minting inside the deliberate-manual issuer for a verified staff principal against an active client, and an operator backfill/verify tool. None can rotate a stored token: every write is INSERT or `ON CONFLICT DO NOTHING`, and the issuer's single guarded UPDATE fires only on a blank token, because rotating one `401`s every link a client already holds (F35). The browser now translates each fail-closed issuer code into the next step instead of toasting the raw code. The test suite holds all four layers to the never-rotate rule offline, with an opt-in disposable-PostgreSQL 16 execution proof of the delta, its idempotence, and its rollback block. |
| Bounded deliverables reconciler inputs | `.github/workflows/linear-deliverables-reconcile.yml`, `migrations/2026-08-03-linear-reconciler-bounded-inputs.sql`, `migrations/2026-08-03-linear-reconciler-comment-index-optional.sql`, `scripts/linear-deliverables-reconcile.js`, `test/linear-deliverables-reconcile-bounded-reads.js`, `test/linear-deliverables-reconcile-bounded-postgres.js`, `docs/ops/LINEAR_RECONCILER_BOUNDED_READ_WINDOW.md` | Source-only no-trigger candidate: service-only compute-on-read views, primary-key keyset pages, exact lifetime comment aggregation, a hard-capped hash-bound hydration RPC, optional concurrent partial index, and exact disposable-PostgreSQL/proof gates. The shared n8n trigger stays at 15 minutes while only its live V2 branch has temporary hourly relief; the repository cron is unchanged. No source trigger, sidecar, cache, or backfill writer is introduced. |
| n8n quota watchdog | `.github/workflows/n8n-execution-quota-watchdog.yml`, `scripts/n8n-execution-quota-watchdog.js`, `test/n8n-execution-quota-watchdog.js` | Daily GitHub-hosted n8n Insights count, 80%/90% owner alerts, month-scoped dedupe, and low-threshold dry-run support. Runs outside n8n so scheduler failure there cannot disable the watcher. |
| B4 outbound | `.github/workflows/linear-outbound-drain.yml`, `.github/workflows/graphics-f2-preflight.yml`, `scripts/graphics-f2-preflight.js`, `scripts/b4-linear-outbound-harness.js`, `scripts/b4-outbound-shadow-audit.js`, `scripts/b4-pager-outbound.js`, and matching `test/*.js` | Durable drainer cadence, fail-closed TEST-only live proof, read-only full-roster shadow analysis, idempotent n8n pager wiring, and the manual-only Graphics F2 hard pre-flight. The pre-flight binds the exact passing pre receipt/binder/current-main release, verifies current Linear/Linear + F2-off state with the existing dedicated read-only PostgreSQL role, requires a fresh later successful schedule with exhaustive latest-attempt clear air, and refuses unless every real `pending|failed|shadow_ok` row across all teams/parity lanes is absent. Global mode defaults to `off`. |
| F42 card-comment import | `scripts/f42-card-comment-import.js`, `scripts/f42-card-comment-export.js`, `scripts/f42-card-comment-apply.js`, `scripts/f42-apply-rehearsal.js`, `.github/workflows/f42-card-comment-import.yml`, `docs/independence/F42_CARD_COMMENT_IMPORT_RUNBOOK.md`, `test/production-comment-slice.js`, `test/f42-card-comment-apply.js`, `test/f42-card-comment-export.js` | Source-only two-surface Calendar+SXR planner with exact manifest/hash coverage and conflict classification; a service-role exporter that reads the live source cards into the two-surface snapshot (F42 fields only, no briefs/linear_raw); a gated apply runner that re-derives the plan, refuses anything not complete/conflict-free, applies via the service-only `production_comment_card_import` RPC in parents-before-children order, and verifies applied==planned against an independent DB readback; a repeatable disposable-PostgreSQL rehearsal that applies every pending migration in order and runs plan+apply against fixtures; and the pinned-SHA `workflow_dispatch` plan/apply lane that runs the whole import from GitHub Actions (no local credentials) with a confirm token and an apply-digest drift guard. |
| Canonical comment mark-done CAS | `index.html`, `test/production-comment-mark-done-cas.js`, `docs/audits/2026-08-05-production-comment-mark-done-cas.md` | Browser-only candidate that moves a replacement, fully paginated canonical version read to immediately after the resolve-destination dialog, keeps exact lifecycle CAS, allows one body-free resolve-only rebase after a real conflict, and proves edit/delete/reopen never auto-retry. Manual Retry cannot invert resolve into reopen. The `stay` path still projects the resolved legacy shadow, while comment-only card flushes preserve overall status. |
| F63 paste-ready flag SQL gate | `test/f63-flip-runbook-sql-gate.js`, `.github/workflows/calendar-unit-tests.yml`, `docs/ops/FLIP_RUNBOOK.md` | Auto-discovered parser and disposable-PostgreSQL 16 execution proof for the runbook's three actionable fence classes: strict one-prior forward actions, explicitly enumerated-prior kill/recovery actions, and read-only utilities. Mutations must fail on wrong or missing prior state, assert one affected row, read back the exact write/audit, and preserve an unrelated sentinel. The fixture creates only the minimal runtime-flag store and audit support; it creates no F27 object and touches no live backend. |
| F27 team rollback + install toolkit | `.github/workflows/f27-team-rollback-proof.yml`, `.github/workflows/deploy-f27-linear-inbound.yml`, `.github/workflows/deploy-f27-section4-closures.yml`, the F27 job in `calendar-unit-tests.yml`, `scripts/f27-team-rollback-proof.sql`, `scripts/f27-migration-preinstall-drift.sql`, `scripts/f27-migration-preinstall-abort-proof.sql`, `scripts/f27-mirror-outbox-snapshot.js`, `scripts/f27-retained-state-diagnose.js`, `scripts/f27-apply-migration.js`, `scripts/f27-private-snapshot-store.js`, `scripts/f27-private-snapshot-fetch.js`, `scripts/f27-edge-source-rollback*.js`, `scripts/f27-database-rollback-*.js`, `scripts/f27-inbound-freshness.js`, `scripts/f27-reconciler-closure.js`, `scripts/f27-final-verification.js`, `scripts/f27-drill-runner.js`, `scripts/f27-install-checklist.js`, and matching `test/f27-*.js` | Disposable PostgreSQL proof of generation-fenced late-insert rejection plus a full reserved no-provider drill, and install/recovery operators for a deterministic private queue/definition snapshot, source-exact 21-predicate retained-state diagnosis, snapshot-bound migration apply, private Shared Drive integrity store/fetch round-trips, source-exact capture/restore/hash-readback rehearsal, generated/executed private database rollback, inbound freshness, an exact nine-file raw-Git reconciler closure with mechanical disabled/two-scan quiescence proof, one packaged read-only final verifier, live reserved drill, and runbook-derived checklist. A preinstall refusal retains a same-transaction private catalog/state inventory before emitting its public verdict; the diagnostic exposes only bounded catalog metadata and normalized function-source hashes and can never authorize an install. The final verifier seals its pre-DDL comparison baseline, then emits one public-safe PASS/FAIL for queue, rollback/replay, flags, full client/team hashes, provider closures, frozen writers, n8n, reconciler, and freshness; the hosted disposable proof also seeds a violation and requires it to remain red. The inbound workflow remains a dispatch-only single-slug lane. The separate §4 workflow consumes the sealed §1 four-function prior-source bundle and performs only the four fenced-closure deploys, or their source-exact restore, in literal serial order with provider readback after each mutation. Live modes require explicit owner-window confirmations. Attempt 2 ran those owner-gated modes on 2026-08-02 from exact release `968a895108beb2a2c41e86bb8b788115e35b14a0`; Section 4 run `30763278795`, `F27_DRILL_RUNNER_OK`, and `F27_FINAL_VERIFICATION_OK` all succeeded. |
| Track-B private recovery | `.github/workflows/track-b-backup.yml`, `scripts/track-b-backup.js`, `scripts/track-b-restore-rehearsal.js`, `test/track-b-backup.js`, `docs/ops/TRACK_B_BACKUP.md` | Six-hour production-read-only PostgreSQL snapshots to private Drive, direct non-n8n freshness alerting, and a production-ref-blocked scratch restore rehearsal. Sensitive packages are never Actions artifacts. |
| Assurance-ledger freshness | `scripts/assurance-ledger-freshness.js`, `test/assurance-ledger-freshness.js`, `test/assurance-ledger-staleness.js`, `.github/workflows/assurance-ledger-freshness.yml`, `docs/testing/ASSURANCE_LEDGER.md` | The ledger is a claim about EVIDENCE and carries its own staleness rule; until 2026-08-22 nothing enforced it and thirteen rows read `FRESH` more than a month past their window. The operator command prints the arithmetic for every row (read-only, never fails, public-safe). The test refuses a row claiming MORE freshness than its date supports, judged against the `State (YYYY-MM-DD)` stamp on the tier tables rather than the header's last-cycle stamp — so an overstatement is caught when it is written and an untouched file cannot spontaneously turn the suite red. Understating stays legal. Since 2026-08-23 the workflow runs `--gate` daily and registers the `assurance_ledger` dead-man lane, so the ledger going stale DMs the owner once per incident: it fires when a row stops supporting the state written beside it, and when nobody has restated or re-proven anything for 60 days — never on a row that is merely old and already admits it, which is why it is green with 15 of 19 rows past their window. |
| Thumbnail revision watcher | `.github/workflows/thumbnail-revision-scan.yml`, `scripts/thumbnail-revision-scan.js`, `test/thumbnail-revision-scheduler.js`, `test/thumbnail-revision-history.js` | Ten-minute, dedicated-signature caller with bounded aggregate-only scanning; the repository variable is the independent scheduler kill. |
| Slice 5: Production assignment, transitions, and live refresh | `supabase/functions/production-write/policy.mjs`, `supabase/functions/production-write/index.ts`, `index.html`, `migrations/2026-07-25-slice5-production-read-path.sql`, `qa/probes/prod_read_path_timing.js`, `scripts/slice5-test-drills.js`, `.github/workflows/slice5-test-drills.yml`, `test/slice5-test-drills.js`, `test/production-assignment-transition-policy.js`, `test/production-live-refresh.js`, `docs/ops/SLICE5_APPLY_WINDOW.md` | Source-only candidate for blockers #8 (F37 + F94 + F136) and #9 (F95). One server-authoritative eligible-assignee projection shared by the `assignee_options` picker read, the create form, and the commit; one role × current × next × team × assignee state machine mirrored byte-for-byte by the picker; an identity-bound personal queue; and a bounded foreground `updated_at` delta refresh with last-success age, degraded state, backoff, and manual Retry. The read path underneath it changes from a four-page OFFSET burst to a sequential primary-key keyset walk, and the source-only migration rebuilds the browser view so each row detoasts `linear_raw` once. The manual-only, pinned-main `slice5-test-drills` lane automates all six owed drills against drill-marked active-TEST fixtures, including the private read-only delta rebaseline; it keeps credentials step-scoped, publishes aggregate evidence only, and is offline-contract guarded. Adding it does not dispatch the drill. `docs/ops/SLICE5_APPLY_WINDOW.md` remains the owner-gated plan. |
| Comment-family twin parity | `test/comment-family-twin-parity.js`, `test/helpers/extract-function.js` | The calendar and Samples comment surfaces are twins by construction — six operations, one canonical-vs-legacy decision, one `_prodCanonicalCommentGate`. This suite asserts they answer the same questions the same way, that no member routes blind, and that none computes its gate behind the reader's role (OPEN_REPAIRS 105.3's defect). The roster is derived from the code rather than hand-kept: every function that consults the gate must be a family member or explicitly excluded, so a seventh operation added to one surface fails until somebody classifies it. It also pins the invariant that makes the calendar's client read correct — four writers project canonical state back onto the card column, ADD writes it itself, and no `_calCanonicalCommentsFor` exists. Matches over code only, via `stripNonCode`, because a comment quoting the gate is documentation, not behaviour. |
| Edge Function type ratchet | `.github/workflows/edge-function-type-ratchet.yml`, `scripts/deno-typecheck-ratchet.js`, `docs/ops/DENO_TYPECHECK_BASELINE.json`, `test/deno-typecheck-ratchet.js` | `deno check` on EVERY Edge Function, with the roster derived from the filesystem rather than hand-kept (a hand-kept list is what goes stale — the first one named six and missed `calendar-upsert` and `sample-review-upsert`, both live client-facing writers). 34 targets, compared against a recorded per-error-code baseline with its own measurement date: NEW type errors fail, the existing ones do not. **26 of the 34 measured CLEAN**, so on those it is a real gate — the first error to appear fails. `pto` is the one exclusion: it already has a passing gate in `pto-ui-tests.yml`. A ratchet rather than a gate because OPEN_REPAIRS 94 says the existing errors must not be repaired unattended — any edit to that file changes the deployed bundle and creates a capture-and-deploy obligation. Keyed by error code, not line number, so an unrelated edit above them cannot go red. Invoked by path and deliberately given NO `npm run` alias: the leave-evidence packet fingerprints `package.json` in its entirety. The unit suite drives the parser and comparator offline, since the lane that produces the output needs deno and the parser is the half that can be silently wrong. |
| Review needs something to review | `index.html`, `test/cal-review-needs-content.js`, `docs/features/SAMPLES_PARITY_LOG.md` | A component cannot be moved to For SMM / Kasper / Client Approval while the thing being reviewed is empty (owner, 2026-09-05). One content definition — `_kasperCompReviewable`, already the rule Kasper's own queue applies — consulted by every writer on BOTH surfaces: the calendar's `_calStatusPick` / `_calSetAllStatus` / `_calApplyAutoStatus` / `_calReviewApplyApprove` and the samples twins `_sxrStatusPick` / `_sxrSetAllStatus` / `_sxrApplyAutoStatus` / `_sxrReviewApplyApprove`. Tweaks Needed and the terminal states are deliberately NOT gated — the first is a rejection, and gating the second would break bulk edits on legacy cards completed before the calendar carried media. The suite derives both writer rosters from the source rather than listing them, so a new writer fails until it is classified, and pins the two orderings that make the gate real rather than decorative: the pending-edits bucket is created after the gate (an empty bucket is a full-card resend, not a no-op) and the content is checked before the ticked change-requests are resolved. |
| Crosswalk bind-and-import (Phase 2) | `migrations/2026-09-05-crosswalk-bind-and-import.sql`, `scripts/crosswalk-bind-rehearsal.js`, `test/crosswalk-bind-and-import.js`, `docs/ops/CROSSWALK_REPAIR_STRATEGY.md`, `docs/ops/OPEN_REPAIRS.md` | `production_comment_card_bind_and_import` binds a calendar card to its deliverable AND copies that card's legacy comment thread in ONE transaction — the operation OPEN_REPAIRS 147 §4 says the crosswalk repair cannot be finished without, because `production_comment_card_import` validates the crosswalk before it copies and so refuses exactly while the crosswalk is broken, while repairing the binding first splits the conversation in silence. Service-role only. **WRITTEN, NOT APPLIED** — the strategy doc's status block is the authority on which phases actually ran. It refuses on seven grounds, and two of them exist because the card's own pointer is not independent authority: a stale pointer naming an unbound row of the same client would otherwise rewrite that row, so the deliverable's `kind` must match the kind the card slot implies (`team` is too coarse — team=video covers kind=video and kind=other) and both sides must name the same Linear issue, with either side missing treated as unproven. The card is selected FOR UPDATE, and the receipt separates processed / imported / already_linked because the nested import returns the existing row on a retry. `scripts/crosswalk-bind-rehearsal.js` executes all of it against a disposable PostgreSQL 16 (reusing the F42 harness's Cluster/FOUNDATION_SQL) with the F27 outbox closure (enqueue binder + hold guard) cut verbatim from the F27 migration, so an eviction's cancel intent is proven against the same fence live runs; skips where the server binaries are absent. |
| Crosswalk Phase 2 repair lane | `scripts/crosswalk-phase2-runner.js`, `.github/workflows/crosswalk-phase2-repair.yml`, `test/crosswalk-phase2-runner.js` | The button-press that makes the Phase 2 calls: exports live cards/deliverables with the service key, forecasts every mismatching calendar slot with the bind-and-import RPC's own questions in its order (identity, client, bound-elsewhere, occupancy — `evict_occupant='card_wins'` only where an occupant holds the slot, `occupant_same_issue` never evicted), carries each card's legacy thread in `p_comments` planned by the F42 import planner against the post-bind crosswalk (a thread that cannot be planned cleanly holds its slot back), applies behind a confirm token and a plan-digest drift guard, then re-exports and reports what is left. Result document runner-local; `renderSummaryMarkdown` is the counts-only projection for the public log. Manual-only lane pinned to a commit on main, service key from the `production` Environment. OPEN_REPAIRS 156; strategy §5. |
| A deep link painted from its own read | `index.html`, `test/prod-deep-link-fast-paint.js`, `test/production-preview-source.js`, `docs/ops/OPEN_REPAIRS.md` (151) | Owner report 2026-09-05: the calendar card's "Open the SyncView Production video sub-issue in a new tab" link takes far too long. It opens `?prod=1&d=<id>` in a fresh tab, and `_prodLoadData` awaited the whole live projection (thousands of rows, paged in sequence) plus every batch before the one-row catch-up read (`_prodFetchDeepLinkRow`) was allowed to start, so the row the reader asked for was the last thing to arrive. `_prodDeepLinkFastPaint` starts that read beside phase one, then reads the row's batch by id and its parent by `linear_issue_uuid` in one parallel round trip, awaits the clients and members reads phase one already started (same promises, nothing twice), merges into the snapshot and marks the pane loaded so the detail paints while the board is still downloading. `_prodCarryDeepLinkRows` keeps the painted row across phase one's wholesale replacement of the deliverable set, so a finished row never drops back to a skeleton and the catch-up read no longer fires for a row already there. Three refusals are the load-bearing part and the suite pins each: the paint never consumes the deep link (108, five rounds; the authoritative pass after phase one still owns that), never writes the cache (a one-row snapshot painted over the next boot is the 2026-08-24 stale first paint again), and never publishes absence (an empty read means NOT YET; the tail decides). A read landing after phase one is discarded by the same generation check the catch-up read uses; a failed read leaves the old path untouched. The one-row filter lives in `_prodDeepLinkRowQuery`, shared by both readers, with its quote characters spelled `\x22\x27` so the source tests' brace extractor can slice the function. |

## Meta

| Path | What it is |
|---|---|
| `.github/` | CI workflows (unit, client-entry visible boot, nightlies, prod-polish gate, reconcile crons, edge-function deploy), PR template, Copilot instructions. |
| `.claude/` | Claude Code config: hooks (README-drift + repo-map reminders), the six quality skills (`master-test`, `overnight-test`, `human-audit`, `feedback-expansion`, `bug-archaeology`, `site-assurance`) — each a general protocol + target binding; when to use which: `docs/testing/README.md`; the shared prioritization contract: `docs/QUALITY_TIERS.md` — plus the meta skills `skill-forge` (the house method for creating/improving skills: amplification intake + house invariants; see `docs/vision/STEP_BACK_2026-07-18.md`) and `night-shift` (unattended work under shared usage limits: checkpoint-every-unit, pause-not-failure, sleep-across-the-reset). |
| `test/` | Fast dependency-free unit/wiring suites — auto-discovered by `test/run-all.js` (every `test/*.js` runs; fixtures live in `test/fixtures/`). Suites are offline except the explicitly required disposable-PostgreSQL 16 F63 gate; none reaches a live backend. Workload plan-date coverage is split between `workload-plan-source.js`, the hermetic production-helper harness `workload-plan-failclosed.js`, and the extended tweak/literal-bucket guard. `workload-capacity-placement.js` is the dedicated hermetic guard for capacity-aware automatic placement: pins are absolute, automatic work walks backward to a day with room, never forward past its ideal day and never before today (enforced at compute time AND re-applied at read time, so a tab open across midnight cannot paint a stored move on a past day), a saturated window keeps its honest over-capacity badge, and no capacity move is ever written to the sidecar. The matching positive source guard lives in `workload-plan-source.js`. |
| `test/helpers/` | Shared test infrastructure, required by suites rather than auto-run (`run-all.js` discovers `test/*.js` only, so nothing here is mistaken for a suite). `extract-function.js` is the ONE correct index.html function extractor: brace-balanced with strings, template literals (tracked as a frame STACK, not a flag), comments and REGEX LITERALS all inert — the hazard in `OPEN_REPAIRS` item 96, where a hand-rolled scanner counts the braces inside `/(\d{1,2})/` or opens a string on the quote in `/[\"]/` and silently over-extracts into the NEXT function, so an assertion passes against a neighbour. Most suites still carry their own scanner and may, for as long as it is right: `test/extract-function-integrity.js` compiles each suite's own extractor, runs it over every function that suite names, and fails naming the file and the function when the result is not that function. |
| `test/helpers/strip-comments.js` | The block-comment stripper the gates share, and the reason they no longer roll their own. `src.replace(/\/\*[\s\S]*?\*\//g, ' ')` — which seventeen suites used — opens a comment at ANY `/` followed by `*`, including the pair inside a string or an HTML attribute, and runs to the next closing delimiter anywhere in the file: two `accept="…,video/*"` attributes swallowed 64k characters of `index.html`, so both upload flows (24 functions, `_tkWireFormEvents` through `_ttpRenderQueue`) were invisible to every gate that stripped that way, and every NEGATIVE assertion over them passed vacuously. It is a stateless RULE, not a parser — a real opener is never preceded by an identifier character, `*`, `/`, `.` or a quote — because a JS scanner run over 5MB of HTML+CSS+JS drifts on an apostrophe in prose and on regex literals containing quotes. `test/comment-strip-is-honest.js` keeps the raw regex out and pins the behaviour. OPEN_REPAIRS 145. |
| `test/prod-backend-bridge.js` | Guards the heavy-lane transport below: that it is not a TLS bypass, that it is a transport and not a fixture, that its hosts are an allowlist rather than a hole out of the sandbox, that it refuses every non-GET/HEAD method BEFORE the request is made (a mutation that reaches the fetch has already changed production, so the lane's end-of-run `noWriteRequests` report is not a guard), and that a run which carried nothing cannot report a tidy pass count. |
| `test/workload-linear-*.js` | Hermetic policy and source guards for the isolated Workload Linear gateway, including the read/write role split, exact workload labels, bounded alias batches, real due dates, honest completeness, true mirror counts, and post-Linear-commit mirror partials. |
| `docs/syncview-design/tests/prod-backend-bridge.js` | Lets a headless Chromium reach the live backend from a sandbox without weakening TLS. Playwright's bundled Chromium does not trust the agent proxy's CA and does not read the system NSS store, so every backend read died `ERR_CONNECTION_RESET` and the heavy lanes could not run off-CI at all — the origin of "the sandbox has no route to the live backend" (OPEN_REPAIRS 125), which was only half true: Node's `fetch` always had one. So Node opens the connection, verifying the certificate exactly as every other tool here does, and `page.route` hands the response back; `--ignore-certificate-errors` is deliberately unused. It is a TRANSPORT, NOT A FIXTURE — live bytes, nothing recorded or replayed — and it refuses mutations at the door. Opt-in via `PROD_BACKEND_BRIDGE=1`, so CI runs byte-identically without it. OPEN_REPAIRS 144. |
| `qa/` | Headless browser QA: the fully synthetic streamed-document `boot` lane, strict current-token TEST-client entry builder/resolver, live test-client probes/scenarios, plus the fully mocked PTO lifecycle simulator and its separately gated disposable live lane. See `qa/README.md`. |
| `scripts/` | Ops jobs run by CI crons + one-shot migration tools (each guarded by a `test/*.js` counterpart). | `repo-identity-exposure-check.js` is the one that reads the repository rather than the estate: this repo is PUBLIC and 45 of the 50 identifying terms on the live roster appear in the tree, so it baselines that and gates GROWTH — it reports counts and file paths and never prints what it matched, because a leak detector whose output names the clients is one more public copy of the leak.

## Where does a new file go?

- **Feature spec / design doc** → `docs/features/` (link it from the feature's code comments).
- **Runbook / operational procedure** → `docs/ops/`.
- **SQL migration** → `migrations/YYYY-MM-DD-<slug>.sql` (additive-only; log it in `EXECUTION_LOG.md`).
- **Unit / wiring test** → `test/` (it runs automatically; no registration needed; any non-hermetic dependency must be an explicit disposable CI fixture, never a live backend).
- **Live probe** → `qa/probes/` + add it to `qa/probes/nightly-manifest.txt` if it should gate the nightly.
- **Stateful feature journey** → a feature-scoped folder under `qa/`; make its live-vs-mocked contract explicit and keep any live lane separately gated.
- **Production-tab (design-kit) test** → `docs/syncview-design/tests/` + wire it into `prod-polish-gate.js` or `package.json`.
- **Finished / superseded doc** → `docs/archive/` (don't delete history).
- **New top-level anything** → think twice, then document it here (CI will remind you).

`docs/audits/2026-09-05-native-intake-reciprocal-review.md` records the independent PR1314 review, both-surface reciprocal completion regression, local correction proof and the separate held creation protocol.
