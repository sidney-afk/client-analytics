# ROLLBACK.md — the "back to a working website in one step" runbook

**2026-09-05 local Create Post editor projection:** restore only the new
editor-read wrapper/caller changes first, returning to the preserved provider
loader. Keep all accepted intake jobs, payloads, receipts and native epochs.
That browser rollback hides unmapped editors again but retains ordinary
automatic assignment and cannot move already accepted work. Only then, if a
future approved release needs it, restore the separately captured reviewed
gateway closure through its manual lane; do not revert unrelated gateway
features or re-gate frozen client writers. An older/failed options endpoint
already degrades to visible picker unavailability and server-side automatic
assignment, never an assumed provider or valid empty native response.
See `docs/audits/2026-09-05-native-intake-editor-projection.md`.

**Unmerged draft, 2026-09-05 (native assignee eligibility):** on PR1302, assignment
eligibility follows the server-resolved native epoch; a native lane never reads
`production_assignee_eligibility` or the provider and applies the exact
active/team/creative-role contract to automatic choices too; the provider
explicit path keeps its flag behaviour and the provider automatic path keeps its
original stored-mapping contract.
Inverse is the team's native epoch CAS alone: accepted native rows keep their
assignee and manifest/receipt provenance, new requests return to the provider
policy. No Live State entry changes. Evidence and gates:
`docs/audits/2026-09-05-native-assignee-eligibility.md`.

**Unapplied draft, 2026-09-05 (reconcile):** the native intake reconciler is
manual-only and unscheduled, so its behaviour rollback is to stop dispatching.
Rows it recovered and slots it bound are accepted work with real receipts and
must be retained like any browser-materialized intake; never delete its reason
rows, recovered children, `skipped` receipts or bound slots. It creates no card.
Its card-table triggers only RECORD facts (created, deleted, slots changed) on
every write once installed and alter nothing; if recording itself must stop,
`alter table ... disable trigger zz_production_card_provenance` on both card
tables restores the exact previous write path, and the table stays. Details:
`docs/audits/2026-09-05-native-intake-reconcile.md`. This changes no Live State entry.

**Unapplied draft, 2026-09-05:** native-only intake depends on PR1293. Its behavior
rollback disables new native admission while retaining the epoch-aware gateway,
manifest epochs and terminal receipts for accepted work. Do not drop/truncate
accepted evidence, strip markers or requeue native receipts. Full release order,
old-serving limitations and retained-data rollback:
`docs/audits/2026-09-05-native-only-intake.md`. This changes no Live State entry.

Draft source note (2026-09-05): mapped component feedback is not deployed. Its
rollback is the bounded browser diff plus the previously captured **reader-only**
`production-comments` closure, retaining all canonical/source data and receipts.
No writer restore, table drop, queue replay, flag or frozen-writer change belongs
to this slice. The existing multi-function onboarding dispatch is not a
reader-only rollback. See `docs/features/COMPONENT_FEEDBACK.md`.

For the first Samples continuity release, use the paired draft-preserving reader
inverse in `docs/ops/FIRST_SAMPLES_RELEASE_PACKET.md` and
`docs/ops/SAMPLES_RECOVERY_REHEARSAL.md`. Historical whole-document revert notes
are not its operational inverse: the old document cannot display new owned debt.
Preserve ownership, save receipts, field debt and cache throughout recovery.

**Pinned ab636 local integration, not released (2026-09-05):** merge
`dd702412f7a36a46f610ea05ecad8ac06469ba73` pairs forward HTML
`aa2be6645b440cd2a9487692112b5f0d23f4c236ad37e990087a80cb1365815f` with recovery
HTML `717b52d9a40b589429a43f90cd81859be08bd8620eab162093ea2f977ef0de02`.
Only this pair preserves that tree's inherited asset changes as well as the
Samples debt schema and save receipts. The `e665ba77` pair remains preserved for
its own document; do not mix either pair or use the historical whole-PR revert
directions below for this integrated release. Before release, abandoning the
isolated local branch changes no serving state. After any separately approved
release, use only the exact paired reader inverse under the packet's owner and
serving checks. No gateway, migration, writer, flag or data rollback belongs to
this reader-only inverse. The inherited optional asset lookup index is not
applied by this work.

**This file is law.** The owner's requirement, verbatim in spirit: *"I need to make sure that if
there's a bug or a problem or anything happening, I can click on a button and everything goes
back to normal. Our whole business depends on this."* Every phase of the independence plan is
executed under the rules below, and the **Live State table** in this file must be updated in the
same PR as any change it describes. If this file is out of date, the migration is out of
compliance — stop and fix it.

**Meaning of “one step.”** A kill flag must contain new harmful behavior in one step. It does not
prove that queued writes are safe to drain or that authority can be reversed without classification.
F27's per-team rollback is installed and reserved-drill-proved in production.
The unsafe #894 design and the real 2026-08-01 failed attempt/Section 7
recovery remain historical evidence. Attempt 2 on 2026-08-02
entered from that exact retained boundary, applied the migration exactly once,
deployed/read back the four protected closures, returned
`F27_DRILL_RUNNER_OK`, and returned `F27_FINAL_VERIFICATION_OK` with PASS across
all 17 enumerated assertions. The final authority reversal is one guarded
statement, but it remains deliberately blocked until F2/F4 are stopped, the
installed begin call seals the team snapshot, every intent is classified, every
approved replay has a correlated terminal receipt, and machine-read team zero
is proved. Use `docs/ops/FLIP_RUNBOOK.md`, never a default drainer, for incident
containment. `docs/ops/F27_INSTALL_RUNBOOK.md` retains the exact executed
attempt-2 procedure and the defective-release recovery contract.

For the coverage map across sync edges, nightly checks, alerts, and containment/recovery paths, see
[`docs/ops/MONITORING.md`](docs/ops/MONITORING.md).

A successful pager or relay execution proves only the observed happy path. It does not prove retry
behavior, lane isolation, terminal Slack delivery, B1 cursor safety, or monitoring independence
(F09/F66/F81/F131/F132). Repair-list and linkage alerts are not currently immediate: live logic
requires two distinct nonzero summaries and shares one throttle across all V2 nonzero classes.
Treat immediate behavior as a target until F132 closes. Do not remove the independent Samples GitHub
schedule while F132 is open; if reducing duplicate cadence, remove the pager's last-branch dispatch.

## 1. Standing rules (non-negotiable)

**Samples G1 candidate (2026-09-04, release held):** browser-only read correction,
based on `13e187a7d0043ed110b486feb50502758a026229`; no new kill flag or writer change.
Before merge, abandon/revert this PR. If later released with coordinator approval,
revert the complete PR's browser/harness/docs commits in reverse order and verify
the reverted Pages bytes plus client reads. The reviewed intermediate head
`c10ebc8465b5fc95976014e21184a6c2076d8d96` is not a recommended recovery target: it
can discard unscoped drafts. Let pending saves settle and preserve unfinished work
before reloading retained tabs; propagation is not instant. The full revert restores
the known false-empty defect, so keep incident evidence and the release hold.
No cache purge, data restore, EF deployment, authority reversal, authentication change
or frozen-writer rollback belongs to this procedure. Monitoring and client-continuity
gates in draft #1268 remain prerequisites for release.

1. **One-step behavior rollback, always.** Every cutover ships behind a server-readable runtime
   kill switch. Flip that switch first; a frontend revert is secondary because stale tabs and Pages
   propagation are not immediate. `git revert` restores repository source, not the captured deployed
   source. Under F51, source-exact EF rollback means redeploying the exact captured provider source/
   entrypoint with its captured JWT posture, then independently matching the deployed-source/JWT
   hashes. Historical transitive graphs are unrecoverable and are not part of that standard; prior
   version IDs remain provenance, not activation handles.
   A Pages deploy/revert or update-banner check likewise does not expire stale callers (F127).
   Mandatory rollback releases require an embedded build/auth-authority epoch, server rejection
   before mutation, privacy-safe population proof, and draft/queue-safe reload plus fresh identity.
2. **The old path stays available until the new path has baked.** n8n workflows being replaced are
   normally left ACTIVE during canary, then only DEACTIVATED (never deleted) after their gate passes,
   then archived only at final cleanup. Activation is a rollback only when the exact approved graph,
   trigger, credential, error, and last-green contract has been read back. **Explicit exception:**
   `MJbMZ789B5ExZz9x` is inactive after a crash cluster and must not be blindly activated (F46). Before
   touching ANY n8n workflow, export its JSON — **raw workflow JSON contains hardcoded keys and
   must NEVER be committed to this public repo (rule 8 wins): export to the private weekly-backup
   Drive folder and commit only a public-safe status stub to `n8n-backups/` (dated), the
   Phase-0/A2 precedent** — in the same PR as the change.
3. **Additive-only database changes during the entire migration.** New tables and new columns
   are allowed; DROP/RENAME/type-changes are forbidden until the final cleanup phase after
   everything has baked. This guarantees the old code path always still works against the
   current schema — which is what makes rule 1 honest.
4. **Snapshot before every phase.** (a) git tag `pre-<phase>` (e.g. `pre-A1`) on `main`;
   (b) JSON export of every n8n workflow the phase touches; (c) Supabase dump (CSV or SQL) of
   every table the phase writes; (d) for every EF, an exact provider-source/entrypoint/JWT/release
   manifest plus independent downloaded server fingerprint. Source-exact EF recovery redeploys that
   capture and requires an independent deployed-source/JWT hash match; it does not reconstruct a
   historical transitive graph, and an old version ID is not an activation handle. Private artifacts stay in the
   private backup store; only public-safe names/hashes go in `EXECUTION_LOG.md`.

   > ⚠️ **The weekly n8n export does not satisfy 4(b) today (found 2026-07-27).** The scheduled
   > "SyncView - Weekly Backup" workflow backs up **zero active workflows** and still reports
   > success. Its *published* export node carries `activeWorkflows: false`, so the export is
   > exactly the complement of production: the 2026-07-26 file holds **54** items — 39 archived
   > plus 15 inactive — and none of the **77 active** workflows. An unpublished draft removes the
   > filter but was never published. Separately, the export/upload path continues after failures,
   > so a short or missing export can still report success; that is a second defect, not the cause
   > of this one. Until both are fixed and the run asserts its exported count against an
   > independently enumerated live total, take the per-workflow JSON export by hand before any n8n
   > edit and do not treat a green weekly run as rule-4(b) evidence. This is rule 7 applied to
   > backups: a completed backup job is not a verified backup.
5. **Log everything.** `EXECUTION_LOG.md` (create in the first execution PR) gets a dated entry
   for every deploy, flag flip, n8n change, DB migration, backup taken, incident, and rollback —
   with enough detail to reconstruct events later. This complements the in-app event ledgers
   (owner decision D7).
6. **Gates are hard stops.** At every gate in `docs/independence/INDEPENDENCE_PLAN.md` §6: post the evidence
   (test results, canary metrics, reconciler-correction count), get the owner's explicit OK,
   only then proceed.
7. **Verify the rollback, not just the deploy.** For each phase, before its canary starts,
   actually EXERCISE the recovery once on the QA client. For Track B this means FLIP_RUNBOOK R2's
   stop/snapshot/classify/resolve/team-zero sequence before any authority reversal—not a direct
   flip-back. Re-soak before flipping forward. An untested recovery is a hope, not a button.
8. **The repo is public.** No secrets in code, docs, commits, or logs — ever.

## 2. Live State table — what is serving production RIGHT NOW

Update in the same PR as any change. "Rollback" must be executable by the owner alone.

> **Root intake manifest draft (2026-09-05), not installed:**
> `docs/audits/2026-09-05-native-intake-manifest.md` records the additive
> parent+expected-request transaction and retained-data rollback contract.
> Restore the exact captured prior production-write closure through its approved
> deployment lane; retain the manifest table, wrapper and accepted/inflight
> receipts. New old-code requests after rollback lack manifests. No live behavior,
> flag, authority or frozen anonymous writer was changed by this draft.

> **Workload candidate clarification (2026-07-23):** the existing editable-plan row describes the
> currently serving plan-date boundary, where deadlines are display-only. The F40/F46/F99/F100
> source candidate has its own row below: Linear-authoritative due writes use `workload-linear`,
> SyncView-authoritative due writes use the guarded `production-write` `surface='workload'` route,
> and an ephemeral sibling receipt triggers an authoritative sensitive re-read. None of those
> candidate mutation or receipt paths is live.

| Surface | Current production path | Kill switch / rollback | Last verified |
|---|---|---|---|
| SyncLinear description images (paste an image into a description) | `description-image-upload` Edge Function writes the public `syncview-description-images` bucket under the service role after binding the object to one active admin/SMM roster actor and verifying the bytes; the browser inserts the returned https URL as markdown, which the outbound mirror carries to Linear. Deploys on merge via `.github/workflows/deploy-description-image-upload.yml`; bucket, `description_images` ledger and the flag row come from `migrations/2026-09-05-description-images.sql` (applied by the owner 2026-09-05). | **One statement, server-side, no deploy:** `update public.syncview_runtime_flags set value = '{"enabled": false}'::jsonb, updated_by = 'owner-kill' where key = 'description_image_upload_enabled';` — the function reads the flag before authenticating anyone and fails closed on a missing or malformed row, so cached tabs and direct callers are contained at once; the browser shows "Image upload is switched off right now" and leaves the description untouched. Re-enable by setting `enabled` back to `true`. Already-uploaded objects stay readable by URL (that is the ratified public-bucket property); deleting them is a Storage action, not a rollback. Code rollback: revert the merge; the function keeps answering only while the flag is on. | 2026-09-05 (merged with #1310; flag row inserted enabled by the migration) |
| Private card history (**DRAFT / UNAPPLIED**) | No installed capture is claimed. `docs/ops/CARD_CHANGE_HISTORY.md` prepares atomic private row images for six owners and a separately activated complete backup corpus. Frozen anonymous writers and all existing events/outboxes remain unchanged. | After a separately approved install, one transaction disables only the six `card_change_journal_after` triggers; retain journal/schema/guards/private packages and record the ensuing capture gap. Do not drop history, change auth, replay outboxes or roll back business rows blindly. Backup fallback to the legacy corpus is explicitly limited, not full-history recovery. The new v5 data-only correction retains prior packages and all33 covered relations; matching authenticated schema/empty-target restore remains blocked. Changing the schedule never authorizes provider/outbox replay. | 2026-09-05 source preparation from `287c16cd`; local synthetic PostgreSQL only, no live deployment/retention claim. |
| Hiring Process capture, review, and interview booking | The private Hiring Process sidecar, `hiring-applications`, and `hiring-automation` are live. Active n8n Application Capture (`oi4BPg79dykdet6H`) mirrors only the dedicated iClosed application event and alerts Kasper. The existing Sales - Call Booked receiver (`xoPqojySDriQ8Mzh`) has a strict first branch for only the dedicated interview event; it records the private hiring status and otherwise continues through its unchanged sales path. Candidate invitation delivery is deliberately **OFF**: `hiring_invites_enabled` is exactly false and the dedicated dispatcher is inactive. | **Immediate candidate-email kill:** keep/set and read back `hiring_invites_enabled={"enabled":false}`; this blocks queue/claim/authorization/send even if an operator later activates the dispatcher. **Capture containment:** deactivate Application Capture to stop new application mirror/alerts. **Booking-mirror containment:** restore/publish the captured pre-branch Sales - Call Booked version `d9d981ec-f133-429d-972a-729189612a99`; this removes only the hiring branch and retains the prior sales booking path. For only the alert-link repair, restore/publish Application Capture version `52274b74-e7d0-4b6f-83fb-6ff36c86e12e`. Read back the graph/active version after any restore; never delete the workflow or drop the private audit rows. | 2026-08-25: active versions read back as `759a33ed-7156-4a86-89ed-bac45497ba55` (capture) and `a82e2ce1-d062-4997-a812-7621b5c1b635` (booking). Controlled internal route proved one provider-receipted invite and `interview_booked` state; booking execution `432073` ran only the hiring branch. Flag returned to false and dispatcher remains inactive. |
| Client review-token provisioning (SOURCE-ONLY 2026-08-04) | **SUPERSEDED 2026-08-08: live is v4 == main**, deployed through the dispatch-only CI lane (`deploy-client-review-link.yml`, fingerprint readback PASS) — the mint-on-demand layers below are LIVE and the one missing-token client was verified working before and after. Historical description (accurate until 2026-08-08): live `client-review-link` was the operator-deployed 2026-07-15 v2, which returns `review_token_missing` for any client whose `public.client_access` row is absent — true for every client whose roster row postdates the one-time 2026-07-05/06 B0 seed (as of 2026-08-04, exactly one: `lukecutting`). The candidate provisions that missing token at three layers: the source-only `migrations/2026-08-04-client-access-auto-provision.sql` (`after insert` trigger on `public.clients` + one-time backfill), on-demand minting inside `client-review-link` for a verified staff principal against an active client, and `scripts/provision-client-access.js` for operator backfill. Merging changes only the Pages toast wording; the token layers require a deliberate-manual EF deploy and a hand-applied migration. **No layer can rotate a stored token** — writes are INSERT / `ON CONFLICT DO NOTHING`, and the issuer's one UPDATE is guarded on the token being blank (F35: rotating one `401`s every link a client already holds). | Browser toast wording: `git revert` the merge and let Pages redeploy. Migration: the one-command owner-only block at the foot of the SQL file drops the trigger, the trigger function, the column default, and the mint function. It deliberately deletes **no** minted token — deleting one revokes a live client link, which is the outage this change exists to prevent. Edge Function: redeploy the captured 2026-07-15 v2 source under F51 source-exact rules and match the deployed-source/JWT hashes. Reverting any layer restores the *old failure*, not an outage; the fallback in every case is `scripts/provision-client-access.js --apply`. | 2026-08-04 (unit suite 193/193 on branch; `client-access-provisioning` proven to go red on both an `on conflict do update` backfill and an issuer `upsert`; delta applied, re-applied, and rolled back against a disposable PostgreSQL 16 with the pre-existing token byte-identical throughout. Nothing was deployed, applied, or written to any live backend.) |
| Production mirror hierarchy (F145) | PR #885 is merged on `main` as `f00da653`: Production follows each deliverable's persisted Linear parent UUID globally instead of inventing hierarchy from batch/title similarity. Pages run `29713997171` deployed that merge, and read-only live-HTML inspection found `_prodResolveParentLinks` plus the raw-parent projection with no `batchParent`. This changed only the browser projection/adapter and guards; no schema, data, writer, runtime flag, authority, Edge Function, or n8n state changed. | Run `git revert -m 1 f00da65341797ec55f2f9a0d53b97e6bccd7056f`, push through the normal reviewed path, then require the resulting Pages deployment and live-HTML readback before calling rollback complete. No flag flip, database cleanup, n8n edit, writer rollback, or client drill is required because this is a read-only hierarchy projection. | 2026-07-20 (exact-head run `29711105120` attempt 2 passed all four F145 lanes; Pages run `29713997171` served the merge. Current-main run `29713997723` later failed fast + interaction while heavy/review-packet passed; the PR-head and merge trees are identical, so this is not evidence of an F145 code delta, but the current-main gate remains red for separate triage.) |
| n8n Cloud execution quota watchdog | A GitHub-hosted daily Action reads the n8n Insights API for the current Guatemala calendar month against `N8N_MONTHLY_EXECUTION_CAP=135000` and alerts the owner once at 80% (108,000) and once at 90% (121,500), including remaining headroom. Monthly alert state is external to n8n and an alert is marked delivered only after the owner-relay execution completes successfully. Live runs are restricted to `main`; feature branches may only dry-run. No n8n workflow, runtime flag, authority, or application path is changed. | Disable the `n8n execution quota watchdog` GitHub Actions workflow. This is a monitoring-only rollback; n8n and SyncView continue unchanged. | 2026-07-13 (`main` run `29268930214` saved the production July 80 marker; July 90 remains armed; feature-branch live proof `29269079957` failed closed before any count/alert/marker step) |
| Track-B independent private recovery (MERGED & ACTIVE 2026-07-15) | PR #840 is merged to `main` (merge `4f9d919`); the `Track-B private backup` Action now runs on a recurring **6-hourly** schedule (first scheduled run pending the next cron tick). Production role `backup_reader` is provisioned with BYPASSRLS plus SELECT on the exact 14-table corpus and zero table-write privileges. Manual run `29444939853` created a transactionally consistent package in the configured Google Workspace Shared Drive and independently re-read its folder parent, filename, byte length, Drive MD5, downloaded bytes, and HMAC before advancing last known good. A separate freshness pass listed and downloaded the same package with zero invalid candidates, and an independent Drive connector still listed the file after runner cleanup. A 229.03-second restore to dedicated scratch ref `pmzamicipnansdgomxfd` previously matched every table count and returned zero for every core orphan check. Failure alerting uses a non-zero GitHub Actions run plus the repository owner's built-in Actions email; Slack is optional and absent without blocking. PITR is unavailable by the owner's explicit paid-add-on opt-out and is recorded as residual risk, not as completed evidence. | To roll back the recurring backup: disable the `Track-B private backup` workflow (or revert PR #840 / merge `4f9d919`) to stop snapshot/freshness cadence and its failure emails. To revoke provisioning, set `backup_reader` and scratch role `track_b_restore` to `NOLOGIN`, remove the corresponding GitHub secrets, and remove the scratch-only trigger helper; this does not touch production data. Restore refuses the production ref and requires the exact scratch ref plus `SCRATCH_ONLY`. Never treat disabling the job as deletion of accepted private packages. | 2026-07-15 (MERGED & ACTIVE via PR #840 / merge `4f9d919`; proof run `29444939853`, source `f9406b8`, package SHA-256 `3bc3f19d50f4f6c3d64559e15dacb2b1863ffcfbe256538392a12790d7ed66db`, 15,562,462 bytes, MD5 `130c2ec109239be280453462d81698a1`, HMAC/readback/parent/freshness verified; recurring 6-hourly schedule active) |
| n8n execution-burn containment | Calendar status reconciliation is dispatched only by active pager `qllIDZPkdNAPRj0b` every 15 min. Redundant n8n trigger `AkiFmromoDkmsh39` and the native GitHub cron are disabled; TikTok pilot status cron `LR6R1mV4NaLNLlLG` is inactive while its queue is empty. Definitions were privately snapshotted before the cut. | Restore any cadence only after reading back the current graph, trigger, credential/error-handler fingerprint, and last-green execution; prove no duplicate cadence; obtain owner approval; then verify activation plus the first green run. TikTok additionally requires a nonempty queue and exact polling need. Reverting the cron-removal commit is a separate reviewed action, not part of the same paste. | 2026-07-13 (qll execution `263769` dispatched successful APPLY run `29292495346`; corrections 0; first post-cut bucket 21 executions/15 min) |
| Monitoring coverage | GitHub Actions nightlies cover Calendar E2E and Samples E2E on the TEST client; Samples nightly includes the read-only Production preview smoke as an independent job. n8n workflow `Tfhc3vebZyG6obOg` is an active Edge Alert Relay; sampled synthetic/EF paths prove only that happy-path last mile. Relay HTTP acceptance is not a general terminal Slack receipt (F09/F66/F81). n8n workflow `qllIDZPkdNAPRj0b` dispatches v2, Calendar/Samples and B1 work, but F131/F132 prove its ambiguous B1 event, stop-on-error coupling and nonterminal checks can advance/look green or suppress later lanes. Five of six sampled load-bearing workflows had no `errorWorkflow`, and the central handler failed 29 of 30 sampled limit-era invocations. Silence is unknown, not green. The coverage map is `docs/ops/MONITORING.md`. | Disable the affected GitHub Actions workflow if a nightly becomes noisy. Disable `Tfhc3vebZyG6obOg` to stop Edge Alert Relay DMs. Disable the pager node `Gate Incremental Refresh 30m` to stop only B1 refresh dispatches, or disable `qllIDZPkdNAPRj0b` to stop all pager DMs and dispatches. For `linear-inbound`, set/read `linear_inbound_enabled={"enabled":false}` or disable the two EF-bound webhooks. **Never remove the shared alert secret for lane containment:** onboarding fallback notifications also consume it (F66). Split/configure a lane-specific alert control before claiming independent alert rollback. Require success-only B1 checkpoints, isolated/correlated terminal receipts, generated workflow/settings census, TEST failure receipts, handler-health monitoring, and an observer outside n8n. | 2026-07-14 (F09/F66/F81/F131/F132 correction; prior happy-path samples retained) |
| Public Actions output boundary (F122) | The B1 incremental-refresh and Production-polish workflows were RE-ENABLED 2026-07-15 (aggregate-only, post-#836 merge). Exactly 414 named retained bundles were deleted (274 B1 plans, 55 Production screenshots, 54 review packets, 31 Argos bundles), and anonymous enumeration returns zero for those names. Candidate B1 publishes one success-only, one-day aggregate JSON artifact with exact-schema/no-array checks; candidate Production publishes no visual/review/Argos artifact or generated summary. Reconciler public logs and historical external Argos builds remain open. | Re-enabled deliberately 2026-07-15: the first B1 artifact validated anonymously against the exact aggregate schema (0 row/person/email/phone keys) and Production creates no artifact/Argos delivery. Do not restore deleted bundles. Keep detailed output runner-local; sanitize reconciler logs and have the owner audit/delete any external Argos history. | 2026-07-15 (#836 merged; producers re-enabled, aggregate-only B1 artifact validated; count-only artifact deletion/enumeration and no payload/body retained) |
| Onboarding and weekly-report reader containment (F76/F77) | The three onboarding list readers are live at v26 and `smm-weekly-reports` at v23 after pinned same-source run `29601466479`; all fingerprints passed. Missing/wrong onboarding keys, anonymous weekly GET, and anonymous `sync_managers` return `401` before service-role access. The signed manager-sync caller reaches its authenticated branch. Anon SELECT on both weekly-report tables is revoked. Protected browser callers merged with #836 (2026-07-15): staff screens are restored with auth (Admin/SMM allow, creative/client deny) instead of failing closed. | **Never roll back by restoring anonymous reads.** If a protected UI fails, stop that surface and repair/merge the authenticated caller. Preserve the deployed gates and revoked table grants. Any function rollback requires an F51-manifested artifact with equal-or-stronger authorization; retire shared compatibility keys only through a separately rehearsed caller migration. | 2026-07-17 (pinned same-source redeploy/fingerprint run `29601466479`; original protected-caller/live-deny proof retained from 2026-07-15) |
| F27 Section 4 four-function deploy provenance (F51 closed for these four) | **Live as of 2026-09-02 (§4 lane, deploy #25, run `33684111985` from `152c050e`): `production-write` v66 / `cc44bf93…`, `linear-outbound` v47 / `1489a4c2…`, `deliverable-write` v35 / `78df060b…`, `batch-write` v35 / `86f9f187…`.** `production-write` v65→v66 carries the comment parent resolution (OPEN_REPAIRS 100 — "no such row" no longer reported as `409 comment_parent_ambiguous`); `linear-outbound` v46→v47 stops retrying two writes Linear can never accept (OPEN_REPAIRS 114). The other two redeployed byte-identical, the lane deploying the four as one serial set. **The newest sealed §4 rollback bundle is `3010578b…` / 524885 bytes and it captures `production-write` at v65 — ONE release behind live v66, so it IS the true one-step restore.** Its `production-write` closure read `2af7fe6d…`, the code that was live before the release, which is the check that proves a bundle sealed the LIVE set rather than the release being deployed. **`08e9f50c…` / 513294 bytes is now superseded** — it captures v64, TWO releases back, so restoring it would undo deploy #25 as well as #24. Full attestation in `EXECUTION_LOG.md`, 2026-09-02.

**This row was found stale again on 2026-09-03**, by an audit rather than by anyone needing it, still describing deploy #24 as live a day after #25 shipped. That is the second recorded time — the row's own middle column and `docs/ops/F27_INSTALL_RUNBOOK.md` both already say "a deploy is not finished until ROLLBACK.md is updated", and it was once found eleven deploys stale. A written rule has now failed to hold this row twice, which is the argument for a check rather than a third reminder: that comparison is derivable, and as of 2026-09-03 it exists: `scripts/rollback-row-freshness-check.js`, run in the suite by `test/rollback-row-freshness.js`, compares this row against `EXECUTION_LOG.md`'s newest deploy receipt — run id, dispatched commit, every function's version and closure, and whether the bundle named below really is ONE release back rather than two. A row left describing the previous deploy now turns a PR red instead of waiting to be found (OPEN_REPAIRS 137). Updating this row by hand is still the fix; the check only stops it being forgotten silently. Until it exists, the danger is specific and worth stating plainly: **a stale row here does not fail loudly, it hands whoever is mid-incident a bundle that silently reverts one more release than they intended.**

*Superseded history below, retained deliberately.* Live as of 2026-09-01 (§4 lane, run `33555586230` from `da2195f0`) was `production-write` v65 / `2af7fe6d…`, `linear-outbound` v46 / `d83f0d7c…`, `deliverable-write` v34 / `78df060b…`, `batch-write` v34 / `86f9f187…` — the proper §4 lane this time, not the onboarding one. `production-write` v64→v65 carries three merged PRs (#1209 asset access opened to any staff role on either team, #1210 the filming plan resolving from the client when the batch has none, #1211 `raw_footage`/`delivery_folder` accepting a file as well as a folder plus the parent/child batch-asset borrow); the other three redeployed byte-identical. Full attestation in `EXECUTION_LOG.md` 2026-09-01, the entry after the description-write one. **`08e9f50c…` / 513294 bytes captured `production-write` at v64 — one release behind live v65 AT THE TIME, a true one-step restore then and no longer one now.** Captured minutes before this dispatch via the owner's local capture script (`AGENTS.md`), and its `production-write` closure `d1c2b666…` equalled the pin the workflow carried before this release, which is the check that proves a bundle sealed the LIVE set rather than something else. The previous entry here (`f2d74f9d…` / 505119 bytes, capturing v63 while live was v64) is now superseded and must NOT be used as the one-step restore; retain it only as a deliberate further-back option, alongside `d306717f…` / 490350 bytes (sealing v62, superseded since deploy #23's own re-pin), `cad350dc…`, `3106805f…` (deploy #20), `d0cf9ee1…` (deploy #19) and `bd79115c…` (deploy #18) before it. (`a54b6bad…` is NOT a bundle — it is the `production-write` v63 source-closure hash, cited elsewhere in this same row as the sanity-check value that release's capture matched; Codex P1 on #1215 caught it standing in this bundle list where it does not belong.) `production-write` and `linear-outbound` remain reachable from the onboarding lane too, whose runbook still carries no update-this-row step — the decay path this row keeps hitting is not closed, only avoided this time. Exact versions, source-closure and entrypoint hashes, provider bundle hashes and `verify_jwt=false` are recorded in `EXECUTION_LOG.md` under `syncview_f27_section4_deployed_versions_v1`; the forward receipt emits that block on every run, so "is what is running the same as the repository?" is answerable from the log plus `scripts/ef-fingerprint.js`. | **The sealed rollback bundle must postdate the most recent deploy, and the lane will NOT tell you if it does not.** Every `ROLLBACK_BUNDLE_SHA256` check verifies the bundle's own integrity, structure, project/CLI/adapter binding and function count; nothing anywhere compares the sealed capture against the currently live versions. Restore writes back exactly the source the bundle contains, so a bundle captured before an earlier deploy silently rolls back past BOTH — reintroducing that older function rather than stepping back one. Take a fresh capture before each dispatch and confirm its captured version equals the currently live one before using it; retain older bundles as deliberate further-back options, never as the only option. Never retry an ambiguous forward response — use the separately confirmed restore operation. | 2026-09-01 (deploy #24, `production-write` v64 → v65, updated within the same session as the dispatch, through the proper §4 lane — none of the onboarding-lane decay path applies to this update. Previously 2026-09-01 (deploy #23, updated in the same working session as the dispatch — and only because PR review caught that the deploy record went into `EXECUTION_LOG.md` and not into THIS row, which is the same failure mode this parenthetical describes. Previously 2026-08-24, deploy #21 — the first time since the rule was written that this row did not lag a deploy. Previously 2026-08-20, deploy #19. **This row was ELEVEN deploys stale — #9 through #19 — when a PR review caught it, having already been corrected once on 2026-08-08 for being two deploys stale. Its middle column states the exact law it keeps breaking, which is the point: a warning written in the same cell as the data does not keep the data fresh.** Nothing generates this row: the lane emits the deployed-versions block into `EXECUTION_LOG.md` automatically, and this row is hand-maintained, so it decays silently after every dispatch. Updating it is now step 4 of the post-deploy record in `docs/ops/F27_INSTALL_RUNBOOK.md`; treat a deploy as unfinished until it is done. **2026-08-27: decayed again within three days of that step being added — because the deploys went through the ONBOARDING lane, which the step does not cover. Corrected same evening by the bug-archaeology sweep; the onboarding-lane gap is the durable fix still owed.**)) |
| Graphics artifact projection (attach scope + thumbnail revision) | `production_artifact_write` refused every graphics attach until 2026-08-06: a card-less row hit `artifact_card_projection_scope_invalid` (intake stamps `origin='calendar'` at birth and the card arrives later — 1,887 of 2,160 graphics rows, 214 of them mid-approval), and a correctly linked row hit `artifact_card_projection_failed` because `syncview_thumbnail_thumb_rev_before_write` fires on `update of thumbnail_url` and overwrites the `thumb_rev` the projection then reads back. `thumbnail_revision_v2` is `{"mode":"on"}`, so the collision applied to every active client. Both were proven against a disposable PostgreSQL 16 in `test/artifact-projection-scope-and-revision.js`, which asserts the OLD definition still fails both ways so the regression cannot return silently. | **Do not "fix" a projection readback by relaxing it to a row count.** The contract is that exactly one row is left in the intended state; the correct repair was to verify against the value the row actually holds after the trigger, not to stop checking. Equally, do not widen the scope predicate past a null `card_id` — a row that NAMES a card whose surface cannot be projected must still refuse, or that card keeps a stale thumbnail while the deliverable claims a new one. Rollback is re-applying the `production_artifact_write` body from `migrations/2026-07-23-f34-f53-production-attachments.sql`; it restores the refusing behaviour exactly and nothing else needs undoing. | 2026-08-06 (both defects reproduced and fixed under a live PostgreSQL; trigger extracted verbatim from its shipped migration rather than imitated) |
| Filming-plan reader / F88 safe subset | `filming-plans` v25 is live after pinned same-source run `29601466479`; its fingerprint passed, and it retains the missing/wrong-key `401` gate. The protected Pages caller merged with #836 (2026-07-15), removing direct REST/realtime/Sheet fallback, so `filming_plans` anon SELECT is revoked (direct REST returns 401/42501). Raw anon reads already return `401` for `thumbnail_media_revisions`, `social_media_managers`, and `smm_weekly_reports`; `clients` and the seven named direct-use tables remain unchanged. | The protected Pages caller merged and the filming-table revoke was applied/read back 2026-07-15. Never restore anonymous grants on the three closed tables. Do not revoke `clients` or a direct-use table until its least-field scoped projection and browser caller are deployed together. | 2026-07-17 (pinned same-source redeploy/fingerprint run `29601466479`; original filming-table revoke/live-deny proof retained from 2026-07-15) |
| Public migration rows and repository history (F64) | Reviewed schema-only replacements exist only in the access-restricted local incident package and pass the count-only migration census. They are excluded from this public branch: anonymous GitHub comparison proved that `.gitattributes -diff` still expands the historical row deletions. Public current files, history, PR refs, caches, forks, and clones therefore remain exposed; no canonical rewrite has occurred. | Use the owner-scheduled freeze in `docs/ops/GIT_HISTORY_PII_PURGE_2026-07-14.md`. One operator removes all five historical path aliases, restores only hash-matched clean files inside the rewritten history, receives final owner GO, force-pushes once, restores protections, coordinates GitHub Support cleanup, and requires fresh clones/fork sanitation plus anonymous proof. Never publish the scrub through an ordinary PR. | 2026-07-14 (private clean-file census + failed public diff-guard proof; history purge not executed) |
| Public onboarding → provider provisioning (F128/F129) | The primary standard/AI submit paths are unauthenticated capture plus an unawaited shared job that can mutate real Drive/CRM/Slack/vault systems. That job can also place raw account-access answers in a workspace-public channel or fallback DM. No provider sandbox, per-step receipt, captured inverse or teardown workflow exists. | **Do not use a fictional submission as TEST or rollback proof.** Separate/authorize provisioning first; structurally remove secrets from every message/log; use provider sandboxes/intercepts and capture exact resource IDs/prior state. Teardown revokes test token/routing first, removes only correlation-tagged objects, restores provider prior state, proves zero jobs/outbox/references, and retains a value-free receipt. If any inverse is missing, the drill stays blocked. | 2026-07-14 (sanitized live graphs + current source/docs; no route/message/body/value/provider object read) |
| Staff UI theme | Light mode remains the default for all staff-facing SyncView surfaces. Staff may opt into dark mode with the header sun/moon toggle, which stores only `syncview_theme=dark` in browser `localStorage`. Production preview (`?prod=1`) follows the staff theme toggle; client-facing `?c=` links remain light-only and hide the toggle. | User rollback is one click: switch the toggle back to Light, or clear `syncview_theme` from browser `localStorage`. Code rollback is `git revert` of the dark-mode PR and a normal GitHub Pages redeploy; no runtime flags, Supabase Edge Functions, migrations, n8n workflows, or save/write paths are involved. | 2026-07-06 (dark-mode PR #691 merged; light default and light-only boundaries verified headless) |
| Workload editable internal plan date (**LIVE + CANDIDATE READ SPLIT**) | `workload_plan` is live with RLS, zero policies, no browser privileges, and service-role SELECT/INSERT/UPDATE only. The deployed `workload-plan` v2 currently permits Admin/SMM list/set/clear and denies Creative. Candidate source widens only the global list projection to Creative so every staff role receives the same saved plan snapshot; Admin/SMM remain the only set/clear roles, and Creative sees visibly read-only work-day controls with no drag handles. Automatic placement is floored against the shared `America/Guatemala` policy day rather than each viewer's local day. This read split is not live until the exact function source is manually deployed after merge. Linear due dates stay display-only, there is no runtime flag or n8n fallback, and the frozen client writers were not touched. PR #889 / merge `09e3dd6` adds client-only hierarchy, client grouping, overload, and group drag; PR #892 / merge `07d123d` adds client-only overload styling, the shared loading skeleton, toolbar placement, and hardened source guards. Both reuse the existing backend boundary. The current display follow-up remains client-only: exception sections default collapsed with a local UI preference, undated work moves below the calendar, popovers become compact, and every refresh uses the skeleton. It adds no plan cache. The historical release drill ended with zero sidecar rows, unchanged deadline/flag fingerprints, and Creative `403` for both list and set under the 2026-07-20 deployment. | **Owner-approved one-step user-facing behavior kill:** revert the candidate Pages merge first through the normal reviewed path and require the resulting Pages deployment/readback; this restores the prior browser read gate and removes Creative calls without changing saved plan data. If the widened function was deployed, redeploy the prior exact `workload-plan` v2 source as a separate captured/read-back operation to restore Creative list denial; keep Creative mutation denial throughout. To remove #892 as well, separately present its exact revert and inverse, then run `git revert -m 1 07d123d6df97054c53c4607d8a38aa3030bfe547` through the same reviewed path. To restore the earlier single-card-only layout too, separately present the exact #889 revert and its inverse, then run `git revert -m 1 09e3dd66535a78ba05d936f353f1bb635f266a35`. These browser reverts leave the deployed writer and saved dates intact. To retire editable dates entirely, separately present the exact #883 Pages revert/retirement and its inverse, then revert/retire the browser caller so Workload returns to exact due-date placement; leave the locked table and function dormant so saved data and recovery remain available. Any Edge Function retirement or schema change is a separate owner-approved operation with captured prior-state/readback rollback. Never open browser table access, add a column to `workload_issues`, write Linear due dates, touch `calendar-upsert` / `sample-review-upsert`, or change an unrelated runtime flag. | Candidate 2026-07-22 (not live until exact manual function deploy; 2026-07-20 migration/readback, exact-source v2 deploy, live 409/Creative/happy/clear drill, exact cleanup, and value-free evidence retained) |
| Workload due-date authority + sibling convergence (F40/F46/F99/F100; **SOURCE ONLY**) | Current live Pages remain display-only for Workload deadlines. Candidate source routes Admin/SMM writes by current team authority: Linear uses deliberate-manual `workload-linear`; SyncView uses `production-write` with `surface='workload'`, exact native deliverable identity, and CAS. A successful native row emits an ephemeral browser storage receipt; a sibling retains only an in-memory invalidation and re-reads guarded sensitive metadata before adopting due/CAS truth. The receipt is not durable commit truth and does not advance `workload_issues.synced_at`. This session deploys no function or Pages source, changes no authority/flag/schema/n8n/live data, and leaves the frozen client writers untouched. | **Before any live deploy:** revert the candidate source commit through review; there is no live rollback. **After a future deploy, contain the affected server lane in one step:** for native-route harm, redeploy the exact captured pre-candidate `production-write` provider source/JWT closure and independently read back its hash plus rejection of `surface='workload'`; for Linear-route harm, delete/retire the newly introduced `workload-linear` function and read back its absence/404. If the lane is unknown or mixed, stop affected staff and perform both containments before resuming. Then revert the candidate Pages merge, require the Pages/live-HTML readback, and require affected staff to close/reload stale tabs. The receipt key and pending invalidations are ephemeral browser hints—do not treat clearing them as data rollback, and do not auto-reverse already-confirmed due edits. Never flip team authority merely to hide a broken writer, fall back to n8n or the frozen writers, or delete native/outbox/audit rows. | 2026-07-23 source-only candidate; no Pages/function deploy, authority/flag change, migration, n8n edit, or live drill. |
| PTO / Time Off (`pto_v1`; **LIVE**) | The base migration, three locked service-role-only PTO tables, scoped `pto` deployment, private setup, and browser release path are live. `pto_v1` was changed and read back as `{"mode":"on"}` after the dark-state/auth/table-denial checks passed. Owner decision D-36 explicitly accepts launch under the current shared-role-key model despite its same-role impersonation/visibility risk; individually revocable sessions remain post-launch hardening, not a prerequisite. The current candidate adds branded controls, Guatemala policy-day handling, minimized absence projection, balance breakdown, server day-count quotes, future-approved cancellation/history, and an additive hardening migration with separate cancellation fields plus transactional request/setup and active-roster approval RPC hardening. Those refinements are source-only until separately applied/deployed; they do not change the current flag. | **One-step user-facing/approval behavior kill:** set and read back `syncview_runtime_flags.pto_v1={"mode":"off"}`. Off hides both entry points, bounces direct `#time-off` navigation home, and makes the Edge Function return `503 feature_disabled` for overview/quote/request/decide/cancel, including stale-tab calls. The admin-only direct setup actions (`set_start_date`, `adjust`) intentionally remain available while off; off deletes no data. Disabling `.github/workflows/deploy-pto-edge-functions.yml` stops future deploys but is not the runtime kill; a Pages/function revert is secondary and must preserve the locked table/auth boundary. Do not drop or revoke the candidate's additive cancellation columns or transactional functions during rollback: use flag-off first and, if needed, redeploy the prior Edge implementation while retaining the locked additive schema. | 2026-07-15 (value-free migration/deploy/seed/flag/browser evidence in `EXECUTION_LOG.md`; D-36 launch acceptance) |
| Calendar v2/v1 browser selection (F104/F125) | Calendar v2 is default. `?v2=0` sets sticky `syncview_calendar_v2_off`; a Supabase REST error also automatically falls back to the legacy Sheet. Write/reorder routing independently keeps the full active roster on Supabase-only Edge Functions. New cards/reorders can vanish on refresh, and stale whole-card Sheet state can overwrite unguarded canonical fields. This is split-brain, not rollback. | **Do not select `?v2=0` or continue writing after automatic Sheet fallback.** Preserve v2 and repair/revert the failing authenticated reader/caller. Sticky-off/fallback is read-only; clear sticky mode with `?v2=1` only after current Supabase health/readback. A future recovery mode must atomically couple read/write authority or be server-enforced read-only with an explicit warning. F104 still blocks deletion pending telemetry/consumer/recovery proof. | 2026-07-14 (deployed source + identifier-free live flag/workflow metadata; no browser state or write changed) |
| Calendar writes (upsert/reorder) | A1/A2 Edge Functions landed with B0.5 commit `c776ad6`; current Pages serves later main including #836. The import-verify/reorder fixes remain present. The runtime flag `syncview_runtime_flags.calendar_upsert_ef_clients` is set to the full active roster: the authoritative live flag value is intentionally not reproduced here. Candidate source removes the flagged reorder EF-to-n8n downgrade and tests it fail-closed; current Pages retains that old downgrade until merge. F67 remains for unlisted clients, flag-read failure and direct unauthenticated n8n callers. | **Flag removal/empty-list rollback is blocked until F67 closes.** During an incident, fail the affected authenticated operation visibly, stop its callers, and repair/revert the specific caller or EF while preserving equivalent authorization. Do not route to n8n merely because the flag or EF failed. A future legacy fallback is allowed only after it enforces the same immutable principal/client scope and passes positive/negative TEST probes. | 2026-07-14 (candidate removes EF-failure downgrade; residual/live F67 paths remain) |
| Samples New (SXR) writes | A2 `sample-review-upsert` and `sample-review-reorder` landed with B0.5 commit `c776ad6`; current Pages serves later main including #836. The full active roster is in `sample_review_ef_clients` (authoritative live value intentionally omitted). Candidate source removes the flagged reorder EF-to-n8n downgrade and tests it fail-closed; current Pages retains that old downgrade until merge. F67 remains for unlisted clients, flag-read failure and direct unauthenticated n8n callers. The saved `MJbMZ789B5ExZz9x` graph is inactive/unpublished and supplies no current Linear fast path. | **Flag removal/empty-list rollback is blocked until F67 closes.** Fail visibly and preserve the authenticated path; revert/repair callers or the EF without downgrading authorization. Do not publish `MJbMZ789B5ExZz9x`. Any retained n8n fallback must first enforce equivalent immutable principal/client scope and pass TEST failure-mode probes. | 2026-07-14 (candidate removes EF-failure downgrade; residual/live F67 paths remain) |
| Thumbnail refresh + Previous/Current comparison v2 (**LIVE**) | PRs #831/#832, the additive migration, four pinned Edge deployments, final-host Drive cache keys carrying persisted server `thumb_rev`, protected per-card comparison reads, private signed snapshot pairs, server minting for same-link/media and graphic-tweak resolution, and continuous active-card watches are serving. The live flag is `thumbnail_revision_v2={"mode":"on","clients":[]}` and the scheduled caller is enabled. The initial all-active scan checked 239 watches with 0 failures; after disposable TEST cleanup, 238 active watches remained with 0 error rows. Same-link and A-to-B rotations, open-tab repaint with zero hard refreshes, and desktop/mobile Previous/Current behavior passed on TEST plus an owner-selected real card. | **One-step behavior kill:** set/read back `syncview_runtime_flags.thumbnail_revision_v2={"mode":"off","clients":[]}` and confirm the `flag_flips` ledger. That makes the comparison reader/scanner fail closed and stops v2 token minting without deleting history. To stop scheduled traffic too, set `THUMBNAIL_REVISION_SCAN_ENABLED=false` (or disable the workflow) and verify no newer run starts. Existing private baseline objects may remain dormant; never restore anonymous revision-table access or make the bucket public. Frontend/EF reverts are secondary and remain F51/stale-caller gated. | 2026-07-14 (live migration/function/flag/scheduler/Pages and browser proof; scheduled run `29370658087` green; public-safe detail in `EXECUTION_LOG.md`) |
| Samples Old compatibility/read fallback (Phase-1 staff retirement shipped; F57/F117 open) | The staff nav is absent and every `#samples[/...]` staff hash redirects to Samples/SXR. The old tokened client URL is **not** a valid compatibility path: it loses the verified client when generic SXR mounts. `?sv2=0` belongs to the dormant old renderer and is not a coupled recovery: the active legacy upsert/reorder graphs fan out to Sheet + Supabase, continue after a Sheet error, and return from the Supabase branch, so success can be unreadable from the Sheet fallback. All three old route workflows have zero retained executions since Jul 1, which is unknown use rather than deletion proof. | Do not use the redirect, `?sv2=0`, or automatic old-Samples Sheet fallback as writable rollback. Fail the old client URL closed or restore a server-bound exact-client portal first. Any owner-approved staff-product rollback must either make Sheet+writer one atomic authority or disable writes; Phase 2 still requires the removal map, snapshots, zero-caller window, cross-client denial, and route/client checkpoints. | 2026-07-14 (live sanitized graphs + metadata; no route/payload/row/write) |
| Legacy Linear -> Calendar/Samples/Workload fast sync | **Not serving production:** n8n workflow `MJbMZ789B5ExZz9x` is inactive/unpublished (`activeVersionId=null`). Its five-node saved graph contains the Calendar, Samples, and Workload branches plus Track-A routing and authority gates, but a saved graph is not a live path. The latest readback found a 24-execution crash cluster and no later execution. The enabled Linear webhook configurations do not make an inactive destination real-time; the 10/15-minute reconcilers and Workload sweep are the current healing paths. | Keep the workflow inactive while the owner chooses one topology. If the fast path is retained, explain the crash/soft-error branches, publish intentionally, verify active-version/node fingerprint and last-green execution, and drill both teams on TEST. Otherwise ratify and monitor the reconciler-only SLA. Do not blindly restore/publish an older graph. | 2026-07-13 (live configuration and execution readback) |
| App → Linear pushes (set-status/comment/intake) | The active n8n webhooks plus FE localStorage outboxes are serving, but **F91 is a current P0 caller-auth defect**: status, comment, video intake, and graphics intake authenticate no incoming principal. Team authority limits direction only and currently permits both teams. | Do not call this a safe baseline or defer it to B5. Preserve normal users while requiring an active immutable principal, or an owner-ratified short-lived exact-client capability for deliberately shareable intake. Prove anonymous/expired/cross-client denial on TEST before relying on the routes. Native reroute and later retirement remain separate changes. | 2026-07-14 (live active versions/source + current Linear/Linear authority; no mutation probe) |
| F44 durable Linear intake containment | VIDEO PRODUCTION AUTOMATION workflow `BrJSe8zCKUccfmIq` serves active version `28dacc7f-4dd7-4d65-ba88-31db737c2c65`. It writes/claims the idempotent Supabase receipt before authority or Linear preflight. Exact confirmed parent/child creation returns `created`; any valid post-receipt **blocking** internal prerequisite failure returns a strict HTTP 202 `received` handoff, with the retained ledger `pending`/`failed`/`partial`, no Calendar creation claim, and unconditional human triage alert. A plan-only successful create keeps its internal marker; protected filming-plan lookup stays server-only. Browser replay is disabled; recovery requires an operator claim. | **Emergency graph rollback only:** in n8n version history restore/publish `af7671ab-deca-4470-a08b-ce591f59e08b`, then read back the active version/node graph and stop intake callers until manual recovery is ready. That rollback reintroduces missing-SMM/internal-prerequisite client refusal, so it is not a normal recovery. Never restore `66e41fca-a86f-4ef3-a977-8ba960bc152d` (it exposed protected plan URLs); `9e5abc46-91f0-49f8-b815-fcc6baa93891` is pruned. Retain `linear_intake_receipts` and unresolved rows during any graph rollback. The earlier F44 live edit had no private pre-edit JSON export; version history is its only historic rollback record. | 2026-07-27 (private snapshot before this generalization; active-version graph readback; no-staff QA receipt/202/alert proof) |
| Write-UI authority guard plane (active mutation gates + inactive saved inbound graph) | The active set-status, add-comment, video-form, and graphic-form mutation surfaces have fail-closed team-authority gates and return accepted 200 or blocked 409/503 after the decision, but those gates do **not** authenticate callers (F91). `MJbMZ789B5ExZz9x` remains inactive/unpublished with five saved authority-gated nodes; those saved gates are not evidence of a live inbound lane. Repository B1/reconciler code has fresh live authority checks immediately before every authoritative mutation; that code and the daily TEST drill / nightly shadow-audit schedules are merged to `main` (#811, 2026-07-13). Authority remains Linear/Linear, normal outbound off, and parity is armed under the 2026-07-28 owner decision. | For an active mutation workflow, restore/publish the affected private pre-edit JSON only as an intentional rollback; caller authentication must remain or become stricter. For `MJbMZ789B5ExZz9x`, do **not** publish either saved graph until the owner resolves the crash/topology choice and drills it. Disable `production-write-drill.yml` or `production-shadow-audit.yml` independently if a soak lane is noisy. For D-29 data bugs, stop affected users and disable/read back the involved F2/F4 lane(s), both if unknown/mixed; return authority to `linear` only after FLIP_RUNBOOK R2's audited intent resolution and team-zero proof (F27/F58). | Current parity posture updated 2026-07-29; other live readback facts retained from 2026-07-14. |
| Write-UI mirror controls (MERGED #812 2026-07-13; live on Pages, authority-locked) | The Linear-mirror tab now ships native status, comment, due-date, and assignee controls in the deployed app. Every control fails closed unless that row's team is SyncView-authoritative. The former bounded active-TEST exception was REMOVED 2026-08-06: the browser stamped a service-drill-only `test_override`, which `production-write` rejects from every browser credential, so the control returned 401 `invalid_test_override` on every human write and the UI rendered that as an expired staff sign-in. Direct diagnostics stay read-only; no request asks for legacy parity or calls n8n/Linear directly. **F94 blocks assignment readiness:** picker/server eligibility is only active+same-team, not compatible role plus usable provider mapping. Current Linear/Linear authority therefore keeps real-team controls read-only. | Revert this UI PR first. The dark gateway and n8n authority gates may remain safely deployed. For a data-integrity defect after a team flip, first stop outbound and follow F27's classified per-team recovery; do not blindly return authority to Linear with an unclassified outbox. Do not treat UI rollback as remediation for an already committed ineligible owner; classify and repair that row/outbox intent explicitly. | 2026-07-14 (F94 source correction; prior mocked control proof retained) |
| Status drift healing | **Calendar path:** `Linear ⇄ SyncView status reconcile` remains the global safety net. **Samples path:** `Samples ⇄ Linear status reconcile` remains restored. Pager `qllIDZPkdNAPRj0b` dispatches both every 15 min, but F132 means its quietness is not a terminal receipt and an earlier failure can suppress the last Samples branch. Calendar is dispatch-only; Samples also has its own `*/10` GitHub schedule, so F01's residual burn cut remains open. | Until F132 closes, retain the independent Samples schedule. If reducing burn, remove the qll Samples dispatch first—not both—and prove the post-cut 24-hour execution rate plus independent terminal observation. If a reconciler misbehaves, stop only its cadence after preserving replacement monitoring. Do **not** use a Track-A allowlist removal/empty list as recovery while it routes to unauthenticated n8n (F67); preserve auth and fail visibly while the affected caller/EF is repaired. | 2026-07-14 (F01/F67/F132 rollback correction; cadence state retained) |
| Client access/auth scaffold (Track B B0 base, current posture) | The additive B0 tables and verifier Edge Functions remain live. Current flags are `auth_enforcement={"mode":"permissive"}`, `prod_authority={"video":"syncview","graphics":"syncview"}` (graphics flipped 2026-08-16, video 2026-08-28 — this row had silently carried the both-linear value through the graphics flip; corrected at the video flip), and `linear_inbound_enabled={"enabled":true}`. Client tokens and role keys stay only in private secret stores/backups. Production is deployed; both teams are SyncView-authoritative, so native writes are live for real teams and Linear edits are detect-only. | Current permissive is a pre-enforcement posture, **not** a safe future rollback. After enforcement, preserve auth and revert/fix the broken caller/verifier first; a global return to permissive is blocked by F70 unless the owner opens a security incident with compensating server containment, forced cache/session invalidation, monitoring, and expiry. Additive DB objects may remain dormant; any EF rollback must satisfy F51. | 2026-07-14 (F70 future-rollback correction; current flag unchanged) |
| Six browser-writer authorization boundary (F35) | Candidate source gives all six service-role writers one fail-closed policy: exactly one configured staff/automation key or active exact-client token, server-derived actor/role/source, and no trust in caller attribution. `calendar-reorder` v32, `sample-review-reorder` v31, `templates-save` v30, and `caption-prompts-save` v30 are live: missing/wrong credentials return `401`, safe TEST allows return `200`, fake reorder IDs touch no row, restored settings match pre-state, and probe events are zero. `calendar-upsert` v38 and `sample-review-upsert` v39 were deployed gated 2026-07-15 from the #836 merge SHA — but were then **OWNER-UN-GATED the same day** (now `calendar-upsert` v43 / `sample-review-upsert` v44, open/tokenless) after re-gating broke clients **twice**: the #836 gate + F03's removal of `client_review_token` from the Clients Info sheet left the client-review write path sending no token → `401` for every client; a later re-gate after #838 re-broke every client still on a pre-existing link. The other four (`calendar-reorder`, `sample-review-reorder`, `templates-save`, `caption-prompts-save`) remain gated. | **⛔ FROZEN — DO NOT re-gate `calendar-upsert` or `sample-review-upsert` without EXPLICIT owner approval** (see AGENTS.md freeze + `EXECUTION_LOG.md` 2026-07-15). Re-lock ONLY after the owner affirmatively says so AND every active client is confirmed on a fresh re-issued link (via the `client-review-link` issuer from #838). Keep the other four writers gated; never restore their public implementations as rollback. If any authenticated path fails, stop/repair that caller rather than downgrading to anonymous n8n. Runtime artifact rollback remains F51-gated. | 2026-07-15 (two upserts OWNER-UN-GATED after a double client-link outage; four writers gated; re-gate **FROZEN** pending owner approval + fresh-link re-issue) |
| B4 staff sign-in + sensitive-surface role consolidation | The active-roster staff sign-in and #796 account/sensitive-surface consolidation are live on `main` (`5c04994`) with `auth_enforcement={"mode":"permissive"}`. The signed-in account popover + Sign out reuses the verified identity; credentials accept admin + SMM role keys, while unstripped onboarding + filming-plan writes accept admin only. Both historical surface secrets remain accepted in parallel. Sign out/invalid-key 401 clear identity and sensitive UI/cache state; recognized-but-disallowed 403 preserves the valid session. | Revert #796's frontend/callers first while leaving additive backend compatibility deployed. The current permissive posture may remain only before enforcement; after enforcement, a global fail-open is F70-blocked. Do not rotate/delete legacy secrets during a UI rollback. Roll back functions only after callers are healthy and through F51's pinned/manifested/artifact gate. | 2026-07-14 (F70 correction + merged/current path reverified) |
| B4 Linear outbound (historical pipe proof; current F27 closure) | The outbound pipe was proven live end-to-end on 2026-07-12, then returned to the recorded stopped stance. F27 Section 4 run `30763278795` later deployed and read back the current `linear-outbound` v35 from exact `main@968a895108beb2a2c41e86bb8b788115e35b14a0`; the 2026-07-17 v33 proof remains historical. Runtime authority/outbound values must be read fresh before any action. | If either outbound lane is active during an incident, immediate containment stops users. **Any F2/F4 runtime-flag write requires explicit owner approval:** first capture the exact current values, then bring the owner the proposed target value(s) and rollback that restores those captured values. Authority reversal additionally requires F27 team accounting/zero. Revert callers first, then use only an F51-compliant artifact rollback. | 2026-08-02 (`linear-outbound` v35 ACTIVE; exact-four provider readback PASS) |
| Write-UI Part 2 gateway + legacy-parity lane (deployed; #850 reroute callers LIVE, TEST-only cohort) | **Backend and callers deployed dark.** PR #850 / `9968bd9` shipped the Calendar/Samples/Submit cohort routing that superseded #813. The earlier v33/v24/v39/v25 inventory remains dated history. The 2026-08-02 F27 receipts establish the current protected closure: inbound v40, outbound v35, production v27, deliverable v26, and batch v26, all ACTIVE at window close. The historical 18-operation TEST drill recorded mutations, `0/0/0`, cleanup and unchanged flags, but did **not** document real generation or a generated-title/fallback result; the F27 reserved drill is separate and performed no real-team reversal. | Read current flags before proposing any action. **Changing `linear_legacy_parity_enabled` or the reroute allowlist requires explicit owner approval:** capture the exact prior value, present the exact target value, and define rollback as restoring and reading back that captured value. Revert callers first and never remove backend support while stale tabs can still call it. Runtime artifact rollback remains F51-gated. | 2026-08-02 (F27 final closure receipt; no frozen writer changed) |
| Write-UI reroute allowlist (`write_ui_reroute_clients`; Phase-2 dark launch) | The dark-merged Calendar/Samples write-UI reroute lane routes browser writes through `production-write` only for listed clients (cohort UPDATED 2026-08-07: the TEST fixture plus enrollment wave 1 — two real clients, owner-executed; always read the live value fresh). A missing/unreadable flag fails to the LEGACY writer lane. F143 was proved on v24; the installed current F27 closure is `production-write` v27. This flag and lane do not touch the two frozen client writers. | One-step kill: restore the CAPTURED prior value and read it back (FLIP_RUNBOOK §F6). During the wave-1 soak that captured value is the TEST-client-only list — NOT `{"clients":[]}`: an empty list also de-enrolls the TEST client, which breaks the ratified rollback expectation the health check and monitors verify. **Any value change is owner-gated.** Enrollment still requires every open first-cohort gate; F143 closure alone is not enrollment approval. | 2026-08-02 current closure; cohort became wave-1 on 2026-08-07 (owner-executed enrollment); rollback value is the captured TEST-only list |
| Active-client Linear project mapping | All 31 active real-client roster rows now have team-tagged `clients.linear_project_ids` entries for both `video` and `graphics`: 28 clients share one verified two-team Linear project and 3 use separately verified per-team projects. All 34 unique projects and all 62 project/team attachments were checked read-only in Linear before the conditional data update. This configuration does not enable Submit or change authority. Authority remains Linear/Linear and outbound remains off; parity was separately armed by the owner on 2026-07-28 and is not part of this mapping. | Run the private transactional rollback SQL captured before the update. It restores the exact prior `linear_project_ids` and `updated_at` values for all 31 rows, fails closed if a row no longer matches this run, and verifies before commit. It does not change parity. | Mapping verified 2026-07-13; independent parity posture updated 2026-07-29. |
| Templates / caption prompts | A4 tables and write Edge Functions landed with B0.5 commit `c776ad6`; current Pages serves later main including #836. `settings_ef_clients` carries the full active roster (authoritative live value intentionally omitted), so listed clients use Supabase overlays/Edge-Function writes. **Current defect (F67):** unlisted clients or a flag-read failure can select an unauthenticated n8n writer. Caption generation and filming-plan tabs remain on n8n and are out of A4 scope. | **Flag removal/empty-list rollback is blocked until F67 closes.** Fail the affected authenticated operation visibly and repair/revert the specific caller/EF. Retain n8n only after it enforces equivalent immutable principal/client scope and passes TEST failure-mode probes. | 2026-07-14 (F67 security correction; routing behavior unchanged) |
| Filming plans runway | Production still uses n8n `filming-plan-tabs` (baseline). QA/headless harness stubs this endpoint by default to stop cold-cache n8n load; set `SYNCVIEW_QA_LIVE_FILMING_TABS=1` for a deliberate live probe. | Revert the QA harness commit, or run QA with `SYNCVIEW_QA_LIVE_FILMING_TABS=1`. Production rollback remains n/a; baseline n8n path is still live. | 2026-07-03 |
| Production tab (Track B) | B1 additive tables/functions and mirror data are live. #812 serves Production with authority-gated status/comment/due/assignee controls: all teams are read-only under current Linear/Linear authority; the bounded active-TEST lane was removed 2026-08-06 because no browser request could ever satisfy the gateway's service-only `test_override`. The B1 incremental refresh remains pager-dispatched. F50's deliverable→card status projection and F53's canonical Graphics media operation are absent, so no team may flip merely because controls render. | Immediate containment after a flip is stop affected users and disable/read back the involved F2/F4 lane(s), both if unknown/mixed; authority returns only after R2's audited team intent resolution/zero (F27/F58). For a UI defect, revert the caller first and allow Pages/stale-tab expiry; keep backend support until callers are gone. | 2026-07-13 (#812 live; F50/F53 gates open) |
| Production detail polish (2026-07-09) | Frontend-only Production detail UI polish is layered on top of the read-only `?prod=1` preview: centered issue bodies, body-level `Sub-issue of` context, title-first parent sub-issue rows with project metadata, guarded add-sub-issue affordances, and compact activity rows. | Revert the detail-polish PR/commit to undo this pass. It changed only `index.html`, Production browser tests, and documentation; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-09 (verified `prod-readonly-smoke.js`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `behav-wired.js` 156/156 guard mode, `pixel-wired.js` light/dark, and focused parent/sub-issue screenshots) |
| Production detail resource polish (PR #765) | Frontend-only Production detail pages label delivered-file URLs as concise resource links while preserving the original URL in the anchor destination/title. Activity empty states, migrated Markdown descriptions, filtered project-detail empty states, and filtered project-board copy remain read-only UI polish layered on the `?prod=1` preview. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests, review automation/docs, and rollback/audit notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-09 (focused guard now covers concise delivered-file labels with preserved URL destinations; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-board/detail scope polish (PR #765) | Frontend-only Production polish removes a decorative no-op `All projects` button and renders it as a static active-scope label instead. This row's former secondary-line parent-trail description is historical and was superseded by owner-ratified #868: project-detail child rows now keep title and parent breadcrumb inline within `.prod-title`, and F105 guards that current contract. | Revert the relevant PR #765 commit(s) to undo the still-current static-scope portion; do not use this historical row to restore the superseded secondary-line layout. It changed only `index.html`, Production browser tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-18 (historical #765 scope proof retained; #868/F105 inline parent-trail contract current) |
| Production project-board/review-packet polish (PR #765) | Frontend-only Production polish balances the Projects board column widths so populated columns have readable cards while empty columns remain visible but narrower. The review packet now records Production state and focused evidence for key screenshots: clean Project board/detail screenshots must be unfiltered baselines, Combined filters must show status/client pills plus deduped rows, selected Actions must show the searchable command menu with the expected command labels, and parent detail must show visible sub-issue/activity evidence in the first desktop viewport. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover readable board column balance, review-packet screenshot state, selected-actions/filter evidence, and parent-detail screenshot evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-detail team-scope polish (PR #765) | Frontend-only Production polish keeps project detail scoped to the active team when a project is opened from Video or Graphics. Mixed-team projects now keep rows, counts, breadcrumb team, and the `Video project` / `Graphics project` label aligned with the Projects board context instead of falling back to `All teams project`. The review packet records the project-detail row teams and rejects non-Video rows in the Video screenshot baseline. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover mixed-team project detail scoping, team crumb navigation, and review-packet project-detail row-team evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-board empty-column polish (PR #765) | Frontend-only Production polish makes empty project-board lanes static: they keep title/count, collapse, and empty copy, but no longer show fake add/options controls. Card-level controls remain only where a specific project card gives the action context. The review packet records empty/populated column evidence and rejects column headers with action controls. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover static empty board lanes and review-packet board-column action evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production group-header fake-action cleanup (PR #765) | Frontend-only Production polish removes no-op add buttons from issue-list and project-detail group headers. The read-only preview keeps useful hierarchy/context affordances, including guarded add-sub-issue controls on detail pages and card-level project controls where card context exists. The review packet records zero fake group-header add controls for desktop list and project detail. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover desktop list/project-detail group headers and review-packet zero fake-action evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project Display count polish (PR #765) | Frontend-only Production polish keeps project-detail counts aligned with the active Display options. When Show sub-issues is off, the right-side Issues card now reports the visible parent-row count instead of the broader total project count, matching the main project issue header and grouped list. | Revert the relevant PR #765 commit(s) to undo this pass. It changed only `index.html`, Production browser tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover hidden-subissues project side-count consistency; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production Argos visual-diff export (historical PR #765; F122 contained) | The local preparation command can still create a private `.codex-tmp` export, but the re-enabled GitHub workflow does not upload that folder to Actions or Argos. All 31 retained GitHub Argos bundles were deleted, the first post-#836 Production run created no public visual/Argos delivery, and current repository configuration exposes no Argos token/project setting. Historical external Argos account/build retention is unproved. | If the computation is noisy, disable the Production workflow; do not restore the upload step, token, or deleted bundles as rollback. The owner must audit/delete any external historical builds separately; keep all local and runner-local packets access-controlled and untracked. | 2026-07-15 (#836 merged; workflow re-enabled with no public visual/Argos delivery; external account history open) |
| Production project-board header chrome cleanup (PR #765) | Frontend-only Production polish removes fake disabled add/options controls from Project board column headers. Board headers now show only collapse, status, title, and count; card-level controls remain where a specific project card gives the control context. The review packet records zero board-column header action controls across both empty and populated columns. | Revert the relevant PR #765 commit(s) to restore the prior guarded column-header controls. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover zero fake Project board column-header actions; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production topbar fake-action cleanup (PR #765) | Frontend-only Production polish removes fake disabled favorite and notification icons from Production list, project detail, issue detail, and pre-paint skeleton topbars. Topbars keep useful navigation breadcrumbs and the context menu dots, but no longer show no-op chrome that reads as a real app action. The review packet records zero fake topbar favorite/notification controls for desktop list, project detail, and parent detail. | Revert the relevant PR #765 commit(s) to restore the prior guarded topbar controls. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover zero fake topbar favorite/notification controls; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production parent sub-issue affordance polish (PR #765) | Frontend-only Production polish changes parent issue detail pages from an unlabeled add-sub-issue plus icon to a visible `+ Add sub-issues` text affordance. The action remains guarded/read-only; this is only a clarity and finished-app polish change. The review packet records the visible label for the parent-detail screenshot. | Revert the relevant PR #765 commit(s) to restore the icon-only parent sub-issue affordance. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover visible parent-detail Add sub-issues text; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production selected-actions plural copy polish (PR #765) | Frontend-only Production polish makes the selected-issue command menu plural-aware: multi-select now shows `Copy issue IDs` and `Delete issues`, while one selected issue keeps singular labels. Clipboard and delete guard behavior are unchanged; this only aligns visible command copy with the selected count. | Revert the relevant PR #765 commit(s) to restore the prior always-singular selected-actions labels. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover selected-actions plural labels in review-packet evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project context-menu picker polish (PR #765) | Frontend-only Production polish makes project-card right-click commands match the visible project controls: `Change status`, `Set lead`, and `Set target` now open guarded read-only pickers instead of appearing as fake disabled mutation rows. `Copy link` remains active. The shared Production test opener also has a retry/diagnostic hardening for slower CI runners. | Revert the relevant PR #765 commit(s) to restore the prior disabled project context-menu rows and previous test opener behavior. It changed only `index.html`, Production browser tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover active project context-menu picker actions, read-only guard behavior, and CI opener reliability; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production detail properties scaffold cleanup (PR #765) | Frontend-only Production polish removes the literal `Controls disabled` pill from issue, sub-issue, and project detail Properties cards. The actual property rows still open guarded read-only pickers, and the composer still shows its disabled preview copy. | Revert the relevant PR #765 commit(s) to restore the prior `Controls disabled` scaffold pill. It changed only `index.html`, Production browser tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover zero `detail-controls`/`project-controls` scaffold pills while preserving guarded property pickers; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-detail empty due polish (PR #765) | Frontend-only Production polish changes project-detail issue rows with no due date from an icon-only calendar pill to a readable `Add date` pill. The control still opens the same guarded read-only due-date picker. | Revert the relevant PR #765 commit(s) to restore the prior icon-only empty due pill. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover readable project-detail empty due labels and review-packet evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-board lane width polish (PR #769) | Frontend-only Production polish gives empty and populated Project board status lanes the same readable desktop width. Empty `Planned`, `Paused`, `Completed`, and `Canceled` lanes no longer collapse into narrow strips; the board uses horizontal scroll when all lanes cannot fit. | Revert the relevant PR #769 commit(s) to restore the prior narrow empty-column layout. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover equal readable Project board lane widths in layout and review-packet evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production selected-actions command-menu polish (2026-07-10) | Frontend-only Production polish makes the selected-issue action bar visually recede while its searchable `Actions` command menu is open. Selection and guarded read-only commands are unchanged; this only reduces stacked active chrome. | Revert the relevant polish commit(s) to restore the prior fully visible action bar under the command menu. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover the receded selected-actions bar and cleanup after guarded command close; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production selected-actions Linear menu correction (2026-07-10) | Frontend-only Production polish replaces the receded selected-actions menu treatment with a Linear-style centered command panel above the action bar. The action bar stays visible, the menu shows a `2 issues`-style header with an Ask Linear hint, and hovering command rows only highlights them instead of opening blocking picker submenus. Click/Enter still opens guarded read-only pickers. | Revert the relevant corrective PR/commit to restore the prior receded action-bar treatment and hover-open submenu behavior. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover centered command-panel placement, visible action bar, and no hover-open picker submenu; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-board scope label polish (2026-07-10) | Frontend-only Production polish makes the Project board `All projects` scope label read as quiet static context instead of a button-like active pill. The real team scope control remains the adjacent `Video`/team filter pill. | Revert the relevant PR/commit to restore the prior active-pill visual treatment for `All projects`. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover non-interactive cursor/pointer/background evidence for `All projects`; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project-card empty-target polish (2026-07-10) | Frontend-only Production polish makes empty target metadata on Project board cards compact and icon-only instead of repeating `No target` on every card. The target picker remains wired and guarded through the same card control. | Revert the relevant PR/commit to restore the prior visible `No target` labels on project cards. It changed only `index.html`, Production review automation/tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover icon-only empty target controls in review-packet evidence; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production detail empty-copy polish (2026-07-10) | Frontend-only Production polish removes internal `migrated row` wording from empty detail descriptions. Issue, sub-issue, and project detail pages use finished copy such as `No description.` and `No project description.` **F138 correction:** the prior `No activity yet.` evidence was visual/test-only; current runtime never invokes the native event loader/Activity renderer. | Revert the relevant PR/commit to restore the prior migration-specific empty copy. It changed only `index.html`, Production browser tests/review automation, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. Activity replacement remains an independent F138 gate. | 2026-07-14 (F138 runtime correction; original description-copy evidence retained) |
| Production sidebar count polish (2026-07-10) | Frontend-only Production polish removes large numeric issue badges from the Video and Graphics sidebar `Issues` rows. Team issue navigation remains clickable; this only reduces sidebar noise. | Revert the relevant PR/commit to restore the prior team issue count badges. It changed only `index.html`, Production browser tests, and audit/rollback notes; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-10 (focused guards cover no numeric badges on team issue nav rows; final PR validation recorded in `EXECUTION_LOG.md`) |
| Production project toolbar/display polish (2026-07-09) | Frontend-only Production polish is layered on top of the read-only `?prod=1` preview: removed the unclear project Open/Closed/All tabs, moved Project details next to Filter, wired project Display grouping and Show sub-issues, removed the Production workspace menu entirely, added the Production boot skeleton, cleaned project grouped-row hover bands, clears stale tooltips on navigation, removes child IDs from sub-issue breadcrumbs, suppresses native browser context menus inside Production, fixes project-card selection focus clipping, prevents project-row metadata/date clipping, constrains combined filter pills, dedupes visible issue IDs, explains filtered-empty project detail states with visible/total counts plus Clear filters, labels filtered project-board counts as matching issues, hides empty board columns when filtered matches exist, uses `No matching projects` for true filtered no-match boards, renders migrated issue-description Markdown/resource links instead of raw markers, replaces unresolved activity skeleton bars with a clear migrated-row empty state, and replaces selected-issue Actions with a searchable command menu plus real Copy issue ID. | Revert the project-toolbar-polish PR/commit to undo this pass. It changed only `index.html`, Production browser tests, and documentation; no Supabase data, runtime flags, n8n workflows, Linear webhooks, or backend write paths changed. | 2026-07-09 (verified `git diff --check`, `prod-readonly-smoke.js`, `prod-structure-subset.js`, `prod-interaction-inventory.js`, `prod-layout-polish.js`, `prod-review-packet.js`, `prod-review-packet-validate.js`, `behav-wired.js` 161/161 guard mode, `pixel-wired.js` light/dark, targeted screenshots, `npm test` 64/64 unit suites, and full `npm run test:prod-polish` in 374.8s) |
| Production polish automation (2026-07-09; F105/F122 update) | `npm run test:prod-polish` and the local review generator remain the Production proof path. The GitHub workflow was re-enabled 2026-07-15 with fast/interaction/heavy lanes, schedule, and concurrency intact; screenshots, review packets, Argos exports, generated live-derived summary content, and private test logs remain runner-local. F105's current-main candidate repairs the post-#850 locked fixture, inline layout, empty-baseline synchronization, and shared read/console audit while keeping every live mutation, persistent/unexplained error, and pending read fail-closed. Fast passed 7/7 in 143.8s and the aggregate passed 10/10 in 484.4s without product-code or live-state changes. | Require a green manual fast/interaction/heavy run on the exact F105 candidate and cloud review before merge. If the computation is noisy, disable the workflow; do not restore public payloads or weaken the zero-live-mutation/read-audit invariants. Local and runner-local review packets remain access-controlled and untracked. | 2026-07-18 (F105 candidate locally green; F122 no-public-output boundary retained; cloud review/merge pending) |
| Linear inbound engine (Track B B3 + Part 2) | LIVE, and **REDEPLOYED 2026-09-04** — run `33899387402`, reviewed release `72fbc4a5be6c570c2d6638a49b320abd4e4b2c5c`, source closure `019a463d…`, `verify_jwt=false`, full receipt in `EXECUTION_LOG.md` under 2026-09-04. **The lane's summary reports the closure, not the version, so the live version number has NOT been read back and is recorded as one past v40 rather than as a number nobody verified**; the workflow's own closing line says that provider readback remains owed. **The one-step restore position moved and the automated path did NOT follow it.** This lane's only automated rollback is `restore-captured-v39`, which writes back the pinned `V39_BUNDLE_SHA256` artifact — one release behind live until 2026-09-04, and **two** behind since. Restoring through the lane now undoes this release AND the 2026-08-02 one. There is no automated path to v40; reaching it is a manual captured-artifact restore. F27 P.3 established the pinned inbound artifact and the 2026-08-02 final production receipt read `linear-inbound` v40 exact, fresh, and ACTIVE. The former v39 listing is historical pre-P.3 evidence. The two EF-bound Linear webhooks provide the current event-time mirror. The two legacy configurations target inactive/unpublished `MJbMZ789B5ExZz9x` and are not a fallback. | Any behavior kill is a separate owner-approved operation: first capture `linear_inbound_enabled` and both EF-bound webhook states, present the exact target changes, and define rollback as restoring and reading back every captured value/state. After reverting all newer callers/contracts, use only the captured exact v40 source/entrypoint/JWT artifact with independent readback. Historical versions 13/14/27/39 are evidence, not automatic rollback targets. Never publish MJb as fallback without F46's decision/drill. | 2026-09-04 (forward deploy run `33899387402`, closure `019a463d…`; provider version readback still owed. Previously 2026-08-02, v40 exact/fresh/ACTIVE final receipt.) |
| Linear deliverables reconciler v2 (Track B B3 stage 2/4) | LIVE measuring instrument: `.github/workflows/linear-deliverables-reconcile.yml` runs dry-run by default and `scripts/linear-deliverables-reconcile.js` compares B1 deliverables to Linear across status, title, due date, assignee, parent, archive/delete, engine-tracked comments, card linkage completeness, and Linear webhook enabled/resource state. It writes one summary event row with `source='reconcile'`. Production still uses the old payload-bearing reader until the source-only #1013 migration and script are separately installed. The GitHub cron remains `*/10` (recently about 13 deliveries/day). On 2026-08-03 only qll workflow `qllIDZPkdNAPRj0b`'s V2 dispatch branch moved to hourly minute 0; its shared 15-minute trigger still owns Calendar, Samples, V2-summary monitoring, B1 gating, and outbound. #1013 adds no live object, n8n change, source trigger, or cache writer by merge alone. **F132 remains:** diff, repair-list, and linkage share a two-summary rule and hourly throttle. Healing stays off unless `apply=true`. | Immediate read-load containment is the isolated hourly V2 branch, never slowing the shared qll trigger. Before installing #1013, disable/quiesce the Actions workflow; apply/read back only the service views/functions and optional concurrent index; require identical legacy-versus-bounded repair/linkage/outbound counters in the same acceptance run while recording absolute baseline/current values as evidence only, then restore only the V2 branch to 15 minutes. Post-install rollback disables/quiesces the workflow, drops only the optional index concurrently plus the three views/seven functions, reverts source while disabled, and proves one terminal reverted run. If the old reader returns, keep or restore the V2 branch's hourly relief. Never change authority, Track-A flags, Linear objects, frozen writers, source rows, or shared qll cadence as reader rollback. | 2026-08-03 (branch-only hourly relief read back; no-trigger redesign source-only and uninstalled) |
| Finished-work history import (Track B) | 3,187 deliverables + 800 batches imported 2026-07-10 from live Linear finished work across 28 clients via `batch_write`/`deliverable_write` with `source='backfill'`, all rows tagged `created_by='history-backfill-2026-07-10'`. Independently verified: counts exact, 0 orphaned batch references, statuses faithful to live Linear spot-checks, reconciler v2 held diff 0 / repair 0 throughout and after (4,323 deliverables checked). Pending card linkage drained to 0 on 2026-07-11 (31 additive slot fills; 208 residue rows all classified non-actionable). | **The former one-command DELETE is invalid; do not run it (F62).** `production_comments` now has NO ACTION foreign keys to tagged deliverables/batches (2,481 + 5 current dependent rows), and the old event predicate matches 0 while 6,473 backfill events reference the tagged IDs. Default behavior is preserve the import. Any removal needs a fresh private dependency snapshot, owner-approved preserve/relink/delete disposition for comments/events/slots/children, dry-run counts, one transaction with assertions, and a TEST/scratch restore rehearsal plus readback. | 2026-07-14 (dependency/FK and event-scope correction; original import proof retained) |

| Write-UI legacy callers + Submit reroute (#850 merged dark cohort) | Calendar/Samples status and comments await `production-write` before source persistence for allowlisted clients and accept native IDs without requiring Linear URLs. A principal-bound, cross-tab-locked repair journal reconciles attempted writes against authenticated `committed_exact`, `absent`, or `conflict` receipts; cache checkpoints are never commit proof. Submit separately checkpoints its idempotent request, authenticated native response, and each Calendar materialization. Historical unverifiable queue debt is quarantined rather than blindly replayed. PR #850 merged the callers; the installed current closure is `production-write` v27. | Revert callers first, close Submit, and preserve the repair journal, legacy queues, quarantine, and durable intake checkpoints until `native_intake=0`, `source_repairs=0`, and `unknown_records=0` or an owner recovery artifact exists. Do not restore unauthenticated n8n writers. Preserve the exact current parity value unless the owner separately authorizes its reviewed CAS; silently disabling it reintroduces the linked-card mark-done failure. Any backend rollback must use F51's captured provider-source/JWT redeploy plus independent deployed-source/JWT hash readback. | 2026-08-02 current closure; parity restored enabled at window close |
| F27 corrective per-team rollback brake + operator toolkit (**INSTALLED AND PRODUCTION-VERIFIED**) | The real 2026-08-01 failed attempt and its successful Section 7 rollback remain historical evidence. Attempt 2 entered from that exact retained boundary on 2026-08-02 and applied migration SHA-256 `6e403d4400f683dbd21a3cb28b74729912dbac092812e6e3187b8e1c7ab868e6` exactly once from release `968a895108beb2a2c41e86bb8b788115e35b14a0`; transaction/self-probe and verify-after returned PASS. Section 4 run `30763278795` deployed/read back outbound v35, production v27, deliverable v26, and batch v26 with 4 PASS / 0 FAIL / 0 ERROR. The reserved production drill returned `F27_DRILL_RUNNER_OK`, retained its audit, proved the required real-finalizer authority-CAS refusal, and changed no real outbox/fence/flag state. `F27_FINAL_VERIFICATION_OK` then returned PASS across all 17 enumerated assertions; at window close inbound v40 and all four closures were ACTIVE, parity was restored enabled, and the reconciler was ACTIVE/quiescent/monitor-only with default `apply=false`. | **Per-team incident rollback:** use FLIP_RUNBOOK R2. Stop the involved F2/F4 lanes, call the installed begin function to atomically hold/snapshot that team, classify every intent, replay only owner-approved rows with correlated terminal receipts, prove team zero, then use the installed guarded finalizer as the one-statement authority reversal. **Defective-release rollback:** follow F27_INSTALL_RUNBOOK Section 7; keep APPLY disabled, restore captured exact source/JWT closures and operative definitions, revoke mutating grants, and retain additive schema, monotone generations, and audit. Never use `DROP ... CASCADE`, overwrite the queue, reset generations, delete audit, default-drain, or infer historical transitive provenance. | 2026-08-02 attempt-2 migration/deploy/drill/final receipts; 2026-08-01 failed attempt retained separately |
| F21 write-UI quarantine startup popup | **REMOVED by owner decision.** Stale pre-upgrade leftovers remain parked silently, are never auto-sent, and remain inspectable by agents/ops through `peekWriteUiLegacyQuarantine()` and `peekWriteUiLegacyQueueState()` if ever needed. Scheduled reconcilers remain the Linear/SyncView drift-healing mechanism. | Queue recovery does not require restoring the popup. If the owner reverses the UI decision, restore only the notice UI; do not alter, replay, discard, or auto-send parked items, and do not change writers, flags, n8n, or Linear. | 2026-07-16 owner decision; popup-only removal. |
| Calendar Create Post + existing-batch append (#850 merged dark cohort) | Staff Create Post keeps client scope implicit, defaults to the latest active batch, and offers a new-batch path. Both choices feed the shared native intake job and paired Video/Graphics builder for allowlisted clients. Existing-batch intake carries batch CAS; the gateway fails closed unless client, active status, project mapping, and each team's parent route match. The service-only `production_intake_append` RPC locks the batch and atomically writes the pair, events, and outbox intents. #850 merged the caller; the installed current closure is `production-write` v27, and no real-client enrollment is implied. | Revert/disable the Calendar caller first and preserve every durable intake checkpoint until reconciled; never materialize a local blank card after an attempted request. The append RPC may remain dormant. Remove it only after proving no deployed caller or in-flight job depends on it, using the owner-only rollback block in `migrations/2026-07-13-production-intake-append.sql`. Future gateway deploys must keep the exact-main-SHA guard and `linear-outbound`-before-`production-write` order; merge/push must deploy neither. | 2026-08-02 current closure; earlier TEST-drill limits remain |
| F201 production-labels outbox CHECK (migration **APPLIED** 2026-07-24) | `migrations/2026-07-23-f201-production-labels.sql` was applied via the Supabase SQL editor 2026-07-24 ~22:00Z, pinned to reviewed SHA `1738ad3`, verified by its per-step SQL boolean. The live outbox operation CHECK now accepts `labels` (see `EXECUTION_LOG.md` and the `migrations/README.md` recipe bullet). No flag, authority, or n8n state changed. | The CHECK replacement is a strict superset and dropped no data; a same-transaction inverse would be a new reviewed migration, not an improvised paste. Data-restore path is the 6-hourly Track-B private snapshot (PR #840 package restore, `docs/ops/TRACK_B_BACKUP.md`) (no dedicated rule-4 phase snapshot was taken for this window — owner-accepted residual; the most recent scheduled pre-window Track-B snapshot is the restore point). Do not narrow the CHECK while any `labels` outbox row or deployed `production-write` ≥ `1738ad3` depends on it. | 2026-07-24 (per-step boolean + external CHECK readback; recorded in `EXECUTION_LOG.md`) |
| F202 production-descriptions outbox CHECK (migration **APPLIED** 2026-07-24) | `migrations/2026-07-23-f202-production-descriptions.sql` applied in the same 2026-07-24 ~22:00Z window, second in order, pinned to `1738ad3`, boolean-verified. The CHECK now also accepts `description`, and the restrictive `deliverable_events` SELECT policy hides `description_change` rows from browser roles (see `migrations/README.md` recipe bullet). | Same posture as F201: strict-superset CHECK, no data dropped; restore path is the Track-B 6-hourly snapshot (no dedicated rule-4 phase snapshot was taken for this window — owner-accepted residual; the most recent scheduled pre-window Track-B snapshot is the restore point). Never re-expose `description_change` events to anon/authenticated readers as a "rollback". | 2026-07-24 (per-step boolean + external CHECK readback; recorded in `EXECUTION_LOG.md`) |
| F203 production issue-create RPCs (migration **APPLIED** 2026-07-24) | `migrations/2026-07-23-f203-production-issue-create.sql` applied third in the same window, pinned to `1738ad3`, boolean-verified: service-role-only atomic create, post-ack linkage, and quarantine RPCs are live (see `migrations/README.md` recipe bullet). The real TEST creation drill is still owed. | Additive-only: the RPCs are service-role-only and dormant without a caller drill. Containment is stopping the deployed `production-write` caller lane, never dropping the RPCs; restore path is the Track-B 6-hourly snapshot (no dedicated rule-4 phase snapshot was taken for this window — owner-accepted residual; the most recent scheduled pre-window Track-B snapshot is the restore point). | 2026-07-24 (per-step boolean; recorded in `EXECUTION_LOG.md`; TEST drill pending) |
| F39/F42/F43 comment-thread lifecycle (migration **APPLIED** 2026-07-24; linked-cohort import EXECUTED 2026-07-25) | `migrations/2026-07-23-production-comment-thread-lifecycle.sql` applied fourth in the same window, pinned to `1738ad3`, boolean-verified; `production_comments` read back service-role-only. The F42 linked-cohort import then executed through the guarded Actions lane: apply run `30138142140` (`f42-import-2026-07-25c`) applied 615 comments (612 calendar / 3 sxr) with 6,032 deferred and 35 quarantined link defects; readback counts matched (see `EXECUTION_LOG.md`). | Additive lifecycle RPCs replace bodies without dropping data; imported canonical rows carry import-run provenance, so any owner-ordered removal is a reviewed, provenance-scoped operation — never a blind DELETE. Restore path is the Track-B 6-hourly snapshot (no dedicated rule-4 phase snapshot was taken for this window — owner-accepted residual; the most recent scheduled pre-window Track-B snapshot is the restore point). The TEST read/write/projection drill (runbook step 6) and rollback-evidence retention (step 7) are still owed. | 2026-07-25 (apply digest `bfc03c2c…`; counts readback matched; steps 6–7 pending) |
| F34/F53/F137 production attachments + browser-read revoke (migration **APPLIED** 2026-07-24) | `migrations/2026-07-23-f34-f53-production-attachments.sql` applied fifth in the same window, pinned to `1738ad3`, boolean-verified. Live readbacks: anon GET `deliverables?select=linear_raw` → `401`; `production_deliverables_browser_v1` → `200`; the CHECK accepts `attachment` (see `migrations/README.md` recipe bullet and `docs/ops/F34_LINEAR_ASSET_RESCUE.md`). Rescue config seed, inventory reconciliation, and the real TEST attachment drill are still owed. | **Never restore whole-table browser SELECT on `batches`/`deliverables` as rollback** — the compatible UI ships with this migration and the revoke is the confidentiality fix. If the bounded view misbehaves, repair/redeploy the view or the paired functions from a SHA ≥ `1738ad3`. Restore path for data is the Track-B 6-hourly snapshot (no dedicated rule-4 phase snapshot was taken for this window — owner-accepted residual; the most recent scheduled pre-window Track-B snapshot is the restore point). | 2026-07-24 (per-step boolean + 401/200 live probes; recorded in `EXECUTION_LOG.md`; rescue window pending) |

| Slice 5 Production read path + F95 refresh (migration **APPLIED** + browser live 2026-07-26) | `migrations/2026-07-25-slice5-production-read-path.sql` replaces the `production_deliverables_browser_v1` body in place (`create or replace view`) so each row detoasts `linear_raw` once instead of 24 times, and adds `deliverables_updated_at_idx`. Column names/order/types, grants, and `security_barrier` are preserved; output is proven byte-identical in both directions of `EXCEPT ALL` over 4,626 rows offline. The paired `index.html` change replaces the projection's OFFSET paging with a sequential primary-key keyset walk and adds the bounded F95 delta-refresh loop. Both halves are now live: the browser merged via PR #944 and the migration/gateway executed in the 2026-07-26 window. | Two independent levers. **View/index:** re-run the original `create view` statement from `migrations/2026-07-23-f34-f53-production-attachments.sql` as `create or replace view`, and `drop index if exists public.deliverables_updated_at_idx;` — both are catalog-only, hold no data, and need no restore. **Browser:** a Pages-only revert of the `index.html` commit restores offset paging and removes the refresh loop; the view keeps working under either paging shape, and the keyset browser keeps working under either view body, so the two may be reverted in any order. Never "roll back" by widening browser SELECT on `deliverables`/`batches`. | 2026-07-26 window: migration applied ~23:45Z via SQL editor pinned to `f3cf20e` (46 columns, grants + `security_barrier` read back, index present); `production-write` v26 deployed via run `30226070558` with 12/12 attestation; measured 1,273→392 ms/page and 3.6–4.3 s→1.15 s/boot walk; assignee gateway fail-closed readback 401; §3 TEST drills still owed (`EXECUTION_LOG.md`) |
| Production-tab issue creation CLOSED (browser live on merge; **server half LIVE as of deploy #21, 2026-08-24, `production-write` v47**) | Owner ruling 2026-08-23: neither top-level nor sub-issue creation happens in the Production tab; posts start on the content calendar. `_prodCreateGateText` returns a single refusal before every other reason, closing all four of its callers at once, and the hand-copied FIFTH gate inside `_prodCreateTopbarButton`'s unscoped branch is deleted — that copy evaluated to "allowed" on the unscoped board and on Graphics project pages, so the button rendered live there. `production-write` refuses `create` with `403 production_create_closed`, placed AFTER `productionCreateReplay` so a create that already committed can still be returned to its author. `_prodCreateRecoveryGateText` is deliberately left open for the same reason. Measured cost: the Production-tab create signature (`origin='manual'` + outbox `legacy_parity=false`) matches 53 rows, all `test_only`, ZERO real-client rows ever. | Two independent levers. **Browser:** delete the one `return PROD_CREATE_CLOSED_TEXT;` line in `_prodCreateGateText` — the whole gate body is retained beneath it, unreachable, precisely so reopening is a one-line deletion rather than a rewrite. Restoring the topbar's inline duplicate is optional and NOT recommended; the scoped branch already routes to the real gate. **Server:** remove the `throw new GatewayError(403, "production_create_closed")` and redeploy. Safe in either order: server-only leaves a straggler modal showing the sentence, browser-only leaves a curl window on a door with no users. | Browser suite `prod-write-gateway-browser.js` re-pointed and PASSING (EXIT 0, 94 assertions); 4 mutations proven fatal by exit code (browser refusal deleted, server refusal deleted, the two copies of the sentence drifted apart, recovery path wrongly closed). Decision recorded in `OPEN_REPAIRS.md` item 31 and `FLIP_BUG_LEDGER.md` §0-7 |
| Attribution slug guard widening (migration **APPLIED 2026-08-23**; browser half merged 2026-08-23) | `migrations/2026-08-23-attribution-slug-guard-widening.sql` re-issues the `production_deliverables_browser_v1` body as `create or replace view` with `&` added to two character classes (`raw_attribution_client_slug`, `raw_attribution_provisional_client_slug`), so one ACTIVE roster slug stops being sanitised to NULL and 147 deliverables stop arriving `resolved` with no slug. Body is `pg_get_viewdef` of the live view with two literals changed; column names/order/types, grants and `security_barrier` preserved and re-asserted. The transaction ends with an assertion that fails if any active roster slug still fails the widened guard. The paired `index.html` change makes `_prodResolveAttributions` treat an ABSENT persisted slug as missing evidence rather than a disagreement, so it no longer stamps `persisted_resolved_client_disagrees_with_current_mapping` and no longer propagates `hierarchy_conflict_propagated` across the family — 147 of 176 conflict banners. | Two independent levers, safe in either order. **View:** re-run the body with the original class `^[a-z0-9][a-z0-9_-]{0,99}$` in both guards (pre-change definition in `migrations/2026-07-25-slice5-production-read-path.sql`) — catalog-only, holds no data, needs no restore. **Browser:** a Pages-only revert of the `index.html` hunk; the browser half is correct under either guard, and the view is correct under either browser, so neither depends on the other. Never "roll back" by widening browser SELECT on `deliverables`. | Pre-apply proof 2026-08-23 against the LIVE database with zero permanent change: new body instantiated as a TEMPORARY view and compared in-query — 5,316 rows both sides, 46 columns both sides, `resolved`-with-no-slug 147→0, symmetric difference 294 rows = the same 147 counted once per direction. Browser half mutation-proved (4 mutations fatal by exit code) in `test/attribution-absent-slug-not-conflict.js`. Applied by the owner 2026-08-23 pinned to `8887d2a0`; committed clean, and the transaction's own assertion read 0 offending roster slugs. Readback: resolved-with-no-slug **147 -> 0**, 5,316 rows and 46 columns unchanged, `security_barrier` and both anon/authenticated grants preserved; inverse test confirms 147 rows now carry a slug the OLD guard rejected. Window: `docs/ops/ATTRIBUTION_SLUG_GUARD_WINDOW.md`; receipt in `EXECUTION_LOG.md` |
| Canonical comment surface link validity (#937 follow-up, browser **unmerged**) | The canonical comment gate treats a card as linked only when its card→deliverable link is crosswalk-VALID — the deliverable's `origin`/`team`/`client_slug`/`card_id` must describe that exact card, validated in the browser exactly as `scripts/f42-card-comment-import.js` does. Mismatch, absent deliverable, fetch error and not-yet-resolved all fall back to legacy card-array rendering, and an empty canonical projection never overwrites non-empty legacy content. This closes a durable data-loss path: an invalid-link card was previously treated as linked, its canonical read returned empty, and the calendar projection wrote that empty array through `_calSetCommentsFor`, which also rewrites the `*_tweaks` wire string — so the next save persisted the loss, on staff sessions as well as client ones. Read path only; it reads the five crosswalk columns anon already holds on `deliverables`. | **Pages-only revert.** Revert the `index.html` commit through the normal reviewed path and require the resulting Pages deployment plus live-HTML readback. No schema change, no migration, no runtime flag, no Edge Function deploy, no n8n edit and no data cleanup is involved, because nothing outside the browser read path changed. Note the pre-revert behavior is the destructive one: reverting restores the state in which an invalid-link card can empty its own stored comment JSON, so do not revert as a routine precaution — only for a specific proven regression. | not merged (unit suites green; `npm run test:prod-polish` and the review packet pending a reviewer with CDN egress) |
| Canonical comment mark-done CAS refresh (browser **candidate**) | Staff Calendar and Samples/SXR resolve discards cached comment pages and rereads the complete exact canonical thread immediately after the destination dialog, revalidates staff identity plus the live card/deliverable binding, and sends the freshly read version/timestamp. An exact first `409/write_conflict` may trigger one reread and one body-free resolve retry; already-resolved is an idempotent zero-write success, while a second conflict and every failed/incomplete reread remain fail-closed. Error-banner Retry preserves resolve and its chosen destination rather than entering the reopen toggle. Edit/delete/reopen do not auto-retry. `Mark done — don't change the status` still writes the successful canonical comment projection into the legacy card shadow, but comment-only flushes no longer recompute overall status and send no status field. Browser only; no Edge Function, schema, runtime flag, authority, Linear, or n8n change. | **Pages-only revert.** Revert the candidate's `index.html` commit through the normal reviewed path and verify the resulting Pages deployment/live HTML. There is no backend or data rollback because the candidate changes none. Reverting restores the longer stale-CAS window, resolve/reopen Retry ambiguity, comment-only status recomputation, and manual-repeat failure, so use only for a demonstrated regression. | not merged (focused behavior/sabotage guards and all 201 unit suites passed on the current-main rebased candidate) |
| Kasper Ad Performance panel (**v1 + v2 + v3 fully LIVE — table, function, n8n, and UI all merged, deployed, and proven**) | **v1 + v2 (live):** table `kasper_ad_performance_daily`, `kasper_ad_performance_by_ad_daily`, `kasper_ad_leads`, the Edge Function (returning `rows`/`summary`/`by_ad`/`leads`), the n8n live-pull + backfill workflows, and the full UI (date-range toggle, per-ad table, per-lead list) are merged (#1127, #1131), deployed, and proven against real production data (real n8n executions `427729`/`427742`/`427743`; live-HTML fetch from `https://syncview.synchrosocial.com/` confirmed the UI is actually serving). **v3 (live):** one new table, `kasper_ad_unfinished_leads` (PK `lead_key`, real PII: name/email/phone) — applied to production, and the Edge Function redeployed returning `unfinished_leads`. Mirrors n8n's existing `booking_recovery` Data Table (fed by the pre-existing "Sales — Booking Recovery Capture (iClosed)"/"Dispatch" workflows, not a new capture path) — potential/qualified leads on this campaign who never finished booking, disqualified excluded. Merged via #1137. The live-pull n8n workflow was rebuilt twice same-week with a 4th independent branch (`Pull Unfinished Leads` → `Map Unfinished Leads` → `Upsert Unfinished Leads`) that doesn't touch the existing three branches or `Build Daily Rows` at all — not added to the backfill workflow, since the filter is a current snapshot with no historical gap to backfill. First rebuild `CdCYzye6Khp6x5A6` (superseding `BKl9OFVMb4VS2IHf`): credentials wired by the owner 2026-08-25, **proven with a real test execution** (`428427`) — all three existing writers succeeded, a live Supabase readback showed a fresh `updated_at` matching the execution's timestamp to the second, and the new branch ran cleanly writing nothing (filtered on `status='pending' AND utm_campaign='prospecting'`, matched zero rows at that point). **2026-08-25 same-day correction:** the owner manually backfilled 5 real leads from iClosed's dashboard, then noticed the UI showed no follow-up status for people he knew had been emailed — checking `booking_recovery` directly found 4 of 5 already had real rows there with `status=completed`/`suppressed`, never `pending`, because Dispatch moves `status` off `pending` the moment it contacts someone. The `status='pending'` filter was silently excluding every already-contacted lead, a bug that would have recurred going forward, not just a backfill mistake. Fixed by rebuilding again as `6OtjILbhkYLY6yVE` (superseding `CdCYzye6Khp6x5A6`): filter narrowed to `utm_campaign='prospecting'` only, with exclusion of genuinely resolved leads (`suppressed_reason` = `booked`/`disqualified`/`other_calendar`) moved into `Map Unfinished Leads`'s Code node. **Proven with a real test execution** (`431479`): the live pipeline itself re-discovered and correctly rewrote all 4 real leads' `follow_up_due_at`/`email_sent_at`, verified via direct Supabase readback matching the execution's timestamp to the second. The bad values from the original manual backfill were separately corrected via `migrations/2026-08-25-kasper-ad-unfinished-leads-followup-correction.sql`. **Published** (`activeVersionId` confirmed live) and both old revisions (`CdCYzye6Khp6x5A6`, `BKl9OFVMb4VS2IHf`) archived. The UI adds an "Unfinished leads" section (kept separate from "Booked leads" — different columns) showing captured date, name, email, phone (`tel:` link), status, and follow-up (email/SMS sent, or due date). Everything remains additive and read-only from the app's side. | **v1 + v2 (unaffected by v3 rollback):** as before — drop the three tables if truly unused, undeploy/redeploy the function, deactivate/archive the n8n workflows. **v3 table:** no browser access and nothing depends on it; drop if unused (`drop table public.kasper_ad_unfinished_leads;`) — **holds real lead PII (name/email/phone), so prefer dropping it over leaving it if this feature is ever reverted**. **v3 function:** redeploy from the pre-v3 source (or undeploy) to drop the `unfinished_leads` field. **v3 n8n workflow:** deactivate/archive `6OtjILbhkYLY6yVE` (current live revision) in n8n to stop the new branch's future writes; existing rows (if any) are unaffected, and the other three branches on the same workflow would also stop, so restore from `BKl9OFVMb4VS2IHf`'s definition (archived, not deleted) if the base pull needs to keep running without the unfinished-leads branch. **v3 UI:** `git revert` the merge through the normal reviewed path; the backend keeps running untouched. | 2026-08-25 (v1/v2: see above, unchanged. v3: migration applied/read back, function redeployed/401-verified, unit suite green including new PII-logging assertions in `test/kasper-ad-performance-auth.js` covering `email`/`phone`/`first_name`/`last_name`; merged via #1137; n8n workflow `CdCYzye6Khp6x5A6` credential-wired by the owner, proven via real test execution `428427` — all writers succeeded, Supabase readback confirmed a fresh write, the new branch correctly found nothing to write — then published, with the prior revision `BKl9OFVMb4VS2IHf` archived. Same-day correction: owner caught a real `status='pending'` filter bug via a blank follow-up column on 4 real backfilled leads; fixed by rebuilding as `6OtjILbhkYLY6yVE`, proven via real test execution `431479` — live pipeline re-discovered and correctly rewrote all 4 leads' follow-up data, verified via Supabase readback; bad backfill values corrected via a follow-up migration; `CdCYzye6Khp6x5A6` archived; UI gained a phone column) |
| Create Post video-editor picker (**browser merged, gateway needs deploy**) | Calendar Create Post shows a video-editor dropdown defaulting to the editor with the fewest OPEN videos (`todo`/`in_progress`/`tweak`), labelled as a suggestion. The browser sends `assignee_id` only when the suggestion is OVERRIDDEN; the default path is payload-identical to the pre-picker shape, so it works against the currently deployed gateway. `production-write` additionally balances `autoAssigneeForIntake` on open work only and accepts a validated video override (graphics still refused). | Browser: revert the `cal-native-editor-*` block and the `videoAssigneeId` computation in `_calSubmitNativePost` — the payload returns to its previous shape with no server coordination. Gateway: the override path is inert until an override is actually sent, so an un-deployed gateway simply refuses it with `intake_assignee_override_not_allowed` and the dialog explains that. No migration, no flag. | 2026-08-24, unit 283/283; not yet exercised against a deployed gateway |

## 3. Emergency full rollback (worst case, any time during Track A)

1. Flip the affected server-side authority/kill flag to its documented safe stance first. Then
   revert the browser caller and let Pages redeploy (~10 min plus stale tabs); source revert alone
   is not immediate containment.
2. In n8n, restore only an exact approved graph after live readback. Never activate
   `MJbMZ789B5ExZz9x` as a generic fallback; F46 requires a separate owner decision and drill.
3. Confirm monitoring pager `qllIDZPkdNAPRj0b` is active, then require the Calendar dispatch's
   correlated terminal receipt plus an observer outside n8n; pager active-state or quietness is not
   emergency proof (F132). Restore `AkiFmromoDkmsh39` only after the
   current graph/trigger/credential/error-handler fingerprint and last-green proof are captured,
   duplicate cadence is excluded, the owner approves, and activation plus first green are read back.
4. Run the relevant `qa/master.js` lane against the deployed production app/backend using only the
   private TEST fixture. Fail closed on fixture mismatch; clean up and read back exact restoration.
5. Write the incident + timeline into `EXECUTION_LOG.md` before resuming any migration work.

During Track B, stop affected users and disable/read back the involved F2/F4 outbound lane(s), both
if unknown/mixed, for immediate containment. Per-team
authority→`linear` is allowed only after F27's audited intent resolution and machine-read zero.
Before any EF code rollback, remove/revert its callers and require F51's pinned closure/toolchain/release
manifest plus independent server fingerprint and a proved supported artifact-restore route. Do not
claim “nothing is lost” until the affected team's outbox/foreign-intent reconciliation is complete
(F05/F27/F41).

## 4. Backup inventory (what exists, where)

- **Independent Track-B backup (MERGED & ACTIVE 2026-07-15, PR #840 / merge `4f9d919`):** `.github/workflows/track-b-backup.yml`
  takes a production-read-only snapshot of the 14 Track-B tables into an HMAC-signed
  private Google **Shared Drive** package every **6 hours**, plus a production-ref-blocked scratch rehearsal.
  It is now on `main` and the recurring schedule is live (first scheduled run pending the next cron tick).
  Proof run `29444939853` uploaded a real package to the Shared Drive and independently re-read its
  parent/filename/byte length/Drive MD5/downloaded bytes/HMAC before advancing last known good; a
  dedicated-scratch restore completed in 229.03 seconds with exact counts and zero core orphans.
  Failure/staleness alerts use the failed GitHub Actions run and the repository owner's built-in Actions
  email (`sidney.laruel@gmail.com`); Slack is optional and absent.
  PITR is unavailable under the owner's paid-add-on opt-out and is an accepted residual risk.
- **Weekly** (Sun 02:00, n8n `jlVfbg0Njxf1It7h`): main Sheet copy, repo zip, ALL n8n workflow
  JSONs, Supabase dumps (calendar_posts, content_samples, + onboarding tables) → private
  weekly-backup Drive folder. On 2026-07-06, the workflow export filter was corrected so the
  weekly backup includes all workflows, not just the 45 inactive workflows; REST inventory
  readback at the time of the fix was 120 total workflows (75 active, 45 inactive), including
  `MJbMZ789B5ExZz9x`.
- **Repo**: `n8n-backups/` (public-safe status stubs only), `migrations/` (schema), git history
  + tags. Phase snapshots per rule 4 add to these.
- **Phase 0 baseline closed 2026-07-03**: live schema committed at
  `migrations/live-schema-baseline-2026-07-03.sql`; private full n8n export confirmed via
  weekly-backup execution `191240`. Because this repo is public, raw unredacted workflow JSON
  remains private; repo evidence is summarized in `n8n-backups/2026-07-03-phase0-snapshot-status.md`.
- **Pre-A1 snapshot started 2026-07-03**: git tag `pre-A1` points to `main` commit
  `ba365410`; `calendar_posts` was dumped privately to
  `private-backups/2026-07-03-pre-A1/calendar_posts.pre-A1.2026-07-03.json` (outside the
  public repo). A1-scoped n8n workflow versions are recorded in
  `n8n-backups/2026-07-03-pre-A1-snapshot-status.md`; raw workflow JSON remains private.
- **A1 staged rollback point (historical behavior only; not authorized now under F67)**: the
  `calendar_upsert_ef_clients` runtime flag was the single flip point for browser and reconciler
  calendar-upsert routing. The value `{"clients":[]}` made all clients use the old unauthenticated
  n8n upsert path; current operators must not execute that rollback until equal auth/scope exists.
  Canary could contain only the privately configured
  TEST fixture;
  any real-client value requires the owner's next explicit gate approval.
- **A1 TEST deploy/parity status 2026-07-03 (historical, not current workflow state)**: additive A1
  support tables were live, the `calendar-upsert` Edge Function was deployed, and n8n workflow
  `MJbMZ789B5ExZz9x` was then published with flag-based routing. It is inactive/unpublished at the
  2026-07-13 readback; use the Live State table, not this historical checkpoint. Rollback was
  rehearsed with the TEST client and returned to
  `{"clients":[]}`. Final TEST parity passed all six cases, including comment merge; no real
  client is enabled.
- **A1 TEST browser canary status 2026-07-04**: after fixing the stale `p87` probe,
  GitHub Actions `calendar-e2e-nightly.yml` baseline run `28690695855` with the flag empty
  and EF run `28691435661` with only the private TEST fixture flagged both completed green on current
  PR head `ee2de4dd76fe0e96745ac1b1085d3c4c7cff6234`. Both logs report `All 67 probes
  passed`; A1-relevant probes `p89`-`p94` passed on both paths. Rollback was executed
  immediately after the EF run: `calendar_upsert_ef_clients` is `{"clients":[]}` and no real
  client is enabled. `Calendar E2E (nightly)` and `Samples E2E (nightly)` are both
  `disabled_manually`.
- **A1 merged TEST-only live check 2026-07-04**: PR #668 was merged to `main` as merge commit
  `eecfa4b`; GitHub Pages deployment run `28695476386` completed successfully for that commit.
  After Pages completed, `calendar_upsert_ef_clients` was set to the private TEST-only allowlist.
  This enables only the configured TEST fixture for owner-driven
  live testing. Rollback remains one SQL update to `{"clients":[]}`; no real client is enabled.
- **A2 n8n snapshot/edit status 2026-07-04 (historical, not current workflow state)**: workflow
  `MJbMZ789B5ExZz9x` was exported
  privately before and after the A2 samples-branch routing edit; public-safe evidence is in
  `n8n-backups/2026-07-04-a2-snapshot-status.md`. The active version at that checkpoint was
  `405ab03a-12bb-43ca-b70a-14aee3ba7f35`; current `activeVersionId` is null. The old sample-review
  n8n upsert webhook remains a browser-write fallback, but the inactive MJb receiver is not a
  current Linear inbound fallback.
- **A2 TEST deploy/parity status 2026-07-04**: additive flag migration
  `migrations/2026-07-04-a2-writer-edge-functions.sql` has been applied. `calendar-reorder`
  is deployed at version 2; `sample-review-upsert` and `sample-review-reorder` are deployed at
  version 1, all with JWT verification disabled. Live TEST parity passed for
  `calendar-reorder-batch-shape`, `sample-review-reorder`, `sample-review-upsert-create`,
  `sample-review-upsert-link-clobber`, `sample-review-upsert-clear-link`,
  `sample-review-upsert-conflict`, and `sample-review-upsert-comment-merge`. Cleanup verified
  0 `a2_parity_%` rows/events and `sample_review_ef_clients` remains `{"clients":[]}`. No real
  client is enabled and A2 remains draft/unmerged.
- **Pre-B0.5 Track B snapshot 2026-07-05**: git tag `pre-B0.5-track-b-2026-07-05`
  points to post-B0 `main` before the B0.5 branch. Public-safe runtime flag and table-count
  snapshots are stored privately under
  `private-backups/2026-07-05-pre-B0.5-track-b`; raw secrets and workflow JSON remain outside
  the public repo. B0.5 is staged only and must not add any real client to Track A flags.
- **Pre-B1 Track B snapshot 2026-07-06**: git tag `pre-B1-track-b-2026-07-06`
  points to `main` commit `f7d77561c823335964e6d78dd854b0086957884d`. Public-safe snapshot
  evidence is in `n8n-backups/2026-07-06-pre-B1-track-b-snapshot-status.md`; private Supabase
  JSON snapshots are stored outside the public repo. B1 does not edit n8n workflows. The
  additive B1 model has since been backfilled under owner-approved handling rules, while no
  user-facing route depends on it yet.
- **Workload weighted planning + authority-routed deadline candidate 2026-07-23 (source-only)**:
  this branch changes Pages behavior, adds the deliberate-manual `workload-linear` function source,
  and admits `surface='workload'` only in candidate `production-write` source for guarded native due
  CAS. Exact native receipts broadcast an ephemeral sibling invalidation; the sibling must re-read
  sensitive metadata before adopting due/CAS truth. No candidate Pages/function source, authority,
  flag, migration, table grant, n8n edit, frozen-writer change, or live data is deployed or changed.
  Before any live deploy, rollback is a normal reviewed source revert.
  A future release must first capture the exact pre-candidate `production-write` provider source/JWT
  closure and the absent `workload-linear` pre-state, then deploy the reviewed exact merge SHA.
  A Pages revert restores the prior visible behavior for fresh loads but is **not** a complete write
  kill because stale tabs or direct authenticated callers can still reach a deployed function.
  Contain the affected server lane in one step: redeploy the captured pre-candidate
  `production-write` closure for native-route harm, or delete/retire `workload-linear` for
  Linear-route harm, with independent hash/rejection or absence/404 readback. If the lane is unknown
  or mixed, stop affected staff and perform both containments before resuming; then revert Pages,
  read back live HTML, and require affected tabs to close/reload. Separately redeploy the prior exact
  `workload-plan` v2 closure from `fd3e0eaa` only if the Creative read split was also deployed and
  must be reversed.
  Receipt keys/pending maps are browser-only hints, not commit truth or rollback data. Never
  automatically reverse a confirmed due edit—review and correct the authoritative Linear/native row
  individually if business intent changed. Preserve Creative write denial, do not flip authority as
  containment, and never fall back to n8n, `calendar-upsert`, or `sample-review-upsert`.
- **Workload background change-only refresh candidate 2026-07-22 (Pages-only)**: this change touches
  only the browser Workload reader and its tests/docs. It adds no function, schema, migration, grant,
  runtime flag, n8n workflow, or writer change. Roll back with a normal reviewed revert of this merge;
  no live-service rollback or deploy is involved. That revert deliberately restores the prior
  tab-switch/visibility behavior, including forced Linear-reader traffic and calendar skeletons, so
  use it only for a worse browser regression. Keep the existing manual Refresh and post-create direct
  paths intact, and never compensate by changing n8n, a runtime flag, or either frozen client writer.
- **Rename-forked batch adoption + background self-reload candidate 2026-08-24**: two changes in one
  merge, with different rollback scopes, and they are independent.
  `scripts/b1-linear-backfill.js` (`adoptExistingParentClaimants`) is importer-only: it adds no
  function, schema, migration, grant, runtime flag, or writer change, and it never deletes a claim —
  it redirects a group that would otherwise INSERT a second batch for a parent an active batch
  already claims. Roll back with a normal reviewed revert; the next run then resumes minting a new
  batch per rename, which is the prior behaviour. Any batch already adopted keeps its id and its
  children — a revert does not un-adopt, and must not be followed by a manual re-split.
  The `index.html` self-reload is Pages-only. Roll back with a reviewed revert of this merge, which
  restores propose-only behaviour for fresh loads; stale tabs already running the candidate keep the
  self-reload until they reload once. It is bounded by design — one reload per tab per half hour,
  refused outright when any field is dirty, any contenteditable holds text, anything is open on top
  of the page, or `sessionStorage` is unavailable — so containment is a revert, never a runtime flag.
  The **data repair** handed to the owner for the 123 already-forked rows is separate from both and
  is reversed from `public.batches_parent_claim_backup_20260824`, which the repair writes before it
  touches anything: `update public.batches b set linear_parent_ids = k.linear_parent_ids from
  public.batches_parent_claim_backup_20260824 k where k.id = b.id;`. Never reverse it by
  re-inserting claims by hand — the backup is the only record of which batch held which slot.
- **Link-slot seal + gate failure-location candidate 2026-08-25**: two changes in one merge, with
  different rollback scopes, and they are independent.
  The `index.html` seal is **Pages-only**. It adds no function, schema, migration, grant, runtime
  flag, n8n workflow, or writer — it REFUSES a write that the gateway already refuses one step
  later, and it changes nothing about what the gateway accepts. Roll back with a normal reviewed
  revert of this merge; the paste control returns exactly as it was, and no stored row needs
  touching because the seal never wrote one. Note what a revert restores: the ability to paste a
  Linear link onto a SyncView-authoritative component, which is how a card becomes permanently
  unable to have its status changed (`native_link_required`). Prefer flipping that team's
  `prod_authority` back to `linear` over reverting, if the goal is to restore linking: the seal
  unseals itself when authority moves, needs no deploy, and keeps the guard in place for the team
  that is still native. Reverting is for a defect in the seal itself, not for wanting the control
  back.
  Cards already carrying a link keep it and still open it — the seal removed the pencil, never the
  anchor, and never the stored value. Clearing a link was deliberately left ungated in BOTH
  directions, so a revert changes nothing about the repair path either.
  The `docs/syncview-design/tests/**` failure-location codes are **CI-only**: a closed list of
  phase names in `prod-write-gateway-browser.js` and matching literal patterns in
  `prod-polish-gate.js`. They alter no product behaviour and no suite's pass/fail — only the code a
  failure reports. Roll back with a reviewed revert; the gate then classifies those failures as
  `error_generic` again, which is the blackout the change exists to end, so revert this half only
  if a marker itself is wrong. `test/prod-polish-failure-location.js` fails if the two lists ever
  name different sets, so a partial revert of one side is caught before merge rather than after.
- **Video deliverables may carry a canonical artifact 2026-08-30 (PR #1182 + #1183)**: three layers,
  deployed bottom-up, rolled back TOP-DOWN, and the database layer is deliberately not rolled back
  at all. The layers are `migrations/2026-08-30-artifact-video-projection.sql` (applied by the owner
  2026-08-30), the `production-write` closure `cddf9a01` (dispatched from the #1182 merge commit),
  and the `index.html` panel half that stops conditioning the attach control on the graphics team.
  Each fails closed independently, which is what makes the order matter in both directions.
  Roll back by (1) reverting the browser half, which stops offering Attach on a video row, then
  (2) redeploying the prior `production-write` closure through
  `.github/workflows/deploy-f27-section4-closures.yml` — the source-exact restore lane — which
  returns a video attach to `operation_forbidden` from the layer that actually decides it. Stale
  tabs keep the control until they reload, which is precisely why the gateway is step 2 and not
  last. Then (3) **stop**: leave the migration installed. It is additive, and with the gateway
  reverted its video branch is never reached. Reverting it while the layers above still carry the
  widening is the one sequence that produces a real incident — the gateway accepts the attach,
  calls a graphics-only function, and the raise reaches `rpc()` as a 500 `native_write_failed`
  while the panel keeps offering the control. Worse than the widened state and worse than the
  original refusal. Re-apply the August 6 definition only for a defect IN the function itself, and
  only after steps 1-2. The first version of the migration's own rollback note got this wrong and
  was corrected in the same PR as this entry; the note now carries the same sequence.
  **One-step containment** if the widening must stop before a deploy can run: set
  `prod_authority.video` back to `linear`. `production_artifact_write` calls
  `production_assert_authority` on the row team before doing anything else, so every video attach
  raises `team_is_linear_authoritative` at the database — no deploy, no stale-tab window — and the
  browser stops offering the control for the same reason, so there is no dead button. Its cost is
  that it is not scoped to attach: it stops every native video write (status, due date, assignee,
  comments), which after the flip is the whole Production tab for that team. It is the right lever
  for a broad incident and the wrong one for a defect in attach alone.
  No schema, table, column, index, trigger, policy, grant or runtime-flag value was changed by any
  of the three layers, and no row was written at install time, so nothing needs repairing on the
  way back; a deliverable that already carries a `file_url` keeps it, and the calendar card it was
  projected onto keeps its `asset_url`. Neither is un-set by a rollback, and neither should be:
  they are the real artifact and the real link, whatever refuses the next write.
- **Raw footage and the frame folder become editable 2026-08-31**: three layers again —
  `migrations/2026-08-31-batch-asset-write.sql` (`production_batch_asset_write`), the
  `production-write` `batch_asset` operation with its policy gate, and the `index.html` per-row
  Assets editor. Deploy bottom-up, roll back TOP-DOWN, and for the same reason as the video
  artifact: dropping the database function while the gateway and browser still offer the control
  leaves a live Edit button whose save returns a 500 `write_failed` with nothing to explain it.
  Revert the browser half first (the Edit control leaves the two folder rows), then redeploy the
  prior `production-write` closure through `.github/workflows/deploy-f27-section4-closures.yml`,
  and only then, if the function should be gone at all, `drop function if exists
  public.production_batch_asset_write(text, text, text, text, jsonb);`. Leaving it installed under
  a reverted gateway costs nothing — nothing else calls it.
  **One-step containment** is the same lever and carries the same cost as the video artifact: set
  the batch team's `prod_authority` back to `linear`, which the function asserts before it writes,
  and which also stops every other native write for that team.
  What a rollback does NOT undo, deliberately: a folder link somebody corrected stays corrected.
  The write is a plain column update on `public.batches` with a `deliverable_events` audit row, no
  outbox leg and no Linear mirror, so there is no queue to drain and nothing in-flight to reconcile.
  A batch whose link was wrong before this shipped and right after it should stay right.
  Note the widening to know about if it is ever questioned: `batch_asset` is one of the
  operations where a creative is NOT confined to their own team. A batch is one shoot with one set
  of files worked by both teams, and it carries a single `team` value, so the match would have
  handed the shared folder to whichever team happened to be recorded.
  **Corrected 2026-09-01, twice, because an operator reading this to plan a rollback would have
  had the authorization boundary backwards in both directions.** This paragraph named
  `batch_description` as the other cross-team operation. It is not one and never shipped as one:
  the first version of it was widened, review on #1203 caught that this made a new write more
  permissive than the `description` beside it and produced a live browser/gateway mismatch, and
  `staffOperationAllowed` has returned false for it ever since -- a creative cannot perform it at
  all, on either team. The operation that IS cross-team alongside `batch_asset` is `attachment`,
  widened on 2026-09-01 by owner ruling for the same reason (a post is the unit of work, not a
  team); the asset and description READ gate stopped looking at the team in the same change.
  So "every other operation keeps the confinement" now reads: `status`, `due` and `comment` keep
  it, `status` is additionally assignee-bound, and descriptions remain admin/SMM.
  `test/batch-asset-write.js` and `test/asset-access-any-team.js` fail if either boundary moves
  again without this file moving with it.
  **Two later definitions supersede that file and neither may be rolled back to it in isolation.**
  `2026-08-31-batch-asset-team-fallback.sql` derives the authorizing team when `batches.team` is
  null (303 of 1,644 batches), and `2026-08-31-batch-asset-client-slug-insert-arm.sql` sends
  `client_slug` so `batch_write`'s INSERT arm is a valid row — without it every call raises
  `23502` and no batch asset write commits at all, which is the state the estate was in from the
  day the slots shipped until 2026-08-31. Re-applying `2026-08-31-batch-asset-write.sql` therefore
  does not "return to the previous behaviour": it returns to the total outage. If the function must
  go back, drop it (step 3 above) rather than reverting to an earlier definition, and take the
  browser and gateway halves down first in the order already given. `test/batch-asset-write-insert-arm.js`
  fails if a definition ever stops sending a NOT NULL column that `batch_write` does not default.
- **A post's own description becomes editable 2026-09-01**: three layers, same shape as the folder
  links above — `migrations/2026-09-01-batch-description-write.sql`
  (`production_batch_description_write`), the `production-write` `batch_description` operation with
  its policy gate, and the `index.html` description editor on a batch parent. Deploy bottom-up,
  roll back TOP-DOWN: dropping the database function while the gateway and browser still offer the
  control leaves a live Edit button whose save returns 500 `write_failed` with nothing to explain
  it. Revert the browser half first (Edit leaves the batch parent's description), then redeploy the
  prior `production-write` closure through `.github/workflows/deploy-f27-section4-closures.yml`,
  and only then, if the function should be gone at all, `drop function if exists
  public.production_batch_description_write(text, text, text, text, jsonb);`. **All five argument
  types, and the count is load-bearing:** the function takes
  `(text, text, text, text, jsonb)`, and `drop function if exists` with the wrong signature matches
  nothing and exits 0 — so a four-type drop reports success while leaving the real writer
  installed. `test/batch-description-cas-timestamptz.js` fails if this line and the migrations stop
  agreeing. Leaving it installed under
  a reverted gateway costs nothing — nothing else calls it.
  **One-step containment** is the same lever: set the batch team's `prod_authority` back to
  `linear`, which the function asserts before it writes.
  What a rollback does NOT undo, deliberately: a description somebody corrected stays corrected.
  The write is a plain column update on `public.batches` with a `deliverable_events` audit row,
  **no outbox leg and no Linear mirror** — the event deliberately carries no `outbound` key,
  because that key is the enqueue signal and requesting a mirror for a write with no counterpart is
  what produced `f27_authority_generation_stale` on the asset path. So there is no queue to drain
  and nothing in-flight to reconcile.
  **Corrected 2026-09-01.** This said the same team widening as `batch_asset` applies, for the
  same reason -- a post description is read by every sibling on every team, and a two-team post
  carries a single `team` value, so a match would hand the text to whichever team happened to be
  recorded. That reasoning describes a version of the operation that never shipped. It was written
  widened; review on #1203 caught that it made a new write MORE permissive than the `description`
  operation beside it (which has always refused creatives) on no ruling, and that the browser gate
  asks with `description` and was refusing exactly the creatives the widened line admitted -- a
  live mismatch, not a theory. So `staffOperationAllowed` returns false for `batch_description`
  and a creative cannot perform it on either team; only admin and SMM reach it, through the early
  return. Opening it to creatives is an owner ruling nobody has made.
  `test/batch-description-write.js` fails if a batch parent ever becomes writable for anything
  except its description.
  This does NOT touch a sub-issue's own description, which is a different column
  (`deliverables.brief`), a different operation (`description`), and keeps its Linear mirror leg.
  **Superseded 2026-09-01 by `migrations/2026-09-01-batch-description-cas-timestamptz.sql`, and
  that file must not be rolled back to the one above.** The shipped body could not commit a single
  save. Its compare-and-swap compared `updated_at::text` (`2026-08-31 20:18:54.574498+00`) against
  the ISO string PostgREST hands the browser and the browser hands back
  (`2026-08-31T20:18:54.574498+00:00`) — the same instant, different text — so `is distinct from`
  was true on an untouched row and refused every attempt. Its three `raise exception` messages were
  spaced English, which matches none of the gateway's underscore guards, so even a correct refusal
  fell through to 500 `native_write_failed`: a `wait`-class code that told the SMM to retry
  something that could never succeed. The replacement casts inside the body (the parameter stays
  `text` on purpose — a `timestamptz` parameter would install a second overload and PostgREST would
  answer PGRST203 ambiguous) and renames the raises to tokens the ALREADY-DEPLOYED gateway maps, so
  **this repair is SQL-only: paste it in the Supabase SQL editor, no function deploy.** Re-applying
  `2026-09-01-batch-description-write.sql` does not "return to the previous behaviour" — it returns
  to the total outage. If the function must go back, drop it as step 3 above.
  `test/batch-description-cas-timestamptz.js` executes the deployed mapper's own regexes against
  the migration's own raise strings, so the pairing cannot rot silently.

## Social media manager side card (SyncLinear detail)

Two halves, independently reversible, and safe in either order.

- **Browser half** (`index.html`): the `Social media manager` side card under
  `Project`, plus `_prodSmmDirFrom`, `_prodLoadSmmDirectory` and
  `_prodSmmPurgeSensitiveState`. Ships with the ordinary Pages deploy on push to
  `main`. To disable without a revert, make `_prodSmmCardHTML` return `''` — the
  card is additive and nothing else reads its cache.
- **Function half** (`smm-weekly-reports`): `loadOptions` selects
  `source_clients` and `synced_at`, and `serializeManager` returns them.
  Deployed by `deploy-onboarding-edge-functions.yml`, which **attests** the
  function fingerprint rather than gating on a stored digest, so there is
  nothing to re-pin and nothing to reseal. Reverting the two fields is safe on
  its own: with them absent the browser builds an empty directory and the card
  simply does not render.

**There is no data to roll back.** This change writes nothing. It adds two
fields to a read projection and one read-only card. `social_media_managers` is
still populated only by the nightly n8n manager sync (`y3rEWCVdB0esN3tO`), which
this change does not touch.

**Access limitation, recorded because it is a rollout dependency and not a
bug:** `?action=options` is Admin/SMM (`authorizeStaffKey(given, ["admin",
"smm"])`) and this change deliberately does not widen it. A Creative account and
the unsigned client preview cannot reach the endpoint and therefore never see
the row. Widening it means a lower-privilege projection, not a wider grant on
`social_media_managers` — that table's `anon` grant was revoked by F88
(`2026-07-14-f88-safe-sensitive-read-revocations.sql`) and a live check still
returns 401.

**If the browser half ships before the function half**, the endpoint returns
managers without `source_clients`, the directory is empty, and the card does not
render. No error, no console noise, no partial state.

## 2026-09-05 — post-scoped shared assets, Dropbox share keys, binding-first card surface

**Three independent halves, and they roll back independently.**

**1. The Pages browser half** (`index.html`). Ships on merge. Reverting it alone
is safe in both directions: a browser without it reads no per-slot write target
and writes the batch row the open deliverable names, which is the behavior that
shipped before 2026-09-05. The sub-issue file pills degrade to being asked for on
the open row's batch only — i.e. absent on the 109 split posts, as before.

**2. The `production-write` half** (`index.ts`, `policy.mjs`), deployed by
`deploy-f27-section4-closures.yml` against a re-pinned closure. Reverting it
alone returns the asset read to the 2026-09-01 downward, all-or-nothing borrow
and returns `assetUrlType` to refusing Dropbox links carrying `st`, `e`,
`preview` or `subfolder_nav_tracking`. A browser that is ahead of it reads no
write target and falls back, so **there is no order in which this pair produces a
control that fails** — unlike the 2026-08-30 artifact widening, where reverting
the database alone left the gateway calling a function that would raise.

**There is no data to roll back for these two.** The read half writes nothing.
The write half changes only which existing `batches` row a `batch_asset` write
names; no column, table, index, policy or grant is touched, and no backfill runs.
A link written to the post's row while this was live stays exactly where it was
written and is readable by both the old and new read paths (the old one reads it
whenever the reader's own batch is that row, the new one from anywhere on the
post).

**3. `migrations/2026-09-05-artifact-card-binding-first.sql`** — owner-run, and
in **no deploy lane's closure**: it adds and edits no file under
`supabase/functions/`, so it needs no fingerprint re-pin, no sealed capture and no
merge freeze. Rollback is re-applying the definition from
`migrations/2026-08-30-artifact-video-projection.sql`. Nothing else needs undoing:
no layer above it was widened for it, and the only rows whose behavior it changes
are the 7 that were refused before and would simply be refused again. It writes
no row at install time.

**If the browser half ships before the function half** — which it does, by
construction — the panel behaves exactly as it did on 2026-09-04 for the two
shared slots, and the file pills improve on the posts that already had them. No
error, no console noise, no partial state.

## 2026-09-05 — deployed: production-write v67

`production-write` moved 66 → 67 (closure `cc44bf93…` → `d2914ac2…`) from commit
`a05e1126437bb8c36bd3f33e3701a58924a8627d`, carrying the post-scoped shared
asset resolution, the per-slot write target, the Dropbox share query keys, and
the sub-issue file pill scope. `batch-write` (35), `deliverable-write` (35) and
`linear-outbound` (47) redeployed unchanged at their existing closures.

**Restore path:** the sealed prior four are in the `SyncView Backups/` Shared
Drive root as `b66c0ae103e18077521c153fe88b98191de6038a656251fbb2421c73118cde31`
(534,985 bytes), sealing `production-write` v66 / `cc44bf93…`. Restore is the
same lane with `operation = restore-captured-prior-four` and its own
confirmation from the F27 runbook — never a re-dispatch of the forward
operation, per the lane's own instruction that an ambiguous forward response
must not be retried.

**The browser half is already live** and was before this deploy, by
construction: it degrades to writing the row it is on when the gateway offers no
per-slot target. So rolling the function back alone leaves a working page with
the pre-2026-09-05 behaviour, not a broken control.

`migrations/2026-09-05-artifact-card-binding-first.sql` was applied by the owner
the same day, separately and independently of this lane. Its rollback is
unchanged: re-apply the definition from
`migrations/2026-08-30-artifact-video-projection.sql`.

## 2026-09-05 — pending: production-write pinned at d7fc8348… (evidence reuse)

Two repo pins have moved since v67 `d2914ac2…` was deployed, neither yet
dispatched: `6a39a2bc…` (exclusivity truncation guard, #1294) and `d7fc8348…`
(`asset_access_read` reuses a fresh ledger verdict by `(slot, url_sha256)`;
`recheck: true` forces a probe). The next Section 4 dispatch carries both; there
is no intermediate state to restore to.

**The browser half is live first and is safe against either gateway.** It sends
`recheck` only from the Refresh access button; an older gateway ignores the
field and probes as it always has. A newer gateway under an older browser
answers the same shape faster. Rolling the function back to v67 therefore leaves
a working page with live probes, not a broken read.

**There is no runtime switch for the reuse short of redeploying v67.** Per row,
the Refresh access button forces a live probe (`recheck: true`), and the window
a held verdict can be reused for is bounded by `ASSET_EVIDENCE_MAX_AGE_MS`
(five minutes), after which the next read probes again on its own. Disabling it
for everyone is the v67 restore above; there is deliberately no flag the page
reads for it, so nothing new for the harness to be told about.

**The index migration is independent.** `2026-09-05-asset-evidence-by-url.sql`
adds `production_asset_access_checks_by_url_idx`; the lookup is correct without
it. Rollback is `drop index if exists
public.production_asset_access_checks_by_url_idx;`.

**Unapplied reciprocal completion correction:** no schema object, browser or writer/auth change. Withdrawing its state-reader change restores the known false green, so hold the runner and retain unresolved accounting rather than treating old output as completion. Keep accepted rows, receipts and provenance. See `docs/audits/2026-09-05-native-intake-reciprocal-review.md`.

**Local history-v6 follow-up:** preserve original14/21/33 packages as limited historical evidence and keep every newer receipt/provenance/journal row. Never bypass older-target refusal or truncate omitted data. No migration or activation occurred; see `docs/ops/INTEGRATED_RECOVERY_CORPUS.md`.
