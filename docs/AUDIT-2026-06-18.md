# Content-Calendar Audit — 2026-06-18 (overnight)

Scope: SMM calendar, client link, and Kasper Review/Replies, audited from the
Sidney Laruel test client only. Method: 5 parallel static code audits of the
subsystems + live UI testing (Playwright, Buenos Aires timezone) on Sidney.
**No live/production change** was made to the frontend (fixes are on a branch /
PR) or to any other client's data.

---

## ✅ FIXED & VERIFIED — PR #526 (`claude/calendar-audit-fixes`)

All verified: full test suite green (16 suites, exit 0) + live smoke test
(SMM 4 views + client review + Kasper render with **0 JS errors**).

| # | Bug | Severity | Note |
|---|-----|----------|------|
| 1 | **Date off-by-one** in Review card + card preview — `new Date("2026-07-01")` parsed UTC, rendered local → every Americas tz showed the *previous day/month* | 🐞 HIGH | Verified live: Buenos Aires "30 Jun" → correct "1 Jul". Affected you + your US/CR colleagues directly. |
| 2 | **Multi-select leaked across client switches** — bulk-archive could act on the wrong client / unseen cards | 🐞 HIGH | `_calResetSelection()` on every client switch |
| 3 | **Shift-range select grabbed filter-hidden cards** → could archive other-month/blank/read-only cards never seen | 🐞 MED | Now ranges only over visible `.cal-card-selectable` |
| 4 | **Caption generation sent an empty (default) prompt** if a client's custom prompt hadn't loaded yet → tailored prompt silently ignored | 🐞 HIGH | Awaits `_calLoadCaptionPrompts()` before posting |
| 5 | **Kasper "mark as read" over-marked** — resolving an unrelated tweak buried an older still-unread reply | 🐞 HIGH | Stamps the newest *non-Kasper* internal message |
| 6 | **Kasper Replies inbox never refreshed** while open — new replies didn't arrive until you left and came back | 🐞 HIGH | Refresh now runs on Review + Replies; + typing/draft guards |
| 7 | **Title-review kebab switch desynced** across devices (collab synced, title didn't) | 🐞 MED | Added `_calSyncTitleReviewSwitch()` at hydrate + adopt |
| 8 | **`title_comments` missing from re-render gate** → a new YouTube *title* note didn't repaint Review live | 🐞 LOW | Added to `_calPostsEqualForRender` |
| 9 | **Empty 'done' caption** showed "✓ Caption generated" over an empty box | 🐞 LOW | Now settles as an error |
| 10 | **Generated caption could flicker to empty** under v2 Supabase-mirror lag | 🐞 MED | Protected via `_calLocalRecentSaves` |

(Plus the earlier **atomic comment merge** — PR #525 — already live.)

---

## ⚠️ FLAGGED — real issues I did NOT change (need your decision / a bigger change)

- 🐞 **HIGH — Settings row write is whole-row clobber, not concurrency-safe.** Two
  quick toggles (collab then title), or two devices, can lose one toggle; after the
  120 s local-trust window a reconcile flips it back. *Fix:* send the settings row
  through the field-level patch path (like cards), or serialize + re-merge. Bigger
  change to the settings write path — flagging rather than risking it overnight.
- 🐞 **MED — "Edit platforms" is per-browser localStorage only.** It's never
  persisted to the backend, so **the client never sees platform icons at all**, nor
  does a second SMM device. *Fix:* store the enabled-platforms set on the shared
  settings row (like collab/title). Product decision on where platform config lives.
- 🐞 **MED — Sheet tab silently defaults to the *current month*** (not "All months")
  for a client with no saved filter, so older/future cards look "missing" behind a
  small "!" chip. (Confirmed live: TEST 2 / May was hidden under the June default.)
  *Fix (one line):* default `_calNormMonthFilter` to `'all'` — but it's a UX call.
- 🐞 **MED — Undated posts pass *every* specific-month filter** on the Sheet, so an
  undated card shows under "June". *Fix (one line):* `return false` for no-date when
  a concrete month is selected. Behavior change — your call.
- 🐞 **MED — Reorder slot-healing can collide with a hidden post's `order_index`**,
  causing a nondeterministic snap-back on reload. *Fix:* heal against the union of
  all order_index (incl. hidden), or use spaced/fractional slots.
- ⚡ **MED — Bulk archive fires N concurrent upsert webhooks** (no batched archive
  endpoint; reorder has one). 30 cards = 30 calls. *Fix:* a batched archive webhook.
- ⚡ **MED — Every realtime row change = full client re-fetch + full calendar
  re-render.** Softened by an equality gate + thumb-harvest, but a real teammate
  change rebuilds the whole strip. *Fix (architectural):* a per-row patch path.
- ⚡ **LOW — Caption poller doesn't pause when the tab is hidden**; a stalled job
  polls 5 s + a 1 s ticker for up to 12 min. *Fix:* gate the poll on `document.hidden`.
- 🐞 **LOW — Removing YouTube from a card mid-title-review strands** `title_status`
  (drops from queues but data persists, unreachable). *Fix:* confirm/clear on removal.
- 💡 **LOW — Platforms editor lets you save an EMPTY set** → strip + per-card toggles
  vanish everywhere (recoverable via the kebab). *Fix:* require ≥1 or confirm.
- 🐞 **LOW — Restored caption job force-failed** after >3 min closed even if the
  backend finished; restore also re-shows the "Generating X/Y" chip for old jobs.
- 💡 **LOW — Cancel is best-effort** (a caption can still land after the UI says
  "cancelled"); a **manual edit during generation silently drops** the generated one
  with no "use generated?" affordance.
- 🐞 **LOW — Kasper reply send has no in-flight guard** (double-submit only
  prevented incidentally); toggling show-all / mark-read re-renders the whole inbox.
- ⚠️ Note: the Kasper queue realtime channel subscribes to ALL clients' rows
  (intentional for the cross-client tab, but the global fan-out to keep an eye on).

---

## ✅ Checked and found SOLID (not bugs)
- Client/read-only guards for select-mode, archive/link/delete buttons, and kebab
  editors — clients can't reach SMM mutations.
- Month/Week boundary math is timezone-consistent; archived cards filtered at load
  for all views; filters + scroll + selection persist/restore correctly.
- Caption bulk gen is not all-or-nothing; concurrency capped at 2; jobs cleaned up;
  save-slug pinned (no cross-client caption leak).
- **Kasper cross-client safety is clean** — replies route by `item.slug`, are
  internal/Kasper-role, hidden from clients by `_calCommentsForView`, and every
  handler resolves a pid within its own loaded per-client list (stale pid no-ops).
- Whole app renders with **0 JS errors** across SMM (4 views), client, and Kasper.

---

## UI / design notes (from the live screenshots)
The app looks clean and professional — strong card layout, a polished month grid,
clear Kasper threading. The one real UX trap is the **Sheet defaulting to the
current month** (cards can look missing); surfacing the hidden-count more loudly,
or defaulting to All-months, would help. Everything else read as intentional.
