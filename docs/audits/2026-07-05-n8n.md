# Live n8n Re-Audit — synchrosocial.app.n8n.cloud (2026-07-05, Track B mandatory re-audit)

Auditor domain: live n8n instance. Baseline: `docs/audits/2026-07-03-n8n.md`. All facts below are
MEASURED from live MCP reads on 2026-07-05 (~13:00–18:20 UTC) unless labeled ESTIMATE/INFERENCE.
READ-ONLY audit: no workflow was executed, edited, published, or archived. No secret values are
reproduced here; hardcoded credentials are described and redacted.

## 1. Inventory

- **87 workflows total, 75 active / 12 inactive** (MEASURED via search_workflows, count=87).
  Baseline was 87 total, 74 active / 13 inactive.
- **The single activation-state change since 07-03: `MJbMZ789B5ExZz9x` "SyncView Calendar — Linear
  Status Sync" is now ACTIVE** (activated 2026-07-03 per EXECUTION_LOG; last updated
  2026-07-04T05:48:04Z = the A2 samples-routing edit). Linear→app realtime push is back ON.
- **No new workflows since 07-03** (newest createdAt = 2026-07-02, all present in baseline).
- Workflows edited on/after 07-03 (updatedAt): `MJbMZ789B5ExZz9x` (07-04 05:48, A2),
  `t2RP7QNHrbQx52f4` Onboarding Provisioning (07-03 16:39), `hxLFIdKG9hUIzukO` + `ljNY7CKYLKzMOACZ`
  onboarding submits (07-03 02:56 — the "patched" versions already backed up in repo). Nothing else.
- Inactive set (12): ZJOtYpQZj73DcBB1, qmDGbKnvrK0sPFKj, ROx3VQ36xm58AaIM, yQBGgdbZPqOgn2eE,
  aw2b98wxraQTEulJ, G1RRkIDs6Mh7RGk8, 7Pdp6qnkBzwXP3YG, gB17L9M5yYxxk6GT, fJNyB5fNCRSTYNfQ,
  6yhqWsM8RT2c7AOp, VAqlVLk8wczPq6DQ, vb3O0wkTK6Q7Rtro. Identical to baseline minus MJbMZ.
- Still one personal project (`4dvRQbC5gyJNowXX`), 0 tags. Credentials: **33, names/types identical
  to baseline** (incl. "LINEAR" httpHeaderAuth, "GitHub PAT — reconcile", supabaseApi, n8nApi).
  Data Tables: **3, unchanged** (caption_jobs, onboarding_fallback, iClosed Cancelled Calls).

## 2. Track-B-critical workflow deep reads (current live JSON)

### 2.1 MJbMZ789B5ExZz9x — SyncView Calendar — Linear Status Sync (ACTIVE)
- versionId = activeVersionId = `405ab03a-12bb-43ca-b70a-14aee3ba7f35` (matches ROLLBACK.md).
- Trigger: webhook POST `/webhook/linear-status-sync`, **responds immediately** ("Workflow got
  started.") — fire-and-forget; Linear gets a 200 before any processing.
- 4 nodes, webhook fans out to 3 parallel branches:
  1. **`Handle Linear Event`** (Code, calendar): skips non-Issue events, `create` actions, and
     updates whose `updatedFrom` lacks `stateId`. Re-fetches the issue via Linear GraphQL
     (hardcoded Linear API key present — redacted), maps Linear state name → app status:
     contains "tweak"→Tweaks Needed; contains "scheduled"→Scheduled; ="posted"→Posted;
     ="approved"→Approved; contains "smm"→For SMM Approval; contains "kasper"→Kasper Approval;
     contains "client"→Client Approval; backlog/todo/to do/in progress/in process→In Progress;
     else skip. Resolves client slug from project name (slugify strips "dr.", collapses "and"/&).
     Targeted Supabase candidate read on `calendar_posts` (ilike on linear_issue_id /
     graphic_linear_issue_id, hardcoded publishable key) with fallback to full `/webhook/calendar-get`.
     Word-boundary regex match per video/graphic link; canonical-row pick = non-archived >
     most-recent updated_at > higher order_index. Guards: archived skip, Posted protection,
     freshness guard (linAt vs card updated_at; ABOVE_CLIENT = {Client Approval, Approved,
     Scheduled, Posted}), no-op skip.
     **A1 runtime-flag routing (added 07-03/04): before each upsert it GETs
     `syncview_runtime_flags.calendar_upsert_ef_clients` from Supabase REST; if the slug is in the
     set → POST the Supabase Edge Function `calendar-upsert`, else → n8n `/webhook/calendar-upsert-post`.
     Fail-safe: any flag-read error yields an empty set → n8n path.** Headers: X-Syncview-Actor
     "Linear status sync", Role "system", Source "linear".
  2. **`Plan Workload Row` → `Upsert Workload`**: builds a `workload_issues` row purely from the
     webhook payload (no GraphQL); `remove` action → `{active:false, synced_at}` soft-deactivate;
     `parent_identifier` intentionally omitted so partial upsert preserves reconcile-populated value;
     active = !(state.type completed|canceled). POST Supabase REST `workload_issues` with
     `Prefer: resolution=merge-duplicates` using the **supabaseApi credential** (not hardcoded key).
     Both nodes onError=continueRegularOutput.
  3. **`Handle Sample Linear Event`** (Code, samples — the embedded A2 branch): same skeleton as
     calendar branch with differences: mapStatus returns **null for "scheduled" and "posted"**
     (not applicable to samples); ABOVE_CLIENT = {Client Approval, Approved} only; reads
     `sample_reviews` (targeted candidates, fallback = direct full Supabase read, NOT an n8n get);
     **A2 flag routing: `sample_review_ef_clients` → EF `sample-review-upsert` else n8n
     `/webhook/sample-review-upsert`; same empty-set fail-safe.** Sends
     `comments_base_at: ''`, Actor "Linear sample status sync".
- Execution profile: 51 retained executions since first-retained 2026-07-03T19:59 (≈27/day,
  ESTIMATE from retained window). Nearly every execution follows a `linear-set-status` call by
  ~1s (echo of app→Linear writes); a minority are organic Linear-side changes. 0 errors.
- vs baseline: was INACTIVE since 06-28. Now active with the A1/A2 routing layered in. The repo
  backup `calendar-linear-status-sync.2026-06-26.embed-samples-branch.json` is STALE vs this
  version; the current pre/post-A2 exports are private (per EXECUTION_LOG), public-safe note at
  `n8n-backups/2026-07-04-a2-snapshot-status.md`.

### 2.2 VQqqeY9B2GZbh2Bt — Calendar — Linear Set Status (ACTIVE, unchanged since 2026-05-27)
- versionId `f3ec2123-17a0-4aea-b01d-983c9483dd07`. Webhook POST `/webhook/linear-set-status`,
  body `{issue, status}`, CORS *, responds via Respond node. Hardcoded Linear key (redacted).
- **App-status → Linear-state mapping (VERBATIM matching rules, applied against the issue's team's
  state list, case-insensitive):**

  | App status (body.status) | Linear state matched on the team |
  |---|---|
  | In Progress / In Process | state named exactly `Todo` or `To Do` |
  | For SMM Approval | first state whose name contains `smm` |
  | Kasper Approval | first state whose name contains `kasper` |
  | Client Approval | first state whose name contains `client` |
  | Approved | state named exactly `Approved` |
  | Tweaks Needed | first state whose name contains `tweak` |
  | Scheduled | first state whose name contains `scheduled` |
  | Posted | first state whose name contains `posted` |
  | (no match) | returns `ok:true, skipped` so FE doesn't retry |

- **Overdue due-date +2d bump (VERBATIM logic):** on EVERY call, if `issue.dueDate` (date-string,
  UTC compare) `< today(UTC)`, set `newDueDate = now + 2 days (UTC date)`. The `issueUpdate`
  mutation fires even when the state is already correct if a bump is pending (`alreadyInState &&
  newDueDate` still updates dueDate only). Response includes `dueBumpedTo`.
- Volume: 93 retained executions over ~2.2 days ≈ 41/day (baseline 51/day). 0 errors.

### 2.3 8stSpZUiyG7f2LQX — Calendar — Linear Add Comment (ACTIVE, unchanged since 2026-05-22)
- POST `/webhook/linear-add-comment`, body `{issue, body, author}` (author default "SyncView").
- Resolves identifier by regex from the URL, then `commentCreate`. **Comment body format
  (VERBATIM):** `**{author} (via SyncView):**\n\n{text}` (author/text escaped; \r stripped).
  This exact prefix is later parsed back out by Tweak Comments (§2.7). Hardcoded Linear key
  (redacted). Returns `{ok, identifier, commentUrl}`.

### 2.4 Nk3pwR6Fbl4VAPqH — Calendar — Linear Sub-Issues (ACTIVE, unchanged since 2026-05-19)
- POST `/webhook/linear-subissues`, body `{url}` → one GraphQL query → returns
  `{ok, parent:{identifier,title,status}, subIssues:[{id,identifier,title,url,status}]}`.
  Used by Import-from-Linear + link-time status sync. Hardcoded Linear key (redacted).

### 2.5 GP8CSZDNcy5sGdFr — Calendar — Linear Issue Statuses (ACTIVE, unchanged since 2026-06-24)
- POST `/webhook/linear-issue-statuses`, body `{issues:[links]}`; dedupes/caps at 100 ids.
- One aliased batched GraphQL query (fields: identifier, state.name, dueDate, assignee.id,
  project.name, parent.id/project.name). Poison-hardening (2026-06-24): if the batch response is
  nulled by one dead id, re-resolves each id individually IN PARALLEL; `ok:false` only on true
  network outage with zero results. Returns `{ok, statuses:{IDENT:stateName},
  meta:{IDENT:{state,isSubIssue,hasProject,hasDue,hasEditor}}, missing:[]}`.
- **Callers now measured: 1,722 executions in the last ~25h**, arriving in bursts of ~10 calls
  (~1s apart) every 10 minutes around the clock, starting ~15–35s after each
  `AkiFmromoDkmsh39` GitHub dispatch → INFERENCE: the GitHub Actions reconciler makes ~10–11
  batched calls per run (one per client); FE load-time calls are a small residual. 1 error in
  window 07-04T11:30 (60s duration → duration-consistent with a Linear timeout; message not fetched).

### 2.6 lGwC9WWPVJtxphtf — Workload — Reconcile (ACTIVE, unchanged since 2026-06-17)
- Schedule every 10 min → GET `https://synchrosocial.app.n8n.cloud/webhook/linear-issues` (the
  endpoint INSIDE VIDEO PRODUCTION AUTOMATION) with 60s timeout → maps `resp.issues[]` to
  `workload_issues` rows (all fields incl. parent_identifier, active:true, synced_at=runStart) →
  bulk POST Supabase REST `workload_issues` (`merge-duplicates`, supabaseApi cred) → PATCH
  `active=false` where `active=eq.true AND synced_at < runStart` (mark-sweep deactivation).
- Safety gates: read failure OR empty issues array → returns [] → NO writes, no mass-deactivate.
- Runtime per run 12–32s (measured). 144/day, 0 errors.

### 2.7 Reconcile triggers
- **AkiFmromoDkmsh39** (ACTIVE, unchanged since 06-19): every 10 min → POST GitHub API
  `repos/sidney-afk/client-analytics/actions/workflows/296618163/dispatches` with
  `{"ref":"main","inputs":{"dry_run":"false"}}`, cred "GitHub PAT — reconcile". Fires on the
  :00/:10/... +22s cadence (measured). This remains the global calendar-status safety net.
- **ZJOtYpQZj73DcBB1** (still INACTIVE, activeVersionId=null, triggerCount 0): identical pattern
  targeting `sample-linear-reconcile.yml`. Dormant, unchanged since 06-26.
- **qmDGbKnvrK0sPFKj** Samples — Linear Status Sync: still INACTIVE/superseded (logic embedded
  in MJbMZ branch 3).

### 2.8 d7Dod7OuQsVsl1CN — Workload — Tweak Comments (ACTIVE, unchanged since 2026-06-11)
- POST `/webhook/linear-tweak-comments`, body `{ids:[issue uuids]}` (validated, capped 60).
- **Key mechanism (now fully documented): it fetches the "Social Media Managers" tab of master
  sheet 10QQnWOQY73Aj44R8AumYJzFpxMd_bZZiCMXkZ6QqAU8 via the UNAUTHENTICATED public gviz CSV
  endpoint, extracts every value in the `linear_api_key` column that starts with `lin_api_`, and
  queries api.linear.app once per key (all SMM workspaces) in parallel.** Merges comments by issue
  id (first workspace wins), newest-first, max 10/issue, parses the `**Author (via SyncView):**`
  prefix back into `{author, viaSyncView, body}`; non-SyncView comments attributed to Linear
  user displayName/name or botActor. Returns `{ok, comments, requested, found, fetchedAt,
  workspaceErrors[{keyPrefix,error}]}` (leaks 14-char key prefixes in error payloads).

### 2.9 rhDX5VfnmOylc8o7 — Editors — Labor Week (ACTIVE, **updated 2026-06-29** — newest of the
  Linear bridges; post-dates every repo backup)
- POST `/webhook/editors-week`, optional `{weekStart, weekEnd}`, default = previous Mon–Sun week
  computed in **America/Chicago** with DST-correct midnight conversion.
- Pages Linear issues (hardcoded key, redacted) filtered `team.key = "VID"` and
  `updatedAt >= weekStart`, 50/page, ≤30 pages, with per-issue `history(first:250)` state
  transitions. Work model: a "delivery" = transition INTO a review state (name matching
  /smm/, /kasper|casper/, /client/) FROM a work state (in progress, todo/to do, backlog, tweak(s)
  needed); kind = tweak vs firstcut by from-state; de-bounced to ≤1 per (Chicago-day, kind) per
  issue. Issues also included if any in-window transition hit "in progress" (WIP visibility).
  Aggregates per editor (keyed by assignee email/id): totalEvents, distinctVideos,
  avgPerWorkday (=total/5), perDay, full transition timeline per video. GRA team NOT covered.

### 2.10 TJVMyfwl85qrFGeK — Urgent Tweak → Slack (ACTIVE, unchanged since 2026-06-04)
- POST `/webhook/send-urgent-slack` `{issue, client|name}`. Validates the URL against
  `https://linear.app/synchro-social/issue/<IDENT>` (org-pinned), sanitizes client string.
- Resolves the issue's assignee via Linear GraphQL using the **"LINEAR" httpHeaderAuth credential**
  (NOT hardcoded — the only Linear bridge using the credential store).
- Editor→Slack map: reads master-sheet tab **"Video Editors"** (columns `email`,
  `slack_user_id`) via Sheets OAuth; overlays onto a hardcoded 4-entry email→Slack-ID fallback
  map (4 editor gmail addresses — not reproduced here). Assignee email → Slack mention.
- Posts to **#video-editing `C09QTMZST5J`** as SyncView Bot, text exactly:
  `URGENT TWEAKS NEEDED\nClient: {client}\nBy when: ASAP\nLinear: {url}\n<@slackid>`.
  Responds `{ok, posted, mention, editor}`; 400 on invalid URL.

### 2.11 BrJSe8zCKUccfmIq — VIDEO PRODUCTION AUTOMATION (ACTIVE, versionId
  `89d575e6-01d8-4102-9e52-a9fc86f38efe`, unchanged since 2026-06-17 — same version as baseline)
87 nodes / 84 edges, 6 webhook triggers (all read from the live graph):

| Path | Method/mode | What it does |
|---|---|---|
| `/webhook/video-form` | POST, responds immediately | Batch video intake (below) |
| `/webhook/graphic-form` | POST, responds immediately | Batch graphics intake (below) |
| `/webhook/linear-projects` | POST, respond-node | `{data.team.projects}` for VID team → sorted project-name list (FE client dropdown) |
| `/webhook/add-to-calendar` | POST, responds immediately | Legacy per-issue calendar-sheet writer (below) |
| `/webhook/log-linear-submission` | POST, responds immediately | Appends {Timestamp, Client Name, Mode, Webhook JSON} to master-sheet "Linear Submissions" tab (sheet gid 926989078) |
| `/webhook/linear-issues` | GET, respond-node | THE workload cache source (below) |

**`/video-form` flow (measured node chain):** Webhook → Fetch Filming Plans (public gviz CSV of
master-sheet `FilmingPlans` tab) → Fetch SMM (public gviz CSV of `Social Media Managers` tab) →
`Lookup SMM Key`: matches client row → SMM per-client Linear API key + SMM name; maps SMM name →
hardcoded Slack-ID table (7 SMMs: analia, laura, ludmila, molly, raha, sebastian, sidney);
resolves the filming-plan URL from FilmingPlans by normalized client name; builds
`resolvedDescription` = `Filming Plan: <url>` + form notes (any manually-typed Filming Plan line
stripped) → Find Project: VID team (`cd12db10-751a-4cea-bed7-be7bbea1efa6`) project with name
exactly == clientName → Get Editors (master-sheet "Video Editors" tab, gid 607025335) →
Find Editor: Linear `users(filter:{email:{in:[editor emails]}})` each with
`assignedIssues(filter:{parent:{null:false}, state:{type:{nin:[completed,canceled]}}})` →
**Pick Freest Editor (EXACT RULE): sort users ascending by count of their OPEN SUB-ISSUES
(non-completed/canceled issues that have a parent); winner = fewest; tie → first in Linear's
returned order; no editors → unassigned.** → Create Parent Issue: `issueCreate` on VID team,
stateId `0db5ffa8-d0c8-4733-8481-57f9e07f76a2` (a fixed VID state — same id used for sub-issues),
title = form title, description = resolvedDescription, project = matched project, assignee =
freest editor → Slack DM to the SMM ("New Linear issue created for {client}: {IDENT} — {title} +
url") → per-video loop Create Sub-Issues: title `Video {n}`, description
`**Main Camera:** {main_cam}\n**Side Camera:** {side_cam}\n**Audio:** {audio}`, sortOrder = n,
dueDate = per-video form date (TimelessDate) or null, same state/project/assignee as parent.
Issue-creation HTTP nodes carry their own Authorization header (SMM key from the sheet per run —
expression-injected), Find-* nodes use the LINEAR credential.

**`/graphic-form` flow:** same CSV lookups → Download Filming Plan (Google Doc export?format=txt,
skips gracefully if no doc mapped) → Find GRA Project (team `4789fc53-4e9b-4599-aab8-4e22420931d7`,
exact name) → Create Parent Issue1: stateId `6ebdc29c-2f9f-4430-a906-fe0a8ff82e64`, **assignee
HARDCODED to a single designer id `091b6305-34fd-46ee-b84c-4aeee947c5b8` (no freest-editor logic
for graphics)** → Slack DM SMM → `Generate Titles`: calls api.anthropic.com (model
claude-sonnet-4-6) with the filming-plan text to generate 2–5-word titles per video —
**hardcoded Anthropic API key present (redacted)** — → Create Sub-Issues2: title `Video {n}`,
description = generated title, dueDate per video.

**AI-thumbnail chain — MEASURED FINDING: IT IS DISCONNECTED.** The 30-node chain
(`Aggregate Context` → Get Reference Samples → Create Output Folder (Drive folder
"{project} {date}" under folder 1FhmlHTZD0ADj7Eog…) → per-sub-issue loop → Drive bypass →
Replicate prediction (first-frame) → Gemini `gemini-3-pro-image-preview` generate + 3 reference
samples → `gemini-2.5-flash` verify → pick winner → Upload Thumbnail `Video-{n}.jpg` → Comment on
Parent Issue) exists in the active version, but **`Aggregate Context` has NO incoming connection
and `Create Sub-Issues2` has NO outgoing connection** — the only entry points in the whole
workflow are the 6 webhooks, so the thumbnail pipeline cannot run in production. Since the
version is unchanged since 06-17, this was also true at the 07-03 baseline (the baseline
description "generates AI thumbnails" overstates current behavior). Gemini/Replicate nodes use
credential store; Apify node has a hardcoded token in the URL (redacted).

**`/add-to-calendar` flow:** body {url} → GraphQL fetch issue + children + comments (hardcoded
Linear key, redacted) → for each sub-issue extract first f.io link + first drive.google.com link
from comments (rows filtered to those having either) → per item: VID→ Apify web-scraper
(hardcoded token in URL, redacted) scrapes the Frame.io page for the video → OpenAI Whisper
transcription (OPENAI cred) → Claude caption (hardcoded Anthropic key, redacted; model
claude-sonnet-4-6) → reads legacy content-calendar sheet `1XOyGrvSo52e-j3pTQ1CBKzcPFGln1iPHWiay78xO-VI`
tab = client name, row 2 → writes an 11-row column (date, FILMING PLAN, =IMAGE formula, caption,
links…) into the next free column; GRA→ skips scrape/caption and writes the graphics column only.

**`/linear-issues` (GET) handler:** fetches SMM CSV (public gviz), collects every
`linear_api_key`, then per key pages
`issues(first:250, orderBy:updatedAt, filter:{state:{type:{nin:[completed,canceled]}}})` up to 10
pages/workspace; normalizes to {id, identifier, title, url, isSubIssue, parentId,
parentIdentifier, dueDate, createdAt, updatedAt, status, statusType, teamKey, teamName,
assigneeId, assigneeName, assigneeEmail, clientName (project name or parent's project name)};
dedupes across workspaces; returns `{issues, fetchedAt, workspaceCount, totalRaw,
workspaceErrors, pagesPerWorkspace}`. Called every 10 min by Workload — Reconcile (144/day,
run time 7–30s measured).

### 2.12 TcWOfnKd4Csdnnbv — Kasper — Queue (ACTIVE, unchanged since 2026-06-10)
- 1 retained execution (2026-07-03T06:13, success). Repo backup kasper-queue.2026-06-12 = current.

### 2.13 jlVfbg0Njxf1It7h — SyncView — Weekly Backup (ACTIVE, unchanged since 2026-07-02)
- 2 retained executions: manual `191240` 07-03T21:28 (the Phase-0 export, matches EXECUTION_LOG)
  and **scheduled run 07-05T06:00:06Z, success, 23s** — the weekly backup fired this morning and
  succeeded. (Note: fires 06:00 UTC; the baseline's "Sun 02:00" is the configured local-time
  reading — schedule not re-pulled this pass.)

## 3. HUNT: the nightly ~23:00 UTC due-date bumper — NOT FOUND IN n8n (measured elimination)

- **The only n8n code path that writes `dueDate` on existing Linear issues is
  `linear-set-status`'s +2d overdue bump (§2.2).** Grep of every pulled workflow JSON confirms no
  other `issueUpdate`/dueDate mutation; sub-issue creation sets dueDate only at create time.
- **No scheduled n8n workflow runs nightly at ~23:00.** Full execution sweep of
  2026-07-04T22:30→23:59 (220 executions, all pages read): only TikTok Status Cron (every min),
  TikTok Token Refresh (23:00/23:30), the two 10-min reconciles + `BrJSe` `/linear-issues`
  reads, and `GP8CSZDNcy5sGdFr` read-only status batches. None writes dueDate.
- **`linear-set-status` had ZERO executions in that window** (its full retained history: bursts at
  07-03 10:35–20:40, 07-04 00:02–05:16, 07-04 17:21–19:21, 07-05 13:19–13:43 — no ~23:00 cluster
  on any retained night).
- CONCLUSION (measured for n8n, inference beyond): the nightly bump does NOT originate in n8n.
  Remaining candidates for the other auditors: (a) the repo-side GitHub Actions reconcile script
  writing Linear directly with its own key (it runs every 10 min; a date-boundary condition in its
  logic could make bumps cluster at a fixed hour), (b) a Linear-side automation/integration, (c)
  the QA/overnight harness driving app writes that hit set-status (would show in n8n — it does
  not, on retained nights). Cross-check Linear issue history "actor" on a bumped issue and
  `reconcile` script sources; the +2d-from-today signature (not "tomorrow") of §2.2 can be used
  as a fingerprint: n8n bumps land 2 days out, a "roll to tomorrow" bumper is a different actor.

## 4. Execution health (measured; retention now ≈2.5 days, floor between 07-03T03:00 and 07-03T06:13)

- **Total executions in the last ~25h (07-04T17:00→07-05T18:14): 4,052** — down ~84% from the
  baseline's 25,647/24h. Cause: the 07-03 QA filming-plan-tabs stub + end of overnight-QA runs.
- Per-workflow counts, same ~25h window (search_executions count, exact):
  | Workflow | runs | errors |
  |---|---|---|
  | Linear Issue Statuses (GP8CSZ) | 1,722 | 0 (1 on 07-04 11:30, 60s) |
  | TikTok Pilot Status Cron | ~1,513 (ESTIMATE: every minute) | 0 |
  | Reconcile trio (AkiF + lGwC9 + BrJSe/linear-issues) | ~151 each ≈ 453 (measured cadence) | 0 |
  | Sample Review — Upsert | 69 | **0** |
  | Filming Plan Tabs (5S4Jy) | **31** (was 12,725) | 1 |
  | Caption Prompts — Get | 30 | 0 |
  | Calendar — Upsert Post | 25 | 0 |
  | Templates — Get | 21 (was 620) | 0 |
  | Linear Set Status | ~28 in window (93 retained ≈ 41/day) | 0 |
  | Linear Status Sync (MJbMZ) | ~20 in window (51 retained ≈ 27/day) | 0 |
- **Errors, full 07-04T00:00→07-05T18:20 span: 13 total** (exact list): 6× Calendar Upsert
  (07-04 00:26–00:56 — coincides exactly with the A1 EF canary QA window in EXECUTION_LOG),
  1× linear-issue-statuses (60s, 07-04 11:30), 1× calendar-get (07-04 05:24), 1× sample-review-get
  (07-04 17:49), 1× CLIENTS METRICS (07-05 04:00 scheduled run, failed after 21m26s),
  1× filming-plan-tabs (07-05 07:31), 1× ttp-auth-init (07-05 08:06), 1× samples-get (07-05 16:59).
- **The sample-review-upsert error burst is GONE** (815 errors in baseline window → 0 since 07-04).
- Error messages were not fetched (execution data not pulled to keep this audit read-light);
  durations suggest: 60s = Linear timeout; CLIENTS METRICS = mid-run failure of the 04:00 job —
  worth an owner glance since it also has no errorWorkflow alerting.
- Error-alerting gap CONFIRMED unchanged: none of the workflows pulled this pass has
  `errorWorkflow` in settings; "Error Alerts → DM Sidney" remains un-wired.

## 5. Slack senders relevant to Track B (current state)

| Sender | Workflow | Channel/target | Status |
|---|---|---|---|
| Urgent tweak alert | TJVMyfwl85qrFGeK | #video-editing `C09QTMZST5J`, @-mentions editor via Video Editors tab map + 4-entry hardcoded fallback | ACTIVE, unchanged |
| Batch intake notify | BrJSe8zCKUccfmIq (video+graphic forms) | Slack DM to the client's SMM (hardcoded 7-name→Slack-ID map) | ACTIVE, unchanged |
| Weekly top reel (PROD) | BTxic5NSaCMtZMh6 | client channels (baseline description; not re-pulled) | ACTIVE |
| Weekly top reel (TEST twin) | ukLGHr6uDJIEP1pM | test channel `C0B7D49KCD6` (baseline) | still ACTIVE |
| Content Ready Notify | II3sJbSLrptmtWLR | email (Clients Info lookup), not Slack (baseline) | ACTIVE |
| Onboarding/fallback/error alerts | ljNY7/hxLFI/u4ACO/itqDX | DM Sidney `U0ACW93FS30` | ACTIVE |

## 6. Security posture (deltas + confirmations; values redacted, counts only)

- Hardcoded Linear API key (the same house key): CONFIRMED present in Code nodes of **6**
  workflows this pass — MJbMZ (both branches), VQqqeY, 8stSpZ, Nk3pw, GP8CSZ, rhDX5 — plus
  BrJSe's add-to-calendar parser. This closes the baseline's "likely extends to
  add-comment/issue-statuses/editors-week" unknown: YES for all three (tweak-comments instead
  uses sheet-sourced keys; urgent-slack uses the LINEAR credential).
- **NEW finding: hardcoded Anthropic API key in 2 Code nodes of VIDEO PRODUCTION AUTOMATION**
  (Generate Titles, Generate Caption & Build Row1), bypassing the stored "Claude" credential.
- **NEW severity framing: per-SMM Linear API keys live in the "Social Media Managers" sheet tab
  and are fetched via the PUBLIC unauthenticated gviz CSV endpoint** by 3 code paths (VPA
  linear-issues, VPA form intakes, Tweak Comments). Anyone with the sheet's gviz URL can read
  every SMM workspace key. Also `workspaceErrors` responses leak 14-char key prefixes.
- Supabase publishable key (client-safe by design) still hardcoded in MJbMZ branches; Apify token
  still hardcoded in VPA URLs (redacted). All SyncView webhooks remain unauthenticated, CORS *.
- Track-A flag reads from n8n use the publishable key and fail-safe to the n8n path — a Supabase
  flag-table outage silently reverts EF-flagged clients to n8n (correct per rollback doctrine).

## DIFFS vs 2026-07-03 snapshot

**Changed (measured):**
1. `MJbMZ789B5ExZz9x` INACTIVE → **ACTIVE** (activeVersion `405ab03a…`), now carrying A1
   (`calendar_upsert_ef_clients`) and A2 (`sample_review_ef_clients`) runtime-flag routing with
   n8n-fallback fail-safes. Active count 74 → 75. Linear→app realtime push is ON again.
2. Execution volume collapsed 25,647/24h → 4,052/25h. Filming Plan Tabs 12,725 → 31/day
   (QA stub worked); Templates Get 620→21; Caption Prompts 218→30; Sample Review Upsert
   6,456→69. This retroactively proves most baseline "hot" traffic was QA-harness, not users.
3. Sample-review-upsert error burst (815) ENDED — 0 errors since 07-04.
4. Execution retention window stretched ~24h → ~2.5 days (volume-driven pruning, INFERENCE).
5. Errors now 13/1.8 days vs 819/24h; new one-off errors on CLIENTS METRICS (07-05 04:00 run
   failed after 21 min) and single get-endpoints.
6. Weekly Backup has now run on schedule (07-05 06:00 UTC, success) in addition to the manual
   07-03 Phase-0 export.
7. Baseline-description correction: VPA's AI-thumbnail chain is DISCONNECTED (no trigger reaches
   it) in the active version — it was already so at baseline (version unchanged since 06-17), the
   baseline text overstated it as live behavior.

**Unchanged-and-confirmed (measured version ids / updatedAt):**
- VQqqeY (f3ec2123, 05-27), 8stSpZ (05-22), Nk3pw (05-19), GP8CSZ (2203cdde, 06-24 poison-fix),
  lGwC9 (50a8a1e4, 06-17), AkiF (2c8445c3, 06-19), ZJOt inactive (06-26), qmDGb inactive,
  d7Dod (06-11), TJVMy (06-04), BrJSe (89d575e6, 06-17), TcWO (06-10), jlVf (07-02),
  rhDX5 (06-29 — pre-baseline, but note it's newer than any repo artifact).
- 87 workflows, no new ones; 33 credentials; 3 data tables; single project; no tags; error-alert
  workflow still un-wired; webhooks still unauthenticated CORS *.
- GH reconcile dispatch target still actions workflow id 296618163, `dry_run:false`, ref main.

## TRACK B IMPLICATIONS

1. **§13/§14 nightly due-date bumper: n8n is now ELIMINATED as the source.** The only n8n dueDate
   writer is set-status's "+2 days from today when overdue" (not "roll to tomorrow"), fired only
   on app-driven status pushes — and it did not run at ~23:00 on any retained night. The spec's
   open decision should be re-scoped: identify the actor via Linear issue history / the repo
   reconcile scripts; if it is the GH reconciler, B5's "retire reconcile scripts" automatically
   retires the bumper; the "do not replicate" recommendation stands.
2. **The Linear webhook contract Track B must replace is precisely mapped** (MJbMZ §2.1):
   fire-and-forget 200, three independent consumers (calendar status, samples status, workload
   mirror), state-name→app-status mapping tables (calendar vs samples differ on
   Scheduled/Posted), freshness + archived + Posted guards, canonical-row pick. The new
   `deliverable_events`-based flow must reproduce the guard semantics or consciously drop them.
3. **The A1/A2 flag routing lives INSIDE MJbMZ**: any Track B change that deactivates MJbMZ before
   the EF cutover is complete would sever the flagged-client EF path for Linear-driven updates.
   Sequencing per §13 (webhook retired only at teardown) is correct; add an explicit note that the
   flag plumbing dies with it.
4. **Set-status mapping table (§2.2) is the authoritative outbound mapping** for the B-phase
   one-way mirror. Note its two exact-name matches (`Todo`/`To Do`, `Approved`) vs
   substring-matches for the rest, org-team-scoped; and that the FE treats no-match as success
   (no retry). Also: EVERY outbound push heals overdue dates (+2d) as a side effect — the mirror
   replacement must decide to keep or drop this (recommend drop, consistent with §14).
5. **Pick Freest Editor exact rule captured** (§2.11): fewest open sub-issues (parent≠null,
   state.type ∉ {completed,canceled}) among Video Editors-tab emails, resolved per SMM workspace
   key; ties = Linear API order; graphics team has NO auto-assign (fixed designer id). §14's
   "port exactly vs refine" now has ground truth. Note the editor pool source is a Sheets tab —
   Track B's `editors` table must own this.
6. **AI thumbnails need a product decision, not a port**: the chain is currently dead code —
   Track B should NOT budget for replicating it as live behavior; confirm with the owner whether
   it is meant to come back (it was designed: Gemini 3-pro-image-preview + 2.5-flash verify +
   Replicate first-frame + Drive upload + parent-issue comment).
7. **Workload cache replacement scope confirmed**: `/webhook/linear-issues` (multi-workspace,
   active-only, 250/page×10) + MJbMZ webhook mirror + 10-min mark-sweep reconcile with
   empty-read guard. The mark-sweep semantics (`synced_at < runStart` → active=false, never
   delete) is the pattern the Supabase-native replacement must keep.
8. **Real production write volume is tiny** (25 calendar upserts, 69 sample upserts, ~41
   set-status, ~27 inbound Linear events per day): Edge-Function capacity is a non-issue;
   parity testing matters far more than throughput.
9. **Editors — Labor Week was edited 06-29 and exists in NO repo backup** — per ROLLBACK rule 2,
   export it before Track B touches labor reporting. Same for every Linear bridge except the
   reconcile pair (see §below).
10. **Comment round-trip contract**: add-comment writes `**{author} (via SyncView):**\n\n{body}`
   and tweak-comments parses exactly that regex — the Track B comment model must preserve or
   migrate this marker for history continuity.
11. **Secrets cleanup at teardown**: retiring Linear must include rotating the house key
   (hardcoded in 6+ workflows), the SMM keys (public-CSV-exposed), and the hardcoded Anthropic
   key — B5 checklist items.

**Classification (RETIRED-BY-TRACK-B / DEPENDED-ON-DURING-TRANSITION / UNTOUCHED) + repo backup:**

| Workflow | Class | Repo backup in n8n-backups/ |
|---|---|---|
| MJbMZ Linear Status Sync | DEPENDED-ON (carries A1/A2 flags + realtime sync) → retired at B5 step 4 | STALE (06-26); current export private-only + public-safe A2 note |
| VQqqeY Set Status | DEPENDED-ON (outbound mirror) → RETIRED B5 | **NONE** |
| 8stSpZ Add Comment | DEPENDED-ON (mirror) → RETIRED B5 | **NONE** |
| Nk3pw Sub-Issues | RETIRED (import affordance goes inert) | **NONE** |
| GP8CSZ Issue Statuses | DEPENDED-ON (reconciler + FE load) → RETIRED B5 | partial (06-12/06-14 JSON stale; 06-24 .js snippets near-current) |
| lGwC9 Workload Reconcile | DEPENDED-ON → RETIRED B5 (named in §13) | CURRENT (06-17) |
| AkiF Reconcile Trigger | DEPENDED-ON — must stay until B5 (ROLLBACK Live State) | CURRENT (06-19) |
| ZJOt Samples Reconcile Trigger | inactive; RETIRED B5 (named in §13) | CURRENT (06-26) |
| qmDGb Samples Status Sync | inactive/superseded; delete-at-cleanup candidate | CURRENT (06-26) |
| d7Dod Tweak Comments | RETIRED B5 (named in §13) | **NONE** |
| rhDX5 Editors Labor Week | RETIRED B5 (→ deliverable_events query) | **NONE, and live version (06-29) post-dates baseline coverage** |
| TJVMy Urgent Tweak Slack | REPLACED by Track B urgent flow (Linear-resolution parts die; Slack notify re-implemented) | **NONE** |
| BrJSe VIDEO PRODUCTION AUTOMATION | Linear branches RETIRED (intake + /linear-issues); non-Linear branches (add-to-calendar legacy sheet writer, log-linear-submission) need explicit disposition in the spec | **NONE** |
| TcWO Kasper Queue | UNTOUCHED by Track B (Sheets-read only) | CURRENT (06-12) |
| jlVf Weekly Backup | UNTOUCHED / load-bearing for rollback discipline | 07-02 pre-onboarding (near-current) |

## UNKNOWNS

1. **Who bumps the due dates at ~23:00 UTC** — n8n eliminated (measured); the actor is outside my
   domain (GH Actions reconcile script, Linear-side, or other). Needs Linear issue-history actor
   check + repo script read by the other auditors.
2. Error MESSAGES for the 13 recent errors (execution data deliberately not fetched; only
   timing/duration read). In particular why CLIENTS METRICS failed at 07-05 04:21 after 21 min.
3. Whether the GP8CSZ 10-per-10-min bursts are exactly the GH reconciler iterating clients —
   INFERENCE from cadence correlation with AkiF dispatches; confirmed only by reading the Actions
   run logs (repo domain).
4. Weekly Slack pair, Content Ready Notify, and the rest of the ~35 peripheral workflows were NOT
   re-pulled at node level this pass (unchanged updatedAt where checked; Slack channel claims for
   the weekly pair carried from baseline).
5. BACKUPS workflow (vb3O0wkTK6Q7Rtro) remains MCP-inaccessible ("availableInMCP" disabled),
   inactive, contents unknown (same as baseline).
6. Exact configured schedule times (cron vs local TZ) for CLIENTS METRICS' two triggers and
   Weekly Backup were not re-pulled; observed fire times: 04:00 UTC (METRICS, errored 07-05) and
   06:00 UTC (Backup, success 07-05).
7. True 30-day history remains unobservable (retention ≈2.5 days now); baseline's June incident
   history is still unqueryable.
8. `Get Editors` reads master-sheet gid 607025335 (assumed = "Video Editors" tab per name-based
   read elsewhere; gid→tab-name mapping is Sheets-domain to confirm).
