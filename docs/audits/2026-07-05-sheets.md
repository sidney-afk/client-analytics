# Google Sheets Re-Audit (roster + editor feeds) — 2026-07-05

Auditor domain: Google Sheets (master workbook `10QQnWOQY73Aj44R8AumYJzFpxMd_bZZiCMXkZ6QqAU8` + SyncView Calendar workbook `1Gsn5xLImJyMhBMCNjK_tigpoUfcSFnvxTQLkk-A9Yps`).
Method: unauthenticated `curl` against the same public gviz CSV/JSON endpoints the app uses (READ-ONLY), plus `/htmlview` for tab enumeration; code cross-referenced by symbol in the CURRENT `/home/user/client-analytics/index.html`. One read-only n8n `get_workflow_details` call to ground the urgent-Slack ↔ Video Editors linkage. Everything below is MEASURED on 2026-07-05 unless explicitly labeled ESTIMATE/INFERENCE.

---

## Findings

### 0. Access posture (measured)

- Both workbooks are readable **unauthenticated**: gviz CSV/JSON returned data with plain `curl` (no cookies, no API key), and `/htmlview` rendered for both. This is by design (the app is a static page that fetches gviz CSV client-side, `CLIENTS_URL` at index.html:4595), but it means **everything in every tab of both workbooks is public**.
- Master workbook has **17 tabs** (full enumeration via htmlview): Clients Info, Competitors, Monthly Checkup, Metrics, PostTracking, TopVideos, ContentSummaries, Competitor Briefs, Market Research Briefs, Social Media Managers, CaptionPrompts, Video Editors, Templates, Hook Library, Linear Submissions, TikTokUpload, FilmingPlans.
- index.html fetches 8 of them via gviz (consts at index.html:4593–4605 and `KASPER_SMM_URL` at index.html:32012): Metrics, Clients Info, TopVideos, Competitor Briefs, Market Research Briefs, ContentSummaries, FilmingPlans, Social Media Managers. The other 9 tabs are n8n/Apps-Script-facing or legacy.

### a) Clients Info tab

**Header row (measured, exact, in order — 12 columns, gviz confirms no column M exists):**
`client_name, email, competitors, keywords, specific_keywords, content_description, instagram_handle, tiktok_handle, youtube_channel_id, slack_channel_id, upload_post_profile, postforme_account_id`

- **`client_review_token` is NOT a column on the live sheet.** Verified three ways: CSV header, gviz JSON `select *` returns exactly 12 cols (A–L), and `tq=select M` errors `NO_COLUMN: M`. Consequence below in (f).
- **No status/active/former column exists.** Activeness is purely "row present = live" (code comment at index.html:9010–9015 and merge at `wlMergeClientsFromSheet` index.html:9077; rebuild-not-accumulate at `_applyEssentialTexts` index.html:5272–5273).
- **Row count: 29 data rows** (header + 29; gviz JSON showed 30 rows because it treated the header as data).

**The 29 client display names and their normalized slugs** (slug computed per `wlNormalizeClient`, index.html:9001–9009 — see (d) for the exact algorithm; verified it is more than "lowercase + strip non-alnum"):

| # | client_name (sheet) | slug |
|---|---|---|
| 1 | Baya Voce | bayavoce |
| 2 | Jessica Winterstern | jessicawinterstern |
| 3 | Morgan Burton | morganburton |
| 4 | Dr. Sonia Chopra | soniachopra |
| 5 | Doug Cartwright | dougcartwright |
| 6 | Dr. Rocco Piazza | roccopiazza |
| 7 | Edward Mannix | edwardmannix |
| 8 | Alli Schaper | allischaper |
| 9 | Eben & Annie | eben&annie |
| 10 | Miki Agrawal | mikiagrawal |
| 11 | Erica Matluck | ericamatluck |
| 12 | Natalie MacNeil | nataliemacneil |
| 13 | John Wineland | johnwineland |
| 14 | David Kessler | davidkessler |
| 15 | Chelsey Scaffidi | chelseyscaffidi |
| 16 | Danielle Robin | daniellerobin |
| 17 | Morgan Burch | morganburch |
| 18 | Jesse Israel | jesseisrael |
| 19 | Mastin Kipp | mastinkipp |
| 20 | Melissa Pruett | melissapruett |
| 21 | Adriana Rizzolo | adrianarizzolo |
| 22 | Lisa Kleyn | lisakleyn |
| 23 | Lauren Taus | laurentaus |
| 24 | Henry Ammar | henryammar |
| 25 | Lily Baker | lilybaker |
| 26 | Terrin Ammar | terrinammar |
| 27 | Jenna Phillips Ballard | jennaphillipsballard |
| 28 | Alayna Bellquist | alaynabellquist |
| 29 | Amanda Hanson | amandahanson |

Note the slug charset **includes `&`** ("Eben & Annie" → `eben&annie`) — relevant to any Track B slug/URL scheme.

**Per-client field PRESENCE (values not reproduced except non-secret handles' presence):**

| client | email | slack_channel_id | instagram | tiktok | youtube | postforme_account_id | upload_post_profile |
|---|---|---|---|---|---|---|---|
| Baya Voce | Y | Y | Y | Y | Y | – | – |
| Jessica Winterstern | Y | Y | Y | Y | Y | – | – |
| Morgan Burton | Y | Y | Y | Y | Y | – | – |
| Dr. Sonia Chopra | Y | Y | Y | Y | Y | – | – |
| Doug Cartwright | Y | Y | Y | Y | – | – | – |
| Dr. Rocco Piazza | Y | Y | Y | Y | Y | – | – |
| Edward Mannix | Y | Y | Y | Y | Y | – | – |
| Alli Schaper | Y | Y | Y | – | Y | – | – |
| Eben & Annie | Y | Y | Y | – | – | – | – |
| Miki Agrawal | Y | Y | Y | Y | Y | – | – |
| Erica Matluck | Y | Y | Y | – | – | – | – |
| Natalie MacNeil | Y | Y | Y | – | Y | – | – |
| John Wineland | Y | Y | Y | Y | Y | – | – |
| David Kessler | Y | Y | Y | Y | Y | – | – |
| Chelsey Scaffidi | Y | Y | Y | Y | – | Y | Y |
| Danielle Robin | Y | Y | Y | Y | – | Y | Y |
| Morgan Burch | Y | Y | Y | Y | Y | – | – |
| Jesse Israel | Y | Y | Y | – | – | – | – |
| Mastin Kipp | Y | Y | Y | – | – | – | – |
| Melissa Pruett | Y | Y | Y | Y | – | – | – |
| Adriana Rizzolo | Y | Y | Y | – | – | – | – |
| Lisa Kleyn | Y | Y | Y | – | – | – | – |
| Lauren Taus | – | – | Y | – | – | – | – |
| Henry Ammar | – | Y | Y | Y | Y | – | – |
| Lily Baker | – | Y | Y | Y | – | – | – |
| Terrin Ammar | – | Y | Y | Y | – | – | – |
| Jenna Phillips Ballard | – | Y | – | – | Y | – | – |
| Alayna Bellquist | – | – | Y | Y | – | – | – |
| Amanda Hanson | – | – | Y | Y | Y | – | – |

Aggregates: email 22/29; slack_channel_id 26/29 (missing: Lauren Taus, Alayna Bellquist, Amanda Hanson); instagram 28/29 (missing Jenna Phillips Ballard); tiktok 19/29; youtube 15/29; postforme_account_id 2/29 (Chelsey Scaffidi, Danielle Robin — same two for upload_post_profile); **client_review_token 0/29 (column absent)**.

**Filming-plan doc presence:** Clients Info has NO filming-plan column. Filming plans live on the separate **FilmingPlans** tab (fetched at `FILMING_PLANS_URL` index.html:4605). Live FilmingPlans headers: `client_name, doc_url` only (the code comment at index.html:4601–4604 also allows optional `notes`/`plan_months`; those columns don't exist live — non-breaking, code degrades gracefully). **30/30 rows have a docs.google.com doc_url** = all 29 Clients Info clients + **Sidney Laruel** (TEST).

**Former/inactive-looking clients (INFERENCE, method = absence from the "source of truth for who's live" sheet while present elsewhere):** Alyssa Nobriga, Jordan Marks (both still have SMM rows, Calendar/Samples mirror tabs, and 07-03 Linear "In Progress" projects), Kasper Hytonen (internal brand), Sidney Laruel (TEST client — deliberately seed-only). Partially-onboarded-looking (rows present but no email and/or Slack): Lauren Taus (no email, no slack_channel_id despite being a long-standing seed client), Alayna Bellquist, Amanda Hanson, Jenna Phillips Ballard. Morgan Burch is live on the sheet but her Linear project was "Completed" per the 07-03 Linear audit — a cross-source inconsistency to reconcile.

### b) Video Editors tab

**Measured: exactly 2 columns, 4 data rows.**

| video_editor | email |
|---|---|
| Iara | iaramiraille@gmail.com |
| Martin | martinsynchro@gmail.com |
| Nahuel Espindola | espindola.nahuel@gmail.com |
| Santi Gimelli | santigimelli@gmail.com |

- **There is NO Slack column** (`slack_user_id` or otherwise) and no Linear-user-id column on the live tab. gviz JSON confirms 2 cols.
- The **send-urgent-slack** n8n workflow (TJVMyfwl85qrFGeK, verified read-only 2026-07-05) reads this tab and *tries* to build an email→Slack map from a `slack_user_id` column — which doesn't exist — so with today's sheet the map comes **entirely from a hardcoded FALLBACK object inside the workflow's Code node**: iaramiraille@gmail.com→U0ABN1WQ2V8, martinsynchro@gmail.com→U09L10ZG5SS, espindola.nahuel@gmail.com→U0AGCQACEH4, santigimelli@gmail.com→U0ABFL52VTM (Slack member IDs, not secrets). The sheet is effectively decorative for Slack resolution; it only *could* override the fallback if someone adds the column.
- The front-end does NOT fetch this tab at all (no gviz URL for it in index.html). The in-app editor roster is hardcoded: `WL_VIDEO_EDITORS` (index.html:8973–8978) with Linear user UUIDs — Iara Miraille `b199cff7-…`, Martin `6b70f1d8-…`, Nahuel Espindola `e19eb17e-…`, Santi Gimelli `2cea7628-…` — plus allowlists `WL_ALLOWED_EDITORS` (index.html:8956) / `WL_ALLOWED_GRAPHICS` (index.html:8964, Rocío Perez only) keyed by `wlNormalizeEditor` (index.html:8979).
- **Team_members backfill implication:** name/email come from this tab; Slack IDs must come from the n8n fallback map (or Slack lookup-by-email); Linear user IDs from `WL_VIDEO_EDITORS`; the only designer (Rocío Perez) exists ONLY in the hardcoded allowlist — she has no row in any sheet.

### c) Social Media Managers tab

**Measured: 4 columns, 34 data rows.** Headers: `client_name, social_media_manager, linear_api_key, slack_profile_url`.

- **CRITICAL: the `linear_api_key` column contains what appear to be live per-SMM Linear personal API keys, publicly readable** (this whole tab is fetched client-side by every browser via `KASPER_SMM_URL`, index.html:32012, and by anyone with the URL). **7 distinct key values are present across 34 rows — values redacted.** 6 carry the standard `lin_api_` prefix; **1 (Analia's) has an anomalous truncated prefix (`in_api_…`)** and may be broken or a paste error. The front-end never reads this column (`_kasperLoadSMMMap`, index.html:33750–33776, reads only `social_media_manager`, `slack_profile_url`, optional `slack_user_id`/`slack_team_id`) — INFERENCE (method: column name + n8n audit context): it feeds n8n's per-SMM Linear writes. This is a live secret-exposure incident regardless of Track B.
- `slack_profile_url` holds plain Slack member IDs (not URLs) for all 34 rows.
- **SMM → clients (7 SMMs):**
  - **Analia** (U08SLQ4GT39): Jessica Winterstern, Terrin Ammar, Morgan Burton, Dr. Sonia Chopra, Alyssa Nobriga, Alayna Bellquist (6)
  - **Sebastian** (U09KMKAK4UX): Doug Cartwright, Dr. Rocco Piazza, Edward Mannix, Alli Schaper, Natalie MacNeil (5)
  - **Ludmila** (U0AJKSL3JHW): Mastin Kipp, Eben and Annie, Kasper Hytonen, Baya Voce, Jenna Phillips Ballard (5)
  - **Molly** (U07E30JN6KD): Jessica Encell Coleman, Jordan Marks, Adriana Rizzolo (3)
  - **Laura** (U0A73TB2ZEF): John Wineland, Melissa Pruett, David Kessler, Lily Baker, Dr Erica Matluck, Miki-agrawal, Amanda Hanson (7)
  - **Sidney** (U0ACW93FS30): Sidney Laruel, Chelsey Scaffidi, Danielle Robin (3)
  - **Raha** (U0APYPKTLJH): Morgan Burch, Jesse Israel, Lauren Taus, Lisa Kleyn, Henry Ammar (5)
- Name-variant rows ("Eben and Annie", "Dr Erica Matluck", "Miki-agrawal") all collapse to the canonical slugs via `wlNormalizeClient` — verified against the algorithm; coverage is **29/29 Clients Info clients** plus 5 extra rows: Alyssa Nobriga, Jordan Marks, Kasper Hytonen, Sidney Laruel, **Jessica Encell Coleman** (the last exists in Linear backlog but in NO other roster source — she is invisible to the app since she's neither in the seed nor Clients Info).

### d) Roster 3-way diff (hardcoded seed vs Clients Info vs Linear projects)

**Normalization ground truth** — `wlNormalizeClient` (index.html:9001–9009) is: lowercase, trim, strip accents (NFD), strip leading `dr./dr `, collapse ` and `/` & ` to `&`, then strip everything not `[a-z0-9&]`. (The task brief's "lowercase, strip non-alphanumerics" is incomplete — the `dr`, `and→&`, accent rules are what make "Dr Erica Matluck"/"Miki-agrawal"/"Eben and Annie" match.)

**Hardcoded roster** — `WL_CLIENT_NAMES` (index.html:9021–9052), 30 names, explicitly "SEED + OFFLINE FALLBACK ONLY"; `getClientRoster()` (index.html:9068) = seed ∪ sheet via `wlMergeClientsFromSheet` (index.html:9077, additive-only, never removes). Seed = the 26 sheet names above (rows 1–26) **plus** Alyssa Nobriga, Jordan Marks, Sidney Laruel, Kasper Hytonen; seed does **NOT** contain Jenna Phillips Ballard, Alayna Bellquist, Amanda Hanson.

**Reconciliation table** (Linear column = 07-03 linear.md §3 project state; Cal tabs = live 07-05 Calendar workbook):

| slug | canonical name | seed | Clients Info | SMM tab | FilmingPlans | Cal/Samples tabs | Linear (07-03) |
|---|---|---|---|---|---|---|---|
| bayavoce | Baya Voce | Y | Y | Y | Y | Y | In Progress (×2 + ALE dup) |
| jessicawinterstern | Jessica Winterstern | Y | Y | Y | Y | Y | Backlog |
| morganburton | Morgan Burton | Y | Y | Y | Y | Y | Backlog |
| soniachopra | Dr. Sonia Chopra | Y | Y | Y | Y | Y | In Progress |
| dougcartwright | Doug Cartwright | Y | Y | Y | Y | Y | Backlog |
| roccopiazza | Dr. Rocco Piazza | Y | Y | Y | Y | Y | Backlog |
| edwardmannix | Edward Mannix | Y | Y | Y | Y | Y | Backlog |
| allischaper | Alli Schaper | Y | Y | Y | Y | Y | Backlog |
| eben&annie | Eben & Annie | Y | Y | Y | Y | Y | In Progress |
| mikiagrawal | Miki Agrawal | Y | Y | Y | Y | Y | Backlog (×2) |
| ericamatluck | Erica Matluck | Y | Y | Y | Y | Y | In Progress |
| nataliemacneil | Natalie MacNeil | Y | Y | Y | Y | Y | In Progress |
| johnwineland | John Wineland | Y | Y | Y | Y | Y | In Progress |
| davidkessler | David Kessler | Y | Y | Y | Y | Y | Backlog |
| chelseyscaffidi | Chelsey Scaffidi | Y | Y | Y | Y | Y | In Progress |
| daniellerobin | Danielle Robin | Y | Y | Y | Y | Y | In Progress |
| morganburch | Morgan Burch | Y | Y | Y | Y | Y | **Completed** |
| jesseisrael | Jesse Israel | Y | Y | Y | Y | Y | In Progress |
| mastinkipp | Mastin Kipp | Y | Y | Y | Y | Y | Backlog |
| melissapruett | Melissa Pruett | Y | Y | Y | Y | Y | Backlog |
| adrianarizzolo | Adriana Rizzolo | Y | Y | Y | Y | Y | Backlog |
| lisakleyn | Lisa Kleyn | Y | Y | Y | Y | Y | In Progress |
| laurentaus | Lauren Taus | Y | Y | Y | Y | Y | In Progress |
| henryammar | Henry Ammar | Y | Y | Y | Y | Y | In Progress |
| lilybaker | Lily Baker | Y | Y | Y | Y | Y | Backlog |
| terrinammar | Terrin Ammar | Y | Y | Y | Y | Y (+dup `terrinamar`) | Backlog |
| jennaphillipsballard | Jenna Phillips Ballard | **–** | Y | Y | Y | **–** | In Progress |
| alaynabellquist | Alayna Bellquist | **–** | Y | Y | Y | **–** | Backlog |
| amandahanson | Amanda Hanson | **–** | Y | Y | Y | **–** | In Progress (created 07-02) |
| alyssanobriga | Alyssa Nobriga | Y | **–** | Y | – | Y | In Progress (+×2 legacy) |
| jordanmarks | Jordan Marks | Y | **–** | Y | – | Y | In Progress |
| sidneylaruel | Sidney Laruel (TEST) | Y | **–** | Y | Y | Y | In Progress (TEST) |
| kasperhytonen | Kasper Hytonen (internal) | Y | **–** | Y | – | Y | Backlog (×2) |
| jessicaencellcoleman | Jessica Encell Coleman | **–** | **–** | Y | – | **–** | Backlog |

**Linear-only projects with no sheet/seed/app presence at all** (from 07-03 §3; not re-verified live — Linear is another auditor's domain): Swati Garg, Jess Zweig, Lucas Salame, Kelp Skincare|Macky, Nick Bruss, Nathan Hirschaut, Amber Rae (×2), Dr. Scott Lyons (×2), Mikkel Clausen (×2), Art of Love (×3), Jordan Bowditch, Bright Harbor, Seth Blaustein, Simone, ONEWORLDEDUCATION, Bridget Videos, Client Example, Test Project, legacy In-Progress noise (Mike, Ohai ×2, Tracy Litt ×2, Avanti, PERSONAL), and the 17 Completed legacy projects.

**Key asymmetries:**
1. 26 clients agree across all three sources (25 active-state + Morgan Burch whose Linear project is Completed).
2. 3 clients (Jenna Phillips Ballard, Alayna Bellquist, Amanda Hanson) exist ONLY via the sheet-merge path at runtime — they are not in the seed, and **not in the Calendar workbook mirror**. If the sheet fetch fails, they vanish from the app.
3. 4 seed entries (Alyssa Nobriga, Jordan Marks, Sidney Laruel, Kasper Hytonen) are not on the sheet but are resurrected into `getClientRoster()` by the seed (the additive merge never removes) — so the app's "roster" is actually 33 clients, not 29. Two of them (Nobriga, Marks) look like former clients whose Linear projects were never closed.
4. Jessica Encell Coleman: SMM-assigned + Linear Backlog, yet in no roster source — either a not-yet-onboarded or off-books client.

### e) SyncView Calendar workbook (`1Gsn5xLImJyMhBMCNjK_tigpoUfcSFnvxTQLkk-A9Yps`)

**Tab enumeration succeeded read-only** via `/htmlview` (no credentials): **exactly 63 tabs** —
- 31 `Calendar_<slug>` tabs + `Calendar_Config` (32 total)
- 31 `Samples_<slug>` tabs
- The 31 slugs = the 30 seed slugs (all present, incl. `sidneylaruel`, `alyssanobriga`, `jordanmarks`, `kasperhytonen`) **plus the stale duplicate `terrinamar`** — i.e. BOTH `Calendar_terrinamar`/`Samples_terrinamar` AND `Calendar_terrinammar`/`Samples_terrinammar` exist. `wlNormalizeClient('Terrin Ammar')` = `terrinammar`, so the `terrinamar` pair (from a "Terrina Mar"-style spelling) is the orphan; 07-03's duplicate-variant flag is **confirmed still unresolved** (4 tabs involved).
- **No tabs for the 3 newest clients** (jennaphillipsballard, alaynabellquist, amandahanson): the mirror workbook is frozen at the seed-era roster. Consistent with the code comment (index.html:9016–9020) that tab provisioning is no longer required since Supabase became authoritative — but it means **the "Sheet mirror" for new clients doesn't exist**; whether n8n mirror writes for those clients fail silently is for the n8n auditor (the 07-03 n8n audit says mirror steps are onError:continueRegularOutput, so likely silent no-ops/failures).
- 07-03 said "~61 tabs"; today it is exactly 63. Cannot determine from read-only access whether 2 tabs were added since 07-03 or the old count was approximate (no revision history access).

### f) Clients Info columns vs what index.html expects

Parsing: `fetchEssentials()` (index.html:5321) → `_applyEssentialTexts` (index.html:5267) → `clientMap[client_name] = row` (index.html:5273) → `wlMergeClientsFromSheet(Object.keys(clientMap))` (index.html:5279). Column access is by lowercase header name from `parseCSV`.

Fields the code reads from Clients Info rows, with live status:

| field | read sites (symbol:line) | live column? |
|---|---|---|
| client_name | clientMap keying :5273; placeholders :5282–5288 | Y |
| content_description | brief payloads :5021,:5713,:6318,:6772; modal :7017; weekly Slack :7038 | Y |
| competitors | :5645,:5728 | Y |
| keywords | :5713,:5733 | Y |
| specific_keywords | :5560 | Y |
| instagram_handle / tiktok_handle / youtube_channel_id | table/cards :7076,:6648ff; calendar info :18172 | Y |
| slack_channel_id | weekly-Slack gate :6942,:7030–7031 | Y |
| postforme_account_id | TikTok upload mapping :30025 (+UX copy :30279,:30592) | Y |
| **client_review_token** | share-link builders smCopyShareLink :12062, sxr :26551; link gates :25663 (calendar), :25706 (samples), :25745 (SXR portal), :25928 (analytics `?c=`) | **N — column absent** |
| upload_post_profile | *(no read anywhere in index.html — legacy column)* | Y (2/29 populated) |

**Net effect (measured code behavior + measured sheet state):** every `client_review_token` read yields `''`; all six gates fail OPEN (console warning only, e.g. index.html:25940). **Every client-facing link — analytics `?c=`, calendar, samples, and the new SXR sample-reviews portal — is currently unguarded**, and SMM-copied share links are generated without `&t=`. The 07-03 code audit listed token population as an UNKNOWN; it is now resolved: **0 tokens, column doesn't even exist**. (Silver lining: had the column existed with values, gviz would be publicly serving the tokens — the current design cannot ever hold secrets safely.)

FilmingPlans vs code: code accepts `client_name | doc_url | notes | plan_months` (comment index.html:4601–4604); live tab has only `client_name | doc_url` — compatible (missing optionals degrade to runway-only signalling).

SMM tab vs code: `_kasperLoadSMMMap` (index.html:33750) reads `client_name`, `social_media_manager`, `slack_profile_url`, optional `slack_user_id`/`slack_team_id` — all satisfied (slack_user_id/team_id absent but optional). The `linear_api_key` column is ignored by the front-end but still downloaded by every visitor.

---

## DIFFS vs 2026-07-03 snapshot

**Changed / newly measured (baseline had it wrong, unknown, or unstated):**
1. **`client_review_token` column does not exist on Clients Info** (0/29 populated). Baseline (code.md:88,121) described the mechanism and listed population as an unknown (code.md:136). Now MEASURED: absent entirely → all client links fail open today. (Cannot tell if the column was deleted between 07-03 and 07-05 or never existed — no revision history access.)
2. **SMM tab exposes 7 distinct Linear API keys publicly** (`linear_api_key` column; values redacted; one with an anomalous truncated prefix). No 07-03 doc mentions this column or the exposure — NEW finding, live security incident.
3. **Video Editors tab has NO Slack column** (2 cols only: video_editor, email). Baseline n8n.md:74 said urgent-Slack uses "Sheets Video Editors tab email→Slack map"; MEASURED: the workflow expects `slack_user_id` from the sheet, finds none, and relies 100% on a hardcoded fallback map in its Code node. Spec §hardening line ~263 ("Slack ids from the Video Editors sheet tab") is factually wrong today.
4. **Calendar workbook tab count is exactly 63** (baseline: "~61"). Full first-time enumeration achieved read-only via htmlview.
5. **3 sheet-only clients have no Calendar_/Samples_ mirror tabs** (Jenna Phillips Ballard, Alayna Bellquist, Amanda Hanson) — mirror provisioning has stopped; not stated in baseline.
6. Clients Info now has **29 rows incl. Amanda Hanson** (her Linear project was created 2026-07-02 per baseline; her sheet row, SMM row (Laura), and FilmingPlans row all exist as of 07-05 — onboarding completed on the sheet side).

**Unchanged and confirmed:**
- Master workbook ID and the 8 gviz-fetched tabs (code.md:35) — confirmed, same SHEET_ID at index.html:4593.
- Duplicate Terrina Mar variants in the calendar workbook (07-03 flag) — confirmed: `terrinamar` + `terrinammar` pairs, 4 tabs, still unresolved.
- Hardcoded 30-name seed `WL_CLIENT_NAMES` + additive sheet merge (code architecture unchanged; symbols relocated to index.html:9021/9068/9077).
- Video editor identities (4 editors: Iara, Martin, Nahuel, Santi) + Rocío Perez as sole designer allowlisted in code — matches supabase.md:131's "no people table anywhere" gap.
- SMM map mechanism `_kasperLoadSMMMap` + slack_profile_url as member IDs (code.md/n8n.md context) — confirmed; 7 SMMs, 34 rows.
- Sheets remain fully public via gviz (implicit in baseline architecture) — confirmed unauthenticated.

---

## TRACK B IMPLICATIONS

1. **Spec §3 (clients single source of truth):** the reconciliation table above is the deliverable. Concretely: 26 three-source clients; +3 sheet-only (need `clients` rows; note their mirror tabs don't exist — don't block on mirrors); +4 seed-only (mark `active=false` or `internal/test`: Nobriga, Marks = former; Kasper Hytonen = internal; Sidney Laruel = test); +1 SMM/Linear-only (Jessica Encell Coleman — needs an owner decision: onboard or ignore); Morgan Burch active-on-sheet vs Completed-in-Linear needs an owner call. Slug scheme must tolerate `&` (`eben&annie`) or define a new migration mapping; the full `wlNormalizeClient` rules (dr-strip, and→&, accents) must be ported exactly or client joins will silently miss the three variant spellings that exist TODAY in the SMM tab.
2. **Spec §6.4 (review_token moves from Sheets, flip to fail-closed):** there is nothing to move — the column doesn't exist. Tokens must be **generated fresh** in Supabase, and flipping to fail-closed will break every currently-circulating client link (all are tokenless today). Plan a re-issue-links step per client with SMM comms. Also: the current sheet design could never have held tokens safely (gviz is public) — this strengthens the case for §6.4 but changes its backfill step from "copy" to "mint".
3. **team_members backfill (spec ~line 263):** correct the source list — name+email from Video Editors tab (4 rows); **Slack IDs from the hardcoded FALLBACK map in n8n workflow TJVMyfwl85qrFGeK's Code node** (U0ABN1WQ2V8/U09L10ZG5SS/U0AGCQACEH4/U0ABFL52VTM), not from the sheet; Linear UUIDs from `WL_VIDEO_EDITORS` (index.html:8973); Rocío Perez (designer) exists only in `WL_ALLOWED_GRAPHICS` (index.html:8964) — she needs manual entry; 7 SMMs with Slack IDs from the SMM tab. Kasper + Sidney need admin rows.
4. **Urgent-Slack port:** Track B's replacement should read team_members, not the sheet — today's sheet contributes nothing; parity with the n8n fallback map is the actual bar.
5. **URGENT (out-of-band of Track B, but Track B should absorb it):** the public `linear_api_key` column must be rotated + removed regardless of the migration timeline; Track B's server-side Edge Functions with per-SMM auth is the structural fix, but rotation can't wait for it. Report recommends: owner rotates all 7 keys, deletes the column, and moves keys to n8n credentials/Supabase secrets. (No write actions taken by this audit.)
6. **Mirror strategy:** the Calendar workbook is drifting (no tabs for 3 newest clients, stale terrinamar duplicates). Track B's plan to demote/retire the mirror is validated; any interim "fix the mirror" work should provision 6 tabs (3 clients × Calendar/Samples) and delete/merge the 2 terrinamar pairs — owner action, not audit action.
7. **Analytics stays sheet-fed:** Clients Info (12 cols) remains load-bearing for analytics/handles/slack_channel_id/postforme mapping — Track B §3's "sheet keeps feeding the seed" line is compatible with today's reality; nothing in the current header set conflicts with the code (only the token column is missing, and only its 6 read sites are affected).

---

## UNKNOWNS

1. **Whether `client_review_token` ever existed on the live sheet** (removed since 07-03 vs never created). No revision-history access read-only; 07-03 audit never measured the sheet itself. Owner can check File→Version history.
2. **Whether the 7 exposed Linear API keys are live/valid.** Verifying would require calling Linear's API with them — deliberately NOT done (would be using leaked credentials). Treat as live until rotated. Also unknown how long they've been public (no revision history).
3. **Whether n8n calendar/samples mirror writes for the 3 tab-less clients fail silently or auto-create tabs** — n8n execution logs are the n8n auditor's domain; workflow config suggests best-effort continueRegularOutput.
4. **Why the calendar workbook count moved ~61→63** — can't attribute (approximate baseline vs. real additions).
5. **Google Doc ACLs for the 30 FilmingPlans doc_urls** — presence measured; whether the docs themselves are public was not probed (out of scope; the sheet cell exposure itself is public either way).
6. **Analia's anomalous `in_api_` key prefix** — typo'd truncation vs. a differently-issued key format; owner should verify when rotating.
7. **`Calendar_Config` tab contents** — enumerated but not read (not roster-relevant); flag for the owner if config lives there.
