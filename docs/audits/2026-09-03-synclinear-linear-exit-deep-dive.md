# SyncLinear / Linear exit: deep-cutover addendum

Status: analysis-only. This document changes no production behaviour, data, runtime flag, Edge Function, n8n workflow, share link, or credential. It extends and, where stated, corrects [the 2026-09-03 strategy](2026-09-03-synclinear-linear-exit-strategy.md).

Repository base: `00d0e8883f754dd03676216e23a602a9e5bd353c` (`origin/main`, inspected 2026-09-03). Live measurements below are point-in-time, aggregate-only, and deliberately omit client identifiers, share tokens, bodies, attachment URLs, and secrets.

## Executive answer

Yes. The first strategy identified the main migration lanes, but a safe retirement has more failure domains than a route flip. The deeper audit found client-critical problems in fallback semantics and cached browser state; current, measurable native-link and Workload gaps; historical-data/asset/recovery gaps; and operational trigger/deployment paths that can revive or silently degrade a Linear dependency.

Linear must **not** be scheduled for credential revocation or account cancellation yet. This is not because SyncLinear is unused: the live authority flags are native for both production teams. It is because native authority does not prove that every client action, old browser, background job, historical asset, or staff recovery path has a provider-free durable outcome.

The hard rule remains unchanged: existing client share links keep their current open Calendar/Sample writer contract. Do not solve an exit problem by adding a token or login gate to either writer. That caused client-wide approval failures before and is expressly frozen by `AGENTS.md:3-14`.

## Correction to the earlier map

The earlier map described Samples fallback failure as an error/stale presentation. That was too favourable. The primary Samples REST read does have a catch path, but its legacy fallback neither checks the HTTP result nor validates the response envelope. It returns `ok: true` with an empty list for a malformed, wrong-shaped, or empty fallback payload: `index.html:60922-60938`. `loadSxrCards` then treats it as success, clears the error/stale state, writes the empty result to the seven-day cache, and renders it: `index.html:60967-61018`, especially `:60972`, `:60987-60992`.

If the legacy Samples fallback is unavailable or changes shape when Linear is retired, a client can therefore see an empty Samples board rather than their content or an honest error. This is a P0 cutover blocker. The Calendar reader already models the required contrast: it rejects an unusable/empty fallback result and retains last-good cards with a stale notice: `index.html:34587-34618`, `:35181-35195`.

Whether the source at those lines is the serving revision is **UNPROVEN**: `sample-review-upsert`, `calendar-upsert`, and `client-token-verify` have no automatic CI deployment path according to `docs/ops/EF_DEPLOY_MANIFEST.md:25-30,49-50`. The defect is nevertheless reachable in the audited browser bundle and must be tested against the serving revision before any retirement gate can pass.

## Newly confirmed current blockers

| Domain | Confirmed evidence | What a user would see if Linear disappeared now | Required exit condition |
| --- | --- | --- | --- |
| Client Samples read fallback | A primary-read exception can turn a bad/empty fallback into successful cached emptiness: `index.html:60922-61018`. | Client sees no Samples/review content, without an error explaining that their data is unavailable. | Typed fallback validation; preserve last-good content/cache on every unusable fallback outcome; live share-link fault test. |
| Client write fault routing | Missing, malformed, slow, or failed routing-flag reads deliberately choose legacy n8n/Linear status/comment lanes: `index.html:25073-25141`, `:32198-32361`, `:60287-60334`, `:64994-65097`. | An approval, tweak, or comment can look locally/source-saved while the team-delivery leg queues or fails after Linear is gone. | Every failure branch sends one durable native intent/receipt or gives an explicit, recoverable native error; none may call a provider endpoint. |
| Cached clients and local debt | Legacy status/comment queues live in browser storage and resume on startup, focus, page restore, reconnect, visibility, and a minute cadence: `index.html:32134-32196`, `:64948-64992`, `:65681-65841`. Calendar/Samples card caches retain shapes for seven days: `index.html:34371-34376`, `:60791-60857`. Stale tabs are intentionally allowed to keep running while dirty: `index.html:75914-76290`. | A client returning to an older tab can retry a retired endpoint, or an old cached shape can make an action targetless. Server queue health alone cannot show this local debt. | A provider-free compatibility receiver/translator survives the agreed stale-bundle and cache horizon, or the same client is shown a durable native recovery state. Never silently clear their queued intent. |
| Interactive Calendar linkage | Read-only aggregate check at 2026-09-03: 587 component slots would throw `native_link_required`; 31 are actionable and 4 were linked after native authority changed. The classifier defines the precise reachable predicate: `scripts/calendar-native-link-gap-check.js:3-43,153-216`. | A client or staff member can press a status/tweak action on a still-live card and receive a refusal because it has a provider link but no native target. | Zero actionable half-linked components, or a durable native exception route that preserves the offered client control. The post-flip bucket must remain zero. |
| New-card linkage | In the latest eight-week aggregate, 455 cards were created; 20 were unlinked, one remains live, and 17 were removed before judgment: `scripts/card-linkage-leak-check.js:120-192`. | The one live card may be an intentional reference rather than lost work; that classification is **UNPROVEN**. If it is work, no provider shutdown can repair its missing destination. | Privately classify each live unlinked card; make creation atomically attach native work or make its source-only/reference role explicit before exposing an action that needs work. |
| Workload completeness | Native visibility check at 2026-09-03 found 14 real live native rows invisible to their owner, above the gate baseline of 13: `scripts/workload-native-visibility-check.js:218-335`. | Staff misses work they owe. This is staff-facing, but it makes a Linear-free Workload source unsafe. | Native renderer, plan identity, due/assignment/comment reads, forced refresh, and visibility check all pass with Linear unavailable. |
| Workload deep links | A live aggregate read at 2026-09-03 found 5,173 of 5,176 native Workload subissue rows retain a provider URL. The native view selects it: `migrations/2026-09-02-workload-native-view.sql:161-176`. | Staff clicking almost any current Workload item reaches a dead provider link after shutdown. | Replace interactive deep links with native routes; retain legacy identifiers only as non-clickable provenance/archive metadata. |
| Public/staff creation | `production-write` resolves provider project/state/assignee information before the native intake write: `supabase/functions/production-write/index.ts:2168-2517,5953-6009,6837-6902`. | A client or staff member using Submit/Create Post can receive a pre-write service failure. This is client-critical wherever public intake is offered. | Provider-free native project/team/state/assignee mapping is deployed and fingerprinted; share-link and staff Create Post journeys prove a durable native receipt. Production-board create stays closed: `index.html:52200-52256`; `production-write/index.ts:3455-3488`. |
| Provider-only assets/history | The archive reader recognizes private provider uploads and shows an unavailable placeholder when one is unresolved: `supabase/functions/production-archive/index.ts:1-5,124-155,204-230`. The asset rescuer requires a provider credential and a certified final export: `scripts/f34-linear-asset-rescue.js:4-11,436-448,478-524,591-686`. | A historical attachment can become unavailable. The archive reader is staff-only, not a client continuity route: `production-archive/index.ts:242-284`. | Export, classify, rescue/rehost, hash-readback, and share-link retrieval test every visible asset class before access is revoked. |
| Comment/history coverage | F42 deliberately imports only selected linked Calendar/Samples fields; deferrals and wrong-link defects are not imported: `scripts/f42-card-comment-export.js:4-38`; `scripts/f42-card-comment-import.js:31-58,654-685`; `scripts/f42-card-comment-apply.js:105-119`. | Older notes, replies, lifecycle information, or attachments can vanish from the user experience if their only copy is provider-side. | Every comment/reply/tweak/lifecycle record has exactly one disposition: native preserved, protected retrievable archive, or explicit owner-approved retention disposition. |
| Running automation roots | Active GitHub workflows include B1 refresh, deliverable/status/Samples reconcile, outbound drain, E2E, monitoring, and inbound deployment. Source confirms direct provider consumers and independent trigger roots: `.github/workflows/b1-linear-incremental-refresh.yml:24-25,38-42,70-93`; `.github/workflows/linear-outbound-drain.yml:3-17,58-70,91-134`; `.github/workflows/linear-deliverables-reconcile.yml:1-10,48-60,68-130`; `.github/workflows/sample-linear-reconcile.yml:1-15,49-55`. Read-only published n8n inspection also found active Calendar/Workload/urgent-tweak/intake/qll provider paths. | A shut-down provider can cause background failures or error-continued notification/assignment degradation while pages appear to work. | Replace/retire every trigger root—GitHub schedule, dispatch, manual runbook, qll/n8n trigger, provider webhook—not merely one cron. |
| Recovery/rollback | `linear-outbound` in off mode stops calls but does not terminalize ordinary pending work: `supabase/functions/linear-outbound/index.ts:62-74,956-1127,1355-1458`. Manual deployment/rollback bundles can redeploy provider code: `.github/workflows/deploy-onboarding-edge-functions.yml:122-163`; `.github/workflows/deploy-f27-section4-closures.yml:1240-1365`. | Client impact is indirect until a rollback revives an old writer or an unadjudicated queue is forgotten. | Every intent is delivered, explicitly cancelled/classified, or archived with an auditable reason. Provider-free recovery replaces provider replay before irreversible shutdown. |

The 2026-09-03 point-in-time aggregate evidence is intentionally not a claim that every row is defective. The linkage checker measures a component-level, reachable `native_link_required` condition; the eight-week creation check measures whole cards; the Workload checker measures live owner visibility. Their different predicates and timestamps must not be collapsed into one number.

## What the current signals do and do not prove

Read-only live snapshots at roughly 22:00Z on 2026-09-03 show both production teams are SyncView-authoritative, the client-comment gateway is enabled, and the relevant client cohorts are populated. That proves the healthy browser branch is configured for native use at that moment. It does **not** prove a failed flag read, stale bundle, or legacy browser queue follows the native path; the source explicitly says otherwise: `index.html:25073-25141`, `:60287-60334`.

The same snapshot recorded active GitHub workflow definitions with recent completed runs. In particular, the latest scheduled Calendar and Samples E2E runs were failing, while several provider-dependent reconciler/drain runs reported success. A green provider job proves it ran, not that a provider-free client journey works. A red E2E run is a cutover blocker until its cause is classified; its current cause is **UNPROVEN** from this audit. Workflow state can change after this snapshot, so the final gate must take a fresh read immediately before any transition.

Anonymous access returned authorization failures—not zero rows—for protected data such as outbox, intake receipts, comments, archive, workload plans, F27 ledgers, and asset-rescue state. That proves the data is protected from the published role, not that it is absent. Counts, ages, and reconciliation state for those objects are **UNPROVEN** until a private privileged preflight captures them.

## Additional exploration still required

| Question that must be answered privately | Why source/read-only public data is insufficient | Required evidence before approval |
| --- | --- | --- |
| Which source revision is actually serving for manual/no-CI functions? | `production-write`, client writers/verifier, inbound/outbound, archive/comments, and Workload lanes have manual or pinned deployment paths: `docs/ops/EF_DEPLOY_MANIFEST.md:3-7,24-30,38-57`. | Protected deployed source hash/version, deployment receipt, and real test-scope action/readback for each client-critical writer. |
| What remains in protected queues, receipts, mappings, and rollback state? | Track-B backup is a fixed 14-table corpus and omits Calendar/Samples/event ledgers, intake receipts, workload plans, comment receipt/audit/link tables, F27 ledgers, asset-rescue tables, and external bytes: `scripts/track-b-backup.js:42-57`; `docs/ops/TRACK_B_BACKUP.md:21-34,160-207`. | A separate immutable Exit Archive manifest: counts/hashes, DDL/function/trigger versions, crosswalks, external-object hashes, plus isolated restore and retrieval rehearsal with provider egress disabled. |
| Are all provider assets reachable after shutdown? | F34 only recognizes a defined upload-host pattern and selected fields: `scripts/f34-linear-asset-rescue.js:18-20,91-148`; the sidecar has the same bounded shape: `migrations/2026-07-23-f34-f53-production-attachments.sql:687-768`. | Independent final provider-export asset census by host/type/location; zero unclassified occurrences; private byte/hash readback; client-link retrieval tests for every visible asset class. |
| Does every old share link have the credentials/crosswalk a replacement comment route would require? | The native comment reader has exact token, read-budget/audit, and surface-policy constraints: `supabase/functions/production-comments/index.ts:158-191,271-340`; `production-comments/policy.mjs:51-76`. | Fresh and old share-link test matrix. The answer may not be "add auth" because frozen writers remain tokenless: `AGENTS.md:3-14`. |
| Are all published n8n/provider-admin/external paths accounted for? | Repository source cannot establish provider-side webhooks, automations, OAuth apps, personal tokens, Slack/email integrations, browser extensions, or external scripts. Live graph inspection was partial. | Owner-admin export/inventory, role-by-role staff acceptance checklist, and an offline trigger-to-sink dependency scan of exported published workflows. |
| What happens to historical URL/bookmark/404 routes? | The SPA fallback drops query parameters for unknown non-onboarding paths: `404.html:15-24`. Current share-link query behavior is strict: `index.html:59765-59856`. | Inventory of historic route shapes/bookmarks and a no-loss redirect/compatibility test. Absence of old path-based links is **UNPROVEN**. |
| Are existing monitors protection or merely liveness? | The watchdog watches legacy heartbeats and job execution rather than a real client journey: `scripts/monitoring-watchdog.js:57-107,134-139,190-293`. | Replacement alert delivery drill: deliberately fail only the allowed test-scope synthetic and verify owner notification. |

## Extended, ordered exit gates

These gates add to the primary strategy's plan. They are deliberately ordered: a later gate must not be used to compensate for a missing earlier client-safe compatibility guarantee.

### Gate D0 — freeze the evidence baseline and prove serving revisions

- **What changes:** no product behavior. Capture private source/version fingerprints for every manual/pinned Edge Function, published n8n export/version, provider-admin integration inventory, and current protected-data aggregate. Create a separate immutable Exit Archive rather than changing the existing Track-B backup contract.
- **Why it sits first:** a source-only plan cannot certify production behavior where deployment provenance is unknown. A later rollback or dry run is unsafe if it silently restores provider-dependent source.
- **Gate:** protected receipts show each client-critical function's serving hash and writer posture; an isolated archive restore retrieves a representative source card, native work item, comment thread, attachment, receipt, and mapping without provider egress.
- **Post-ship test:** owner runs the retrieval rehearsal and a public test-scope share journey against the observed deployed revision; both have stored aggregate pass/fail evidence.
- **Client sees mid-flight:** nothing changes. This is read-only capture and rehearsal; no writer, flag, URL, or browser cache is changed.
- **Who acts:** database/archive owner, Edge Function deploy owner, n8n owner, and provider administrator. A merge alone cannot complete it.
- **Undo:** additive evidence capture is reversible only as a reference artifact. It must not be deleted during the cutover program.

### Gate D1 — make client reads fail closed to last-good content, not to empty success

- **What changes:** replace Samples' unchecked legacy fallback with Calendar-equivalent response/envelope validation, last-good preservation, stale/error presentation, and no-empty-cache-write behavior. This is a client reader repair, not a writer re-gate.
- **Why it precedes provider retirement:** otherwise a provider outage can masquerade as an empty-but-successful client board, making a client-visible loss invisible to monitoring.
- **Gate:** primary read fault plus each fallback result—non-2xx, invalid JSON, wrong envelope, explicit failure, and empty payload—never overwrites a nonempty cache or hides last-good content. Cold cache produces an explicit retry/error state, not a false empty success.
- **Post-ship test:** fresh and cache-primed share links load Calendar and Samples through the above fault matrix, then refresh/reconnect. The test validates screen state and cache/readback, not only HTTP status.
- **Client sees mid-flight:** no content/control disappears. Before release, they see the existing behavior; after release, a transient outage keeps their prior content visible with an honest stale notice.
- **Who acts:** frontend merge; GitHub Pages release; deploy owner only if a server fallback contract changes.
- **Undo:** revert the reader code while the legacy endpoint remains live. Do not use an empty cache as a rollback mechanism.

### Gate D2 — replace every client writer failure fallback with a native durable receipt

- **What changes:** implement a provider-independent compatibility adapter/outbox for Calendar and Samples approval, status, tweak, plain note, linked/unlinked comment, and client collaborative Calendar creation. It accepts legacy cached payload shapes, performs idempotent native persistence, and gives the browser a durable receipt. It must preserve the frozen open writer posture.
- **Why it precedes cache retirement and operations shutdown:** routing failures currently choose legacy endpoints. Turning those endpoints off first creates the exact client-side action loss the project must avoid.
- **Gate:** under each one-at-a-time fault—flag REST timeout/5xx/malformed/missing value, Realtime failure, old cache, old bundle, and provider-blocked endpoint—each offered client action produces exactly one source/native receipt and survives reload/BFCache. No provider request is attempted.
- **Post-ship test:** scheduled private synthetic journeys use only the designated test scope and exercise client Calendar/Samples load, approve, tweak, note/comment, collaborative Calendar create, refresh, offline/reconnect, and page restore. Assert the visual result and server readback.
- **Client sees mid-flight:** nothing changes in controls, share URLs, or auth. During dual compatibility, an old page reaches the adapter rather than Linear; it either succeeds natively or shows a visible pending/retry state tied to the same action.
- **Who acts:** frontend and Edge Function changes land by merge; manual writer deploy owners fingerprint serving revisions. n8n is not edited unless its owner explicitly approves the same change window.
- **Undo:** route the adapter to its retained native compatibility implementation. Do not undo by re-gating a client writer or deleting browser-local debt.

### Gate D3 — close target/linkage debt and prove creation semantics

- **What changes:** privately classify every actionable half-linked component and live unlinked card into native target, source-only/reference disposition, or durable native exception. Modify creation/link workflows so a provider-shaped link cannot create a future native-target refusal. Preserve legacy IDs as provenance.
- **Why it precedes provider-free actions:** native status/comment adapters cannot safely choose a destination until every interactive component has one. Backlog repair alone is a treadmill while post-flip gaps are still being created.
- **Gate:** `calendar-native-link-gap-check --gate` is zero for post-flip actionable gaps; all other actionable gaps are zero or have an owner-reviewed native exception receipt; every live unlinked card is classified. The checker's total is monitored after every deploy.
- **Post-ship test:** staff and client use representative native, legacy-shaped, no-work/reference, and exception cards. Status/tweak/comment/create actions persist and read back without a provider request; Production creation remains closed.
- **Client sees mid-flight:** existing cards remain visible and actionable throughout. A card is never converted from a provider link to no target; dual identifiers remain until after the full retirement soak.
- **Who acts:** database owner for additive mapping/backfill; frontend/function owners for adapter/creation changes; staff workflow owner for adoption/link controls.
- **Undo:** disable the new reader/adapter while retaining mappings and exception receipts. Do not delete cards, links, comments, or mappings as rollback.

### Gate D4 — decouple public/staff creation and staff views

- **What changes:** replace provider project/team/state/assignee validation in public intake with native mappings; re-source Submit project selection, Calendar/Samples Create Post, Production labels, urgent-tweak assignee resolution, Calendar metadata, and Workload reads/writes/deep links to SyncLinear. Production continues to reject subissue creation.
- **Why it precedes trigger shutdown:** public intake calls provider resolution before native write today. Stopping the provider first can turn a client-facing Create Post into a pre-write failure.
- **Gate:** provider-blocked test-scope Submit and staff Create Post each creates one native work object and receipt, appears in the appropriate client/staff surface, and does not create a Production-tab subissue. Workload visibility check exits zero, native plan identities are mapped, and no interactive Workload deep link targets the provider.
- **Post-ship test:** staff opens Submit, Calendar, Samples, Workload, Production, Filming, and Kasper paths; client opens Calendar and Samples. Exercise create, edit, status, tweak, note, assignment/urgent notification where offered, plan day/due edit, refresh, and native deep link while provider traffic is blocked in the test environment.
- **Client sees mid-flight:** no client control moves or disappears. The client continues to use existing Create Post/review controls; only their durable backend destination changes after it is verified.
- **Who acts:** frontend/code merge, database migration owner, manual Edge Function deploy owner, and notification/integration owner. Any n8n modification remains owner-gated.
- **Undo:** switch only the native mapping/reader to its retained previous native-compatible version while the provider still exists. After provider revocation, rollback means restoring SyncLinear service from the Exit Archive, not reviving Linear.

### Gate D5 — archive history, comments, assets, and recovery state outside the provider

- **What changes:** produce a private final provider export and Exit Archive that cover active/archived/deleted work, hierarchy, statuses, labels, users, comments/replies/reactions/edit history, attachments/inline assets, external IDs, receipt/outbox state, mapping/rollback state, and workflow/admin configuration. Rehost/rescue every visible provider-hosted asset or record an explicit irreversible disposition.
- **Why it precedes credentials and subscription changes:** provider cancellation can make export, attachment rescue, and investigation unrecoverable. Existing backups and F42/F34 are useful components, not proof of comprehensive coverage.
- **Gate:** per-class count/hash manifests reconcile; every non-test intake/outbox/F27 item is terminal or explicitly classified; every comment/history record has a disposition; asset census has no unclassified host/type; an isolated restore retrieves representative material without provider egress.
- **Post-ship test:** authorized operator retrieves a historical issue/tree, comment/reply, status, attachment, receipt, and crosswalk from the private archive. Separately, a client share journey retrieves every asset class that the product still displays.
- **Client sees mid-flight:** nothing changes. Export and rescue are read-only/additive until a tested native/rehosted reader is already available.
- **Who acts:** provider administrator, database/archive owner, storage owner, and legal/retention owner. This is not merge-only work.
- **Undo:** exports and rescued copies are additive recovery artifacts. Provider account deletion/cancellation is not practically reversible; that action remains blocked until this gate is signed off.

### Gate D6 — replace monitoring before removing operational roots

- **What changes:** introduce native client synthetic, native receipt/age, projection freshness, provider-egress, asset/archive, and Workload/linkage monitors. Then replace or retire provider-dependent B1/reconcile/outbound/shadow/drill workflows, n8n paths, and qll dispatches as a coordinated operational change.
- **Why it precedes scheduler/webhook removal:** existing monitoring largely proves a heartbeat/job run, not that an unauthenticated client can still use the product. Removing old jobs first either creates intentional false alarms or removes the only signal before a replacement is proven.
- **Gate:** a deliberately failed test-scope client synthetic alerts the responsible owner; healthy synthetic proves load/approve/tweak/note/comment/create-plus-readback; provider-egress counter is zero except for the explicitly approved final export window; no active trigger-to-sink path reaches Linear.
- **Post-ship test:** run each scheduled replacement at least once, include failure-injection, and observe alert delivery. Re-run after a full schedule cycle and after a qll/manual-dispatch attempt. A passing dead-man alone is insufficient.
- **Client sees mid-flight:** nothing changes because their compatibility adapter/native writers were completed in D1-D4. Monitoring runs against the permitted test scope only.
- **Who acts:** repository/workflow owner, n8n owner with explicit same-window approval, monitoring/on-call owner. Do not edit n8n merely to satisfy this document.
- **Undo:** restore exported workflow versions while Linear is still available and the native monitors remain on. Do not let a source-exact rollback bundle reintroduce provider traffic after final revocation.

### Gate D7 — staged external shutdown and irreversibility control

- **What changes:** stop new provider outbox intent creation; adjudicate existing work; disable every inbound/outbound provider integration and staff provider creation path; update deploy/rollback bundles so routine recovery cannot revive provider code; then revoke credentials. Cancel/delete the provider workspace only after a post-revocation soak.
- **Why it is last:** credential removal before D1-D6 turns compatible fallback, export, rescue, and recovery failures into client-visible or irrecoverable loss.
- **Gate:** fresh and held-prior-bundle client journeys are green across the agreed cache horizon; zero provider egress spans scheduled, manual, cached, and fault paths; native freshness/receipt monitors are green; provider administrator confirms webhooks, automations, OAuth/API keys, integrations, browser extensions, and permissions are disabled; final archive delta is reconciled.
- **Post-ship test:** after each substep, rerun client/staff journeys and monitor all scheduled cycles. After revocation, validate the same products without credentials and watch for first-attempt egress or client 4xx/5xx/aged receipt alert.
- **Client sees mid-flight:** nothing changes. A fresh or stale page always reaches a provider-free reader/writer before any credential is touched; an internal delay never removes a client control or converts it to a provider error.
- **Who acts:** code/workflow owners, n8n owner, database/archive owner, deploy owner, and provider administrator. Credential revocation and account cancellation require explicit owner authorization in their own change window.
- **Undo:** before credential revocation, restore native-compatible adapters/configuration and exported workflow versions. After revocation, recovery is the tested SyncLinear/archive plan; after workspace cancellation, provider recovery is not a dependable rollback.

## Watchers that must exist before cutover

| Watcher | What it proves | Alert condition and route | New infrastructure? |
| --- | --- | --- | --- |
| Unauthenticated client synthetic | The actual share-link contract loads content and persists each offered client action through refresh/readback. | Any non-2xx, missing native receipt/readback, stale projection, or unexpected empty board pages the owner immediately. | Can start in existing Actions, but needs a private fixture and authoritative result. |
| Fault-routing synthetic | Flag/Realtime/read failures and a held prior bundle still use a native compatibility receiver, once. | Any legacy endpoint/provider request, duplicate receipt, browser-local-only success, or lost BFCache/reconnect action alerts. | Requires test harness/aggregate receipt query. |
| Samples content-integrity monitor | No fallback response has replaced known content with a false empty success. | Empty cache write after fallback, bad envelope accepted, or a content-bearing test row rendered empty alerts. | Small code metric plus test; no client data in alert. |
| Native receipt/outbox monitor | Accepted actions have durable terminal/native state and no provider-bound work accumulates. | Missing receipt, pending-age breach, failed terminal state, or any new provider-bound row alerts. | Add aggregate query/metric. |
| Native freshness/visibility monitor | Calendar, Samples, and Workload show source changes promptly and Workload does not hide live work. | Stale age breach, nonzero linkage post-flip count, or nonzero Workload gate result alerts. | Existing read-only scripts can be scheduled; freshness needs an aggregate source/projection comparison. |
| Provider-egress census | No browser, Edge Function, GitHub workflow, n8n execution, or outbox transition reaches Linear after its cutoff. | First unexpected request/execution/transition alerts with route/category/time only. | Mostly existing logs/source scans; n8n export/telemetry access needed. |
| Archive/asset recovery monitor | Private recovery remains retrievable after provider loss. | Manifest hash/count mismatch, unclassified asset, unresolved visible asset, or restore/retrieval failure alerts. | Private scheduled check/storage access. |
| Alert-delivery drill | A monitor actually reaches a human. | A deliberately failed test-scope synthetic must produce a verified notification; failure blocks the next gate. | No new platform if existing failed-run notifications reach the responsible owner; that coverage is currently **UNPROVEN**. |

## Cosmetic, documentation, and human-work sweep extended

Do this only after D1-D7 have passed. A string scan is not proof, but it supplies the inventory for reachable UI testing.

- **Client-visible:** all Calendar/Samples/Submit copy; empty, stale, retry, pending, and success messages; status/tweak/comment help; deep links; tooltips; attachment placeholders; cached-page update notices; and any provider-named remedy. The client must never be asked to open, wait for, or contact Linear.
- **Staff-visible:** Workload provider deep links and refresh/fallback instructions; Calendar/Samples link/adoption/subissue controls; Submit/Create success/failure copy; Production label/error messages; archive placeholders; Kasper/editor/tweak assignment wording; runbooks and manual remediation links.
- **Operational:** GitHub workflow names/descriptions/dispatch inputs, n8n workflow labels, monitoring ownership, deploy/rollback bundles, secrets/variables, provider-admin webhooks/automations/integrations, browser bookmarks/extensions, Slack/email notification recipients, and billing/retention/access ownership.

The human-work inventory is a separate acceptance exercise. Code cannot prove that staff do not create or search provider work manually, rely on a browser extension, receive a provider notification, or follow an off-repository runbook. These are **UNPROVEN** until an owner-admin and role-by-role review records the replacement or retirement of each practice.

## Irreversible boundaries added by this audit

1. Provider asset access may be needed to rescue historical files; account cancellation can make those bytes unavailable.
2. A provider-only comment, reply, audit event, or hierarchy relation cannot be reconstructed from an outbox count alone.
3. Clearing browser-local queues cannot prove client intent was delivered and would discard the only copy for an old tab. It is prohibited as a cutover shortcut.
4. A generic rollback/deploy bundle that restores provider code is not a rollback after credentials are gone. Update it before irrevocable shutdown.
5. Credential revocation may be operationally reversible only while the account and export window remain intact; workspace cancellation/deletion is the final, practically irreversible act.

## Decision rule

The successful end state is not "zero Linear strings" or "SyncLinear authority enabled." It is this: with Linear unavailable, a fresh or stale client share page can still see content, approve, request changes, and write every offered note/comment; every action has a durable native result or visible native recovery; staff have working native operational views; history/assets are retrievable; and any silent regression alerts a human before a client has to discover it.

Until the gates above have independent evidence, the correct status is **not ready for Linear shutdown**.

## Second deepening -- protocol, semantic, and control-plane closure

### Revised conclusion

No. D0--D7 are necessary but were still too coarse to authorize a shutdown. A Linear call can be gone while the product still derives a client-visible result from Linear-era state, an old browser still speaks the old protocol, a comment has only one half of its thread, or an external producer silently creates new Linear-only work.

The following findings are new cutover blockers. They do **not** authorize a writer redeploy, a flag change, a migration, an n8n edit, or any production mutation. In particular, they do not alter the frozen open posture of the two client upserts.

| Finding | Evidence and boundary | Consequence if Linear is removed now |
| --- | --- | --- |
| Deployed-client-writer contract is unknown and source disagrees with the frozen live contract | Candidate source invokes fail-closed browser authentication before Calendar/Samples upserts: `supabase/functions/calendar-upsert/index.ts:497-505`, `supabase/functions/sample-review-upsert/index.ts:509-516`; its shared policy rejects missing credentials: `supabase/functions/_shared/browser-write-auth.ts:46-103`. The deployed writers are deliberately open/tokenless by owner directive: `AGENTS.md:3-14`, and neither has a CI deploy path: `docs/ops/EF_DEPLOY_MANIFEST.md:25-30,49-50`. | An incidental manual deploy can reintroduce the historic all-client approval/comment `401`, even though the repository source looks more secure. Source behavior is **not** serving-behavior evidence. |
| An update banner cannot retire an old client protocol | The browser's version floor can defer while work is dirty: `index.html:75977-76024`, `:76161-76190`. The known F127 gap is that no immutable build/authority epoch reaches a server writer: `docs/truth/APP.md:104-110`, `ROLLBACK.md:38-47`. | A held tab, BFCache restore, offline retry, or cached queue can call an old Linear fallback after fresh Pages code is correct. A generic `upgrade_required` that refuses an active share-link action would itself violate the client invariant. |
| Native comment adoption is not yet thread parity | A read-only, count-only live snapshot found 20 full-roster Calendar component slots with 32 client root comments stored only in the legacy card representation; eight were not Archived/Posted. The classifier establishes why the split is reachable, distinguishes safe front-door cases, and pages all source reads: `scripts/card-comment-transport-split-check.js:6-33,153-214,238-340`. | A client may still see a root comment while a staff reply, edit, resolve, or delete uses a canonical route that has no matching thread. Text import alone does not preserve replies, audience, edits, tombstones, versions, attachments, or retry identity. Canonical-row coverage is protected from anonymous inspection and is therefore **UNPROVEN**. |
| SyncLinear visibility still interprets raw Linear lifecycle markers | The browser filters every displayed deliverable through `_prodDeliverableLive`: `index.html:49970-49984`, `:50676-50683`; the browser view exposes raw archive/delete fields from `linear_raw`: `migrations/2026-07-25-slice5-production-read-path.sql:79-168`. A read-only aggregate found 278 hidden native rows, 269 of which have a native non-terminal status; four active Calendar cards reference candidates. | Removing raw-marker interpretation can make old work unexpectedly reappear or leave live work hidden. The aggregate does **not** prove 269 product defects; it proves native `status` is not yet a lifecycle substitute. Client-token reachability for the four active-card candidates is **UNPROVEN**. |
| Archive is a UI action, not an atomic native lifecycle transaction | Calendar archives the card first and only then best-effort parks components: `index.html:40998-41095`. Samples only archives its own row: `index.html:62002-62025`. `production-write` has no archive/restore operation: `supabase/functions/production-write/policy.mjs:5-18,77-91`; `production-archive` is a staff reader: `supabase/functions/production-archive/index.ts:1-5,322-445`. | A card can disappear while its component work remains live in Workload/Production, or a restore has no defined compensating action. That can strand staff work and make a client request look lost. |
| The public share link is not an API row-scope boundary | Count/status-only live anonymous probes could read Calendar/Samples/Production/Workload surfaces while service-only tables returned authorization failures. The source policies grant broad anonymous reads: `migrations/live-schema-baseline-2026-07-03.sql:404,410-414`, `migrations/2026-07-06-b1-linear-data-model.sql:667-700`; this is the documented F88 boundary: `docs/truth/SUPABASE.md:68-76`. | This does **not** prove the normal UI mounts a wrong client. It does prove that tightening RLS/raw reads during the Linear cutover could blank a client surface. Access hardening must be a separately shadowed program, not a shutdown prerequisite. |
| Source upserts do not yet prove exactly-once client behavior | The source upserts use read-then-write shapes and no universal request/idempotency key: `supabase/functions/calendar-upsert/index.ts:298-378`, `supabase/functions/sample-review-upsert/index.ts:298-392`; browser client saves intentionally omit the scalar base version: `index.html:39380-39437`, `:62529-62568`. Audit event writes are after the primary write: `calendar-upsert/index.ts:523-550`, `sample-review-upsert/index.ts:536-563`. | A lost response, double-click, two tabs, stale cache, or retry might duplicate/overwrite work or show success before durable history exists. These are source-level risks only; actual deployed writer behavior is **UNPROVEN** until D0/E0 fingerprints it. |
| Browser-persisted provider queues are external consumers too | Calendar/Samples persist legacy comment/status entries and resume them after verified entry, startup, focus, page restore, online/visibility, and a minute cadence: `index.html:30000-30064`, `:31935-32001`, `:64586-64614`, `:64802-64992`, `:65681-65826`. Some client-linked/source-gated entries intentionally bypass the normal flipped-team quarantine. | A clean database outbox and a successful deployment do not neutralize an old client device. Opening a tab can retry a provider endpoint long after central cutover. Remote-clearing browser storage would instead discard the only copy of client intent. Live queue prevalence is **UNPROVEN**. |
| Submit has its own old-browser receipt and card-materialization escape hatch | Old/local-receipt/partially-cached Submit branches intentionally choose legacy form routing: `index.html:46194-46422`, `:46719-46781`. The follow-on job polls provider issue/subissue data, persists in local storage, resumes later, and can discard a pending job after authority changes: `index.html:47256-47562`, `:65784-65790`. | A client can be told a submission was received but never get a native visible card, or an old pending job can be discarded with manual-recreation advice. This is a client P0, not merely a staff workflow inconvenience. |
| Green scheduled probes can currently prove the legacy lane, not the exit | Eleven of 69 scheduled probes force legacy routing, including client-facing Calendar cases: `qa/probes/p28_linear_sync.js:20-32`, `qa/probes/p29_linear_kasper.js:1-19`, `qa/probes/p30_linear_client.js:13-17,54-56`, `qa/probes/nightly-manifest.txt:6-9`, `qa/run-probes.js:37-48,103-130`. The edge fault suite is not scheduled and still expects deferred provider intent: `qa/probes/ot4_t0_client_edge_conditions.js:16-20,127-206`. | A green nightly can certify the path that must be removed. Retry masking also hides a first-attempt cutover failure: `.github/workflows/calendar-e2e-nightly.yml:4-16,30`, `qa/run-probes.js:11-15,107-123`. |
| Linear is still an ingress, egress, recovery, and human-control plane | B1 is scheduled and its stray catcher is explicitly post-flip/import-only: `.github/workflows/b1-linear-incremental-refresh.yml:24-41,70-121`, `scripts/b1-linear-backfill.js:69-88,1380-1527`. Outbound supports parity/replay beyond normal `off`: `supabase/functions/linear-outbound/index.ts:1267-1408,1796-1951`; deploy/restore routes remain: `.github/workflows/deploy-f27-section4-closures.yml:1294-1339`, `.github/workflows/deploy-onboarding-edge-functions.yml:122-164`. | Human, integration, or provider-side work can continue being created only in Linear; a restore can re-arm egress; or an old operational control can silently fail after credentials vanish. The active provider-admin/n8n integration inventory is **UNPROVEN** from this repository. |
| A provider export is not a client-visible attachment archive | The existing rescue scope is not a universal URL/object census: `scripts/f34-linear-asset-rescue.js:436-448`; Track-B backup is a fixed database corpus, not external bytes/history: `scripts/track-b-backup.js:42-57`, `docs/ops/TRACK_B_BACKUP.md:21-34`. Linear's workspace CSV omits attachment files, and its workspace deletion becomes permanent after the documented cancellation window: [Linear export documentation](https://linear.app/docs/exporting-data), [Linear workspace documentation](https://linear.app/docs/workspaces). | A count-matched export can still leave a client-facing attachment, comment, or archive record unrecoverable after deletion. |

### Required separation of releases

There are three different programs here. They must not share a risky release window.

1. **Client compatibility and native persistence.** This is the Linear-exit program: accept every still-supported link/bundle/payload, persist a native result exactly once, and render last-good content or a visible native recovery state. It must complete first.
2. **Semantic and historical parity.** This establishes native lifecycle, hierarchy, comments, approvals, assets, and recovery without changing client affordances. It can run additively alongside compatibility, but must be proved before deleting raw/provider evidence.
3. **Access-boundary modernization.** Scoped reader/RLS/CORS/token changes are valuable but separate. They require their own client-link migration and must never be smuggled into the cutover. A source-only authentication cleanup is specifically forbidden for the frozen writers without the owner's same-request approval and confirmed fresh links.

### Additional ordered gates

These refine the D gates; each is additive to, not a replacement for, the original exit plan.

### Gate E0 -- seal the serving contract before changing any route

- **What changes:** add no product behavior. Privately capture source hash, JWT posture, CORS/auth posture, routing flags, and a controlled client-action result for each manual/pinned client-critical function. Add a release check that prevents an operator from deploying a client-facing writer whose compatibility matrix has not been signed against the observed serving matrix.
- **Why it sits first:** a repository diff cannot distinguish a safe native replacement from an accidental redeploy of a credential-gated writer. Every later test is invalid if it targets source rather than the actual service.
- **Gate:** an owner-private manifest maps every client-critical endpoint to serving fingerprint, current compatibility formats, auth/JWT posture, and a non-secret test receipt. Calendar/Samples upserts explicitly prove the frozen tokenless client path remains available. No manifest row may be inferred from a workflow green check.
- **Post-ship test:** fresh and held share-link sessions save Calendar and Samples approval, tweak, note, and comment against the observed function revision; protected staff endpoints still reject an anonymous caller where their contract requires it. Store only aggregate status/receipt facts.
- **Client sees mid-flight:** nothing changes. This is observation and controlled test-scope proof; no writer is redeployed, gated, or turned off.
- **Who acts:** Edge Function deploy owner and database owner. A merge cannot complete it; n8n is read-only in this step.
- **Undo:** retain the manifest and serving artifact. There is no client-facing rollback because nothing serving was changed.

### Gate E0a -- make the cutover test prove a provider-free client journey

- **What changes:** retain legacy probes as compatibility-adapter coverage, but add a required, independent native-only client canary. It uses only the designated test scope, network-rejects every legacy/provider endpoint, and proves actual deployed share-route load, approval, tweak, note/comment, reload, native receipt, and rendered readback. Upgrade the edge/fault suite to native semantics and include it in the required schedule/release path.
- **Why it sits before a compatibility rollout:** the current scheduled suite deliberately forces provider routing in several client probes. A green result cannot be used to decide that Linear can be switched off; retry-until-green is especially unsafe for a cutover signal.
- **Gate:** the native-only canary runs against the serving build/function fingerprints from E0, has zero tolerated first-attempt failures, and covers fresh plus two-release-old share-link/page formats. Its fault matrix includes non-2xx, timeout after server commit, rate limit, malformed flag/response, offline/reconnect, BFCache, stale queue, and lost acknowledgement. The legacy probe remains green only as a test of the compatibility adapter, never as native-exit evidence.
- **Post-ship test:** execute one un-retried production-like test-scope run after each relevant manual function deploy and Pages release; deliberately fault the canary once and verify immediate owner notification. A source-string/unit test or intercept-only boot test does not satisfy this gate.
- **Client sees mid-flight:** nothing changes. This is test and monitoring work against the permitted test scope; no live client writer, flag, link, or route is changed.
- **Who acts:** QA/workflow owner, monitoring owner, frontend owner, and manual Edge Function deploy owner.
- **Undo:** retain both test lanes. If the new canary is faulty, repair the test without using a provider green run as a substitute for product evidence.

### Gate E1 -- introduce a dual-format native compatibility protocol for stale clients

- **What changes:** server-side compatibility receivers accept the current native payload and every deliberately supported legacy Calendar/Samples queue shape, old Submit receipt, and pending card-materialization job. They preserve the original request/comment identity when present and return one durable native receipt. Add a privacy-safe build/contract epoch header/metric, but do not make an active client share link fail merely for being old.
- **Why it precedes endpoint, flag, or credential retirement:** Pages is globally published while Edge Functions are manually deployed, and old tabs defer reload while dirty. Removing a legacy route first converts an ordinary stale tab into a lost client action.
- **Gate:** a held prior bundle, seven-day cache, BFCache restore, offline-to-online retry, malformed/failed routing-flag read, browser-persisted legacy comment/status intent, old Submit receipt, and provider-blocked legacy endpoint all reach a native compatibility result. An incomplete legacy Submit card job is reconciled to a native visible card or an explicit preserved native triage receipt; it is never silently removed. A final mandatory refresh is valid only after the action is server-checkpointed and the same page shows a native pending/retry/recovery state; it may not be a generic `401`, blank screen, dropped draft, or manual-recreate instruction.
- **Post-ship test:** use the designated test scope to exercise Calendar and Samples load, approve, tweak, comment, create where offered, reload, back/forward restore, two-device handoff, and dirty-dialog update. Validate browser UI, receipt, rendered native state, and no provider request.
- **Client sees mid-flight:** their existing URL, visible controls, and saved intent continue to work. If a format is being retired, the page keeps the action visibly pending until native acknowledgement and then gives a safe refresh path; it never silently removes the control or rejects the link.
- **Who acts:** frontend/function owners and manual deploy owner. Do not change n8n without the owner's explicit same-window authorization.
- **Undo:** leave both native protocol versions active and turn the new preference off server-side. Do not undo by expiring clients, deleting queues, or restoring a provider fallback.

### Gate E2 -- prove action conservation, concurrency, and materialization

- **What changes:** every offered client action gets a server-owned idempotency key, intent fingerprint, terminal receipt, and event/source readback; Calendar/Samples/Submit materialization becomes a server ledger rather than browser-only local storage. Preserve browser queues only as recovery clients until the server record is terminal, including old provider-shaped Submit receipts whose provider work never finished materializing.
- **Why it sits before comments, creation, and stale-route retirement:** a native call that returns `200` is not enough if its response is lost, a retry duplicates it, a late tab overwrites it, or a committed deliverable never becomes a visible card. F134 already identifies browser-only materialization as insufficient: `docs/ops/MONITORING.md:91`.
- **Gate:** double submit, lost response after commit, two simultaneous tabs, stale version, reconnect, link change, and browser-storage loss yield exactly one durable action or a visible conflict/retry bound to the same receipt. The server independently reconciles accepted intake to card/component/event/thread visibility; no committed item may age without an owned state.
- **Post-ship test:** after the test journey, query protected aggregate receipt and job counts, then reload client and staff surfaces from a second browser. Deliberately interrupt each phase after native commit and after card materialization. Verify that retry resolves rather than duplicates.
- **Client sees mid-flight:** an action is either visibly saved once or visibly pending under its original wording. The client never sees success followed by disappearance, nor loses a typed note because the response was lost.
- **Who acts:** database migration owner, frontend/function owners, monitoring owner, and manual deploy owner.
- **Undo:** retain immutable receipts and native jobs; route new calls through the last known native-compatible handler. Do not delete stuck jobs or browser queues as an apparent rollback.

### Gate E3 -- migrate complete comment threads before retiring either transport

- **What changes:** build a per-thread crosswalk and append-only import/dual-read contract for legacy card comments and canonical native comments. It preserves root/reply relation, audience, author attribution, created/edited/deleted/resolved state, attachments, revision, idempotency identity, and delivery receipt. The browser uses a deterministic merged view while both stores exist; it must not treat an empty canonical response as an empty thread.
- **Why it precedes linkage repair and provider cleanup:** fixing a card-to-deliverable crosswalk before its legacy comment thread moves can flip a reply/edit/delete to a canonical route with no parent. It turns a data repair into a visible communication failure.
- **Gate:** every thread is classified as native complete, legacy-only and still served, archive-only and retrievable, or explicitly owner-approved retention exception. The 20 observed one-way slots and all newly detected ones are zero before legacy thread retirement. Protected aggregate parity proves root/reply/version/audience totals agree; an unreadable protected table is never treated as zero.
- **Post-ship test:** an actual test share link adds, replies, edits, resolves/unresolves, deletes where offered, refreshes, and retries after a dropped response on linked and unlinked Calendar/Samples cards. Staff performs the reciprocal operations and both audiences read the correct rendered thread.
- **Client sees mid-flight:** the existing thread stays visible and writable. A failed canonical leg falls back to the preserved compatible representation or shows one clear native pending/retry state; it cannot collapse the thread to empty or ask the client to contact Linear.
- **Who acts:** database/archive owner, comment-reader/writer owner, frontend owner, and manual deploy owner.
- **Undo:** retain source representation, crosswalk, and dual-read until a validated parity soak completes. Imported native comments are additive and must not be deleted to roll back.

### Gate E4 -- make lifecycle, archive, hierarchy, and approval state native facts

- **What changes:** introduce a canonical native lifecycle/tombstone model separate from operational status, with reversible archive/unarchive receipts. Classify every raw-provider-terminal/native-live conflict before choosing a canonical state. Replace UI dependence on raw archive/delete markers only after that classification. Make Calendar and Samples archive one server transaction (or an explicit durable compensating workflow) covering the card plus every component; preserve parent/child aliases, native ownership, assignee eligibility, due-date/time-zone policy, and historical deep-link redirect behavior.
- **Why it precedes raw-field deletion and staff-source replacement:** raw Linear markers currently decide whether SyncLinear displays a row. Calendar and Samples currently have non-equivalent archive paths, and `production-write` has no archive/restore operation. Reversing the order can resurrect old rows, hide live work, or remove a client's content while staff still owns work.
- **Gate:** every provider-terminal/native-live row has a private disposition/receipt; zero active-service conflicts remain except explicitly quarantined archive records that no offered surface renders. Archive/reopen tests show card, both components, Workload, Production, Calendar/Samples, assignee, due date, and Activity converge atomically or expose a durable compensating state. Approval is bound to a content/artifact revision so a later media/caption change visibly reopens/requeues review (F113: `docs/ops/MONITORING.md:80`).
- **Post-ship test:** use staff archive/reopen on Calendar and Samples, then use a client share page to verify no offered content disappears incorrectly and an altered approved artifact requires the configured renewed-review outcome. Exercise a parent/child with a prior deep link and date boundary in the business timezone.
- **Client sees mid-flight:** no client card disappears, reappears, or retains a stale green approval merely because a background migration runs. A card continues using the current read representation until its native lifecycle receipt is complete.
- **Who acts:** database migration owner, frontend/function owners, Workload/Production owner, archive owner, and manual deploy owner.
- **Undo:** keep raw markers, aliases, and archive records read-only alongside the new native lifecycle state. Revert a renderer preference; never erase tombstones or historical identifiers to undo.

### Gate E5 -- replace provider validation and external ingress with native work creation

- **What changes:** replace provider project/team/state/assignee validation with native registry and mapping decisions; make every Submit, Create Post, urgent tweak, notification, and inbound request produce a native object plus visible card/thread and durable delivery notification receipt. Disable creation directly in SyncLinear/Production as it is today; sub-issue creation remains Calendar-only.
- **Why it precedes B1/n8n/provider shutdown:** B1's documented stray-catcher is still an insert-only way for direct Linear work to become product work. Intake/notification acknowledgement can precede the visible Calendar job. Turning off the producer before replacement would let a client request be accepted but never materialize.
- **Gate:** provider-blocked test-scope Submit/Calendar/Samples creation produces exactly one native work object, the correct allowed client/staff card, parent/component relation, and notification receipt. The legacy Submit form remains a native-compatible receipt/materialization adapter throughout the old-client horizon; it cannot return accepted/received without a terminal native materialization state. Provider-admin census accounts for every person, personal/service token, OAuth app, webhook, automation, external integration, and manual practice capable of creating/updating Linear work; a defined observation period records zero strays before the provider account is disabled. The live external census is **UNPROVEN** until that owner review happens.
- **Post-ship test:** test each creation entry point, unknown-assignee and notification-failure/retry cases, duplicate request, and a Calendar-only subissue attempt. Confirm the client sees an honest native status, not a provider-derived promise; staff sees one actionable native item.
- **Client sees mid-flight:** the same submit/review/create affordance remains. It either becomes a visible native item with a receipt or displays a recoverable native pending/error state; it cannot claim acceptance while leaving no work for staff.
- **Who acts:** frontend/function/database owners, notification/integration owner, provider administrator, and n8n owner only with explicit authorization.
- **Undo:** keep native registry/mappings and receipt retry. Before provider shutdown, restore only a captured native-compatible integration graph; do not recreate work in Linear as rollback after the client has been acknowledged.

### Gate E6 -- treat raw-read/RLS and link/auth modernization as a later, independent release

- **What changes:** first add a scoped, server-mediated read projection in shadow mode; measure own-client success and cross-client/direct-anonymous denials without logging rows. Only after link migration and exact behavior proof may raw anon reads, CORS, or frozen writer authentication be changed under separate owner authorization.
- **Why it is intentionally outside the Linear flip:** the current public-data boundary may be undesirable, but revoking it while the SPA still reads those tables can make a client page empty. Re-gating the frozen writers is explicitly prohibited without owner approval and confirmed re-issued links.
- **Gate:** a fresh and old approved share link each reads only its permitted projection; negative cross-client/direct-anonymous probes deny; all client Calendar/Samples/notes flows remain populated; a privacy-safe failure monitor is green through a soak. This gate is **not** required to revoke Linear credentials if E0--E5 prove the current client contract works provider-free.
- **Post-ship test:** use test-scope old/fresh links, cross-client negative attempts, direct raw-read negative attempts, cache/BFCache reload, and every offered client write. Never record a slug, token, content, or URL in CI output.
- **Client sees mid-flight:** no content or control disappears. Scoped reads run in shadow until their response parity is proved; the existing compatible reader remains available.
- **Who acts:** security/database/frontend owner plus explicit owner decision for any writer/auth/link change.
- **Undo:** route back to the existing read contract while retaining scoped projection telemetry. Do not rotate/reissue links without the separately required owner approval.

### Gate E7 -- neutralize control-plane ingress, egress, restore, and late callbacks

- **What changes:** before removing a provider credential, install a no-egress sentinel across browser, Edge Functions, Actions, n8n, outbox, and restore paths; classify all current outbox/parity/replay rows; capture provider-admin integration inventory; and change inbound handling to a signed, disabled acknowledgement that records no native mutation during its drain horizon. Add a retirement transform/egress poison to every restore runbook so a database or source restore cannot re-arm Linear.
- **Why it precedes revocation:** normal `off` does not cover replay/parity, and deleting a credential before a webhook drain converts late callbacks into retries/noise rather than observable closure. Linear documents retries after 1 minute, 1 hour, and 6 hours for a non-200 or slow webhook response: [Linear webhook documentation](https://linear.app/developers/webhooks). A source search cannot see provider-admin or member-connected integrations.
- **Gate:** all active trigger-to-sink paths are inventoried; all outbox/replay rows are terminal or explicitly retained in a provider-free archive; no-egress sentinel records zero after two clean scheduled intervals and the agreed callback drain horizon; a scratch restore with provider credentials absent produces zero attempted egress. Provider admin confirms integrations/webhooks/automations/OAuth/API keys are disabled only after the drain evidence.
- **Post-ship test:** deliberately trigger the permitted test-scope workflow/queue/recovery paths, test a signed disabled inbound delivery, run a scratch restore, and inspect aggregate no-egress/readback results. Prove an alert reaches a named owner.
- **Client sees mid-flight:** nothing changes because E1--E5 already route their existing actions through native compatibility. A late provider callback is acknowledged without changing a client-visible record.
- **Who acts:** provider administrator, Edge Function deploy owner, repository/workflow owner, n8n owner with explicit authorization, database/backup owner, and monitoring/on-call owner.
- **Undo:** before credential revocation, restore captured native-compatible workflows only. After egress poison is committed, a provider restore requires a separately approved emergency change, not an automatic rollback.

### Gate E8 -- certify historical data, assets, and evidence separately from rows

- **What changes:** create a private Exit Archive manifest that includes provider API pagination/cursor completeness, active/archived/deleted work, hierarchy, labels/projects/cycles/templates, comments/replies/reactions/edit history, events, attachments/inline assets, delivery/audit metadata, integrations/configuration, and all native crosswalk/receipt/outbox state. Rescue or disposition every external byte and preserve a retrieval reader before account deletion.
- **Why it precedes account cancellation:** CSV field coverage and database row counts do not include attachment bytes or prove a client/staff reader can retrieve history. Workspace deletion is eventually permanent, so this cannot be a post-cancellation repair.
- **Gate:** each class has count, hash, page/cursor-complete, source/destination, retention, and retrieval evidence; the asset pointer census has no unclassified provider host/type; an isolated restore with provider egress blocked retrieves representative history, attachment, comment thread, lifecycle receipt, and alias. If provider audit history is available, retain the exit-period evidence before its retention expires; availability is **UNPROVEN**.
- **Post-ship test:** authorized operators retrieve historical work, reply chain, audit event, asset, and mapping from the private archive; test client and staff journeys load every asset/history class still displayed by the product with provider access blocked.
- **Client sees mid-flight:** nothing changes. Export/rescue is additive; a client continues seeing the currently served asset until its native/rehosted equivalent is verified.
- **Who acts:** provider administrator, archive/database/storage owner, legal/retention owner, and client/staff surface owners.
- **Undo:** archive copies and manifests are additive. Provider workspace cancellation/deletion is not a practical rollback and remains a separately authorized final action.

### Gate E9 -- dark switch only after a compatibility soak

- **What changes:** stop new provider intent creation, retain provider-free compatibility receivers through the observed stale-bundle/cache horizon, deactivate -- never delete -- retired operational graphs under their owner-approved procedure, revoke credentials, then perform a post-revocation soak before a separately authorized account cancellation/deletion.
- **Why it is last:** credentials are an enforcement mechanism, not an implementation plan. A credential-first switch converts every still-hidden dependency into a client/staff incident.
- **Gate:** fresh, held-prior-bundle, dirty-tab, offline/reconnect, BFCache, and second-device client journeys are green across the agreed horizon; all receipt/lifecycle/thread/materialization monitors are green; no-egress is zero; current final archive delta reconciles; a test-scope failure alerts a human. Account deletion waits until the owner signs the irreversible checklist after the post-revocation soak.
- **Post-ship test:** rerun the complete client experience (content, approval, tweak, note/comment, and every offered action) plus staff Calendar, Samples, Workload, Submit, Filming, Production, and Kasper paths with provider access disabled. Observe every scheduled cycle and one manual/recovery attempt.
- **Client sees mid-flight:** nothing changes. A client with an old but supported page keeps a working native-compatible route. If there is a transient failure, the page preserves last-good content and shows durable native recovery, never a Linear remedy or an unexplained empty/success state.
- **Who acts:** all prior owners plus the owner for credential revocation and a separate owner authorization for workspace cancellation/deletion.
- **Undo:** before cancellation, retain native compatibility and restore only provider-free operational artifacts. After cancellation/deletion, recovery is the tested archive/SyncLinear procedure; it is not Linear reactivation.

### New watchers required by E0--E9

| Watcher | What it proves | Alert and minimum implementation |
| --- | --- | --- |
| Client protocol-population monitor | Old/new compatible client contracts are accepted, and no stale client action falls into a provider route. | Count only build/contract/surface/result; alert on legacy provider selection, client `401/403`, unsupported active client action, or forced refresh before receipt. Requires server metric plus existing scheduler/synthetic. |
| Independent native-only client canary | The serving product, not an intercepted/source-only test, loads and persists every offered client action without any provider endpoint. | Run every 5--15 minutes and after relevant release; first attempt failure pages immediately. Test provider endpoint is network-rejected and all results are test-scope/aggregate only. |
| Action-conservation monitor | Every user action has one intent, source/native result, event, and visible materialization. | Alert on duplicate fingerprint, receipt-age breach, write/event/card/thread mismatch, or stuck materialization. Requires protected aggregate query; no body/token/card identifier in alert. |
| Legacy-client debt monitor | Browser-held legacy queue/Submit receipt/job traffic is being converted to a terminal native disposition rather than silently retried, dropped, or sent to Linear. | Alert on old-route invocation, unclassified legacy payload, queue/job age, provider attempt, discarded materialization, or accepted-without-native-card. Requires privacy-safe endpoint/receipt counts; it cannot depend on reading a device's local storage remotely. |
| Thread-parity monitor | Legacy/canonical comment representations have a safe disposition and no one-way thread grows. | Alert on new transport split, root/reply/audience/version count mismatch, empty canonical thread after legacy content, or client/staff operation refusal. Existing check is a starting point; protected canonical totals require new aggregate reader. |
| Lifecycle/archive monitor | Native lifecycle, card state, components, Workload, and approval revision agree. | Alert on provider-terminal/native-live conflict without disposition, incomplete archive/reopen compensation, client-card link to hidden work, or stale approved revision. Requires aggregate database check plus UI synthetic. |
| Creation/notification monitor | A client-visible acknowledgement maps to one visible native item and one staff notification/retry state. | Alert on accepted-without-card, missing parent/component, notification age/failure, or direct Production subissue creation. Server receipt/job query plus existing test-scope synthetic. |
| Provider ingress/egress and restore sentinel | No code, scheduler, webhook, integration, queue, or restore reaches Linear after its cutoff. | Alert on first request/execution/outbox/replay/disabled callback; include route/category/time only. Requires provider-admin log review and n8n export/telemetry owner access. |
| Asset/archive retrieval watcher | Client/staff-visible historical material survives without provider egress. | Alert on pointer classification gap, manifest/hash/page failure, external-byte failure, or archive reader/retrieval failure. Private scheduled retrieval is sufficient; no public artifact. |
| Alert-delivery drill | A monitor reaches a human rather than merely producing a failed job. | Deliberately fail only the designated test scope and require a verified notification before advancing a gate. Existing notification coverage is **UNPROVEN**. |

### Additional unknowns that remain blocking, not assumptions

- Exact deployed source/JWT/auth/CORS posture for manual/no-CI functions, especially frozen client upserts, verifier, production gateway, archive, and comments readers.
- Current n8n published graph versions, queue/retry state, external integrations, and owner-side credential consumers.
- Provider-admin tokens/OAuth applications/webhooks/automations/member-connected integrations/manual practices capable of creating or editing work.
- Protected counts and terminal state of receipts, outbox/replay, canonical comments, recovery jobs, mappings, archive records, and asset inventory.
- Whether every historical browser route/share-link shape has a provider-free compatibility response.
- Exact retention/availability of provider audit history and complete provider export pagination.

All six are **UNPROVEN** from public source and anonymous reads. Each needs a privacy-safe, owner-authorized read-only preflight; none may be replaced with an inference from a clean repository string scan.
