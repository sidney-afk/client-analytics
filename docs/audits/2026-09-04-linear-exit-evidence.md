# Final-arbiter evidence and per-surface ledger — 2026-09-04

This is evidence, not an execution plan. The sole current sequence is [GO_LIVE_CHECKLIST](../independence/GO_LIVE_CHECKLIST.md). The original Phase1 snapshots below are preserved after their seal. All source references bind to 5765cfe80b7ca9844bab79a55fd75784bf9cb693 unless another hash is explicit; the two-line copy-only remote delta to85a1baf does not change the traced symbols. Machine results are in [measurements](2026-09-04-linear-exit-measurements.json).

## Current interpretation and Phase2 corrections

1. Joining serving proof to the source map establishes production-write v66/outbound v47/inbound v46/comments v24/archive v8 source behavior at20:56Z. Generic source-only disclaimers in sealed component reports must be read with that later join. Exact source does not prove all real request outcomes.
2. The sealed backend row groups ordinary status/due/assignee writes. Phase2 found an omitted conditional prerequisite: production-write ASSIGNEE_ELIGIBILITY_FLAG2460 reads syncview_runtime_flags2467–2469; absent2473 or failed2476 returns providerMappingRequired:true. assigneeEligibilityContext2509 calls assigneeProviderPool2481; non-null assignments validate before commit, while null unassign skips at2540. The complete17-row live flags snapshot contains no production_assignee_eligibility key. Treat assign/picker as provider-dependent in this observed state. This correction deliberately does not rewrite the independent snapshot.
3. Fresh classifier reruns21:10Z:31 actionable Calendar linked/no-native component candidates and13 real Workload visibility exclusions. A single default graphics cutoff does not prove four were created after their respective team flips; the scripts omit targetless/invalid-native slots and other classes. Default Workload baseline13 would pass with13 omissions.
4. Current serving workload-linear v6 differs materially from newer source authority guards. Frozen client writer differences are intentional. Do not classify every mismatch as a defect.
5. The Linear account query21:06:52Z reports cancellation scheduledSeptember15 at13:46:53UTC. It proves neither full export completeness nor perpetual paid/read-only retention. createdIssueCount19321 is provider metadata; the independently fetched extant-access lower bound is251, not a complete census.
6. Production comments POST can record audit/budget writes. This audit did not call it, client writers, mutation RPCs or n8n webhooks. The strict read-only methods are documented in the snapshot; later TEST probes require separate authorization.
7. n8n full published-graph census remains incomplete:97of129 succeeded before connector loss. Eight direct-provider candidate graphs were found, six active; structural reachability does not evaluate branch gates or dynamic credential/host indirection. External execution, zero-egress and real client notification delivery remain UNPROVEN.

## Sealed Phase1 snapshot

# Sealed independent Phase 1 snapshot

Sealed before opening the withheld historical strategies, PR #1257, reviews, specified repair entries or GO_LIVE/Track-B specifications.

Original current remote source: 5765cfe80b7ca9844bab79a55fd75784bf9cb693. Pages bytes matched this source exactly at 2026-09-04T20:54:24Z; SHA256 2d83eb13cc0299d8d468bd7cf4f4053e1f52d0569108910fe70f23185b0a639e. Remote advanced during investigation to 85a1bafc268722eda310d8f1af94c9686585bbe0; reviewed full diff consists only of two onboarding explanatory-copy replacements at unchanged lines 21183/21331, no dependency delta. No claim is made that a continuously moving main stays pinned.

## Independent decision

NOT READY for Decision A. Native team authority is operating but native intake/append/component-fill validate provider projects, labels read provider metadata before writes, Workload membership/refresh and tweak readers remain provider-dependent, runtime-flag failures return to legacy transports, and serving workload-linear lacks repository authority guards. Samples masks failed fallbacks as empty success; actual-source offline probes reproduce this while Calendar v2 rejects the same responses. Frozen client-writer serving differences are intentional and must not be replaced with authenticated repo writers.

## Independent live measurements

Read-only measurement window 2026-09-04T20:54:06Z–20:59:21Z. Both teams syncview; outbound live; inbound/parity/client-comment gateway enabled; public intake enabled; auth permissive. Four routing cohorts equal all43 active roster entries (42 client-kind,1 test), zero missing/extras. All35 functions ACTIVE, platform verify_jwt=false; source closure comparison30 exact,5 different,0 errors. Five differences:2 intentional frozen auth removals;1 material workload-linear authority-fence omission;2 formatting/comment-only equivalences.

6330 deliverables;1674 batches;9937 Calendar cards;6655 Samples cards;3538 Workload mirror rows. Full paginated outbox6567:5595 written,863 skipped,95 stale,14 failed. All14 failed are TEST,7 per team;0 real normal pending/failed/shadow_ok. This does not clear stale rows or action conservation. Native comments14607;mutation receipts465;card links615;import conflicts0. Zero archive-asset-ref inventory rows: this means asset preservation is unproven, not that there are no assets. Counts are service-role global counts, not proven render-eligible/client-visible populations; sequential reads are not a transactionally consistent cutoff snapshot.

Live n8n catalog129 workflows,93 active,40 credential entries.97 graph reads succeeded,32 failed;8 known direct-provider graphs,6 active. Published graph structural paths include video-form/graphic-form/project picker/linear-issues, add-to-calendar comment read, linear-add-comment, linear-set-status, editors-week, linear-issue-statuses and send-urgent-slack. Graph reachability does not prove branch execution; dynamic hosts/code, unpublished/manual callers and inaccessible graphs remain UNPROVEN. The connector subsequently disappeared in a targeted followup, leaving a concrete external inventory gap. No workflow was executed or edited.

## Evidence rules and limitations

AGENTS.md was read completely first; frozen anonymous writers remain open. Current maps were read as navigation; historical plans withheld. Existing ef-fingerprint source was inspected before its read-only comparison; it computes exact repository-local import closure plus live source/JWT/entrypoint equality, not request routing, SQL/RPC deployment, external npm/HTTP modules or client journeys. Supabase service/Management credentials were used only in process memory for GET/HEAD; Linear GraphQL POSTs were read-only queries. No client endpoint that records reads, no write probe, no workflow dispatch, no SQL migration, no flag change, no merge or deploy occurred. Secrets, client identifiers and provider source captures stay outside repository/report. No current write-path or alert-delivery drill was executed; those remain future owner-designated TEST gates.

Artifact hashes below seal the independent evidence. Later findings belong in Phase2 or the final strategy and must not rewrite this snapshot.

|Artifact|Bytes|SHA256|
|---|---:|---|
|phase1-client-surfaces.md|20178|8ae4ec30cd8beae499ebd6d123156d082a0a37f318036a581d67c485249e1785|
|phase1-staff-surfaces.md|13538|4b25cd28a9881861c395782d320922fb80f8dc7b5c37be9e6ec02b70cc450735|
|phase1-backend-paths.md|16589|f0c1836d966b423263d8692cdeb789eb8c02a61555dd7f77bec7603347d962ca|
|phase1-serving-deltas.md|5102|93b2adc22e75b8687b1bcc2dfb490bd3296d96bc8adafa5028eb3fc0661d9405|
|phase1-n8n-targeted.md|3759|c4e41d1e3e513607dcb94c6e69db0e76a22c7f425f833ff3dd08e4033b25022f|
|phase1-reader-probe.cjs|1651|108dc9b25b52d002a9173b406b7e97b527de156c75db8902dfd18eff9f7dcfb8|
|phase1-live-measurements.json|14304|885e346cbd145d055d7718d4346da10b879a49437538a4adf47f40a821fe94de|
|phase1-live-accounting.json|4312|3b529cdf84b07bf0d34806914570040da00093ce010bc972dca64799fa3db4bb|
|phase1-serving-proof.json|28020|07bd5ba77d9c6cb09666c3aa6cba7d90aefc81d7028471704fe1a11975c7d5a5|
|phase1-serving-captures.json|2895|eddd43031bb13495602849dbbbdfa95f70781f3e16e1a6335daef6d946937605|
|phase1-n8n-inventory.json|49379|b3a6ecd50b6a3dd9dd52c4d73a1deb31c63146e5acdb6369806b573af05bf3fc|
|phase1-n8n-traces.json|4436|ee43f4a1dfd9f06bc27ca18ce08901bf846d1e0cd27f437afd92702ea438a4f7|


## Component evidence: clients and review surfaces

# Independent Phase 1: client, Samples, Calendar and review surfaces

Evidence anchor: `sidney-afk/client-analytics` remote source commit `5765cfe80b7ca9844bab79a55fd75784bf9cb693`. Captured 2026-09-04. All `index.html` lines below refer to this exact commit. This file was written before reading any withheld strategy/review. No production requests, mutations or backend tests were made by this investigator. Root investigator owns serving-source and live-flag/data measurement. Unless separately joined to root evidence, every deployment/live claim below is **UNPROVEN**. Local extracted-function tests are synthetic source proof, not production frequency proof.

Read first: complete AGENTS.md; REPO_MAP.md; repository retrieval/current-truth maps; APP/ENDPOINTS source maps; EF_DEPLOY_MANIFEST from phase0 because it is outside the sparse checkout. AGENTS frozen writer rule expressly keeps calendar-upsert and sample-review-upsert open/tokenless. No proposed exit implementation may turn these into a new client authentication gate. Production/SyncLinear must not regain issue/sub-issue creation merely because dormant source remains.

## Independent decisive findings

1. **Samples can turn fallback failure into successful emptiness.** `loadSxrCards` 61176 -> `_sxrFetchPosts` 61158 -> `_sxrSupabaseFetchAllRows` primary REST `sample_reviews`; primary failure except AbortError -> `SXR_GET_URL` 61171 -> `resp.json()` -> `{ok:true,posts:(j.items||j.samples||j.posts)||[]}` 61173. No HTTP-status check, no `j.ok` check, no array or empty guard. `loadSxrCards` takes `result.posts||[]` 61208, adopts it at 61223, clears the error at 61225, writes the cache at 61226 and clears stale notice at 61228. Thus a primary failure plus valid JSON error fallback can erase the visible list and poison its cache. Actual source extracted into local VM proves HTTP500 JSON error, HTTP200 `ok:false`, and HTTP200 empty posts each return `ok:true,row_count:0`. This is a client and staff continuity risk even before any provider cutover. Whether live n8n currently responds in these shapes is **UNPROVEN** here.
2. **Calendar v2 has the stronger guard already.** `_calV2FetchPosts` 34622 -> REST `calendar_posts`; fallback checks HTTP 34634, `json.ok` and array shape 34651, and rejects zero posts 34652. Same actual-function test throws on all three cases above and accepts one-row valid fallback. Do not propose this guard as missing from v2. But `_calV2Ready()==false` direct lane at 34983-34991 remains different: it parses webhook JSON directly and accepts `json.ok` with `json.posts||[]`. `_calV2Enabled` 34524-34539 defaults true but honors sticky `?v2=0`/`false`; current client URL verification may reject these query additions, while historical bundles/staff sticky state remain a required separate test.
3. **Healthy flag enrollment is not independence.** Calendar writer enrollment failure clears the EF set at 25086-25098, Samples writer enrollment failure clears it at 60523-60535, shared reroute/comment flag failure clears gateway cohort and client-comment gateway at 25140-25151. Native-status routing uses the shared gateway cohort, source-row persistence uses a different per-surface EF cohort. Each must be tested independently at boot, refresh, realtime flag changes and recovery. A read failure can send a user back to n8n even after normal enrollment is complete.
4. **Staff Create Post falls back to cards without native work.** `addCalBlankCard` 40970-40990 and `addSxrBlankCard` 62223-62235 select native creation only if shared reroute enrollment succeeds. False/missing/unreadable flag takes `_calInsertLocalBlankCard` 39914 or `_sxrInsertLocalBlankCard` 62214. The sample blank shape 62200 has neither native IDs nor Linear IDs. This is a work-nowhere risk, not proof of Linear-only work. Client Collaborative-mode suggestions deliberately take the calendar-only path 40975-40978 and must be preserved as suggestions. Healthy native path is `_calOpenNativePost` 40743 -> `_calSubmitNativePost` 40830 -> `intake_create` payload 40916 -> `_resumeNativeIntakeJob` 40940 -> `_runNativeIntakeJob` via 47331-47350. Server validations and serving evidence belong to backend investigation.
5. **Comment sources and actions differ materially.** Calendar client canonical reader is intentionally not available: `_prodCanonicalCommentGate` 54812-54894 requires validated crosswalk but returns `linked:false` for Calendar client surface at 54875. `_prodVerifiedClientCommentSurfaceContext` 54145-54165 requires current verified Samples-review capability. Samples client exact valid linked threads can be canonical; invalid/unresolved crosswalk and legacy-retained coverage fall back to card history. Client never seeing less is an existing enforced rule, not permission to declare all comments migrated.
6. **Do not rely on the current-truth map's old update-banner summary.** Current `appUpdateNudge` 76211+ skips loopback only, not `?prod=1`; it includes `app_version_floor`, 4-hour escalation, document Last-Modified comparison and unsaved-work deferral. This is current browser protection, not server-enforced cutoff or proof no old bundle survives. Old code lacks newer protections, and a dirty active page can defer the wall by design.
7. **Staff Linear import is reachable despite later sealing.** More -> Import menu 35906-35910 -> `openCalLinearImport` 33328 -> `_calLinearImportFetch` 33372 -> `LINEAR_SUBISSUES_URL` 33381. Applying import checks live seal only later at 33518 and can refuse after successful provider fetch. Bulk Linear sync similarly fetches first then applies authority seal at 33829. It is wrong to call these whole paths dormant merely because post-flip import application is refused.
8. **Excel import bypasses native creation.** More -> Import from Excel at 35908 -> `_calRunImport` 34354 -> builds new source-only post objects at 34373 (no deliverable IDs) -> `_calBulkUpsertPosts` 34384. No native intake is in this actual path. Classify imported planning rows versus actionable production requests; do not silently invent native work for every historical row.

## Per-surface dependency map

| Surface / entry | Verified source chain and classification | Linear reachability and failure effect |
| --- | --- | --- |
| Client Calendar view, staff Calendar/SMM sheet/review | `mountCalendar` 29552 / `mountCalendarEmbedded` 29772 -> `loadCalendarPosts` 348xx/34976 -> `_calV2FetchPosts` 34622. Primary canonical source-card read is Postgres; fallback is n8n. | Healthy native read does not call Linear from this chain. v2 fallback provider dependence requires live n8n inspection. With fallback unavailable, preserved cards/stale notice or a visible retry is preferable; v2 empty guard already exists. Current old-lane cold path remains separate. |
| Client Samples / staff Samples | `mountSxrView` 60730 / `mountSxrClientView` 64542 -> `loadSxrCards` 61176 -> `_sxrFetchPosts` 61158. | Primary REST works without provider; fallback is n8n and can produce false empty state as above. Real-client visibility filtering and asset retrieval still need synthetic/browser and live aggregate proof. |
| Client approval / staff routing (Calendar) | Review button -> `_calReviewApprove` 43484 -> `_calReviewApplyApprove` 43526 -> pending status edits -> `_calFlushCardSave` 39179 -> `_calPushStatusToLinear` 39328/39352 -> `_writeUiUseGatewayWhenReady` 25555. Gateway branch -> `_writeUiGatewayWithRepair` 27106 -> `_writeUiGatewayPost` 27129 -> production-write 27182, requires `native_committed:true` 27194. Then source upsert 39460-39470. | Conditional canonical write gate, not just enrichment. Gateway failures prevent source success; native commit plus source failure leaves source-repair debt. Shared reroute false routes legacy n8n Linear-set-status and browser outbox instead, sometimes after source save. Caption/title never have work-item IDs and are source-only. |
| Client approval / staff routing (Samples) | `_sxrReviewApprove` 63464 / `_sxrReviewApplyApprove` 63502 -> `_sxrFlushCardSave` 62605 -> `_sxrPushStatusToLinear` 62722/62746 -> gateway/legacy selection 65230-65274 -> per-surface upsert. | Same two-store/conservation concern. N/A/non-work-item behavior must stay distinct. Linked/unlinked and Video/Graphics need separate cases. |
| Review-panel plain notes | `_calReviewComment` 43614 (button at 43315) and `_sxrReviewComment` 63537 -> card comment array -> per-surface source-row save. | These handlers contain no `_calPostLinearComment`/`_sxrPostLinearComment` call; a nearby helper does not prove all notes call Linear. Card durability is primary here. Native history convergence needs explicit comparison. |
| Notes modal roots/replies, client change requests | `_calAppendComment` 45126 -> component selected/inherited -> `_prodCommentAddRoutesLegacy` -> `_calPostLinearComment` 45214 -> conditional gateway/legacy; then source array 45228 and `_calWatchNoteSave` 45245. Samples `_sxrAppendComment` 64334 -> `_sxrPostLinearComment` 64378; linked client canonical commit adopts native projection 64385-64398, other lanes persist legacy array 64405-64411. | Video/Graphics feed may be canonical, provider mirror or legacy-only according to exact card crosswalk and client gateway flag. Client Calendar can retain source comments while front-door gateway delivers to team. Removing provider without native team-delivery equivalence can leave client comment apparently saved but team missing it. |
| Review-panel tweak request | `_calReviewRequestTweak` 43655 -> locked action/journal -> comment commit 43817 -> deferred legacy/source barrier 43828-43840 -> associated status and source save. Samples twin 63556/63711. | Composite action has more than one effect: note, status, source row, receipt and team delivery. Tests must conserve all effects through timeout/replay/retry and not report overall success from only one receipt. |
| Edit / resolve / reopen / delete note | Calendar 45266/45297/45371 and Samples 64303/64416/64450. Canonical branch -> `_writeUiCommitCardCommentLifecycle` 32537 -> `_writeUiCardCommentLifecycle` 32437 -> production-write `comment` with action, exact version/timestamp and idempotency key. Resolve alone rereads full canonical thread and may rebase once 32476-32535. Unlinked resolve/reopen/delete updates card JSON/tombstones and source save only. | These are not all provider calls. Canonical linked mutations need native comment reader plus gateway; legacy lifecycle does not automatically mirror edit/delete/resolve to provider. Preserve tombstones, root/reply semantics and audience visibility. UI only offers edits when canonical/can_edit; do not invent a legacy edit feature. |
| Kasper Calendar review board | `_kasperRenderReview` 72791 -> `_kasperLoadReview` 72811 -> `_kasperFetchAllRelevantPosts` -> cross-client REST 73229, fallback KASPER_QUEUE_URL 73256 then per-client Calendar-get. Mutations `_kasperPersistPostWrite` 74141 -> `_calPushStatusToLinear` 74202 then `_calUpsertFetch` 74222; comments 74509. | Healthy reads native; degraded reads n8n. Native write is an actual gate and source materialization follows; stale cache cannot count as proof of complete queue. Provider-only legacy side effects and repair debt are conditional. |
| Kasper Samples review board | `_sxrKasperLoadQueue` 66344 -> `_sxrKasperFetchAllSamples` 66337 -> REST. `_sxrKasperApplyAndPersist` 66595 comment-first gateway 66643, status 66674, source save 66692, deferred legacy provider pushes 66695/66700. | No Samples-get fallback in cross-client reader itself. Atomic user intent still spans native and source stores. Source failure must preserve committed native intent and visible retry. |
| Calendar staff Create Post / Samples + | Exact chain in finding 4. Role verified before native dialog at 40753. Native intake uses durable lock/signature and materialization. | Actual native write gate may still require provider validation and receipt mirror; backend agent to prove. Legacy flag failure risks source-only blank work. |
| Client Collaborative suggestion | addCalBlankCard 40975-40978 -> `_calInsertLocalBlankCard`. | Existing client feature intentionally source-only. Preserve it; later accepted production work needs explicit native creation/linking contract. |
| Calendar Excel import | 35908 -> 34354 -> 34373 -> 34384. | Source-only data import, no native creation in this handler. Imported actionable components can have no canonical work, independent of provider availability. |
| Linear import / bulk link | 35909/35910 -> 33328/33596 -> 33381/33658 provider lookup, apply authority seal 33518/33829. | Reachable provider-dependent staff workflow; later seal makes an exposed dead end. Requires removal or native import/relink replacement; do not reopen forbidden issue creation. |
| Manual card Linear link / open link | `_calLinearSlotHtml` 37810 and `_sxrLinearSlotHtml` 61514 render existing provider links for staff, explicitly hide for clients. Nonempty commit 38198/61708 checks seal then `_calSyncStatusFromLinear` 38222 / `_sxrSyncStatusFromLinear` 61727 -> provider webhook. | Existing staff links remain clickable after seal; paste may be sealed. Optional provider enrichment/write follows manual action. Clear-link repair remains reachable and must not be removed before native linkage repair is complete. |
| Calendar archive | `_calArchiveOne` 41016 -> source upsert 41040, then `_calArchiveParkSubIssues` 41077 -> conditional status gateway/provider for each linked component 41097. | Archive can succeed while native/provider parking fails; it tells staff in that case. Must account for phantom work after source removal. This is a real cross-surface action dependency, not cosmetic text. |
| Samples archive | `archiveSxrCard` 62239 -> `_sxrArchiveOne` 62256 -> source upsert 62259. | No equivalent parking call in this source function. Does not prove backend event effects absent; live/backend behavior **UNPROVEN**. Source checks HTTP only, unlike Calendar JSON acknowledgment. |
| SyncLinear creation UI | `_prodCreateTopbarButton` 52334 -> `_prodCreateGateText` 52297 returns closed text immediately at 52302; dormant old gates/form remain below return. | Fresh creation unavailable by design. Do not count dormant create form as live provider reachability or reopen it. Root backend audit covers recovery path and direct handler defenses. |
| Old Samples route | `_resolveRetiredSamplesRoute` 20632 maps samples to sample-reviews; strict client canonicalization 60218 converts legacy v=samples after verifier; `_sxrEnabled` 60470 gives verified client capability priority over staff sticky opt-out. | Current route is SXR. Old `content_samples` implementation 19652 onward and `loadSamples` 20078 remain source/historical compatibility; not proof current clients use that backend. Historical cached bundles may, so inventory them separately. |
| Browser queues and stale bundles | Calendar Linear outbox key 30041; Samples Linear outbox key 60439; deferred tweak journals and source-repair refs 27106/43834; intake resume 47331; legacy card jobs key 47574. Legacy drains choose LINEAR_ADD_COMMENT_URL/LINEAR_SET_STATUS_URL at 32015/65069. | New-page success does not retire pre-existing local debt. Deletion of queue keys is not action conservation. Need minimum compatible transport/server cutoff plus safe queue translation or explicit reconciliation before removing old endpoints. |

## Native identity, history and assets

- `_writeUiNativeId` 25591 explicitly returns no native ID for caption/title. Do not try to attach their comments to Video as a convenience.
- `_writeUiClassifyTargetless` 25563 throws native_link_required for SyncView-authoritative team or TEST; unknown authority fails explicitly. A Linear-only card and a card with neither provider nor native work need different remediation; counts must distinguish them.
- Valid comment crosswalk is origin/team/client/card identity, not presence of a native ID. `_prodCanonicalCommentGate` 54821 and source comment 25601 explain the relationship; `legacy_retained` at 54839 preserves client history when canonical coverage is incomplete. Test a full visible multiset, timestamps, replies, audience, resolved/deleted state and attachments, not count alone.
- production-comments source contains native database projection and legacy identifier fields, not a direct provider fetch in that function; pinned deployment remains unproven here. production-archive reads `linear_archive`, `production_comments`, `linear_archive_asset_refs`, strips private provider URLs and gates rescue links. It is a staff reader, not evidence client history/assets are rescued. Files linked from current card asset fields, description bodies and comments need separate asset inventory with actual bytes/readability.

## Cosmetic and dead-end sweep findings

Client: card Linear linking controls already explicitly hidden (`_calLinearSlotHtml` 37811; `_sxrLinearSlotHtml` 61515). Still test error text from gateway/missing-link/failed media; do not assume a literal search identifies what clients see. Preserve readable existing media, note history, retry notices and all presented suggestion/review controls. Caption/title needs no provider terminology.

Staff: exposed Import from Linear and Bulk Linear sync are provider-dependent lookup then sealed apply; existing linked Linear icon opens provider; archive partial-failure guidance says move work in Linear at 41087/41111; old dormant creation labels and historical comments are separate from rendered controls. Remove/refactor exact rendered calls while preserving stable internal routing (`production` versus `linear`) and native IDs. Cosmetic changes are later than continuity repairs.

## Required proof before any exit conclusion

- Serving fingerprint for pinned production-write/production-comments/production-archive; no-CI calendar-upsert/sample-review-upsert/calendar-reorder/sample-review-reorder; current Pages body hash mapped to commit.
- Live private aggregate flags for each enrollment and native authority; flag-read failure synthetic test, not just SQL showing enabled today.
- Native-only test-client journey on current and old supported bundles: view, approve, plain note via each offered composer, request change, reply, allowed edit/delete/resolve/reopen, assets, collaboration suggestion, staff create/import/archive and Kasper queue.
- Linkage/conservation census: active and client-visible historical cards with provider-only, native-only, both, neither; false crosswalk; native and card comment coverage; pending request/receipt/source-repair/outbox counts, by action and oldest age.
- Owner-authenticated live n8n read proving what degraded reads and legacy writers call. Endpoint names alone do not prove provider dependence.
- Deterministic faults: primary reader denied, fallback error JSON/empty, HTTP timeout after commit, source write failure after native commit, cross-tab retries, stale flag cache, offline queue resumed after cutover, BFCache/client-route replacement, canary-linked and unlinked component states.
- Do not call current lack of observed errors a 0-dependency proof. Egress instrumentation must include webhook/credential/browser fallback and scheduled jobs, not only direct api.linear.app URLs.

## Local synthetic evidence

`phase1-reader-probe.cjs` outside repo extracts the actual two source functions and mocks primary failure plus four fallback responses. Results: Samples returns successful empty on HTTP500 JSON error, HTTP200 ok:false and HTTP200 empty; Calendar v2 throws all three; both return one row for valid nonempty fallback. It computes source return semantics only, cannot prove cache user experience, serving function version, live fallback response, provider availability or actual data loss. No network calls occurred.

## Scope limit

This bounded independent investigation covers the requested client/review/Calendar/Samples/browser scope. Root and sibling investigators cover Submit details, Filming plans, Workload, full server policy, schedules/external integrations and live measurements. No withheld plan was consulted. No source file or production state was changed.


## Component evidence: staff and work creation

# Phase 1 independent staff/intake surface evidence

Source snapshot: `5765cfe80b7ca9844bab79a55fd75784bf9cb693`. All line references below are `index.html` at that SHA unless explicitly stated. This review read AGENTS.md completely, including the frozen anonymous Calendar/Samples writer directive, and the relevant current truth APP/ENDPOINTS maps. No historical strategy documents, review branches or withheld OPEN_REPAIRS entries were read. Comments inside current source were not treated as proof when executable code disagreed.

This is **source-only evidence**. Deployed browser identity, serving Edge Function revisions, live flags, actual n8n API calls and live cohort counts need the root investigation. A called webhook with “Linear” in its name proves a browser dependency on that webhook, not by itself a serving Linear API call.

## Independent conclusion

Decision A is **NOT READY on current source evidence**. Workload's real base reader is still a provider-fed mirror with live-provider fallback/manual refresh; its native view is diagnostic. Submit retains legacy routing under flag/storage/recovery conditions. Filming plans are independently stored, but onboarding provisioning and several import/history paths require separate backend proof. Browser-held intake/card jobs can stop retrying and be removed, so conservation cannot be established by native row counts alone.

## Surface map

| Surface | Real entry and call chain | Reachability, ownership and flags | If Linear disappears tonight |
|---|---|---|---|
| Submit picker; staff and anonymous intake | `navTo('linear')` 20864 -> `renderLinearView` -> cold `fetchLinearProjects` 20867 -> POST `LINEAR_PROJECTS_WEBHOOK` 13796; then optional native `clients` REST 13812–13821. | Legacy project fetch is **always attempted when this loader runs**, even with native enrollment. Failure is caught; prior list is retained and native registry loading continues. Native names are included only for reroute cohort/pending native recovery. Presence of project text is not itself a provider write gate. | Enrolled clients can still be selectable when native registry/flag reads succeed. Cold legacy-only or failed-flag sessions may lose usable options; stale cached projects can remain. Provider outage need not block every picker invocation. |
| Submit creation, both teams and advanced single-team buttons | Buttons 45828/45845/45846 -> `submitLinearForm` 46560 -> `_submitLinearFormRoutedOnce` 46816. Native: `_linearIntakePending` payload 46934 -> `_resumeNativeIntakeJob` 46970 -> `_runNativeIntakeJob` 47199 -> `production-write` 47205 -> `_writeNativeSubmissionCardsToCalendar` 47233. Legacy: `_submitLinearFormLegacy` 46812 -> `_submitLinearFormOnce` 46572 -> `_linearAwaitCreate` 46474 -> per-team target 46291. | **Conditional canonical intake**, including anonymous `?intake=1` (46880 skips staff identity only there). Existing legacy receipt, localStorage read error, missing routing helper, or false cohort decision force legacy. Existing native job retains native recovery. Native response must attest `native_committed` and exact item mapping before card materialization (47002–47015). | Fresh native requests may still fail inside the serving gateway if it validates provider projects/users; backend proof required. Legacy requests depend on video/graphic webhook serving contracts. Failures preserve request for retry on live click; partial native commit is explicitly shown as missing Calendar cards (46990–46992). |
| Reroute flag degradation affecting Submit and shared writers | `_writeUiPrimeRerouteFlag` 25156 catches failed read, sets empty clients and failed marker 25162–25165; `_writeUiRerouteUseGateway` 25267 checks set membership; Submit false branch 46851 invokes legacy. | **Failure returns to legacy**, not a hard native default. `_writeUiHealRerouteFlag` 25185 can recover on subsequent lifecycle events; the failure interval still exists. | A provider outage combined with flag-read failure can select legacy transports even when the normal cohort is native. Healing is not proof that every intervening action survived. Native-only degraded-path canary must cover this exact timing. |
| Workload base list, foreground/manual refresh, new-work discovery | Route -> `initWorkloadView` 16144; loading settles `loadLinearIssues(force)` and plan projection 15210. `loadLinearIssues` 14407 reads browser cache; ordinarily `_wlV2FetchIssues` 14098 reads `workload_issues`; forced load calls `LINEAR_ISSUES_WEBHOOK` 14436–14440. | Provider-fed **canonical membership dependency** persists. Mirror empty/error falls through; `?wl2=0` is sticky per browser (13934–13948). Force bypasses mirror altogether. Existing snapshot/cache can mask outage (15217–15220). | Warm view may keep old work, while new/changed work stops appearing. Explicit refresh fails or retains stale snapshot; a malformed successful legacy response can normalize to empty `issues` at 14443. Native-only work without provider identity is not covered by the current base reader. |
| Workload native comparison tool | `?wlnative=1` -> diagnostic `wlNativeDiff` via mount 16177 -> `_wlNativeFetchRows` 14147 -> `workload_issues_native_v1`. | **Diagnostic only**, no state/render change. Excluded fields 14178 include IDs, parent IDs, URL, assignee ID, parent identifier, timestamps and sort order. Both source pagers cap at 50,000 without explicit cap-exhaustion rejection (14102/14152). | Existing tool cannot establish a successful source swap, functional plan writes, native links, refresh/realtime parity, or native-only item support. A zero comparison is only agreement on selected fields. |
| Workload plan dates, due dates and weights | Plan list `wlFetchPlanRows` 14482 -> `workload-plan`; writes `_wlPlanWriteRequest` 16719. Metadata `wlFetchLinearMetadata` 14873 -> fresh `prod_authority` 14526 -> partition -> native metadata or `workload-linear`. Due write `_wlDueWriteRequest` 17025 chooses `production-write` 17031 or `workload-linear` 17053. | Native metadata is already a **separate authority partition**; unknown authority fails closed 14887–14891, with no native-to-Linear metadata fallback. It does not replace base membership. Plan sidecar identity is still tied to old issue identity; comments describe this at 14124–14130, requiring backend confirmation. Both functions' serving provenance must be measured because maps call them manual candidates. | Native deadlines may continue for already-covered rows. Linear-owned metadata/due operations fail; plans may display degraded or retained state. Swapping just the reader risks breaking drag/save for native-only IDs. |
| Workload tweaks/comments | Tweak popover `tweakSubs` 19256 -> `wlFetchTweakComments` 19000 -> POST `LINEAR_TWEAK_COMMENTS_WEBHOOK` 19007; 3-comment display 19029. | **Conditional read/enrichment** on opening a tweak popover; no authority partition here. TTL can bypass network. Missing per-ID result becomes empty list 19016. | Failure message says open the issue in Linear (19269), now a dead end. HTTP-success incomplete results can silently suppress comments. Replace with canonical thread mapping, and account for older-comment link/text at 19038. |
| Filming Plans main view and Submit plan mapping | Nav button 7815 -> Filming view -> `_fpEnsureLoaded` 13409 -> `_fpLoadFromSupabase` 13385 -> authenticated `filming-plans`; add/edit handlers 13548/13569 -> `_fpPostPlan` 13478. Submit `loadLinearPlanMap` 19527 uses same source. Anonymous intake delegates plan resolution to server (19528). | **Native plan read/write**; no direct Linear browser call in traced path, no Sheets fallback. Response shape checked 13406. Names containing “Linear” in Submit mapping are local names, not provider API evidence. | Main plan rows/editing should continue if serving function and its storage are independent. Serving backend confirmation still required. Anonymous intake's server-side plan resolution also needs proof. |
| Kasper Filming coverage | `_kasperLoadFilming` 72376 -> protected plans; each row calls `_filmsFetchContentBank` and `_filmsFetchTabMonths` 72405. Content reads Calendar v2 or calendar-get fallback 72165; tab enrichment calls filming-plan-tabs 72192. | **Display enrichment**, distinct from plan ownership. Cached coverage can bypass fresh requests 72380. No Linear API established by UI alone. | Plan list survives a provider-only outage absent hidden n8n dependencies. Coverage can be unknown/stale if dependent readers fail; tab errors are represented rather than coerced into valid coverage. Do not retire unrelated Docs/calendar webhooks just because they share n8n. |
| Client onboarding submission and staff onboarding readers | `_obSubmit` bound at 21504 -> primary standard/AI n8n submit 21635, retry 21638 -> `_obBackupSend` 20992 -> `onboarding-capture`, then n8n fallback. Staff `_obvFetchLists` 12266 calls three protected onboarding readers; `_obvFetchFull` 12374 calls full reader. | **Client canonical intake plus downstream provisioning**, actual Linear reachability **UNPROVEN** until live workflow trace. `_obPost` 20989 checks HTTP but accepts invalid JSON as `{ok:true}` and does not require body `ok=true`. Successful fallback shows thank-you; a successful primary triggers a backup. | Submission may be stored while provider-dependent downstream setup fails. “Thank you” cannot prove new native projects/batches are provisioned. Require storage receipt plus independent downstream provisioning/alert checks; preserve anonymous public capture. |
| Calendar import from Linear | Staff menu 35909 -> `openCalLinearImport` 33328 -> `_calLinearImportFetch` 33372 -> `LINEAR_SUBISSUES_URL` 33381. Apply `_calRunLinearImport` 33499 checks live video link-slot seal 33518 before any writes. | Reader is **reachable even when apply will be sealed**. Under SyncView video authority, apply is intentionally refused; not a valid native migration path. Optional graphic lookup failure can be ignored while video pick UI proceeds (33391–33397). | Lookup fails or presents a later sealed dead end. Replace/remove offered import path only after necessary provider-only history/work is inventoried and an explicit native import/recovery workflow exists. |
| Calendar Excel import | Staff menu 35908 -> `openCalImport` -> `_calRunImport` 34354 -> constructed cards 34373 -> `_calBulkUpsertPosts` 33897 -> `_calUpsertFetch` 33901 with verify/retry. | **Source-card creation**, not proven native work creation: constructed cards have no deliverable IDs. No direct Linear call in construction. Writer transport is shared/runtime-selected. | Display cards may exist without corresponding work; distinguish intentional content-only imports from promised production work. Test offered status/tweak actions under native authority before calling import independent. |
| Production archive asset repair | Topbar button 58687 -> `_prodOpenArchiveRepair` 50018 -> list/detail functions 49892/49937 -> `_prodArchiveRequest` 49781 -> `production-archive`. | **Historical reader/rescue**; browser response validates `ok=true`. Function source/manifest does not itself prove serving rescue certification or asset completeness. | Historical content/assets remain unproven without rescued bytes and retrieval drills. Retaining rows that only point to provider files is insufficient. |

## Conservation blocker discovered from executable code

`_runNativeIntakeJob` checkpoints native result before Calendar materialization (47223–47226) and removes a completed job after materialization (47233–47237). That ordering is useful but not the complete lifecycle.

`_resumeNativeIntakeJob` catches failures and calls `_linearIntakeDiscardTerminallyRefused` at 47346. Actual code at 47260–47325 permits background removal after:

- 2 non-auth 4xx failures for an uncommitted job;
- 6 counted boot failures with status >=500 for an uncommitted job;
- 4 failures for `recovery_only` jobs, including failures without HTTP status, even when native work exists.

401/403 are excluded. Committed ordinary jobs are retained. Removal at 47315 has a visible notice but the reviewed path does not attest a durable recoverable debt record. The code invokes a submission-request logger before gateway (47202); its serving receipt, exact payload coverage, retention and replay recovery are **UNPROVEN** here. A zero browser queue does not prove all work materialized. Future exit gates must reconcile logged requests, native commits, Calendar cards, recovery-only removals and rejection receipts.

## Required validation and limits

Before deleting transports, exercise fresh/warm/cached browser states; sticky wl2 opt-out; forced Workload refresh; flag timeout then click before healing; existing legacy receipt; existing native receipt; disabled storage; anonymous intake; native commit followed by lost response; card materialization failure; repeated background recovery failure; native-only item planning; tweaks/comments with empty/malformed provider responses; onboarding storage-success/provisioning-failure; and sealed Linear import entry.

No tests, live writes, workflow edits, deploys or flags were run in this subtask. Export UI coverage beyond the identified import/history readers is **UNPROVEN**; absence from focused function searches is not proof no export path exists. Root/backend trace must finish endpoint serving and external creator inventory. The truth maps are navigation aids with dated statements, not current deployment receipts.


## Component evidence: backend, jobs and deployment inventory

# Independent Phase 1: backend and Production paths

Evidence baseline: `5765cfe80b7ca9844bab79a55fd75784bf9cb693` (2026-09-04). Read AGENTS.md completely, REPO_MAP.md, FIND_ANYTHING.md, truth/BRIEFING.md, EF_DEPLOY_MANIFEST.md and relevant current lifecycle map. No withheld strategy document or review was opened. No product, production, flag, n8n or database mutation was performed. References below are source findings at this SHA; deployments and live flags require separate serving evidence. Current maps are navigation clues, not proof of current external workflow behavior.

## Independent verdict

Decision A is not proven. Native work creation still validates Linear projects before creating native work, the label UI reads a live provider catalog, and multiple scheduled/inbound lanes still use Linear. A running native-authoritative team is not sufficient proof of provider independence.

## Concrete dependency paths

| Surface / caller | Verified chain and classification | If Linear access disappears | Gate / deployment limitation |
|---|---|---|---|
| SyncLinear list/board | `index.html:_prodLoadData:57583`, native deliverable and batch projections beginning 48570; descriptions use `production-write:handleDescriptionRead:4593` -> `deliverables` 4613, not provider | Native list/description reading is structurally independent, subject to complete native data and working Supabase | Staff. Native completeness and current serving revisions must be measured; historical `linear_raw` columns alone do not prove API use |
| SyncLinear labels (opening real issue details and label picker) | `index.html:_prodRender:58605–58609` -> `_prodEnsureLabels:48876` -> `labels_read` fetch 48969/48978 -> `production-write:handleLabelsRead:4636` -> `linearLabelSnapshot:4658` -> provider identity query 811–821 + catalog 822/752. Native selected labels chosen only afterward at 4663 | Real issue details can show a label-read error; label picker unavailable. Synthetic batch parent exits before request (48881+) | Reachable on real detail render, conditional on row/auth/cache; not every board render. Provider read occurs even when authority is SyncView. Pinned manual deploy |
| SyncLinear label write | `index.html:_prodRunLabelsWrite:53512` -> `_prodGatewayWrite:53160` -> EF labels operation -> `linearLabelSnapshot:5202` before `production_deliverable_write:5244` | A label edit fails before native commit | Provider catalog is a write gate, not optional display enrichment |
| SyncLinear status/due/assignee/title/description/comments | `index.html:_prodGatewayWrite:53160` -> EF `handleEntityOperation:4679`; authority and fence 4836–4845; native RPC (ordinary entity lane 5418), then mirror scheduling 5434–5457 | Ordinary native operations can commit despite subsequent mirror debt; no basis to classify every write as a provider-blocked write | Role/team/authority must pass. SyncView normal lane asynchronous; legacy parity and TEST await targeted drain. Mirror receipt and native receipt must remain distinct |
| SyncLinear new issue/sub-issue | `_prodOpenCreate:52639` -> `_prodCreateGateText:52302` returns closed guidance; server `handleProductionCreate` does committed-intent replay 3460 then unconditional `production_create_closed` throw 3486 | New creation remains intentionally unavailable. Committed ambiguous drafts still require recovery | Provider calls below throw (3488+) are dead on NEW creates and must not be counted as a reachable create dependency. Keep replay path until debt is accounted for. Never enable SyncLinear sub-issue creation |
| Dormant/recovery create options | EF `handleCreateOptions:3229` -> `productionCreateScope:3237` -> `projectForIntake:2976`; label catalog 3239. Browser loader 52778 and recovery drawer remain | Historic ambiguous draft options can still encounter provider reads | Distinguish normal closed creation from surviving recovery/tool endpoint; reconcile pending drafts before removing |
| Submit / Create Post (Calendar and Samples) | `index.html` common intake operation dispatch 40916; submission 46053/46934; EF `handleIntakeCreate` -> loop `projectForIntake:6003` before native work creation -> `readLinearProject:2415` -> `linearRead:2220/2185` | Successful native-first design still fails before new native work if provider project validation cannot complete | Always-called for each requested team on a valid intake path, including SyncView-authoritative teams. Public intake accepts a credentialless submission only through explicit public flag/rate lane 5959–5995; do not break this by assuming every submitter is signed-in staff |
| Append Create Post into existing batch | Same intake gate, plus `parentRouteForAppend:6366/6386` -> `validateLinearBatchParent:2322/2239` when validating direct parent routes | Append can fail on project or parent validation | Parent validation conditional; exact-row retry disables external parent revalidation at 6374/6394, but earlier project validation remains |
| Fill missing Calendar/Samples component | UI `_cal...` component action 38036 -> EF `handleComponentFill` -> `projectForIntake:5719`, route validation -> RPC `production_component_fill:5822` | Cannot fill missing component if provider prerequisite fails | Native-born, SyncView-only lane 5717–5718; not Production new-create lane. Native sibling/card/batch identity must become sufficient authority |
| Filming Plans | Nav 7815 -> mount 13666 -> `_fpEnsureLoaded:13409` -> `_fpLoadFromSupabase:13385` -> `filming-plans` GET; EF 100–107 reads table. Save `_fpSaveExisting:13538` / `_fpSaveNew:13560` -> EF POST upsert 124–138 | This master Google Doc list/save has no Linear call in reviewed handler | Staff read; narrower admin write. Current source has no Linear failure fallback. This does not prove Submit's separate plan-tab transport is independent |
| Production comments reader | `_prodComments` fetch at `index.html:54392` -> `production-comments`, target read 318 and native comments queries 344/349 | Reads native comments, no direct Linear API | Pinned deploy. Endpoint POST has budget/audit writes (RPC 228/242, audit insert 202); invoking it is not a strictly mutation-free measurement |
| Client Calendar comments | `_prodCanonicalCommentGate:54812`, explicit routing at 54845–54881 reports unverifiable canonical client surface as unlinked, leaving legacy card composer/storage available | Must preserve existing card-backed comments; cannot replace with current protected reader wholesale | `production-comments` client authorization admits Samples-bound origin, not Calendar. A canonical-only simplification can remove working Calendar composers |
| Archive/history | UI staff repair action `_prodArchiveRequest:49781`; EF `production-archive` list reads `linear_archive:300`; detail reads native comments 365 and asset refs 372, rewritten refs 145/158 | Native archive can work, but absent archive/rescued bytes are not fabricated by this reader | Staff-only archive repair is not evidence that client historical content/assets are reachable. Requires audience-specific recovery and full content/asset coverage |
| Onboarding | UI onboarding posts to n8n (20959–20966); `onboarding-full` itself reads three native tables (45–49). Lifecycle map §6 documents unawaited Drive/HubSpot/Slack provisioning; §7 row10 (438) documents manual Linear project creation | Existing native onboarding capture reader is independent; future production-ready client setup remains unproven without replacing project prerequisite | Live n8n workflow source and owner actual procedure required. Do not infer automatic Linear project creation from lifecycle prose; current map says manual |

## Writer safety and native accounting

- AGENTS.md frozen directive: `calendar-upsert` and `sample-review-upsert` must remain tokenless/open for existing anonymous review links. This audit offers no re-gate or key rotation. Production authority checks do not justify changing client writer authentication.
- Server `authorityFor` (production-write 1152–1165) fails closed on missing/failed/malformed authority. `assertLegacyParityEnabled` (1221–1228) fails closed. There is no verified provider-exit freeze/cutoff simply because these authority controls exist.
- `migrations/2026-07-20-f27-team-rollback.sql:track_b_f27_write_authorization:2294` returns current team authority/generation. `production_assert_authority:2472` locks outbox and current authority/parity rows, requires expected authority; this is an authority/fencing mechanism, not automatically a complete client-preserving exit cutoff. Exact installed SQL must be read back.
- Intake append native RPC and outbox payload are coupled (`production_intake_append:6482` with rows/events); native create/child event plans retain deterministic IDs, dedup, intent fingerprints, dependencies and attribution. Moving to native-only must replace provider routes without bypassing those conservation controls.
- A mirror being off does not stop native code from forming mirror obligations: normal entity result reports `mirror_pending` (5434–5457). A valid retirement must account for every queued obligation and receipt, not relabel outstanding work as delivered or zero a table.

## Inbound/outbound and scheduled lanes

| Lane | Source behavior | Important limit |
|---|---|---|
| `linear-inbound` | Signed delivery -> enabled flag 1332; failed/missing enabled read becomes disabled ACK via `readRuntimeFlag:140–151`. Issue path 725, normal SyncView team business fields detect-only | Detect-only is not zero mutation: source 751–781 can change attribution/client ownership after provider hierarchy/project moves. Comment path persists `production_comments` at 1245 BEFORE detect-only check 1247. Keeping inbound enabled is not read-only retention |
| `linear-outbound` | Service-only drainer; flag reads 183 fail closed. Ordinary mode + authority, parity flag + authority, TEST and F27 replay are distinct control lanes at 1222–1264 | Live drain reads provider viewer at 1371 even when selected outbox is empty. Empty outbox does not prove zero API egress. Provider issue/parent/comment/state reads and mutations occur after row claim; backoff/retries and terminal states need explicit retirement |
| Outbox missing provider issue | Catch 1922–1939 marks skipped, retains native UUID; source comment labels missing issue as deletion | Error text by itself is weaker than workspace/permission evidence. Do not treat skipped as restored linkage or independent proof of actual provider deletion |
| `linear-outbound-drain.yml` | Scheduled every 10m (17), repository_dispatch (15), manual, n8n scheduler dispatch routes also described in workflow | Disable/retire all triggers, not only cron. Verify serving drainer and queue cutoff to defeat stale callers |
| `linear-deliverables-reconcile.yml` | Every 10m (9) -> script 97 and inbound pager 113; script `loadLinearIssuesById:526`/`loadLinearWebhooks:598`, plans read both 758/759 | Provider comparisons/authoritative repair and summaries are not native independence proof. `applyHealing:1937`, main 2085. Must replace needed native linkage/accounting alerts before retirement |
| `sample-linear-reconcile.yml` | Every 10m (15); script resolves n8n Linear issue status (213/228), classifies SyncView+outbound-live as pull-only (343–345), fresh gate before correction (452–462) | Still pulls provider-derived status into client-facing cards when conditions pass. Stopping all reconcilers without first proving native card materialization can leave stale client status |
| `linear-sync-reconcile.yml` | Current workflow is dispatch-only; external scheduler still possible. Script provider status read 154/170; SyncView+live pull-only 371, foreign-pull logic retained; fresh read at 526–542 | Do not equate absent cron with inactive workflow. n8n trigger inventory needed |
| `b1-linear-incremental-refresh.yml` | Every 30m (25), invokes B1 script. Direct provider API in script 479. Full apply requires both teams Linear-authoritative (`assertFullApplyAuthority:2240`); incremental and stray-catcher have distinct policy | Not an unconditional overwrite of flipped teams. `assertFreshLinearAuthority:2252` uses SyncView authority in explicit stray-catcher lane (2269); external creator catch-up must be separately retired after inventory/freeze |
| Backup/shadow/deadman | Track B backup every 6h (track-b-backup.yml:5), restore rehearsal dispatch; shadow daily (production-shadow-audit.yml:6); deadman 15m (monitoring-deadman.yml:18) | Existing code/schedules do not prove successful export, restore, alert recipient receipt or complete egress observability |

Additional source-only direct-provider tooling found by filenames: B1 constraint/preflight/dry-run/cursor-gap/delivery-link tools; B3 comment catchup and mirror scenario tools; B4 echo probe, comment backfill and outbound harness; batch-parent-recovery; F200 attribution snapshot; F44 intake transformer; graphics-F2 evidence; production-write drill/project-mapping; Slice5 drills. These are inventory leads; none was run, and this filename search does not prove active external creators or complete egress coverage.

## All 35 Edge Functions: deployment classes

The generated manifest summary is arithmetically incomplete: 11 + 7 + 14 = 32, because three unpinned `workflow_dispatch` rows are outside its manual-dispatch-only summary count. Actual per-row partition is 11 push+dispatch, 10 dispatch-only (7 pinned +3 unpinned), 14 no-CI. Every slug is accounted below. Deploy path is not serving source equality.

| Class | Functions |
|---|---|
| Push + dispatch (11) | ai-onboarding-list; client-credentials; filming-plans; key-verify; legacy-onboarding-list; onboarding-full; onboarding-list; pto; smm-weekly-reports; thumbnail-revision-read; thumbnail-revision-scan |
| Pinned dispatch-only (7) | batch-write; deliverable-write; linear-inbound; linear-outbound; production-archive; production-comments; production-write |
| Other dispatch-only (3) | client-review-link; hiring-applications; hiring-automation |
| No CI path (11 ordinary) | calendar-reorder; calendar-upsert; caption-prompts-save; kasper-ad-performance-read; onboarding-capture; quiz-capture; quiz-leads-list; sample-review-reorder; sample-review-upsert; templates-save; thumbnail-folder-resolve |
| Deliberate manual subset of no CI (3) | client-token-verify; workload-linear; workload-plan |

All manual, pinned and no-CI current serving source remains UNPROVEN within this source-only report. Root has separate live deployment metadata and is comparing serving bodies. Manifest statements that workload-linear has never deployed and workload-plan is version 2 are not accepted as current live facts.

## Proof needed before a final exit release

1. Native catalog/project/parent/attribution authorities sufficient for intake, component fill and label edits; negative provider-access canary proves successful native work and exact card materialization.
2. Current deployed source/import equality for every touched Edge Function, and exact installed SQL/RPC bodies/grants/fences.
3. Action ledger reconciles acknowledged user intent, committed native row, card/receipt materialization, pending retries and terminal mirror decisions. Preserve ambiguous legacy Production-create recovery without re-enabling new creates.
4. Full native-owned coverage of client Calendar/Samples content, comments, requested changes and attachments. Staff archive-only availability does not clear a client continuity gate.
5. Read-only inventory of actual n8n workflows, external Linear credentials/webhooks/creators and all schedule/dispatch routes. Current docs and snapshot backups do not prove active workflow state.
6. Preserve anonymous client write access throughout server cutoff; if staff creation needs a coordinated freeze, clients still save approvals/notes/tweaks through working existing writers.
7. Provider egress proof includes read traffic, static `create_options`/recovery endpoints, empty live drain viewer lookup, stale browsers, failure fallbacks, dispatch callers and external automations. Browser request blocking alone misses server traffic.
8. Alert delivery drill names actual recipient/channel and proves acknowledgement; monitoring code existing, green backups or zero outbox alone is insufficient.

Snapshot scope limitation: this report is an independent source trace, not a live provider inventory, successful degraded-path test, export completeness certificate or restore drill. All such missing evidence is UNPROVEN.


## Captured serving-source differences

# Independent Phase 1: current serving-source differences

Baseline repository SHA: `5765cfe80b7ca9844bab79a55fd75784bf9cb693`. Root captured all 35 deployed function source closures through Supabase at 2026-09-04 20:56:27Z and reported 30 exact matches, five mismatches. This follow-up independently compared the five private captured source trees to the repository. Captured files stay private. No endpoint was invoked and no live mutation was performed.

## Findings that change the safety decision

| Function / serving version | Actual difference | Verdict and exit consequence |
|---|---|---|
| `calendar-upsert` v48 | Captured `functions/calendar-upsert/index.ts` omits `authorizeBrowserWrite`, `browserWriteAuthResponse`, `normalizeBrowserWriteClient` import; uses `actorFrom:123`; handler 479 obtains actor at 489 and runs build/read at 501–504 without authorization call. Source branch includes call at repo 501. Shared thumbnail-revisions is byte-identical | Intentional owner-frozen open writer, consistent with AGENTS.md. Repository writer is not serving behavior. Blind deploy of repo copy would reintroduce authorization. Preserve captured implementation as rollback source; do not recommend re-gating or redeploying repo source |
| `sample-review-upsert` v49 | Same intentional auth removal; captured `actorFrom:91`, handler 491, actor at 504, build/read 513–516 without authorization call. Source branch includes call at repo 513. Shared thumbnail-revisions byte-identical | Same frozen directive and same blind-deploy hazard. Both serving writers retain ordinary native merge/guard/event/thumbnail paths; no direct Linear API call introduced by these differences |
| `workload-linear` v6 | Captured entrypoint lacks `requireLinearAuthority`, `requireCurrentLinearTeam`, team-related imports/types and mirrored-team selection. Its `policy.mjs` lacks corresponding authority/team helpers. Captured handler 384 authorizes staff, 390 requires Admin/SMM-compatible role, 394 checks active writable sub-issue, then 395 immediately calls `setLinearDueDate`. That function 277 issues `issueUpdate` at 281 via Linear request 135/141. No `prod_authority` read fences this serving writer | Material source/serving contradiction. Current browser may route both SyncView teams to native writes, but stale/direct eligible staff callers can still write Linear through serving function. Source assertion that both teams now always 409 is WRONG for v6. Before provider cutoff, supply a server-enforced compatibility/cutoff or close this staff-only obsolete writer after proving browser/stale routes; keep client writers untouched |
| `sample-review-reorder` v37 | Entrypoint and `browser-write-auth.ts` / `staff-role-auth.ts` match exactly. Only `browser-write-auth-policy.mjs` raw bytes differ; normalizing CRLF/LF and terminal whitespace makes entire file identical; Git textual diff is empty | Formatting-only closure mismatch, not behavioral drift. Still uses authorized reorder path at captured line79. Keep exact-byte provenance separate from semantic equivalence |
| `onboarding-capture` v34 | Diff is introductory comments and missing final newline only. Executable capture code unchanged: upsert `onboarding_fallback` at captured55; optional `SLACK_ALERT_WEBHOOK` on real fallback 68–81 | No provider dependency difference; no Linear call. This backup endpoint is not the primary n8n onboarding/provisioning workflow, whose live contents remain separate evidence |

## Additional distinctions

- Open writers annotate audit actors from request/body and semantic action roles. This is current intentional serving behavior, not proof of authenticated actor identity. Do not describe native event counts as cryptographic actor proof.
- The `workload-linear` discrepancy is not theoretical source-only stale code: this is the captured ACTIVE v6 source. Whether a specific existing client/staff session exercises it tonight remains unmeasured, but the authenticated server path is reachable and lacks the claimed authority guard.
- Metadata reader on `workload-linear` remains provider-dependent too: captured handler 352–369 -> `metadataRows:229` -> `linearRequest:242`. It is staff-authenticated; empty provider metadata is not native Workload parity evidence.
- Root's matching captures for `production-write` v66, `linear-outbound` v47, `linear-inbound` v46, `production-comments` v24 and `production-archive` v8 establish serving-source support for the corresponding dependency chains in `phase1-backend-paths.md`. Live routing/frequency and data coverage remain separate questions.
- Classifying all five source mismatches as deployment defects would be wrong: two intentional writer directives, one material stale gateway, and two behavior-equivalent differences were found.

Needed gate: any release bundle touching a frozen writer must compare against its captured serving source and explicitly verify that tokenless anonymous review writes remain available; absence of a Linear egress path must be proved at the actual serving gateway, not inferred from newer repository guards.


## Publication validation (2026-09-04)

This is an analysis-only change based on main 85a1bafc268722eda310d8f1af94c9686585bbe0. Local documentation checks passed: repo-map-sync 270/270, truth-sync 526/526, EF deployment manifest verification, and git diff whitespace validation. The initial truth-sync ancestry failures were caused by a shallow checkout; after fetching complete commit history, all checks passed without changing truth records.

The actual-source offline reader probe was rerun successfully: Samples returned successful empty results for HTTP500/error JSON, HTTP200/ok:false and HTTP200/empty fallback cases; Calendar v2 rejected all three. Both accepted the valid one-row fixture. These are injected, network-free source tests, not claims of observed production client failures or successful live client write journeys.

The independent Phase 1 seal remains intact. The prior checklist is preserved as an exact suffix beneath the historical/nonoperative banner. All ten linked workflow filenames exist at the publication base. Publication scans found no client slug terms, secret patterns, token-bearing URLs or email addresses in the added material. No raw serving capture, private historical task transcript, credential or client record is included. Future client mutation, alert-delivery and provider-denial drills remain unexecuted and are explicit release gates.
