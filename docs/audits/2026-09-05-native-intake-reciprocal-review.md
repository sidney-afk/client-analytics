# PR1314 independent review and reciprocal completion correction

Source-only review of PR1314 at `42452cdbf5c36ba74b08885a152943ff827c9aa3`, stacked on `48f75012a3826d27ef087556eca90b941709d3c1`. Fresh metadata showed OPEN, draft, unmerged and three passing exact-head checks. Captured main `99d31c815de3e1a46deeb01c45c09bf2937040ad` was not integrated. These are source/CI facts, not installed or serving proof.

## Independent adjudication

- **Confirmed corrected:** the content-based late-job classifier and BEFORE guard are absent. Stage 2 never inserts Calendar or Samples cards. Its AFTER provenance recorder captures creation, deletion and slot changes without classifying human content. The real repository writers accept both rename-back controls. A provenance insert failure still aborts its enclosing card write; this requires the existing save-failure continuity gate before installation, despite shorthand that the trigger "refuses no write."
- **Confirmed corrected:** a planned bind locks the card before the expected children and rechecks exact manifest identity/cardinality before writing. Existing actual-SQL concurrent re-card/delete and injected-failure controls remain in the lane. This review does not prove every installed crosswalk producer follows the same lock protocol; current-main authority changes require integration and serving proof.
- **Confirmed corrected:** private report paths resolve real existing ancestors and reject both in-repository two-dot names and symlinks into the repository. An independent offline run of the unchanged CLI suite passed 16 checks, including bounded stdout/stderr and RPC error output. This does not prove resistance to a hostile concurrent filesystem administrator replacing path ancestors after validation.
- **Confirmed new defect, corrected locally:** the shared state reader previously declared a forward-filled card complete after its expected deliverable had moved elsewhere. Both Calendar and Samples then returned Stage 2 `complete`, owed zero cards, and disappeared from the backlog. Independent actual-SQL baseline controls recorded 68 PASS / 2 FAIL (original 66 plus two successful setup controls, then the two failing reciprocal checks).

## Small correction and evidence

Only `production_intake_reconcile_state` changes runtime behavior. Completion additionally requires each exact expected child to exist, retain its manifest batch/client/team/kind identity, and point back to the expected card. Missing, moved, cleared or identity-conflicting children keep card debt visible. Existing Stage 2 reasons handle the conflict without undoing human bindings. Accepted manifests, request fingerprints, epochs, receipts, provider transports, browser code and both frozen writer sources are unchanged.

Candidate proof: **76 PASS / 0 FAIL**, comprising the original 66 plus ten new checks. Map validation passed 293 checks and truth validation 530; the diff whitespace check passed. The retained lane adds both Calendar and Samples controls for successful reciprocal binding, moved/cleared/identity-conflicting children, paginated backlog and exact summary debt, full card/child/receipt conservation after refusal, and exact identity restored by the fixture owner. Provider/drainer transport remains denied. Historical baseline failures are retained independently. The first candidate attempt passed the original 66 and the moved/cleared Calendar controls, then its added wrong-kind setup collided with an existing unique slot; the setup was corrected to the existing valid, unoccupied `other` kind without altering runtime or weakening the identity assertion.

The disposable database runs real migration functions and real source handlers through the existing RPC/HTTP test seam. It uses the harness's synthetic schema/bootstrap, not a copy of the installed database; full installed triggers, permissions and concurrent external writers remain UNPROVEN. Source writer tests do not establish behavior of serving frozen v48/v49. No live, TEST-client, provider, migration, workflow, flag, auth, n8n, deployment or publication action occurred in this correction.

## Remaining completion path

This is a bind-only hold, not finished automatic card materialization or Decision A. Missing cards remain visible operator debt. The old browser full-row replay hazard remains red: identical human and retry payloads cannot safely be separated by their text, and returning 200 while discarding a human edit is unacceptable.

The frozen directive is specifically an authentication/re-gating constraint. It does not by itself require a new permission request before designing a compatible additive operation protocol. The next finite implementation is a manifest-bound card-create identity with original request/item/card/client/team identity, an atomic creation receipt and tombstone check, and exact receipt adoption on replay rather than reapplying initial fields. A source header alone is not an identity or acceptance receipt. Existing anonymous approvals, notes, comments and tweak writes must keep their current authentication and semantics.

That protocol also needs a structural transition for the legacy creation cohort and every retained browser job. A new protocol-owned card must not remain exposed to an indistinguishable old full-row create replay. Prove a compatible creation-only bridge (including accepted partial jobs and lost responses) or leave that legacy cohort held; never infer it has drained by waiting. Any creation refusal needs recoverable original intent and a visible retry path, and must not become a general client freeze. Prove old tabs, public intake, staff jobs, human rename-backs, late retry after edit/delete, reciprocal locks and zero provider requests before enabling creation. These are implementation and release gates, not an authorization to change frozen auth.

## Rollout and rollback

Unapplied draft source only. Keep the runner unscheduled and uninvoked until the existing migration/schema/backup, provenance-write-failure UX, integration, serving and owner release gates pass. This correction adds no schema object. Restoring the old state function would restore a known false green; if the correction must be withdrawn, hold execution and retain explicit unresolved accounting. Retain all accepted children, receipts, bindings and provenance; no data deletion is an inverse. Clients see no change during this local preparation.

## Stable source anchors

At reviewed base 42452: `production_intake_reconcile_state` (migration line85; forward-only complete predicate around192), `production_intake_reconcile_cards` (line571; complete early skip around614), `production_intake_reconcile_backlog` (around825) and `production_intake_reconcile_summary` (around859). Candidate symbols are unchanged; exact candidate line numbers and source hashes are in the private final receipt. CLI: `insideRepository` and `publicReport`. Proof: `scripts/native-intake-reconcile/lane.mjs` R7, original R/P/S checks, and `test/native-intake-reconcile-cli.js`. Line numbers are subordinate to commit plus symbol.
