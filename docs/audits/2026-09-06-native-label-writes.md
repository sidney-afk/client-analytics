# Native labels: cloud correction and executable native-save proof

Exact input: integration8514a83ed1a65145a3a51ffe52e5fcbb2976be31. The applied cloud proposal is preserved as its own892894e1b commit. Current remote main5ed005c was observed separately, not integrated. The [catalog contract](../ops/NATIVE_LABEL_CATALOG_FOUNDATION.md) remains subordinate to the single go-live checklist. This source is draft/uninstalled and no release gate is promoted.

| Reviewed claim | Independent adjudication |
|---|---|
| Cloud native picker works for native-only cards | WRONG in preserved cloud: real handler refuses linear_issue_unavailable before native catalog. Candidate selects strict native read before provider ID lookup; both actual teams pass without provider identity. |
| Cloud malformed flag never falls back | WRONG: actual cloud requests with false/0/empty/object/null values attempt provider transport. Candidate installed capability refuses; malformed pre-install responses also refuse in actual-function controls. Valid absent-RPC plus exact absent-row compatibility remains. |
| Cloud native reads are authoritative complete | UNPROVEN: foundation always reports provider completeness unverified. Candidate stores an explicit immutable privileged operator attestation and never claims SQL authenticates its source. Real import evidence remains a release gate. |
| Cloud native label commits implemented | WRONG: original branch was permanently held. Candidate has actual service-only SQL commits against fictional attested catalogs, existing CAS/F27/event/journal/receipt owners and no native provider intent. |
| Cloud accepted retry preserves ordering | WRONG: actual preserved handler rejected an accepted retry at stale CAS after catalog changed. Candidate reads exact retained receipt before new catalog policy; current authentication/scope/permission and exact identity remain mandatory. |
| First correction handled authority changes | INCOMPLETE: independent review found authority/F27 still preceding accepted adoption. The final narrow read-only path adopts only exact native receipts across authority flip/missing/denied reads. New/provider/mismatched requests retain existing gates. Actual SQL and gateway conservation controls pass. |
| New native writes need a second outbox or catalog | Rejected. One existing catalog owner and mirror_outbox identity suffice. Native INSERT is terminal skipped with immutable catalog marker, distinct from provider debt. No second retirement epoch. |

Final focused actual handler/disposable PostgreSQL proof:40 groups PASS. It includes both teams/global/foreign/group/archive rules, original selected history, complete empty catalogs, clear/no-op, current human row preservation, immutable attestation, no staging residue, old-cloud negative controls, stale picker, actual concurrent capability blocking, F27 stale-generation refusal, journal failure rollback, native receipt retention, active client403 and private SQL execution denial. Accepted native readback crosses authority flips/outages with no gateway RPC or mutation; provider-era receipts are not converted or drained under label hold. Exact source hashes/cases and private receipt digest are in [the public-safe evidence JSON](2026-09-06-native-label-writes.json).

The offline lane exercises44 actual-handler/model and browser-source controls. Three execute the actual picker/save functions in a VM: native version is forwarded, provider request omits it, and existing actor replacement blocks late response adoption. Additional malformed RPC/flag controls never guess provider state. HTTP fetch is fully substituted; this is not SDK or independent socket receiver proof. Real SQL uses the existing combined migration fixture with explicit inherited platform prerequisites, not a reconstructed installed schema. Expected Node22 warnings and forced RPC-failure logs are not hidden.

Preserved setup-only failures are listed in the JSON; no production constraint or negative assertion was weakened. Historical Sept5 foundation48-SQL/20-handler evidence remains separately dated and is not relabeled current. The standalone historical SQL launcher received literal-loopback/noninteractive/PG-environment safeguards; its full48 checks were not rerun here.

Recovery is explicitly RED: selected37 and its schemas/helpers do not cover production_label_catalog_versions or these added receipt guards. Exact eight-column owner, all service/private functions, flag and trigger dependencies are listed in the contract. A separately versioned corpus and authenticated schema/trigger-aware reconstruction with actual restored native receipts must precede installation. Current source also excludes later existing-assignment/HTTP/cutoff branches; their behavior and the final manual gateway closure must be preserved at integration. No current serving-source, Deno typecheck, whole-repo suite, native activation, real catalog export or provider retirement is claimed.
