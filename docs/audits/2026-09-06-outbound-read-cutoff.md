# Ordinary outbound read cutoff: bounded source correction

Base: `8e3d0865041a26010bc2d2f6ee78346357eb2aa3`. Status: **SOURCE_ONLY plus disposable SQL/actual-handler proof; G8, serving and activation remain held.** The canonical execution authority remains [GO_LIVE_CHECKLIST](../independence/GO_LIVE_CHECKLIST.md).

The unchanged base's complete handler makes a viewer request even with no selected rows after cutoff. Its issue reader also makes a request using a retained pre-cutoff lease. Both negative controls reproduce with intercepted fetch; the SQL cutoff alone cannot stop that old source from reading Linear.

The correction passes the claimed row and SQL client through every provider helper. The sole `linearGraphql` fetch requires `linear_outbound_authorize_dispatch_v1` immediately before every request, including viewer, issue, team, comment, attachment pagination and mutation. The returned authorization must bind the exact outbox ID and generation as safe integer JSON numbers, plus an authorization UUID. The viewer runs inside the claimed-row path, after the SQL-only F27 drill exit. Existing pre-mutation authorization and pre-attempt checkpoint order remain intact; the transport check is additional. No SQL, new owner, flag, gateway, browser, anonymous authorization or import closure changed.

## Finite evidence

* **34/34 disposable SQL/actual whole-handler groups PASS.** Native gateway acceptance/replay and the exact reserved F27 SQL-only drill remain successful without provider attempts. The two base negatives, empty queue, accepted-after-cutoff debt, failed flag read, missing provider key/control, cutoff after claim/between read and mutation, all read helpers and both pagers are exercised. Generation-zero null/empty/boolean/string/undefined/negative/fractional receipt values refuse before transport.
* **33/33 offline source/actual authorization-function checks PASS.** The existing F27 drill, mapping/pagination, self-echo, comment/attachment and gateway suites also pass. The two existing source loaders changed only for the moved viewer and added pager argument; prior semantic assertions remain.
* The recording fetch intercepted **10 attempts**, comprising the base negatives, positive mutation, first pages and the explicit in-flight case; **zero external requests** occurred. No provider response, Supabase transport, installed schema, serving version, live workload capacity or client journey is proved by this fixture. The whole handler uses actual repository code; only Deno/Supabase HTTP/fetch boundaries are translated. SQL queries, claims, grants, updates, triggers and receipts execute in a disposable database. The fixture administrator resets only its control between independent cases; no product re-enable API is introduced.
* Three earlier failed runs remain private: 13/1 with an incomplete attachment marker, 13/1 with a deliberately fictional but unsupported attachment URL, then 25/1 with omitted F27 service table-read grants in the partial fixture. The final fixture uses the actual attachment allowlist and exact grants from the owning migration. No production predicate was weakened.

The checksummed public-safe receipt is [outbound-read-cutoff.json](2026-09-06-outbound-read-cutoff.json). Source symbols: `linearGraphql`, `authorizeProviderDispatch`, `readRows`, `readViewer`, `readIssue`, `readCommentByMarker`, `readAttachmentRevisionPresent`, and the `Deno.serve` claimed-row loop in `supabase/functions/linear-outbound/index.ts`. The receipt's source hashes bind these symbols independently of later line drift.

## Release and rollback limits

This is one prerequisite under [the G8 owner contract](../ops/LINEAR_OUTBOUND_CUTOFF.md), not a new execution plan. Review and install compatible cutoff SQL/recovery first, then deploy and verify the exact matching worker closure. Recompute the combined candidate's declarative F27 pins; this isolated branch does not claim its inherited deployment pins are current. Prove old worker isolates/jobs have quiesced before making an all-reads-stopped claim. The retained unchanged-base negative is the reason that SQL installation or a new source SHA alone is insufficient.

A server grant that commits **before** cutoff may be followed by one already authorized network attempt. The actual test activates after that grant and observes one allowed attempt, followed by refusal of the next request. `authorized_before_cutoff` now conservatively includes provider reads: it is evidence of a permitted attempt, not evidence that a mutation happened or succeeded. It must never be converted into terminal success without the existing receipt/reconciliation evidence. Every provider request adds an authorization RPC/update; local correctness proof is not a throughput benchmark.

Clients see no change during this unapplied source work. At a future coordinated worker release, their native content/write owners and frozen anonymous handlers remain byte-identical; mirror-only provider debt remains retained. That architectural fact does not substitute for the required share-link checks and delivery drills. Staff mirror work can pause on a refused claim/grant; progression aborts on any unexplained native-write failure, serving mismatch, missing census/receipt, or provider attempt after a post-cutoff grant should have been refused.

Before activation, the source change is reversible to the captured prior worker. After activation, do **not** roll back to its unconditional viewer/read behavior: retain the new transport fence while stopping the worker, reconcile all in-flight and newly accepted debt, and use a separately reviewed compatible recovery. Never delete queue/control receipts, disable the stale-worker guard, or claim a flag change atomically revokes a network request. Other Edge Functions, browser/fallback jobs, n8n, inbound/webhooks, credentials and independent schedulers remain outside this proof. No deployment, flag, n8n, notification, provider or live-data action occurred.

Reproduce with `node scripts/linear-outbound-cutoff-rehearsal.js --read-fence` and the existing explicit disposable `G8_TEST_*` bindings. The discovered `test/linear-outbound-read-cutoff.js` requires the actual lane under CI's existing F63 PostgreSQL binding; missing binding fails. No workflow was edited.
