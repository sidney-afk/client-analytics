# B4 Readiness — gate evidence, auth operationalization, road to writable

**Date:** 2026-07-11 · **Second-pass corrections through:** 2026-07-14 · **Status:** living checklist (update in the same PR as any item it tracks)
**2026-07-15 update:** PR #836 merged to main as `2a99141` (security containment + F44 browser fix `c7b325e`); F35/F44/F76/F77/F82/F88 live-state reflected below.
**Authority:** `TRACK_B_LINEAR_REPLACEMENT_SPEC.md` defines every phase, gate, and design here —
where this file and the spec disagree, THE SPEC WINS. This file is the operational bridge: what
is live right now, evidence per gate item, who owns each remaining step, and the plain-language
answers to the owner's standing architecture questions.

## 1. Live posture (reconciled 2026-07-14; individual evidence dates retained, UTC)

| Metric | Value | Evidence |
|---|---|---|
| Mirror faithfulness (`diff_count` / `repair_list_size`) | 0 / 0 sustained; only single-run transients that self-clear on the next 15-min tick | reconciler v2 summary events in `deliverable_events` (e.g. events 7743–7746) |
| Pending card linkage (`linkage_actionable`) | moved 0→1 at ~2026-07-14T16:03Z (1 dry-run calendar:video planned linkage, not written; flagged, benign); prior baseline **0** since 2026-07-11 03:22 UTC (drained 31 additive slot fills; residue 208 all classified non-actionable) | events 7744 (before) → 7746 (after); EXECUTION_LOG 2026-07-11 |
| Mirror coverage | all 4,323 deliverables checked, zero-row gap closed | event 7746 `deliverables_checked` |
| History parity | finished-work import complete: 3,187 deliverables + 800 batches, tag `created_by='history-backfill-2026-07-10'`. F62 invalidates the old one-command DELETE because later normalized comments and thousands of ledger events depend on the tagged rows; preserve by default pending a dependency-aware owner-approved rollback rehearsal. | EXECUTION_LOG 2026-07-10/11 plus 2026-07-14 correction; ROLLBACK.md Live State |
| Inbound mirror | `linear_inbound_enabled={"enabled":true}` since 2026-07-07 23:17; fresh `mirror_in_*` events daily | `syncview_runtime_flags`; `deliverable_events` source=`mirror` |
| Reconciler cadence | ~15 min requested via the n8n monitoring pager; run duration ~110–150 s at full 4,323-row scale. F132 proves the combined pager can stop before later lanes and its current checks are not terminal receipts; F131 proves B1's fresh event is not a success-only heartbeat. | GitHub Actions run history; summary event `started_at`/`finished_at`; second-pass pager graph/readback |
| Track A | COMPLETE and closed out 2026-07-10 (3-day audit: zero real-client fallback on all seven legacy write webhooks; ledgers clean; full-roster drift sweep clean) | EXECUTION_LOG 2026-07-10; ROLLBACK.md rows |
| Auth mode | `auth_enforcement={"mode":"permissive"}` (fail-open; flip rehearsed 2026-07-05) | flag + ROLLBACK.md B0 row |

## 2. B3 → B4 gate checklist (spec §14 L1115–1122 + §1.5)

| # | Gate item (spec wording) | Status | Owner |
|---|---|---|---|
| 1 | Mirror zero-diff (modulo §1.4) **7 consecutive days** | ❌ **BLOCKED / NOT CURRENTLY MEASURABLE (F90).** The present aggregate mixes planned TEST drill churn into the real-client soak and pager signal. Gate is met only after source separation, when both hold: (i) the separately labeled TEST diagnostic/harness passes green with reconciler v2 at 0/0/0 throughout, and (ii) **real-client-only** diff/repair faithfulness stays clean for a new full dated seven-day interval (§1.4d transients tolerated; linkage remains a maintenance lane). Do not reuse a pre-separation interval or mark this row green from the mixed aggregate. **Observed (2026-07-15):** repair_list_size=0 and outbound_diff_count=0 across all sampled runs for 8 consecutive days (status parity). The F90 measurement-validity concern is unresolved, so the 7-day gate is observed-clean but not formally certified pending F90. | Owner |
| 2 | Comments webhook subscribed + catch-up pull run (§4.3.4) | ✅ reconciler webhook probe: 4/4 enabled, `missing_comment_resource=0`; comment catch-up ran in B3 stage 3 | evidence in v2 summary `webhooks` block |
| 3 | Echo probe green (§12) | ✅ 2026-07-11 TEST probe: one app comment produced one Linear comment and remained exactly one app-thread entry after both Comments webhooks settled; zero duplicate `mirror_in_comment_add` events; Linear creation 3.096 s, settled proof 11.543 s; all TEST mutations restored | Codex |
| 4 | Editor/SMM UX feedback collected | ❌ Bounded fictional/source walks found F133–F138: split native title, browser-only recovery, inaccessible reorder, illegal creative transitions, collapsed Video assets, and absent Activity. F133–F137 must close before the applicable human flip; F138 must close by the owner-ratified first-flip-or-history-retirement gate. Then obtain explicit real-team sign-off. | Owner |
| 5 | Outbox drain + zero-diff + linkage-zero report (§1.5.3) | ⏳ linkage-zero done (event 7746); formal per-team report at flip time | Codex |
| 6 | Legacy-writer gates verified (reconcilers + n8n bridges) | ⏳ at flip time per team | Codex |
| 7 | Identifier seed check | ⏳ at flip time | Codex |
| 8 | TEST-only flip + full R2/F27 recovery rehearsal: stop affected mutations, disable/read the involved F2/F4 lane(s)—both if unknown/mixed—snapshot/classify/resolve every queued row, prove that team's queue is zero, then reverse authority and verify recovery | ⏳ not yet run | Codex + Owner |
| 9 | Detect-only foreign-write alert tested | ⚠️ BLOCKED in this sprint: the live branch is reachable only when a team's canonical `prod_authority` is `syncview`; both teams remain `linear`, and the sprint forbids every runtime-flag change. Source coverage is green but is not counted as live gate evidence. F55 must also remove the backend-only `supabase` alias before the drill. | Codex + Owner |
| 10 | Nightly due-date roller located AND disabled, or owner-signed detect-only acceptance after full consumer mapping (D-9/F14) | ⚠️ BLOCKED on workspace-owned automation access; live signature and disable plan are recorded below. Nothing was disabled. Blind key rotation is not proof. | Codex + Owner |
| 11 | Dedicated Linear mirror identity distinct from the house account (D-18) | ✅ Implemented as the dedicated SyncView Mirror identity. The deployed secret and echo matching were proved without exposing key material. Rotation and full consumer inventory remain operational duties. | Owner |
| 12 | Editor/SMM sign-off + DR drill | ❌ Blocked by F133–F137 plus the previously listed identity/status/notification gates; F138 separately blocks whichever first-flip-or-history-retirement point the owner selects. Bounded audit evidence is not human sign-off or a TEST DR drill. | Owner |
| 13 | **Auth prerequisite (§6, this file §3):** write attribution enforced before any real write phase | ⏳ WP-A1/A2 live and proven; WP-A3/A3b staff sign-in, account menu, and sensitive-surface consolidation are live (#796); F31/F35, write-attempt telemetry, revocation, and the enforcement gate remain | Owner + Codex |
| 14 | **B1 checkpoint + monitoring receipts (F131/F132/F09/F66):** only a complete success advances B1; pager lanes are isolated/correlated; relay delivery is authenticated and terminal; an observer outside n8n proves liveness | ❌ **BLOCKED.** Green recent runs prove happy paths only; deterministic cursor skip, false-green, partial-execution and acceptance-without-delivery paths remain. | Track B + Monitoring owner |

## 3. Auth — operationalizing spec §6 (design is already owner-ratified; do not redesign)

**Locked tier design (D6), with the shared-key interpretation security-blocked by F31:** admin,
SMM, and creative remain the authorization tiers. Current requests send a tier key plus a roster
choice, but that proves only a claimed active member; before flip the server needs an individually
revocable session carrying immutable member identity, or an explicit time-boxed owner risk
acceptance with a tested offboarding/forced-login procedure. Client review links stay no-login, gated by
per-client minted tokens verified server-side (`client-token-verify`, live). Forward enforcement is
one owner flag action. **It is not symmetric:** after enforcement, global permissive reopens access
and is blocked by F70 pending a security-incident containment/expiry design.

**Already built (verified in repo + live):** B0 tables (`team_members`, `clients`,
`client_access` service-role-only, `client_access_events`, `syncview_auth_events`, `flag_flips`),
`client-token-verify` EF (timing-safe compare, permissive-mode logging, 410 on enforced+invalid,
FE blocks on a fresh 401/410/enforced-fail), `key-verify` EF, role/actor header plumbing on write
paths, flag-flip audit trigger. **F38 correction:** the frontend currently allows and positively
caches verifier 500/network/parse failures, and a cached allow bypasses later 410 revocation in the
same tab. This is acceptable only during the explicitly permissive epoch and blocks WP-A5.

**Remaining work-packages, in order:**

| WP | Status | What | Notes / rollback |
|---|---|---|---|
| WP-A1 | ✅ DONE 2026-07-11 | Every staff write EF now persists `X-Syncview-Actor` / `X-Syncview-Role`: calendar/sample upserts and reorders use their card event ledgers; template/caption-prompt saves use additive service-only `settings_events`. One disposable TEST write per EF proved all six paths and cleanup restored the two settings rows exactly with zero probe rows/events left. | No routing/enforcement behavior changed; leave the additive `settings_events` table dormant if needed. Private source snapshots are not exact runtime artifacts (F51): revert callers first and use only a manifest/fingerprint/artifact restore that has been captured and drilled. |
| WP-A2 | ✅ DONE 2026-07-11 | The three role keys were already minted in EF secrets. Their deployed digests match the private B0 backup, and `key-verify` resolved compatible active roster rows for admin, SMM, and creative in permissive mode (proof events 3–5). | Secrets only; no key material is committed. Rotation is not a generic rollback or blind experiment: inventory consumers, stage replacements, revoke provider-side, invalidate sessions/caches, and prove denied old access. F31 requires that mapped action now for the exposed shared credential and an individually revocable replacement before attribution is trusted. |
| WP-A3 | ✅ DONE + LIVE 2026-07-11 | The §6.1 active-roster sign-in is on `main`: no free-text actor, role key stored locally, boot revalidation through `key-verify`, verified key/actor/role on Supabase EF requests only, normal Production navigation after verification, direct `?prod=1` diagnostics preserved, 401 clear, and verifier-outage fail-open. Dummy-only desktop/mobile coverage is green. Production “My issues” still ignores that identity (F37). | Revert the WP-A3 frontend commits; operational fail-open is tolerated only while `auth_enforcement={"mode":"permissive"}`. F37/F38 must close before creative authority or enforcement. |
| WP-A3b | ✅ DONE + LIVE 2026-07-11 (#796, `5c04994`) | One signed-in identity owns every sensitive staff surface. The staff button opens an account popover (`Signed in as <name> · <Role>` + **Sign out**, no switch-user path); the full form exists only signed out. Credentials accept admin + SMM role keys, while unstripped onboarding and filming-plan writes accept admin only. The server derives role from the matching secret and keeps `CREDENTIALS_STAFF_KEY` / `ONBOARDING_STAFF_KEY` compatibility in parallel. Sign out and invalid-key 401 purge sensitive credential/onboarding UI + caches; recognized-but-disallowed 403 preserves the valid session. | Roll back the frontend/callers first by reverting #796 while leaving additive backend compatibility deployed. Consider backend rollback only through F51's manifest/fingerprint/artifact gate. Both legacy secrets remain unchanged; do not retire them until every non-browser caller is inventoried and identifier-only telemetry proves zero legacy use through an owner-approved window. |
| WP-A4 | ⏳ BLOCKED BY F35 | Add credential outcomes to every actual Track-A write, then measure those write attempts over real traffic; **gate: zero unkeyed writes for 72 h**. `syncview_auth_events` sign-in-only rows cannot prove this. | no behavior change |
| WP-A5 | ⏳ OWNER GATE — BLOCKED BY PHASE-0 AUTH/READ/WRITE FINDINGS (F97) | After every Phase-0 boundary is closed—including F31/F35/F38/F67/F69/F70/F76–F89/F91—execute GO_LIVE Phase 0.75 **before any real-client parity cohort**: exact current-token roster proof, cache/session epoch purge, F5 CAS to `enforced`, exact readback, and the negative matrix. The prior standalone F5 snippet was not connected to the canonical sequence. | Do not globally flip back to permissive. Preserve auth and fix/revert the failed verifier/caller; any temporary fail-open requires an owner security incident, compensating server containment, cache/session purge, monitoring, and expiry. |
| WP-A6 | ⚠️ OWNER BLOCKER — F31 | Shared role keys plus a caller-selected roster actor cannot revoke one departed holder or guarantee who performed a write. Rotate/offboard the exposed shared credential now. Before flip, the owner must either approve individually revocable sessions with immutable member IDs, or explicitly accept the residual impersonation/revocation risk with compensating controls and a time limit; do not call current actor labels trustworthy attribution. | credential/session decision + offboarding proof |

## 4. Readiness-sprint probe ledger (2026-07-11)

| Probe / work item | Status | Evidence / next safe step |
|---|---|---|
| B3 legacy-comment echo (§12) | ✅ DONE | `scripts/b4-comment-echo-probe.js` snapshots the TEST deliverable thread, seeds one app comment through `deliverable_write`, sends the matching legacy Linear comment, proves exactly one app copy and no duplicate inbound event, then deletes the Linear comment and restores the original thread. Proof events `7769`–`7770` retain the audit trail; the TEST issue ended with its original two Linear comments and zero app comments. |
| B4 strict-AND outbound echo + TEST create→status→comment→due (§1.5.6) | ✅ DONE 2026-07-11 (staged dark) | `linear-outbound`, guarded write wrappers, and the durable outbox are deployed behind `linear_outbound_enabled={"mode":"off"}`. Final fail-closed run `b4-1783817733488-ff8283` passed 17/17: shadow (4 intended writes, zero Linear mutations), then live TEST create/status ladder/atomic local-first comment/due/assignee/title/priority/parent/archive/restore, 23 strict echo drops with zero unexpected mirror events, pause/resume newest-edit-wins using the authenticated source-edit clock, mid-drain off, idempotent re-drain, cleanup, and reconciler 0/0/0. No production authority or runtime flag changed. |
| Manual assignee eligibility (F94) | ❌ OPEN / OWNER DECISION | The picker and gateway accept any active same-team roster row and do not require a usable Linear mapping before native commit. Ratify exact creative roles, serve one server-authoritative eligibility projection, and reject ineligible/unmirrorable targets before state/outbox writes; retired mode removes only the provider-mapping requirement. |
| Foreground Production convergence (F95) | ❌ OPEN | Operational rows load once; the 30-second timer refreshes authority only, return events trigger data reload, and the normal UI has no manual refresh. Add realtime/catch-up or bounded foreground polling plus stale UX and prove two foreground tabs converge across every creative handoff. |
| Touch-mobile personal queue (F96) | ❌ OPEN | The ≤900px layout hides the sidebar containing My issues and the visible palette trigger; the mobile top bar has no personal/team switch. Add touch-visible navigation and prove fresh/reload/account-switch/breakpoint behavior after F37. |
| Detect-only foreign-write alert (§1.5.8) | ⚠️ BLOCKED | `linear-inbound` records `foreign_write_detected` when the affected team's canonical authority is `syncview`. With both teams Linear-authoritative and runtime-flag changes forbidden, no truthful live drill is possible. F55 must first make every consumer reject the legacy `supabase` alias consistently. Then use an owner-approved TEST-only authority override and F27-safe recovery; do not count the source test as gate evidence. |
| D-9 nightly due-date roller | ⚠️ BLOCKED | Read-only forensics cleared the inspected n8n/GitHub scheduled writers and narrowed the remaining search to workspace Apps Script/OAuth/credential consumers. **Owner action, not executed:** inspect triggers/executions and source under every relevant workspace/service owner; disable, do not delete, the exact writer and observe two nights. If unresolved, first map every shared `Form` and personal-key consumer, then obtain explicit owner-signed detect-only acceptance with a drilled alert/tolerance contract. Any rotation is a separate evidence-based credential action, never an automatic fallback or proof the roller stopped. |
| Legacy Calendar/Samples/Workload fast inbound (F46) | ❌ OPEN | Live 2026-07-13 readback: `MJbMZ789B5ExZz9x` is inactive/unpublished, its saved five-node graph is authority-gated, a 24-record crash cluster precedes it, and there is no later execution. Current healing is scheduled. Owner must choose and TEST-drill repaired/published fast path versus reconciler-only SLA, then capture active state, active-version/node fingerprint, and last-green execution. Do not blind-publish the saved graph. |
| Urgent-review notification truth (F47) | ❌ OPEN | The urgent-review action can display **Sent** when no exact recipient was resolved. Make unresolved delivery explicit, persist recipient-specific receipt evidence, and alert on pending age before relying on this path after Linear inbox retirement. |
| Editors-week confidentiality and completeness (F48) | ❌ OPEN | The current endpoint is unauthenticated and paging completeness is unproved; the proposed native parity path also misses transitions and historical attribution. Contain access immediately, then prove role-filtered, fully paged native parity before B5. |
| Backup and capacity gate (F13/F49) | ❌ OPEN / CURRENT N8N SUCCESS NOT RESTORE EVIDENCE | The weekly n8n graph continues critical errors and substitutes empty dumps, with no expected manifest/hash/readback; a green run can be partial. Build D-1 independently, preserve a complete last-known-good pointer only after verified readback, then prove Dashboard headroom/PITR/spend controls and one timed scratch restore. |
| Deliverable → card status projection (F50) | ❌ OPEN | Linkage IDs exist, but a Production deliverable status change does not currently project the Calendar/Samples review status. Either implement an explicit mapping or move every reader to canonical deliverable state before a team flip. |
| Reproducible EF release and rollback (F51) | ❌ OPEN | Only 7/24 functions use the deploy Action and tracked function config covers 17/24 slugs; three omitted write functions also lack any tracked deploy command. Pin dependencies/tooling, declare every slug/auth posture, and prove an all-function release manifest plus supported exact-artifact capture/restore before go-live; do not claim rollback from mutable repository source. |
| Provider credential containment (F52) | ❌ OPEN | A plaintext provider credential remains live in one reachable workflow branch and readable in retained workflow versions. Its complete consumer, access, scope, and provider-side usage inventory is unverified. Remove the literal, rotate/split as evidence requires, and prove every retained consumer. |
| Canonical Graphics artifact path (F53) | ❌ OPEN | The ordinary designer workflow lacks a protected canonical deliverable-file write/read path. Do not substitute organizer-only card-shadow edits; define assignment, upload/link, replacement, visibility, and audit behavior end to end. |
| Inactive-client queue/write exclusion (F54) | ❌ OPEN | Inactive clients can still contribute renderable Graphics work and their deliverables remain staff-writable. Add queue filters plus server-side authorization and define the offboarding disposition before a Graphics flip. |
| Authority-value split brain (F55) | ❌ OPEN | The browser accepts only `linear\|syncview`, while several backend/automation consumers still normalize legacy `supabase` to `syncview`; this page previously instructed `supabase`. A legacy value can make the UI fail read-only while inbound/outbound act SyncView-authoritative. Standardize and enforce one vocabulary, migrate/reject aliases, and add an all-consumer contract test before any authority drill. |
| Current-state freshness (F56) | ⚠️ THREE DOC CORPORA RECONCILED / MECHANICAL GATES OPEN | Operative topology, authority-gated Production capability/testing, and design-kit resume routing now agree. Prototype handoff/loop files are non-operative; current wired tests are the only runnable design route. Generate one expiring machine manifest covering Pages SHA, flags, deployed functions, n8n graphs/last green, and schema; add active-doc semantic/legacy-command lint plus an independently pinned artifact-oracle identity; the owner flip must consume that exact preflight token. |
| Samples Old two-phase retirement (F57/F117) | 🚨 CLIENT-LINK + RECOVERY BLOCKER / PHASE 2 OWNER-GATED | Staff hashes are retired, but a valid old client URL loses its binding in generic SXR. The dormant `?sv2=0`/automatic Sheet reader is not coupled to its legacy Sheet+Supabase fan-out writers; Sheet errors can continue into successful Supabase responses. Fail the link closed and never use that selector as writable rollback; inventory stale builds/callers/store parity, then execute Phase 2 only with snapshots, zero-use and route/client proof. |
| Dual outbound containment (F58) | ❌ OPEN | F2 normal outbound and F4 parity write independently. Unknown/mixed incidents must stop users and disable/read back both lanes; add a TEST dual-kill drill plus lane-specific pager evidence. |
| Flip-order enforcement (F59) | ⚠️ DOCS FIXED / GATES OPEN | The canonical order now puts every identity/writer/reader/predicate/alert/recovery gate before mutation. Enforce it mechanically so stale prose evidence cannot authorize a flag action. |
| B5 restore truth (F60) | ❌ OPEN | Deactivate/archive is preferred, but each teardown action still needs an exact private restore object, recreation procedure, machine readback, owner approval, and TEST drill before it is reversible. |
| Atomic B5 cutoff (F61) | ❌ OPEN | The corrected grace-end order still lacks a server-side maintenance/write-freeze or atomic high-water transition. Add fail-closed UX and stale-tab/retry/automation race proof before retired mode. |
| Finished-history recovery (F62) | ❌ OPEN | The old DELETE cannot execute safely against current comment foreign keys and would orphan audit history. It is withdrawn; preserve by default until a dependency-safe, owner-reviewed, TEST-rehearsed recovery exists. |
| Paste-ready operator actions (F63) | ❌ OPEN | Runbook snippets are now single-action CAS/readback transactions. CI must parse every fence and exercise each forward/kill/recovery action on an isolated TEST flag store before owner use. |
| Public-repository privacy incident (F64) | P0 OPEN / PRIVATE CLEAN PACKAGE READY / PUBLIC TREE+HISTORY OPEN | Reviewed schema-only replacements pass the private count-only census but are withheld from this public branch because GitHub expanded their deleted rows despite an exact-path diff guard. Use them only inside the owner-scheduled history rewrite; then complete Support, cache/fork/reclone and wider tracked-identity/provider-link remediation. |
| Public Actions data distribution (F122) | P0 PARTIAL / ARTIFACT SUBLANE CONTAINED LIVE | Both unsafe artifact producers are disabled and all 414 named retained B1/Production/review/Argos bundles are deleted; anonymous enumeration returns zero for those names. Candidate B1 source is aggregate-only and candidate Production source has no public visual/Argos delivery. Keep both disabled through merge, prove the first post-merge behavior, then close the still-open reconciler logs and historical external Argos review. |
| Samples finish-log persistence (F65) | ❌ OPEN | `kasper_finish_log` is sent but no current column/writer persists it. Add bounded schema/writer support and prove Finish/re-finish/undo/retry/refresh/second-device equality. |
| Shared alert-secret blast radius (F66) | ❌ OPEN | Removing the shared alert secret also disables onboarding fallback notifications. Use lane-specific controls, split configuration, and prove both alert paths independently. |
| Auth-preserving Track-A recovery (F67) | ❌ OPEN | Unlisted clients, failed flag reads, and some EF failures select unauthenticated n8n writers. Block allowlist rollback, fail visibly, and require equivalent immutable principal/client scope or retire every fallback before enforcement. |
| Comment-import recovery (F68) | ❌ OPEN | The advertised full DELETE has dependent self-links and subsequently connected/versioned rows. It is withdrawn; require a current dependency/version preflight and TEST-rehearsed preserve/relink/delete transaction. |
| Completed comment-import replay (F103) | ⚠️ DOC QUARANTINED / RUNTIME GUARD OPEN | The executed playbook exposed reusable full-apply commands with a consumed run ID, while the script can write newer same-run source state. Commands are inert in this PR; add a server-side terminal-run ledger/CAS, fresh-source authorization contract, and completed-playbook lint. |
| Calendar-v1 cleanup safety (F104) | ⚠️ DELETION RECIPE QUARANTINED | The former checklist falsely called every user v2 and its deletions behavior-preserving; it would break a live v2 metadata reader and the current opt-out/recovery path while named tests stayed green. Require owner retirement, usage telemetry, replacement recovery, whole-repo consumers, full cross-surface tests, and F60 per-object proof. |
| Calendar v1/v2 read-write authority (F125) | ❌ OPEN / ADVERTISED ROLLBACK WITHDRAWN | `?v2=0` **and normal Supabase-read failure** read the legacy Sheet, while full-roster routing still sends upserts/reorders to Supabase-only EFs. Work can disappear after cache/optimistic windows and stale whole-card state can overwrite unguarded fields. Make either fallback state read-only now; build one coupled recovery authority plus complete CAS/cross-product proof. |
| Production aggregate browser gate (F105) | ❌ OPEN / LIVE CI RED | Fast PR coverage passes, but current-main interaction and heavy lanes fail on superseded B2 picker assumptions; heavy failure skips review-packet/Argos generation. Split locked live-read/zero-mutation from fully mocked writable cases, require long lanes before merge, and make the aggregate gate plus fresh artifacts green. |
| Atomic client enrollment/offboarding (F69) | ❌ OPEN | Onboarding omits static Track-A routing lists, so first writes fall back anonymously; offboarding does not atomically revoke the token and the verifier omits client activity. Make mapping, token revision/revocation, auth routing, first-write proof, and teardown one idempotent workflow. |
| Durable onboarding acknowledgement (F110) | ❌ OPEN / VERIFIED LIVE GRAPH + FICTIONAL BROWSER | Standard and AI forms clear recovery state after an intake-row 2xx, while credential import is fail-soft and provisioning runs without a job/step ledger; duplicate-row success bypasses both. The Kasper snapshot omits fallback/dead-letter work and status/actions/paging/freshness. Persist one resumable job with truthful readback plus an operable inbox and TEST every partial-failure/stale-device boundary. |
| Onboarding privileged-provisioning authorization (F128) | 🚨 P0 OPEN / VERIFIED LIVE GRAPHS | Public submit can launch real Drive, CRM, Slack and vault side effects after only caller-supplied identity/email; there is no invitation/correlated sale/staff approval, sandbox or complete inverse. Split capture from a server-owned authorized job and do not run the fake-client drill until provider interception plus exact teardown exists. |
| Onboarding secret-to-Slack boundary (F129) | 🚨 P0 OPEN / VERIFIED LIVE GRAPH | The active full-brief builder includes raw account-access answers in a workspace-public channel and fallback DM although UI/docs say securely stored/excluded. Structurally allowlist nonsecret fields, emit only vault receipt metadata, and complete private Slack/history/rotation incident review before further credential-bearing onboarding. |
| Current onboarding operator handoff (F111) | ⚠️ DOCS CORRECTED / LEGACY OBJECT + ACK ALARM OPEN | The runbook's replaced Notion entry is removed. Its active-labelled legacy workflow still reports no production trigger and no retained executions. Use the SyncView job/inbox as sole entry, page independently on capture without staff acknowledgement, and retire the old object only through F60 after zero-use proof. |
| Post-enforcement auth recovery (F70) | ❌ OPEN | A global return to permissive reopens protected access and is not routine rollback. Preserve enforcement; build scoped break-glass with containment, session/cache purge, monitoring, and expiry. |
| Mirrored lifecycle master (F71) | ⚠️ MATCHED QUARANTINE DRAFTS / UPDATE OPEN | This PR and `synchrosocial` draft PR #51 carry a byte-identical non-operative warning. Neither is merged. Complete and atomically land the line-by-line two-repo update plus drift check. |
| Historical QA security labels (F72) | ⚠️ REPORT CORRECTED / CENSUS OPEN | The EF routing report's old PASS called anonymous n8n selection fail-safe and exposed the TEST identity. Its rollback is withdrawn and identity scrubbed; audit the remaining QA/history corpus for security conclusions and identifiers. |
| Samples fresh deep-link/reload (F73) | ❌ OPEN / VERIFIED LIVE | GA `_sxrEnabled()` defaults on, but three boot copies and CI still require the old opt-in. A fresh staff Samples route mounts, then reload drops to home. Unify boot semantics and drill desktop/mobile/staff/card/client/opt-out/token cases. |
| Private fixture/report migration (F74) | ❌ OPEN / GENERATOR CENSUS EXPANDED | Live fixture literals remain, and many manual report tools can write tokens/links/identities/bodies/IDs to stageable caller-selected worktree paths. Move to private config/fictional data; centralize output-path refusal, ignores and strict aggregate serializers; recursively scan generated trees and prove clean checkout. F122 owns active public Actions copies. |
| Operative Samples/reconcile docs (F75) | ⚠️ QUARANTINED / REWRITE OPEN | “Must keep” Samples and reconcile guides asserted default-OFF, active `MJb...`, and safe anonymous fallback. Current-state banners/reclassification block use; finish line-by-line rewrite and stale-epoch lint. |
| Weekly-report confidentiality/integrity (F76) | P0 PARTIAL / LIVE ANON PATHS CLOSED | The function now returns `401` to anonymous report reads and manager sync, the signed service caller reaches its authenticated branch, and both underlying tables deny anon SELECT. Candidate Pages auth plumbing still needs merge/browser proof; individual SMM sessions, access-log review and incident disposition remain open. |
| Onboarding list confidentiality (F77) | P0 PARTIAL / LIVE ANON PATHS CLOSED | All three onboarding list readers now authenticate before service-role access and return `401` for missing/wrong keys. Candidate Admin callers obtain the key after verified sign-in and need merge/browser proof. Wildcard CORS, full-list background discovery, shared-key lifecycle, logs/embedded links and incident disposition remain open. |
| Thumbnail revision scanner security (F78) | ✅ DONE / VERIFIED LIVE | The scanner now requires a dedicated scheduler signature, fails closed with `503` when the server credential is absent and `401` for a wrong caller credential, bounds work, and returns aggregate counts only. TEST-only same-link/no-write/change proof passed; the initial all-active scan checked 239 with 0 failed, and the first scheduled GitHub run completed green with all 239 unchanged. |
| Thumbnail folder resolver authorization (F79) | ❌ OPEN | `thumbnail-folder-resolve` remains anonymously reachable with service-role/Drive authority. Require originating principal plus exact client/row scope, bounded/audited work, and deployed negative/correct-scope proof. Scanner closure does not close this separate resolver path. |
| Thumbnail folder resolver atomic CAS (F80) | ❌ OPEN | The resolver's final write still lacks exact normalized URL plus row-version compare-and-set. Require zero-write stale handling and reversed-completion, retry, clear/archive, and Calendar/Samples concurrency proof. |
| Public onboarding-capture abuse (F81) | ❌ OPEN | Anonymous caller-controlled service-role upserts/alerts lack nonce, rate, size/schema/kind, ownership, and immutable chronology. Add bounded submission sessions and adversarial TEST proof. |
| Filming-plan confidentiality (F82) | P1 PARTIAL / EF CLOSED / TABLE MERGE-GATED | The live Edge reader returns `401` for missing/wrong keys. Candidate Pages routes the direct read through that reader, but current Pages still uses raw REST, so anon table SELECT remains intentionally open until the caller merges and is proved. The public row-bearing migration remains F64 history-rewrite work; least-field role/client scope and private document-sharing review also remain open. |
| Thumbnail revision metadata confidentiality (F83) | ✅ DONE / VERIFIED LIVE | Raw browser table access now returns `401`; an unsigned private object request returns `400`. The exact protected role/card projection succeeds, cross-client scope returns `403`, and only short-lived signed image URLs are returned. Desktop and mobile Previous/Current comparison were verified on an owner-selected real Calendar card without exposing client identity, raw paths, or Drive metadata. |
| Credential vault least-secret/audit boundary (F84) | ❌ OPEN | List bulk-delivers plaintext before visual masking; direct extraction bypasses the fire-and-forget reveal audit; shared/legacy keys lack member binding; first password edit would retain old/new plaintext in history. Require individual sessions, metadata-only list, one-secret fail-closed reveal, and no plaintext history. |
| Full onboarding read authorization (F85) | ❌ OPEN / OWNER QUESTIONS | Shared admin/legacy-key possession bypasses active-member revocation and read audit for the full corpus, including retained credential arrays. Inventory holders, retire fallback, bind active immutable admin, audit/minimize reads, and decide who may retain the arrays and why. |
| Staff/client directory minimization (F86) | ❌ OPEN / VERIFIED LIVE | Raw anon tables expose inactive rows plus email/Slack/Linear/project mappings beyond the SPA's least-field need. Revoke raw access after mapping readers and deploy purpose-specific active projections. |
| Verifier request/audit resilience (F87) | ❌ OPEN | Public verifiers lack request controls, uniform denials, and bounded event retention; every request synchronously writes audit and failures can feed F38. Add controls and TEST-only outage/capacity proof. |
| Operational read-confidentiality decision (F88) | 🚨 P0/P1 OWNER DECISION / VERIFIED LIVE | Twenty nonempty operational tables are directly anon-selectable, bypassing token/UI enforcement. Explicitly accept every exposed field as public with legal/client review, or migrate to scoped projections and revoke raw policies. |
| Client-token evidence semantics (F89) | ❌ OPEN / LIVE WINDOW NOT READY | `ok` means allowed, so permissive tokenless opens look successful. Seven days contain 588 missing-token true events and zero valid events. Require separate validity/revision fields and an exact full-roster machine gate. |
| Public Linear mutation routes (F91) | 🚨 P0 OPEN / VERIFIED LIVE CONFIG | Active status, comment, video-intake, and graphics-intake n8n webhooks authenticate no caller; authority gates constrain direction only and currently allow both Linear-authoritative teams. Contain now with active immutable staff identity or a short-lived exact-client intake capability, server-side scope, audit/limits/idempotency, and deployed negative tests. Owner decides whether shareable intake remains a supported product contract. |
| Sales Intake caller authorization (F106) | 🚨 P0 OPEN / VERIFIED LIVE CONFIG | The active privileged paperwork workflow authenticates no caller, while its browser surface uses only a per-tab query unlock. Owner ratifies Kasper/Admin reach; bind an active individual session and server-derived actor before ledger/provider/email work, then prove denial and replay controls or deactivate the route. |
| Sales Intake durable completion (F107) | ❌ OPEN / VERIFIED LIVE GRAPH | Both send branches return success before Gmail, then discard the browser draft; preview-send trusts browser-round-tripped row/contract/link values and no request idempotency key exists. Add a server-owned receipt/state machine, truthful pending/completion UX, and TEST partial-failure/retry proof. |
| Provider callback authenticity/correlation (F115) | 🚨 P0 OPEN / VERIFIED LIVE CONFIG | The invoice and contract callbacks do not validate native provider signatures or correlate the event to server-owned sale state; one has no gate and the other trusts a static caller-body token. Both acknowledge on receipt. Contain them; persist a replay-safe verified/correlated provider inbox before 2xx and pass the full negative matrix. |
| Atomic contract+payment onboarding gate (F116) | ❌ OPEN / VERIFIED LIVE GRAPH | Each mirrored callback evaluates the other flag and `onboarding_sent` from a pre-write contact snapshot. Concurrent valid events can lose the email; duplicates can dispatch it twice; async children have no durable receipt/reconciler. Replace with one unique resumable job and synchronized race/failure proof. |
| Feature-contract rollout truth (F108) | ✅ DONE IN THIS PR | Four active feature docs and their deployed migration records now describe current/historical state instead of pending SQL/n8n/branch work; stale Kasper source comments are corrected, private/obsolete resume references are removed, and truth-sync rejects the known executable stale-rollout markers. |
| YouTube title approval integrity (F109) | ❌ OPEN / VERIFIED SOURCE | Generic SMM/Collaborative-client `name` edits can change an Approved title without clearing its approval stamp/status or writing an old/new event. Ratify material-edit semantics, enforce atomically server-side, expose approval age, and prove both-role/concurrent/offline/second-device cases. |
| Media/caption approval revision binding (F113) | ❌ OPEN / VERIFIED SOURCE + CURRENT #813 | Calendar/Samples remove Approved components from client review, but later staff media/caption edits preserve the old status/stamp; Calendar Collaborative clients can also edit approved caption text. Bind approval to a server-owned material revision/hash, invalidate or visibly age it atomically on change, and prove same-link replacement plus approval/edit races before relying on sign-off. |
| Native card navigation/ownership (F112) | ❌ OPEN / VERIFIED LIVE SOURCE + CURRENT #813 | Calendar/Samples still render Linear URL buttons and never join their native deliverable IDs to `deliverables → team_members`; the promised `View sub-issue` link/editor chip has no test. Build an authority-aware per-component projection and pass mixed-team/reassignment/missing-link/mobile/second-device/Linear-unavailable proof before either flip or B5. |
| Legacy Samples client-link binding (F117) | 🚨 P0 OPEN / VERIFIED LIVE SOURCE | A valid old `v=samples` link verifies one client, then the retirement redirect discards that binding and generic SXR can select another client from pins/preferences/Add-client while client actions remain active. Fail closed or mount one server-bound exact-client portal; prove cross-client denial across fresh/residual/mobile/second-device cases. |
| Public onboarding-media provenance (F118) | ❌ OPEN / VERIFIED PUBLIC TREE | Forty-three hosted onboarding media files include many documented client-source derivatives and an audio set without recorded provenance. Rights are unproved, not presumed absent. Privacy/legal classifies or replaces every file, coordinates removal with F64 history/cache handling, and adds provenance CI. |
| TikTok Pilot compliance/eligibility (F119) | ❌ OPEN / VERIFIED SOURCE + PROVIDER GUIDANCE | Current source auto-selects/disables `SELF_ONLY` and lacks the music-use declaration, contradicting current provider rules and the audit pack. Keep review/posting disabled; implement/test exact UX and obtain a provider-backed owner/legal eligibility decision for agency-operated client posting. |
| Onboarding recovery guidance (F120) | ✅ DONE IN THIS PR | The current Edge-reader failure no longer tells operators to revive historical n8n list routes; the feature contract now points to Kasper → Onboarding/current readers and preserves F77/F85's access boundary. |
| Kasper mobile subtab reachability (F121) | ❌ OPEN / VERIFIED DEPLOYED SOURCE + FICTIONAL BROWSER | At 390 and 768 px the max-content eight-tab row expands/pans the entire page, later deep-linked tabs stay off-screen, and tab semantics/arrow navigation/active reveal/Back traversal are absent. Add contained touch scrolling, accessible roving tabs and owner-ratified history; pass every key/width/zoom/orientation/populated/second-device case. |
| Kasper Review/Messages failure recovery (F130) | ❌ OPEN / VERIFIED SOURCE + FICTIONAL FAILURE | A rejected shared load leaves Messages on its loading skeleton forever; Review renders an error but no Retry. Use an active-tab-aware error/cache state and visible Retry; pass cold/cached/retry/tab-switch/mobile/keyboard failures. |
| Project Central atomic replacement/auth (F123) | 🚨 P0 OPEN / VERIFIED LIVE GRAPH | Unauthenticated load can turn one failed Sheet read into a valid partial tree; save clears all three live tabs before validation/append and has no staging/revision/idempotency/restore receipt. Contain; require role auth and an atomically validated staged version with full failure/concurrency proof. |
| Client analytics coverage truth (F124) | ❌ OPEN / VERIFIED LIVE GRAPH + AGGREGATE RUN TOPOLOGY | A retained Metrics run failed on its first append and skipped the remaining 25 roster clients after the PostTracking path ran. Four green Top Videos runs collapsed 4–7 of 15 configured YouTube lanes per run into an undifferentiated no-source branch while still writing all 29 client results. The SPA can present that output—or a seven-day cache—as current/no-content. Add typed per-client/platform receipts, last-good degraded UI, isolated retries and full failure/valid-empty/cache-recovery tests. |
| Linear sub-issue expansion completeness (F126) | ❌ OPEN / VERIFIED LIVE GRAPH + SPA | The first unpaged children/comments response is accepted as complete and drives Calendar import/link/status writes; child 51+ and later-page links can disappear or be misclassified. Require exhaustive cursors + complete receipts and zero mutation on partial data; retire or harden the retained `/add-to-calendar` branch. |
| Stale-build/authority-epoch enforcement (F127) | ❌ OPEN / VERIFIED PUBLIC SOURCE + OFFLINE BROWSER | The update banner is absent in direct Production, probes 404 on onboarding aliases, can baseline a cached old page to the new ETag, and is dismissible while old code continues; protected calls carry no build/epoch. Embed/telemeter the running build, define optional versus mandatory releases, and reject below-minimum callers before mutation with draft/queue-safe reload and fresh auth proof. |
| TEST versus real-client soak signal (F90) | ❌ OPEN / CONCURRENT NUMBER COLLISION RESOLVED | TEST drill diffs feed the real soak/pager criteria. Split source aggregates, retain a TEST diagnostic, prove mixed/TEST-only/real-only behavior, and keep private TEST identity out of public evidence. |
| Living-truth/operator coverage guard (F93) | ✅ DONE IN THIS PR | Freshness date + resolvable ancestor commit + 30-day maximum, briefing/register maximum, open-P0/P1 operative-doc presence, composed Edge routes, and literal/composed counts are machine-checked. These checks prevent silent omission; structured control meaning and live semantic verification remain human-owned. |
| Auth-enforcement forward transition (F97) | ❌ OPEN / CANONICAL STEP ADDED | GO_LIVE Phase 0.75 now binds F5 to exact current-token evidence, stale-verdict/session purge, readback, and negative proof before any real cohort. Underlying Phase-0 findings and the owner enforcement drill remain open. |
| First-handoff forward transition (F98) | ⚠️ DOCS CORRECTED / EXECUTABLE GATE OPEN | The prior F1→F2 order could accept native work with normal outbound still off. Prove both-team real/non-parity normal-lane zero, pre-arm/read back F2 live, and require correlated terminal drainer/credential receipts plus an observer outside n8n, with zero normal writes (classifying any parity writes) and a fresh zero while both teams remain Linear; only then execute Graphics F1. A fresh pager timestamp or quiet run is not health (F131/F132). Paused nonzero is not readiness. Add machine order/preflight enforcement and interruption drills. |
| Production calendar-day semantics (F99) | ❌ OPEN | Relative due inputs/calendar today use a script-load-frozen local date while overdue uses fresh UTC. Ratify one zone contract, compute on demand, re-render at midnight/return, and pass fake-clock timezone/DST/long-open tests before creative due writes. |
| Production due-year integrity (F100) | ❌ OPEN | Mouse quick options/calendar cells drop the selected year and stored `MM/DD` cannot seed the picker. Carry ISO values separately from labels, seed from `dueRaw`, unify mouse/keyboard/bulk conversion, and pass Dec→Jan/leap/future-year tests. |
| Single-team intake component integrity (F101) | ❌ OPEN / OWNER DECISION | Current and parked Submit can create one creative leg while materializing the absent sibling as `In Progress`; readiness counts it but no control can advance it. Enforce the locked paired model and repair/classify single-link rows, or ratify explicit active/N/A semantics across every consumer before #813 or a team flip. |
| Unknown-client route isolation (F102) | 🚨 P0 OPEN / VERIFIED FICTIONAL INPUT | Any nonempty `?c=` bypasses the password before resolution; an unknown slug skips token verification and falls into staff Home, while `c`+`prod=1` reaches Production first. Resolve+verify before data/cache/route entry, fail every cross-route combination closed, and require individual staff sessions for staff views. |
| B1 incremental-refresh checkpoint/receipt (F131/F132) | ❌ OPEN; HAPPY-PATH SAMPLE RETAINED | Private pre/post snapshots bracket a successful pager dispatch and identifier-free DM, but per-row, success, and failure records share one action. The cursor and age-only pager can therefore advance/look green after partial or failed work, and an earlier pager lane can suppress this dispatch/observation. Keep the old successful run only as happy-path evidence. Require a distinct success-only terminal type, durable last-success high-water, exact expected/attempted/written validation, failure convergence, correlated terminal receipts, and an observer outside n8n before treating B1 as healthy. Disable `Gate Incremental Refresh 30m` to stop only this dispatch, or disable the pager for the global cadence/alert kill. |

## 5. Plain-language answers (owner questions, 2026-07-11) — with spec citations

**"The cards and the new-Linear sub-issues are the same thing, right?"** Almost — they stay two
linked rows, on purpose (locked decision 6; spec §2.3, §9.6). `deliverables` is the single source
row per task (13-status production vocabulary); the calendar/samples **card** keeps its own row
and its own 8-status review vocabulary (D-2), joined by `video_deliverable_id` /
`graphic_deliverable_id` ↔ `card_id`. A card's video slot and graphic slot each point at their
 own deliverable — same reason a card has two Linear links today. Realtime keeps Production and
 Workload readers current from the deliverable row, but **it does not presently project a changed
 deliverable status into the card's separate review-status field (F50)**. **F112 confirms that
 neither card surface currently dereferences the native ID for the promised assignee chip or
 D-20 Production deep-link.** The rows are linked, not interchangeable; the review and
 production surfaces keep different vocabularies and lifecycles. A team flip is blocked until the
 projection contract is implemented or every affected reader uses canonical deliverable state.

**"What happens to all the cards linked to Linear sub-issues at cutover?"** Already handled
continuously, not as a one-time migration: linkage maintenance (§4.3.5) keeps
`*_deliverable_id` current (drained to zero 2026-07-11; reconciler v2 watches it as a lane). At
each team's flip, the four link-keyed predicate families re-point from `linear_issue_id` to
`*_deliverable_id` (§9.2): the status-pill lock, dupe-link warning, link nudges, Kasper-queue
gates. After B5 teardown, the Linear link columns stay on cards but inert (§13.4.i).

**"What about the Linear submission tab?"** On current `main`, the visible **Submit** tab (internal
`linear`) still creates in Linear and mirrors in. Authority-gated Production write controls shipped
in PR #812, but that does not reroute Submit. The broader Calendar/Samples/Submit native-gateway
work is proposed in unmerged PR #813; the D-32 cohort flag required to make that rollout safe is
absent from both the inspected branch head and the live runtime.
No owner handoff may rely on it until source, flag, deployment, readback, and TEST behavior are all
proved together. At B5 the `linear-subissues` webhook is retired only after the native intake path
and its parity gates pass (§13.4.g).

## 6. B4 rollout & UX — owner decisions (ratified 2026-07-11)

Recorded here in plain language; the authoritative entries are spec §14 **D-19…D-32** (this
section is the operational reflection, not a competing source).

- **Rollout model (D-32 + D-28; supersedes D-25's simultaneous-team instruction):** the historical
  outbound-pipe proof used no per-client pilot. The human write-UI epoch is different: enroll staged
  real-client cohorts through `write_ui_reroute_clients`, prove parity while team authority remains
  Linear, then roll **Graphics first**. Video stays Linear-authoritative until the Graphics soak is
  boring and every gate is re-proved. The cohort flag is not deployed yet, so this cannot run now.
- **Reversible pause (D-26, mechanism superseded by F27/F58):** immediate containment is stop new
  team mutations and disable the involved outbound lane(s)—both F2 normal and F4 parity if the
  source is unknown/mixed. Authority may return to `linear` only after the team's pending,
  retry, and failed intents are snapshotted, classified/replayed/quarantined/discarded with owner
  reasons, and a machine-read team zero is proved. Durable queues and timestamp comparison do not
  make a blind flip-back safe.
- **Historical structure freeze (D-27):** outbound `parent` and `restore` operations are suppressed
  only for pre-B4 entities with explicit backfill provenance or a pre-B4 Linear completion marker.
  They remain counted as `tolerated_historical`; live-era parent moves/restores and all other fields
  remain writable.
- **Card → Production deep-link (D-20):** the card's old "open in Linear" button becomes
  **"View sub-issue,"** opening that deliverable in the Production tab in a **new browser tab**.
- **Legacy Linear-link fields (D-21):** keep them **inert but visible with a phase-aware
  disclaimer** during the fallback window; retire the field quietly after teardown.
- **Fallback grace period (D-22):** keep the **~1 week** window, but F27 invalidates “one-flag
  rollback.” Linear becomes the team's work surface only after the audited intent-resolution and
  team-zero gate above; after a clean week, freeze Linear read-only, then archive.
- **Submission form UI (D-23):** its form and behavior remain unchanged; the top-nav label alone is
  now **Submit** while its internal key/route stay `linear` / `#linear`. Only backend plumbing flips
  at B4.
- **One password per person (D-24):** consolidate the three separate keys (role / credential /
  onboarding) into the **role key** — each person types one password once, and their role decides
  reach: **credentials → admin+smm; onboarding → admin only; creative → neither.** Additive +
  reversible (old key paths kept alive during transition, then retired). Signed-in state shows an
  **account menu** (name · role + Sign out; **no Switch user**). The sign-in surface to be polished
  to a finished/premium standard and thoroughly tested (master-tester vision pass + `/human-audit`).
  Runs its own careful auth sprint *after* the B3 harness; not blocking B4.

D-24's implemented status and release evidence are tracked in WP-A3b above.
The outbound backend, TEST-only drills, sampled happy-path alert branches, and PR #812's authority-
gated Production controls are shipped. Terminal delivery, pager lane isolation/correlation and an
outside-n8n observer remain open under F09/F132. Full Calendar/Samples/Submit rerouting, the required
runtime flag, and the owner's one-team authority/live flips remain gated.

### Write-UI epoch human rollout (D-28 / D-29) — added 2026-07-12

The outbound *pipe* is proven live (flips 24/25) and then paused back to the D-26 stance. PR #812's
Production controls are live, while the remaining caller reroutes and human-safe contracts are
tracked in `LINEAR_CUTOVER_TOUCHPOINT_INVENTORY.md`. The rollout is staged, not one flip:
1. **Silent shadow soak** (~1 week): write-UI shipped, team stays on Linear, outbound in `shadow`; catch bugs with nobody watching.
2. **Graphics team live first**; video stays on Linear as the safety net; roll video once graphics is boring.
3. **Bug policy (D-29):** cosmetic/UI bugs are fixed in place (no flip); a data-integrity bug pauses *that team* by stopping affected mutations and disabling outbound. Authority reverses only after the R2/F27 snapshot, classification, resolution, and team-zero proof. No whole-company flip-flop.

### Outbound readiness criteria (D-25 / D-26)

| Criterion | Current evidence | Gate state |
|---|---|---|
| TEST shadow matrix is exact and sends nothing | Summary event `8879`: 4 `shadow_ok`, 0 written | ✅ |
| TEST live operation matrix + strict echo drop | 25 written, 1 stale-dropped, 23 echoes dropped, 0 unexpected mirror writes; disposable issues archived | ✅ |
| Pause / inbound fallback / resume preserves newer Linear work | Pause event `8942`, resume event `8944`; queued older title marked `stale` | ✅ |
| Normal-lane F2 off stops immediately and retains its queue (parity was not the stop proved here) | Off event `8946`, resumed event `8948`, idempotent re-drain `8949` | ✅ NORMAL LANE ONLY; F58 DUAL-LANE DRILL OPEN |
| Two-way reconciler returns zero | TEST authority event `8950` and post-cleanup event `8959`: diff 0 / repair 0 / linkage actionable 0 | ✅ |
| Happy-path alert branches deployed; terminal watcher contract | Post-merge Action run `29181125012` completed green. Pager execution `251537` delivered the harmless failed-write signal; execution `251672` delivered harmless backlog-growth and shadow-mismatch signals. Normal event `9003` restored a clean mode-off summary. These samples prove selected happy paths only; qll remains stop-on-error/correlated-terminal/external-observer incomplete. | ✅ sampled branches; ❌ F09/F132 terminal delivery/lane isolation open |
| Read-only full-roster shadow preflight | D-27 pre-alignment run `b4-shadow-1783877861264` returned 0 unexpected intents and reported all 73 prior findings as `tolerated_historical`. After the required SyncView-side terminal-row alignment, identical run `b4-shadow-1783878356762` returned 0 divergences, 0 intended writes, 0 repairs, and 72 historical parent tolerances. Both runs proved zero Linear mutations and unchanged flags/outbox. Public-safe evidence: `docs/audits/2026-07-11-b4-postmerge-shadow-evidence.md`. | ✅ RERUN GATE PASSED |
| Full-roster shadow window clean | Bounded deployed window (flip ids 20–23) held both teams SyncView-authoritative for 146.945 seconds: active roster had 0 unexpected intents / 72 historical tolerances; 40 additional tolerances belonged only to inactive internal rows. Drainer event `9162` was shadow with 0 writes/failures/backlog/echoes; controls were restored and post-window event `9163` was 0/0/0. The reviewer approved the pipe handoff recorded in the next row; that historical approval does not authorize the still-blocked human cutover. | ✅ SHADOW STEP PASSED; ✅ REVIEWER APPROVED FOR PIPE PROOF |
| All-client live handoff | Reviewer approved after the bounded shadow gate. Flip 24 set both teams SyncView-authoritative; flip 25 set outbound live; live drainer `9171`/`9172`/`9175` zero-write/zero-failure, reconciler `9174` 0/0/0, pager `256097` green. **Then flips 26/27 returned it to authority Linear/Linear with normal outbound off.** PR #812's Production controls are now shipped, but the complete caller reroute and F27/F35/F38/F46–F55/F58 plus every current checklist gate remain open. Re-enable is not authorized by the old proof. No team message was sent. | ✅ PIPE PROVEN 2026-07-12; ❌ HUMAN CUTOVER BLOCKED |

### Historical pipe-proof order — **superseded; do not execute for go-live**

The earlier both-team sequence has been superseded by D-28 and by the open second-pass findings.
`docs/independence/GO_LIVE_CHECKLIST.md` and `docs/ops/FLIP_RUNBOOK.md` are the only operative execution
documents. They must block until the required caller flag exists, F27's auditable per-team outbox
quarantine exists, and every relevant open gate above is closed. The current rollout changes only
one team at a time, starting with Graphics, with readback in `flag_flips` after every update. The
involved outbound lane(s) must be disabled and read back—both F2/F4 when unknown or mixed;
changing one team's authority back to
`linear` is allowed only after F27's audited intent resolution and team-zero proof. Neither action
is permission to replay an unclassified outbox.

## 7. Explicitly out of scope here

Write-path EF design for B4 (spec §4), teardown order (spec §13), and any change to gates —
all live in the spec. This file only tracks evidence and execution.
