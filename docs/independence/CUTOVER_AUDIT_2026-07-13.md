# Full Cutover Audit — 2026-07-13 (findings register)

**What this is.** On 2026-07-13 the owner commissioned an exhaustive multi-agent audit of the
entire Linear→SyncView cutover: 8 parallel system maps (SMM / Kasper / client / editors+designer /
samples lifecycle / intake / live systems / docs), 4 gap-hunting lenses (merge-day, flipped
operations, dependencies & retirement, plan soundness), adversarial verification of every
candidate finding against code and live data, and a completeness critic. **26 findings survived
verification (0 were refuted).** A relentless second pass then continued from that baseline;
confirmed second-pass findings start at F27 below. This file is the durable register so later
sessions build on it instead of rediscovering it. Full evidence (file:line cites, live queries)
lives in the audit session transcript; this register carries the operative one-liners.

**Status legend:** `OPEN` (required closure remains) · `IN-PROGRESS` (assigned/being built) ·
`MITIGATED` (risk reduced, but a named residual remains open) · `DONE` (fix merged/verified) ·
`ACCEPTED` (owner explicitly accepted the risk/behavior).

Severity: P0 breaks a real user's day or loses data at the step where it occurs; P1 must fix
before that step (merge / flip / B5); P2 has a workaround; P3 polish/comms.

## A. Urgent — independent of go-live

| ID | Sev | Finding (verified) | Next action | Status |
|---|---|---|---|---|
| F01 | P1→P2 | n8n quota reached 111,251/135,000 at Jul 14 01:13Z. The two main cuts are **verified live**: redundant Calendar trigger `AkiFmromoDkmsh39` and idle every-minute TikTok cron `LR6R1mV4NaLNLlLG` are inactive/unpublished, and the Calendar GitHub cron is dispatch-only. Insights rose 256 executions in ~2 h 40 m after the prior snapshot (short-window ~2.3k/day, not yet a 24 h rate), moving the projected cap boundary roughly to Jul 24 if sustained. One duplicate remains: Samples still has its independent GitHub schedule while the combined pager also dispatches it; recent runs contain both trigger types. F132 proves Samples is the pager's last stop-on-error branch, so an earlier lane can suppress it. **No foreign writer / no D-9 burn.** The cap remains a hard stop. | Until F132's lane isolation and terminal receipts are proved, retain the independent Samples GitHub schedule. If one duplicate must be removed now, remove the pager's Samples dispatch, not the independent schedule. Then measure a complete 24 h Insights delta and alert delivery. Owner still confirms the plan/cap raise path before headroom becomes operationally tight. | MITIGATED — MAIN CUTS DONE; SAFE SAMPLE DEDUP + 24 H PROOF OPEN |
| F09 | P1→P2 | The reconciler DID detect today's 6.4h mirror gap AND **the pager DID fire** — Codex verified qll DM'd Sidney at 05:00/06:00/07:00/08:00/09:00/10:15/11:30/12:45Z (v2_nonzero), plus a synthetic test DM (Edge Alert Relay exec 263676) delivered 22:40Z. Detection + that happy-path last mile are PROVEN (closes B6), but the general terminal-delivery contract is not. The pager is n8n, so a quota/platform outage silences it; five of six sampled load-bearing workflows had no `errorWorkflow`, three accumulated 135 error/crash/cancel records, and the central handler failed 29 of 30 sampled limit-era invocations. The pager and Edge Alert Relay have no retry/error-workflow protection. Relay returns HTTP 200 before Slack runs, so callers generally prove only acceptance, not terminal delivery; the quota watchdog is the known exception because it correlates and verifies the relay execution. F132 separately proves the pager's internal lanes can stop before later dispatches/reads/Slack. | Wire every load-bearing workflow to a tested handler; add retry/backoff, correlation, idempotency and terminal delivery receipts to pager/relay; TEST-prove one sanitized failure receipt per workflow and caller. Independently add a NON-n8n observer/page path so alarms survive an n8n outage and prove the observer itself is alive. | OPEN — INTERNAL COVERAGE + TERMINAL DELIVERY + NON-N8N BACKUP |

## B. Merge-day landmines (current post-#850 reconciliation)

| ID | Sev | Finding (verified) | Next action | Status |
|---|---|---|---|---|
| F02 | P0 | **ORIGINAL DEFECT, CURRENT SOURCE RECONCILED 2026-07-20.** Superseded #813 head `885026a` had no `write_ui_reroute_clients` read or per-client gate, so a merge could have routed the whole roster into disabled parity. The guarded implementation from `e3aa028` later landed through PR #850 / `9968bd9`; the serving lane is dark outside the allowlist, last verified TEST-only. | Treat the D-32 source/deployment boundary as present. Before any real enrollment, read the flag fresh and satisfy the remaining per-row go-live gates; formal closure of this row is outside the ratified Order-1 batch. | OPEN FOR FORMAL ROW REASSESSMENT — #850 CARRIED THE FIX; NO REAL-CLIENT ENROLLMENT AUTHORIZED |
| F23 | P1 | **ORIGINAL DEFECT, CURRENT SOURCE RECONCILED 2026-07-20.** Superseded #813 lacked F02's client allowlist/canary boundary. PR #850 / `9968bd9` landed the TEST-default cohort boundary; Pages still requires normal deploy/revert propagation, and enrollment remains separately gated. | Measure deploy/revert propagation and retain the server-side kill/read-failure behavior as a release gate. Formal closure of this row is outside the ratified Order-1 batch. | OPEN FOR FORMAL ROW REASSESSMENT — COHORT BOUNDARY LANDED VIA #850; ENROLLMENT GATES REMAIN |
| F03 | P1 | The original circulating client links were token-less; the write gateway has no permissive bypass, and live `client_access` tokens belong only in protected storage. #850 carried the exact-client link work from the superseded #813 line, but F33 remains the controlling safety/owner-decision record. | **Do not copy tokens into Sheets (F33).** Re-verify every share builder against the staff-authenticated exact-client link endpoint, then preserve D-31's human intent: each SMM re-shares their own clients' links. | OPEN — D-31 MECHANISM REMAINS GOVERNED BY F33; #850 MERGE DOES NOT BY ITSELF CLOSE THIS ROW |
| F11 | P1 | ~62 active calendar slots (~10 real clients) hold Linear URLs with no resolvable mirror row (e.g. GRA-6447, live in Kasper Approval) → post-merge those approvals hard-404 forever where n8n succeeds today. | Pre-merge sweep script + backfill (~60 mirror rows) + checklist gate "active-card resolvability = 0 failures". | OPEN (fix-pack) |
| F12 | P1 | **CURRENT RECONCILIATION 2026-07-20.** The pinned `production-write` deployment contains the guarded server-owned `GRAPHIC_TITLE_*` source path; the Graphics-only TEST harness is merged and available in the repository/Actions, not embedded in the deployment. The ordinary drill deliberately sends `skip_graphic_generation:true`, so it is not real-generation evidence. The historical 18-operation TEST receipt records create/mutations, `0/0/0`, cleanup and unchanged flags, but no generated-title result. No durable deployed-run receipt records `graphic_generation_verified:true`, and no provider-failure receipt proves zero batch/deliverable/outbox writes plus successful recovery. | Closure requires a future, separately owner-approved private-TEST real-generation run and a separately controlled provider-failure/restore run, retaining aggregate generated-title, `0/0/0`, unchanged-flags, cleanup, zero-native-write failure, and recovered-green receipts. **This documentation pass authorizes no drill, provider/secret change, runtime-flag change, or client write.** Before execution, bring the owner the exact change and exact rollback; no real-client write is permitted. | OPEN — GENERATION SOURCE DEPLOYED; TEST HARNESS MERGED/AVAILABLE; REAL-GENERATION + FAILURE/ROLLBACK RECEIPTS MISSING |
| F06 | P1 | **CURRENT RECONCILIATION 2026-07-20.** The original unpinned `production-write` v11 drift is no longer current: pinned run `29601466479` deployed `production-write` v24 from exact `main@9d76df6` after `linear-outbound` v33, and all ten workflow fingerprints passed. F143 separately closed the browser preflight defect. What remains under F06 is the unresolved TEST-contract boundary—browser credentials cannot enter the service-only TEST override—and F51's complete all-function source-exact restore contract. The deploy run explicitly left the separate drill outcome `PENDING`. | Ratify the TEST contract, align SPA/gateway and one cross-boundary test, then retain positive/negative TEST receipts under the owner-gated drill rules. Complete F51's all-function provider-source/entrypoint/JWT/release capture, redeploy rehearsal, and independent deployed-source/JWT hash readback; an old version ID is not an activation handle. | OPEN — SOURCE DEPLOY DONE; TEST CONTRACT + F51 SOURCE-EXACT RELEASE/ROLLBACK REMAIN |
| F10 | P2 | **ORIGINAL DEFECT; superseded-head grading.** Before the carried fix, a signed-out Kasper/SMM or Production creative received only a dead-end write-error toast after a protected-write 401. The repair from `e3aa028` landed via #850. | Formally re-verify session invalidation, preserved intent, sign-in recovery, and retry behavior across Kasper/SMM/editor expiry, revoked key, account switch and second device before closing this row. | OPEN FOR FORMAL ROW REASSESSMENT — FIX LANDED VIA #850; NOT CLOSED BY THIS RATIFIED DOC PASS |
| F21 | P2 | **OWNER DECISION 2026-07-16:** the startup popup for stale pre-upgrade Linear updates is not understandable or actionable for any current user. | Remove only the popup/count UI. Leave every parking/quarantine path unchanged: the stale leftovers remain parked and must never auto-send. Agents/ops can inspect them through `peekWriteUiLegacyQuarantine()` or `peekWriteUiLegacyQueueState()` if ever needed; scheduled reconcilers remain the Linear/SyncView drift-healing mechanism. | DONE — POPUP REMOVED BY OWNER DECISION; PARKED QUEUE REMAINS SILENT AND INSPECTABLE |
| F20 | P2 | **ORIGINAL DEFECT; superseded-head grading.** The native reroute initially dropped the owner-retained +2-day overdue due-date bump. The carried implementation from `e3aa028` landed via #850. | Formally re-verify the owner-ratified flag-gated behavior and rollback boundary before closing this row. | OPEN FOR FORMAL ROW REASSESSMENT — FIX LANDED VIA #850; NOT CLOSED BY THIS RATIFIED DOC PASS |
| F19 | P2 | **ORIGINAL DEFECT; superseded-head grading.** The earlier Create-Post build could offer team-incompatible batches and failed to disambiguate duplicate active names. The carried implementation from `e3aa028` landed via #850. | Formally re-verify team compatibility and duplicate-name annotations against current main before closing this row. | OPEN FOR FORMAL ROW REASSESSMENT — FIX LANDED VIA #850; NOT CLOSED BY THIS RATIFIED DOC PASS |
| F24 | P3 | At parity-arm, editors' tweak delivery transport + comment author identity change ("SyncView Mirror" + real name in body) with zero comms planned. | One comms line to both teams at parity-arm. | OPEN |

## C. Flip-day gaps (must close before the first team flips)

| ID | Sev | Finding (verified) | Next action | Status |
|---|---|---|---|---|
| F04 | P1 | **ORIGINAL DEFECT, now fixed in source:** natively-created thumbnails could not enter Kasper's queue because visibility tested only the legacy URL column. | **Implementation complete.** The shared URL-or-native-deliverable predicate now owns all four Kasper decisions and both Calendar/Samples pill locks. Keep the first real native-Graphics Kasper-queue observation as an unchecked Phase-3 go-live gate. | DONE (IMPLEMENTATION) — `e3aa028` LANDED VIA SUPERSEDING PR #850 / `9968bd9`; SOURCE + NATIVE-ID-ONLY REGRESSION RE-VERIFIED 2026-07-19. LIVE KASPER-QUEUE OBSERVATION REMAINS OPEN |
| F07 | P1 | **ORIGINAL DEFECT, now fixed in source:** flipped-team writes had only cron/pager drain latency, so the intended seconds-scale Linear check could fail on a healthy system. The earlier 157–314 ms E2E proof covered only TEST. | **Implementation complete.** A successful SyncView-authoritative write schedules the exact `syncview_live` targeted drain in background work only while normal outbound is live. Keep the first non-TEST flipped-team seconds-scale receipt as an unchecked Phase-2 hard stop. | DONE (IMPLEMENTATION) — MERGED VIA PR #850 / `9968bd9`, DEPLOYED FROM PINNED `main@9d76df6`, AND EXACT TARGET/GUARDS RE-VERIFIED 2026-07-19. NON-TEST SECONDS-SCALE SLO PROOF REMAINS OPEN |
| F05 | P1 | Flip-back rollback strands up to ~1h of the flipped team's undrained native writes (outbound pauses them; inbound then re-applies stale Linear over native rows). GO_LIVE said "nothing is lost" — false. The first documentation fix (“default drain, then flip”) was invalidated by F27: outbound-off drains nothing and the summary has no per-team zero proof. | Do not run the old drain/flip sequence. FLIP_RUNBOOK now blocks team rollback pending F27's audited per-team quarantine/classify/replay/discard path and machine-read team zero; only then may authority return to Linear. | OPEN — UNSAFE DOC REMOVED; F27 TOOLING REQUIRED |
| F15 | P1 | Editors' planned post-Linear push channel reads team_members.slack_user_id — NULL for all 20 rows; notify EF doesn't exist; no substrate replaces Linear Inbox. (n8n's hardcoded email→Slack map keeps today's ping alive mid-epoch.) | Backfill slack_user_id for active members (source: Slack member IDs / the n8n map); smoke-test the re-sourced ping on TEST before epoch re-source. | OPEN — NOT A BLOCKER (owner 2026-07-15): no consumer exists (post-Linear editor-notify EF unbuilt; today's pings ride the legacy n8n email→Slack map + Linear). Backfilling slack_user_id is deferred until/unless that notifier is built. |
| F16 | P1 | **ORIGINAL DEFECT; superseded-head grading.** The earlier pager could not see a stalled outbound path at flip volumes. The carried per-team age/retry visibility implementation from `e3aa028` landed via #850. | Formally re-verify the summary and page thresholds against current main before closing this row. | OPEN FOR FORMAL ROW REASSESSMENT — FIX LANDED VIA #850; NOT CLOSED BY THIS RATIFIED DOC PASS |
| F08 | P2 | **ORIGINAL DEFECT, now fixed in source:** both daily write-path monitors hard-required Linear/Linear authority and would turn permanently red after a team flip. | **Authority-tolerance defect fixed.** The drill accepts valid mixed authority/outbound modes and the shadow audit permits legitimate live-team queue movement while preserving flag and zero-mutation proof. Keep operational green-watch open: the latest inspected scheduled drill `29676924170` and shadow audit `29678352879` (as of 2026-07-19) are red for reconciliation/data-integrity signals, not authority-stance rejection. | DONE (FLIP-TOLERANCE DEFECT) — `e3aa028` LANDED VIA PR #850 / `9968bd9`; MIXED-AUTHORITY/LIVE-MODE SOURCE + REGRESSIONS RE-VERIFIED 2026-07-19. PHASE-3 GREEN-WATCH GATE REMAINS OPEN |
| F14 | P1 | D-9 (nightly ~23:45Z due-date toucher, unattributed). **CONFIRMED FIRED 2026-07-13 23:45:27-41Z**: ~18 real VID+GRA issues (≥5 clients) touched in a ~13s sub-second burst — but it is a **NO-OP re-save**: all 18 were ALREADY due 2026-07-14, and Linear recorded **no due transition, no actor, no bot/workflow metadata even with detailed history enabled**. Invisible to SyncView (no inbound events; reconciler v2 SUPPRESSES non-null due diffs ≤2 days at `linear-deliverables-reconcile-lib.js:258`; legacy reconcilers ignore due dates; SyncView-authoritative OUTBOUND compare is EXACT at `:374`). **Excluded actors:** shared `Form` key (its 15-min reader session began 23:45:56Z, 15s AFTER the burst), n8n, all GitHub workflows/history, SyncView Workload, visible Google Sheets. **Remaining:** a bound/private Apps Script, an external OAuth scheduler, or a Linear-internal automation. | **BLOCKED on owner-only evidence:** (a) Apps Script "Executions" + "My Triggers" under BOTH `sidney@` and `house@` Google accounts for Jul 13 17:44-17:47 Guatemala (= 23:44-47Z); or (b) Linear support's request logs for the issue IDs at 23:45:27-41Z. Then remove ONLY the matching clock trigger (or a reversible project-property kill switch); preserve the `Form` key + all other triggers. Neutralize (or owner-signed detect-only acceptance) before the Graphics flip. | CONFIRMED no-op; actor BLOCKED on owner Google/Linear-support check |
| F13 | P1 | **RESOLVED 2026-07-15 (PR #840 merged `4f9d919`): D-1's independent export/restore package is now BUILT, cloud-verified, and LIVE on `main`.** The `Track-B private backup` GitHub Action runs every 6 h, taking one HMAC-signed transactionally-consistent 14-table `pg_dump` snapshot to a private Google Shared Drive with a versioned expected-corpus/schema/count/byte/hash manifest, independent Drive list/download/HMAC readback (proof run `29444939853`, 53,819 rows), fail-closed on any missing/extra table or hash mismatch, and last-known-good advanced only on complete success. A dedicated-scratch restore rehearsal completed in 229 s with every table count matched and all 12 core orphan checks zero; the restore guard refuses the production ref. The alert path is the failed-run GitHub email to the owner (Slack optional/absent). PITR stays owner-declined as accepted residual risk. **Original finding retained below for history.** **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG + RETAINED EXECUTION METADATA; no backup body, credential, client record, or restore target was read.** Post-flip Supabase becomes the sole system of record, but at audit time D-1's independent export/restore package was never built. The weekly backup still runs inside n8n and ten critical copy/download/export/dump/upload nodes continue after errors. Its builders serialize whatever arrives; the Supabase builder explicitly substitutes `[]` for missing/failed table dumps. There is no versioned expected-object/schema manifest, checksum, completeness join, independent readback, last-known-good pointer, or restore rehearsal, and the four dumped tables are not a full logical database export. A retained scheduled run labelled success therefore cannot prove a complete restore set. | Build D-1 outside n8n with a versioned expected corpus/schema/count/byte/hash/per-object manifest; fail closed; upload then independently list/read/hash; never advance the complete/last-known-good pointer for a partial run; alert independently of n8n. Enable the owner-approved PITR posture and complete one timed scratch restore. Inject failure/truncation/error-item/valid-zero/pagination/workflow-count/readback faults at every boundary and prove no incomplete set is selectable. **All of the above is now implemented, merged (#840), and proven — the fault suite is green for truncation, error-item, valid-zero, pagination, corpus/workflow-count, and readback mismatch. Remaining non-blocker: capacity/egress evidence tracked under F49; PITR owner-declined.** | DONE — INDEPENDENT D-1 BACKUP + RESTORE REHEARSAL MERGED & ACTIVE (PR #840, `4f9d919`); PITR OWNER-DECLINED (ACCEPTED RESIDUAL) |

## D. Plan & docs (truth/executability) — fixed in this PR unless noted

| ID | Sev | Finding | Status |
|---|---|---|---|
| F17 | P1 | Three docs prescribed three different flip sequences; D-28's ratified shadow-week missing from the checklist; Phase-2 misdescribed the parity lane. | DONE — GO_LIVE rewritten as the single canonical sequence (parity soak model per D-32; D-28 satisfied by staged parity enrollment + nightly shadow audit — ratified by owner merging this PR). |
| F18 | P1 | No paste-able flag payloads anywhere; "deploy runbook" cited twice didn't exist; checklist's `enforcing` value silently fails open (code accepts only `enforced`). | DONE — docs/ops/FLIP_RUNBOOK.md created; checklist corrected. |
| F22 | P2 | ROLLBACK.md rows for #811/#812/gateway were in stale pre-merge voice; row 68 claimed "no browser caller" after the caller shipped. | DONE — rows corrected to merged-state, caller-first rollback. |
| F25 | P3 | GO_LIVE Phase 5 was 4 ownerless bullets while spec §13 has the real ordered B5 plan. | DONE — Phase 5 now links spec §13; replacement build tickets still to be assigned. |
| F26 | P2 | "No n8n execution cap left to hit" post-B5 is false: ~20 non-Linear n8n webhooks (templates, briefs, TikTok, filming plans, hook library, weekly Slack…) + Google Sheets remain load-bearing; A4/Track-A spec rows exist but are incomplete. | DONE (claim corrected in GO_LIVE) / migrations OPEN |

## E. Second-pass findings (F27+)

| ID | Sev | Finding | Next action | Status |
|---|---|---|---|---|
| F27 | P1 | **#894 PROOF SUPERSEDED; CORRECTIVE SOURCE MERGED; NOTHING LIVE.** PR #901 records the correct pre-DDL/deploy abort. The owner-merged correction closes #894's late-authorized-insert and actorless replay-echo races with a server generation fence and exact open-rollback binder, and supplies the permanent-audit `__f27_drill__` no-provider lane plus migration self-probe. The source-only operator toolkit now makes the installation executable: deterministic private outbox/definition capture, approved Shared Drive round-trip, exact source capture/restore/readback, inbound freshness, full reserved drill runner, and a runbook-derived checklist. It changes only `linear-inbound` from floating `esm.sh @2` to npm `2.49.8` with the already-produced frozen lock/config. No migration, deploy, drill, flag, authority, n8n, frozen writer, or client-data action occurs in the toolkit PR. | Cloud-review and owner-merge the toolkit. Then require two distinct owner windows under `docs/ops/F27_INSTALL_RUNBOOK.md`: (1) capture v39 and deploy only pinned inbound in a quiet window, immediately prove webhook freshness, and record its new source-exact baseline; (2) snapshot the live queue/definitions first, apply the exact migration/self-probe, deploy/read back only the remaining four fenced closures, run the reserved drill, and verify dormant. Preserve all drill audit. Keep R2 blocked until cloud review independently verifies that later live install; never use #894, the default drainer, or a global green summary. | OPEN — TOOLKIT SOURCE/CLOUD REVIEW PENDING; PREPARATORY DEPLOY AND LIVE INSTALL ARE SEPARATE OWNER-GATED WORK |
| F28 | P1 | **VERIFIED DOC CONFLICT.** Owner-ratified D-22 and B4 readiness set a roughly one-week dual-ready Linear fallback, but spec §7.3/§13 and GO_LIVE Phase 5 prescribed eight weeks. The stale value predated D-22 and sat in the canonical operator path. | **Corrected in this PR:** all three operative references now use D-22's roughly one-week post-flip dual-ready window, distinguish it from D-28's pre-flip soak, and sequence human freeze/outbound stop before archive/export and later cancellation. | DONE (docs) |
| F29 | P1 | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG.** The keep-until-B5 Workload and tweak-comment readers silently accept partial Linear snapshots. One of seven source credentials has returned HTTP 400 continuously in every sampled run since at least Jul 10; a second source also failed transiently. All inspected workflows still reported success. The Workload feeder ignores `workspaceErrors`, upserts the partial union, then runs its stale-row deactivation; both browser consumers ignore degradation. The active Linear-status metadata reader has the same contract: if any ID resolves it returns `ok:true`, while `missing` conflates genuine not-found with transient source failure. It also silently truncates the caller's deduplicated request to 100 IDs and computes `missing` only inside that slice; both browser callers send one unchunked full set, and the metadata caller hashes/throttles that full set before fetch, so stable IDs after 100 can be starved indefinitely. The status-mirror workflow ignores GraphQL errors in its state lookup and can return `ok:true, skipped:true` for an incomplete response; browsers treat that as delivered and do not retry. Current source overlap and small status vocabularies mask known failures but are not completeness guarantees. **CURRENT SOURCE-ONLY FOLLOW-UP at `07d123d`; no browser/backend request or live data was used.** The current browser issue reader also has no terminal deadline: `loadLinearIssues()` can wait indefinitely on either the Supabase path or its legacy fallback (`index.html:12613-12649`). When that read rejects, `wlLoadSnapshot()` adopts a supplied cache or the current `issueSnapshot` without carrying an issue-read degraded/error state (`:12762-12790`); initial load clears the visible error after that fallback, and the visibility refetch only logs failure (`:13222-13283`, `:13298-13310`). The prior “As of” board can therefore remain presented as current after a failed refresh. | Rotate/remove failed credentials; exhaust paging/chunking; publish expected/attempted/successful-source and per-ID tri-state (`resolved|not_found|source_failed`) receipts with `requested = resolved + not_found + source_failed`; return degraded/failed on any source failure or unattempted ID; skip destructive mark-and-sweep and do not advance a full-success throttle. Preserve last-good browser data with visible staleness and targeted retry. A status mirror may report intentional `skipped` only after a complete state lookup proves the target unsupported. Add TEST failure injection, clean dead-ID versus transient-failure cases, 99/100/101/250 IDs, target state on page two, partial GraphQL envelopes, mixed/total outage, cache/second-device behavior, and anonymized per-source visibility-set equality proof. Bound the browser issue read independently from plan loading. Any cache/snapshot fallback must retain its timestamp plus a visible unavailable/stale reason and targeted Retry; only a complete fresh receipt may clear degradation. Add the affected Workload boot/visibility-refresh sequence to F162. | OPEN |
| F30 | P1 | **VERIFIED-LIVE.** Native video intake balances lifetime deliverable history instead of current open workload. The legacy allocator intends to count nonterminal Linear children, but its unpaged query clips two live editors at 50: workflow counts are 40/50/50 while fully paged counts are 40/139/179. Native instead counts all non-duplicate history (157/231/382/488). The two flawed policies pick the same next editor only temporarily; after about ten more assignments legacy reaches a false three-way 50 tie while real loads still differ by up to 139. The spec codifies native lifetime history, so neither path is a trustworthy parity oracle. | Owner: ratify the intended workload policy. Implement one atomic, pagination-safe aggregate of current nonterminal work for active eligible editors, with deterministic tie-break/leave rules and a maximum skew. Correct §9.1; test >50 and >1,000 rows, sequential batches, and simultaneous intake; compare against independently paged live counts. | OPEN |
| F31 | P1 | **VERIFIED-LIVE current-state/security contract.** One active native creative roster row maps to an inactive Linear user, while the single credential shared by all editors/designers has not been rotated for that departure. The server accepts that group key with any caller-selected active compatible identity, and the browser persists both in local storage. Deactivating only the leaver's row therefore does not revoke a retained group key: it can be paired with another active creative identity, and allowed native comments/statuses would be misattributed. Recent auth-event review found no use attributed to the stale row; retained-key possession or misuse is a threat scenario, not a claimed observation. | Before any flip, deactivate the stale row and rotate the shared creative key with coordinated forced re-login. Add an HR-to-roster/credential offboarding procedure and reconciliation. Replace shared keys/caller-chosen actor names with individually revocable sessions carrying immutable member IDs, plus version invalidation and mismatch alerts. | OPEN |
| F32 | P0 (first flip/B5) | **INFERRED from current main, the parked write-UI branch, shipped RPCs, and canonical teardown docs.** SyncView-authoritative intake still calls Linear GraphQL to validate project/parent linkage before native commit, contradicting the promised B4 behavior that a Linear outage only pauses mirroring. An outage/key/rate-limit therefore 503s Create Post on day one. No Linear-retired epoch exists either: every native mutation supplies a required outbound intent and the database enqueues it, while B5 turns outbound off and cancels Linear. | For authoritative teams, route intake from reviewed native mappings and make Linear asynchronous mirror work. Before B5, add an owner-controlled retired epoch that removes Linear validation/assignee eligibility, mints native identifiers, and suppresses new outbox rows transactionally. Drill all TEST mutations with Linear unavailable; pre-B5 native commits may queue mirror debt, retired mode must add zero. | OPEN |
| F33 | P0 if implemented / P1 now | **VERIFIED-LIVE.** F03/D-31's ratified mechanism would publish every client write credential if tokens entered Clients Info: the sheet is anonymously downloadable through GViz, while the protected store contains unique tokens accepted by the gateway as client write authority. #850 carried the staff-authenticated exact-client link implementation, but its owner/safety decision and end-to-end proof are not closed by this batch. | **Owner question:** ratify the staff-authenticated, exact-client link-builder endpoint while retaining the decision that each SMM re-shares their own links. Never put tokens in Sheets/clientMap/bootstrap/logs. Re-verify all four builders and that anonymous sheet/roster reads contain zero tokens. | OPEN — OWNER DECISION / D-31 SAFETY PROOF REMAINS; #850 MERGE DOES NOT BY ITSELF CLOSE THIS ROW |
| F34 | P1 (B5) | **VERIFIED-LIVE.** SyncView has 16,837 archived issue rows but no archive reader/UI; all archive `comments` fields are null. Of 12,731 normalized comments, 9,231 are archive-only and cannot be requested by the sole reader, which requires `deliverable_id`. Linear-hosted upload references remain in 420 operational briefs, 603 archive descriptions, and 239 comment bodies (197 archive-only; all attachment arrays empty). These are affected-row counts, not unique-asset counts. No rescue implementation/report exists. | Before cancellation, ship a role-gated paginated archive UI/reader, rescue and privately rewrite Linear-hosted assets with a per-row gap report, reconcile archive/comments/export against final Linear, and require staff retrieval drills plus hard gates `archive_reader_live` and `zero_unreviewed_image_gaps`. | OPEN — BLOCKS B5 |
| F35 | P1 | **PARTIALLY CLOSED 2026-07-14 — VERIFIED SOURCE + LIVE DENY/TEST ALLOW.** All six writers now share fail-closed server authorization in candidate source: exactly one configured staff key or active exact-client token, canonical target scope, inactive-client denial, and server-minted actor/role/source. Caller actor/role claims and flagged reorder downgrade to anonymous n8n are removed. `calendar-reorder` v32, `sample-review-reorder` v31, `templates-save` v30, and `caption-prompts-save` v30 are live from source commit `5b8f2cc`: missing/wrong credentials return `401`; dedicated staff-automation TEST calls return `200`; fake reorder IDs touched no row, and settings rows were restored byte-for-byte with zero probe events. `calendar-upsert` and `sample-review-upsert` remain on their previous public source because current `main` reconciler callers do not yet send the new key; deploying them before the workflow change would break a merged safety lane. | Merge the candidate caller/workflow change, confirm the managed `SYNCVIEW_STAFF_KEY` secret is present, then deploy both upserts from the exact merged SHA and repeat missing/wrong/cross-client/inactive-token denial plus TEST allow/restore proof. Keep direct legacy n8n writers tracked under F67; add rate/body bounds, per-human sessions and a token-uniqueness constraint after private duplicate audit. | SIX OF SIX LIVE — calendar-upsert v38 + sample-review-upsert v39 deployed 2026-07-15 from merge SHA; shared authorizeBrowserWrite fail-closed gate (staff-key XOR client-token, client-scoped, inactive-denied, 503-on-db-error); all six anon wrong-key/no-key verified 401. Remaining: unkeyed-write telemetry gate (not yet measured). |
| F36 | P1 | **VERIFIED-LIVE TEST DRILL.** Two simultaneous Calendar-surface status writes against the TEST client used the same stale row version and both returned 200; the final row was last-writer-wins. Calendar/Samples senders omit expected status/version, and the gateway applies CAS only to Production. Both successful intents entered the TEST-only outbox; the exact original row was then restored and verified. | Require an expected canonical version on every native Calendar/Samples/Production mutation, reject stale requests with 409 before outbox insertion, adopt the current server row, and give the user compare/reapply UX. Drill every status/content/reorder/comment/compound family in both arrival orders. | OPEN — BLOCKS FIRST FLIP |
| F37 | P1 | **VERIFIED-LIVE AGGREGATE + VERIFIED SOURCE; peer-write consequence INFERRED without a live peer mutation.** Production “My issues” ignores the member identity verified at sign-in. It chooses a specially named assignee if present, otherwise the first active assignee in name order. Fresh anonymous-safe aggregates show 4,353 visible issues and seven active represented assignees; every browser gets the same 382-row queue, so only one identity sees their own work and six see someone else's. Creative status/comment authority is also same-team-wide and not assignee-bound; fixing the queue alone would still let one editor act on peer work unless the owner explicitly authorizes that policy. | Bind “My issues,” “Assigned to me,” owner-ratified team/assignment scope, comment scope, and audit actor to the server-verified immutable member ID. Owner ratifies peer-work rules alongside F136's transition matrix. Signed-out/inactive/revoked/mismatched sessions show no personal queue. Test every active creative, peer-assigned/unassigned work, account switching, duplicate names, roster reorder, direct links and zero-row members. | OPEN — BLOCKS CREATIVE FLIPS |
| F38 | P1 | **VERIFIED-LIVE SOURCE + DUMMY HARNESS.** The client-link verifier served by Pages matches current main. It allows and positively session-caches real EF 500 errors, network failures, and malformed responses; the bare `slug\|token=true` cache has no TTL, mode, or token revision. The exact function harness proved that after a 500, a later 410 is never fetched in the same tab. A fresh 410 denies correctly. Thus future enforced mode still fails open and token rotation does not revoke reads in an already-open/reloaded tab. The verifier also checks only the token row, not `clients.active`; current joins have zero inactive/missing token owners, but the first incomplete offboarding would remain valid. F88 separately proves direct anon REST bypasses this UI gate. | Before WP-A5, fail closed on every verifier error in enforced mode; require an active client/current token revision; cache only short-lived signed/versioned verdicts; revalidate on boot/focus/pageshow/online and before refresh. On offboarding/revocation, atomically revoke the token and purge client DOM/data/caches/channels/write state. Drill inactive/missing client, same-tab reload, second device, offline return, and rotation. | OPEN — BLOCKS AUTH ENFORCEMENT |
| F39 | P1 | **VERIFIED-LIVE SOURCE/CONFIG/DATA.** The protected Production comment reader authenticates an active creative identity but never looks up the requested deliverable or compares its team with the member's team. It returns full bodies/attachments/audience/lifecycle fields for any supplied deliverable ID. Live normalized comments materially cover both teams, and target IDs/team are anonymously readable. This contradicts the spec's own-team creative rule and the system map's claim that the server repeats it. | Resolve the immutable member, fetch the exact target, enforce own-team (or an owner-ratified narrower own-assignment rule), and return non-enumerating denials. Apply the same scope to totals/pages/archive/future subscriptions; add non-secret principal/target allow-deny read audit plus request controls, positive own-team and negative cross-team/unassigned/revoked TEST-safe reads, then pinned redeploy proof. | OPEN — CURRENT CONFIDENTIALITY GATE |
| F40 | P1 | **VERIFIED-LIVE SOURCE/DATA; still current after #850.** The board always reads Linear-derived `workload_issues`, falls back to n8n `/linear-issues`, keeps realtime off, caches five minutes, and emits Linear links. It ignores `prod_authority` and never reads native rows. The live table is fresh but includes 28 out-of-scope rows; a read-only parity comparison also found 13 stale ghosts and 39 genuine top-level native rows that today's sub-issue-only board hides. A naive table swap changes the set. | Build an authority-aware mixed adapter from `deliverables+batches+clients+team_members` for flipped teams, with native links, realtime/catch-up, explicit CON/STR and top-level policy, semantic parity report, and no fallback to foreign Linear truth. Gate each flip on mixed-authority TEST propagation; delete the legacy feed/cache/links only after B5 parity. | OPEN — BLOCKS FIRST FLIP/B5 |
| F41 | P1 | **INFERRED OPERATIONS DEFECT, verified in canonical docs.** Runbook R3 tells every team to keep working in Linear during a Supabase outage. That is valid only for a Linear-authoritative team. A flipped team's direct Linear edits are foreign/detect-only and can be discarded or overwritten when reconciliation resumes; the authority flag cannot be changed while its store is down. R3's immediate reconcile/zero-diff instruction can certify the overwrite. | **Docs corrected in this PR:** R3 now branches by the last verified authority, stops/incident-logs work for SyncView-authoritative or unknown teams, inventories foreign edits before reconciliation, and requires every intent accounted for before resume. Build and rehearse the automation hold/manual-merge path and mixed/partial/prolonged-outage table-top. | DONE (docs) / REHEARSAL + HOLD MECHANISM OPEN |
| F42 | P1 | **VERIFIED-LIVE DATA + VERIFIED SOURCE; failure inferred without a write.** Calendar/Samples have 314 active linked non-deleted comment roots users can reply to, but zero source IDs resolve in `production_comments`. The #850 caller sends the source parent ID; the gateway requires exactly one normalized ID match and returns 409 otherwise. The recovery script imported `deliverables.comments`, not the actual Calendar/Samples arrays, so it missed these active threads. | Migrate active video/graphics roots/replies from both card stores with a composite identity crosswalk and full audience/component/tweak/lifecycle state. Classify unresolved links and duplicate historical IDs. Block enrollment until exact parity and an existing-root TEST reply survive gateway, projection, refresh, and retry. | OPEN — BLOCKS REAL-CLIENT ENROLLMENT |
| F43 | P1 | **VERIFIED-LIVE DATA + VERIFIED SOURCE; save-failure consequence INFERRED from source without a live write.** Comment truth is split by entry point: review-panel plain comments and every resolve/reopen/delete update only card JSON; Notes roots/replies use the normalized gateway first; Production writes only normalized rows and advertises “Client-visible”; client links never read that store. Among uniquely body-matched active rows, lifecycle/audience/tweak/component metadata disagree materially. Current-main Calendar/Samples review-save functions also catch and resolve failures, so a request-tweak caller can enter `.then()` and post the Linear comment even though canonical card/status/thread persistence failed; optimistic UI can leave the queue while only foreign Linear truth exists. Production renders replies, but its composer exposes no Reply action and sends no `parent_id`; edit/delete/resolve/reopen are absent. | Route every video/graphics create/reply/edit/resolve/reopen/delete through one canonical gateway. Make card JSON a verified projection or switch readers; add exact-client token-scoped audience reads. No external/mirror side effect may occur until canonical persistence succeeds; failures retain the draft/queue with visible retry. Retry makes exactly one canonical mutation and one applicable mirror intent while mirroring is enabled, but zero mirror/outbox intents in retired mode. Gate rollout on the full SMM/Kasper/client/editor lifecycle, failure injection, refresh and second-device matrix. | OPEN — BLOCKS REAL-CLIENT ENROLLMENT/FLIPS |
| F44 | P0 current / P1 cutover | **SERVER CONTAINMENT VERIFIED LIVE; CLIENT DEPLOYMENT STILL OPEN.** Workflow `BrJSe8zCKUccfmIq` now serves active version `f6a5ca07-e7a7-4a41-a3d3-3638a682b596`: it writes an idempotent Supabase intake receipt before work, rejects null/ambiguous project, missing SMM credential, filming plan or roster, and responds success only after exact parent/child create confirmation. Bounded retry plus payload-bearing dead-letter/replay and partial-create operator recovery are present. Safe malformed probes `268305`/`268306` each returned HTTP 400 before receipt/create; the live ledger stayed at zero rows and no work was created. The browser fix on `claude/reduce-n8n-linear-deps-vmphp6` awaits the real result, deduplicates double-clicks and retains drafts on failure/timeout, but is not yet owner-merged/deployed; the currently served SPA can still ignore the server failure, clear the draft, and show green. | Owner merge and deploy the client branch, then prove create-failure-after-acceptance, double-click, timeout, retry, refresh and second-device behavior against the deployed app. Until that deployment is verified, SMMs must confirm the new card actually appeared in Calendar rather than trusting the green confirmation. Preserve workflow version `d867fa43-2ab2-44a4-93c8-57254846ca1c` only as an emergency rollback point; restoring it reopens the early-success defect. | SERVER + BROWSER FIX MERGED & LIVE (#836/`c7b325e`, 2026-07-15); durable receipt + response-checking browser change deployed to Pages. Remaining: TEST failure/double-click/timeout drills against the deployed build. |
| F45 | P1 | **VERIFIED-LIVE WORKFLOW/EXECUTIONS + READ-ONLY PARITY.** The active `linear-projects` dropdown query has no cursor/pagination. Twelve sampled successful runs each returned exactly 50 projects, while a fully paged read found 58 eligible video-team projects; eight allowed-state projects are silently absent. The omitted cohort can shift with ordering and no completeness field or UI warning exists. **CURRENT SOURCE-ONLY FOLLOW-UP at `07d123d`; no browser/backend request or live project data was used.** `fetchLinearProjects()` has no deadline; it catches a failed legacy request, retains the prior-or-empty array, and still sets `linearProjectsLoaded=true` in `finally` (`index.html:12358-12408`). With no prior rows, `renderLinearSearchResults()` publishes “No projects loaded” (`:15232-15247`). This is the current browser manifestation of F45's incomplete/degraded dropdown contract, not a new finding. | Page to `pageInfo.hasNextPage=false`, fail closed on any page error, return completeness/count/version metadata, and reconcile the exact set against canonical native mappings. Add >50 fixtures, a live anonymized set-parity gate, and preserve the draft on degraded reads; retire this reader for authoritative intake per F32. Give both project sources bounded, typed receipts; failure or partial coverage must remain visibly degraded and must not become the same state as a complete empty project set. Preserve the draft/last-good choices, expose Retry, and add the actual picker boot/failure/recovery sequence to F162. | OPEN — CURRENT INTAKE CORRECTNESS |
| F46 | P1 before parity/flip; P2 current | **VERIFIED-LIVE N8N STATE/EXECUTIONS + SOURCE/DOCS.** Keep-until-B5 workflow `MJbMZ789B5ExZz9x` is inactive/unpublished (`activeVersionId=null`), while the audit-start canonical docs called it active/realtime. Its saved authority-gated graph followed a 24-record crash cluster and has had no later execution, while other keep readers remain active. Current ten/fifteen-minute reconcilers provide slower healing, but the promised fast Calendar/Samples patch and Workload upsert lane do not exist. Re-publishing the untested saved graph is not a safe automatic correction. | **Docs corrected in this PR.** Owner still chooses and drills one topology: explain the crash/soft-error branches and deliberately publish the gated graph, or ratify reconciler-only latency. Add a machine-read active-version/node-hash/last-green go-live probe. | OPEN — LIVE TOPOLOGY GATE; DOCS CORRECTED |
| F47 | P1 before Video flip / P2 current | **VERIFIED-LIVE WORKFLOW + BROWSER SOURCE.** URGENT's Linear assignee lookup and roster-Sheet lookup are fail-soft; the message builder permits an empty mention, Slack can post the generic channel message, and the workflow still returns `{ok:true,posted:true}`. Calendar/Kasper then persist and latch “Sent” even though no exact editor receipt exists. All 11 retained executions did contain a mapped mention, so no past missed recipient is claimed. | Resolve an immutable native assignee plus Slack ID, require exactly one active recipient, and return success only with a confirmed Slack timestamp/recipient. Missing mapping/dependency failure must remain pending/retryable and must not latch Sent. Run TEST-safe failure/reassignment/idempotency drills. | OPEN — NOTIFICATION CONTRACT / EXTENDS F15 |
| F48 | P1 current / B5 | **VERIFIED-LIVE ENDPOINT/DATA + SOURCE.** The current Editors-week reader is unauthenticated, accepts arbitrary historical ranges, and returns confidential people/client/work timelines. It attributes every past transition to the issue's **current** assignee. Last week returned 131 delivery keys; one was TEST-only. Applying §9.11 literally to native events returned 245 (114 false extras); correct production scope gives exact 130/130 delivery-key parity, but not report parity. Of 401 source transitions for the same production videos, 27 are absent and 239 native rows are unmatched/duplicative; exact source time and immutable event-time assignee are not stored. §9.11 omits the UI's load, finish/open, timeline, scope, paging, and cache contracts. | Authenticate/scope the current reader immediately and rotate/move embedded credentials without publishing values. Persist source event ID/time plus event-time member; define the full saved-query contract and complete paging/cache invalidation. Require exact deliveries, plate components, finish/open, daily bars, timelines, and historical attribution for a full native week before retirement. | OPEN — CURRENT CONFIDENTIALITY / BLOCKS B5 |
| F49 | P2 (F13 now DONE) | **VERIFIED-LIVE SUPABASE CONFIG/AGGREGATES + DOCS.** The organization is Pro and healthy; seven retained physical daily backups are complete through Jul 13, with seven-day retention and restore-to-new-project entitlement. PITR is currently off between planned risk windows. Database use is 0.45 GiB; current Storage, Realtime, and Edge Function volumes are far below Pro allowances. At audit start, §7.1 still said Free/no PITR and the failure table used a 500 MB cap, contradicting D-1. Backup metadata still does not prove restore. Exact billing-cycle egress, peak connections, and spend-cap state remain Dashboard-only. | **Plan/capacity docs corrected in this PR.** Still capture exact Dashboard egress/peaks/spend cap, run post-#850 bootstrap/mobile/cache load tests, and set thresholds. **F13's six-hour export + timed scratch restore are now DONE and live (PR #840); PITR was owner-declined (accepted residual).** What remains for F49 is capacity evidence only. **Owner question:** what are the current Dashboard egress/spend-cap values? | OPEN — CAPACITY/EGRESS EVIDENCE ONLY (F13 BACKUP/RESTORE DONE); DOCS CORRECTED |
| F50 | P0 at first creative flip | **VERIFIED-LIVE TEST DRILL ON BOTH TEAMS + DEPLOYED SOURCE.** The canonical TEST lane advanced one linked graphics and one linked video deliverable to SMM Approval; each returned HTTP 200 with native commit and mirror acknowledgement, but the linked card's corresponding component status stayed unchanged throughout delayed readback. Both exact originals were restored and re-verified. Live `production-write` calls only the deliverable RPC and has no Calendar/Samples projection; #850 patches only its local Production row. SMM Review, Kasper, client, Calendar, and Samples still read card status. | Implement one canonical status authority: either transactionally project every accepted deliverable status into the correct card slot with mapping/CAS/idempotency/realtime, or make every downstream reader use deliverable status. Never depend on a Linear round-trip. Gate each team on TEST Production→SMM→Kasper/client→tweak→done walks across refresh, second device, concurrency, and retry. | OPEN — PRIMARY HANDOFF / BLOCKS BOTH CREATIVE FLIPS |
| F51 | P1 release/rollback; P2 current | **PLATFORM LIMIT ACCEPTED; F27 SOURCE-EXACT STANDARD DEFINED; FLEET STILL OPEN.** Supabase CLI 2.109.0 cannot reactivate a prior ESZip; recovery downloads source and creates a new deployment. Historical transitive graphs for the currently deployed functions are unrecoverable through provider source readback and available platform tooling. The owner explicitly accepts that standing limitation and defines behavioral rollback as exact captured provider-source/entrypoint/JWT redeploy followed by independent deployed-source/JWT hash equality; transitive reconstruction is not required. This toolkit makes the scoped direct-import correction only for inbound (`npm:@supabase/supabase-js@2.49.8`) and keeps its already-produced frozen Deno v4 lock/config, without vendoring or cross-function lock generation. The four install closures keep byte-identical exact `2.49.8` import surfaces—direct in outbound/production-write and through `_shared/b4-write.ts` for deliverable-write/batch-write. Prior version IDs remain provenance. Six onboarding-family functions still use floating npm `@2` and remain out of scope because editing their auto-deploy paths would release them on merge. Other fleet manifest/config gaps also remain. | Cloud-review/merge this source, then establish inbound's source-exact live baseline in its separate quiet window. Later schedule the onboarding-family direct-import pins as a deliberate deployment and complete fleet source/JWT/release manifests and independent readbacks. Do not reopen historical transitive-provenance work as an F27 blocker. | OPEN — F27 SOURCE-EXACT STANDARD DEFINED; PINNED INBOUND NOT YET LIVE; SIX ONBOARDING FLOATS + FLEET MANIFEST REMAIN |
| F52 | P1 current security | **VERIFIED-LIVE CARRY-FORWARD / STALE REGISTER OMISSION.** The redacted July 5 n8n audit already recorded a plaintext provider key in the graphics title-generation Code node; it remains live in `BrJSe8zCKUccfmIq` and all 50 retained versions. Workflow-detail/export/history readers can retrieve it. An exact-pattern census found no second copy among the 92 workflows returned by that search and the repo contains no copy; older docs list a larger instance inventory, so the unreturned remainder is not cleared. Sampled execution data did not expose it and no abuse is claimed. The value is never included here. | Immediately restrict workflow/version/export access. Stage a managed replacement for the known live branch and prove TEST success/failure; then the owner revokes/rotates the exposed value. Continue the complete workflow/version/export/backup/provider census and monitor for any previously unknown consumer failure; prune retained copies where possible. Add no-literal machine gates while keeping every public artifact value-free. | OPEN — LIVE SECRET ROTATION / COMPLETE ACCESS AUDIT |
| F53 | P0 at first native Graphics intake | **VERIFIED DEPLOYED/PARKED SOURCE + LIVE ANONYMIZED AGGREGATE; first-intake failure mechanically inferred without a real-client write.** Native Graphics intake creates the deliverable without `file_url` and materializes a card with a blank thumbnail. Production can read an existing file/folder fallback, but its protected operations expose no upload, Drive picker, or file/delivery-link setter. An internal organizer can manually patch a card-side link outside the designer's normal Production path, but that does not set canonical `file_url` and creates split truth. The spec promises per-deliverable media and a comment-link “set as delivery link” action that do not exist. Status can advance to SMM Approval without resolvable media, while Calendar/Samples review suppress blank-media items. | Add an authenticated canonical file/link operation (or first-class upload/Drive picker) with URL validation, actor/time/history, CAS/idempotency, and transactional projection into the correct card component. Reject SMM Approval without resolvable media. Gate Graphics on a fresh TEST intake→attach→SMM→Kasper/client→tweak→replacement→approval walk across refresh and second device. | OPEN — GRAPHICS HANDOFF / BLOCKS FIRST FLIP |
| F54 | P1 before Graphics flip | **VERIFIED-LIVE READ-ONLY DATA + VERIFIED SOURCE.** Production downloads every client and every otherwise-live deliverable, then filters only deliverable archive/delete markers; neither its adapter/queue predicates nor staff write gate applies `clients.active`. The staff-authenticated gateway also omits client activity for status/comment/due/assignee, while only client-token auth checks it. A current anonymized snapshot contains 67 renderable Graphics rows across six inactive real clients: 56 Backlog, 10 SMM Approval, and one Kasper Approval. All 10 assigned rows point to an active Graphics member. Fixing F37 alone would still put departed-client approvals in the designer's writable queue. | Hide inactive-client work from normal personal/team queues, expose only an explicit role-gated recovery view, and enforce activity server-side for **every** staff mutation unless audited recovery mode is active. Reconcile all open inactive-client rows before flip; add inactive/offboard/reactivate fixtures, negative gateway probes, and an anonymized zero-stale-queue gate. | OPEN — QUEUE/OFFBOARDING/AUTHORIZATION GATE |
| F55 | P1 before any authority drill/flip | **VERIFIED SOURCE + CANONICAL-DOC CONTRADICTION; failure mechanically inferred without a flag change.** The browser's `_prodAuthorityValue` accepts only `linear\|syncview` and fails the whole authority read closed on any other value. In contrast, `production-write`, `linear-outbound`, `linear-inbound`, shared B4 writers, reconcilers, and n8n authority guards normalize legacy `supabase` to `syncview`. Before this docs correction, `B4_READINESS.md` told operators the detect-only branch required `supabase`, while FLIP_RUNBOOK correctly forbade any value except `syncview`. Setting that legacy alias can therefore make Production controls unavailable while backend mirrors/reconcilers act SyncView-authoritative—a split-brain outage at the exact drill meant to prove safety. No live flag was changed and no current incident is claimed. | **Operator docs corrected in this PR.** Standardize the flag contract on `linear\|syncview`; remove the legacy alias or normalize it at one authoritative boundary before any consumer sees it. Add a DB/flag-write validator, an all-consumer contract test over valid/missing/malformed/legacy values, and a preflight that rejects alias residue. TEST-drill readback plus recovery. | OPEN — AUTHORITY-VOCABULARY SPLIT BRAIN; DOCS CORRECTED |
| F56 | P1 operator/handoff safety | **VERIFIED CURRENT SOURCE/HISTORY + ACTIVE DOC COUNTEREXAMPLES; no runtime flag was changed or live authority re-read in these docs-only rounds.** The first corpus mixed July-5 planning with current execution: completed B0–B3/Track-A work appeared unstarted, full-roster flags TEST-only, n8n primary, #796/#812 draft, and `production-write` backend-only. A second corpus called Production inherently read-only, called all polish tests zero-write, omitted the fully mocked gateway lane, and left four shipped operations deferred. A third “START HERE” design-kit chain still advertised a prototype-era external scratch/build/publish flow, obsolete tester/workflow, and saved provider profile as the operational resume, so an operator could change or republish the wrong artifact while `index.html` remained untouched. Canonical truth currently records Linear/Linear for real teams; that dated stance is not permanent architecture. | **Corrected in this PR:** operative topology/state docs; agent/reviewer/test/adapter/parity wording; and the design-kit router. Historical handoff/continuation/parity/loop files are now non-operative tombstones, prototype originals are NO-RUN, current wired tests are the sole repository proof route, and the token file is a frozen baseline. Add a generated expiring state manifest, active-doc semantic/legacy-command lint, and an independently pinned artifact-oracle identity so stale milestones or simultaneous oracle edits cannot redefine truth. | DONE (THREE CURRENT DOC CORPORA) / MANIFEST + DRIFT LINT + ORACLE PIN OPEN |
| F57 | P2 current operations / later cleanup | **VERIFIED CURRENT SOURCE + TESTS/DOCS + SANITIZED LIVE LEGACY-WORKFLOW CONFIG/METADATA; scope corrected by F117; no route, payload, row, identity, or write was used.** Canonical plan/System Map/ROLLBACK said “keep Samples Old” as an active staff baseline, while current `index.html` removes its nav and redirects every `#samples[/...]` staff route to Samples/SXR. The earlier claim that its client portal remained intact was false: F117 proves the verified exact-client binding is discarded. Its `?sv2=0` selector is also not writable recovery. The dormant renderer normally reads Supabase and automatically/stickily falls back to Sheet; active legacy upsert/reorder graphs fan out to both stores, continue after a Sheet error, and return success from the Supabase branch. A successful old-code save can therefore be invisible to its own Sheet fallback. All three old route workflows have zero retained executions since Jul 1, which does not prove zero stale-tab/direct use. | Staff-hash truth is corrected. F117 must fail the old client URL closed or restore an exact-client server-bound handoff. Withdraw `?sv2=0`/automatic Sheet fallback as writable recovery; any temporary old-code restoration couples one read/write authority or is explicitly read-only. Privately inventory stale builds, every link/direct caller, store parity and current route usage; then execute `SAMPLES_LEGACY_REMOVAL_MAP.md` only with owner approval, snapshots, zero-caller observation, route/client proof and rollback readback. | REOPENED — CLIENT BINDING + DORMANT FALLBACK/RETIREMENT PROOF OPEN |
| F58 | P1 during parity/incident response | **VERIFIED SOURCE + UNIT CONTRACT + RUNBOOK.** `linear_outbound_enabled=off` stops only normal SyncView-authoritative rows. Legacy-parity rows deliberately keep scanning/writing when `linear_legacy_parity_enabled=true`; the Edge Function derives their mode solely from F4 and the unit test pins this independence. FLIP_RUNBOOK and dependent checklists nevertheless called F2 `off` the universal/global emergency stop, so an operator could believe bad Linear writes stopped while the parity lane continued. No live flag or row was changed. | **Docs corrected in this PR:** F2 and F4 are lane-specific; unknown/mixed incidents stop users, set F2 `off` **and** F4 `false`, read both back, and preserve/classify intents. Add an automated dual-lane emergency drill and a pager/diagnostic field that names which lane can still write. | OPEN — DOCS CORRECTED; DUAL-KILL DRILL REQUIRED |
| F59 | P1 before first team flip | **VERIFIED CANONICAL CHECKLIST DEFECT.** Track-B §1.5 performed the owner authority/outbound flip in step 4, then only afterward verified legacy-writer/reconciler/n8n gates, live watchers and TEST drills, Kasper/link predicates, detect-only inbound, identifier collision, and the F27 rollback rehearsal. Because readiness says the spec wins, following it could flip before the safeguards that prevent stale Linear re-entry, invisible Graphics work, ID collision, and unrecoverable rollback. | **Corrected in this PR:** soak, every writer/reader/predicate/identity/alert gate, and the full TEST recovery rehearsal now precede the owner flip; only post-flip observation follows. Make the preflight machine-enforced and fail the flag transaction unless every dated evidence handle is current. | DONE (docs) / ALL UNDERLYING GATES REMAIN OPEN |
| F60 | P1 before B5 teardown | **VERIFIED CANONICAL RUNBOOK DEFECT.** Track-B §13 called the entire teardown “reversible until cancellation” while instructing webhook deletion, EF/workflow retirement, saved-graph/branch removal, and credential rotation without an exact per-action restore object, recreation procedure, or readback. F46/F51 prove that a saved workflow/source or same-SHA rebuild is not automatically a working rollback. | **Docs corrected in this PR:** prefer deactivate/archive; each action now requires an exact private restore object, rehearsed restore/recreate procedure, machine readback, and owner sign-off before proceeding. Build and drill those inverses; preserve the unexplained inactive graph unless its topology decision explicitly authorizes change. | OPEN — DOCS CORRECTED; RESTORE ARTIFACTS/DRILLS REQUIRED |
| F61 | P1 before B5 retired-epoch activation | **VERIFIED CANONICAL-DOC/CONTROL GAP; failure inferred from current controls.** The initial B5 order activated the Linear-free epoch before the roughly one-week grace even though that grace keeps outbound mirroring Linear. The order is now corrected, but its transition still depends on a prose-only “freeze human/app writes.” No server-side team maintenance/write-freeze exists, so stale tabs, retries, automations, and second devices can create mutations between the final zero/export and retired-mode activation. | **Docs corrected in this PR:** build/test retired mode only behind a service-only TEST override; at grace end stop parity, drain normal intents, stop normal outbound, reconcile/export, then activate retired mode. Before executing that order, add a fail-closed server maintenance/cutoff state or an atomic high-water transition that blocks every writer with explicit UX and proves no intent crosses the boundary. Drill stale-tab/retry/automation races on TEST. | OPEN — ORDER CORRECTED; SERVER FREEZE/ATOMIC CUTOVER MISSING |
| F62 | P1 destructive recovery | **VERIFIED-LIVE SCHEMA/DATA + CANONICAL ROLLBACK.** The finished-work history row advertised a three-DELETE rollback that cannot execute safely. `production_comments` has NO ACTION foreign keys to tagged deliverables/batches (2,481 and 5 current dependent rows). Its event predicate matches zero rows, while 6,473 backfill events reference tagged history IDs. Parent deletion therefore aborts; bypassing the constraint would still leave thousands of ledger events and unclassified dependent state. | **Dangerous command withdrawn in this PR.** Preserve the import by default. Any removal needs a fresh private dependency graph, owner-approved preserve/relink/delete disposition for comments/events/card slots/children, assertion-bearing transaction, and TEST/scratch restore rehearsal with exact readback. | OPEN — STALE ROLLBACK REMOVED; DEPENDENCY-SAFE RECOVERY MISSING |
| F63 | P1 operator mutation safety | **VERIFIED CANONICAL RUNBOOK SQL.** FLIP_RUNBOOK F1 originally placed Graphics forward, Video forward, and both reversals in one paste fence, so running the advertised block netted back to Linear/Linear. Forward updates replaced the whole authority JSON without compare-and-set and could clobber another team's concurrent change. F2–F5 similarly mixed kill/forward snippets or used unconditional whole-row writes, allowing a copied incident block to turn a lane on. | **Corrected in this PR:** one action per SQL fence; one-key `jsonb_set`; exact expected-state CAS; row-count assertion; mandatory readback. Add a parser/lint gate for every operator SQL fence and transactionally exercise every forward/kill/recovery action on an isolated TEST flag store before owner use. | OPEN — DOCS CORRECTED; EXECUTABLE SQL GATE MISSING |
| F64 | P0 privacy incident / P1 broad repository compliance | **PRIVATE CLEAN PACKAGE VERIFIED; PUBLIC TREE/HISTORY OPEN.** Reviewed schema-only replacements remove the 21 onboarding, 30 filming-plan and 19 sample rows; a private count-only scan across all 34 migration SQL files finds zero email-shaped strings, HTTP(S) URLs, `COPY` payloads or literal row inserts for those tables. They are excluded from this public branch: an anonymous GitHub comparison expanded every deleted row even when exact-path `-diff` rules existed in both refs, so PR #829 was closed and the feature branch rewound. Public current files, historical aliases, PR refs, forks, clones, caches and the wider tracked-identity/provider-link inventory remain exposed. | Follow `docs/ops/GIT_HISTORY_PII_PURGE_2026-07-14.md` in an owner-scheduled freeze. One operator uses `git-filter-repo` 2.47+ against all five path spellings, restores only the privately hash-matched clean files inside the rewritten history, receives final owner GO, force-mirror pushes once, restores protections, opens a private GitHub Support cache/PR-ref/GC request, and requires fresh clones/fork sanitation plus anonymous proof. Never publish the row deletions through an ordinary PR. | PARKED BY OWNER 2026-07-15 (accepted residual: PII file remains in public git history; no live endpoint serves it — the live API leak is closed). Reopen when the owner schedules the freeze/rewrite. OPEN — PRIVATE CLEAN FILES READY; PUBLIC CURRENT TREE/HISTORY/CACHES/FORKS/WIDER REPO OPEN |
| F65 | P2 current / P1 Samples cutover audit trail | **VERIFIED SOURCE/SCHEMA; known truth promoted into the cutover register.** SXR appends and sends `kasper_finish_log` on Finish/re-finish, but `sample_reviews` has no column for it and `sample-review-upsert` omits it from both accepted and mirrored fields. The request still succeeds, so the repeated-finish audit history is silently discarded while only the latest finish timestamp survives. | Add an additive column and exact validation/size cap; accept and mirror it in every current/fallback writer; migrate any recoverable local state; then TEST Finish, re-finish, undo/retry, refresh, and second-device convergence. Gate Samples persona parity on durable log equality. | OPEN — SILENT AUDIT-HISTORY LOSS |
| F66 | P1 incident containment / onboarding | **VERIFIED SOURCE + CANONICAL ROLLBACK + LIVE SANITIZED RELAY GRAPH.** ROLLBACK told the operator to remove shared `SLACK_ALERT_WEBHOOK` to stop `linear-inbound` anomaly messages. `onboarding-capture` consumes the same project secret, so that scoped “rollback” can silently disable onboarding alerts. The active relay also accepts multiple source classes without source authentication or a typed/versioned schema, correlation ID, dedupe key, or terminal receipt. The same rollback row described the current inbound kill as already false although live state is enabled. | **Docs corrected in this PR:** never remove a shared secret for lane-specific containment; use the inbound flag/webhook or a dedicated alert-lane switch. Split credentials/lanes or require authenticated source identity, a versioned allowlisted schema, correlation, dedupe and terminal delivery verification. Prove each caller/alert path independently and read back the actual inbound flag before/after every action. | OPEN — DOCS CORRECTED; SHARED-SECRET + RELAY TRUST BOUNDARY REMAIN |
| F67 | P1 current security / rollback / outage | **PARTIALLY CLOSED IN CANDIDATE SOURCE; LIVE RESIDUAL VERIFIED.** Reachable Calendar/SXR/settings n8n fallback webhooks remain unauthenticated through B5. Unlisted clients and direct callers can use them, and routing-flag read failure becomes an empty list that silently chooses n8n. Candidate source removes both flagged Calendar/SXR reorder EF-failure downgrades and tests them fail-closed; current Pages retains the old downgrade until merge. F35's two upsert deployments are also merge-gated. The canonical B5 order previously deferred these writers until teardown despite the Phase-0/enforcement gate. | Merge/prove the fail-closed reorder callers. Authenticate/scope every reachable legacy writer with the same immutable principal/client contract, or retire it before enforcement. Inventory direct callers/stale tabs; make rollback preserve authorization; probe manual fallback and missing/failed flags plus EF 4xx/5xx/network denial, and add a no-anonymous-writer preflight. **Docs corrected:** Phase 0 owns containment/retirement; B5 performs only final archive/deletion after zero-caller proof. | PARTIAL — EF-FAILURE DOWNGRADES REMOVED IN CANDIDATE; UNLISTED/FLAG-FAIL/DIRECT N8N PATHS OPEN |
| F68 | P1 destructive recovery | **VERIFIED-LIVE SCHEMA/DATA + PLAYBOOK.** The comment-import playbook separately called its full DELETE “one-command ready.” Current state disproves that: 223 rows reference tagged comments through self NO ACTION parent/root links, including one row outside the import run; 41 tagged rows have advanced to version 2. The source-only guard would not detect those mutations, and the delete can block or erase subsequently connected history. | **Dangerous command withdrawn in this PR.** Preserve the import by default. Build a current dependency/version preflight, define owner-approved treatment for in-run and cross-run descendants, delete/relink in dependency order only inside an assertion-bearing transaction, and rehearse a full restore in TEST/scratch before offering any rollback. | OPEN — STALE ROLLBACK REMOVED; DEPENDENCY-SAFE RECOVERY MISSING |
| F69 | P1 new-client onboarding / security | **VERIFIED SOURCE/LIVE-CONFIG/DOCS; first-write failure inferred without onboarding a real client.** Clients Info makes a new client visible, but the three Track-A `*_ef_clients` flags are static current-roster lists and onboarding updates none of them. Sanitized current-graph review of both submit workflows plus their provisioning subworkflow found zero token/client-access, project-mapping, team-authority, runtime-flag, or Track-A policy enrollment step. A new slug is therefore omitted by construction and its first Calendar/SXR/settings writes route to F67's unauthenticated n8n fallbacks. The onboarding guide called this “automatic” and required only mapping/token for cutover. Conversely, offboarding has no atomic token revocation; current joins show zero inactive/missing token owners, but the verifier itself does not check `clients.active`. | Make onboarding/offboarding atomic across the active client row, exact team/project mapping, protected token mint/revision/revocation, every required authenticated Track-A routing/policy enrollment, and a durable receipt/readback; or replace static lists with authenticated active-client policy. Block go-live until a TEST fake-client first write proves authenticated EF routing and removal immediately denies its token with no anonymous fallback. Include idempotent replay and F110's resumable step ledger. | OPEN — NEW CLIENTS ARE SECURITY-DOWNGRADED BY CONSTRUCTION |
| F70 | P1 post-enforcement security incident | **VERIFIED SOURCE/FLAG SEMANTICS + CANONICAL RUNBOOK.** FLIP/ROLLBACK/B4 docs treated `auth_enforcement: enforced → permissive` as a routine one-step rollback. Permissive is intentionally fail-open for invalid/stale client verification and is tolerated only before enforcement; after go-live it reopens protected access rather than merely reverting UI behavior. No scoped bypass, compensating containment, automatic expiry, or cache/session purge accompanies the flag. | **Unsafe global rollback removed from the owner runbook in this PR.** Preserve enforcement and fix/revert the failed verifier/caller through the release path. If the owner ever authorizes emergency fail-open, treat it as a security incident with exact affected surfaces, server-side containment, forced cache/session invalidation, monitoring, expiry, and a separately reviewed CAS action. Build a scoped break-glass alternative before enforcement. | OPEN — DOCS CORRECTED; SAFE AUTH RECOVERY MISSING |
| F71 | P1 operator/handoff truth | **VERIFIED CURRENT REPOSITORY + MIRROR HASH.** `docs/CLIENT_LIFECYCLE_MAP.md` calls itself the byte-identical master across two repositories, and both `main` copies currently share the same blob. It nevertheless describes Track A as a per-client canary, Track B tables as empty/planning, the legacy combined n8n Linear receiver as active, healing as ten-minute-only, and app saves as n8n-primary. Current serving reality is full-roster Track A, populated Track-B mirror, a live authority-locked Production caller, active EF inbound, an inactive/unpublished combined n8n receiver, and pager-assisted reconciliation. | **Quarantine staged byte-identically:** this PR and companion draft `synchrosocial` PR #51 carry the same prominent non-operative warning (companion commit `e3b2204`; neither merged). Complete the line-by-line lifecycle update, land it byte-identically in both repositories, and add an automated cross-repo blob/current-state check before restoring “master map” status. | OPEN — MATCHED TWO-REPO WARNING DRAFTED; FULL UPDATE/DRIFT GATE REQUIRED |
| F72 | P1 security/rollback evidence; P2 privacy cleanup | **VERIFIED CURRENT QA REPORT.** The historical EF write-path report labels empty/error routing to n8n a passing “fail-safe,” advertises slug removal as clean one-step rollback, and embeds the private TEST identity. F67 proves the selected writers are unauthenticated, so a future operator can follow a green report into an authorization downgrade; the identifier also violates the current fixture-hygiene rule. | **Corrected in this PR:** report banner and conclusions now classify the behavior as F67 fail-open, withdraw the rollback, and replace the TEST identity with private-config notation. Audit every historical QA/report/harness for security conclusions that changed after enforcement design, scrub remaining private identifiers under F64, and make current security gates supersede old PASS labels mechanically. | DONE (REPORT) / BROADER QA-HISTORY + F64 SCRUB OPEN |
| F73 | P1 current Samples staff UX / cutover persona readiness | **VERIFIED-LIVE READ-ONLY BROWSER + SOURCE + TEST.** `_sxrEnabled()` is GA default-on, but the prepaint gate and two early boot copies still require `?sxr=1` or a legacy `syncview_sxr_on=1` key. In a fresh browser, ordinary navigation mounted `#sample-reviews` with no opt-in key; reload of that exact hash redirected to home and removed the SXR view while `_sxrEnabled()` remained true. The explicit `?sxr=1` route worked. Existing `boot-gate-parity` tests deliberately require the stale strict expression, so CI enforces the defect. Only GET/HEAD traffic occurred in the live proof. | Unify all prepaint/deep-link/staff/client boot decisions behind one GA-default-on helper or generated expression, while preserving explicit sticky `?sxr=0` opt-out and token gates. Replace the stale CI assertions with semantic route cases. Drill fresh/returning desktop, mobile, copied staff/card/client links, reload, second device, missing/invalid token, and opt-out. | OPEN — FRESH STAFF SAMPLES DEEP LINKS/RELOAD BREAK |
| F74 | P1 privacy-remediation durability | **VERIFIED REPOSITORY/ACTIVE TEST + RECURRENCE + GENERATED-OUTPUT CENSUS.** The private TEST identity remains literal across a broad QA/script/doc corpus and seven current test files. One active source test required a feature doc to contain it; concurrent main work reintroduced it, proving added-line prevention absent. Beyond F122's active public CI producers, at least twelve manual/dormant report generators can emit real review tokens/tokened links, client/staff/contact fields, bodies, titles, IDs, URLs, patches or outbox detail to caller-selected paths. Generic `artifacts/`, `.codex-tmp/`, `private/`, `reports/`, and `*.private.json` are not ignored and are stageable; several scripts suggest repository paths or do not forbid the worktree. No generated file is currently tracked. | **Two direct recurrences corrected in this PR:** the thumbnail history doc/test and concurrent audit row use private notation. Move remaining live fixtures to required private config or fictional data; make one shared output helper resolve/refuse the git worktree for private reports; ignore private-output classes; constrain public serializers to allowlisted aggregates; scan exact generated trees recursively; remove identities/IDs from reports/assertions; and enforce source/generated/artifact/added-line gates plus clean-checkout proof. F122 separately owns already-published Actions/log/Argos copies. | OPEN — FIXTURE CORPUS AND MANUAL REPORT GENERATORS CAN REINTRODUCE PRIVATE DATA |
| F75 | P1 operator/rollback truth | **VERIFIED CURRENT DOC CLUSTER.** Four Samples rebuild/go-live/parity files are classified by the legacy-removal map as shared/current “must keep,” while they still assert default-OFF/`?sxr=1`, call the inactive combined n8n receiver live/mandatory, and offer old workflow/fallback operations. `docs/ops/LINEAR_SYNC_RECONCILE.md` likewise calls that n8n receiver the active fast path, describes anonymous endpoints as safe, and gives obsolete schedule activation. These are not harmless archive notes because current maps explicitly route operators to them. | **Quarantined/corrected in this PR:** each file now carries a current-state override; the removal map reclassifies them as historical evidence; current register/System Map/Monitoring/Rollback win. Complete a line-by-line rewrite or move executable current procedure into generated truth, then add a stale-epoch linter so historical PASS/go-live text cannot regain operator authority. | DONE (QUARANTINE) / FULL REWRITE + LINTER OPEN |
| F76 | P0 current confidentiality / P1 integrity and availability | **ACTIVE LEAK/DEACTIVATION PATH CLOSED 2026-07-14; INCIDENT/IDENTITY WORK OPEN.** `smm-weekly-reports` is live at v23 after pinned same-source run `29601466479` from exact `main@9d76df6`; its fingerprint passed. The original containment behavior traces to source commit `1f9eb20`: anonymous GET and anonymous `sync_managers` return `401` before body/service-role access. Reads and manager sync are admin-only; submit/options allow admin/SMM; both historical staff secrets remain transition compatibility. The live n8n manager-sync caller sends its existing managed header credential; safe pinned execution `268153` reached the authenticated branch and returned the expected no-write `400 unknown action`. Anon SELECT on `social_media_managers` and `smm_weekly_reports` is revoked and both direct browser-key reads return `401`. The protected browser caller merged with #836 and restored authenticated staff screens. | Preserve the protected caller and browser-walk Admin/SMM allow plus creative/client deny; review access and integrity history; replace shared keys with individually revocable sessions and immutable member binding; constrain CORS and audit/limit submit/options/sync. Record the legal/incident disposition. Do not reopen anonymous access to restore availability. | PARTIAL — LIVE ANON EF/TABLE PATHS CLOSED; UI MERGED (staff screens restored with auth post-#836, 2026-07-15); INCIDENT/PER-HUMAN AUTH OPEN |
| F77 | P0 current onboarding privacy | **ACTIVE ANONYMOUS DISCLOSURE CLOSED 2026-07-14; LEAST-PRIVILEGE/INCIDENT WORK OPEN.** All three readers are live at v26 after pinned same-source run `29601466479` from exact `main@9d76df6`; all three fingerprints passed. Authorization runs before service-client construction; missing and wrong keys return `401`, while only an admin role key or the existing onboarding transition secret can pass. `onboarding-capture` remains public intake but has no SELECT/read path and returns only its created ID/kind. The protected Pages caller merged with #836, obtains the key only after verified Admin sign-in, and never hardcodes it. CORS remains `*`, and the authenticated background existence check still retrieves full lists rather than a minimal opaque projection. | Preserve and browser-walk the Admin screen/standalone viewer; replace full-list discovery with a minimal authorized existence projection; constrain CORS; review function/access logs and embedded private links/credentials; add stale-tab/cache/cross-role denial and per-human session/read-audit controls. The owner/legal reviewer decides notification for the 21 affected people. | PARTIAL — LIVE ANON READ CLOSED; UI MERGED + Codex Admin browser-walk 2026-07-15; CORS/MINIMIZATION/INCIDENT REVIEW OPEN |
| F78 | P1 current integrity/availability/security and external cost | **VERIFIED-LIVE DEPLOYED AUTH/BOUNDS + TEST CHANGE PROOF + SCHEDULED READBACK.** `thumbnail-revision-scan` now requires a dedicated scheduler signature and exposes bounded aggregate counters only. Deployed fail-closed proof returned `503` when the server credential was absent and `401` for a wrong caller credential. Correctly authorized TEST-only same-link, no-write, and A→B change probes passed without exposing identities, URLs, object paths, or item-level results. The initial all-active scan checked 239 with 0 failed; [scheduled run 29370658087](https://github.com/sidney-afk/client-analytics/actions/runs/29370658087) completed green with all 239 unchanged. | Keep the scheduler secret dedicated and privately rotated; preserve the bounded aggregate-only response/log contract and dark repository-variable switch; alert on failed/nonzero-failure runs. Any expansion of scope, response detail, caller identity, or provider work requires fresh negative, cost, and concurrency review. | DONE — FAIL-CLOSED BOUNDED SCANNER AND FIRST SCHEDULED RUN VERIFIED LIVE |
| F79 | P1 current authorization/confidentiality/integrity and provider cost | **VERIFIED-LIVE CONFIG/READ-ONLY AGGREGATES + DEPLOYED SOURCE; resolver not invoked.** Active `thumbnail-folder-resolve` has JWT verification off, CORS `*`, and no custom auth while live Google service credentials are present. Anonymous table reads expose the exact caller inputs; thousands of current Calendar/Samples rows have thumbnail URLs. Any caller can make the service identity resolve private Drive parents, return/store file/folder identifiers, and perform service-role writes. | Require originating authenticated principal plus exact client/row scope, or a signed internal job identity; constrain response metadata, CORS, rate/work, and audit; reject inactive/unauthorized clients server-side; review provider/function usage. Pass deployed no/malformed/wrong-scope/correct-key TEST probes before keeping the resolver reachable. | OPEN — ACTIVE ANONYMOUS DRIVE RESOLVER/SERVICE-ROLE WRITER |
| F80 | P1 current thumbnail metadata correctness | **VERIFIED DEPLOYED SOURCE; race not induced.** The resolver's advertised stale-write protection reads `thumbnail_url`, performs a remote Drive request, then updates only by `(client,id)`. It does not compare the same URL/version in the UPDATE. Two A→B resolver calls that complete in reverse order can therefore pair current thumbnail B with folder metadata derived from A even though the initial stale check passed. | Make the final write an atomic compare-and-set on exact normalized `thumbnail_url` plus row version/timestamp; treat zero affected rows as stale and return no metadata mutation. Add reversed-completion, same-URL retry, URL-clear, archive, and Calendar/Samples concurrency tests plus a repair sweep for mismatched derived rows. | OPEN — TOCTOU CAN COMMIT STALE DRIVE PARENT METADATA |
| F81 | P1 current onboarding integrity/availability/abuse | **VERIFIED-LIVE CONFIG + DEPLOYED SOURCE + SANITIZED RELAY GRAPH; no request sent.** Public `onboarding-capture` has JWT verification off and no nonce/CAPTCHA/rate, request-size/schema/allowed-kind limit, or caller binding. Any caller chooses `id`, kind/funnel/contact/full payload/note; the service role upserts it, and `submit-fallback` can post caller text to the shared alert route. That fallback sends a Slack-shaped `text` body while the relay consumes typed alert fields, so it degrades to a generic unknown-issue/team alert rather than an actionable onboarding receipt. A known/obtained real ID can be overwritten, unlimited fakes can consume DB/Edge/Slack attention, and each upsert resets `created_at`, eroding chronology. Public intake needs anonymous clients, but it does not need unbounded anonymous mutation. | Add a server-minted short-lived submission nonce/session, per-IP/session/ID abuse throttling or CAPTCHA, strict byte/schema/kind constraints, conditional/versioned update ownership, immutable `created_at`, sanitized final-only notifications, and abuse/audit monitoring. Replace fallback text with the authenticated/versioned typed relay schema and a privacy-safe non-sensitive record handle—never raw contact/notes—then prove terminal delivery. Test spam, replay, oversize, unknown kind, foreign-ID overwrite, tab-beacon concurrency, and relay/Slack failure without losing legitimate drafts. | OPEN — UNBOUNDED PUBLIC UPSERT/ALERT ABUSE, ID OVERWRITE, AND NON-ACTIONABLE FALLBACK |
| F82 | P1 current client/Drive-link confidentiality and repository privacy | **EDGE + RAW TABLE PATHS CLOSED; PUBLIC SEED/LEAST-PRIVILEGE WORK OPEN.** `filming-plans` is live at v25 after pinned same-source run `29601466479` from exact `main@9d76df6`; its fingerprint passed. GET authenticates before service-role configuration/client creation, and missing/wrong keys return `401`. Read source accepts verified Admin/SMM/creative role keys; POST remains Admin-only. The protected Pages caller merged with #836, removes raw PostgREST, anonymous realtime, and Sheet fallback, and purges cached data on sign-out. Anon table SELECT is revoked and direct REST returns `401`/`42501`. The row-bearing public migration remains F64 rewrite work. | Preserve the protected caller and direct-table denial; continue intended-role/sign-out/cache regression proof. Complete F64's coordinated repository rewrite; constrain CORS, minimize fields/scope, and privately review source Sheet/Google Doc sharing. | PARTIAL — EF DENIES ANON; filming_plans anon SELECT REVOKED 2026-07-15 (direct REST now 401/42501); EF serves Admin; PUBLIC SEED OPEN UNDER F64 |
| F83 | P1 current client/media confidentiality | **VERIFIED-LIVE RAW-DATA DENIAL + PROTECTED EXACT-CARD PROJECTION + BROWSER PROOF.** Browser-key HEAD on `thumbnail_media_revisions` now returns `401`, and an unsigned request to the private snapshot object returns `400`. The protected reader succeeds for the exact authorized role/card scope, returns only short-lived signed image URLs plus least-field comparison metadata, and returns `403` for cross-client scope. Desktop and mobile Previous/Current comparison were verified on an owner-selected real Calendar card without reproducing client identity, token, Drive URL, object path, or private media in this public register. | Keep the raw table service-role-only and the snapshot bucket private. Preserve exact principal/client/card binding, no-store least-field responses, short signed-URL lifetime, cross-client denial, and browser regression coverage; do not reintroduce direct PostgREST/realtime or public-object access. | DONE — RAW HISTORY PRIVATE AND PROTECTED DESKTOP/MOBILE COMPARISON VERIFIED LIVE |
| F84 | P1 current credential confidentiality/attribution and next-edit history exposure | **VERIFIED-LIVE COUNT-ONLY AGGREGATE + DEPLOYED SOURCE/UI; no secret body fetched or reproduced.** All 3 active vault rows have passwords. `client-credentials` list returns every selected row including plaintext password before UI masking; reveal occurs locally and its caller-invoked audit is fire-and-forget, so direct API/DevTools extraction is unlogged. Shared/legacy-key auth is not bound to an active member and actor name comes from the body. Password is also a diff field whose old/new plaintext would be returned/rendered by history; zero password-change events exist today, making that a pre-first-edit risk rather than claimed current history exposure. | Replace shared/legacy keys with individually revocable active-member sessions and server-derived actor. Make list metadata-only; reveal exactly one secret just-in-time with synchronous fail-closed audit. Never retain old plaintext passwords absent an owner-approved encrypted/time-limited break-glass design. Test direct API/DevTools/copy/audit failure, stale/offboarded identity, cross-client/role, and first password change; privately review access and rotate exposed keys/secrets. | OPEN — VAULT BULK PLAINTEXT DELIVERY AND BYPASSABLE/FORGEABLE AUDIT |
| F85 | P1 current onboarding/credential confidentiality | **VERIFIED-LIVE AUTH NEGATIVE + COUNT-ONLY AGGREGATES + DEPLOYED SOURCE; no answer/credential body fetched.** Missing-key `onboarding-full` requests deny 401, but either the shared admin key or legacy onboarding key passes by secret match alone—without active `team_members` lookup, immutable member binding, or read audit—then service role returns full records and legacy credential arrays. Live totals are 1 standard, 1 AI, and 21 legacy rows; 19 legacy rows retain credential arrays. Deactivating a member/key-verify identity does not revoke a retained shared/legacy secret. | Require individually revocable active-admin sessions, server-derived member identity, synchronous append-only read audit, minimized/paginated responses, and dated legacy-key retirement after holder/caller inventory and zero-use proof. Rotate sessions/keys and privately review access. **Owner questions:** who still holds the legacy onboarding key, and must the 19 credential arrays remain after F64/F77 containment? | OPEN — FULL ONBOARDING/CREDENTIAL CORPUS BEHIND UNREVOCABLE SHARED-KEY-ONLY READ |
| F86 | P1 current staff/client privacy and authorization-surface minimization | **VERIFIED-LIVE COUNT/FIELD-PRESENCE ONLY + DEPLOYED RLS/SOURCE; no identity or identifier value fetched.** Raw `team_members` is anon-selectable: 20 rows including 6 inactive; 13 carry email, Slack-user, and Linear-user IDs. Raw `clients` is anon-selectable: 39 rows including 6 inactive; 26 carry Slack-channel IDs and 37 carry Linear-project mappings. RLS grants every column, while sign-in needs only a least-field active directory. | Revoke raw-table anon SELECT; map all SPA readers and replace them with purpose-specific minimal active projections. Protect inactive/recovery rows and email/Slack/Linear/mapping columns. Test direct omitted-column/inactive selection denial plus every legitimate sign-in/Production reader. | OPEN — PUBLIC RAW STAFF/CLIENT DIRECTORY AND INTERNAL PLATFORM MAPPINGS |
| F87 | P1 pre-auth-enforcement availability/security resilience | **VERIFIED DEPLOYED SOURCE/SCHEMA + LIVE AGGREGATE BASELINE; no load test and no abuse observed.** Public staff/client verifiers have no application request limits, lockout/challenge, source thresholds, or audit retention cap. Staff denials expose distinct internal reason classes. Every request performs service-role reads plus synchronous event insert; verifier/audit failure returns 500. Baselines are 85 staff events (59/24h) and 614 client events (99/24h), with little invalid traffic. F38 shows verifier 500 can be permissively cached. | Use individual high-entropy sessions, uniform non-enumerating denials, platform/app request controls and alerts, bounded/partitioned audit retention, and explicit fail-closed behavior. TEST burst/quota/audit-write-failure/timeout/recovery cases without production data or real secrets. | OPEN — VERIFIERS LACK REQUEST-ABUSE/RETENTION CONTROLS AND DEPEND SYNCHRONOUSLY ON AUDIT WRITES |
| F88 | P0 if operational/client content is confidential; P1 definite owner-decision/go-live omission | **SAFE SUBSET PARTIALLY CLOSED LIVE; SYSTEMIC BOUNDARY OPEN.** Direct anon reads now return `401` for `thumbnail_media_revisions`, `social_media_managers`, and `smm_weekly_reports`; the SPA has no raw dependency on those tables. Candidate source moves the one direct `filming_plans` read behind its staff EF, but its table revoke is held until Pages merges. `clients` was not revoked because Production still calls `_prodRestRows('clients', ...)`. This task deliberately leaves the directly used `syncview_runtime_flags`, `calendar_posts`, `content_samples`, `templates`, `caption_prompts`, `workload_issues`, and `team_members` grants unchanged pending minimal projections. The broader census still includes other operational Production/event tables and remains an owner/legal decision. | Merge the filming caller, apply its revoke, then build principal/client/role-scoped least-field projections table by table before removing each remaining raw grant. Preserve `syncview_runtime_flags` only if the owner explicitly confirms its intended-public contract. Pass direct REST/cross-client/inactive/cache/stale-tab/mobile/second-device denial; do not treat SPA tokens as read authorization. | PARTIAL — THREE SAFE TABLES CLOSED; FILMING CLOSED 2026-07-15; RAW CLIENT/DIRECT TABLE WORK OPEN |
| F89 | P1 pre-auth-enforcement release evidence | **VERIFIED-LIVE READ-ONLY AGGREGATE + DEPLOYED SOURCE/SPEC; no token/client value fetched.** The verifier stores `ok=access_allowed`, not credential validity. In permissive mode missing/invalid credentials are allowed and logged `ok=true`; no separate validity field exists. Of 614 events, 611 are missing-token/permissive/true. The last seven days contain 588 such events and zero valid-token events; the only valid event is over eight days old. §6.4 requires a fresh validated event per active client, but no machine gate exists, so an `ok=true` check can false-green tokenless opens. | Persist separate `credential_valid` and `access_allowed`, current token revision/fingerprint, and active-client binding; deprecate ambiguous `ok`. Build an exact full-active-roster gate requiring fresh valid evidence at current revision, with missing/extra/stale failure, and test permissive/enforced/missing/invalid/rotation/inactive cases. | OPEN — LIVE TOKEN-EVIDENCE WINDOW IS ZERO-VALID AND AMBIGUOUS OK CAN FALSE-GREEN |
| F90 | P2 soak/pager evidence | **VERIFIED-LIVE IN CONCURRENT MAIN AUDIT; renumbered from its colliding F27 after rebase.** TEST-drill diffs feed the same `diff_count` and two-consecutive-nonzero pager/soak criteria as real-client divergence. A `<PRIVATE_TEST_CLIENT>` drill held a small nonzero diff for roughly six hours on 2026-07-13 while repair stayed zero and real clients were clean. The gate therefore cannot distinguish planned TEST churn from a real-client incident. The concurrently merged row also reintroduced the private TEST identity, now scrubbed to notation under F74. | Separate TEST and real-client aggregates at the reconciler source; keep TEST divergence visible in a dedicated diagnostic but exclude it from real-client soak/pager pass criteria. Add mixed TEST+real, TEST-only, and real-only fixtures plus public-safe output checks. | OPEN — CONCURRENT NUMBER COLLISION RESOLVED; TEST/REAL SOAK SIGNAL STILL MIXED |
| F91 | P0 current Linear integrity / P1 cutover authorization | **VERIFIED-LIVE SOURCE/CONFIG; no mutation endpoint invoked and no unauthorized write observed.** Four active n8n mutation surfaces—status, comment, video intake, and graphics intake—have no webhook authentication or incoming staff/client credential check. Their code validates only the target team's authority before using server-held Linear credentials. Both teams are currently Linear-authoritative, so that direction gate permits rather than contains the serving routes. The shipped `?intake=1` page intentionally bypasses staff sign-in and sends client-selected intake payloads with no caller identity; status/comment browser calls likewise omit the verified principal available at the preceding card save. Recent metadata confirms the status/comment workflows are serving successfully. The inventory documented these facts but deferred neutralization to the epoch, and no current containment gate existed. | **Contain now:** require an individually revocable active staff session for staff bridges and either staff auth or a server-minted, short-lived, exact-client intake capability for a deliberately shareable intake page. Resolve target/client/team server-side; bind immutable actor/role, audit, rate/size limits, and idempotency; pass deployed anonymous/expired/cross-client/forged-actor negative tests without production writes. **Owner question:** must `?intake=1` remain externally shareable, and if so who may mint it, for which client, for how long, and how is it revoked? Later reroute through the native gateway/outbox and retire the bridges at the planned epoch. | OPEN — PUBLIC LINEAR MUTATION ROUTES; CONTAIN NOW |
| F92 | P1 before end-of-grace/B5 | **VERIFIED SOURCE + CANONICAL ORDER; failure inferred without a write.** Both operative retirement sequences proved both teams' outbox zero, turned F2 normal outbound `off`, and only then said “final reconcile.” The Track-B reconciler exposes an apply mode that, for SyncView-authoritative diffs, enqueues or requeues outbound intents. Interpreting the canonical phrase as apply can therefore create new mirror debt after the zero proof and after the only normal drainer is disabled, then allow retired-mode activation/export on a false-zero state. F27 is the opposite ordering failure (off before drain); F61 is the missing freeze. This is the distinct post-zero enqueue window. | **Docs corrected:** the post-F2 reconciliation is explicitly dry-run/detect-only. Any diff or would-enqueue result aborts the transition, returns under the server freeze to F2 live, classifies/drains/dispositions the work, and re-proves per-team zero before retry. Mechanically bind freeze/high-water, F4 false, zero, F2 off, final dry-run zero, and retired-epoch activation in one stale-state-failing transition; TEST an apply attempt and a late system diff. | OPEN — DOCS CORRECTED; ATOMIC POST-ZERO ASSERTION/DRILL MISSING |
| F93 | P1 operator/current-truth control | **VERIFIED SOURCE/DOC COUNTEREXAMPLE.** The living-truth contract promises a dated commit stamp and says the docs cannot silently rot, but `truth-sync.js` accepted any date anywhere, did not require a commit anchor, did not compare the briefing's advertised finding boundary with the audit register, and did not ensure every open P0/P1 finding appeared in an operative go-live/readiness/monitoring/rollback surface. It also ignored four Edge routes composed from a base constant while claiming exact endpoint-set coverage. The suite was green while the read-first briefing stopped at F81, several stamps lacked commit provenance, the touchpoint inventory retained three superseded authority-gate claims plus an obsolete endpoint count, and the operator-doc union had no explicit F79 reference. A fresh-looking entrypoint could therefore omit a current blocker while CI certified it. | **Corrected in this PR:** reconcile the counterexamples; require an exact date plus a resolvable ancestor commit no older than 30 days; require the briefing's `through Fxx` boundary to equal the register maximum; require every non-closed P0/P1 register row to appear in an operative control doc; resolve the simple base+suffix Edge routes; and check the literal/composed count. Keep semantic/live truth review human-owned; these checks prevent silent omission, not factual drift inside a present row. | DONE — DOCS/CI COVERAGE GUARDS ADDED |
| F94 | P1 before first creative-team flip | **VERIFIED SOURCE; no assignment write performed and no invalid current assignment claimed.** Production's manual assignee picker offers every active same-team `team_members` row. Its bootstrap omits `linear_user_id`; the server validates only target ID, active state, and team; and the schema permits admin/SMM/editor/designer rows to carry either creative team. It does not enforce the owner-approved creative role or a usable Linear mapping while mirroring is mandatory. The native assignment commits and the UI adopts success before asynchronous `linear-outbound`, which later throws when the selected row lacks a mapping; a nonempty but inactive provider user is not rejected pre-commit either. Auto-intake has a separate, stricter mapping pool, so this finding is the manual reassignment lane and is distinct from F30 allocation, F31 caller revocation, and F37 personal-queue identity. | Define one server-authoritative eligible-assignee projection and enforce it at commit: active native member, owner-ratified compatible role/team, and active provider mapping until retired mode. The picker must consume the same projection; audit existing assignments; reject stale/ineligible/unmapped targets before native state/outbox writes. At retired activation remove only the provider-mapping requirement atomically. Add negative noncreative/cross-team/inactive/unmapped/provider-inactive/stale-picker tests. **Owner question:** may admin/SMM ever own a creative deliverable, or is Video=`editor` and Graphics=`designer` exact? | OPEN — MANUAL ASSIGNMENT CAN COMMIT AN INELIGIBLE OR UNMIRRORABLE OWNER |
| F95 | P1 at first creative-team flip | **VERIFIED SOURCE/TEST CONTRACT; no live write or private data read.** Production loads clients/members/batches/deliverables once at mount. Its only 30-second timer refreshes authority, not work data; operational reload runs only on focus, visibility return, pageshow, or explicit retry/error handling. The normal top bar intentionally has no Refresh control and there is no deliverables/comments realtime subscription. An issue opened in a continuously foreground tab therefore keeps cached assignment/status/due/artifact/comment state indefinitely; comment refresh occurs only when the issue is reopened or a general return refresh runs. The current test's broad-sounding “bounded timer” assertion checks authority only, while the spec promises a realtime staleness watchdog and polling fallback. | Add an owned realtime subscription with catch-up or a bounded foreground poll for operational rows and the open comment thread, plus last-success age, visible stale/degraded state, backoff, and a keyboard/touch-accessible manual refresh. Preserve focus/scroll/drafts and merge by version. Prove two-tab/second-device assignment, status, tweak/comment, artifact, archive, and failure/recovery convergence while both tabs remain foreground. | OPEN — FOREGROUND CREATIVE QUEUES/DETAILS CAN STAY STALE INDEFINITELY |
| F96 | P1 creative mobile readiness | **VERIFIED SOURCE/RESPONSIVE CENSUS; no live interaction or data read.** At widths at or below 900px Production hides the entire sidebar. “My issues” and the only visible Search button that opens the command palette live inside that sidebar. The mobile-visible top bar offers team scope, Active/Backlog/All, filters, and display options, but no personal/team queue switch. Keyboard `/` or Ctrl/Cmd+K can still open the palette with hardware keys, and a crafted `?view=my` URL renders the personal view, but an ordinary fresh touch-mobile user has no discoverable route to it. No Production test covers this navigation. This remains after F37 binds the queue to the correct identity. | Add a touch-visible responsive queue-scope control or navigation drawer for Issues/My issues (and an accessible palette trigger if retained). Preserve deep-link/back behavior and team filters. Test fresh sign-in, reload, account switch, zero-row identity, portrait/landscape, 900px boundary, touch-only navigation, and every active creative role after F37. | OPEN — MOBILE CREATIVES CANNOT DISCOVER OR NAVIGATE TO THEIR PERSONAL QUEUE |
| F97 | P1 before the first real-client parity cohort | **VERIFIED SOURCE/CANONICAL-SEQUENCE COUNTEREXAMPLE; no runtime flag changed.** The canonical checklist closes many auth prerequisites in Phase 0, runs a TEST-only dark merge in Phase 0.5, and then immediately arms parity and enrolls real clients in Phase 1. It never executes FLIP_RUNBOOK F5 in any later phase. The current flag remains permissive, and `client-token-verify` deliberately allows a missing or invalid client token whenever the mode is not exactly `enforced`. An operator can therefore complete every named phase, route real cohorts, and later hand off both teams while client-link enforcement never starts. The gateway's independent credential checks do not repair that link/read posture; F88's raw-anon read decision is a separate boundary. F70 covers the unsafe reverse, while this is the missing forward transition. | **Docs corrected in this PR:** add an explicit Phase 0.75 owner gate before any real cohort. It requires every Phase-0 auth/read/write blocker closed, a fresh exact-roster valid-token/current-revision report, the fixed F38 cache/revocation behavior deployed, forced stale-verdict/session invalidation, exact permissive-state CAS to `enforced`, readback, and missing/invalid/expired/rotated/inactive/verifier-failure negative proof. Phase 1 is blocked unless the flag remains enforced; an incident preserves enforcement per F70. Mechanically bind the action and proof to the same unexpired preflight token. | OPEN — CANONICAL FORWARD STEP ADDED; UNDERLYING AUTH GATES/ENFORCEMENT DRILL REMAIN |
| F98 | P1 at the first Graphics authority flip | **VERIFIED SOURCE/CANONICAL-SEQUENCE COUNTEREXAMPLE; no runtime flag changed and no past incident claimed.** GO_LIVE and Track-B told the owner to change Graphics authority to SyncView and only then turn normal outbound live. Once authority is SyncView, `production-write` accepts the normal mutation, transactionally commits native state plus its outbox intent, does not targeted-drain that normal lane, and returns success with `mirror_pending=true`. While F2 is off, `linear-outbound` does not even select normal rows. If the second flag action, readback, browser session, or operator handoff fails after F1, staff are already told SyncView is authoritative and successful native work accumulates while the promised Linear fallback remains stale. F27 then forbids a blind authority reversal. F59 is the broad preflight order; this is the unsafe intermediate state inside the final forward transition. | **Docs corrected in this PR:** first prove exact zero real, non-parity normal rows for **both teams** in `pending/failed/shadow_ok`, owner-classifying/resolving any residue. Then arm/read back F2 `live` while both teams remain Linear-authoritative; require correlated terminal drainer/credential receipts plus an observer outside n8n, with zero normal-lane writes (any write must equal an expected, acknowledged `legacy_parity_written`), and re-prove both-team normal zero. A fresh pager timestamp is not health (F131/F132). Only then execute/read back Graphics F1. Per-row authority makes the intermediate state fail-safe, but paused residue can starve the global batch or become writable at F1. Any F2 failure leaves authority unchanged. Mechanically enforce this order and TEST interruption after every CAS/readback; preferably expose one stale-preflight-failing transition. | OPEN — FAIL-SAFE FORWARD ORDER DOCUMENTED; EXECUTABLE TRANSITION/INTERRUPTION DRILL MISSING |
| F99 | P1 before writable creative due dates / P2 current display | **VERIFIED SOURCE + DETERMINISTIC CLOCK SIMULATION; no live data or write used.** Production captures browser-local midnight once when the whole SPA script loads and `_prodToday()` forever clones that frozen value. Relative parsing, Tomorrow/week choices, calendar focus, and today highlighting all reuse it; focus/pageshow refresh reloads rows but cannot advance the constant. Overdue styling uses a different, fresh UTC date. Production defines no business/viewer timezone contract. A tab opened before midnight can write the prior day's relative date after midnight; independently, in a UTC−6 locale a due-today item becomes visually overdue at 18:00 local when UTC advances. Existing tests assert only ISO shape/call wiring, not the intended date. | Owner chooses one calendar-day contract (business IANA zone or explicitly viewer-local). Derive current date on demand from one helper for parsing, option values, highlighting, overdue comparison, and writes; invalidate/re-render at the next chosen-zone midnight and on focus/visibility return. Add fake-clock tests for long-open tabs, UTC-negative/positive zones, DST, midnight, leap day, and mouse/keyboard/bulk due paths. F100 separately owns year loss in picker values. | OPEN — FROZEN LOCAL CLOCK AND FRESH UTC OVERDUE DISAGREE |
| F100 | P1 before any creative due-date write | **VERIFIED SOURCE + DETERMINISTIC HELPER SIMULATION; no live write used.** The adapter renders stored ISO due dates as `MM/DD`, but the due parser accepts neither slash dates nor explicit year input, so the existing due cannot seed the picker month/selection. Quick options and every mouse-click calendar cell store only a `Mon D` label; the click path reparses that label using the frozen current year. Keyboard calendar Enter instead passes a `Date` and preserves its year. The exact helpers turn a Dec 29, 2026 “In one week” mouse click into `2026-01-05` instead of `2027-01-05`; a mouse click under a visible July 2027 header writes July 2026, while keyboard Enter writes 2027. | Keep canonical ISO `YYYY-MM-DD` in every quick-option/cell value and `dueRaw`; format only visible labels. Seed month/selection from `dueRaw`, compare selection by ISO, and make mouse/keyboard/bulk paths call the same converter. Support explicit-year input without silently substituting the current year. Test existing/far-future dates, Dec→Jan, leap day, month navigation, mouse vs keyboard, typed ISO/year, and multi-select. | OPEN — MOUSE DUE PICKER SILENTLY DROPS THE SELECTED YEAR |
| F101 | P1 before real native-intake enrollment / current single-team review correctness | **VERIFIED CURRENT POST-#850 SOURCE AND LIVE AGGREGATE COUNTS; no row bodies, identities, links, or write used.** Submit exposes Video-only and Thumbnail-only actions. The legacy path calls only the selected team and leaves the other link blank while initializing both component statuses to `In Progress`; #850's dark native path likewise creates only the requested team while materializing both statuses. Calendar overall status and client readiness always include Video, Graphics, and Caption, while an unlinked Video/Graphics status control is disabled and bulk status skips it. A single-team card can therefore remain permanently in progress and hidden from the ready client sheet because a nonexistent sibling cannot advance. A count-only live census found 697 rows with exactly one creative link; 46 store the absent side as `In Progress`, including 3 graphics-only rows whose present Graphics and Caption legs are already terminal. The census proves the shape/consequence, not that every row came from Submit. This also contradicts the owner-locked paired Video+Graphics intake model. | **Owner decision:** enforce the locked paired model by removing the two single-team actions and rejecting single-team intake server-side, then classify every single-link row as a repairable missing link versus an intentionally absent component. If single-team work remains a supported exception, persist explicit active-component semantics and make overall status, readiness, review queues, bulk actions, comments/alerts, artifacts, migration, and all personas operate only on active legs; render absence as N/A, never fake approval. Test all three legacy modes plus native Submit/Create Post, mixed authority, client visibility, migration, and missing-link repair. | OPEN — ABSENT DELIVERABLE IS MATERIALIZED AS UNFINISHABLE WORK |
| F102 | P0 current staff/client read boundary / P1 before token enforcement | **VERIFIED-LIVE WITH A FICTIONAL UNKNOWN CLIENT PARAMETER + VERIFIED SOURCE; all non-GET/HEAD requests were blocked, and no record content, identity, or write was retained.** Any nonempty `?c=` sets client-link mode and bypasses the password before the client exists or a token is checked. `init()` loads the analytics/bootstrap data, verifies only a recognized client, and otherwise falls through to `navTo('home')`, which renders the staff Home view. Separately, `?prod=1` is processed before client-link resolution and grants the read-only Production preview. A fresh browser with a fictional unknown slug returned 200, hid the password, activated Home, painted the Client Analytics staff surface, and showed no invalid-link state. The verifier is never called for the unknown slug, so F97's future enforcement action cannot close this path. Client-mode write restrictions still apply; no unauthorized mutation is claimed. This is distinct from F38's recognized-client verifier/cache failure and F88's direct anonymous table reads. | Replace the boolean query-param shortcut with a fail-closed entry state machine: resolve an allowed client and validate its current token **before** any client bypass, data load, cache hydration, or route. Unknown/malformed client, unsupported view, invalid token, and every `c`+staff-route/hash/`prod` combination render one invalid-link surface and purge loaded DOM/cache/channel state. Require an individually verified staff session for Production/staff routes; never let a client URL fall through to staff Home. **Owner question:** exactly which client-link views are supported? Encode that allowlist server-side and test fictional unknown/encoded/empty clients, missing/invalid/rotated tokens, every `v`/hash/`prod` combination, history/saved-nav, cache, mobile, and second device. | OPEN P0 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F103 | P1 operator/data integrity now | **VERIFIED CURRENT DOC + SOURCE; no migration command or live system was run, and no rerun/incident is claimed.** The completed comment-import playbook still published TEST, full-production, and legacy-recovery `--apply` commands using the consumed 2026-07-12 run IDs/timestamp. Its historical identical rerun wrote zero only against that snapshot. The script requires merely a nonempty explicit run ID, has no completed-run ledger, deliberately accepts same-run rows, and plans inserts plus updates when source timestamps/lifecycle advance. It performs RPC writes before its post-apply reconciliation report. Full rollback is independently blocked by F68. A later copy/paste can therefore alter current comments before discovering changed counts, without an approved recovery. | **Immediate docs correction in this PR:** quarantine the playbook as a historical non-runnable record and remove apply/rollback recipes, including script-header examples. Data-migration owner must make completed run IDs terminal via a server-side completion ledger/CAS; any future import needs a fresh owner-approved ID, immutable source checkpoint/hash, current script/schema commit, exact dry-run plan, expiry, bounded TEST rehearsal, and dependency-safe compensating plan. Add CI rejecting active full-apply recipes in executed/historical docs; audit other completed playbooks and report artifacts. | OPEN — PLAYBOOK QUARANTINED; CONSUMED-RUN RUNTIME GUARD/AUDIT MISSING |
| F104 | P1 current operator/availability and data-integrity risk | **VERIFIED SOURCE + CURRENT-DOC COUNTEREXAMPLE; no code, workflow, flag, or live data was changed.** The unbannered Calendar Phase-4 checklist said `_calV2Ready()` was true for every user and its first two deletion phases were behavior-preserving. Current source still honors `?v2=0` plus a sticky opt-out, and README/System Map still advertise that rollback. Worse, Step 1 told operators to delete `LINEAR_STATUSES_URL` with the v1 reconciler, but the live v2 `_calRefreshParentLinkFlags()` also uses it on foreground/return loads; its caught failure would silently stop project/due/editor/parent-link metadata refresh. The two required tests reference none of the flag, fallback, or metadata symbols. The recipe also collapses distinct read, refresh, save-shape, base-watermark, Films, and three-tier Kasper fallback behavior, while its “orphan” helper is extracted by two reconciliation scripts. | **Immediate docs correction in this PR:** replace the checklist with a non-operative quarantine explaining the counterexamples. Product/ops owner must decide whether per-browser rollback retires; measure identifier-free opt-out/fallback usage; provide replacement outage containment; perform whole-repo symbol analysis; reconcile auth/read policy; and pass v2-on/off, Supabase-failure, n8n-fallback, metadata/banner, Calendar/client/Kasper/Films, concurrency, focus/mobile/second-device tests. Update README/System Map/ROLLBACK atomically. Every workflow/Sheet deletion separately obeys F60's exact snapshot/recreate/rehearsal/readback rule. | OPEN — DELETION RECIPE WITHDRAWN; OWNER RETIREMENT/TELEMETRY/RECOVERY PLAN MISSING |
| F105 | P1 current release evidence / before any human flip | **POST-#850 TEST-EPOCH AND READ-AUDIT REPAIR VERIFIED ON A TEST-ONLY CANDIDATE BASED ON CURRENT MAIN `6a8416c`; live-observation lanes remained zero-mutation and writable behavior remained fully mocked.** Hosted run `29624162471` is historical initiating evidence, not the exact current-main baseline; later runs `29654228319` and `29655695552` showed the still-red fast/heavy split. Detailed assertions were runner-local under F122, so exact-source local runs supplied the private failure evidence. Classification: **(a)** four stale owner-behavior assertions — three status/due/assignee first-row checks could select the bounded writable TEST override, and one layout check pinned the pre-#868 secondary-line parent trail instead of the owner-ratified inline project-row breadcrumb; **(b)** `dueFocusSync`, `ctrlXGuard`, `composerBoxClick`, `filterArrowRight`, `commentBodyWrap`, and `commentSignedOutStateNotSkeleton` shared one empty-active-team reset synchronization defect, while structure/layout/accessibility/behavior shared a broken read/console classifier that treated recovered reads as unconditional failures and could snapshot before outstanding reads settled; **(c)** zero product defects. | **Repaired only test robots and their docs.** Exact non-TEST selection, loaded-row fallback with fail-closed diagnostics, the inline-breadcrumb layout assertion, and one strict shared read/console audit were added. That audit accepts only an exact method+URL eligible network/429/5xx failure followed by 2xx/304; each generic resource-console error additionally needs a one-to-one exact URL/time match. It separately accepts an old-document read proven canceled by a committed main-frame navigation. Non-retryable, persistent, pending, unrelated, page/console, unproven-abort, and every write-like failure stay red; mutation checks run after the bounded settle. Candidate fast passed 7/7 in 143.8s; `npm run test:prod-polish` passed 10/10 in 484.4s; behavior passed 168/168 with zero writes/unexplained errors, zero recovered retries, and one proven navigation abort. The dependency-free classifier matrix passed. No product code, Edge Function, flag, n8n, Linear, database, deployment, or live data changed. Require a green manual fast/interaction/heavy run on the exact candidate plus cloud review before merge; detailed logs and review output remain runner-local under F122. | DONE — TEST ROBOTS + LOCAL EXACT-CANDIDATE AGGREGATE GREEN; CLOUD REVIEW/MERGE PENDING |
| F106 | P0 current business-workflow integrity and communications boundary | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG + CURRENT SOURCE + PRIOR AUDIT CARRY-FORWARD; the webhook was not invoked, execution bodies were not read, and no identity, client value, credential, or private record was retrieved.** The active Sales Intake workflow exposes its POST webhook with authentication `none`; the current 19-node graph has no authorization/signature decision, incoming staff check, replay/idempotency control, request limit, or configured error workflow. The browser sends only `Content-Type`; its Kasper query flag is a per-tab UI unlock, while the staff capability map protects Credentials and Onboarding but not Sales Intake. The route can write the protected intake ledger, create an agreement with a server-held provider credential, send client email, and notify staff. The earlier July audit already classified the general route P0, but this operative register omitted it. Metadata currently retains zero executions, so neither use nor abuse is claimed. | Sales automation + security/ops owner chooses Kasper-only, Admin-only, or both. Require an individually revocable active-member session and server-derived role/actor **before** every branch/side effect; enforce exact action/scope, bounds, immutable audit, idempotency, and deployed no-key/expired/wrong-role/replay denial without production writes. If that cannot ship immediately, owner decides whether to deactivate the route and use the manual process. | OPEN — ACTIVE PRIVILEGED SALES WORKFLOW AUTHENTICATES NO CALLER |
| F107 | P1 current client-communications and data-integrity risk | **VERIFIED-LIVE SANITIZED ACTIVE GRAPH + CURRENT SOURCE/SCHEMA; the route was not invoked and no execution payload or client record was read.** Both normal and `send_existing_contract` branches return `{ok:...}` before Gmail runs. The response exposes no queued/pending or email-completion receipt; the browser immediately clears its draft/preview and renders “Intake sent,” while later Gmail failure can only take a continue-error branch. The send-existing branch accepts browser-round-tripped preview row ID, contract ID, and signing URL, updates the service-only table by that ID, and sends from those values without a database read, ownership/state comparison, ID-shape check, or provider-host allowlist. Default/preview requests mint new IDs, and no request/idempotency key exists beyond the row primary key. A lost response or manual retry can create duplicate paperwork; a later email failure leaves a false browser success with no durable resume token. | Persist one server-minted idempotency/preview receipt and explicit state machine. Resolve and CAS server-owned preview state, validate provider URL/state, resume incomplete steps, and return “accepted/processing” with a protected status/retry surface—or success only after required communication/audit completion. Page on stuck/failed states and TEST lost response, duplicate click, provider/email failure, wrong/stale preview, and retry after partial commit. | OPEN — SUCCESS PRECEDES EMAIL AND REPLAY TRUSTS BROWSER-ROUND-TRIPPED STATE |
| F108 | P1 current operator/change safety | **VERIFIED CURRENT SOURCE/SCHEMA/FOCUSED TESTS + SANITIZED SALES WORKFLOW CONFIG; no runtime, schema, workflow, or production data changed.** Four files under the repository's purportedly living feature-spec surface still described deployed work as pending and retained copyable rollout/change instructions. YouTube Title Review said its backend and Notes routing were pending although the schema, writer, picker/routing source, and tests are live; Kasper global state and thumbnail cache said their backend fields were absent/per-device although their schema/writer paths are deployed; Sales Intake said PR #652/its obsolete branch were active, specified a separate top-level tab, called the surface authenticated, and treated provider setup as unfinished although the current implementation is a Kasper subtab backed by the active graph in F106/F107. Source comments said replies reopen finished Kasper cards while code/tests enforce explicit reroute only, and four adjacent migration headers still instructed readers to perform the completed rollout manually. All five focused suites passed against current behavior. | **Corrected in this PR:** convert the two rollout files to deployed-state contracts; reconcile YouTube and Sales status/placement/backend truth; mark the unimplemented title system-event timeline explicitly deferred; remove obsolete live-change/branch/private-reference instructions; correct Kasper source and migration comments; redefine `docs/features/` as status-bearing contracts; and add a truth guard that rejects the known stale-rollout markers in both feature contracts and deployed schema records. | DONE — FOUR ACTIVE FEATURE CONTRACTS, SOURCE COMMENTS, AND MIGRATION RECORDS RECONCILED |
| F109 | P1 before treating YouTube title approval as durable client sign-off | **VERIFIED CURRENT SOURCE + FOCUSED OFFLINE TESTS; no live call, row, or write used.** The YouTube title is the generic editable card `name`. After `title_status` reaches `Approved`, an SMM—and a client when Collaborative mode permits—can change that input. The generic input/blur handlers persist only `name`; they do not clear `client_title_approved_at`, move `title_status`, or append a title lifecycle record. Approval stamps clear only when status drops, not when the approved text changes. The design reserved an `edit_title` old→new event precisely because edits could leave status unchanged, but current source emits none and the focused suite has no approved-title-edit case. The altered title can therefore retain a prior Approved state/stamp with no durable evidence of which text was approved. | **Owner decision:** a material title edit must invalidate approval and re-enter review, or atomically append an immutable server-generated old/new event tied to actor and row revision while the UI visibly marks approval as predating the edit. Prefer both renewed approval and audit for client-approved content. Enforce server-side; test SMM and collaborative-client edits, no-op/whitespace changes, concurrency, offline retry, undo, timestamp behavior, and second device. | OPEN — APPROVED TITLE TEXT CAN CHANGE WITHOUT INVALIDATING OR RECORDING SIGN-OFF |
| F110 | P1 current onboarding completeness / before any new-client go-live | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG + CURRENT SOURCE/DOCS + FOCUSED FICTIONAL-DATA BROWSER SIMULATION; no route was invoked and no execution payload, identity, credential, or client record was read.** Both active standard and AI submit graphs send HTTP 200 after the intake-row insert and a fail-soft notification, then dispatch provisioning without waiting; credential import is a separate fail-soft branch outside the response dependency. A same-ID insert conflict returns `{ok:true, duplicate:true}` directly and reaches neither branch. The active provisioning graph has no durable job/step receipt, resume decision, completion callback, configured error workflow, or whole-run partial-failure reconciliation; it also contains none of F69's token/project/authority/runtime enrollments. The SPA treats any 2xx as success, clears the draft and stable submission ID, and renders Thank You for primary or capture-only fallback acceptance. If insert succeeds but credentials/provisioning later fails, retry reports duplicate success and cannot heal the partial client. The Kasper inbox reads only the three primary tables, omits fallback/dead-letter records, does not display returned status, and has no acknowledgement/completion/retry/archive action, pagination, polling, or realtime; a second device can see a new row while an already-open inbox remains stale. Zero retained execution metadata means no historical incident is asserted. | Onboarding automation + security/ops owner persists one server-minted idempotent onboarding job **before** acknowledgement; distinguish captured/processing/complete/failed; make every side-effect step durable, retryable, read-back verified, and resumable on duplicate/lost response. Completion includes F69 mapping, protected token/revision, authenticated Track-A routing/policy, credentials receipt, and every required legacy provisioning receipt. Make fallback/dead-letter work, status/step age, paging/freshness and audited acknowledge/retry/resume visible to staff. TEST failure after insert and before/after every side effect, lost response, duplicate click, partial run, retry, capture-only replay, stale/second-device inbox, and teardown. | OPEN — CAPTURE SUCCESS IS MISREPORTED AS PROVISIONING SUCCESS; DUPLICATE RETRY CANNOT HEAL |
| F111 | P1 current onboarding operator handoff | **VERIFIED-LIVE SANITIZED WORKFLOW METADATA + CURRENT OPERATIVE DOCS; no route, payload, credential, identity, or client record was read.** The current SyncView standard/AI forms explicitly replaced the old Notion intake, but the operative new-client runbook still calls the Notion database and its Slack DM the entry point. The named legacy workflow is active-labelled, yet its current sanitized trigger report says it has no production trigger and can run only manually; its own description says it is inactive until setup, and retained execution metadata is empty. That silence cannot prove use or health. A real SyncView submission can therefore be captured while the operator follows a handoff that is neither the current product path nor proved operational. | **Docs corrected in this PR:** make the SyncView standard/AI inbox plus F110's durable receipt/alert the sole current entry and mark the Notion path replaced. Owner archives the legacy active-labelled object only through F60 after private backup/restore proof and identifier-free zero-use evidence. Add an independent capture-without-staff-acknowledgement alarm so a fail-soft notification cannot strand a client. | OPEN — STALE ENTRY REMOVED; DURABLE CURRENT HANDOFF AND LEGACY RETIREMENT REMAIN |
| F112 | P1 before the first creative flip and B5 SMM handoff | **VERIFIED-LIVE STATIC APP SOURCE + CURRENT POST-#850 MAIN + DOC/TEST CENSUS; no data row, identity, credential, or write used. Existing inventory carry-forward, promoted here because it remained outside the go-live register.** The deployed root returned 200 and includes a working Production `?prod=1&d=<id>` detail route, but both Calendar and Samples card-slot renderers still receive only `linear_issue_id` / `graphic_linear_issue_id`, build “Open the linked Linear…” anchors/editors, and contain neither native deliverable ID nor “View sub-issue.” Current main transports `video_deliverable_id` / `graphic_deliverable_id` and uses them for native write routing/materialization, but neither card surface dereferences those IDs to `deliverables → team_members`; the app's team-member reads serve sign-in and Production, not a card ownership projection. No test asserts the promised card deep-link or editor chip. This contradicts §9.2/§9.6/D-20 and the §12 probe definition. | Calendar/Samples + Production owners build one authority-aware per-component projection. For a flipped team, the staff card shows the current active assignee (or explicit unassigned/inactive/degraded state) and **View sub-issue** opens `?prod=1&d=<id>` in a new tab; Linear-authoritative legs retain the fallback link only for the documented grace epoch. Reassignment/realtime/return refresh must converge without disrupting card drafts. Gate Graphics, Video, and B5 on Calendar+Samples, mixed authority, missing/stale linkage, unassigned/inactive member, mobile/new-tab/back, Linear-unavailable, and second-device tests. F47 still owns notification receipt; F50 owns review-status projection; F94 owns assignment eligibility. | OPEN — NATIVE CARD IDS ARE WRITE-ROUTING-ONLY; SMM NAVIGATION/OWNERSHIP STILL POINTS TO LINEAR |
| F113 | P1 current client sign-off integrity / before relying on Calendar or Samples approval at go-live | **VERIFIED-LIVE PUBLIC STATIC SOURCE + SANITIZED ACTIVE N8N NODE INTERNALS + CURRENT POST-#850 MAIN + FOCUSED OFFLINE EXECUTION/TEST CENSUS; no live row, identity, credential, or write used.** Calendar and Samples client approval moves a component to `Approved`, stamps `client_<component>_approved_at`, and removes the last-approved card from the client queue. The staff Sheet remains editable after that state: an SMM can replace `asset_url` or `thumbnail_url` on either surface and change Calendar `caption` / `caption_alt`; a Collaborative Calendar client can also change existing caption text, while media and Samples-client fields remain read-only. Extracted real handlers preserved the old Approved state/stamp while queuing only those content fields. The generic handlers never invoke approval invalidation; `_calClearStaleApprovals` / its Samples twin clear stamps only after a status regression and explicitly treat `Approved` as valid. Both active legacy upsert graphs accept content and approval fields independently with no content-to-approval invalidation or material-edit event. Current Edge writers likewise emit status/approval/link/comment events but no material-content revision event, and the schema stores no approved revision/hash. The same-link thumbnail refresh path intentionally allows the visible file to change without a URL change; `thumb_rev` is only a cache bust. Existing tests cover status-driven stale-stamp clearing and hidden Calendar `Approved`, but no test binds approval to later content. A client can therefore approve one video/thumbnail/caption and staff can ship a different revision under the old green status/timestamp without the card re-entering review. F109 remains the title-specific case. | Calendar/Samples + data owners define a server-side per-component material revision/hash contract. An approval records the exact revision; every material edit or provider-side same-link revision atomically invalidates/reopens review (or visibly marks sign-off stale under an explicit owner policy) and appends an immutable server-derived actor/old/new revision event. Normalize true no-ops, protect approval/edit races with CAS, and preserve drafts on conflict. Test Calendar+Samples video/graphic/caption, SMM and Collaborative-client permissions, read-only clients, alt captions, same-link file replacement, multi-field edits, approval-versus-edit concurrency, offline retry/undo, queue re-entry, refresh, and second device. | OPEN — APPROVED MEDIA/CAPTION CAN CHANGE WITHOUT INVALIDATING OR VERSION-BINDING CLIENT SIGN-OFF |
| F114 | P3 developer truth / immediate cleanup | **VERIFIED CURRENT SOURCE + EXISTING TEST CONTRACT; no live call or runtime change.** Two Calendar review comments said a tokened client keeps `Tweaks Needed` cards in the Review queue and can add more notes. The adjacent predicate returns that state only for the internal non-client preview, and `review-collab-gate.js` asserts a real client link hides it. The comments therefore contradicted shipped behavior and could send a maintainer toward the wrong regression. | **Corrected in this PR:** describe the exact `Client Approval`-only tokened-client queue and reserve `Tweaks Needed` retention for the internal preview. No executable behavior changed. | DONE — STALE REVIEW-QUEUE COMMENTS RECONCILED |
| F115 | P0 current sales-funnel integrity and client communications | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG + CURRENT PROVIDER DOCUMENTATION; neither callback was invoked and no event body, identity, credential, contact, or client record was read.** The active invoice-paid callback uses an unauthenticated webhook and does not verify the provider signature/raw body, timestamp, event ID, exact event type, paid/live/account state, or a server-owned payment-to-sale correlation; it locates a contact from caller-supplied event fields. The active contract-signed callback is also unauthenticated at the webhook. Its only gate compares caller-controlled body status and a static body token; it does not verify the provider's native raw-body HMAC or correlate a provider agreement ID/metadata to the agreement created by Sales Intake. Both callbacks acknowledge on receipt, so invalid events and downstream failures can still receive HTTP success and suppress provider retries. Either route can falsely advance the two HubSpot gate flags and the onboarding-email chain. | Sales automation + security/ops owners contain both callbacks now. Verify the exact provider-native signature over the unmodified body, bounded timestamp and replay-safe unique event ID; allowlist exact event type/status, live/test mode and account; resolve a server-owned expected payment/agreement correlation before any state change; and persist a unique durable provider inbox before returning 2xx. Coordinate any static-token removal/rotation with the provider and every proved consumer—never rotate blindly. TEST valid, malformed, stale, replayed, wrong-account/mode/type/status, wrong-agreement/payment, downstream-failure, retry, and key-rotation cases without a real client event. | OPEN — BOTH SALES GATE CALLBACKS TRUST UNVERIFIED CALLER EVENTS |
| F116 | P1 current onboarding-email completeness and duplication risk | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG; no callback was invoked and no event body, identity, credential, contact, or client record was read.** Each callback reads the HubSpot contact, writes only its own gate flag, then decides whether both gates are true from the earlier snapshot. If contract and invoice arrive together, both can observe the other flag as false, both stop, and the final contact can hold both true with no onboarding email. Conversely, duplicate/retried events can independently pass the stale `onboarding_sent` check and dispatch the email child more than once. Child execution is asynchronous and there is no durable gate job, uniqueness claim, completion receipt, configured error workflow, or reconciler; email success, the `onboarding_sent` write, and deal-stage movement are separate failure boundaries. | Replace the mirrored read/patch/check graphs with one atomic, idempotent sales-gate transition keyed by the server-correlated sale and unique provider events. Claim one durable onboarding-email job with a uniqueness constraint/CAS, explicit pending/sent/failed states and step receipts; make retries resume it; add a reconciler for “both gates true, not sent” and stuck partial work. TEST a barrier-synchronized two-callback race, duplicates in both orders, lost response, child/email/HubSpot/stage failure at every boundary, retry, and recovery alarm. | OPEN — CONCURRENT VALID GATES CAN LOSE THE ONBOARDING EMAIL; RETRIES CAN DUPLICATE IT |
| F117 | P0 current legacy client-link confidentiality and cross-client integrity | **VERIFIED-LIVE PUBLIC STATIC SOURCE + CURRENT SOURCE + FOCUSED OFFLINE ROUTE SIMULATION; no real token, client row, identity, or live write was used.** A legacy `?c=…&v=samples&t=…` link first verifies its exact client/token, stores only a legacy-module client variable, then routes `samples` through the retirement resolver to generic Samples New. Generic SXR ignores that verified binding and instead loads browser pins/preferences; a fresh device gets an unbound shell, while a device with residual state can load another client and exposes **Add client**. Client-link mode remains true, so the approval/comment/tweak controls operate on whichever client generic SXR selected without re-verifying that client. Current SXR writes also inherit F35/F67's unauthenticated server routes. Phase-1 docs and tests asserted the legacy client portal remained intact but exercised only staff hashes, not a tokened link. | Fail closed immediately: either sunset the legacy URL with an explicit invalid/expired-link page, or preserve a token-bound old portal until data and approval parity are complete. If redirecting to SXR, carry a server-verified exact-client capability into `mountSxrClientView`, discard pins/preferences and all client-switch controls, and require every read/write to enforce that same client server-side. Test fresh and residual browsers, every old deep link, invalid/missing/rotated token, cross-client pins/Add-client attempts, old-data versus SXR parity, refresh/back/cache, mobile, and second device. | OPEN P0 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F118 | P1 current public privacy/licensing boundary | **VERIFIED CURRENT TRACKED TREE + PUBLIC STATIC HEAD REQUESTS + CURRENT FEATURE DOCS; no client source system or private file was opened.** The onboarding feature tracks 43 media assets (~23.4 MB). Its own implementation record identifies at least 33 as derivatives of client frames, source folders, or Drive originals, while nine audio clips have no recorded provenance. Representative tracked video/image assets are served publicly by GitHub Pages. The repository contains no consent, release, licence, intended-audience, retention, or provenance manifest proving that these materials may be republished indefinitely as public product examples. This does not assert infringement; it means publication rights and deletion obligations are presently unproved. | Privacy/legal + product owner inventory every onboarding asset and its source, people/voice/brand rights, licence, permitted audience, retention and deletion duty. Replace uncertain media with fictional, commissioned, or explicitly licensed examples; remove disallowed binaries from Pages and coordinate F64's Git-history/cache/fork assessment rather than assuming a tree deletion retracts copies. Add a public-safe provenance manifest and CI that rejects unclassified onboarding media. **Owner question:** which existing assets have written permission for permanent public product use? | OPEN — PUBLIC CLIENT-DERIVED ONBOARDING MEDIA HAS NO PROVED RIGHTS/PROVENANCE BOUNDARY |
| F119 | P1 before any TikTok Pilot review or post | **VERIFIED CURRENT SOURCE + CURRENT FEATURE DOC + OFFICIAL PROVIDER GUIDANCE; no OAuth flow, account, upload, or post was invoked.** The audit pack promises a privacy selector with no preselected value, but current source automatically assigns `SELF_ONLY` when the app is unaudited and disables the selector. The compose surface also lacks the required pre-submit Music Usage Confirmation declaration. Current provider guidance requires the creator to choose privacy manually with no default and requires the applicable music-use declaration before posting. The same guidance restricts internal/team-managed upload utilities; this repository describes agency staff posting on connected client accounts, so product/legal must establish whether the intended use is eligible rather than presenting the pack as submission-ready. | Keep the pilot disabled for review/posting. Render only provider-returned allowed privacy values with an unselected required choice; add the exact conditional music-use declaration and commercial-content controls; preserve creator-info restrictions; and test every allowed/disabled combination, reload/draft, keyboard/mobile, and server rejection. Product/legal records a provider-backed eligibility decision for the agency-operated use case before review. Update the audit/demo pack only after source and end-to-end sandbox evidence agree. | OPEN — PILOT UX CONTRADICTS CURRENT PROVIDER RULES AND ELIGIBILITY IS UNDECIDED |
| F120 | P1 operator recovery and security guidance | **VERIFIED CURRENT SOURCE + DEPLOYED PUBLIC STATIC SOURCE + CURRENT FEATURE DOCS; no endpoint or workflow was changed.** When the Kasper onboarding inbox cannot load, the UI tells the operator to activate a list webhook in n8n. The live loader actually calls three Supabase Edge readers, and the old n8n readers are historical service-role routes whose activation would not repair the current request and could reopen an obsolete public data path. The feature doc also placed the inbox under Templates and ended with instructions to activate the old readers. | **Corrected in this PR:** the UI now identifies the current Edge-reader failure and tells the operator to preserve the access boundary and escalate; the feature contract identifies the Kasper subtab/current readers and removes legacy activation/finish instructions. Truth-sync rejects the stale activation phrase. Runtime read authorization and least-field response remain blocked by F77/F85. | DONE — OBSOLETE N8N REACTIVATION GUIDANCE REMOVED |
| F121 | P2 current Kasper mobile usability / onboarding day-one access | **CORRECTED IN CANDIDATE SOURCE + FOCUSED FICTIONAL-DATA BROWSER SIMULATION; no live row, identity, credential, backend call, or write was used.** Review/Samples/Messages/Filming now remain in a stable priority row while Team and Pipeline/Admin destinations live in an accessible More menu. The selected overflow destination replaces the generic More label. Focused browser proof found zero page/nav/button overflow at desktop, 768 px and 390 px with `Client Credentials` active, plus 390 px at 200% text scaling; both dark and light themes remained legible. Mouse, Enter/Space, arrows, Home/End, Escape, outside dismissal and listener teardown are covered. Capability-hidden items stay out of focus traversal, and denied sensitive routes now atomically select Review and canonicalize memory, `syncview_kasper_subtab_v1`, and the hash. Subtab changes still use `replaceState`, so Back does not traverse them. | Owner decides whether the existing replace-state history policy should remain. Before closing the finding, repeat the candidate proof on the hosted build with real touch/pinch, direct denied deep links, reload/back, and a second device; keep the navigation change unmerged until the normal review gate passes. | PARTIAL — OVERFLOW, ACCESSIBLE REACHABILITY, ACTIVE LOCATION, AND DENIED-STATE RECURRENCE CORRECTED; HISTORY POLICY/HOSTED PROOF OPEN |
| F122 | P0 current public privacy incident | **ARTIFACT SUBLANE CONTAINED; LOG/ARGOS REVIEW OPEN.** Exactly 414 retained target artifacts were deleted—274 B1 plans, 55 Production screenshots, 54 review packets and 31 Argos bundles—and anonymous GitHub API enumeration returned zero for all four names; 31 unrelated artifacts were untouched. After #836 merged, B1 and Production Actions were deliberately re-enabled 2026-07-15. The first B1 artifact validated as one exact success-only, one-day, aggregate-only JSON file with zero row/person/email/phone keys; Production creates no Actions artifact, Argos delivery, or live-derived generated summary, and detailed output remains runner-local. Current repository configuration has no Argos token/project variable, but historical external Argos retention is not proved. Calendar/Samples reconciler public identifier/roster logs remain open. | Preserve the no-public-visual/no-live-derived-summary boundary and never restore deleted bundles. Canary-scan public stdout/summaries, sanitize reconciler output, inspect/delete historical public run logs where feasible, and have the owner audit/delete any external Argos builds. Preserve private incident evidence and record legal disposition. | PARTIAL — B1 + Production Actions RE-ENABLED 2026-07-15; B1 artifact validated aggregate-only (0 row/person/email/phone keys), Production emits no artifact/Argos. Reconciler public logs + historical Argos review still open. |
| F123 | P0 current operational-data integrity and authorization | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG; neither webhook was invoked and no Sheet row, identity, credential, or payload was read.** Project Central's active GET and POST webhooks authenticate no caller. Its three Sheet reads independently continue on error, and the join converts a failed source into a structurally valid partial hierarchy. Save clears Projects, Areas, and Items in sequence before validating or staging any replacement, then appends; an empty request, post-clear failure, stale client that loaded a partial tree, lost response, or concurrent save can erase or partially rebuild the live hierarchy while the workflow still lacks revision/CAS, idempotency, transaction/staging, immutable backup, or restore receipt. No repository test covers the route. | Contain the route; require an active role/scope and immutable audit before read/save. Validate complete input, relationships and expected source counts/hashes before mutation. Replace clear-live-first with a database transaction or copy-on-write staged sheets plus validation and one atomic pointer/rename; add version/CAS, request idempotency, immutable backup and proved restore receipt. TEST each source failure/error-item, empty/malformed tree, every clear/append failure, stale/concurrent saves, lost response/retry and auth negatives; the active version must not change until one complete staged commit passes. | OPEN — UNAUTHENTICATED PARTIAL SAVE CAN CLEAR THE PROJECT-CENTRAL HIERARCHY |
| F124 | P1 current client-facing analytics integrity | **2026-07-20 LIVE UPDATE — CLIENTS METRICS FIXED AND FIRST-RUN PROVED.** Workflow `Q4n1bagJYBkurEaI` is active at `b92fb693-1dd4-4ce2-a60e-98a1701c369d`; scheduled execution `287059` consumed all 29 roster clients, emitted 29 unique typed terminal receipts, completed 29 Metrics writes, and passed `Validate Roster Coverage` with zero write failures. One provider failure exactly preserved its last-good row, whose affected values were already legitimate zeros; two successful platform results persisted fresh numeric zero fields with `used_last_good=false`, proving that zero alone does not trigger fallback. No `genuinely_empty` receipt occurred live; pinned pre-publish execution `286168` covers that branch. The 31m12s run completed without a Sheets quota error, and `fb180e5f-79ee-4d49-9dec-70999b422b71` remains the direct rollback. **Remaining TOP VIDEOS finding:** provider errors still enter the same valid-looking empty/partial path as missing or valid-empty sources; four retained green runs sent only 8–11 of 15 configured YouTube lanes through processed stats while 4–7 collapsed into the undifferentiated “No YouTube” branch. The SPA still cannot distinguish a TopVideos outage from valid no-content. | CLIENTS METRICS implementation is complete; retain quota monitoring and the named rollback. For TOP VIDEOS, persist typed per-client/platform completeness receipts, separate provider failure from valid empty, preserve prior trusted content while degraded, and expose source age/coverage in the browser. TEST provider failure, valid empty, partial platform coverage, retry/duplicate behavior, stale-cache revalidation, and recovery before closing the remaining half. | PARTIAL — CLIENTS METRICS LIVE-PROVED; TOP VIDEOS RECEIPT/DEGRADED PATH REMAINS OPEN |
| F125 | P1 current Calendar recovery/data integrity | **VERIFIED DEPLOYED PUBLIC STATIC SOURCE + LIVE IDENTIFIER-FREE FLAG/WORKFLOW METADATA + CURRENT SOURCE/TEST CENSUS; no live write, execution payload, identity, slug, or secret was read.** `?v2=0` persistently changes Calendar reads to legacy Sheet-backed `calendar-get`; a normal Supabase REST failure automatically falls back to the same Sheet reader. Neither path changes write authority: routing independently honors the full-roster `calendar_upsert_ef_clients` allowlist and sends upserts/reorders only to Supabase Edge Functions. A fallback/v1 browser's new card or reorder can vanish when its optimistic/recent-save window expires or immediately on another device. Existing-card v1 saves send stale whole-card Sheet state; the Edge scalar-conflict list omits ordering, several approval/title/Kasper/history fields and thumbnail revision, so those fields can overwrite newer canonical state without rejection. Local cache can mask the split. One unattributed retained `calendar-get` execution and no central mode/caller telemetry cannot prove zero use; tests reference none of the sticky switch or cross-product consumers. This is distinct from F104 retirement, F67 auth fallback, and F36 general CAS. | **Docs corrected in this PR:** withdraw `?v2=0` as writable rollback and stop treating automatic Sheet fallback as write-safe. Until one coupled recovery exists, sticky-off or REST-fallback + EF-enrolled sessions are explicitly read-only with a warning. Product/ops defines one atomic read/write authority; a lagging recovery reader either disables writes or durably journals/reconciles them. Add server-side version/CAS for every mutable whole-card field and privacy-safe mode/surface/fallback/build telemetry. TEST v2 on/off × enrolled/unenrolled/flag failure × REST/fallback × edit/create/archive/reorder/import × same/second device/cache expiry/two tabs; choose an observation window covering infrequent devices before F104 retirement. | OPEN — CALENDAR FALLBACK READS SHEET WHILE SUCCESSFUL WRITES LAND ONLY IN SUPABASE |
| F126 | P1 current Calendar/link/status integrity | **VERIFIED-LIVE SANITIZED ACTIVE-WORKFLOW CONFIG + CURRENT SPA SOURCE + RETAINED EXECUTION METADATA + OFFICIAL PAGINATION CONTRACT; no route was invoked and no payload, identity, client row, link, or credential was read.** The active Linear Sub-Issues reader requests `children` without a cursor/page size, ignores GraphQL errors, converts missing/partial children to an empty array, and returns `ok:true`. The current SPA treats that result as complete when importing Calendar cards, bulk-overwriting video/graphics links, and deciding parent-versus-leaf before persisting status adoption. Linear's default first page is 50, so child 51+ is invisible; unequal partial cohorts can mispair links by order; and a partial response can make a parent look like a leaf. All 37 retained runs since Jul 11 are labelled success, which this contract cannot use as completeness evidence. A retained `/add-to-calendar` branch repeats the defect for both children and per-child comments, drops children whose required link is outside the returned comment page, appends only survivors to the legacy Sheet, and acknowledges before a completeness receipt; its current caller is unknown. | Page every connection to `hasNextPage=false`; reject GraphQL errors, missing page info, repeated/missing cursor, cap/count drift, and any incomplete nested source. Return an explicit requested-parent/pages/unique-child/per-source receipt; `ok:true` is forbidden unless `complete:true`, and only a complete zero count proves a leaf. Calendar must preserve drafts/old links/status and perform zero mutation while degraded. Either retire `/add-to-calendar` after a privacy-safe zero-caller proof or make it an idempotent durable job that acknowledges verified final writes. TEST 50/51/100/101 children, relevant child/link on later pages, page failure, partial envelope, zero-child leaf, duplicate/repeated cursor, unequal media cohorts, 51+ comments, retry and zero-mutation-until-complete. | OPEN — FIRST-PAGE SUB-ISSUES/COMMENTS ARE ACCEPTED AS COMPLETE AND DRIVE WRITES |
| F127 | P1 before auth enforcement, either human flip, or stale-caller retirement | **VERIFIED-LIVE PUBLIC STATIC SOURCE/HEADERS + CURRENT TEST CONTRACT + FOCUSED FICTIONAL OFFLINE BROWSER; no real token, identity, row, or write was used.** The app-update nudge cannot prove old code expired. Direct Production (`?prod=1`) returns before installing any check. Clean onboarding aliases request their own rewritten path, whose public `HEAD` is 404, so they never compare a build. Root/index responses may be cached for ten minutes; the running page has no embedded build ID, and its first no-store `HEAD` is adopted as the baseline. A cached v1 document whose first check sees v2 therefore shows no banner. Dismissal likewise adopts v2 while v1 keeps running. There is no `pageshow`/online recheck, executable minimum-build or authority epoch, build/epoch on protected reads/writes, server `upgrade_required` rejection, or build-population telemetry. The tracked test affirmatively requires the Production skip, first-network-response baseline and dismiss semantics, and cites a nontracked scratch behavior test. A backend-only function change also cannot move the Pages ETag. Old Production/editor/designer, SMM, and client callers can therefore remain indistinguishable through a deploy, revert, auth change, or cutover; queued writes and session verdicts are not bound to the code/authority epoch. | Embed an immutable running build plus auth/authority/cache epoch in the served document; compare it to a fixed same-origin manifest on root/index, direct/in-app Production, every onboarding alias, `pageshow`, focus and offline→online. Send build/epoch on every protected read/write and reject below-minimum callers **before mutation** with `upgrade_required`. Classify releases as optional or mandatory: mandatory updates cannot be dismissed, block writes, preserve/checkpoint drafts and queues, then reload and reverify auth/token; supplement F61's server freeze rather than replacing it. Aggregate privacy-safe build/surface/epoch telemetry. TEST cached-v1-first-check, deploy during baseline, dismiss, BFCache, offline return, backend-only deploy, forward/revert, second device, session-verdict rotation, dirty drafts and queued work. **Owner questions:** what is the maximum optional stale window, and which release classes are mandatory? | OPEN — UPDATE BANNER CANNOT EXPIRE OR REJECT STALE CALLERS |
| F128 | P0 current provider/CRM integrity and staff-attention abuse | **VERIFIED-LIVE SANITIZED ACTIVE STANDARD/AI SUBMIT + SHARED PROVISIONING GRAPHS, CURRENT SPA/SCHEMA/DOCS; no route was invoked and no execution body, row, identity, credential, or provider object was read.** Both public primary onboarding webhooks accept a caller-created submission ID, slug, name, email and answers without invitation, authenticated principal, expected-sale correlation, rate/size/schema boundary, or staff approval. After only a nonempty-email gate and intake insert, an unawaited shared workflow can create a real Drive folder, upsert a CRM contact and advance a linked deal, create a workspace-public creative Slack channel/invite staff/post the brief, and invoke credential-vault import. Duplicate ID success bypasses provisioning rather than making its side effects idempotent; no provider sandbox, durable job/step ledger, captured inverse, or teardown workflow exists. A fictional “TEST client” would still mutate real providers and cannot be safely cleaned up, so the requested live drill was correctly not run. | Separate public capture from privileged provisioning now. Provision only a server-owned durable job after a short-lived invitation/capability bound to a verified, server-correlated sale **and/or** individually authenticated staff approval under an owner-ratified policy. Server-derive canonical client identity; apply strict limits/rate/replay controls; make every provider step idempotent, read-back verified and reversibly identified; add provider sandboxes/intercepts plus immutable audit. TEST anonymous, replay, wrong-sale/client, forged identity, duplicate/lost response, every provider failure and exact teardown without touching real clients. **Owner question:** is onboarding invitation-only, or public capture followed by explicit staff approval? | OPEN — PUBLIC FORM CAN LAUNCH PRIVILEGED REAL-PROVIDER PROVISIONING |
| F129 | P0 current client credential confidentiality | **VERIFIED-LIVE SANITIZED ACTIVE PROVISIONING GRAPH + CURRENT FORM/DOCS; no message, credential value, execution body, client row, or provider object was read.** The form tells clients that typed account access is “Stored securely,” and the lifecycle map says the full Slack brief excludes credentials because they live only in Supabase. The active provisioning builder instead includes raw account-access answers—including the backup/recovery-code field—in the full brief posted to a workspace-public creative channel; if channel creation fails, the same brief is sent by direct message. The live workflow description repeats the false exclusion claim. Historical occurrence/count is unknown because no message or execution payload was inspected, but every submission containing those fields is mechanically eligible for this disclosure path. | Stop secret-class data from every message/log immediately: use a server-side allowlist projection that cannot serialize the account-access object and emit only a vault receipt/count/status. Add structural secret-field/canary tests across channel success, fallback DM, logs, alerts, retries and future fields; keep access values only in the protected vault. Privately inventory affected channel/DM history and access logs, delete/contain copies, notify and rotate/revoke credentials or recovery codes as incident review requires, and correct UI/workflow/docs claims. Never copy values into audit evidence. | OPEN — RAW CLIENT LOGINS/RECOVERY CODES ENTER SLACK DESPITE “STORED SECURELY” CLAIM |
| F130 | P2 current Kasper review/message recovery | **VERIFIED CURRENT SOURCE + CONTROLLED FICTIONAL OFFLINE BROWSER FAILURE; no backend request, live data, identity, credential, or write was used.** Cold Messages and Review share the review loader. Messages paints “Checking for messages,” but on rejection the catch writes only to `#kasperReviewBody`, which is absent on Messages, and never rerenders that tab; the loading flag clears invisibly, leaving the skeleton indefinitely with no error or retry. Review does show an error, but its loader searches for a refresh control that the renderer never creates, and the error HTML has no retry. The controlled 390 px failure reproduced an unchanged Messages loading skeleton and zero retry controls on both tabs. Existing QA opens only Review/Samples and does not cover this failure. **CURRENT SOURCE-ONLY FOLLOW-UP at `07d123d`; no browser/backend request or live queue data was used.** The shared Review/Messages loader also lacks a completeness contract. A per-client payload with `ok!==true` becomes empty queue/history/replies, rejected client settlements are omitted, and the aggregate is returned as successful even when some or all clients failed (`_kasperFetchAllRelevantPosts()`, `index.html:52317-52524`). `_kasperLoadReview()` then adopts that partial/empty aggregate as the queue (`:52131-52269`). Its SMM-map rejection becomes an empty map and a held SMM read can block the shared `Promise.all` indefinitely (`:52272-52296`). This extends, rather than duplicates, F130's cold/cached/retry contract. | Use one active-tab-aware load state and error renderer. Preserve any cached content under an explicit stale/error banner; cold failure shows a diagnosable empty state; both Review and Messages expose visible keyboard/touch retry that recovers without reload. TEST cold failure, cached refresh failure, retry success/repeat failure, tab switch during the request, abort/race, mobile and keyboard. Require expected/attempted/successful/failed client receipts and keep complete-empty, partial, failed, and stale distinct. A nonessential SMM lookup may degrade card attribution but must not block core queue settlement. Preserve last-good queue data with a visible completeness warning, add targeted Retry, and cover Review and Messages actual boot/refresh sequences under F162. | OPEN — MESSAGES CAN LOOK PERMANENTLY LOADING; REVIEW HAS NO RETRY |
| F131 | P1 B1 incremental integrity / pre-flip monitoring | **VERIFIED-LIVE SANITIZED PAGER GRAPH + RETAINED RUN METADATA; VERIFIED CURRENT SOURCE; PROVED-OFFLINE WITH FICTIONAL INPUTS. No production payload, client row, identity, credential, or write was used.** `scripts/b1-linear-backfill.js` chooses the newest `linear_incremental_refresh` event as the next cursor, but the same action labels per-deliverable writes, success summaries, and failure summaries. A failed run can write a fresh `ok:false` finish time; the next run then starts from that failure time minus five minutes and can omit older fetched-but-unwritten issues. If the failure summary itself is lost, a per-row event can become the cursor. The live pager selects the same ambiguous action and checks age only, so a failed/partial/malformed fresh event can look healthy. The newest 100 B1 Actions runs were green, so this is a deterministic latent loss path, not a claimed incident. | Give per-row events and terminal summaries distinct types. Store server-owned run ID, query high-water, expected/attempted/written counts, terminal status, and a durable last-success checkpoint; advance only after complete write/readback success and reuse the prior checkpoint after any failure. Pager requires the exact terminal type, `ok === true`, and matching counts. TEST nth-step failure, overlap-window overrun, lost summary, retry/idempotency, and convergence. **Owner question:** will B1 adoption/team flips stay blocked until the checkpoint is success-only? Recommendation: yes. | OPEN — FAILURE OR PER-ROW EVENTS CAN ADVANCE THE CURSOR AND LOOK HEALTHY |
| F132 | P1 monitoring / go-live containment | **VERIFIED-LIVE SANITIZED ACTIVE GRAPH/SETTINGS + RETAINED EXECUTION METADATA; VERIFIED CURRENT WORKFLOW CONFIG; PROVED-OFFLINE WITH FICTIONAL MATRICES; failure consequences partly INFERRED from documented n8n semantics. No production failure was induced.** The active 15-node pager combines five dispatches, six health reads, conditions and Slack in one v1 execution. Only outbound dispatch continues on error; the other lanes have default stop-on-error, no retries and no error workflow, with Samples last. An earlier failure can suppress later dispatches/reads/Slack while already accepted work remains live; static throttle state saves only after whole-run success. Retained metadata has 13 errors/370 executions, including seven consecutive scheduled errors. Calendar/Samples inspect only five unfiltered runs and suppress failure/no-completed checks while any fresh queued/running item exists. Outbound trusts the prior summary's embedded mode, missing/malformed becomes off; fresh malformed V2 becomes zero; first nonzero diff/repair/linkage is quiet and all three share one two-summary rule/hourly throttle, contradicting immediate repair/linkage docs. | Split dispatchers from observation and add an observer outside n8n. Require per-dispatch correlation and terminal Action receipts; inspect exact terminal branches, runtime authority independently, and page missing/failed/malformed/future/over-age/queue-depth/mode mismatch. Isolate lanes; add retry/backoff, idempotency and an error workflow. Give diff/repair/linkage separate state/throttle keys and keep repair/linkage immediate unless owner explicitly chooses/tests another policy. TEST every node's 429/500/timeout, accepted-never-started, pending-plus-failed, queue depth, manual runs, Slack partial delivery, last-branch failure, quota exhaustion, runtime flip, malformed time and duplicate correlation. **Owner question:** retain immediate repair/linkage alerts? Recommendation: yes. | OPEN — PAGER CAN BE QUIET OR PARTIAL WITHOUT TERMINAL HEALTH |
| F133 | P1 before real native-intake enrollment | **VERIFIED ON THE CARRIED #850 SOURCE LINE + FICTIONAL OFFLINE BROWSER; no live request, identity, row, token, or write was used.** Create Post offers latest/new batch but no canonical title. Intake creates generic `Video N` deliverable titles, materializes that name to the card, then focuses the Calendar title input. Editing it updates only `calendar_posts.name`; `calendar-upsert` does not update `deliverables`, and `production-write` lacks the spec-required title operation. An SMM can therefore type the real post name in the focused field while Production/creative queue/mirror retain `Video 1`, violating §9.4's `deliverable.title == card.name`. This begins before review and is distinct from F109's post-approval edit. | Collect/validate the canonical title before commit and transactionally create deliverables/card with it. Add authenticated CAS/idempotent title mutation that atomically updates both stores, records `title_change`, and mirrors asynchronously; generic names are explicit visible placeholders only. TEST latest/new/single/multiple, whitespace/no-op/duplicates, pre/post-review title, lost response/retry, two tabs, reload/offline and exact equality on Calendar/Samples/Production/mirror. | OPEN — NATIVE CREATE POST IMMEDIATELY SPLITS CANONICAL TITLE |
| F134 | P1 before real native-intake enrollment and staff offboarding | **VERIFIED ON THE CARRIED #850 SOURCE LINE + CONTROLLED FICTIONAL OFFLINE HARNESS; no live call, identity, row, token, or write was used.** Native intake commits deliverables server-side, but card materialization/recovery exists only in browser `localStorage`. Partial completion checkpoints card IDs; sign-out makes the committed incomplete job `recovery_only`/`suspended` and actor-bound. A second SMM cannot resume (`native_intake_actor_mismatch`) and any unrelated signature is rejected (`native_intake_pending_conflict`). The harness retained the suspended job, rejected the second actor and new intake, and found no user/admin release, reassignment, recovery inbox, or server reconciler. Clearing storage deletes the only recovery knowledge while committed deliverables remain orphaned from cards. | Move post-commit materialization to a durable server-side idempotent job/reconciler keyed by request/card IDs. Browser observes/requests retry but is not recovery authority. Add protected recovery inbox, audited admin reassignment, immutable original actor and no global block on unrelated intake. TEST failure after each item/card, sign-out, actor switch/offboarding/key rotation, storage clear/private mode, second device, concurrent new intake, exact-once materialization, no orphan and lost response. | OPEN — ACTOR-BOUND BROWSER RECOVERY CAN WEDGE A SHARED DEVICE AND STRAND NATIVE WORK |
| F135 | P2 current Calendar/Samples mobile/accessibility | **VERIFIED CURRENT SOURCE + CONTROLLED FICTIONAL 390 PX TOUCH BROWSER.** Calendar and Samples expose a non-interactive span grip and implement reorder only through HTML5 `dragstart`/`dragover`/`dragend`/`drop`. There are no touch/pointer handlers, keyboard reorder, movement buttons, position picker or equivalent accessibility semantics. The touch simulation produced zero drag/drop events and unchanged order; existing reorder tests contain no touch/keyboard/accessibility lane. | Add explicit accessible Move left/right or position controls plus optional touch drag with scroll arbitration and keyboard behavior; every method uses the same serialized/CAS reorder. TEST physical iOS Safari/Android Chrome at 390/768, portrait/landscape, horizontal scroll versus drag, keyboard/screen reader, filtered cards, concurrent reorder, offline/retry and second device. | OPEN — CALENDAR/SAMPLES ORDER CANNOT RELIABLY CHANGE BY TOUCH OR KEYBOARD |
| F136 | P1 before first creative-team authority flip | **VERIFIED-LIVE DEPLOYED STATIC SOURCE + VERIFIED-OFFLINE PURE-POLICY MATRIX; flipped failure INFERRED WITHOUT A LIVE WRITE. No real row, identity, token, flag, or payload was used.** Production exposes eight creative next statuses regardless of current status. The server policy receives role, operation, member team, target team and next status only—not current status or assignee. A deterministic matrix accepted each exposed next status from reviewer/terminal states (`kasper_approval`, `client_approval`, `approved`, `scheduled`, `posted`). After flip, a creative can open same-team work through All/direct link and regress it to To Do/Tweak, cancel it or mark duplicate. CAS stops stale concurrency, not an illegal transition. This is distinct from F37 identity/ownership, F50 projection and F113 revision semantics. | Production backend/security + workflow owner ratify/enforce one server-side role × current × next × team × assignee state machine; picker consumes the same policy. Reviewer/terminal regression, cancel and duplicate require separately authorized/audited override unless explicitly approved. TEST full 13×13 matrix across every role/team, assigned/unassigned/peer work, list/direct link, stale CAS, retry and second device. **Owner question:** exactly which transitions and peer-work actions may creatives perform? | OPEN — CREATIVE AUTHORIZATION DOES NOT VALIDATE CURRENT STATE |
| F137 | P1 before Video authority flip | **VERIFIED-LIVE DEPLOYED STATIC SOURCE + CONTROLLED FICTIONAL OFFLINE BROWSER; real-user impact INFERRED WITHOUT LIVE DATA/WRITE. No real row, asset URL, identity, credential, or payload was used.** Production loads four distinct Video resources—deliverable `file_url` plus batch delivery, footage and filming-plan URLs—then collapses them to `file_url || delivery || footage || filming`. Detail shows only that priority winner under the fixed label “Delivered file”; batch detail exposes none. A fixture with all three batch links displayed only delivery. If delivery is absent, raw footage or filming plan is mislabeled. An editor can therefore lose the plan/source needed to start/revise work as Linear disappears. | Preserve/render a labelled Assets panel for Filming plan, Raw footage, Delivery/Frame folder and deliverable file, with explicit missing/invalid/expired/permission-denied states. Validate access/type without substituting one field for another. TEST all 16 presence combinations, failures, native intake/backfill, refresh/reassignment, desktop/mobile and second device. **Owner question:** must all four remain visible, and which is editable canonical delivery versus read-only source? | OPEN — VIDEO SOURCE/DELIVERY ASSETS COLLAPSE INTO ONE MISLABELED LINK |
| F138 | P2 current / P1 before Linear history or Inbox retirement | **VERIFIED-LIVE DEPLOYED STATIC SOURCE + CONTROLLED FICTIONAL OFFLINE BROWSER + SOURCE/DOC/TEST CENSUS; post-Linear impact INFERRED WITHOUT LIVE EVENT DATA/WRITE. No real identity, row, event body, credential, or payload was read.** The gateway records `deliverable_events` with actor/role/action/time/from/to. At that audit point, the SPA defined `_prodLoadEventsFor()` and `_prodActivity()` without a runtime caller; detail rendered Comments only, and a fictional event was never requested or shown. Editors therefore could not see who changed assignment/due/status, when or from what after Linear history disappears. `WIRED-PARITY.md` previously claimed Activity rows and a “No activity yet.” state; that documentation was corrected while the runtime gap remained. **CURRENT SOURCE-ONLY FOLLOW-UP at `07d123d`; no event row or live payload was read.** `_prodDetail()` now invokes `_prodLoadEventsFor(d.id)` (`index.html:41565-41570`), superseding the earlier statement that the loader had no runtime caller. The loader nevertheless pre-seeds the map with `[]`, converts read failure back to `[]`, and rerenders (`:40760-40770`); `_prodActivity()` remains defined without any render caller and would label an empty/error-collapsed array “No activity yet” (definition only at `:41641-41649`). Events are therefore requested but still neither visibly rendered nor given loading/error/confirmed-empty semantics; F138 remains the owner. | Add a protected role/team-scoped cursor-paginated event reader—never a new raw-anonymous path—and chronological Activity with stable event ID, actor, time, action and from→to, safely separate/merged with Comments. Include loading/empty/stale/retry/paging, second device and payload redaction; keep WIRED-PARITY aligned with runtime until proved. TEST every event type, order/page, none, failure/recovery, auth/team denial, mobile and comment coexistence. Do not pre-seed or catch to authoritative empty. Model event loading, complete-empty, stale, and failed states separately, visibly render Activity, preserve last-good events under an error banner, expose Retry, and add Production-detail event failure/recovery to F162. **Owner questions:** required event types/retention, and is timeline a first-flip or B5 gate? | OPEN — EVENTS ARE REQUESTED BUT ACTIVITY REMAINS INVISIBLE AND FAILURE-COLLAPSED |
| F139 | P1 current Calendar/Samples staff workflow + n8n execution burn | **VERIFIED-LIVE READ-ONLY (n8n execution data + anon Supabase read + source + local regex repro; found by /site-assurance 2026-07-17).** One Approved calendar card (`p_mquxb0wk_emmed`) stores a UUID-form graphic Linear URL. The shared `linear-issue-statuses` webhook's loose ident extraction fabricates a garbage ident from inside the UUID (`A-94`), one bad id nulls the whole aliased GraphQL batch, and the node's ~50-way parallel per-id fallback then intermittently exceeds the n8n 60 s task-runner cap. Observed overnight 2026-07-17: five webhook timeout executions (`275895`–`276012`), the 01:30Z calendar reconcile Actions run red, and a per-pass heal burst of single-link executions (burn). Browser `_calIdentFromUrl` had the same loose match, so the banner-meta lane could poison batches too. | Repo side: strict `/issue/<ident>` extraction + batch hygiene + `test/linear-ident-uuid-guard.js` (draft PR `claude/linear-ident-uuid-sanitize`). Owner-gated: fix the n8n Code node's regex and bound its fallback (workflow `GP8CSZDNcy5sGdFr`, snapshot first), and correct or clear the stored UUID-form link on the card. | OPEN — REPO FIX STAGED; N8N NODE + STORED-LINK CLEANUP OWNER-GATED |
| F140 | P1 current Samples/Kasper staff workflow | **OWNER-RATIFIED REGRESSION; VERIFIED-CI + HERMITIC RACE PROOF (2026-07-17).** Samples must match Calendar: Kasper may stack component change requests, then Finish hands the card to the SMM. Historical exact-path evidence is narrower than the original four-night summary: `graphic__smm_approve__kasper_request__finish` failed 6/8 with `kasper.finish() → disabled` in runs `29240029813` (2026-07-13, `a4fff1a`) and `29483415231` (2026-07-16, `6f2e0d4`), while run `29317692645` had tree 24/24 green. Those heads predate #813/#850. On current post-#850 code, a separately proven cause existed: `_sxrKasperApplyAndPersist` passed a fresh `attempted:false` status companion through recovery reconciliation, so a newer native `Kasper Approval` clock could overwrite local `Tweaks Needed` before its first gateway send/source save; echo/reload then correctly left the strict Finish gate undecided. | **Fixed in `939bdfc`.** Reconcile only already-attempted recovery companions; a fresh companion now sends and persists `Tweaks Needed`. `_sxrKasperUndecidedComps` is unchanged. The new hermetic race proves one status send, source persistence, and an empty real Finish gate, while the attempted-recovery half still adopts provably newer native truth without a duplicate send. The owner ruling is pinned on the tree leaf. Full unfiltered branch run `29629528360` executed the five master lanes and passed the exact F140 leaf 8/8; the aggregate stayed red on independent pre-existing F141 reorder, TEST-row divergence, and shallow-checkout truth-sync signals. All ten follow-on nightly probes were also executed from `939bdfc`: the five F140-adjacent Kasper/gating/concurrency/cold-open/realtime probes were green; three independent stale probes (four assertions) remained red. Frozen writers are byte-identical to base `32099d5`. Cloud reviewer verifies before merge. | DONE — OWNER-RATIFIED FIX + BRANCH EVIDENCE COMPLETE; CLOUD REVIEW/MERGE PENDING |
| F141 | P1 current Samples reorder staff workflow (shared strip logic with Calendar) | **ROOT-CAUSED + REPO FIX (site-assurance run 2, 2026-07-19); VERIFIED BY SOURCE SYMMETRY + HERMETIC BEHAVIORAL PROOF + LIVE READ-ONLY AUTH PROBE.** `create_drag_reorder_persist` (which drives `#sxrStrip`, the Samples strip) failed 2026-07-16 with DOM and DB agreeing on the WRONG first card — reorder never persisted, no save-failure surfaced (cross-tier invariant 2). Classification (the register's question, answered): **it does NOT 401 in the signed-in flow** (keyless callers get `401 credentials_required` — live-proven; a signed-in SMM carries `X-Syncview-Key`), **the write FIRES and returns 200**, and it **reverts silently**. Root cause: `sample-review-reorder` EF returned `updated: parsed.items.length` (count of REQUESTED items), and `_sxrPersistReorder` only checked `resp.ok`/`json.ok` — so any item whose id did not match a live row (stale / mid-create / archived / raced-ahead-of-the-insert) was skipped by the `.eq(id)` update yet reported as success, and the unpersisted card snapped back on the next server-truth reload with no error. The **calendar-reorder EF+client were written correctly** (true match count + `updated < items.length` fail-closed, guarded by `a2-writer-edge-source.js`); samples diverged — the silent-loss window opened when F35 gated samples onto this EF (07-14), matching the green-13 → red-16 timeline. | **Fixed repo-side on `claude/site-assurance-run-2-r0ovr9`:** EF now returns the true matched-row count; `_sxrPersistReorder` fails closed on `updated < items.length` (EF route) → reverts + `showNotify`. Guards: symmetric assertions in `test/a2-writer-edge-source.js` + new hermetic `test/sxr-reorder-failclosed.js` (extracts the real function; proves revert-on-partial, persist-on-complete). 133/133 unit suites green. **Handoffs (cannot run in this sandbox — no staff key; Chromium egress TLS reset):** (1) redeploy the EF for the fix to take effect (F51: source ≠ artifact); (2) live TEST-client browser drill to confirm the exact nightly scenario is green on current code; (3) owner-gated: the dormant n8n fallback `Sample Review — Reorder` "Wrap Response" node also returns `updated: items.length` — mirror the true-count fix (snapshot first, rollback rule 2). | FIXED REPO-SIDE — EF REDEPLOY + LIVE DRILL + N8N MIRROR PENDING |

## F. Residual blind spots (completeness critic) — not yet closed

| # | Blind spot | Cheapest closure |
|---|---|---|
| B1 | **WRITE POLICY CLOSED / READ CONFIDENTIALITY REOPENED AS F88.** Live policies still prove direct browser-anon writes are default-denied, but “RLS enabled/SELECT-only” never proved safe reads. The exhaustive live census found 20 nonempty anon-selectable operational tables; client tokens/UI checks do not constrain direct PostgREST. Public service-role writers remain F35/F67. | Execute F88's explicit accept-or-remediate decision and generated direct-read matrix; do not use the old B1 closure as a read-security gate. |
| B2 | **CLOSED 2026-07-14 AS F44/F45/F69/F110/F128/F129:** the complete read-only desk-walk proved onboarding is capture plus unawaited provider side effects, not an atomic client lifecycle. It creates no canonical client, project mapping, review token, routing enrollment, assignment or teardown receipt; caller-derived identity can seed the vault. Worse, public submit can launch real provider/CRM work and Slack can receive raw account-access answers. A fake drill is not safely isolatable because no sandbox or complete inverse exists; not running it is a proved blocker, not an unknown. | Contain F128/F129; build F69/F110's server-owned, authenticated, resumable lifecycle with provider sandboxes and captured inverses. Only then onboard/remove a canonical kind=test client and prove exact mappings/token/routing/credential receipt/first writes/second-device denial/teardown. |
| B3 | Rocio's full day-one flipped-Graphics flow remains unwalked. Bounded creative/SMM source+fictional walks found additional blockers (F133–F138), but they are not the real Graphics authority simulation. Her personal queue is already proved wrong for almost every identity (F37); assignment, transition policy, assets, done-marking, tweak delivery, activity history, and silent Linear Inbox still need the complete walk. | Fix F37/F50/F53/F95/F133–F138 as applicable, then run the full graphics-role TEST walk before Phase 2. |
| B4 | ~~Deployed-EF version fingerprinting~~ **CLOSED 2026-07-14 and refreshed 2026-07-20:** the then-live/main function set, states, JWT settings, versions, independent source closures and server fingerprints were checked. F06's former `production-write` source drift is removed by pinned v24 deploy/fingerprint run `29601466479`; its TEST-contract question remains. Under F51, source-exact recovery redeploys the captured provider source/JWT posture and requires independent deployed-source/JWT hash equality; historical transitive provenance is unavailable and not required. | Close F06's TEST contract and retain complete source/JWT/release capture plus independent post-deploy fingerprinting as release gates. |
| B5 | ~~n8n burn source unidentified~~ **CLOSED 2026-07-13**: identified as redundant calendar-reconciler dispatch (3 triggers) + an idle every-minute TikTok cron — NOT a foreign writer or D-9. See F01. | — |
| B6 | ~~Pager happy-path last-mile unproven~~ **CLOSED 2026-07-13**: qll DM'd Sidney 8× during today's gap + a synthetic test DM delivered (exec 263676, 22:40Z). That sampled happy path works; F09/F132 keep the general terminal-delivery contract, lane isolation, and non-n8n backup open. | — |
| B7 | ~~Shared role keys + staff churn~~ **CLOSED 2026-07-13 as F31:** live roster/credential/auth semantics proved that row deactivation alone cannot revoke a retained group key or preserve trustworthy actor attribution. | Implement F31's immediate rotation/offboarding procedure and individual-session replacement. |
| B8 | **API-accessible reality CLOSED 2026-07-14 as F49:** live plan/health, disk/Storage, seven completed daily backups, retention/PITR/add-ons/restore entitlement, API/Realtime/function traffic, and current Production bootstrap size were measured. The stale Free/500 MB claims were confirmed and corrected in this PR. Exact billing-cycle egress, peak connections, spend cap, and post-#850 load remain remediation evidence—not unknown plan state. **The actual restore is now proven: F13's independent export + 229 s timed scratch restore are DONE and live (PR #840, 2026-07-15).** | Close F49's remaining Dashboard/load capacity record before any flip; F13's independent export + timed scratch restore are complete. |
| B9 | ~~Concurrent-writer collision unknown~~ **CLOSED 2026-07-13 as F36:** the first TEST two-writer drill proved both stale status writes succeed and last writer wins. The full mutation/409 UX matrix remains remediation scope, not an unknown. | Implement F36 and run the remaining status/content/reorder/comment/compound matrix. |
| B10 | **PARTIALLY CLOSED 2026-07-14 as F38/F127:** a positive client-link verdict survives verifier failure/revocation, and the shipped update nudge cannot expire old code: Production skips it, onboarding aliases probe 404, cached old pages can adopt the new header as baseline, and writes carry no build/epoch. Actual build population, staff/mobile/second-device residue, and failure UX remain unmeasured. | Close F38/F127, add privacy-safe build/session/epoch telemetry plus server minimum-version rejection, then run the phone/second-device/stale-build matrix. |

## Decisions taken from this audit (recorded in spec §14)

- **D-30 (owner, 2026-07-13):** KEEP the +2-day overdue auto-bump — port it into the native write
  path, flag-gated. Supersedes D-8's "drop it".
- **D-31 (owner intent retained; mechanism reopened by F33):** each SMM re-shares their own clients'
  links from the existing share action; no central resend. **Do not put bearer tokens in the public
  Sheet.** Distribution remains blocked pending an owner-approved, staff-authenticated exact-client
  link-builder endpoint wired to all four builders.
- **D-32 (owner, 2026-07-13):** #850's reroute shipped behind a per-client allowlist flag defaulting
  to TEST-only. The merge is dark; real clients enroll only in separately approved staged cohorts with parity armed. This
  replaces the "merge = everyone at once" model and satisfies D-28's soak intent with real traffic.
- **D-33 (owner, 2026-07-15):** No notification of the 21 people whose onboarding contact data was
  anonymously reachable.
- **D-34 (owner, 2026-07-15):** F64 public-history PII purge PARKED by owner; accepted residual
  documented in F64.
- **D-35 (owner, 2026-07-15):** F15 slack_user_id backfill deferred until a post-Linear notify EF
  exists.
- **D-36 (owner, 2026-07-15):** the PTO tracker (`pto_v1`, PR #843) MAY be enabled under the current
  shared-role-key identity model. The owner explicitly accepts the documented same-role
  impersonation/visibility risk for launch ("I don't care about making it safer — I just want this
  to work"). Individually revocable staff sessions remain a roadmap upgrade, not a launch gate.
  This supersedes the "must stay off until individual sessions" go-live blocker in
  `docs/features/PTO_TRACKER.md` §Go-live blocker for the launch decision specifically; the private
  HR-data handling rules there (no roster/values in the public repo or logs) remain binding.

## G. RESUME — owner-review checkpoint (2026-07-14)

**Status:** paused for owner review at F138. This is a durable handoff, **not** audit dryness and not
go-live approval. The private working ledger remains 0/3 dry rounds with a nonempty backlog. Project
Central follow-up is explicitly outside the continuation scope; existing F123 is preserved only as an
already-recorded finding. Continue only in `client-analytics` and its byte-identical lifecycle-doc
companion unless the owner changes scope.

### Complete finding state

- This register is the complete public-safe finding set, F01–F138. Every new row identifies whether
  evidence is verified live, verified source/config, proved with fictional/offline input, or inferred.
- The last checkpoint added F131–F138: unsafe B1 checkpoint/heartbeat; partial/false-quiet pager;
  native-title split; actor-bound browser recovery; touch/keyboard reorder; creative transition
  authorization; collapsed Video assets; and an unwired native Activity timeline.
- F01/F09/F10/F37/F43/F66/F81/F121/F124 were extended where the new evidence changed their scope.
  No runtime flag, production/Linear record, n8n graph, provider object, client metric row/value,
  credential, token or message body was written or copied during these rounds.

### Second-pass distillation (2026-07-15)

Adversarial re-verification of F01–F138: 55 CONFIRMED_REAL, 42 over-graded-but-real, 23
stale/already-fixed, 8 owner-decision, 5 duplicate, 4 needs-access, 1 refuted. 12 loud P0/P1 rows
were STALE — graded against the superseded #813 head `885026a`; the fix was present in `e3aa028`.
That implementation later landed through superseding PR #850 / merge `9968bd9`; #813 closed
unmerged. The 2026-07-19 reconciliation re-verified and closed the F04/F07/F08 implementation
defects specifically. Their separate live observations remain in `GO_LIVE_CHECKLIST.md`; every
other listed row retains its own register status and is not batch-closed by this lineage note.
F21's notice is subsequently superseded by the 2026-07-16 owner decision recorded in its register row.

### Ranked continuation backlog

1. **Current containment/owner decisions:** resolve the register's open P0/current-security and
   credential/privacy incidents first; record explicit owner answers in the relevant finding rows.
2. **Monitoring integrity (F01/F131/F132/F09/F66/F81):** on TEST/fictional inputs only, fail every B1
   write/summary boundary and every pager/relay dispatch/read/Slack boundary. Prove last-success
   checkpoint reuse, convergence, correlation, retry/dedupe, lane isolation, terminal delivery and
   an observer outside n8n. Retain the independent Samples schedule while the pager is coupled; if
   reducing burn, remove its pager dispatch first and prove the post-cut 24-hour rate (F01). Decide
   whether repair/linkage alerts remain immediate (recommendation: yes).
3. **Post-#850 cohort/recovery reassessment (F02/F23/F133/F134):** inspect current `main`; retain the
   dark per-client cohort and require atomic canonical title plus server-owned materialization/recovery
   before any real enrollment. Any future TEST failure drill is a separate owner-approved action with
   an exact rollback; this documentation reconciliation authorizes none.
4. **Creative day-one gates (F37/F50/F53/F95/F112/F135–F138):** owner-ratify the creative current/
   next/ownership matrix and F138's first-flip-versus-history-retirement timing; preserve all Video
   assets; wire protected Activity by that chosen gate; add touch/keyboard
   reorder. Then run the complete four-editor, Rocio, SMM, Kasper and tokened-client TEST/device walks,
   including exact assignment/tweak/SMM/URGENT recipient and terminal receipt.
5. **Residual blind spots:** complete B3's real Graphics authority simulation, B10 build/session
   population after enforcement exists, D-9's external automation/credential attribution, real
   client-token revocation/second-device behavior, retained `/add-to-calendar` caller disposition,
   legacy Samples population/parity and provider-dashboard-only facts.
6. **Cross-system adversarial matrices:** finish mutation CAS/409, comments/audience/history,
   intake/idempotency, runtime flags, outbox/inbound/reconciler races, backup/restore, mixed outage,
   archive/asset rescue, Workload/Editors parity, all credential/integration retirement and full
   persona×phase failure/containment/rollback/recovery proof.
7. **Dryness proof:** only after the backlog is empty, run the broad code-symbol census, broad live
   automation/config census and persona/docs counterexample pass. All three must produce zero new
   finding and zero new backlog item; otherwise reset the count to 0/3.

### Covered — do not repeat

- All 77 active n8n graphs are structurally readable; all 24 deployed Edge Functions have a current
  independent source/config fingerprint. Structural/readback coverage is not health or rollback proof.
- B2/B4/B7/B8/B9 are resolved to numbered findings/remediation gates. B10 is narrowed to
  F38/F127 plus missing post-remediation population evidence. D-9's inspected n8n/GitHub reader
  window is cleared; only external automation/OAuth/remaining credential holders remain.
- Current-main/candidate source-level Kasper, SMM and Video-editor walks are complete and must not be
  repeated. The outstanding work is the explicitly listed post-fix TEST/physical-device/live-receipt
  proof, not another broad source walk.
- Canonical checklist, Track-B §13/§14, B4 readiness, system/app/n8n truth, monitoring, rollback,
  lifecycle and Production parity claims are reconciled through F138 in this PR.

### Resume procedure

Open draft PR #827 for the complete audit checkpoint. Draft PR #836 (owner-authorized
live-exposure containment package) MERGED to main 2026-07-15 as `2a99141`, carrying the security
containment plus the F44 browser fix (`c7b325e`). Confirm #827's head/checks and read this section plus the finding
rows. Preserve unrelated local files. #836's merge-gated security sequence is complete (see the addendum below); return to
monitoring fault injection unless the owner explicitly reprioritizes it. Keep real production/Linear
data read-only; use only the private TEST fixture for writes; do not flip runtime flags or
destructively change n8n without explicit owner sign-off; keep every committed artifact free of client
identifiers and secrets.

### 2026-07-14 security-containment addendum (PR #836 — MERGED 2026-07-15 as `2a99141`)

- **Closed live:** all three onboarding list readers and the weekly-report reader/roster-sync deny
  anonymous requests with `401`; four low-coupling service-role writers deny missing/wrong
  credentials and have restored TEST allow-path proof; the filming-plan EF denies anonymous reads;
  anonymous table reads are revoked for thumbnail revision metadata and both weekly-report tables.
  The exact deployed versions, source SHAs and response-size proof are recorded in F35/F76/F77/F82/
  F88 and PR #836.
- **Public artifacts contained:** both unsafe producers remain manually disabled, 414 target bundles
  are deleted, and anonymous enumeration returns zero target artifacts. Candidate B1 output is
  aggregate-only; candidate Production emits no Actions/Argos review delivery. F122's first-run,
  reconciler-log and external-Argos review remain open.
- **Merge-gated — DONE 2026-07-15 (#836 merged as `2a99141`):** authenticated browser/scheduled
  callers merged; onboarding, weekly reports and filming browser-walked; `calendar-upsert` v38 plus
  `sample-review-upsert` v39 deployed from the merge SHA with deny/TEST-allow/restore proof repeated;
  anonymous `filming_plans` table SELECT revoked (direct REST now 401/42501).
  Never restore an anonymous path to repair a protected UI.
- **Still owner-gated:** the schema-only migration package remains access-restricted because GitHub
  exposed deleted rows even behind a diff guard. Current public files/history/caches/forks/clones stay
  open under F64 until the scheduled freeze, final GO, single canonical rewrite, Support cleanup and
  fresh-clone/fork proof. `clients` and the seven explicit direct-use tables remain F88 projection
  work. Reconciler logs and external Argos history remain F122 work.
- **Owner decisions:** OWNER DECISION 2026-07-15: no notification (see D-33); do not rotate
  the shared Form Linear API key without a complete consumer inventory and controlled migration.

### 2026-07-17 bug-archaeology addendum (F142–F144)

History-driven preventive sweep over the last 3 weeks of incidents (`/bug-archaeology`; corpus:
this register, `EXECUTION_LOG.md` incidents, fix PRs, regression suites). 14 scored candidates,
8 refuted with evidence, 3 parked undecided, 3 confirmed below. All live checks were read-only
(OPTIONS preflights and key-only flag reads); no live state was changed by the run.

| ID | Grade | Evidence | Recommended fix | Status |
|---|---|---|---|---|
| F142 | P2 | **CONFIRMED-LIVE (read-only OPTIONS probe) + SOURCE.** `thumbnail-folder-resolve`'s `Access-Control-Allow-Headers` (source and deployed) omits `x-syncview-key`, but `_syncviewEfHeaders` (index.html) has attached `X-Syncview-Key` to every signed-in-staff `functions/v1` call since the 2026-07-14 hardening. The browser preflight is rejected, so staff thumbnail-folder resolution silently no-ops (catch → console.warn only; Node callers unaffected — CORS is browser-only, same blindness as the 2026-07-15 share-button incident). The EF's allowlist predates the helper change: two individually-correct changes colliding. | Add `x-syncview-key` to the EF allowlist (parity with `thumbnail-revision-read`) and keep the new class guard `test/ef-cors-allow-headers.js` (derives browser-sent headers from index.html call sites, asserts every targeted EF allowlist covers them). The live EF must then be redeployed manually — no deploy workflow covers `thumbnail-folder-resolve`, and the thumbnail deploy loop is deliberately pinned to two slugs. | DONE — fix merged (#855) and the live EF redeployed to v22 on 2026-07-17; read-only OPTIONS probe confirms `x-syncview-key` in the deployed allow-headers |
| F143 | P1 (resolved 2026-07-17) | **RESOLVED-LIVE.** Pinned `deploy-onboarding-edge-functions` run `29601466479` deployed the workflow's eight push-safe staff readers, then `linear-outbound` v33 before `production-write` v24 from exact `main@9d76df6`; all ten source fingerprints passed and neither frozen writer was in the deploy set. Read-only OPTIONS after deployment, and again at 2026-07-20 03:20Z, returned `204` with `x-syncview-source` allowed. The TEST-only reroute browser preflight is therefore no longer blocked by this defect; the deploy summary correctly left the separate drill outcome `PENDING`. | Preserve the pinned exact-main-SHA deploy order and fingerprint attestation. After any future `production-write` deployment, re-probe browser-required headers before enrollment. This lane does not touch the two ⛔ FROZEN client writers. | DONE — TEN-FUNCTION PINNED DEPLOY/FINGERPRINT + REPEATED READ-ONLY PREFLIGHT PROOF CONFIRM `x-syncview-source` LIVE |
| F144 | P2 current / P1 on any F44 worker reinstall | **SOURCE + LIVE READBACK.** Linear removed `Project.team` (singular) — the 2026-07-16 intake outage. The live F44 workers were hand-patched to `Project.teams.nodes` (workflow `BrJSe8zCKUccfmIq`, active version `9e5abc46`), but `scripts/f44-linear-intake-transform.js` — the tested installer/source-of-truth for those workers — still emitted the removed singular query, and `test/f44-linear-intake-transform.js` mocks pinned the stale shape, so the drift was invisible offline. Any reinstall from the repo would re-halt every form batch at the project check. | Port the live workers' plural membership check verbatim into the installer and pin the plural shape in the test (staged in draft PR). Class rule: after any live n8n hand-patch, diff the repo-side installer the same day. | OPEN — FIX STAGED (draft PR); live workers already correct |
| F145 | P2 current Production-mirror hierarchy display (Tier-1 data-model correctness; no data loss) | **CONFIRMED-LIVE (read-only REST + owner report, 2026-07-17).** The mirror modeled hierarchy by `batch_id`, not by Linear’s real parent link: in the TEST family, sub-issues VID-12570/12612/12613 (and their GRA twins) shared batch `b1_b_d0fe…`, while the true Linear parent VID-12569 sat in a different batch. Production therefore presented VID-12570 as a parent even though the three issues were siblings. Display/model defect only: reconcilers, statuses, and writes were unaffected. | **Fixed, merged, and served without schema, n8n, writer, flag, or live-data changes.** Production projects `deliverables.linear_raw.issue.parent.id`, resolves the live native parent globally across creation batches/teams/clients, and never elects a parent from batch/title similarity. Missing, duplicate, archived, self, and cyclic targets remain visible roots. The hierarchy guard pins the VID-12569/VID-12570 sibling shape, three-level chains, cross-boundary edges, and fail-closed cases. Pages run `29713997171` deployed the merge, and read-only live HTML contains the resolver/raw-parent path with `batchParent` absent. | DONE — PR #885 MERGED AS `f00da653` (HEAD `e94531b`) AFTER EXACT-HEAD RUN `29711105120` ATTEMPT 2 PASSED ALL FOUR LANES; CURRENT-MAIN RUN `29713997723` IS RED FAST/INTERACTION WITH HEAVY/REVIEW-PACKET GREEN. THE TREES ARE IDENTICAL, SO THIS IS NOT EVIDENCE OF AN F145 DELTA, BUT CURRENT-MAIN TEST HEALTH REMAINS OPEN |
| F146 | P0 current client-feedback durability | **RUN-4 LIVE FAILURE INJECTION + SOURCE + REGRESSION PROBE.** On both client-link surfaces, `_sxrReviewRequestTweak` and `_calReviewRequestTweak` optimistically changed the acted component to `Tweaks Needed` and cleared its draft before persistence. Forced 5xx/offline failures left the database at `Client Approval`; unlike approve, the request-tweak `_saveError` branches neither restored `prev` nor the typed body. Because client review panels exclude `Tweaks Needed`, the recorded error was no longer rendered, leaving no visible error or usable retry, and reload permanently discarded the request. `qa/OVERNIGHT_TEST_REPORT.md` RUN 4 and `qa/probes/ot4_t0_client_edge_conditions.js` P4 reproduce the loss; the frozen writers were not involved. | Mirror the healthy approve failure path in both request-tweak handlers: on a terminal save error, restore `prev` (including every approval stamp cleared optimistically), restore the captured raw draft, keep the existing error, and repaint so the active panel exposes the failure and a retry with the exact original text. Stage each linked legacy team comment/status pair durably before the source write, but release it only after the exact pinned source confirms the canonical comment, intended status, component link, and client principal; unavailable verification remains pending across reload, while superseded/conflicting/relinked/expired work fails closed. Flip P4 for linked Samples and Calendar cards to require rollback, visible error, exact raw-input and approval-stamp preservation, no team side effects on failure, and exactly one persisted request, team comment, and status mirror after in-place retry or committed-but-lost-ack reload recovery. | DONE — frontend rollback, visible error, exact preserved-input/stamp retry, and source-gated linked-team recovery restored on both client surfaces; branch proof: `npm test` 131/131, P4 edge conditions 89/89, and the other five Tier-0 client journeys 65/65 (all six 154/154), including live commit-then-lost-ack source markers, cache-only reload confirmation, exactly-once team drain, and verified synthetic-row cleanup; ⛔ frozen writers unchanged |

### 2026-07-19 boot, refresh and browser-history addendum (F149–F185)

Read-only route audit at `e238bc4`, source and browser revalidated at
`f00da653` with fictional fixtures and no live mutation. Publication was rebased
onto then-current `main` `07d123d`; the later rebase through `3d8bbfb` also
contains the separate F27 release, the Workload release series, Kasper overflow
work (`470beb9`), and Calendar thumbnail preview (`347cbc8`). Those intervening
mainline changes do not replace the named audit evidence or claim a byte-identical
application snapshot.
F147/F148 were already assigned on current main, so the boot rows use the next
free contiguous range F149–F185. F149–F162 preserve the controlled browser and
source evidence from the original route audit. A publication-time targeted
source-only expansion added F163–F169; those rows do not claim browser or
backend execution. Remediation-phase controlled synthetic-browser evidence added
F170–F171, while F172 is explicitly source-only. Initial exact-head remediation
review of draft #891 candidate `baa4ebf` added source-only F173–F174. Candidate
`02105e9` carries their source corrections; completed source-only exact-head
review of `02105e9` with audit companion `3189203` then added F175–F182. A
later exact-head cloud source review of PR #891 commit `59022d` added F183 and
expanded the already-open F176/F179 evidence; source inspection reconfirmed the
same F183 lifetime gap at then-current candidate `93fc297`. Candidate `13c042b`
passed local `npm test` 149/149 and visible boot 22/22, but exact-head cloud source
review (review `4741233371`; comments `3619424490`, `3619424493`) returned the
workflow-direct F176 registry omission and cross-catalog F179 selector defect. A
prior review at `adb1bca`, reconfirmed at `13c042b`, separately found F184's
pre-verification persisted-queue replay. Pre-split candidate `c9a79ef`
locally closed the previously known F176 probe-registry omission and remediated
F179/F184; it passed 150/150 unit suites plus 23/23 actual
visible boot groups. Its latest exact-head cloud source review is not clean:
review `4741601566`, comment `3619744849`, found the staff issuer ambiently inherited
through `qa/overnight_runner.sh`'s direct process tree outside the 39-probe registry.
Follow-up local source tracing found the transitive `qa/overnight_cron_chunk.sh`
pass-through, unrelated helper inheritance, and the missing declared broker for the
legitimate scenario/master consumers. This expands F176 and does not create F185.
The later owner split retained the complete F176/F179 containment in parked,
  unmerged draft #908 for later review. It is non-blocking for #891 and must not
  be expanded or reopened in this integration. Final #891 exact-head cloud review
  completed at `babbb2d`; product-owned rows stay OPEN only through owner merge,
  while F185 remains separately deferred. The verifier is
already live at v28 and is not redeployed. The owner-selected hard cutover does
not wait for active-link confirmations; old links intentionally reach the
existing updated-link screen. The
only non-source execution was isolated local synthetic proof: Python access
logging plus a Playwright `goto` failure for F178, and inert command-shape
substitution with no payload execution for F179. It used no real token or staff
key, external network, backend/API, live data, flag or writer and read no
protected credential/header value or real request/response body. The exact-head
F176/F179/F183/F184 reviews used no browser, backend, credential value, live data
or write. F176/F179 are OPEN/parked under #908. Final #891 exact-head review completed
at `babbb2d`; the product-owned rows remain open only until owner merge. F177 is
resolved by the living-doc corrections in this change. The public-safe evidence is
`docs/audits/2026-07-19-boot-refresh-history-audit.md`. F102/F117 remain the
existing client-entry P0 owners; F29/F45/F121/F127/F130/F138 were also retained
rather than renumbered.

| ID | Grade | Evidence | Recommended fix | Status |
|---|---|---|---|---|
| F149 | P1 current client-facing refresh correctness | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** A client profile history entry carries `clientTab`, but the head predictor returns on any `history.state.client` without assigning `data-boot-nav` (`index.html:79-80`). The default Analytics skeleton remains selected (`:1759`) until late init restores Calendar or Brief (`:41896-41904`). On hard reload, Chromium captured Analytics active, the Analytics heading/search, and the overview skeleton at `DOMContentLoaded` and a controlled follow-up before the requested client tab settled. No real client data or live call was used. | Resolve the client subtab in the prepaint route descriptor and mount from that same descriptor at runtime. The PR guard must hard-reload actual client Calendar and Brief routes and assert every captured early frame belongs only to the requested client tab. Close F102/F117 in the same client-entry slice before declaring the boundary safe. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F150 | P1 current staff data readiness | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** Direct `#sample-reviews` returns from `init()` at `index.html:41535-41538` before the shared `fetchAll()` is created at `:41742`. Samples Review can load independently, but entering Home later only renders the untouched Analytics model. The browser fixture contained one Analytics client; after Samples Review → Home, `allData` remained empty and the dashboard showed zero clients. | Give every route an idempotent data-requirement coordinator. Entering Home must start/await Analytics core regardless of the document's original route, and route-aware completion must repaint only the current consumer. Guard the real direct-boot → visible Home sequence. | OPEN — STAFF-FLOW PR |
| F151 | P1 current Analytics truthfulness | **SOURCE + CONTROLLED HTTP-FAILURE BROWSER.** `fetchEssentials()` and `fetchExtras()` call `.text()` without checking `response.ok` (`index.html:8287-8307`). Fetch therefore resolves normally for 4xx/5xx; controlled 500 JSON bodies were parsed as CSV and the app settled into a normal-looking zero-client dashboard instead of the error path. | Use one checked read helper with source identity and bounded error classes. Non-2xx must render an explicit failed/degraded state with visible retry and must never produce authoritative empty business data. Add visible 500/429/malformed-body/retry guards. | OPEN — SILENT-FAILURE PR |
| F152 | P1 current boot availability | **SOURCE + CONTROLLED NEVER-SETTLING BROWSER.** Init awaits stored-staff verification (`index.html:41507-41513`), whose fetch has no deadline (`:17779-17790`). Cold Home can await essentials plus all optional extras (`:8287-8320`, `:41846-41859`), and client-link verification is another unbounded awaited fetch (`:41988-42011`). Holding one dependency open left only a skeleton with no terminal state. No current production outage is claimed. **CURRENT SOURCE-ONLY ROUTE CENSUS at `07d123d`; no browser/backend request or live data was used.** Additional instances of this same deadline/abort class are: Templates detail waits on unbounded base, routing-flag, and optional overlay reads before leaving its skeleton (`index.html:10368-10436`, `:11590-11614`, `:20927-20962`); Production core retries only after each unbounded fetch settles and awaits all core sources together (`:38082-38128`, `:40708-40758`); and Kasper Samples awaits an unbounded cross-client Samples read together with the optional SMM map (`:47190-47239`). These pure never-settling cases belong to F152 rather than new route rows because a landed rejection already has an explicit route error on Templates/Production/Kasper Samples. | Give every boot-blocking read a named deadline, abort on route exit, and explicit cached/degraded/error/retry policy. Optional Analytics extras must not block first usable Home. Test timeout, abort, late completion and retry through visible route frames. Apply a per-attempt terminal deadline, abort/supersession ownership, and route-exit cleanup to the additional readers. Optional attribution/overlay dependencies must not block core usable data; a deadline must become a visible terminal error or explicit degraded last-good state with keyboard/touch Retry. Extend F162 to Templates detail, Production core, and Kasper Samples. | OPEN — SILENT-FAILURE PR |
| F153 | P1 current Back/Forward correctness | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** The client `popstate` branch directly renders and clears only a subset of nav state (`index.html:10246-10268`), bypassing `navTo()`'s complete nav/body/teardown lifecycle (`:16228-16305`). Back from Calendar, Samples Review, Filming Plans, Kasper, Production and other routes left Analytics plus the prior route active and/or retained its body mode; Forward recovered, and a second Back reproduced the hybrid. | Parse Back/Forward into the same route object and transition lifecycle used by clicks. Assert actual A → client → Back → Forward → Back for each affected route, with exactly one active nav, one body mode, and no stale poller/subscription/lock. | OPEN — STAFF-FLOW PR |
| F154 | P1 current supported deep-link fidelity | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** Calendar, Samples Review and Kasper parse a supplied client/card/subtab, then `navTo()` serializes only the top-level hash (`index.html:16321-16329`). Calendar attempts to preserve detail only after the earlier transition (`:22157-22167`); Samples Review consumes `window.__sxrDeepHash` once (`:42427-42439`); direct Kasper subtab boot calls `navTo('kasper', false)` (`:41821-41827`). A first load could focus correctly while the URL lost the state required by the second reload. | Introduce structured parse/serialize round trips for client, card and subtab, including sheet-discovered clients. Test copied link → reload → second reload → away → Back/Forward with URL, selection and open detail unchanged. Keep F121's replace-state policy question separate. | OPEN — STAFF-FLOW PR |
| F155 | P2 current early-shell presentation | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** Known destinations reuse unrelated skeleton families: Time Off explicitly maps to Analytics (`index.html:1771`), Weekly routes reuse Sales Intake/Kasper (`:1772-1773`), and most Kasper subtabs share Review. Templates index, Submit and TikTok Pilot also showed shape-mismatched early shells. These are provisional-DOM mismatches, not claims that the settled routes fail. | Generate exact or deliberately neutral shells from the route registry. CI must capture early visible frames under slow parsing/data and fail if a known destination exposes another page's heading, search, nav-active state or table. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F156 | P2 current Production new-tab entry | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER.** The visible Production anchor advertises `#production` (`index.html:6890`) and modifier clicks deliberately use the native href, while the head predictor recognizes only `?prod=1` (`:110`) and normal `navTo()` adds that query (`:16323-16329`). Ctrl-click/new tab reached Home; direct `?prod=1` remained healthy. | Make the advertised href independently bootable and parse all canonical aliases through one route owner. Guard the actual modifier-click/new-tab path, not a synthetic direct query alone. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F157 | P2 current Weekly history integrity | **SOURCE + CONTROLLED FICTIONAL-DATA BROWSER; LOWER-REACH ROUTE.** Weekly filters call `history.replaceState(null, ...)` (`index.html:19847-19858`). The null-state fallback (`:10275-10283`) does not parse Weekly or reset route modes. After a controlled continuation to Calendar, Back restored the Weekly URL with Analytics DOM, Calendar active and Weekly hidden-chrome mode. Weekly is direct-link-only, so reachability is lower than F153. | Preserve a valid Weekly route state and route all null/native history entries through the canonical parser/transition. Test filters plus Back/Forward without relying on internal settled state. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F158 | P2 current direct-Calendar failure recovery | **CONTROLLED OFFLINE BROWSER + SOURCE.** Top-level Calendar fast boot mounts before the shared Analytics/roster promise settles; its rejection is logged only (`index.html:41837-41843`). In the controlled failure, the route remained on an empty Calendar “Refreshing…” shell with no visible failure or retry. This is distinct from Calendar's later backend loader, which already has a timeout/retry state. | Give the route prerequisite its own visible error/degraded state and keyboard/touch retry. Preserve a last-good roster only when labelled stale; never represent failed prerequisite data as “No clients.” | OPEN — SILENT-FAILURE PR |
| F159 | P2 current restricted-storage boot | **SOURCE + CONTROLLED STORAGE-DENIED BROWSER; UNCOMMON ENVIRONMENT.** The head gate wraps storage, but the main script still has unguarded reads, including Analytics preferences (`index.html:9576-9578`), saved navigation (`:41541`) and the outer auth decision (`:42065`). Forced `SecurityError` removed the password overlay into a broken page after valid entry. | Use one safe storage adapter for every read/write/parse. Storage denial must degrade to non-persistent state without aborting routing. Test explicit storage blocking and restricted embedded/private contexts. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F160 | P2 current native fragment ownership | **SOURCE + CONTROLLED BROWSER.** The app has one global `popstate` listener (`index.html:10246`) and no `hashchange` listener. A native fragment change updated the URL while Home remained mounted and active. Header clicks hide the gap because they call `navTo()` directly. | Route native fragment changes through the canonical parser/transition, with recursion and duplicate-entry guards. Test typed/copied fragment changes and integration-driven hash updates. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F161 | P2 current saved-route consistency | **SOURCE + CONTROLLED BROWSER.** `navTo('sample-reviews')` persists the route, but the head FAST/RESTORABLE lists omit it (`index.html:89-90`) and the post-await bare-root saved-nav branch omits it (`:41946-41948`). A plain-root new tab therefore opened Home and overwrote the saved route; exact `#sample-reviews` refresh remained healthy. | Make route restore policy an explicit registry field. If Samples Review is restorable, all boot copies must honor it; if Home is the owner-approved new-tab default, stop persisting a contradictory route. Pin the chosen behavior in a real browser. | OPEN — ROUTE-CONSISTENCY FOLLOW-UP |
| F162 | P1 release-evidence gap for every boot remediation | **TEST CENSUS + BROWSER COUNTEREXAMPLES.** `test/boot-gate-parity.js` and `test/templates-refresh-routing.js` are predominantly source assertions; Production has route-specific browser coverage, but no PR lane records the actual visible sequence for every supported route across cold boot, refresh, second refresh, Back/Forward, slow/5xx and never-settling reads. All F149–F161 counterexamples coexisted with a green 135/135 unit run at the audited SHA. **CURRENT SOURCE-ONLY ROUTE-COVERAGE FOLLOW-UP at `07d123d`; no new browser proof is claimed.** The targeted recon behind F163–F169 and the added F29/F45/F130/F138/F152 evidence identifies additional visible routes whose failure transitions are not driven by the current boot lane: Brief tab/general/content synthesis; Samples Review primary-plus-fallback; top-level and Kasper Filming; onboarding inbox/profile entry/standalone; Weekly Reports; Credentials list/modal/history; Kasper Editors; Workload; Linear picker; Kasper Review/Messages/Samples; Templates detail; and Production core/detail Activity. **REMEDIATION-PHASE CONTROLLED SYNTHETIC-BROWSER FOLLOW-UP:** F170–F171 add Calendar primary/ancillary/realtime lifetime, no-load exit, pagehide/BFCache, and held client-A → client-B continuation coverage. **SOURCE-ONLY FOLLOW-UP:** F172 adds staff Analytics bootstrap/document-lifetime coverage without claiming a reproduced staff failure. | Extend the data-driven browser matrix to every affected route above using synthetic/TEST-only fixtures. From actual document start and refresh, inject slow success, 4xx/5xx, parseable error envelopes, malformed 200, confirmed empty, and never-settling reads. Assert a bounded transition to a route-correct terminal error/degraded state; unavailable data must never paint as business-empty. Drive visible keyboard and touch Retry to recovery without document reload, exercise repeat failure, preserve labelled last-good data where allowed, and prove route exit or a superseding retry prevents every late response from repainting the current route. Calendar guards must additionally hold/release real primary, ancillary and realtime work across client replacement, no-load exits, pagehide and persisted pageshow, and prove that a held A continuation cannot mutate, persist or repaint B. Staff Analytics must receive the same actual-document/BFCache lifetime guard when F172 is remediated. Settled-page screenshots and source assertions remain insufficient. | OPEN — REQUIRED GUARD ON EVERY REMEDIATION PR |
| F163 | P1 current client Brief truth and recovery | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** `fetchTabSummary()` (`index.html:7541-7565`), `generateGeneralBrief()` (`:7819-7909`), and `generateContentSummary()` (`:9750-9790`) parse response bodies without first requiring `resp.ok`, so a parseable 5xx/error envelope can become cached empty or derived output. `renderTabSummary()` suppresses its error state (`:7568-7594`); `renderGeneralBrief()` checks missing data before its error branch and immediately schedules another generation (`:7733-7766`); the generation triggers start persisted polling without validating their POST result (`:8578-8712`), while `fetchBriefs()` catches a failed refresh and polling can continue over prior arrays for 15/40 minutes (`:8326-8335`, `:8715-8756`). Content-summary failure exposes no Retry and directs the user to refresh the page (`:9830-9833`). | Require a bounded request plus HTTP, success-envelope, and payload-schema validation before publishing or caching any synthesis. Stop/mark a pending generation when its trigger fails; polling must distinguish refreshed-complete, refreshed-empty, degraded, failed, and timed-out state instead of searching stale arrays. Preserve last-good output under a visible stale/error banner, expose keyboard/touch Retry without document reload, reject late/superseded results, and satisfy F162 with synthetic parseable-5xx, malformed-200, hang, retry, and recovery sequences on the actual Brief route. | OPEN — BRIEF FAILURES CAN LOOK LIKE GENERATION OR VALID OUTPUT |
| F164 | P1 current client Samples Review truthfulness | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** After the primary Samples read fails, `_sxrFetchPosts()` calls the fallback, parses JSON without checking `resp.ok` or `json.ok`, and unconditionally returns `{ok:true, posts: ... || []}` (`index.html:42971-42987`). `loadSxrCards()` then adopts and can cache that result as a confirmed empty collection (`:42989-43030`). A parseable 5xx/error envelope can therefore erase the distinction between unavailable and genuinely no Samples. | Require HTTP success, an explicit success envelope, and an array payload from either reader. Publish confirmed-empty only from a complete successful receipt; on total failure preserve last-good cards with visible staleness or show a terminal error, and expose keyboard/touch Retry without reload. Abort route-exit/superseded requests and satisfy F162 with actual Samples Review boot/reload tests for primary failure plus fallback 5xx/malformed/hang, visible error, retry, recovery, and no fake-empty frame. | OPEN — FALLBACK FAILURE CAN PUBLISH A VALID EMPTY REVIEW |
| F165 | P1 current Filming Plans truth and recovery | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** The top-level loader has no deadline (`_fpLoadFromSupabase()` / `_fpEnsureLoaded()`, `index.html:11979-12018`). On rejection, `mountFilmingPlansView()` writes status through `_fpSetStatus()`, which rerenders and remounts while `filmingPlansData` remains null; `renderFilmingPlansView()` therefore keeps the loading skeleton and starts another load (`:12096-12103`, `:12228-12263`). In Kasper, `_filmsMapLimit()` and the runway/month readers collapse worker, HTTP, malformed, and thrown failures to `null` (`:51553-51610`); `_filmsClassify()` then treats missing runway as authoritative red “No scheduled content” (`:51613-51628`), and `_kasperLoadFilming()` caches/paints that classification (`:51743-51783`). | Give each dependency a bounded typed state and stop implicit remount/retry loops. Keep `unknown/unavailable` distinct from confirmed no plan/no scheduled content; partial runway or Doc-tab failure must be visibly degraded and must not emit a red business assertion. Preserve last-good rows when safe, expose keyboard/touch Retry without reload, reject late/superseded results, cross-link the generic deadline requirement in F152, and satisfy F162 on both top-level and Kasper Filming routes with synthetic 5xx/malformed/hang, retry, recovery, and confirmed-empty cases. | OPEN — DEPENDENCY FAILURE CAN LOOP OR BECOME A FALSE FILMING ALERT |
| F166 | P1 current onboarding route truth and recovery | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** `_obvFetchLists()` runs three unbounded readers and returns `Promise.allSettled()` (`index.html:10897-10922`). `_obvEnsureSlugIndex()` converts rejected sources to empty arrays and total exceptional failure to an empty Set, so source failure can hide the client-profile Onboarding entry (`:10924-10942`). The standalone viewer ignores `_obvError`; after `_obvEnsureLoaded()` converts a failed load to `_obvSubs=[]`, `_obvRenderStandalone()` publishes “No onboarding form on file” (`:10949-10984`). This runtime absence contract is not owned by F120, which closed obsolete operator guidance. | Add per-source deadline/completeness receipts and keep unknown, partial, failed, and confirmed-empty distinct. An unavailable slug index must not silently hide a known entry, and standalone failure must paint a terminal diagnosable error with keyboard/touch Retry rather than “no form.” Preserve the existing authorization/redaction boundary, suppress late/superseded results, cross-link F152 for deadlines, and satisfy F162 on inbox, client-profile entry, and standalone deep-link boot/reload with synthetic partial/total failure, malformed/hang, retry, recovery, and true-empty cases. | OPEN — ONBOARDING FAILURE CAN MASQUERADE AS NO ENTRY OR NO FORM |
| F167 | P2 current Weekly Reports truth and recovery | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** `_srpApi()` checks HTTP status but has no deadline and returns `{}` for an empty successful body (`index.html:19518-19552`). `_srpLoadOptions()` turns a missing managers array into `[]` (`:19554-19558`); `_srpLoadReports()` suppresses option failure with `{}`, accepts missing reports/weeks as empty arrays, and paints them (`:19874-19888`), after which the result renderer says “No reports match” (`:19922-19923`). A held read strands the skeleton, while the landed error replacement omits the report toolbar and a visible Retry. | Require bounded reads and an explicit schema/completeness receipt before publishing options, weeks, or reports as empty. Preserve last-good reports under a visible stale/error banner where available; cold failure must show a terminal error with keyboard/touch Retry and recover without reload. Ignore late/superseded responses, cross-link F152 for deadlines, and satisfy F162 with actual Weekly Reports boot/reload tests for empty-success, malformed-200, 5xx, hang, retry, recovery, and confirmed no-match filtering. | OPEN — REPORT READER FAILURE CAN LOOK LIKE NO REPORTS OR NEVER SETTLE |
| F168 | P1 current Client Credentials availability and audit-history truth | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, secret body, or write was used.** `_ccApi()` validates HTTP/top-level `ok` but has no deadline (`index.html:51045-51069`). The Kasper and per-client list loaders accept a missing/non-array `credentials` field as `[]`, so malformed success can become “No credentials” (`:51099-51133`, `:51407-51425`); landed errors have no in-body Retry. `_ccOpenHistory()` accepts missing/non-array `events` through `json.events || []` and publishes “No history yet,” while a held request leaves the history loader indefinitely (`:51332-51342`). F84 remains the separate confidentiality/attribution owner. | Require bounded requests plus action-specific response schemas: list must provide a credentials array and history an events array/completeness receipt before empty is authoritative. Keep unavailable history distinct from no events, preserve last-good metadata where safe, expose keyboard/touch Retry without reload, suppress late/superseded results, and preserve every F84 confidentiality/redaction constraint. Cross-link F152 for deadlines and satisfy F162 on Kasper list, per-client modal, and history with synthetic malformed-200/5xx/hang, retry, recovery, and confirmed-empty cases without secret fixtures. | OPEN — CREDENTIAL AND AUDIT READ FAILURE CAN LOOK AUTHORITATIVELY EMPTY |
| F169 | P2 current Kasper Editors truth and recovery | **CURRENT SOURCE-ONLY TARGETED RECON at `07d123d`; no browser/backend request, live data, identity, credential, or write was used.** `_kasperLoadEditors()` has no deadline and accepts any parsed JSON unless `json.ok === false`; a 200 `{}` is stored and cached as editor data (`index.html:53999-54031`). `_kedPaint()` maps a missing editors array to `[]` and publishes “No editor deliveries last week” (`:54045-54060`). A held request leaves the skeleton indefinitely; a landed error replaces the body but has no in-body Retry, only the persistent toolbar Refresh. | Require a bounded response with explicit success, week identity, and an editors array before publishing or caching empty. Distinguish confirmed no deliveries from unavailable/malformed data, retain last-good data under a visible stale/error banner when safe, provide keyboard/touch Retry in the failure state, and suppress late/superseded results. Cross-link F152 for deadlines and satisfy F162 with actual Editors boot/reload tests for malformed-200, 5xx, hang, retry, recovery, cache behavior, and confirmed-empty. | OPEN — MALFORMED OR HELD EDITOR READ CAN LOOK EMPTY OR LOAD FOREVER |
| F170 | P1 current Calendar transport/realtime lifetime ownership | **CURRENT SOURCE + CONTROLLED SYNTHETIC BROWSER at `86fe60c`; synthetic clients only, with no backend request, live data, identity, credential, runtime-flag change, or live write.** `loadCalendarPosts()` owns only its primary request sequence and clears loading ownership before starting fire-and-forget Linear reconcile, Linear metadata and realtime subscription continuations (`index.html:27413-27852`). `_calV2Teardown()` removes the current channel/timer but owns no in-flight primary or ancillary request (`:27328-27402`), while Calendar `pagehide` only flushes pending writers and `pageshow`/focus can start another read (`:23128-23206`). Controlled no-load exit, held-read/channel, pagehide and real persisted-BFCache sequences showed old work or channel ownership surviving the visible document transition, including a stale completion/finally racing the replacement loader. Draft #891 candidate `02105e9` adds a generation-owned controller, realtime epoch, exact-owner finalization and exit/BFCache guard; it is unmerged, and continued exact-head review found the separately registered F175/F176 and F178–F184 release-control blockers. | One Calendar document/surface generation must own the primary read, every post-load Linear/meta continuation, realtime channel/timer, loader state and deferred render. Abort and revoke that exact owner on client replacement, route/no-load exit and pagehide; a persisted pageshow may start exactly one fresh epoch. `finally` may release only its own generation. Satisfy F162 with actual visible cold/refresh/client-switch/no-load-exit/pagehide/BFCache sequences, held transports and late release. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F171 | P0 current cross-client Calendar continuation integrity/confidentiality | **CURRENT SOURCE + CONTROLLED SYNTHETIC CLIENT-A → CLIENT-B BROWSER at `86fe60c`; fictional rows and intercepted requests only, with no backend request, live data, identity, credential, runtime-flag change, or live write.** The post-load v1 reconcile and Linear metadata refresh are fire-and-forget and consult mutable global `calState` after their awaits (`_calReconcileLinearStatuses()` / `_calRefreshParentLinkFlags()`, `index.html:25837-26080`, invoked at `:27830-27852`). The reconcile can mutate the posts currently mounted, append `_calPendingEdits`, seed suppression state, call `_calFlushCardSave()`, persist metadata/cache and repaint after the selected client changed. A controlled fixture held client A's reconcile, switched visibly to client B, then released A and reproduced the stale continuation mutating B and enqueueing an intercepted Calendar write against B's current surface. That is a cross-client integrity and confidentiality boundary, not a cosmetic stale-frame issue. Draft #891 candidate `02105e9` leases/aborts the ancillary work, rechecks the exact owner around awaits and mutations, and guards the real visible A → B sequence; it is unmerged, and continued exact-head review found the separately registered F175/F176 and F178–F184 release-control blockers. | Capture immutable client slug, row identity and generation before any ancillary request. Abort on client/route/document replacement and revalidate the same owner immediately before every mutation, cache/meta persist, render, suppression token and writer enqueue; never derive a stale continuation's destination from current global client state. Satisfy F162 with held A reconcile/meta responses, a real visible A → B switch, late release, zero B mutation/write/cache/render, replacement-client health, repeat switching and pagehide/BFCache. | OPEN P0 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F172 | P1 current staff Analytics document/BFCache lifetime ownership | **CURRENT SOURCE-ONLY at `86fe60c` plus draft #891 candidate `02105e9`; no browser/backend request, live data, identity, credential, runtime-flag change, or write was used, and runtime frequency is not claimed.** Current-main staff Analytics `fetchAll()` has no document generation or abort controller (`index.html:8320-8330`, bootstrap at `:41918`). The draft introduces an abortable run only for client links: staff bootstrap calls `fetchAll(null)` and `fetchEssentials()`/`fetchExtras()` attach a signal only when that client run exists, while `_syncviewSuspendClientEntry()` exits immediately for staff. Staff Analytics reads and their late global-data/cache/render continuations therefore remain unowned across pagehide/BFCache in both the current source and the candidate. No staff browser reproduction or remediation is claimed; that work remains in the staff-flow phase. | Give staff Analytics bootstrap one document/run generation and abort controller covering essentials, extras, cache publication and re-render. Revoke it on pagehide/route replacement, allow persisted pageshow to create exactly one fresh run, and make every late continuation prove the current document owner before global mutation, cache write or paint. Satisfy F162 with actual visible staff cold boot, held essentials/extras, no-load exit, pagehide/BFCache restore, late release, retry/recovery and confirmed-current rendering. | OPEN — SOURCE-ONLY; STAFF-FLOW BROWSER REPRODUCTION AND REMEDIATION REQUIRED |
| F173 | P1 current protected TEST-credential handling / client-entry release harness | **REMEDIATION-REVIEW SOURCE-ONLY at unmerged draft #891 head `baa4ebf`; no courier command or browser/backend request was run, and no credential, request body, response body, live data or write was read.** The candidate routes EF write-path client Calendar and Samples through the strict TEST-token builder (`qa/ef-writepath/lib.js:32`, `:343`, `:386`). The resulting client preflight sends the protected token in a POST body and later client EF calls can send it in `X-Syncview-Client-Token`. `_courierFetch()` writes request bodies to `_pd_*.bin`, writes responses to `_resp_*.bin`, never removes either file, and puts the target URL plus forwarded headers directly in a shell-built curl command (`qa/ef-writepath/lib.js:97-113`). An `execSync` failure is returned as `e.message`, which can include that command. The current-main courier plumbing already has the residue/argv behavior; #891 newly couples it to the required protected TEST credential. **SOURCE-ONLY CANDIDATE FOLLOW-UP:** `02105e9` replaces this EF path with stdin curl config, fixed argv, in-memory framed response and generic failure handling, with a synthetic-only contract guard. No courier command, token or request was used for this documentation conclusion. | Preserve the corrected token-bearing courier contract and its synthetic marker guard. Do not print or persist the actual TEST token. The remaining release-harness/lifetime paths are separately owned by F175/F176 and F178–F184. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F174 | P1 current client-entry release evidence / operative overnight QA contract | **REMEDIATION-REVIEW SOURCE-ONLY at unmerged draft #891 head `baa4ebf`; no browser/backend request was run, no token was resolved, and no live client or write was used.** The operative overnight target binding still prescribes a client Samples URL with `c`, `v`, `sxr` and `v2debug`, but no `t` (`.claude/skills/overnight-test/SKILL.md:92-98`). The candidate's strict client-entry envelope requires exactly one `c` and `t`, permits only `c,t,v,sxr`, and therefore rejects that documented route before client data can load; the canonical TEST-only builder already exists in `qa/test-client-entry.js:39-51`. Following the read-first overnight protocol would not exercise the intended client-share boot and could create false release confidence for F102/F117/F149. **SOURCE-ONLY CANDIDATE FOLLOW-UP:** `02105e9` changes the operative skill to resolve the protected TEST token in-process and build the Samples route through `testClientEntryPath()` with only `c,t,v,sxr`; the offline contract now rejects a hand-built or debug-mixed client route. No browser run or token resolution was performed for this documentation conclusion. | Preserve the canonical strict-route instruction and contract guard. The remaining workflow, transport and lifetime defects found in continued review are separately owned by F175/F176 and F178–F184. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F175 | P1 current protected TEST-credential handling / remaining shared SXR network helpers | **SECOND EXACT-HEAD REMEDIATION REVIEW, SOURCE-ONLY at unmerged draft #891 head `02105e9`; no courier/helper/browser/backend command was run, no token was resolved, and no credential/header value, request/response, live data or write was read.** The hardened courier and `nodePost()` use stdin curl config, but `_exec()` remains a shell `execSync` wrapper (`qa/sxr_courier_lib.js:101-118`). Direct `supa()`, `supaEvents()` and `supaCal()` still interpolate each protected REST URL plus `apikey` and `Authorization` headers into a shell command (`:242-248`, `:519-521`). A process failure can retain that command in the thrown error. These helper calls sit outside the F173 transport guard and are used by seed/readback/cleanup paths in the same TEST release harness. | Move every shared-SXR HTTP helper onto the same fixed-argv, curl-config-over-stdin, in-memory response primitive. Remove shell curl construction from `supa()`, `supaEvents()` and `supaCal()`; keep errors generic and use in-process timers rather than shell sleep. Extend the synthetic-only platform-real guard to every direct helper and prove URL/header markers appear in neither argv, exceptions, logs nor residue after success and forced failure. Never resolve or print a real token in this guard. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F176 | P1 current protected TEST-token/staff-issuer lifetime and nightly process scope | **SECOND EXACT-HEAD REMEDIATION REVIEW, SOURCE-ONLY at unmerged draft #891 head `02105e9`; no workflow or browser command was run, no token was resolved, and no credential, environment value, request/response, live data or write was read.** `exportTokenToGitHubEnv()` appended `SYNCVIEW_TEST_CLIENT_TOKEN` to the runner's `GITHUB_ENV` (`qa/test-client-entry.js:92-108`), so broad nightly steps and later children inherited it. **POST-F182 CLOUD SOURCE REVIEW at `59022d`:** the token export was replaced, but `qa/run-probes.js:109-115` reconstructed `SYNCVIEW_STAFF_KEY` into every manually selected probe child. **EXACT-HEAD CLOUD REVIEW at `13c042b`, review `4741233371`, comment `3619424490`:** the local 38-entry census omitted workflow-direct `sxr_client_persist_guard.js`, so scheduled Samples lost the issuer capability it required. **LOCAL CANDIDATE FOLLOW-UP at `c9a79ef`:** the immutable registry has 39 exact probe consumers (37 manifest, one workflow-direct, one temporal), `npm test` passed 150/150 and the actual visible boot lane passed 23/23. **LATEST EXACT-HEAD CLOUD SOURCE REVIEW at `c9a79ef`, review `4741601566`, comment `3619744849`:** the direct `qa/overnight_runner.sh` process tree still inherits the operator-provided `SYNCVIEW_STAFF_KEY` before probe classification. **LOCAL SOURCE-ONLY FOLLOW-UP:** `qa/overnight_cron_chunk.sh:27-59` transitively forwards both credentials; staff-only and Calendar probes, unknown/manual paths, cleanup, unit, timeout/log/readiness/Git helpers and wrappers inherit an issuer they do not need. The static server and browser/courier boundary are already credential-stripped and must stay so. Scenario and master processes genuinely open strict client actors, but receive the issuer ambiently rather than through an explicit declared broker. This is another F176 occurrence, not F185. No QA runner/probe/helper command, browser, staff key/token value, backend, live data or write was executed or read for the latest review or follow-up. | Keep the token-export/GITHUB_ENV handoff removed. At entry to both shell runners, privately capture the staff issuer and immediately unset both client-entry credentials before any child process. Default every descendant to credential-free. Restore only `SYNCVIEW_STAFF_KEY` for an exact registry-approved probe or a separately declared scenario/master broker, and deliver it only to the final operative Node process—not to a timeout/wrapper or through argv/log/output. Never restore `SYNCVIEW_TEST_CLIENT_TOKEN`; server, browser, couriers, cleanup, unit, readiness, Git and unrelated helpers receive neither credential. Use the JavaScript probe registry rather than a second shell allowlist and declare the scenario/master broker classes explicitly. Add an actual shell-launch guard with synthetic markers proving persistence probe plus scenario/master get staff key only, while staff-only/Calendar/unknown/manual/cleanup/unit/server/timeout/Git get neither; exercise the cron handoff and prove no secret-shaped marker appears in argv, logs or output. Preserve the actual visible workflow-selected `sxr_client_persist_guard` TEST Samples boot, visible resolution failure and zero credential residue. No literal or real credential may enter repository evidence. | PARKED BY OWNER 2026-07-22 — COMPLETE WORK RETAINED IN UNMERGED DRAFT #908; NON-BLOCKING FOR #891; DO NOT REOPEN IN THIS INTEGRATION |
| F177 | P2 current-truth review-status provenance | **SOURCE-ONLY DOC REVIEW at audit companion `3189203`; no runtime, workflow, browser, backend, token, credential, request/response, live data or writer was inspected.** `docs/truth/APP.md:50-55` and `docs/independence/SYSTEM_MAP.md:318-325` still said cloud review was pending even though exact-head source review of candidate `02105e9` had completed and returned F175/F176. The stale wording could cause a read-first session to omit active merge blockers. | Update APP, SYSTEM_MAP, BRIEFING, this register, the public-safe audit, REPO_MAP and the operative go-live checklist together: review completed on `02105e9`/`3189203`, F175/F176 and continued-review F178–F182 remain open, and only a post-remediation exact-head review may clear the handoff. | DONE — RESOLVED BY THIS DOCS-ONLY CURRENT-TRUTH CORRECTION; NO PRODUCT, WRITER, FLAG OR LIVE-SYSTEM CHANGE |
| F178 | P1 current protected TEST-token disclosure through access/navigation failure logs | **EXACT-HEAD SOURCE + ISOLATED LOCAL SYNTHETIC PROOF at unmerged draft #891 `02105e9`.** The canonical client path places the TEST token in the query string. The Samples nightly starts `python3 -m http.server` without redirecting its access stream (`.github/workflows/samples-e2e-nightly.yml:45-57`); the shared opener calls `page.goto()` with the complete target (`qa/sxr_courier_lib.js:473-478`); and probe/master runners retain and tail child output (`qa/run-probes.js:62-73`; `qa/master.js:133-157`, `:168-176`). Isolated local Python-access-log and Playwright-`goto`-failure checks reproduced the marker in those surfaces using only a synthetic token. No real token, external network, backend/API, live data or writer was used. | Suppress or structurally redact static-server request targets before runner output, catch navigation failures at the credential-owning boundary, and emit only a generic route-safe error. Never retain or tail raw token-bearing URLs. Add forced-access-log and forced-`goto`-failure guards with synthetic markers that inspect all stdout/stderr/report surfaces, then drive the actual visible TEST boot without printing the current token. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F179 | P1 current manual-nightly selector validation / command injection in credential-bearing steps | **EXACT-HEAD SOURCE + ISOLATED INERT SUBSTITUTION at unmerged draft #891 `02105e9`.** User-controlled `workflow_dispatch` inputs were inserted directly into shell `run` lines for Samples `scn` and Calendar `probes`; at that exact head the broad steps inherited F176's protected TEST token, so metacharacters were interpreted by a credential-bearing shell. A local synthetic-marker substitution confirmed the command-shape break without executing the substituted payload or using a real token/staff key. **POST-F182 CLOUD SOURCE REVIEW at PR #891 `59022d`:** the inert environment handoff still failed closed-set validation. `parseScenarioFilter()` discarded empty comma components, while `run_scenarios.js` accepted a partial valid-plus-unknown selector whenever any valid substring matched; inputs such as an approved selector plus an empty or unknown component could therefore reach and run the approved scenario instead of rejecting the complete dispatch value. Then-current candidate `93fc297` began unmerged complete-set/empty-component remediation. No credential, browser, backend or live scenario was executed for that added evidence. **LOCAL CANDIDATE FOLLOW-UP at `13c042b`:** complete-set validation is retained before live-harness loading, and the 149/149 unit run includes synthetic empty/mixed-known-unknown/substring/metacharacter rejection contracts. **EXACT-HEAD CLOUD SOURCE REVIEW at `13c042b`, review `4741233371`, comment `3619424493`:** `.github/workflows/samples-e2e-nightly.yml:19-43` supplies one `scn` to `qa/master.js`; master assigns it to both scenarios and tree (`qa/master.js:287-304`, `:375`). Each `run_scenarios.js` invocation then loads only its own flat or tree catalog and independently requires every selector term to match that catalog (`qa/probes/run_scenarios.js:6-32`). Because the catalogs differ, legitimate flat-only selectors such as `resolve_via` fail tree and legitimate tree-only selectors fail flat. **LOCAL CANDIDATE FOLLOW-UP at `c9a79ef`:** one immutable selector is validated once against the real flat/tree/visual union before live loading, then partitioned into lane-local matches with stable clean skips; malformed, duplicate, option-shaped and union-unknown inputs fail closed. Focused guards and the 150/150 unit run passed. Exact-head cloud review `4741601566` returned no separate F179 thread, but this row remains open until owner merge. No credential, browser, backend or live scenario was executed for the source reviews. | Never substitute dispatch text directly into `run`. Parse the complete selector list as inert data and validate it once, before restoring any credential or loading either live harness, against the union of exact flat and tree catalog IDs. Reject empty components, duplicates, option forms, metacharacters and every union-unknown or partial-match component; one valid component must never mask one invalid component. Partition a valid selection into exact per-catalog matches: each lane runs only its matches and a sibling with none skips cleanly. Keep the staff issuer key only in the exact in-process token-resolution boundary and strip protected values from every child. Add synthetic empty/mixed-known-unknown/substring/semicolon/command-substitution/newline/option-injection guards with no payload execution, plus workflow-shaped flat-only `resolve_via`, tree-only and shared-selector guards that drive the actual visible TEST route for the matching lane and prove the sibling clean skip; an unknown must fail before harness load. | PARKED BY OWNER 2026-07-22 — COMPLETE WORK RETAINED IN UNMERGED DRAFT #908; NON-BLOCKING FOR #891; DO NOT REOPEN IN THIS INTEGRATION |
| F180 | P1 current full-quota probe transport confidentiality/residue | **EXACT-HEAD SOURCE-ONLY at unmerged draft #891 `02105e9`; no probe/helper command, credential, request body, response header, backend/API, live data or writer was run or read.** `qa/probes/p94_nav_full_quota.js:12-37` implements a private courier that interpolates intercepted URLs and headers into shell curl argv, writes POST bodies to random `/tmp/qa/_p94_*.txt` files, writes response headers to the shared `/tmp/qa/_p94h.txt`, reads them back, and never removes either class of file. It bypasses the F173 shared transport guard and can expose token-bearing URLs/headers and payloads on success or failure. | Route p94 through the common fixed-argv, fileless in-memory HTTP primitive; remove shell command construction and all request/header temp files; sanitize thrown errors; and strip protected child environment values. Add synthetic marker guards for URL, headers, body, response headers, argv, stdout/stderr and residue after success plus forced failure before rerunning the visible full-quota boot sequence. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F181 | P1 current EF write-path auxiliary transport confidentiality/residue | **EXACT-HEAD SOURCE-ONLY at unmerged draft #891 `02105e9`; no helper command, key/header value, request body, backend data, live client or writer was run or read.** The drift checker builds shell curl commands containing the REST target and both key-bearing headers (`qa/ef-writepath/21-drift-check.js:9-18`, `:34-36`). The settings safety-restore writes its client/prompt body to `/tmp/qa-efwp/_restore_prompt.json`, shells out to curl, does not remove the file, and prints `e.message`, which can include the command (`qa/ef-writepath/13-settings.js:64-75`). Both paths sit outside the corrected F173 primitive and its synthetic guard. | Move drift reads and settings restore onto the shared fixed-argv, fileless in-memory HTTP primitive. Keep keys, URLs and bodies out of argv/files/errors/logs; return generic operation-labelled failures; remove legacy residue; and add synthetic marker success/forced-failure/abrupt-exit guards before any TEST-only write-path drill. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F182 | P1 current optional vision-API credential/payload handling | **EXACT-HEAD SOURCE-ONLY at unmerged draft #891 `02105e9`; no API key, screenshot/prompt body, helper command, API response, external network or live data was read or exercised.** `judgeViaApi()` writes the base64 screenshot plus review prompt to `_vision_req_*.json`, places `ANTHROPIC_API_KEY` in shell curl argv, and returns `execSync`'s command-bearing `e.message` on failure (`qa/vision_judge.js:84-124`). The `finally` unlink does not protect the argv/error surface and cannot guarantee cleanup after abrupt termination. | Use native HTTPS with a fixed endpoint and in-memory request/response, or an equivalently fileless fixed-argv transport whose secret header is never process-visible. Return generic errors and retain no request payload. Add synthetic key/body markers covering success, forced failure and abrupt termination; inspect argv, errors, logs and temp residue without making an API call. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F183 | P1 current client Brief async-job lifetime / capability revocation | **POST-F182 EXACT-HEAD CLOUD SOURCE REVIEW at unmerged PR #891 commit `59022d`, reconfirmed in then-current candidate `93fc297`; no browser, backend, token, live data or write was used.** `_syncviewPurgeClientEntrySurface()` cleared `briefPollingState` and `tabSummaryCache` without first clearing the polling intervals retained in the former state or aborting the controllers created inside `fetchTabSummary()` (`index.html:7626-7660`, `:8820-8868`, `:42638-42649` at `59022d`; corresponding symbols persisted at `93fc297`). After pagehide/BFCache suspension revoked the client capability, those detached async jobs could still fetch, update global Brief arrays/cache/state, persist tab summaries and repaint through late continuations. Zeroing the maps lost the handles needed to cancel them; it did not terminate the work. **LOCAL CANDIDATE FOLLOW-UP at `13c042b`:** cancellation now precedes state reset; polling, delayed tab-summary starts, active summary controllers and both Brief-sheet reads share the client-entry generation and recheck currentness before mutation. Both direct and `qa/master` visible boot lanes passed 22/22. The guard used real pagehide and `pageshow.persisted === true`, held all three transport classes, proved abort/late-release denial with zero stale global/cache/localStorage/paint mutation, and observed one fresh verified Brief generation. This is synthetic local evidence, not cloud review. | Put every Brief poll interval and tab-summary controller under the current client-entry generation before any request starts. On client replacement, invalidation, pagehide or BFCache suspension, clear/abort those exact owners before zeroing state; every late response, cache/localStorage write, global mutation and render must prove the same still-current run. Add an actual visible client Brief pagehide → persisted BFCache restore guard with held poll and tab-summary responses, late release, zero stale mutation/cache/paint, and exactly one healthy fresh generation. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F184 | P1 current pre-verification legacy-queue replay on client-link documents | **EXACT-HEAD CLOUD SOURCE-ONLY at unmerged PR #891 commit `adb1bca`, reconfirmed unchanged at candidate `13c042b`; no browser, backend, token, live data or write was used.** Every document installed focus, pageshow, online, visible and 60-second timer triggers for `_writeUiResumeLegacyQueues()`, then called it unconditionally at startup. On any `?c` document, those triggers could run while strict client verification was pending or after it failed/rotated, reaching residual Calendar/Samples Linear outboxes, pending Calendar card jobs, source-repair queues and native-intake debt from a prior same-origin staff/session context. **LOCAL CANDIDATE FOLLOW-UP at `c9a79ef`:** queue inspection is gated by exact client generation/slug/principal or staff identity/verification epoch; client recovery can touch only matching Calendar/SXR rows; finalization rechecks ownership inside the acquired surface lock; retry timers are owner-purged. The actual visible 23/23 lane drives startup/focus/online/visible/timer/pageshow.persisted, an armed retry timeout, a second 500/re-arm, real pagehide/BFCache cancellation, fresh-owner success and a same-slug/foreign-principal byte-identity check. Exact-head cloud review `4741601566` returned no separate F184 thread, but the row remains open until owner merge. | Preserve the one principal-aware gate in front of startup and every focus/pageshow/online/visible/timer resume trigger. Before strict client verification settles, a client-link document must not inspect any persisted queue. After exact-generation client verification, it may resume only Calendar/Samples retry rows carrying the same normalized client slug and compatible principal; foreign, empty/unknown and staff-owned rows remain byte-identical. A client document must never start staff-only Calendar-card jobs, source repairs, Kasper repairs or native intake. Staff-wide recovery requires the current verified staff identity/epoch. Retain the actual visible synthetic-debt lifecycle, per-surface held-lock, real timeout/pagehide cancellation and fresh-owner guards. | OPEN P1 — LOCALLY REMEDIATED IN DRAFT PR #891; FINAL INTEGRATED-HEAD CLOUD REVIEW COMPLETE AT `babbb2d`; OWNER MERGE REQUIRED; PARKED #908 IS NON-BLOCKING BY OWNER DECISION |
| F185 | P1 current hosted client-entry visible-boot runner portability | **EARLIER EXACT-HEAD CLOUD SOURCE REVIEW at unmerged PR #891 commit `f91aba17f8cc30c49aae293addc8df02aa0ab4d2`, review `4759712636`, comment `3634666109`; no credential, client data, backend, browser drill, deployment, flag or writer was read or changed for this finding.** `.github/workflows/client-entry-visible-boot.yml:49-54` caches only Playwright browser files under `~/.cache/ms-playwright`. A cache miss runs `npx playwright install --with-deps chromium`, but a cache hit runs only `npx playwright install chromium`. Restored browser binaries do not carry the ephemeral runner's Linux system packages, so a fresh hosted runner can hit the cache yet lack Chromium launch dependencies. The exact-head hosted visible-boot job passed 23/23, making this a portability/reliability gap rather than evidence of a current product boot regression. The later final product-head review at `babbb2d` returned no new findings and left F185 deferred/non-blocking. | In a separate follow-up PR, install Chromium system dependencies unconditionally or use `playwright install --with-deps chromium` on both cache paths, then add a source guard that rejects a cache-hit path without dependency installation. Do not widen #891 or reopen F176/F179; retain the existing one-attempt visible-sequence contract. | DEFERRED P1 — REGISTERED FROM EARLIER EXACT-HEAD CLOUD REVIEW AND RETAINED THROUGH `babbb2d`; NON-BLOCKING FOR #891 BY OWNER'S SCOPE-FROZEN RULE; FOLLOW-UP PR REQUIRED |

Related non-register outputs of the same run: `ROLLBACK.md` Live State gained the missing
`write_ui_reroute_clients` kill-switch row (the newest live flag was absent from the emergency
runbook); `test/linear-sync-reconcile-extraction.js` now guards the calendar reconciler's eval
sandbox (the `_sxrNormStatus` crash class had a guard only on the samples twin). Full run report:
`docs/audits/2026-07-17-bug-archaeology.md`.

### 2026-07-20 Workload live-release review follow-up (F147–F148)

PR #886 merged the Workload live-release record while two exact-head cloud-review P2 threads
remained unresolved. The effective live schema and private TEST behavior are independently recorded;
these findings narrow provenance and future-regression assurance rather than refuting the live
save/clear result.

| ID | Grade | Evidence | Recommended fix | Status |
|---|---|---|---|---|
| F147 | P2 current release provenance | **CONFIRMED DOC/SHA CONTRADICTION; EFFECTIVE LIVE POSTURE VERIFIED.** The release log attributed the migration and function deployment to exact merge `fd3e0eaa`, but that commit's SQL did not include the explicit service-role DELETE/TRUNCATE/REFERENCES/TRIGGER revoke later committed in `fe07aa8`. Live readback nevertheless shows those revokes effective, RLS/zero policies, no browser privilege, service SELECT/INSERT/UPDATE only, and zero residue. | Recover the private operator command/input or database migration ledger, hash the exact SQL applied, and tie the revoke correction to one immutable artifact/SHA. Correct the release provenance without rerunning the migration or changing live grants unless readback first shows drift. | OPEN — LIVE GRANT POSTURE PROVED; EXACT CORRECTION ARTIFACT/SHA UNRESOLVED |
| F148 | P2 current future-regression assurance | **CONFIRMED CLOUD-REVIEW TEST GAP; LIVE BEHAVIOR STILL PROVED.** The new `test/workload-plan-source.js` regex can match `.select(...)` later in the extracted block without proving it remains on the same fluent `upsert(...).select(...)` chain. A future edit could therefore break true returned-row counting while the guard stays green. The private TEST release still proved one-row save/reload/clear and the pre-write `409` path. The two Workload test files also reuse `F141` in comments/output even though F141 is already the Samples reorder finding; current truth names the Workload guard by behavior instead of treating that collision as a second register assignment. | Replace the regex with a structural extraction or exact chain assertion, add a negative mutant where `.select(...)` is detached or removed and require the guard to fail, and remove the conflicting F141 label from the Workload test comments/output. Keep the live short-count race out of production; hermetic proof is the correct closure. | OPEN — REGEX DOES NOT PIN SAME-CHAIN TRUE-COUNT CONTRACT; TEST LABEL COLLISION DISCLOSED |

### 2026-07-22 staff boot, refresh, browser-history, and BFCache addendum (F186–F199)

Read-only Round 2 audit at `a479cd01e3a2dc83a7b5f29284292879b740d1c4`.
PR #891 is merged and live; its client-facing routes were outside this round.
The new rows below cover only staff surfaces. Evidence is current-source-only
except for one fully intercepted, fictional visible-browser observation added
to existing F155. No application source, product test, harness, backend, live
data, identity, credential, token, writer, deployment, flag, migration, or
workflow was read or changed by the audit. The public-safe evidence and the
owner's one-line decisions are in
`docs/audits/2026-07-22-staff-boot-refresh-history-audit.md`.

| ID | Grade | Evidence | Recommended fix | Status |
|---|---|---|---|---|
| F186 | P0 current protected staff-state lifetime | **SOURCE-ONLY.** The weekly-report model retains rendered reports, manager options, week options, and filters, but the shared staff-identity purge does not clear that model or replace its DOM. The route has no continuing identity lease and no BFCache-return revalidation. Cross-tab sign-out or a persisted return can therefore leave already-rendered protected reports visible after identity loss. No real identity, report, backend, or browser was used. | Put every protected report model, DOM surface, request, and cache under the current verified staff-identity/document generation. On identity loss or pagehide, revoke reads and clear protected state before painting a sign-in boundary; on persisted return, reverify before restoring content. Drive the actual visible sign-out/pagehide/BFCache sequence. Owner decision: OQ-R2-01. | OPEN — BLOCKER; OWNER MUST SELECT REMEDIATION SLICE |
| F187 | P1 current Production Back/Forward correctness | **SOURCE-ONLY.** Production URL parsing changes team, issue scope, and project-detail state only when each query parameter is present. Its history payload omits those fields. Back to a URL that omits one can therefore retain the later in-memory value, producing a default-looking URL with filtered or collapsed content. | Define canonical defaults in one Production route descriptor and fully replace route state on every parse/popstate rather than patching only present fields. Guard visible Back/Forward through nondefault → default entries. Owner decision: OQ-R2-05. | OPEN — IMPORTANT; ISOLATED PRODUCTION ROUTE FIX |
| F188 | P1 current Production async lifetime | **SOURCE-ONLY.** Production core and silent reads have no generation/abort owner. Silent refresh leaves the loading lock false, allowing overlapping focus/pageshow loads; every completion publishes unconditionally. An older response can therefore become the visible winner after a newer return refresh or route re-entry. | Give Production one route/document generation, abort owned reads on exit/pagehide, and require exact-generation checks before state/cache/render mutation. Guard held old response → fresh return response → late release. Owner decision: OQ-R2-06. | OPEN — IMPORTANT; ISOLATED PRODUCTION LIFECYCLE FIX |
| F189 | P1 current Workload reload freshness | **SOURCE-ONLY.** A cache considered fresh for five minutes can satisfy cold Workload boot without an immediate watermark read. The automatic watermark check starts on its interval, so a hard reload can present an older schedule for up to the first 60-second tick. | Preserve fast cached paint only with explicit last-confirmed labelling and an immediate non-destructive watermark revalidation, or adopt the owner's chosen reload-bypass policy. Guard visible cache paint → immediate check → atomic replacement/error. Owner decision: OQ-R2-08. | OPEN — IMPORTANT; ISOLATED WORKLOAD BOOT FIX |
| F190 | P1 current Workload BFCache lifetime | **SOURCE-ONLY.** Route teardown clears timers but does not abort or generation-invalidate an in-flight watermark/snapshot run; persisted `pagehide` invokes no Workload teardown. The old busy/in-flight state can therefore survive, suppress the return check, and let the pre-hide continuation publish. No browser-frequency claim is made. | Start a new Workload document generation at pagehide/persisted pageshow, abort or retire the old work, and require exact-generation checks before every state/cache/render write. Guard real BFCache with held old work and one fresh replacement. Owner decision: OQ-R2-07. | OPEN — IMPORTANT; ISOLATED WORKLOAD LIFECYCLE FIX |
| F191 | P1 current weekly-report filter ownership | **SOURCE-ONLY.** Weekly filter changes start overlapping reads without an abort controller, request generation, or captured-filter owner. A slower prior response can overwrite the newer selection and paint the wrong or false-empty report set. This is distinct from F157 history serialization and F167 timeout/read-truth. | Make each filter descriptor own one latest request and publish only when both route and filter generation still match. Retain or replace last-good content according to OQ-R2-09, with visible failure and Retry. Guard out-of-order responses. | OPEN — IMPORTANT; ISOLATED WEEKLY-REPORT FIX |
| F192 | P1 current uploader read truth | **SOURCE-ONLY.** Both TikTok queue renderers mark the queue as loaded before the first network read. Their first-load error branch is consequently unreachable after a failed HTTP/network read, and error-shaped success bodies normalize to an empty list. Unavailable queue data therefore looks authoritatively empty on both uploader surfaces. | Use a typed checked-read result with explicit unknown/loading/error/last-good/empty states. Never set loaded before a validated envelope; expose Retry and preserve labelled last-good rows only if the owner selects that policy in OQ-R2-03. | OPEN — IMPORTANT; TWO ISOLATED UPLOADER READERS |
| F193 | P1 current TikTok Upload readiness | **SOURCE-ONLY.** Direct Upload boot mounts before the shared client/account map finishes. A restored selection can resolve to missing, paint a blocking “No account” state, and remain there because the shared essentials-ready callback does not repaint Upload. Incidental interaction is the only self-heal. | Represent account mapping as unknown/loading/present/missing, then repaint exactly once when the shared map becomes authoritative. Do not render missing from an unready map. Guard a held map read through visible resolution. Owner decision: OQ-R2-04. | OPEN — IMPORTANT; SHARED READINESS HANDOFF |
| F194 | P1 current first-party uploader selection ownership | **SOURCE-ONLY.** Creator constraints use one global state object. Client switches and OAuth return can start overlapping, non-abortable reads; a late result for the prior selection can replace the current selection's constraints. | Bind creator metadata, request, and render to the exact selected-client/compose generation; abort or ignore superseded work. Apply the owner's session policy from OQ-R2-12 and guard late prior-selection release. | OPEN — IMPORTANT; ISOLATED FIRST-PARTY UPLOADER FIX |
| F195 | P1 current staff caption-settings readiness | **SOURCE-ONLY.** Caption-prompt loading is fire-and-forget and unbounded. The editor can state that no custom prompt exists while the read is pending/failed, permitting an overwrite; Generate awaits the same unresolved read before creating visible job state and can hang without a terminal surface. | Add explicit prompt unknown/loading/ready/error states, bounded checked reads, visible Retry, and an owner-selected fallback policy. Prevent editing/generation from treating unknown as absent. Guard editor and Generate visible sequences. Owner decision: OQ-R2-13. | OPEN — IMPORTANT; ISOLATED CAPTION/SETTINGS FIX |
| F196 | P1 current Submit/intake draft isolation | **SOURCE-ONLY.** Staff Submit and the public intake entry restore the same draft key without a principal/surface namespace. In one browser, either surface can boot with the other surface's unfinished fields. No real draft or browser was read. | Separate or explicitly principal-bind the two draft stores, migrate only a deliberately compatible record, and prove reload/Back/second-surface isolation with fictional fields. Owner decision: OQ-R2-14. | OPEN — IMPORTANT; ISOLATED STORAGE FIX |
| F197 | P1 current admin-workspace notification truth | **SOURCE-ONLY.** Time Off and onboarding counts begin as a nonnumeric unknown placeholder. The combined More badge coerces unknown/error to zero and hides itself, so pending or failed notification reads are indistinguishable from a confirmed zero. | Model notification count as unknown/loading/ready/error rather than deriving it from rendered text. Keep unknown/error visible and retryable according to OQ-R2-10; sum only validated counts. Guard partial success and total failure. | OPEN — IMPORTANT; SHARED ADMIN COUNT FIX |
| F198 | P1 current admin-workspace BFCache freshness | **SOURCE-ONLY.** The admin workspace's focus/pageshow return owner refreshes only Review, Messages, and Samples. Time Off, onboarding, filming, editor metrics, and credentials can return from BFCache with pre-suspension data and no automatic freshness boundary. | Give each More-menu surface an explicit return policy under one shared document owner: force refresh, labelled last-good refresh, or deliberate retained draft. Reverify protected projections before repaint. Owner decision: OQ-R2-11. | OPEN — IMPORTANT; SHARED ADMIN LIFECYCLE + PER-SURFACE POLICY |
| F199 | P2 current Hook Library receipt truth | **SOURCE-ONLY.** Reloaded “Saved” state is local-only. The save has no deadline, accepts every HTTP 2xx without a typed success envelope, and persists success; a held request remains stuck on “Saving.” | Require a bounded typed server receipt before durable “Saved,” retain visible Retry/unknown outcome, and reconcile local deduplication according to OQ-R2-15. | OPEN — NICE-TO-HAVE; ISOLATED ACTION FIX |
