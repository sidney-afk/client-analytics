# Atomic save and cutoff preparation checkpoint

Preparation only. Nothing merged, deployed, installed or activated. The owner
approved internal persistence changes preserving client URLs, links, payloads
and tokenless access, plus retained verified receipts with zero Linear work.
This checkpoint is not the final installation handoff.

## Actual handler and database evidence

Both captured serving handlers now have separate preparation candidates that
commit the card, comments, events and two durable follow-up intents together.
The original captures and repository authentication refusal controls remain.
The source composer preserves production imports and `Deno.serve`; SDK-injected
test files are not release artifacts. The bundle generator writes separate
function trees and refuses an existing output directory.

Final portable PG17 handler receipt:
`linear-exit-card-atomic-handlers-f4588674c3bd44aaacb8095f98dcfe79`.
It proves both captured tokenless saves, both repository 401 controls, atomic
row/event/task admission, rollback on an actual event-trigger fault, conflict
409, explicit maintenance 503, and zero external fetch attempts. The disposable
server stopped. This is isolated handler/SDK-to-SQL evidence, not hosted REST.

Production candidate SHA-256 values:

- Calendar: `379e7fd9e7120f0ff3c5af41a90a578f2965dfb80a09f6b491804f4a5db349dd`.
- Samples: `5b877a4bf02c8e6203afd5e2bd944cbb7d757647cbb68a75ec306925a3236465`.

The SQL prerequisite includes the captured current Calendar merge owner
(`31663a6b62bbc96efe85a8cdc8485c42f07f9455b5c043e2112ff07e36bba429`).
Its timestamp assignment differs from the older repository owner; tests must
not silently substitute the older body. Captured Samples v50 and its dependency
are now public, hash-pinned fixtures, so this lane needs no private source path.

## Admission and background work

The initial atomic admission SQL passed PG17 receipt
`6f8e48e29d474a32a08007379a4a0211`. The extended application DML boundary passed
`f5951f9e254443288fdcf9067d555f0f`: 86 table guards, direct-write/cutoff race,
exact flag controls, protected worker row scope, same-transaction completion,
and an epoch-rotating reopen that retains accepted work and refuses stale
cutoff requests. New private tables bring the relational inventory to 90.

The actual source-derived helper, Deno runtime and pinned PostgreSQL driver
passed receipt `linear-exit-followup-worker-026218a03e6c437096493a2abc5783b8`.
Four tasks with no graphic transition completed through real transactions
under the closed gate; the SQL owner independently verified those no-op reasons.
The context table was empty afterward and sealing remained non-activating.
Helper candidate SHA-256:
`840e765c19b6f89afdb18628dfe45c9ce439dc999f0da7e5ba409bd7cdcf4f00`.
That first receipt did not prove graphic capture or resolution.

The final four-owner worker receipt is
`linear-exit-followup-worker-e182b29247d14f7e84f00a855b73e832`.
It proves six completions, including actual source-derived graphic capture and
changed-resolution database effects under the closed gate, plus an intentional
SQL failure that rolls revision rows back and remains a durable failed task.
A stale attempt changes neither rows nor immutable objects. Storage/Drive
transports are synthetic; no hosted image or external provider was touched.
The real driver also verifies bounded standalone queries: service role,
seven-second statement timeout and five-second lock timeout. Runtime and all
four SQL source hashes match before and after the run. The server stopped.

The fourth owner supports only exact existing F27 discard/quarantine under
closure, with preimage checks, preserved native/drill evidence and durable
disposition history. Receipt `0fa74b00f6714360857352138ca0661f` passed two
dispositions and eleven refusal/rollback controls. It does not prove provider
success, external-worker fencing or creation of a new F27 snapshot after closure.

Admission preflight receipt
`linear-exit-admission-preflight-1f9a1fdd85a74ac99bee193de162f7cf`
checks all four owners: 17 functions, four private tables and 173 triggers,
including execution grants, function bodies/results/settings, column metadata,
RLS and deferred context consumption. Five actual catalog mutations are refused,
with a fresh successful comparison after each rollback. Twenty-eight offline
corruption checks also pass. These are source-specific checks, not a replacement
for the broader existing deployment preflight or hosted pre-state review.

Worker preparation binds the database-owned payload to one transaction, keeps
database errors fatal even if the source helper catches them, and refuses an
unconfirmed completion. Ambiguous commit results require ledger readback, not
automatic retry. Snapshot uploads use immutable content-hash paths, no overwrite,
and byte readback; stale attempts may leave unreferenced immutable objects.
The positive graphic-effect test above uses actual SQL and synthetic external
bytes. Hosted credentials, Storage access, cadence and monitoring remain later
configuration and acceptance work.

A separate self-contained private worker bundle is now prepared. Its endpoint
is disabled by default, requires a private runner key, binds database and Storage
to the configured project, claims only one task per invocation and exposes
bounded health counts. The source bundle passes Deno type checking with a frozen
dependency lock. No endpoint or schedule was installed. Offline endpoint tests
cover authorization, disabled behavior, bounded bodies and ambiguous failures.

## Preserved failures and remaining build work

Handler rehearsals first exposed an invalid synthetic column and missing
source-owned Samples grants; both fixtures were corrected without relaxing
constraints. Initial actual worker receipts `82527e2cb7814ab8b94703d72f93bdcf`
and `498d67fdcf25455a96fcf1d61c3206e3` failed because the driver encoded an
already-stringified JSON argument as a JSON string. Native object parameters
fixed the defect; the later receipt above is the passing result.

The final four-owner 90-table recovery now passes; see the complete-data
checkpoint. Still required: safe retry/recovery for failed or unknown follow-ups,
unsupported helper outcome proofs, external provider-worker fencing and replay,
closed-gate F27 snapshot creation, complete release ordering/resume integration,
source/caller coverage beyond table DML, operational worker configuration and
cadence, a live object-export adapter and independent custody, notification
handover and hosted compatibility checks. WR-101 remains its separate reviewed
release. These are explicit remaining requirements, not waived by this checkpoint.

Direct sequence operations, DDL, newly created tables, Auth and Storage are not
covered merely by the 86 application-table guards. Existing accepted work from
before durable admission needs its own reconciliation. The retirement activation
routine remains refusing; no green isolated test authorizes installation.
