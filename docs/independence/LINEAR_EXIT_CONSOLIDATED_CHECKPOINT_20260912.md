# Consolidated Linear-exit preparation checkpoint

Later preparation: see `LINEAR_EXIT_OBSERVED_BASELINE_AND_JOURNAL_20260912.md` for the exact observed starting catalog, routine reconstruction and durable installation journal. The pinned checkpoint below remains historical.

This is a preparation checkpoint, not a finished installation build. It advances
`LINEAR_EXIT_RECOVERY_AND_EXPORT_CHECKPOINT_20260912.md`; earlier receipts remain
historical. No merge, deployment, installation, production write, workflow dispatch
or n8n execution/edit is authorized. Installation and retirement remain HOLD.

## Approved shutdown meaning

Keep verified SyncView save/completion receipts. Require zero work still destined
for Linear and zero unresolved provider debt. Unknown, malformed or failed work
still blocks shutdown. Narrow atomic-persistence preparation inside Calendar and
Samples is approved; preserve current URLs, payload compatibility, review links
and tokenless access. These decisions do not authorize activation.

## What this checkpoint adds

The seventh SQL owner aligns the two existing Drive URL parsers and graphic-mode
configuration lookup with the frozen helper. The transactional adapter now supports
the exact non-null database filter used to initialize a graphic watch. Unsupported
normalizations and uncertain outcomes still refuse. No captured writer/helper was
modified. The migration is `20260912200637_thumbnail_parser_contract_alignment.sql`,
SHA-256 `ae372bbb1b62df37d273c4539a6efe255bf4a7ed415149fbbb73b423dba63f4e`.

Actual PG17/Deno receipt `776a825d93e140e7b37d0a11469b533e` passes all 14 helper
cases, 15 parser fixtures, padded-mode/client normalization and field/history
preservation, with zero real external requests. Previous missing-filter and URL
failures remain recorded; the final matrix resolves them through actual SQL effects.
This is isolated evidence, not a hosted save or production graphic download.

Independent source review found a mixed-path counterexample:
`/document/d/AAAA/file/d/BBBB` selected `BBBB` in the frozen helper but `AAAA`
in SQL, including the original pending watcher. The final seventh owner fixes
ordered matching in both SQL APIs. Actual mixed-path pre-save watcher, baseline,
initialization and resolution checks pass. Earlier `4833d6a444d246ba9f33e5092b520dbc`
is historical; 15 passing fixtures do not claim universal URL equivalence.

A portable supervisor is prepared and disabled by default. It bounds concurrency,
request deadlines and polling, leaves failed/unknown work visible, and stops on
cancellation or authentication refusal. Offline tests pass, including literal
boolean enablement. No schedule or independent observer has been installed.

## Database stages and remaining mismatch

Source-only receipt `416c9af385ed4215924b46ce94aa7b79` passes the 67-to-86 table
transition without application fixture DDL or synthetic rows. It preserves 99
source pins and 31 after-entry fingerprints. The existing `description_images`
owner is explicitly applied once before the transition because that table already
exists in the observed live database. The original 64-entry inventory is unchanged;
its candidate classification is not an instruction to replay an existing owner.

The prepared starting schema has 67 tables and 86 functions. A separate read-only
hosted observation found the same 67 table names but 115 functions. Counts and table
membership do not establish full routine/catalog compatibility. Map exact missing
or alternate source owners before treating the prepared starting schema as a hosted
installation baseline. Do not invent missing DDL or use application test fixtures.

The separately pinned post-candidate catalog contains 86 tables and 178 functions.
It covers raw and normalized function definitions, defaults, permissions, triggers,
indexes, constraints, policies and dependencies. Its stage is explicitly
POST_64_OWNER_PRE_ADMISSION. Its passing comparison does not satisfy a pre-64 gate.
Catalog receipt `cf3ebf161ed748f8b14ce424691c003c` includes ten actual mutation
refusals; 20 offline checks pass. Raw hashes catch carriage returns inside literals
that normalized hashes could miss.

## Final seven-owner acceptance

- Admission receipt `f5323c1dba2c47df9c3437579f33fbe3`: 22 functions,
  four private tables, 173 triggers; seven actual mutations refused, including a
  semantic carriage-return change hidden by normalized hashes. Thirty offline
  corruption checks pass. Contract SHA-256:
  `8980fc5124f9d86064536e55f65f02eedb68cc200e53a266a2a2cd96a9c412f7`.
- Complete V2 recovery receipt `bb96297cfe46486789efe6d9714d30f8`: 90 tables,
  89 populated, empty transaction context; retained failure/retry history, exact
  reconstruction, corruption rollback and encrypted reopening pass. Artifact SHA:
  `c91b0800ef571049d9099d120d3de3e0203e4ca802936d3934aefb43924b0817`.
- Both servers stopped. The final worker bundle passes frozen-lock Deno checking;
  writer candidates retain the previously reviewed handler bytes and tokenless
  settings. No endpoint or schedule was installed.
- Seven-owner source extension SHA-256:
  `7411fe784373a84a9682ec84ed183ebc6571ff06e6776605b5291aadee36f8c6`.
  It includes the prepared supervisor. Source/order drift tests and the 73-entry
  non-executable source plan pass; this does not prove interruption recovery.

## Storage observation and export limits

Read-only September 12 SQL observed two buckets, both with versioning disabled,
and 1,038 current object records; no archived objects, delete markers, versioned
objects or unknown flags were observed. No hosted object bytes were downloaded.
This is a point-in-time observation, not a storage closure certificate.

The export adapter now requires fresh read-only capability observations before and
after capture/export and matches bucket/object counts to the API inventory. It
refuses unsupported history or changed observations. The authenticated inventory
and encrypted metadata sidecar retain this evidence. The 102-object loopback test
passes exact decrypted bytes/metadata, pagination and refusal controls; older V1
export callers remain compatible. Database/API project binding, required SQL asset
reference coverage, atomic cutoff, independent key custody and hosted restore are
still unproven.

## Finish preparation in this order

1. Reconcile the starting schema's exact functions and metadata with authorized
   read-only hosted evidence. Keep observed alternatives explicit.
2. Build and rehearse the actual installation/resume procedure, including failures
   inside migrations with internal commits. The existing source plan and prefix
   classifier do not execute SQL or authorize resumption.
3. Finish external Linear worker fencing, safe replay/disposition and reconciliation
   of earlier accepted work. Table DML guards do not cover every platform writer.
4. Complete required-asset coverage, independent recovery/key custody and the
   notification configuration, ownership and delivery/recovery handover.
5. Resolve the final retirement activation protocol and the separately scoped
   WR-101 Edge Function preparation. Keep activation refusing until its gate passes.
6. Publish/review the resulting exact build. Hosted installation and activation
   require separate owner authorization and their own acceptance evidence.

Do not restart the broad audit, chase unrelated newer main changes, use Docker as
a prerequisite, relax failed assertions or describe this checkpoint as the full
build. Portable PostgreSQL 17 is available for the isolated lanes. Private receipts
and generated bundles remain outside the public repository; the committed source
and sanitized evidence are reproducible, but the private files are not implied to
be attached to GitHub.

Publication verification preserves exact tested bytes for three catalog/inventory dependencies through explicit Git attributes: the native sign-off SQL owner, source catalog SQL query and remaining-application fixture. This prevents checkout line-ending conversion from invalidating their existing hashes. No source logic or catalog assertion was changed for packaging.
