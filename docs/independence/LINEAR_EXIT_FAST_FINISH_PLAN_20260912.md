# Fast completion plan ? 2026-09-12

Latest continuation: read `LINEAR_EXIT_CLAUDE_HANDOFF_20260912.md` first. The private-control recovery profile now passes an integrated encrypted capture/reopen/restore of 94 tables. Checkpoint, acknowledged update/comment recovery and the separate WR-101 preparation have isolated SQL proof. These newer owners still need composition into the exact observed installation plan; the older 35-source target does not cover them. Historical source reconstruction is rehearsal scaffolding, never an instruction to replay baseline owners on the hosted database.

Owner request: finish the preparation build and strategy economically, then
provide a handoff for a fresh review before installation. No merge, deployment,
workflow dispatch, production writes or n8n execution/edit in this session.
Existing tokenless links, URLs and payload compatibility remain unchanged.
The owner approved narrow writer-internal preparation on September 12; original
serving captures remain immutable controls. No installation is authorized.

## Execution order and stopping criteria

Current critical path after the published 42-source calibration (September 13 update):

Recorded create and comment recovery are published. The final issue/attachment
observer is frozen and independently reviewed, with 56 isolated PG17 checks and
10 offline transport checks passing; publication is pending. Guarded retirement
and separate native reopening have a published 29-check component proof. The
retirement recovery profile has an encrypted 94-table proof (`cfbf66455be44f98ad49e42fce2a4cb0`),
which predates the final issue observer. External-worker and notification handover
procedures are prepared. These component results do not replace final integration.

Remaining execution: publish the reviewed observer, refresh exact switch and
recovery metadata, run the expanded post-reopen business matrix, then calibrate
and replay the complete packaged installation in a fresh database. Consolidate
the actual final evidence and release matrix into the reviewer handoff. Retain
all separately authorized hosted acceptance gates.

1. Finish the recorded batch/non-F203 recovery review and replay regression. Reuse
   immutable V2 context, exact source receipts and narrow linkage; do not resend or
   invent unrecorded history. Finish operation-specific recovery for other recorded
   uncertain responses; ambiguous inputs must remain unresolved rather than be waived.
2. Reuse existing ordinary native receipt owners and the repaired retirement
   recognizer. Implement guarded retirement and a separately guarded native reopen;
   the existing reopen refuses retired state. Prove permanent provider-send refusal
   and actual ordinary writes after reopening. Reuse the existing business fixtures
   instead of duplicating their tests. External evidence bindings are not proof of truth.
3. Run current-public recovery with the reviewed new owners and exact observed F203
   prerequisite. The existing 94-table encrypted recovery profile remains evidence;
   newer public routine coverage is an additional integration requirement.
4. After final owners freeze, calibrate their target once and replay it in a fresh
   database through the packaged entry, including full preflight and interruption/
   recovery boundaries. The earlier 42-source/49-chunk calibration is not final
   acceptance. Avoid rerunning an expensive final replay before pending owners land.
5. Finish remaining asset/access/custody and notification handover preparation,
   then publish one reviewed full preparation revision and update the handoff. Fresh hosted
   observations, external-worker coverage, independent key retrieval and actual
   installation/activation remain separately authorized acceptance steps.

The older steps below explain the preparation already consolidated; they are not
an instruction to restart completed audits or re-run every historical lane.

1. Close native sign-off integration: actual isolated SQL-backed initial reads,
   protected receipt verification, planning and pre-write revalidation. Retain
   provider regression and precise later-reopen/refusal tests.
2. Finish installation composition: add the verifier with explicit dependencies
   to the pinned source inventory and required preflight surface. Run ordered
   composition and relevant resume/recovery checks once on the final revision.
3. Resolve the installation-versus-cutoff boundary. Produce one release matrix:
   what can be installed additively, what must remain dormant, and which exact
   proofs authorize later removal. Do not activate retirement through a failing
   freeze or silently reinterpret the canonical B5 gate.
4. Close remaining installation-critical code gaps identified by that matrix.
   Consolidate existing evidence instead of restarting the historical audit.
   Treat backup completeness and a tested inverse as requirements; prefer a
   supported full backup/restore route over inventing per-table formats where
   that can satisfy the same requirement. No exclusion of data is implied.
5. Run one targeted final acceptance pass: native intake, ordinary lifecycle,
   client save/approval, sign-off completion, notification handover preparation,
   source-exact installation and recovery. Test retained frozen serving contracts
   correctly. Broaden only for a concrete failure or affected dependency.
6. Publish exact code, source inventory, evidence index, release matrix and one
   short reviewer handoff on the draft PR. List unresolved owner-window checks
   explicitly. A quick review is possible only if no undisclosed build gap remains.

## Deliberate time savings

### Continuation after the published checkpoint

The published preparation checkpoint is `6050f67ba750bdde819c795e30f4446c011acaaf`
(code `2de445389b578d2307c05ece7f0a540734d29725`). Publishing it did not complete
the active build goal. Continue along these dependencies rather than reopening
the historical audit:

1. Transactional follow-up recovery: settle the task lock before deciding whether
   a lost connection committed, preserve completed receipts, and refuse legacy
   attempts without the approved transaction protocol. Then cover remaining helper
   outcomes. Do not turn expiry into proof of success.
2. Closed-gate F27 snapshot creation and source-bound disposition: reuse the exact
   existing owner with a narrowly scoped context. Keep external worker fencing and
   provider replay separate; a local snapshot cannot attest that workers stopped.
3. Integrate the new SQL owners, writer/worker artifacts and original source inventory
   into one installation order and observed-prefix recovery procedure. Batch catalog
   and complete recovery checks after related owners stabilize.
4. Connect the existing object custody/export engine to read-only Storage transport,
   preserve object metadata and reconcile application history references. Determine
   backend version-history coverage explicitly. Prepare worker cadence, notifications
   and custody handover without enabling any of them.
5. Complete the final switch implementation only when its source/caller coverage is
   resolved. Run affected acceptance checks, publish exact bytes and rewrite the final
   handoff around remaining owner-window observations rather than hidden build gaps.

Parallel work is limited to independently owned gaps. Reuse the already passing
atomic handlers, portable PostgreSQL and existing recovery engine. Do not repeatedly
rerun unrelated lanes as each documentation or source-pin change lands.

### Existing savings retained

Reuse the existing isolated PostgreSQL and captured-handler harnesses; no Docker
repair, repository-wide re-audit, UI polish, wholesale documentation import,
unrelated schema cleanup or redundant full-suite runs. Keep optional improvements
outside the critical path. Preserve original failed evidence instead of rewriting
history. Reuse the already prepared code rather than reconstructing old bundles.

## Non-negotiable acceptance

No loss of accepted saves, comments, approvals or required history/assets. Native
receipts must be genuinely admitted, not merely shaped like valid JSON. Preserve
rollback/recovery and explicit failed reads. No synthetic test is hosted proof.
Installation and actual Linear retirement are separate release decisions; do not
call removal ready while final admission/drain, caller cutoff or custody evidence
is missing. The old activation RPC stays refusing until its prerequisites are met.

Current baseline: ab474961 (code9701c3a4), draft PR1382. This plan is the first
artifact of the resumed run; results will be recorded separately against exact
revisions. Completion is not claimed by writing this plan.
