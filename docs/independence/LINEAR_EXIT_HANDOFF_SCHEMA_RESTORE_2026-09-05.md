Continue in the existing card-history/restore review Claude session.

Repository: sidney-afk/client-analytics. This is an isolated implementation/review task under the coordinator's existing Linear-exit program, not a new exit audit. Read current AGENTS.md completely, including the frozen anonymous-client-writer directive, then the owning truth/runbook documents and deployment manifest. Work from the exact base below in your own checkout/branch; record main drift without integrating unrelated changes.

No production or TEST data access/mutations, merges, deployments, migrations applied to a real service, flag changes, n8n edits/execution, provider calls, credentials/billing changes, alerts, or scheduled check-in loops. Local disposable databases and fully intercepted synthetic browser tests are allowed. Never expose client identities, secrets or sensitive records. Preserve historical commits and failed receipts. Frozen calendar-upsert/sample-review-upsert and anonymous access must remain unchanged. SyncLinear must never create subissues.

Deliver a clean separately stacked draft PR, exact head/base, meaningful baseline-fails/candidate-passes tests, client-visible behavior, rollback, and explicit unproved serving/live gates. Do not broaden scope or weaken tests to get green. Stop at a finite handoff; the coordinator owns integration and release.

Base: PR1311 at aab2acd23112f7bdff849a9c0b68306d41bbf62c, stacked on preserved PR1299. Verify this exact head before work. Read docs/ops/TRACK_B_BACKUP.md, docs/audits/2026-09-05-card-history-restore-correction.md and the actual package/restore tools.

The known dependency-corpus defect is already corrected with explicit history-v5,33 tables, RESTRICT, exact crosswalk/replay/intake/F27 recovery coverage, while v3/v4 meanings remain unchanged. Do not repeat that audit or weaken atomic journal capture. The initial hosted v5 failure was a disposable-role password bug; this base fixes it and has19 real SQL checks plus4 wrong-password controls on a SCRAM-authenticated local cluster. Hosted evidence is separate.

Your task is the remaining F-B blocker: produce an authenticated schema-and-data recovery package and prove reconstruction into a genuinely empty disposable target. A schema hash or data-only COPY into a prepared fixture is insufficient.

First define the exact recoverable boundary: schema objects, required extensions/platform prerequisites, roles/grants/RLS, functions/triggers, sequences, dependencies, matching data and version compatibility. Capture schema and data from a coherent database snapshot or an explicitly enforced DDL boundary; the existing separate catalog preflight does not prevent a DDL race. Never rewrite historical package meanings. Use an explicit new artifact/version where necessary and retain old readers/packages.

Implement capture, verification and reconstruction using disposable synthetic sources only. Preserve confidentiality of function bodies and records; never include passwords or provider credentials in a public fixture/artifact. Authenticate the schema, data and their binding; reject mismatched/tampered/incomplete packages before destructive target work. Require an independently verified scratch target, no unbacked CASCADE and no provider/n8n egress. Keep the restoration role genuinely restricted and prove actual password authentication.

Tests must include empty-target reconstruction without manually repairing migrations, tampering and mismatched schema/data, missing dependencies, unexpected target state, failed restore/recovery, exact restored rows/keys/sequence next values, intended RLS/grants/trigger behavior, and replaying an already accepted comment mutation without a duplicate action. If managed-platform primitives cannot be recreated locally, explicitly pin and verify them as prerequisites instead of fabricating a full-platform restore claim.

Define dormant watcher outputs for package freshness, schema mismatch, verification/restore failure and retained coverage, but do not install scheduling or send alerts. Cloud retrieval, real installed-schema parity, asset-byte rescue, key custody and elapsed retention remain separately unproved.

Deliver the smallest executable package/restore change and a dependency-ordered draft handoff. This does not authorize journal installation, billing changes or Linear shutdown.
