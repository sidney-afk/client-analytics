# Linear exit: bounded release preparation

This packet implements the current owner scope in [the canonical checklist](GO_LIVE_CHECKLIST.md).
It is a preparation record, not an executable release approval. Source engineering
still remains in the rows below. No merge, deployment, live/TEST write, n8n edit,
flag, credential, billing, cancellation, deletion or new alert is authorized.

## Decisions already settled

- Keep current client, Kasper and SMM workflows and needed content usable.
  Unused historical export is optional; PR1327 remains paused. Delete nothing.
- Keep Submit available and distinct from SyncLinear. The normal video path is
  Kasper request -> editor -> For SMM Approval -> SMM -> Kasper Approval.
- Preserve existing tokenless Calendar/Samples links. Native markers select a
  protocol; accepted server manifests and receipts govern native creation.
- Keep SyncViewbot primary. Owner screenshot confirms both September5 drills.
  This does not prove recurring monitoring or future incident acknowledgement.
- Prepare a short extension of Linear access. September15 remains scheduled;
  nothing in Billing has changed. No observation clock has started.

## Pinned completed work

Runtime [PR1326](https://github.com/sidney-afk/client-analytics/pull/1326) is
`d12d761d309cc0bc98bbefb597afa3c62516b54e`, runtime parent59b. It combines d144
with Calendar/Samples refused-request retention, saved-native Submit routing
and retained Workload admission. Root and named post-publication review pass.
Combined focused groups59/16/72/58 pass; map507/truth542 pass. The prior459-suite
hosted result is d144 evidence, not inherited by d12. Current d12's seven
applicable hosted checks now pass; its two optional polish jobs remain skipped.

Tooling [PR1329](https://github.com/sidney-afk/client-analytics/pull/1329) is
`ef0a7d0bf2e14c540b6193fa6d6eba1048f664f2`. Editor handoff and source-feedback
cells pass8/8 on combined59b, including refused save and missing feedback.
Tooling source is unchanged from testedbddd; a later README records the receipt.
Real backend authorization, media quality and live persistence remain unproven.

## Remaining work and proposed action boundaries

Staged operations [PR1332](https://github.com/sidney-afk/client-analytics/pull/1332),
`cfb042aca6394edc0f6f9c4ebab928b1e223f806`, contains the separate frozen-writer
builder and independent receipt/heartbeat consumer. Named source review and root
focused checks pass. It installs nothing; hosted publication checks are pending.

| Stage | Concrete preparation still needed | Approval action and required readback | Recovery / stop condition |
|---|---|---|---|
| 1. Source and recovery | Final composed tokenless closures; installed-schema/catalog/roster binding; complete v8 custody; exact additive migration order and disposable inverse. Current d12 source inventory has32 files,11SQL migrations and7 modified function entries; it is not installation order. | Later approve exact source/SQL/custody hashes, one installation step at a time. Verify installed definitions/grants and complete served entry/shared/JWT closures. | No installation without verified recovery. Stop on source drift. Preserve new owners, receipts and human edits; reverting source is not restoring data. |
| 2. Old routes | F44 needs a server-owned legacy-key/native-acceptance mapping and compatible completion. Both n8n card aliases need exact raw-body/marker forwarding and terminal response before all old write tails. Sanitized graphs are insufficient. | Later approve exact two graph patches plus F44 adapter only after old-payload rehearsal. Capture current raw exports first and verify exact returned versions. | No manufactured actor/key/created response. Retain unknown/partial accepted debt. Never resume old whole-row writes over native edits. |
| 3. Required media | Reconcile the bounded native-brief references with all other offered comment/card readers. Prepare private independent bytes, exact occurrence/source revision mapping and a reader that uses those copies. | Later approve a bounded copy/repair with exact source/destination, content hashes, audience/access checks and CAS. Verify every copied byte and actual staff/client rendering with provider denied. | No blanket exclusion by status/age. A private archive Drive link does not supply an inline native image. Retain original content/mapping; never substitute missing media with a claim of completion. |
| 4. Workflow and monitoring | Install-compatible release plus exact reserved TEST identities and provider-denial observer; independent receipt checks, backup delivery and incident acknowledgement/escalation. | Later approve one TEST window and separate monitor activation. Run the finite cases below and actual failure/recovery/missed-run drills. | Stop on any real-client mutation, lost/duplicate action, unexpected provider request, broken existing link or unobserved alarm. Restore compatible native behavior and reconcile receipts. |
| 5. Product cutoff | Current provider-root graph, both outboxes and inbound debt, CON/STR disposition, old isolates and in-flight outcomes. Workload control/grants precede handler serving. | Later approve exact scheduler/n8n/CAS actions and then separate credential removal. Read back stopped roots, conserved debt and zero new intents/attempts while native client saves continue. | Never globally stop client saves. After cutoff keep the admission fence; do not restore the old unfenced Workload handler. |
| 6. Observation | Independent working alarms and all earlier gates. | After product credential removal observe72 hours; owner records Decision A. Then retain controlled workspace access at least14 more days. | Restart relevant proof after a material incident. Timer expiry never authorizes cancellation or deletion. |

The current32-file inventory receipt is SHA256
`5557f17bdfd1dd9abea6b61c33f6e952ef486297eb3488225f703f38cd723c56`;
the earlier31-file d144 inventory remains intact.

The staged anonymous composition uses captured Calendarv48/Samplesv49 plus
the existing native materialization and thumbnail helpers: four unique files,
three per closure. Its builder preserves all captured bytes outside five
reversible native additions. Its38 full-handler/model controls pass. One
disposable HTTP/SQL run passed42 groups and64 loopback requests with zero
external attempts; four groups directly exercise the exact staged anonymous
handlers. The broader native matrix in that lane still uses repository-auth
handlers. Private report SHA256
`829919dbcadd0307f4abaa6fc3c8db800705588cef45c08e87ad8789a4ef8b58`.
Current serving equality, full staged native matrix and old n8n payloads remain
separate gates; do not transfer the old derived graft's certificate or deploy
repository writer copies as-is.

## Required media: first concrete bounded input

LIVE_READ at September7 15:27:14Z selected all native deliverables with an active
client and literal Linear upload references in `brief`, across all statuses:
486rows,1338occurrences,1167 distinct URL hashes. Private receipt SHA256
`8ed45da9de45b188022df2626735c4c6644d9c1e3c3af2cde7570426c6d208b2`.
Only row/body/URL fingerprints and metadata were saved, no raw bodies or URLs.
Nothing was downloaded/copied or rewritten. URL hashes are not file-byte hashes.

The actual `description_read` -> `_prodEnsureDescription` -> `_prodDescriptionHTML`
path returns native brief text and renders its original image/link URLs. Existing
`production-archive` rescue projection belongs to the separate archive reader;
its private Drive view links do not fix native inline rendering. This is a
current product dependency, not justification to restart exhaustive export.
Other hosts/fields, comments and embedded card feedback need bounded coverage
before any complete required-media claim. Per-client visibility remains distinct
from active-client membership.

## Minimum reserved TEST window

Use the exact final browser/functions/SQL, owner-designated TEST identities,
private pre-state and provider denial scoped and verified to TEST. All cases
require authoritative receipt/status readback from a fresh context.

1. Kasper requests changes; actual editor sends SMM approval; separate SMM sends
   Kasper approval. Refuse one editor save, verify retained work and safe retry.
2. Existing anonymous Calendar and Samples links view media, approve, request
   changes and save notes/replies. Include retained old tab and mobile/keyboard
   reachability. No new401 or changed anonymous gate.
3. Submit and Create Post/append/fill preserve original request identities. Lose
   an accepted response and fail materialization: one acceptance, complete cards
   or explicitly conserved recoverable debt. Verify assignments/batches/reload.
4. Warm/cold populated and genuinely empty views, failed routing/read then
   recovery, stale responses and old Workload/F44/card aliases. No false-empty
   success, cross-client paint, provider attempt or new mirror intent.
5. Deliver a scoped new incident and recovery only after approval; prove primary
   and independent fallback, missing terminal receipt, stopped observer and
   unacknowledged-incident escalation. The two past screenshots need no repeat.

## Access extension prepared for review

Proposed bounded option: retain the existing monthly paid access for one further
billing period, targeting October15, without adding seats/features or changing
workspace permissions. This is a proposal; current invoice/renewal terms and the
ability to reverse pending cancellation are not yet verified in the account.
The cost must be shown before any billing approval; no amount is inferred from
the old subscription enum or current advertised annual price.

The account owner can inspect **Settings -> Administration -> Billing**. Linear's
[current billing documentation](https://linear.app/docs/billing-and-plans), checked
September7, says changes take effect at the subscription-period end; cancellation
does not delete records, but issue creation and member roles change on the free
plan. This does not guarantee the required account-specific API/media behavior.

Before a billing action, pin the displayed renewal/cost/end date and confirm it
supports the full72h-plus14d sequence. A hypothetical clean September7 product
cutoff would reach September24 at the earliest; later fixes move that date.
October15 is not a guarantee if readiness or observation slips. After explicit
approval, read back the new paid-through date and retain the confirmation. No
automatic re-cancellation or deletion is part of this packet.

## Independent alarm setup being prepared

Reuse existing protected GitHub receipts and one delivery observer with
SyncViewbot. A direct bot route can bypass n8n but still shares Slack failure.
The backup needs an independent scheduled receipt reader plus separately hosted
missed-check email. A second GitHub job cannot detect a common scheduler outage.
The owner need not supply server details or repeat existing Slack/TEST secrets.

A managed scheduled reader and [Healthchecks missed-ping email](https://healthchecks.io/docs/)
are concrete candidates, not configured services. Source preparation must require
complete pinned receipt evaluation before a healthy heartbeat; job success or
a liveness-only sentinel is insufficient. No duplicate delivery-enabled observer
without a shared notification lease. New incident acknowledgement and a backup
escalation still needs a concrete implementation. The owner selected a backup
email, recorded privately; no additional person was selected. Service configuration,
cost, recipient and scoped drills require later approval.
