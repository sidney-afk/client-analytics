# Graphics flip — status

**This file is a pointer, not the document.**

The maintained, living flip-status document is
**`docs/independence/GRAPHICS_FLIP_STATUS.md`**. Read and edit that one.

This copy was created 2026-08-05 to hold a single wave-1 enrollment blocker —
the Drive-artifact SMM-approval refusal (`asset_redirect_invalid`: the probe's
own derived `uc?export=download` URL failed `providerQuerySafe`, confirmed by
run `31034175188`). That blocker was fixed the same day (#1026, deployed
2026-08-05) and wave 1 was enrolled 2026-08-07 — but this file was never
updated, and the 2026-08-08 reset audit found the two copies telling different
stories: this one still opened with a blocker that no longer existed while the
independence copy carried the real state. Same silent-drift failure as the
CLIENT_LIFECYCLE_MAP mirror retired on 2026-07-19, resolved the same way: one
canonical copy, one stub.

Do not add content here. If you found this file through a link or a search,
follow the path above.
