# Final observed installation preparation

The current local preparation proof passed **48 pinned source entries, 55 transaction chunks and an exact independently saved target comparison**. It is not deployment or retirement authorization. The complete public evidence is [the final proof manifest](../independence/LINEAR_EXIT_OBSERVED_FULL_PIPELINE_20260913.json); raw catalog captures remain private.

Calibration `332df4aeaf7b48218006e705b9128ff7` derived the target from the explicitly captured observed 67-table baseline and the reviewed additive source plan. Fresh replay `e0b065b2a07b470681bab971ed8fca64` reconstructed a separate disposable baseline and matched that saved target. Both exited zero and stopped their servers. The final schema has 90 public application tables; 39 final public owner bodies are compared as raw hashes, with the complete catalog comparison and retirement dependency checker retained.

The fresh run terminated the actual installer backend after the final 62149 journal write and before COMMIT. A separate connection confirmed the backend was gone. The parent verified the exact 54-chunk prefix, unchanged prefix catalog, absence of the final owner's activation function and private fence, 90 ALWAYS maintenance guards, and refusal of another writer. A fresh worker resumed the same plan and database identity, completed chunk 55, matched the target, and finalized removal of all 90 maintenance guards. The unchanged retirement contract passed again in the committed final state.

The source ordering explicitly includes 62741, the narrow 190840 private-helper privilege correction, 181213, 183021 and then 62149. The ACL correction handles observed default function privileges; it does not rewrite historical owners. CR-containing function bodies are encoded before journal parsing so PostgreSQL retains exact bytes. No fingerprint normalization is used. The two calibration failures and two interruption-fixture failures remain listed as failures in the proof manifest.

## Review and owner-window limits

The plan SHA-256 is `3c000b76db5cf6dc31a90b61ad7dc7751d6ce02c74b6d40939dbbcccbe6acbfb`. The saved target SHA-256 is `f3db4b7cd0649800e4811d1c4b32cf3f37e5c2bf951d4b12d22009faf08aa28f`. The manifest pins both calibration and final replay source bytes. Only the selected final-owner interruption is exercised here; standalone journal and maintenance interruption evidence remains separately scoped.

This result does not refresh the hosted baseline, prove external worker shutdown, prove off-device custody, or authorize activation. The foundation/platform prerequisites, current configuration, private recovery custody and operator review remain explicit. Native activation and post-reopen business acceptance are covered by the separate 46-check switch proof, not inferred from schema installation. No hosted writes, deployment, provider request or retirement activation occurred in this lane.
