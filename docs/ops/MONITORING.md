# SyncView Monitoring Coverage

This page answers: what watches each critical SyncView sync edge, where alerts land, and how to
contain or roll it back. A one-step kill may stop new behavior; F27 and F51 prove that safe team
authority recovery and exact Edge Function rollback require additional evidence.

| Edge | Watcher | Cadence | Alert / evidence | Containment / rollback |
|---|---|---:|---|---|
| n8n Cloud monthly execution quota | `n8n execution quota watchdog` GitHub Action (`.github/workflows/n8n-execution-quota-watchdog.yml`) | Daily at 13:17 UTC; manual dry-run on demand | Reads the n8n Insights API for the current Guatemala calendar month against the configured 135,000-execution plan cap. Owner DM at 80% (108,000) and again at 90% (121,500), with used count, cap, and remaining headroom. Exact month/threshold cache keys suppress repeats; a marker is saved only after the matching owner-relay execution finishes successfully. Live mode is restricted to the default branch so branch-scoped caches cannot duplicate production alerts; feature branches may only dry-run. API/relay outages leave the Action red and unmarked so the next run retries. | Disable the GitHub Actions workflow. This stops monitoring only; it does not change n8n, SyncView, runtime flags, or authority. |
| Staff UI -> Supabase writes | Track A Edge Function guard suites, source tests, parity harnesses, and the TEST-scoped Calendar/Samples E2E nightlies | On PR + nightly | Failed GitHub Actions run; write failures are also recorded in the relevant event ledger | **F67 blocks flag removal/empty-list rollback:** those states and flag/EF failures can select unauthenticated n8n writers. Stop affected callers, fail visibly, and repair/revert the authenticated caller or EF. Re-enable a legacy fallback only after equivalent auth/scope and positive/negative TEST proof. |
| SyncView -> Linear outbound (B4, staged dark) | `linear-outbound` summary events, `SyncView Linear outbound drain` Actions workflow, reconciler v2's outbound lane, and n8n pager `qllIDZPkdNAPRj0b` | The Actions schedule requests every 10 min and qll actively dispatches every 15 min; every drain writes a summary even when mode is `off` | Pager DMs on any failed write, a growing backlog above 100, more than 50 writes in one run, any shadow-vs-actual mismatch, or no fresh summary for 90 min while normal mode is active. Summary fields: `counts.{enqueued,shadow_ok,written,failed,retried,echo_dropped,stale_dropped}`, `backlog`, and `alerts`. | Immediate containment: stop affected users; set `linear_outbound_enabled={"mode":"off"}` for the normal lane and `linear_legacy_parity_enabled={"enabled":false}` for parity when the incident is unknown/mixed (F58). A team's authority may return to `linear` only after FLIP_RUNBOOK R2's audited snapshot/classification/replay/discard and machine-read team zero (F27); a default-drainer green is not proof. Disable the Actions workflow or pager node `Trigger Outbound Drainer` to stop cadence only. |
| Write-UI D-28 soak (PR #812 controls live; broader caller reroutes still gated) | `Production write gateway TEST drill`, `Production write-UI shadow audit`, aggregate `deliverable_events`, and the staged qll soak watcher | Both GitHub Actions daily schedules are already live; only qll's soak-reader transform is staged pending deployment/readback | TEST drill: native create, status, comment, due and assignee via `production-write`, Linear reflection, exactly-once comment, no echo/foreign-write storm, flags unchanged, cleanup, and reconciler 0/0/0. Shadow audit: full-roster read-only classification, zero unexpected divergence/intent/repair, and unchanged flag/outbox high-water. Pager grace is 36 h after installation, then summaries older than 30 h or any data-integrity count page. | Disable only the relevant Actions workflow. Pager-only rollback restores the private qll pre-soak snapshot. D-29 never auto-flips authority: a data bug pages with the affected team, users stop, and the involved outbound lane(s) are disabled per FLIP R1 (F4 for parity; F2+F4 if unknown/mixed), then F27 governs any later authority reversal; cosmetic bugs are fixed in place. |
| Supabase -> staff UI render | Unit tests, visual/source tests, `prod-readonly-smoke.js`, the fully mocked `prod-write-gateway-browser.js`, and the Production polish gate/review packet | Fast lane on PR; interaction/heavy only after `main`, manual dispatch, and schedule | The locked live-read lane and fully mocked gateway lane are green, but F105 makes the aggregate red: current main interaction/heavy jobs fail on stale guard-mode assertions, and heavy failure skips review-packet/Argos generation. A green fast PR job is not render/write-capability readiness. | Repair/split the stale tests before go-live. Revert a defective frontend PR if appropriate; if a real native write already committed, contain/classify it rather than treating frontend rollback as data repair. |
| Linear -> calendar/sample cards | `Linear ⇄ SyncView status reconcile`, `Samples ⇄ Linear status reconcile`, and the n8n monitoring pager. Legacy receiver `MJbMZ789B5ExZz9x` is inactive/unpublished; its saved authority-gated graph is not serving traffic. | **No current webhook real-time lane.** Pager `qllIDZPkdNAPRj0b` dispatches both card reconcilers every 15 min with schedule-equivalent healing (`dry_run=false`, cap 15), then checks staleness every 15 min. Calendar's redundant n8n trigger and GitHub cron are disabled. Samples still also has its own `*/10` GitHub schedule, so both schedule and pager dispatches are present until F01 removes one cadence. | Reconciler run summaries and card event ledgers; pager alerts if a reconciler has no fresh queued/running dispatch and its latest completed run is older than 2h or not green. A go-live preflight must also machine-read the intended n8n topology: active state, active-version/node fingerprint, and last green execution. | Remove one Samples cadence while retaining one monitored healer. Restore a cadence only after current graph/trigger/credential/error-handler fingerprint, last-green proof, no-duplicate-cadence proof, owner approval, activation readback, and first green. Restoring a real-time lane requires a separate owner topology decision plus a repaired/published/drilled graph—not a blind publish of the current saved graph. |
| Linear -> deliverables mirror | `linear-inbound` webhooks plus `Linear ⇄ deliverables reconcile v2` and the n8n monitoring pager | Webhook real-time; GitHub cron is requested every 10 min but observed delivery may be roughly hourly; n8n pager dispatches v2 dry-run every 15 min and alerts if no summary is fresh within 90 min | Reconciler summary events; pager alerts on non-zero `repair_list_size` or `linkage_actionable`; `diff_count` pages only after two distinct consecutive non-zero v2 summaries or a growing non-zero sequence, while a single non-zero tick is logged quietly; unmapped state and unknown-assignee anomalies POST to the n8n Edge Alert Relay with identifier-only payloads | Set `linear_inbound_enabled` to `{"enabled":false}`; hard stop is disabling the two EF-bound Linear webhooks; disable the pager workflow to stop v2 dispatch/alerts |
| Linear -> dormant B1 history refresh | `B1 Linear Incremental Refresh` plus n8n monitoring pager `qllIDZPkdNAPRj0b` | GitHub cron requests every 30 min but may degrade to multi-hour gaps; the 15-min pager dispatches it through a 30-min gate and checks its summary freshness every tick | `deliverable_events` action `linear_incremental_refresh`; pager DMs when the latest summary is older than 90 min | Disable the pager node `Gate Incremental Refresh 30m` to stop only the reliable dispatch, or disable the GitHub workflow; disable the pager for the existing global kill switch |
| Card linkage slots | Reconciler v2 `linkage_actionable` metric and the linkage backfill plan | n8n pager dispatches v2 dry-run every 15 min; GitHub schedule remains a fallback | Summary event field `linkage_actionable`; classified raw residue is counted separately | Leave slots dormant or reverse the specific private-artifact slot fills if owner asks |
| Front-end render health | Calendar/Samples E2E nightlies, Production locked-state smoke, fully mocked native-write lane, and scheduled Production polish gate | Nightly/weekday schedule + on demand | F105: scheduled/main interaction and heavy lanes are currently red; heavy failure can prevent fresh review-packet/Argos artifacts. | Fix the test epoch before relying on this monitor; separately classify any already committed write. |

## Known monitoring and evidence gaps — go-live blockers

These are not covered by the green watchers above. A green pager or reconciler therefore cannot
waive them.

| Finding | Missing detector / evidence | Required closure |
|---|---|---|
| F47 — urgent-review delivery truth | No recipient-specific pending-age or receipt check catches a staff/SMM/Kasper urgent action that resolves no exact recipient while the UI reports **Sent**. | Persist resolved-recipient and delivery outcome, show an honest unresolved state, and alert when recipient-specific pending work exceeds the owner-approved SLA. |
| F48 — Editors-week exposure/completeness | No monitor proves the unauthenticated response is access-contained or complete across pagination; native parity also misses transitions and historical attribution. | Contain the endpoint first; then add role-scoped, fully paged parity assertions and page on proven omissions or foreign rows. |
| F13/F49 — backups and capacity | Current weekly n8n success can contain missing/empty objects because critical nodes continue and the builder substitutes `[]`; no manifest/hash/readback proves a restorable corpus. Repository tests also do not prove capacity/spend/PITR posture. | Build the independent D-1 exporter; emit expected-versus-actual per-object counts/bytes/hashes, validate upload readback, advance last-known-good only on complete success, alert outside n8n, capture Dashboard thresholds, and complete a timed scratch restore. |
| F50 — deliverable/card state divergence | Reconciler linkage counts do not detect a changed Production status whose linked Calendar/Samples review status remains stale. | Define the mapping or canonical reader, then add a cross-surface mismatch metric and TEST scenario before either team flips. |
| F51 — deployed-function provenance | A green deploy job does not attest every deployed Edge Function's exact source/artifact, commit, secret-independent configuration, or supported rollback object. | Emit and retain an all-function release manifest; independently fingerprint/read back every deployed function; prove one exact-artifact restore. |
| F52 — provider credential containment | A plaintext credential remains in a reachable workflow branch and retained versions, while complete consumer/access/scope/provider-usage evidence is absent. | Restrict access immediately; stage and TEST-prove the managed replacement for the known live branch; owner revoke/rotate; then finish the broad census while monitoring unknown-consumer failures. |
| F53 — Graphics artifact lifecycle | No monitor proves a designer attached/replaced the canonical deliverable file and that SMM/client surfaces see the same artifact. | Implement the protected artifact contract, then add assignment-to-delivery and replacement-visibility probes with audit evidence. |
| F54 — inactive-client work | Queue counters and write guards do not exclude inactive clients, so apparently healthy totals may include offboarded work. | Filter every queue reader and reject writes server-side; alert on any inactive-client row that is renderable, assigned, or mutated. |
| F55 — authority vocabulary split | No cross-consumer check detects a legacy `supabase` side that the browser rejects while backends normalize it to SyncView authority. | Constrain/migrate flag writes, remove the alias, and run an all-consumer contract probe; page and freeze forward changes on any invalid/partial/disagreeing parse. |
| F56 — operative-doc/state drift | No generated expiry proves the Pages SHA, authority flags, deployed functions, n8n graphs/last-green, schema, active reviewer/test wording, and design-kit resume route still describe one epoch. The visual oracle also lacks an independently pinned identity. | Generate one expiring preflight manifest consumed by the flip; lint unqualified active-doc claims and obsolete external build/publish/profile commands while exempting explicit tombstones/history; pin the artifact oracle independently so changing implementation plus oracle cannot manufacture parity. |
| F58 — lane-specific emergency stop | A normal-mode `off` readback does not prove the independent parity lane is disabled, and current summaries do not give an operator one explicit “no lane can write” verdict. | Expose/read both F2/F4 decisions and active-lane counts; drill the dual kill; page if an incident stop leaves either lane writable. |
| F60 — teardown restore proof | Monitoring shows runtime health, not whether every retired webhook/workflow/credential has an exact captured inverse that was successfully restored and read back. | Require an owner-signed per-action restore manifest + rehearsal event before the teardown gate can advance. |
| F61 — retired-epoch transition | No server control proves every stale tab/service/retry was rejected between final team-zero/export and retired-mode activation. | Add a server maintenance/cutoff or atomic high-water transition; page on any accepted cross-boundary write and require exact zero before resume. |
| F63 — operator SQL executability | Prose review does not prove a paste fence is one action, valid SQL, compare-and-set safe, or transactionally exercised. | Parse/lint every fence and run every action against an isolated TEST flag store; publish the resulting action/hash/readback manifest. |
| F65 — Samples finish audit history | No detector compares requested `kasper_finish_log` appends with stored/mirrored values. | Add schema/writer support and an append-equality monitor; alert on any successful response that omits or truncates the log. |
| F66 — shared alert-secret blast radius | Removing the shared project alert secret can silence both Linear-inbound anomalies and onboarding fallback notifications. | Add lane-specific controls/secrets and independent TEST heartbeats; never use shared-secret removal as scoped containment. |
| F67 — unauthenticated fallback writers | EF auth telemetry cannot see direct or routed writes to legacy n8n fallback webhooks. | Authenticate/scope or retire every fallback; run direct negative probes and page on any anonymous accepted write. |
| F68 — comment-import rollback dependencies | No current preflight detects cross-run self-FK descendants or post-import versions before deletion. | Keep rollback blocked; require a fresh dependency/version graph and assertion-bearing TEST/scratch rehearsal before documenting a command. |
| F103 — completed migration replay | No terminal completion ledger rejects a consumed import run ID; a historical zero-write rerun can later plan source-driven writes. | Keep the executed playbook non-runnable; reject consumed IDs before the first RPC and alert on any apply attempt against a completed/historical ID. |
| F104 — Calendar-v1 retirement | No identifier-free signal proves sticky `?v2=0`/fallback usage is zero, and current tests omit the v2 metadata reader and outage branches. | Keep the cleanup recipe quarantined; add opt-out/fallback telemetry and replacement-recovery probes before any owner-approved retirement. |
| F125 — Calendar read/write authority split | Sticky-off **or automatic REST-fallback** browsers read the Sheet while enrolled writes/reorders land only in Supabase; local optimistic/cache guards can hide the divergence until later or on another device. | Emit identifier-free mode/surface/fallback/build counts; force either Sheet-read+EF-write state read-only; detect source-versus-target revision divergence and page before data loss. TEST cache expiry, second device, create/edit/archive/reorder and stale whole-card fields. |
| F126 — Linear child/comment completeness | A green reader run does not prove every child/comment page was consumed; current `ok:true` can represent a first page or partial GraphQL envelope that later drives card/link/status writes. | Emit requested parent, pages, unique children and nested-source completeness only; reject incomplete/repeated-cursor/count-drift receipts and page on any attempted downstream mutation without `complete:true`. Synthetic 51/101-child and later-page-link probes must remain zero-write until complete. |
| F105 — Production aggregate test epoch | The required PR job runs only `fast`; interaction/heavy run after merge/schedule and are currently red on stale B2 picker assertions. Heavy failure skips current review artifacts. | Make long lanes required before merge for Production changes, split explicit locked/writable fixtures, require zero live mutation, and page/block on any aggregate lane or review-artifact failure. |
| F106 — Sales Intake caller authorization | The active privileged webhook has no caller-auth decision, and retained execution metadata currently contains no run from which authorized-versus-anonymous use can be measured. | Require an active individual principal before side effects; record only privacy-safe actor/action/outcome audit; alert on denied/replayed/over-limit requests; prove deployed negative cases without sending production paperwork. Treat missing history as unknown, not green. |
| F107 — Sales Intake completion/replay | The browser receives `ok` before email, while no receipt joins agreement, ledger, email, and retry state; preview-send trusts browser-returned identifiers/link values. | Emit one server-owned receipt/state timeline and age/failure/duplicate metrics; page on stuck or terminal failures; expose protected retry/status; TEST lost response, duplicate, stale preview, provider/email failure, and partial-commit recovery. |
| F115 — provider callback authenticity/correlation | Callback HTTP success does not mean the event had a valid native signature, correct account/mode/type/status, or a server-owned agreement/payment match; on-receipt 2xx can also hide downstream failure from the provider. | Record privacy-safe verified/rejected/replayed/correlation-failed counts by provider/reason and unique inbox age; page on any accepted-unverified event, repeated rejection or stuck verified event. Keep raw bodies and client identifiers out of alerts. |
| F116 — contract+payment gate/job integrity | Contact flags do not reveal whether a simultaneous pair lost its email dispatch or duplicate callbacks created more than one child; no reconciler owns “both true, not sent.” | Expose unique gate-job state/age and step receipts; page on both-verified-without-one-job, duplicate job/email claim, failed/stuck child, sent-without-contact/stage receipt, and reconciler repair. Drill synchronized races and every partial failure on TEST. |
| F109 — YouTube title approval integrity | No event joins approved title text/revision to a later generic `name` edit, so status/stamp can remain green after the approved content changes. | Persist approved revision/hash plus immutable material-edit event; alert/report approval-text mismatch; force renewed review or visibly age the approval per owner policy; TEST both editable roles and concurrent/offline paths. |
| F113 — media/caption approval revision binding | Approved Calendar/Samples components can leave the client queue while later media/caption edits retain the original approval stamp; URL equality also cannot detect a provider-side same-link replacement. | Record approved versus current per-component revision, privacy-safe stale-approval counts by surface/component, and immutable material-edit events. Page/block release on any stale approved revision; TEST queue re-entry, same-link replacement, races, offline retry, and second device. |
| F117 — legacy Samples client-link binding | Generic SXR client selection can diverge from the client/token that the old URL verified; ordinary endpoint health cannot detect a cross-client mount. | Emit privacy-safe expected-versus-mounted client-scope denial counts server-side; synthetic fresh/residual browser probes must prove the old URL fails closed or stays exactly bound, with Add-client absent and cross-client reads/writes denied. |
| F118 — public onboarding-media provenance | Availability checks prove only that an asset is public, not that its publication rights/retention are approved. | CI requires a provenance/rights classification for every tracked onboarding-media file and rejects additions or changed bytes without one; privacy/legal separately owns expiry/deletion review and F64 history/cache disposition. |
| F119 — TikTok Pilot compliance/eligibility | A green submit/status poll cannot prove manual privacy choice, required declarations, or eligible intended use. | Keep posting disabled; require source/UI tests for unselected privacy and declaration gates plus a dated provider-backed eligibility decision. Alert on any pilot post attempt while the gate is closed. |
| F121 — Kasper mobile subtab reachability | Backend health stays green while later max-content tabs sit outside the mobile viewport. | Add a fictional-data viewport smoke across every Kasper subtab that fails on page-wide horizontal overflow, off-screen active tab, hidden focus, or inaccessible touch navigation at supported breakpoints/orientations. |
| F122 — public Actions data distribution | Green safety/visual jobs currently publish B1 row-plan JSON, live Production visual/DOM bundles, or roster/diff identifiers in logs/summaries; artifact success/retention is the disclosure, not a health signal. | Publish only allowlisted aggregates; use fictional intercepted visual fixtures; canary-scan exact archives/stdout/summaries recursively; fail on any live host or sensitive key/value class. Monitor artifact names/count/retention and external Argos delivery, with zero unsafe producers required. |
| F123 — Project Central atomic version | A success response does not prove all three source tabs loaded or that a clear-and-reappend completed; no version/hash/restore receipt exists. | Emit authenticated request ID, expected/staged/validated/promoted version hashes and per-tab counts only; page on any failed stage or stale CAS. Never change the active version before complete validation; drill restore and concurrent saves. |
| F124 — client analytics source coverage | Scheduled workflow success cannot distinguish valid empty data from provider/state failure, skipped later clients, or partial platforms; the UI lacks freshness/degraded receipts. | Emit expected/attempted/succeeded/count/fetched-at/error-class by client/platform in protected telemetry and aggregate coverage alarms; keep last-good visible/stale and page on missing coverage or false-zero attempts. |
| F110 — onboarding capture versus completion | A 2xx proves only an intake row/capture, while credential import and unawaited provisioning have no joined job, step age, resume, or terminal receipt. Duplicate-row success can hide an incomplete client indefinitely. | Emit privacy-minimized counts/ages for captured, processing, complete, failed, and retrying jobs plus per-step receipts; page on stalled/failed work and on duplicate capture without a complete job. Prove every alert and resume path on TEST. |
| F111 — current-intake handoff | The replaced Notion workflow is active-labelled but reports no production trigger and zero retained executions, while the operative runbook told staff to await its DM. Silence cannot distinguish no clients from a dead handoff. | Make the SyncView standard/AI durable job the canonical intake signal; alert independently on capture-without-staff-acknowledgement; measure legacy use without identifiers and archive only after F60 restore/zero-use proof. |
| F112 — native card ownership/navigation | Native linkage fields can be populated while Calendar/Samples still render a Linear URL and no current-assignee chip; linkage-zero therefore does not prove the SMM handoff exists. | Emit identifier-free counts by surface/team/authority for native target resolved, correct detail route, assignee resolved/unassigned/inactive, and forbidden Linear fallback. Require zero flipped-team Linear routes and bounded projection freshness before handoff/B5. |

## Alert destinations

- n8n workflow failures do **not** yet have blanket alert coverage (F09). The central
  `SyncView - Error Alerts -> DM Sidney` handler exists, but a 2026-07-14 live settings sample found
  five of six load-bearing cutover workflows unwired; three of those had 135 error/crash/cancel
  records since Jul 7. The handler itself also failed 29 of 30 sampled invocations at the execution
  limit. Treat silence as unknown until a machine census proves every load-bearing active workflow
  points to a tested handler and the independent non-n8n pager is live.
- The n8n execution-quota Action runs outside n8n. Threshold notices reuse the active Edge Alert Relay, while a failed n8n read or relay confirmation leaves a failed GitHub run rather than a false green or a consumed monthly marker. Secrets are stored as `N8N_API_KEY` and `N8N_QUOTA_ALERT_WEBHOOK`; the plan cap is the repository variable `N8N_MONTHLY_EXECUTION_CAP`.
- Edge Function anomaly alerts use the project `SLACK_ALERT_WEBHOOK` secret and send public-safe messages only: anomaly type, Linear issue identifier, and team. They do not include client names, assignee names, emails, or raw payloads. `onboarding-capture` also consumes this secret, so removing it is not a lane-specific rollback (F66). n8n workflow `Tfhc3vebZyG6obOg` is the active Edge Alert Relay to DM Sidney; synthetic relay execution `219237` and real EF anomaly relay execution `219602` succeeded on 2026-07-08.
- n8n workflow `qllIDZPkdNAPRj0b` is the active Monitoring Pager + Reconciler Trigger. It runs every 15 minutes, dispatches `linear-deliverables-reconcile.yml` with `apply=false`, dispatches the calendar and samples reconcilers with `dry_run=false` and cap 15, dispatches the B1 incremental refresh through a 30-minute gate, and actively dispatches `linear-outbound-drain.yml`. It reads each outbound summary; because normal mode is `off` and parity is separately disabled, outbound-summary staleness paging is suppressed. It throttles each DM alert condition to at most once per hour.
- The write-UI soak watcher is repository-staged only. After the scheduled workflows merge, qll adds two
  aggregate readers and alerts on stale/failed write drills, echo or reconcile counts, unexpected
  shadow work, or a failed zero-write proof. Alert text contains only team labels, aggregate counts,
  and event handles. It does not contain client/project names or payload bodies.

## Nightly test boundaries

- Calendar and Samples nightlies are scoped to the private active TEST fixture and dummy/test rows; each probe cleans up after itself.
- The Production locked-state smoke may read live production data (including the bounded comment-read POST) but rejects live mutations. The write-gateway browser lane is fully intercepted and sends no live mutation. F105 means the aggregate gate is **not** green: interaction/heavy still encode the pre-write picker model, and heavy failure prevents fresh review artifacts. Do not describe either the aggregate gate or the shipped capability as inherently read-only.
- The production-write drill is service-authenticated, requires exactly one active `kind=test`
  client, and lets the gateway resolve its server-private TEST project allowlist. It introduces no
  new GitHub secrets. It uses the gateway's TEST override without changing
  `prod_authority` or `linear_outbound_enabled`, archives its disposable fixtures, and uploads only
  a public-safe aggregate. The shadow audit reads real-client rows but makes zero Linear, flag,
  outbox, or authoritative-row mutations; its row-level file stays in runner-temporary storage and
  is never uploaded.
