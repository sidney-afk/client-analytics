# SyncView Monitoring Coverage

This page answers: what watches each critical SyncView sync edge, where alerts land, and how to roll it back in one step.

| Edge | Watcher | Cadence | Alert / evidence | One-step rollback |
|---|---|---:|---|---|
| n8n Cloud monthly execution quota | `n8n execution quota watchdog` GitHub Action (`.github/workflows/n8n-execution-quota-watchdog.yml`) | Daily at 13:17 UTC; manual dry-run on demand | Reads the n8n Insights API for the current Guatemala calendar month against the configured 135,000-execution plan cap. Owner DM at 80% (108,000) and again at 90% (121,500), with used count, cap, and remaining headroom. Exact month/threshold cache keys suppress repeats; a marker is saved only after the matching owner-relay execution finishes successfully. Live mode is restricted to the default branch so branch-scoped caches cannot duplicate production alerts; feature branches may only dry-run. API/relay outages leave the Action red and unmarked so the next run retries. | Disable the GitHub Actions workflow. This stops monitoring only; it does not change n8n, SyncView, runtime flags, or authority. |
| Staff UI -> Supabase writes | Track A Edge Function guard suites, source tests, parity harnesses, and the TEST-scoped Calendar/Samples E2E nightlies | On PR + nightly | Failed GitHub Actions run; write failures are also recorded in the relevant event ledger | Remove the affected slug from the Track A runtime flag, or set the flag to `{"clients":[]}` |
| SyncView -> Linear outbound (B4, staged dark) | `linear-outbound` summary events, `SyncView Linear outbound drain` Actions workflow, reconciler v2's outbound lane, and n8n pager `qllIDZPkdNAPRj0b` | Actions requests every 10 min; pager requests every 15 min after the workflow lands on `main`; every drain writes a summary even when mode is `off` | Pager DMs on any failed write, a growing backlog above 100, more than 50 writes in one run, any shadow-vs-actual mismatch, or no fresh summary for 90 min while mode is active. Summary fields: `counts.{enqueued,shadow_ok,written,failed,retried,echo_dropped,stale_dropped}`, `backlog`, and `alerts`. | Global stop: set `linear_outbound_enabled={"mode":"off"}`. Team pause: set that team's `prod_authority` to `linear`; inbound stays live and queued rows remain durable. Disable the Actions workflow or pager node `Trigger Outbound Drainer` to stop cadence only. |
| Write-UI D-28 soak (schedules/pager staged; authority gates live) | `Production write gateway TEST drill`, `Production write-UI shadow audit`, aggregate `deliverable_events`, and the staged qll soak watcher | Daily schedules after owner merge; qll reads the latest aggregate every 15 min only after its post-merge transform | TEST drill: native create, status, comment, due and assignee via `production-write`, Linear reflection, exactly-once comment, no echo/foreign-write storm, flags unchanged, cleanup, and reconciler 0/0/0. Shadow audit: full-roster read-only classification, zero unexpected divergence/intent/repair, and unchanged flag/outbox high-water. Pager grace is 36 h after installation, then summaries older than 30 h or any data-integrity count page. | Disable only the relevant Actions workflow. Pager-only rollback restores the private qll pre-soak snapshot. D-29 never auto-flips a flag: a data bug pages with the affected team and the owner may pause only that team; cosmetic bugs are fixed in place. |
| Supabase -> staff UI render | Unit tests, visual/source tests, `prod-readonly-smoke.js`, and the Production polish gate/review packet for the Production preview | Fast safety lane on PR; complete parallel gate on relevant `main` pushes, manual dispatch, and scheduled weekdays | Failed GitHub Actions run; Production smoke/polish checks assert rows render, 0 console errors, 0 write-like requests, finished interactions, layout/a11y basics, and validated screenshot artifacts | Revert the frontend PR and let GitHub Pages redeploy |
| Linear -> calendar/sample cards | Existing n8n webhook `MJbMZ789B5ExZz9x` plus `Linear ⇄ SyncView status reconcile`, `Samples ⇄ Linear status reconcile`, and the n8n monitoring pager | Webhook real-time; pager `qllIDZPkdNAPRj0b` is the sole calendar-reconciler cadence and dispatches both card reconcilers every 15 min with schedule-equivalent healing (`dry_run=false`, cap 15), then checks staleness every 15 min. The redundant calendar n8n trigger and native GitHub cron are disabled. | Reconciler run summaries and card event ledgers; n8n `errorWorkflow` alerts DM Sidney; pager alerts if a reconciler has no fresh queued/running dispatch and its latest completed run is older than 2h or not green | Remove affected slugs from `calendar_upsert_ef_clients` / `sample_review_ef_clients`; for inbound fallback, keep n8n active. Re-enable `AkiFmromoDkmsh39` or restore the GitHub cron only if qll itself is unavailable; disable the pager workflow if it becomes noisy. |
| Linear -> deliverables mirror | `linear-inbound` webhooks plus `Linear ⇄ deliverables reconcile v2` and the n8n monitoring pager | Webhook real-time; GitHub cron is requested every 10 min but observed delivery may be roughly hourly; n8n pager dispatches v2 dry-run every 15 min and alerts if no summary is fresh within 90 min | Reconciler summary events; pager alerts on non-zero `repair_list_size` or `linkage_actionable`; `diff_count` pages only after two distinct consecutive non-zero v2 summaries or a growing non-zero sequence, while a single non-zero tick is logged quietly; unmapped state and unknown-assignee anomalies POST to the n8n Edge Alert Relay with identifier-only payloads | Set `linear_inbound_enabled` to `{"enabled":false}`; hard stop is disabling the two EF-bound Linear webhooks; disable the pager workflow to stop v2 dispatch/alerts |
| Linear -> dormant B1 history refresh | `B1 Linear Incremental Refresh` plus n8n monitoring pager `qllIDZPkdNAPRj0b` | GitHub cron requests every 30 min but may degrade to multi-hour gaps; the 15-min pager dispatches it through a 30-min gate and checks its summary freshness every tick | `deliverable_events` action `linear_incremental_refresh`; pager DMs when the latest summary is older than 90 min | Disable the pager node `Gate Incremental Refresh 30m` to stop only the reliable dispatch, or disable the GitHub workflow; disable the pager for the existing global kill switch |
| Card linkage slots | Reconciler v2 `linkage_actionable` metric and the linkage backfill plan | n8n pager dispatches v2 dry-run every 15 min; GitHub schedule remains a fallback | Summary event field `linkage_actionable`; classified raw residue is counted separately | Leave slots dormant or reverse the specific private-artifact slot fills if owner asks |
| Front-end render health | Calendar/Samples E2E nightlies, Production read-only smoke, and scheduled Production polish gate | Nightly/weekday schedule + on demand | Failed GitHub Actions run; screenshots/log artifacts stay in Actions or private local artifacts | Revert the frontend PR and let GitHub Pages redeploy |

## Alert destinations

- n8n workflow failures use the existing `SyncView - Error Alerts -> DM Sidney` wiring.
- The n8n execution-quota Action runs outside n8n. Threshold notices reuse the active Edge Alert Relay, while a failed n8n read or relay confirmation leaves a failed GitHub run rather than a false green or a consumed monthly marker. Secrets are stored as `N8N_API_KEY` and `N8N_QUOTA_ALERT_WEBHOOK`; the plan cap is the repository variable `N8N_MONTHLY_EXECUTION_CAP`.
- Edge Function anomaly alerts use the project `SLACK_ALERT_WEBHOOK` secret and send public-safe messages only: anomaly type, Linear issue identifier, and team. They do not include client names, assignee names, emails, or raw payloads. n8n workflow `Tfhc3vebZyG6obOg` is the active Edge Alert Relay to DM Sidney; synthetic relay execution `219237` and real EF anomaly relay execution `219602` succeeded on 2026-07-08.
- n8n workflow `qllIDZPkdNAPRj0b` is the active Monitoring Pager + Reconciler Trigger. It runs every 15 minutes, dispatches `linear-deliverables-reconcile.yml` with `apply=false`, dispatches the calendar and samples reconcilers with `dry_run=false` and cap 15, dispatches the B1 incremental refresh through a 30-minute gate, and has a staged dispatch for `linear-outbound-drain.yml` that becomes effective when this PR lands on `main`. It reads outbound summaries now; because global mode is `off`, pre-merge dispatch misses fail softly and stale paging is suppressed. It throttles each DM alert condition to at most once per hour.
- The write-UI soak watcher is repository-staged only. After the scheduled workflows merge, qll adds two
  aggregate readers and alerts on stale/failed write drills, echo or reconcile counts, unexpected
  shadow work, or a failed zero-write proof. Alert text contains only team labels, aggregate counts,
  and event handles. It does not contain client/project names or payload bodies.

## Nightly test boundaries

- Calendar and Samples nightlies are scoped to the `sidneylaruel` TEST client and dummy/test rows; each probe cleans up after itself.
- The Production preview smoke and polish gate are read-only. They serve `index.html`, open `?prod=1`, verify migrated rows render, assert zero write-like browser requests, and publish validated review artifacts for visual inspection.
- The production-write drill is service-authenticated, requires exactly one active `kind=test`
  client, and lets the gateway resolve its server-private TEST project allowlist. It introduces no
  new GitHub secrets. It uses the gateway's TEST override without changing
  `prod_authority` or `linear_outbound_enabled`, archives its disposable fixtures, and uploads only
  a public-safe aggregate. The shadow audit reads real-client rows but makes zero Linear, flag,
  outbox, or authoritative-row mutations; its row-level file stays in runner-temporary storage and
  is never uploaded.
