# Native label catalog and writes: default-disabled, uninstalled

This bounded G2 slice belongs to [the single go-live checklist](../independence/GO_LIVE_CHECKLIST.md). Base: 8514a83ed1a65145a3a51ffe52e5fcbb2976be31. Original cloud proposal892894e1b is preserved; [current evidence](../audits/2026-09-06-native-label-writes.md) distinguishes its failures from the corrected source. Observed main5ed005c32732f415ea1868951fb42a132ddf045a was not integrated.

**Implemented source:** authorized staff can read and save labels using one immutable, operator-attested catalog version without a provider request or new provider intent in that admitted path. Default mode remains provider. **Installation, real catalog completeness, serving, external zero-egress and release remain HELD/UNPROVEN.** No live/TEST/deploy/flag/n8n/credential/alert/billing action occurred. Frozen anonymous writer and auth files are unchanged.

## One owner and honest attestation

The original foundation migration retains production_label_catalog_versions. Manifest schema1 validates original UUID/name/color/description, explicit global/team applicability, groups, archives, distinct VIDEO/GRAPHICS mappings, archived-inclusive cursor chain, unique IDs, terminal page/count and bounds:50 pages,100 rows/page,5000 rows,5MiB. Unknown/incomplete metadata refuses before staging. A selected-label union is never treated as a catalog. Full metadata is retained before staff team filtering.

The new migration adds operator_attestation to this same owner. production_label_catalog_stage_attested creates a NEW immutable version with evidence; exact replay preserves it, changed manifest/evidence conflicts. An old unattested version cannot be promoted in place. Original stage/read_version/validate_selection remain validation-only; original activate/read_active still always refuse. The new capability/read/write API does not change those old contracts.

The service-only attestation binds original source digest, workspace fingerprint, team mapping, capture ID/count, archived-page/count-review booleans, reviewer subject/time, export-package digest and independent review-evidence digest. These are privileged operator assertions, NOT automatic export authentication. SQL verifies structure and equality, not whether a caller captured the entire workspace. provider_completeness_verified remains false; operator_attested:true has the narrower meaning above.

Before any real attestation the owner must independently verify the authenticated original export, archived-inclusive pagination/terminal/count evidence, workspace/team mapping, original UUIDs/metadata, and taxonomy stability or capture cutoff. A self-consistent truncated file or invented zero count is insufficient. A genuinely empty catalog needs the same complete zero-source evidence. Exports, descriptions, reviewer identities and raw errors stay private. No real attestation/import happened here.

## Capability and accepted-work contract

The migration seeds production_native_label_catalog with schema_version1, mode:provider, version_id:null, without overwriting an existing row. Native mode requires an exact catalog UUID; hold blocks fresh labels. The server capability RPC refuses missing/malformed installed configuration. Pre-install provider compatibility requires BOTH an explicitly missing RPC and an independently read exact error:null/data:null flag result. Failed/malformed reads never guess provider mode.

Current caller/row/team/role authorization remains before any receipt adoption. Current authority/F27 still guards all fresh writes and staff catalog reads. Clients cannot obtain the staff catalog. Native fresh writes require ordinary authorized admin/SMM staff; TEST/parity paths are not promoted. The picker sends catalog_version on save. A stale picker or bundle missing the version receives visible reload guidance without Linear fallback. Ordinary anonymous approval/comment/tweak transports are unchanged.

Fresh native SQL obtains the complete selected relation from the locked current deliverable, checks CAS and immutable catalog membership, then invokes existing production_deliverable_write. The original canonical UUID/fingerprint bytes remain unchanged; catalog version is a separate receipt marker. New active global/same-team labels are selectable; already-selected archived/foreign/absent UUIDs can remain or be removed. Missing legacy color uses the existing display fallback. Unrelated row fields and metadata remain intact. Clear and legitimate empty no-op receive one idempotent receipt; no provider issue ID is manufactured.

The capability row SHARE lock lasts through commit: a concurrent activation waits; a version changed before lock acquisition refuses. Existing F27 generation checks, CAS, event, journal and outbox identity remain atomic. A new native receipt is inserted directly as skipped with immutable _native_label_catalog_version and linear_result.native_labels:true. It is never briefly pending, never new provider debt and launches no drainer. Native receipt UPDATE/DELETE/TRUNCATE are guarded. Existing provider receipts are never reclassified; replay under catalog native/hold cannot restart their drainer. Independent schedulers and old debt still require the global cutoff/accounting gates.

After current caller/scope/operation authorization, exact native replay uses the original fingerprint/version before current authority/F27 and fresh catalog/CAS policy. It returns the current scoped row, conserving later human edits, with read_only:true and authority_source:accepted_native_receipt (not a current authority claim). The gateway makes no RPC/write/provider request in this adoption path; direct SQL adoption uses existing receipt locks without changing data. Catalog hold/version changes, authority flips and unavailable authority reads cannot invalidate that exact retained receipt. Fresh, mismatched and provider requests keep their existing admission gates. Changed actor/role/client/entity/payload still encounters the existing exact dedup checks. No second retirement epoch is created.

## Ordered installation and rollback holds

1. Keep this source uninstalled. Current clients/staff use serving code. This branch does not include later assignment/HTTP/cutoff work; final integration must preserve those branches, re-pin the manual gateway closure and run affected combined checks. A commit is not serving proof.
2. Extend authenticated recovery FIRST. Canonical history-v7 selects37 tables and excludes this catalog owner. Its packages remain unchanged. Introduce explicit versioned data/schema/ACL/trigger coverage for all objects below. Prove exact full catalog/attestation/native-receipt restoration, terminal status, enabled immutable guards and failure rollback with external egress disabled. Existing helpers must not silently disable new guards or claim coverage. Installation remains blocked until this succeeds.
3. Apply reviewed additive SQL only in a separately authorized window after exact installed schema/grant/F27 preflight. Default provider mode remains. Verify unchanged anonymous serving revisions independently; never redeploy frozen repository auth to gain this prerequisite. Abort on unexpected rejection, widened privileges, receipt rewriting or schema drift. Stage/attest only the independently verified private export.
4. Serve the compatible gateway before the browser version field, with native mode still off. This is a FUTURE manual production-write deploy. Prove current caller auth, provider controls, exact replay, stale bundles and failure guidance on that serving revision. Already-running provider requests and external jobs still need canonical cutoff/observation proof.
5. Authorize the exact native version only after canonical staff/client canaries, actual zero-provider-egress proof, old queue accounting, read-failure alerts and restore evidence. Staff see native selections or explicit retry/reload errors; anonymous client features retain existing writers. Missing evidence keeps activation held.

After future native acceptance, containment is mode:hold WITH the compatible gateway retained. Preserve every catalog/attestation version, event, journal and native/provider receipt. Never delete the flag to mimic pre-install absence, strip the marker, requeue a skipped receipt, disable guards or roll back to a gateway treating native work as provider debt. Returning to provider is a separate reviewed reconciliation. Withdrawing uninstalled source is reversible; deleting accepted data is not rollback. No installed restore point is claimed.

## Complete additional recovery inventory

| Object | Required coverage |
|---|---|
| production_label_catalog_versions | Eight columns: version_id UUID PK, schema_version, manifest JSONB, manifest_sha256, staged_at, staged_db_session, verification_state, operator_attestation JSONB. Existing checks/RLS, no policies/FK/sequence, no direct PUBLIC/anon/authenticated/service table grants, no prune. |
| Original foundation functions | production_label_catalog_immutable, production_label_catalog_check_manifest, production_label_catalog_stage, production_label_catalog_read_version, production_label_catalog_validate_selection, production_label_catalog_activate, production_label_catalog_read_active. Preserve exact definitions/ACL/search paths and held old APIs. |
| Original table triggers | production_label_catalog_no_change and production_label_catalog_no_truncate, including enabled state and narrowly owned restore handling. |
| New service-only RPCs | production_label_catalog_stage_attested(uuid,jsonb,jsonb), production_label_catalog_read_attested(uuid,text), production_label_catalog_capability(), production_labels_write(jsonb,jsonb). Preserve SECURITY DEFINER/search paths and exact EXECUTE grants. |
| New private trigger functions | production_native_label_receipt_guard(), production_native_label_truncate_guard(). No public/service EXECUTE. |
| Existing outbox new triggers | zzz_native_label_receipt_guard before INSERT/UPDATE/DELETE; zzz_native_label_truncate_guard before TRUNCATE. Prove F27/other-native coexistence, restored retention and no CASCADE. |
| Existing flag owner | production_native_label_catalog value in syncview_runtime_flags. Restoring evidence must not automatically activate a catalog. |

## Proof limits

[QA handoff](../../qa/native-label-catalog/README.md):44 actual-handler/model checks and40 actual-handler/disposable-SQL groups. Real SQL covers both teams, immutable attestation, complete empty catalog, selected history, clear/no-op, exact replay across catalog hold/version, original-cloud failures, malformed config, observed concurrent activation blocking, F27 generation race, authority-flip/missing/denied-read adoption, wrong-actor/new-request refusal, journal failure atomicity, active client403, private RPC denial and provider receipt conservation. HTTP fetch is fully substituted/refused; this is not deployed SDK/socket/serving evidence. The actual migration fixture includes explicit inherited platform prerequisites, not an installed full schema reconstruction. Local PostgreSQL16 and Node22 runtime warnings are disclosed. No live export/restore, full-suite or whole-product independence claim follows. Taxonomy create/rename management and reachable create_options dependencies remain outside this slice.
