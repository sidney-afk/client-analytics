# Native continuity recovery: explicit history-v9

SOURCE_ONLY, uninstalled. This extends the [v8 contract](RECOVERY_SCHEMA_V8.md) under the existing G0–G10 plan; it does not activate capture, writers, watchers or a recovery target.

## Exact selected data

The new authenticated `history-v9` format selects 42 tables: the unchanged v8 39 plus `public_intake_log` (generated-always bigint `id` and its sequence), `legacy_intake_native_triage` (text `payload_hash`), and `native_brief_media_occurrences` (UUID `id`, no sequence). Public intake provenance already existed and was omitted by older formats. V9 makes that omission explicit and includes it; earlier packages do not acquire this coverage retroactively.

Versions v3–v8 retain their names, authenticated identities and 14/21/33/35/37/39 table meanings. The scheduled default remains `legacy-v3`; schema rehearsal defaults to `history-v7`. V9 requires explicit selection. Older-format source and destructive-restore preflights refuse when either new triage or media owner exists, including empty tables. The preexisting public log alone does not change older-format behavior. Never bypass refusal or remove evidence to fit an older format.

`scripts/track-b-history-v9-backup-prerequisites.sql` checks exact primary keys, new key types, the public log identity, private runtime ACLs, the append-only media service role, selected foreign-key closure and retained immutable receipt validators. Distinct reviewed capture and scratch roles receive narrow privileges. A v9-only definer helper disables/re-enables user triggers on exactly 42 tables inside the data restore transaction; foreign keys remain enforced, and truncation uses RESTRICT. A failed COPY rolls back trigger state and data. This helper is not granted to runtime roles.

The existing authenticated schema engine reconstructs the complete public schema into an empty restricted target before selected data and sequences. Its callable restrictions, authentication, fingerprint and transaction/quarantine rules remain those of [v7](RECOVERY_SCHEMA_V7.md). Whole public schema capture does not imply all public table data is selected.

## Source closure and rehearsal

The F44 migration is pinned to `1faf9a28114124ac5631db4d7561c514e19615ad60806afdd902ba335d484831`, including the same-payload provider INSERT advisory fence. The media migration is the unchanged c8a schema, SHA-256 `f21774e72a70658a2f26f65d893ab4b5a4f1d8c89c5f50214be2944d57fe8ddc`. Its verified-occurrence identity excludes `source_updated_at`; changed status cannot justify a second object occurrence. The final positive continuation additionally applies the separately frozen comment migration `2026-09-07-native-comment-media.sql`, SHA-256 `6de5febbb7dda475e3f2e14eaff4615c9641b6bcd33ed7e14469cd66130cb508` (comment source eb93555). It adds audience/version binding to the same ledger and permits 100MiB comment video while retaining the 50MiB brief bound. The 42-table identity is unchanged. Subsequent schema changes require a new explicit pin and rehearsal.

Explicit `TRACK_B_RECOVERY_TEST_CORPUS=history-v9` adds the two migrations to the existing disposable schema/data rehearsal. A synthetic platform bucket fixture supplies the media migration's prerequisite. Actual gateway and SQL calls create one completed paired F44 intake, another pending capture and historical provider debt. The source retains multibyte/whitespace original envelopes, real public-log rows, separate public-origin and staff-completion actors, and a later edited current card. Synthetic pending/held/verified media ledger rows and the exact hash-only `native_brief_owner_deferred_v1` flag shape are included. These synthetic hashes do not attest external bytes or an actual owner decision.

The rehearsal compares exact PostgreSQL JSON-text row images across all 42 owners and every public sequence after restricted schema reconstruction, then exercises the distinct data-only restore. Restored F44 completion must reuse the original native identity and retain later human edits; pending and historical holds stay visible; old provider UPDATE/other-team INSERT admission stays fenced. Private reads and media mutation privileges refuse. A timestamp-only changed verified occurrence must still violate the unique content identity. Prior card, label, comment, cutoff, rollback, wrong-authentication and committed-but-unverified quarantine controls remain in the same focused lane.

The original full rehearsal reached its 15-minute process deadline after both source phases, capture negative controls and positive package creation. It is **TIMED_OUT, not PASS**. Its retained source is the input to the separately authorized `scripts/track-b-native-continuity-continuation.js`: one fresh positive capture, restricted schema/data restoration and new-owner replay. It does not repeat the old matrix or reseed the accepted work. The continuation applies the frozen additive comment schema and adds a media occurrence bound to an actual canonical comment body, audience and version. HMAC and local role credentials, original seed, all packages and databases remain private. The standalone comment SQL test passed12 constraint refusals,4 ACL refusals and3 exact restored rows; that test is separate from combined recovery.

The positive continuation completed authenticated capture, restricted empty-schema reconstruction, all 42 row images and all sequence comparisons, and actual restored-owner replay before the host shut down. Its own final report is absent, so it is not relabeled as a completed full run. A separately authorized cold resume authenticated the saved package and exact retained source, verified the target structure/runtime ACLs/trigger helper, found the distinct restricted data restore already committed, checked all 42 original digests and the public intake sequence, then passed actual F44/media replay: **5 checks PASS**. It did not recapture or reconstruct again. [Sanitized evidence and source pins](../audits/2026-09-07-native-recovery-v9-evidence.json) preserve all three stages and the earlier failures. Node's existing experimental type-loader/module warnings are retained; no application warning is hidden.

This package covers the exact older source pins in that receipt, including the frozen comment schema. Later main naming/runtime integrations are excluded. The [separate named append correction](NATIVE_INTAKE_NAMED_APPEND.md) has an additional restored-target SQL proof, but does not retroactively change the authenticated package or claim final combined recovery.

## Separate media custody and recovery

The selected table contains object identity, source binding and verification evidence. It does **not** contain object bytes, storage bucket configuration, signed URLs or private owner-receipt bytes. Restoring it does not recover objects. Preserve a separately authenticated private object inventory and bytes bound to bucket/path/content digest, byte length and readback evidence, and independently preserve private source/owner receipts whose hashes occur in the table or `syncview_runtime_flags.native_brief_media.owner_deferred_references`. The flag itself is fully retained in v9 through the existing runtime-flags owner, including unrelated fields. The synthetic rehearsal verifies retention of that shape, not its authorization or runtime projection semantics.

Before release: review/install the exact schema closure and compatible capture/readers; configure explicit v9 capture with private custody and independent freshness observation; rehearse the actual installed schema and restore its separate media objects/receipt bytes into private storage; prove authenticated access and content readback against the recovered ledger. Keep writers held until those proofs and the installed serving/workflow bindings are reconciled. No schedule, bucket or flag is activated by this source change.

Before installation, revert preparation without data effects. After native acceptance, hold new admission and preserve all 42 owners, compatible schema, packages and separate bytes. Never restore an old provider worker to retry a native-owned receipt or reinterpret missing objects as successfully recovered. Reuse a failed target only with verified empty rollback; quarantine committed or unknown targets and keep diagnostics. Source withdrawal is not restoration of user work.
