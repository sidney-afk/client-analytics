# Historical prototype continuation — non-operative

> **QUARANTINED 2026-07-14 (F56/F64/D-17). NO COMMAND IN THE FORMER VERSION IS
> APPROVED FOR CURRENT USE.**

This filename remains only as a fail-safe for old links. The previous document was a July 5
prototype-session resume containing session-specific local paths, an external build/publish flow,
obsolete tester commands, and saved-provider-session guidance. It is not a source of current
runtime, deployment, rollback, or test truth. Git history retains the record for private
archaeology; do not copy commands from it.

Resume current work through [`README.md`](README.md). Use the repository application,
[`WIRED-PARITY.md`](WIRED-PARITY.md), [`ADAPTER.md`](ADAPTER.md), and
[`tests/README.md`](tests/README.md). Runtime authority and go-live decisions come from current
truth/independence documents plus live readback, never from a prototype handoff.

The separate F64/D-17 provider-session containment remains open. Documentation quarantine is not
revocation or denial evidence.
