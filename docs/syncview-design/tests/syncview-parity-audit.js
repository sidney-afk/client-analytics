export const meta = {
  name: 'syncview-parity-audit',
  description: 'SyncView <-> Linear behavioral re-audit: 5 parallel agents drive every surface via Playwright, find interaction divergences vs real Linear, and verify prior fixes held. Returns {divergences, regressions, regressionsChecked}.',
  whenToUse: 'Re-verify SyncView behavioral parity vs Linear or hunt remaining interaction divergences. Run `node build.js` first (audit reads out/_sv.html + out/syncview-app.src.html).',
  phases: [{ title: 'Re-audit', detail: '5 parallel surface agents verify fixes held + hunt missed/regressed/deeper behaviors' }],
}

const SCHEMA = {
  type: 'object', additionalProperties: false,
  properties: {
    surface: { type: 'string' },
    divergences: { type: 'array', items: { type: 'object', additionalProperties: false,
      properties: { element: { type: 'string' }, currentBehavior: { type: 'string' }, linearBehavior: { type: 'string' }, severity: { type: 'string', enum: ['high', 'med', 'low'] }, kind: { type: 'string', enum: ['missed', 'regression', 'deeper'] }, fixHint: { type: 'string' } },
      required: ['element', 'currentBehavior', 'linearBehavior', 'severity', 'kind', 'fixHint'] } },
    regressionsChecked: { type: 'array', items: { type: 'string' }, description: 'previously-fixed behaviors re-verified still working' },
  }, required: ['surface', 'divergences', 'regressionsChecked'],
}

const COMMON = `SyncView = single-file vanilla-JS Linear-clone. TEST: file:///C:/Users/Sidney/linear-design-probe/out/_sv.html . SOURCE (read): C:/Users/Sidney/linear-design-probe/out/syncview-app.src.html . Driver ref: C:/Users/Sidney/linear-design-probe/sweep.js . Playwright installed under C:/Users/Sidney/linear-design-probe (cd there; node -e "const {chromium}=require('playwright');..."; headless; viewport 1440x900; page.goto file:// then waitForSelector('.row')). App: global S state, render() rebuilds #app, overlays in #layer (openSearch()=⌘K palette, openStatusMenu/openAssignMenu/openDueMenu/openPPick=pickers, clearLayer()). It's a DELIBERATELY SIMPLER SKELETON (NO priority/labels/cycles/inbox/triage-nav/views/manual-new-issue/drag-drop-required) — do NOT flag those omissions.

ALREADY FIXED across recent batches (VERIFY these still WORK & didn't regress; do NOT re-report as new gaps): list client-chip→navigates to the CLIENT/PROJECT PROFILE (project detail page, not a filter) — same for the breadcrumb project crumb; due-pill→opens date picker; assignee-avatar→assignee picker; status-icon→status picker; live sub-issue progress count; sub-issue rows have inline status/due/assignee; ⌘K + search-icon → command palette (openSearch) over issues/projects/people; team-scoped project boards (Video≠Graphics≠Workspace); star = real favorite toggle (fills gold); group-header chevron collapses on click; project card status-ring → project-status picker; project-detail Status/Lead/Target rows → pickers; project-issue-rows inline status/due/assignee; tab order All issues-first; brand SyncView click → workspace menu; project count from real list; My Issues populated (3 issues assigned to "You"/sl); DELETE (ctx-menu or ⌘/Ctrl+Backspace) now shows an UNDO toast (clickable "Undo" + ⌘/Ctrl+Z restores the deleted issues to their original positions); BOARD KEYBOARD NAVIGATION (on a projects board with nothing open: j/ArrowDown & k/ArrowUp move a focus ring within a column, ArrowLeft/ArrowRight move between columns, Enter opens the focused card, and s→status / a→lead / ⇧D→target open the focused card's picker); list j/k focus ring + Enter-opens + s/a/⇧D/⇧P bulk pickers on selected/focused rows; Cmd/Ctrl+click a row TOGGLES multi-select (no navigate); Shift+click ANYWHERE on a row (incl. over a status/due/avatar/chip glyph) RANGE-selects (only a PLAIN glyph click opens that glyph's picker); Shift+ArrowDown/Up (and Shift+j/k) EXTEND the selection from an anchor (grow/shrink); the detail ⋯→"Due date" submenu FLIPS to the left of the parent menu near the right edge (like Status/Assignee/Project — no overlap); a truncated row title shows the full title via a native title attribute on hover; a freshly-opened menu's first ArrowUp wraps to the LAST item; Escape dismisses the keyboard focus ring (S.focusCard/S.focusRow) after clearing any selection; the focus ring is CLEARED when switching views (sidebar nav / favorite-view / palette command/person) but PRESERVED across a detail open→back; Enter opens the focused-OR-hovered list row (not just the focus-ring row); Cmd/Ctrl+A selects ALL issues in the tab/filter even across COLLAPSED groups; the selection RECONCILES on tab-switch and filter add/remove/clear (no phantom "N selected" bar over a filtered-out set) and collapsing a group does NOT drop its selection; the detail breadcrumb issue title exposes its full text via a native title attr on hover; the ACTIVITY feed now logs system events made on the open issue — status/assignee/due/project changes and sub-adds appear as muted single-line ".act-sys" rows ("You changed status to In Progress · just now") interleaved chronologically with comments (do NOT flag Activity as comments-only anymore); clicking away (blur) from a comment editor DISCARDS the edit (Save button / Enter are the only commit paths — no silent commit); the detail-side Due property row tooltip shows the concrete date when set ("Due Aug 3 · overdue"); a leaf issue with no children shows a minimal "+ Add sub-issues" affordance instead of a "Sub-issues 0" counted header; the command palette wraps ArrowUp/Down (first↔last); selecting a palette PERSON keeps the current status tab (only widens to All if it would be empty); the DUE calendar month-nav ‹/› now moves the focused day into the browsed month (arrow keys continue from there); COMMENT delete shows an Undo toast (clickable Undo + Ctrl/Cmd+Z restore) like issue-delete; row/breadcrumb/board-card titles set a native title attribute ONLY when actually truncated (truncation-aware, not unconditional); the Filter/Display toolbar buttons are real toggles (click again closes, pressed .menu-open state while open); the multi-select action bar has inline Status/Assignee/Due quick-action buttons beside ⌘ Actions; property pickers (Status/Assignee/Project) show a "No results" row when the search filters everything out; the DUE picker keeps a typeable date input in the calendar (Custom) view; the comment composer shows a "⌘↵ to comment" hint; **BOARD CARD MULTI-SELECT** — on the projects board Cmd/Ctrl+click a card TOGGLES it into S.cardSel (no navigate), Shift+click RANGE-selects across the flattened board order, a hover/selected checkbox (.pcard-check) toggles it, plain click clears the selection and opens the card, selected cards show a checkbox + tinted .pcard-sel; a board bulk action bar ("N selected" + Status/Lead/Target quick buttons + clear) applies pstatus/plead/ptarget to ALL selected projects, board keyboard s/a/⇧D target the selection when non-empty, board `x` toggles the focused card into the selection, and Escape / view-switch clear S.cardSel; the ISSUE-DETAIL panel (.detail-main) preserves its scroll position across a property change AND restores it when you navigate back to a previously-scrolled issue (render() saves/restores .detail-main scrollTop in scrollMem, keyed by the issue); a sub-issue's status/due/assignee change made from the PARENT's sub-row now logs to that CHILD's own activity feed (applySub logs on the edited id regardless of which issue is open); the FILTER value submenu shows a "No results" row when the typed query matches nothing (same as the property pickers).

KNOWN ACCEPTED LIMITATIONS — do NOT report these (already triaged & intentionally deferred): (1) the ISSUE-DETAIL side Properties pickers (Status/Assignee/Due/Project) open downward and visually overlap the sibling property rows below; clicking a COVERED sibling row is swallowed by the open popover (the picker-switch works only for rows the popover does NOT cover) — this is a known layout limitation, flagged 5+ times, not a new finding. (2) skeleton reductions: no priority/labels/cycles/inbox/triage-nav/views/manual-new-issue; no block-level markdown; no sub-issue drag-reorder; no marquee/rubber-band drag-select; palette person cross-team edge cases.

YOUR TASK — RE-AUDIT your surface for THREE things (kind field): (1) "missed" — interaction gaps vs Linear the earlier audit did NOT cover; (2) "regression" — anything the fixes above NEWLY BROKE (test the fixed behaviors!); (3) "deeper" — subtler Linear interactions: keyboard shortcuts, multi-select + bulk actions, focus/scroll management, hover affordances, empty/error/loading states, escape/click-away behavior, edge cases (0 items, long text, self-reference). For each real divergence (NOT a skeleton omission): record {element, currentBehavior (observed via Playwright), linearBehavior, severity, kind, fixHint}. Also list regressionsChecked (fixed behaviors you re-verified still work). Actually CLICK/interact via Playwright to observe. Be rigorous but do NOT invent gaps for intentional skeleton reductions.`

phase('Re-audit')

const surfaces = [
  { key: 'list-row-clickables', prompt: `SURFACE: LIST ROW sub-element clicks + row keyboard/selection. RE-VERIFY (regression): client-chip→client nav, due-pill→date picker, avatar→assignee picker, status-icon→status picker each still intercept (NOT open the issue) and APPLY changes. Then hunt MISSED/DEEPER: does clicking a due pill's picker actually CHANGE the row's date live? multi-select (x or checkbox) then keyboard shortcuts (does pressing s/a/etc with rows selected open bulk pickers? Linear: yes)? hover states/tooltips still correct? clicking a chip when grouped-by-project? row with no due/no assignee? Report divergences + regressionsChecked.` },
  { key: 'list-chrome', prompt: `SURFACE: LIST CHROME + keyboard. RE-VERIFY (regression): tab order Active/Backlog/All issues + filtering; group-header CHEVRON now collapses on click (not steal by checkbox); filter menu + stackable pills; context menu actions (status/assignee/due/project/move/delete apply). Hunt MISSED/DEEPER: keyboard shortcuts (Linear c=create[skeleton-omit ok], but does Escape/arrow work in menus? does the filter search filter?), bulk action bar with multiple rows selected (do bulk status/assignee/delete work on MULTIPLE?), select-all via group checkbox then bulk op, empty-state text per tab/filter, does clicking away close menus. Report divergences + regressionsChecked.` },
  { key: 'detail-props-breadcrumb', prompt: `SURFACE: ISSUE DETAIL right-panel + breadcrumb + STAR. RE-VERIFY (regression): status/assignee/due/project pickers apply+rerender; STAR now toggles favorite (fills gold, toast, second click un-favs); breadcrumb parent/client nav; ⋯ menu; back. Hunt MISSED/DEEPER: does the due picker's calendar/typed-date still work? keyboard in pickers (Enter/Escape/arrows)? clicking the same property twice? the star state persist across nav away+back? focus trap in pickers? Report divergences + regressionsChecked.` },
  { key: 'detail-subissues-activity', prompt: `SURFACE: SUB-ISSUES + ACTIVITY/composer. RE-VERIFY (regression): sub-issue rows inline status/due/assignee pickers APPLY to the child + reflect LIVE in the parent list AND the parent progress badge (e.g. set a child to Posted → badge increments); add-sub; description edit; comment submit+persist. Hunt MISSED/DEEPER: does changing a child due via the inline pill update it in place without opening the child? comment composer keyboard (Enter submits, Shift+Enter newline?)? editing a comment/deleting it (Linear allows)? description markdown/links? multiple rapid sub-issue status changes? Report divergences + regressionsChecked.` },
  { key: 'board-sidebar-nav', prompt: `SURFACE: BOARD + SIDEBAR + PALETTE + brand. RE-VERIFY (regression): team-scoped boards differ (Video 11 / Graphics 1 / Workspace 12); project card status-ring → picker (not detail) + moves card; project-detail Status/Lead/Target pickers; ⌘K + search-icon palette (arrow/enter/esc, opens issue/project); brand click → workspace menu; My Issues populated; nav routing+history. RE-VERIFY (regression): BOARD KEYBOARD NAV — on the Video projects board with nothing open, dispatch keydown j/ArrowDown/k/ArrowUp (focus ring .pcard-kfocus moves within a column), ArrowLeft/ArrowRight (moves between columns), Enter (opens focused card → S.projectOpen), and s/a/⇧D (open the focused card's status/lead/target picker) — confirm these WORK and that list j/k did not start firing on the board. Hunt MISSED/DEEPER: palette — does clicking a person result filter correctly? does typing then Escape restore? board card drag between columns (present via HTML5 drag)? column collapse? project card ⋯? does the keyboard focus ring survive a re-render / does it scroll the focused card into view? does changing a project status in one place reflect on the board? Report divergences + regressionsChecked.` },
]

const results = await parallel(surfaces.map(s => () => agent(COMMON + '\n\n' + s.prompt, { label: 're-audit:' + s.key, phase: 'Re-audit', schema: SCHEMA })))
const clean = results.filter(Boolean)
const all = clean.flatMap(r => (r.divergences || []).map(d => ({ ...d, surface: r.surface })))
const sev = { high: 0, med: 1, low: 2 }
all.sort((a, b) => (sev[a.severity] ?? 3) - (sev[b.severity] ?? 3))
const regr = all.filter(d => d.kind === 'regression')
log(`Re-audit: ${all.length} divergences (${all.filter(d => d.severity === 'high').length} high, ${regr.length} regressions) across ${clean.length} surfaces`)
return { divergences: all, regressions: regr, regressionsChecked: clean.flatMap(r => r.regressionsChecked || []) }
