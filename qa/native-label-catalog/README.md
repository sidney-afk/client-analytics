# Native catalog foundation proof

Owning contract: [NATIVE_LABEL_CATALOG_FOUNDATION.md](../../docs/ops/NATIVE_LABEL_CATALOG_FOUNDATION.md). Pinned baseline: `ab6366136c03239965c97b050ab5cf7c9763a228`.

`node test/native-label-catalog-foundation.js` runs the structural holds and actual unchanged gateway with all fetches replaced. Set `NATIVE_LABEL_HANDLER_REPORT` to a private output JSON path for the full receipt. Node 22 is required for the existing TypeScript source; two runtime warnings are expected on 22.12. The gateway file must equal the pinned baseline byte for byte; later source integration needs a reviewed proof update, not deleting that assertion.

For real local PostgreSQL proof, use **your own disposable cluster** and a private JSON file with `host:"127.0.0.1"`, numeric `port`, `user:"postgres"`, a SCRAM password, `database:"native_labels_<unique_suffix>"`, and absolute `psql` / `createdb` executable paths. `NATIVE_LABEL_PG_CONFIG` points to that file; `NATIVE_LABEL_SQL_REPORT` points to a private result JSON. Run `node qa/native-label-catalog/sql-proof.js`. Never use a shared/installed/production cluster. The script creates one fresh database and the `anon`, `authenticated`, `service_role` LOGIN roles with random per-role passwords. Existing database/roles refuse rather than being silently reused or modified. It neither starts/stops a server nor removes databases or artifacts. Use a fresh owned cluster to replay the whole setup. SQL `PGSERVICE` indirection is removed and each connection uses the explicit loopback/config identity; passwords are not printed.

The SQL proof uses only this standalone new migration and real PostgreSQL roles. The handler proof uses actual gateway/auth/policy source with a named in-process RPC model; it does not apply existing business writers. Its successful modeled label write retains the applicable provider outbox. Its denied-provider read/write controls intentionally demonstrate that product independence is still held.

Local author evidence: **48 SQL checks + 20 actual-handler checks PASS** on 2026-09-05. Source hashes and exact clean head are recorded in the final handoff, not inferred from this count. The first setup attempts failed before schema proof because command-line/config handling was wrong; corrected isolated setup passed. Preserved private setup failures are not relabeled as product defects or successful SQL cases. No hosted CI, installed/serving proof, export authentication, native activation/commit, restore readiness or zero-egress cutover is claimed.
