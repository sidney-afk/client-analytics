# Historical content and asset coverage — 2026-09-05

**Outcome:** a bounded offline scanner and meaningful synthetic checks. Real
historical coverage remains **UNPROVEN** because no private snapshot was supplied.
Application code and serving systems are unchanged. This is a coverage handoff,
not asset rescue, an export, a restore drill, or a Linear shutdown recommendation.

## Source and serving provenance

Fetched remote `main` at **2026-09-05T06:37:17.7507365Z**:
`706359752e861969e6c68898daa26e29a2eb6edb`.
The coordinator's earlier `39f57cabbe6b0eae613aeab652213894e7c649f6` was not used
as the tested source. Isolated branch: `agent/linear-history-coverage-20260905`.

| Evidence | Source / boundary |
| --- | --- |
| `SOURCE_ONLY` reader trace | The fetched SHA above; `index.html` SHA-256 `8f64f648d4b92ac2147bd9ecf3c3f0747f4081331df275b8c10ff25e0f10c53a` |
| `OFFLINE_TEST` scanner | Every test invocation prints UTC start time, full checkout HEAD and SHA-256 for the scanner, tests, fixture, app, F34 helper and comment policy actually loaded. The accompanying verification receipt pins the final implementation bytes. |
| Serving Pages provenance | **NOT_TESTED**; no production page or asset was fetched in this task |
| Serving Edge Function revisions | **UNPROVEN**; `EF_DEPLOY_MANIFEST.md` describes deployment paths, not serving versions. `production-comments` and `production-archive` have pinned/manual workflows; thumbnail read has a push/manual workflow. None establishes today's deployed source. |
| `LIVE_READ` | No production data/assets read; repository fetch is source acquisition only |
| `LIVE_WRITE_DRILL` | Unavailable under this task's authorization |
| `ISOLATED_BROWSER` | NOT_TESTED; the tests execute pure helpers, not a browser or server |

The complete `AGENTS.md` frozen anonymous-client writer directive was read
before the source trace. Navigation used `REPO_MAP.md`, `docs/FIND_ANYTHING.md`,
`docs/truth/BRIEFING.md`, the testing map, `SYSTEM_MAP.md`, the F34/F42/F53 owning
register entries and F34 runbook. No writer change is recommended. The adjacent
draft PRs #1269, #1270 and #1274 were not edited or treated as product proof.

## Actual reader boundaries found in source

| Surface / reproduction | Expected contract | Actual source behavior / implication |
| --- | --- | --- |
| Calendar client, correctly linked card; `_prodCanonicalCommentGate` | Existing Notes remain usable | The protected canonical reader admits client `sxr` + `samples` only. Calendar's unverifiable client surface returns `linked:false` and retains card-backed history. Canonical comments alone cannot measure Calendar client history. |
| Samples client, unlinked or incomplete canonical coverage | Never silently replace existing visible history with less | `_prodProjectCanonicalCardComments` validates crosswalk, generation and coverage; `_sxrCommentsForView` uses legacy on an unlinked/retained verdict, holds linked reads until ready/client, then uses the canonical slot. A deliverable ID or empty HTTP response is not complete history proof. |
| Card Notes with internal root, client-labelled reply, resolved/deleted/hidden rows | Preserve audience and history semantics | Replies inherit the legacy root's audience; Kasper-role rows are hidden. Resolved roots and their replies are available through history. Deleted legacy and hidden rows do not render; canonical tombstones remain but body/attachments are removed by `publicComment`. Scanner keeps suppressed source references in a separate bucket. |
| Comment containing Markdown image and an attachment | Distinguish retained text from rendered/retrievable media | Calendar/Samples Notes escape body text and make attachment anchors. Markdown image text in a body is a necessary historical reference candidate, not proof of an inline image being painted. Production comment bodies keep link-only behavior; staff descriptions opt into images. |
| Staff archive repair detail; `production-archive` | Staff archive scope must not imply client access | Staff credential/role/team/active-roster gates, paged comments/refs, complete replacement map, certified rescue rewriting and explicit unavailable placeholders. Even `audience:client` here remains a staff-authenticated archive view. |
| Thumbnail compare; `thumbnail-revision-read` | Correct pair for the exact card/client under existing review access | Private revision bucket; exact card and principal checks, runtime client eligibility, selected changed/pending cycle and short-lived signed URLs. Availability is based on row state/path presence, not an object GET. A newer pending user cycle can suppress an older changed pair. Snapshot paths alone do not prove current comparison access. |

Relevant app entrypoints are `_calLoadCommentsField`, `_sxrMigrateShape`,
`_calCommentsForView`, `_prodRootAudienceClientRows`, `_prodCanonicalCardComment`,
`_calRenderCommentsModal`, `_sxrRenderCommentsModal`, `_calDeriveThumbInfo`, and
`_prodCanonicalCommentGate`. Calendar's title thread joins review only on an
engaged YouTube card; the inventory conservatively includes its stored references
without claiming current eligibility. Media cards also use Drive file/folder and
Frame-style preview/open behavior; no provider folder expansion ran.

## Coverage gap list and next concrete proof

All real counts below are **unknown**, not zero. Each gate requires separately
authorized work; none was performed here.

| Gap | Current evidence | Next exact read / export / restore proof |
| --- | --- | --- |
| G1: Calendar/Samples card history missing | `SOURCE_ONLY`; no supplied rows | Supply an existing private snapshot with full `calendar_posts` and `sample_reviews` pages, field lists, row counts and omissions, including video/graphic/caption/title threads and card media. Run this scanner without manufacturing missing fields. |
| G2: Canonical-to-card coverage and thread scope | Real pure helpers tested; current bindings/runtime unknown | Supply complete canonical rows plus scoped deliverable/card crosswalk evidence. Independently reconcile root/reply identities, audience and lifecycle per component. Then an authorized anonymous-client browser read must show the expected full active and resolved thread after reopen. |
| G3: Linear URLs and asset mappings | Synthetic discovery proven; no real F34 inventory/mappings supplied | Reconcile every occurrence against the existing F34 independently generated, externally pinned/HMAC-certified final inventory. Preserve duplicate locations. Reuse that lane; do not infer its missing denominator from SyncView discovery or create a competing export system. |
| G4: Independent storage vs client access | Receipt validation tested only with fiction | Supply occurrence-bound mapping plus independent content-hash/byte-count readback receipts. Then show the exact anonymous client can open the mapped content from the correct card, including refresh/reopen. A private Drive copy alone does not clear this. |
| G5: Signed URLs or inaccessible objects | Parser never infers expiry; failure observations tested synthetically | Supply exact URL/occurrence/principal/time/receipt observations. For thumbnail comparison, separately prove current protected issuance and both image bytes/visible pair. Old signed-URL failure is not proof the durable object is gone. |
| G6: Thumbnail revision objects and selection | Source contains private paths and a selected-cycle policy | Supply revision rows, complete object inventory/readback and exact card linkage; then read the selected comparison through the current reader and inspect the client-visible pair. An eventual restore rehearsal must restore rows **and** objects with policy and scope preserved. |
| G7: Unsupported bodies/derived references | Rich JSON/HTML, relative URLs, ID-only media, unknown asset-bearing fields and attachment overflow produce explicit gaps | Supply representative fictional format documentation and add extraction/projection fixtures with independent denominators. For folders, supply an authorized complete member inventory separately; do not treat a folder URL as its contents. |
| G8: Track-B package is not the client-history/asset backup universe | Source allowlist has 14 tables; see below | Compare an authenticated existing package manifest against required card/revision/mapping coverage. Before any future restore claim, prove a disposable restore of missing row stores and object bytes, including anonymous-client retrieval; package checksum success alone is insufficient. |

`scripts/f34-linear-asset-rescue.js` discovers `uploads.linear.app` occurrences
in deliverable briefs, complete archive JSON, canonical comment bodies and
attachments. It preserves source occurrences, including repeated URLs; its
default planner still reads live services, so it was **not executed**. Apply,
download, upload, configuration and rescue paths were not called. Its complete
inventory gate requires the independent certified final occurrence inventory;
discovery alone cannot certify completeness. Its upload regex is narrower than
a general Markdown parser and it does not scan Calendar/Samples card stores.

`scripts/track-b-backup.js` exports one transactional, allowlisted database dump,
packages it with checksums/HMAC, and verifies private Drive readback. The source
allowlist includes canonical comments, deliverables and `linear_archive`, but
**omits `calendar_posts`, `sample_reviews`, `thumbnail_media_revisions`,
`linear_archive_asset_refs`, and storage object bytes**. The package itself is
neither client retrieval proof nor a full asset restore. No backup command
(including freshness, which can notify/write a marker) was executed.

## Executed offline evidence

| Command / check | Result and exact scope |
| --- | --- |
| `node scripts/linear-history-coverage/test.js` | **30/30 PASS** after the bounded evidence correction below (initial suite: 25/25), Node 22.12.0 on Windows; source SHA-256 manifest in `verification.json`, with the checkout base separately recorded |
| Scanner CLI over emitted fictional fixture | Exit 0; 30 occurrences / 29 unique resources; no current retrieval or independence claim |
| `node test/run-all.js` (the `npm test` entrypoint) | **RED: 398/399 suite processes exit 0**, completed 2026-09-05T06:54:34.166Z against fetched-base application and test source. The one failure is `test/asset-access-any-team.js`, reproduced separately unchanged. |
| Failure reproduction: `node test/asset-access-any-team.js` | `ERR_UNSUPPORTED_ESM_URL_SCHEME` before assertions: its dynamic import receives a Windows absolute path instead of a file URL. This is a test-platform failure, not evidence that its product contract passed. No assertion or file was changed. |
| Disposable PostgreSQL lanes in the repository runner | **NOT_TESTED** locally (explicit skips without their disposable databases/binaries); suite process success does not turn those skips into execution proof |
| `node test/repo-map-sync.js` | 276 checks pass after the minimal map registration |
| `node test/truth-sync.js` | 526 checks pass |
| `git diff --check` and scope diff | Pass; no changes to application, functions, existing tests, rescue or backup scripts |

The full-suite subprocess inherited only operating-system path/temp variables,
not service credentials. Full raw unit logs remain outside tracked files. The
red suite is preserved; hosted Linux CI and its disposable PostgreSQL gate are
the next repository-validation gate, separate from historical asset coverage.

### Correction of two reviewed P2 evidence defects

The independent fictional reproduction ran at **2026-09-05T07:37:17.141Z** on
reviewed head `366a5460a5752ee40c113883d227cc268f9d39b6`. It supplied a valid
stored-copy mapping and an anonymous render receipt for **different bytes**.
Expected: contradictory/unproven mapped retrieval, with stored-copy evidence
preserved. Actual: `supplied_retrieval_and_render_observation`, while no mapped
timestamp/receipt provenance was retained. No live resource was fetched.

Five new regression groups ran against that unchanged scanner at
**2026-09-05T07:38:19.110Z**: the original 25 groups passed and all five new groups
failed. The correction changes only scanner evidence handling and its tests/docs:

- Positive mapped retrieval now requires the observed content hash to match
  the independently verified source/storage-readback pair. A different hash is
  `contradictory_content_observation`; missing/unverified storage is `unproven`.
  Another positive receipt or a self-asserted rendition flag cannot override a
  mismatch. Independently stored copies retain their storage classification.
- Each accepted mapped observation now appears in `mapped_observations`, with
  paired normalized timestamp, receipt digest, result, observed content hash,
  byte-binding verdict and supplied visual assertion. Success, failure and
  conflicting receipts remain separately auditable; raw URLs/content stay private.

All **30 groups passed** after the repair at **2026-09-05T07:38:55.281Z**. The
unchanged independent reproduction reran at **2026-09-05T07:38:56.229Z** against
the corrected module resolution and returned `contradictory_content_observation`
while retaining `independently_stored`. Its old provenance counters still read
the original-URL fields (and correctly remain zero); the new regression directly
verifies the separate mapped provenance records for successes/failures/conflicts.
The reproduction script itself was not changed. Exact source digests and final
execution receipt are retained in `verification.json`; final committed-head
validation is recorded in the draft PR handoff.

Remote main was refreshed at **2026-09-05T07:37:17.0050072Z** and observed at
`a4925097aad2be1d8b4710e56da1220a19c850c5`. The correction deliberately remains on
the reviewed PR source; that newer main is not claimed as tested or merged.
Serving provenance and the missing real-snapshot/restore gates remain unchanged.

The required existing unit runner was repeated for this correction, finishing
**2026-09-05T07:43:12.098Z** on reviewed-head application/existing-test bytes:
again **398/399 suite processes succeeded**, with the same unchanged Windows
`asset-access-any-team.js` import failure and explicit disposable PostgreSQL
skips. Map/truth checks again passed 276/276 and 526/526. The new scanner suite
is still an explicit offline command; a successful existing CI run does not
substitute for its 30 behavioral checks.

Run `node scripts/linear-history-coverage/test.js`. The suite exercises actual
app extraction/projection helpers and `production-comments/policy.mjs`; the
canonical gate's authority inputs are synthetic. It establishes no actual
server behavior, persistence, deployed policy, or visible browser result.

The fictional baseline has these **independently asserted** denominators:

| Category | Occurrences |
| --- | ---: |
| Card media URLs | 4 |
| Card text / creative-direction references | 2 |
| Comment body references (all scopes, including suppressed source) | 16 |
| Attachment references (all scopes) | 3 |
| Staff deliverable briefs | 1 |
| Staff archive references | 2 |
| Thumbnail object paths | 2 |
| **Total** | **30** |
| **Unique resources** | **29** |

Baseline classification: **26 provider-dependent, 4 unproven, 0 independently
stored, 0 inaccessible, 0 unsupported**. Those zeros describe only this complete
fictional baseline; mutations separately exercise all five classifications.
Internal, deleted and staff-archive references are never added to a claimed
client-visible total. Tests also cover source/principal/cross-client mismatch,
conflicting/missing mapping receipts, inaccessible mapped objects, observed
signed-URL failure, duplicate references and identities, malformed rows/bodies,
pagination omissions, unsupported future fields and output privacy.

## Scoped assurance freshness

Used the existing site-assurance skill and the pure functions in
`scripts/assurance-ledger-freshness.js` on the owning ledger, restricted to the
four relevant rows as of 2026-09-05. This is arithmetic on existing proof dates,
not a fresh product test or a run of the monitoring workflow.

| Existing ledger row | Last recorded proof | Age / window | Result |
| --- | --- | --- | --- |
| Client review links | 2026-07-20 | 47 / 7 days | EXPIRED |
| Client-visible media | 2026-07-14 | 53 / 7 days | EXPIRED; ledger says client-link half never separately proven |
| Calendar planning/staff | 2026-07-16 | 51 / 14 days | EXPIRED |
| Samples/SXR | 2026-07-18 | 49 / 14 days | EXPIRED |

The shared ledger is not refreshed by these offline tests. The bounded next gate
is G1: supply a source-stamped, completeness-declared existing private snapshot.
Then measure concrete real gaps before proposing any preservation or restore
work. Draft publication is review only; nothing is merged or deployed.
